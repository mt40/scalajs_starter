/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 14);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

function makeEmptyFunction(arg) {
  return function () {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
var emptyFunction = function emptyFunction() {};

emptyFunction.thatReturns = makeEmptyFunction;
emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
emptyFunction.thatReturnsNull = makeEmptyFunction(null);
emptyFunction.thatReturnsThis = function () {
  return this;
};
emptyFunction.thatReturnsArgument = function (arg) {
  return arg;
};

module.exports = emptyFunction;

/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};


/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyObject = {};

if (process.env.NODE_ENV !== 'production') {
  Object.freeze(emptyObject);
}

module.exports = emptyObject;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

if (process.env.NODE_ENV === 'production') {
  module.exports = __webpack_require__(17);
} else {
  module.exports = __webpack_require__(18);
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var validateFormat = function validateFormat(format) {};

if (process.env.NODE_ENV !== 'production') {
  validateFormat = function validateFormat(format) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  };
}

function invariant(condition, format, a, b, c, d, e, f) {
  validateFormat(format);

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(format.replace(/%s/g, function () {
        return args[argIndex++];
      }));
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
}

module.exports = invariant;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyFunction = __webpack_require__(1);

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if (process.env.NODE_ENV !== 'production') {
  var printWarning = function printWarning(format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  warning = function warning(condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }

    if (format.indexOf('Failed Composite propType: ') === 0) {
      return; // Ignore CompositeComponent proptype check.
    }

    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

module.exports = warning;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var printWarning = function() {};

if (process.env.NODE_ENV !== 'production') {
  var ReactPropTypesSecret = __webpack_require__(19);
  var loggedTypeFailures = {};

  printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (process.env.NODE_ENV !== 'production') {
    for (var typeSpecName in typeSpecs) {
      if (typeSpecs.hasOwnProperty(typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          if (typeof typeSpecs[typeSpecName] !== 'function') {
            var err = Error(
              (componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' +
              'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.'
            );
            err.name = 'Invariant Violation';
            throw err;
          }
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        if (error && !(error instanceof Error)) {
          printWarning(
            (componentName || 'React class') + ': type specification of ' +
            location + ' `' + typeSpecName + '` is invalid; the type checker ' +
            'function must return `null` or an `Error` but returned a ' + typeof error + '. ' +
            'You may have forgotten to pass an argument to the type checker ' +
            'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' +
            'shape all require an argument).'
          )

        }
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          printWarning(
            'Failed ' + location + ' type: ' + error.message + (stack != null ? stack : '')
          );
        }
      }
    }
  }
}

module.exports = checkPropTypes;

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

/**
 * Simple, lightweight module assisting with the detection and context of
 * Worker. Helps avoid circular dependencies and allows code to reason about
 * whether or not they are in a Worker, even if they never include the main
 * `ReactWorker` dependency.
 */
var ExecutionEnvironment = {

  canUseDOM: canUseDOM,

  canUseWorkers: typeof Worker !== 'undefined',

  canUseEventListeners: canUseDOM && !!(window.addEventListener || window.attachEvent),

  canUseViewport: canUseDOM && !!window.screen,

  isInWorker: !canUseDOM // For now, this is true - might change in the future.

};

module.exports = ExecutionEnvironment;

/***/ }),
/* 9 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var emptyFunction = __webpack_require__(1);

/**
 * Upstream version of event listener. Does not take into account specific
 * nature of platform.
 */
var EventListener = {
  /**
   * Listen to DOM events during the bubble phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  listen: function listen(target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, false);
      return {
        remove: function remove() {
          target.removeEventListener(eventType, callback, false);
        }
      };
    } else if (target.attachEvent) {
      target.attachEvent('on' + eventType, callback);
      return {
        remove: function remove() {
          target.detachEvent('on' + eventType, callback);
        }
      };
    }
  },

  /**
   * Listen to DOM events during the capture phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  capture: function capture(target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, true);
      return {
        remove: function remove() {
          target.removeEventListener(eventType, callback, true);
        }
      };
    } else {
      if (process.env.NODE_ENV !== 'production') {
        console.error('Attempted to listen to events during the capture phase on a ' + 'browser that does not support the capture phase. Your application ' + 'will not receive some events.');
      }
      return {
        remove: emptyFunction
      };
    }
  },

  registerDefault: function registerDefault() {}
};

module.exports = EventListener;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

/* eslint-disable fb-www/typeof-undefined */

/**
 * Same as document.activeElement but wraps in a try-catch block. In IE it is
 * not safe to call document.activeElement if there is nothing focused.
 *
 * The activeElement will be null only if the document or document body is not
 * yet defined.
 *
 * @param {?DOMDocument} doc Defaults to current document.
 * @return {?DOMElement}
 */
function getActiveElement(doc) /*?DOMElement*/{
  doc = doc || (typeof document !== 'undefined' ? document : undefined);
  if (typeof doc === 'undefined') {
    return null;
  }
  try {
    return doc.activeElement || doc.body;
  } catch (e) {
    return doc.body;
  }
}

module.exports = getActiveElement;

/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 * 
 */

/*eslint-disable no-self-compare */



var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * inlined Object.is polyfill to avoid requiring consumers ship their own
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
 */
function is(x, y) {
  // SameValue algorithm
  if (x === y) {
    // Steps 1-5, 7-10
    // Steps 6.b-6.e: +0 != -0
    // Added the nonzero y check to make Flow happy, but it is redundant
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    // Step 6.a: NaN == NaN
    return x !== x && y !== y;
  }
}

/**
 * Performs equality by iterating through keys on an object and returning false
 * when any key has values which are not strictly equal between the arguments.
 * Returns true when the values of all keys are strictly equal.
 */
function shallowEqual(objA, objB) {
  if (is(objA, objB)) {
    return true;
  }

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) {
    return false;
  }

  // Test for A's keys different from B.
  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwnProperty.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

module.exports = shallowEqual;

/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

var isTextNode = __webpack_require__(22);

/*eslint-disable no-bitwise */

/**
 * Checks if a given DOM node contains or is another DOM node.
 */
function containsNode(outerNode, innerNode) {
  if (!outerNode || !innerNode) {
    return false;
  } else if (outerNode === innerNode) {
    return true;
  } else if (isTextNode(outerNode)) {
    return false;
  } else if (isTextNode(innerNode)) {
    return containsNode(outerNode, innerNode.parentNode);
  } else if ('contains' in outerNode) {
    return outerNode.contains(innerNode);
  } else if (outerNode.compareDocumentPosition) {
    return !!(outerNode.compareDocumentPosition(innerNode) & 16);
  } else {
    return false;
  }
}

module.exports = containsNode;

/***/ }),
/* 13 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



/**
 * @param {DOMElement} node input/textarea to focus
 */

function focusNode(node) {
  // IE8 can throw "Can't move focus to the control because it is invisible,
  // not enabled, or of a type that does not accept the focus." for all kinds of
  // reasons that are too expensive and fragile to test.
  try {
    node.focus();
  } catch (e) {}
}

module.exports = focusNode;

/***/ }),
/* 14 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(15);


/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {
var d,aa="object"===typeof __ScalaJSEnv&&__ScalaJSEnv?__ScalaJSEnv:{},g="object"===typeof aa.global&&aa.global?aa.global:"object"===typeof global&&global&&global.Object===Object?global:this;aa.global=g;aa.exportsNamespace=exports;g.Object.freeze(aa);var ba={envInfo:aa,semantics:{asInstanceOfs:2,arrayIndexOutOfBounds:2,moduleInit:2,strictFloats:!1,productionMode:!0},assumingES6:!1,linkerVersion:"0.6.25",globalThis:this};g.Object.freeze(ba);g.Object.freeze(ba.semantics);
var ca=g.Math.imul||function(a,b){var c=a&65535,e=b&65535;return c*e+((a>>>16&65535)*e+c*(b>>>16&65535)<<16>>>0)|0},da=g.Math.clz32||function(a){if(0===a)return 32;var b=1;0===(a&4294901760)&&(a<<=16,b+=16);0===(a&4278190080)&&(a<<=8,b+=8);0===(a&4026531840)&&(a<<=4,b+=4);0===(a&3221225472)&&(a<<=2,b+=2);return b+(a>>31)},ea=0,fa=g.WeakMap?new g.WeakMap:null;function ga(a){return function(b,c){return!(!b||!b.$classData||b.$classData.ug!==c||b.$classData.tg!==a)}}
function ha(a){for(var b in a)return b}function ia(a,b){return new a.Lk(b)}function k(a,b){return ja(a,b,0)}function ja(a,b,c){var e=new a.Lk(b[c]);if(c<b.length-1){a=a.nh;c+=1;for(var f=e.e,h=0;h<f.length;h++)f[h]=ja(a,b,c)}return e}function ka(a){return void 0===a?"undefined":a.toString()}
function la(a){switch(typeof a){case "string":return ma(na);case "number":var b=a|0;return b===a?oa(b)?ma(pa):qa(b)?ma(ra):ma(sa):"number"===typeof a?ma(ta):ma(ua);case "boolean":return ma(va);case "undefined":return ma(wa);default:return null===a?a.KA():xa(a)?ma(ya):a&&a.$classData?ma(a.$classData):null}}function za(a,b){return a&&a.$classData||null===a?a.n(b):"number"===typeof a?"number"===typeof b&&(a===b?0!==a||1/a===1/b:a!==a&&b!==b):a===b}
function Aa(a){switch(typeof a){case "string":return Ba(Ca(),a);case "number":return Da(Ea(),a);case "boolean":return a?1231:1237;case "undefined":return 0;default:return a&&a.$classData||null===a?a.r():null===fa?42:Fa(a)}}function Ga(a){return"string"===typeof a?a.length|0:a.W()}function Ha(a,b,c){return"string"===typeof a?a.substring(b,c):a.Ol(b,c)}function Ia(a){return 2147483647<a?2147483647:-2147483648>a?-2147483648:a|0}
function Ja(a,b){var c=g.Object.getPrototypeOf,e=g.Object.getOwnPropertyDescriptor;for(a=c(a);null!==a;){var f=e(a,b);if(void 0!==f)return f;a=c(a)}}function Ka(a,b,c){a=Ja(a,c);if(void 0!==a)return c=a.get,void 0!==c?c.call(b):a.value}function La(a,b,c,e){a=Ja(a,c);if(void 0!==a&&(a=a.set,void 0!==a)){a.call(b,e);return}throw new g.TypeError("super has no setter '"+c+"'.");}function Ma(a){var b=[],c;for(c in a)b.push(c);return b}
function Na(a,b,c,e,f){a=a.e;c=c.e;if(a!==c||e<b||(b+f|0)<e)for(var h=0;h<f;h=h+1|0)c[e+h|0]=a[b+h|0];else for(h=f-1|0;0<=h;h=h-1|0)c[e+h|0]=a[b+h|0]}
var Fa=null!==fa?function(a){switch(typeof a){case "string":case "number":case "boolean":case "undefined":return Aa(a);default:if(null===a)return 0;var b=fa.get(a);void 0===b&&(ea=b=ea+1|0,fa.set(a,b));return b}}:function(a){if(a&&a.$classData){var b=a.$idHashCode$0;if(void 0!==b)return b;if(g.Object.isSealed(a))return 42;ea=b=ea+1|0;return a.$idHashCode$0=b}return null===a?0:Aa(a)};function oa(a){return"number"===typeof a&&a<<24>>24===a&&1/a!==1/-0}
function qa(a){return"number"===typeof a&&a<<16>>16===a&&1/a!==1/-0}function Oa(a){return"number"===typeof a&&(a|0)===a&&1/a!==1/-0}function Pa(a){return null===a?Qa().Ri:a}function Ra(){this.Dj=this.Lk=void 0;this.tg=this.nh=this.p=null;this.ug=0;this.Ul=null;this.Xi="";this.ld=this.Si=this.Ti=void 0;this.name="";this.isRawJSType=this.isArrayClass=this.isInterface=this.isPrimitive=!1;this.isInstance=void 0}
function Sa(a,b,c){var e=new Ra;e.p={};e.nh=null;e.Ul=a;e.Xi=b;e.ld=function(){return!1};e.name=c;e.isPrimitive=!0;e.isInstance=function(){return!1};return e}function l(a,b,c,e,f,h,m,q){var y=new Ra,N=ha(a);m=m||function(a){return!!(a&&a.$classData&&a.$classData.p[N])};q=q||function(a,b){return!!(a&&a.$classData&&a.$classData.ug===b&&a.$classData.tg.p[N])};y.Dj=h;y.p=e;y.Xi="L"+c+";";y.ld=q;y.name=c;y.isInterface=b;y.isRawJSType=!!f;y.isInstance=m;return y}
function Ta(a){function b(a){if("number"===typeof a){this.e=Array(a);for(var b=0;b<a;b++)this.e[b]=f}else this.e=a}var c=new Ra,e=a.Ul,f="longZero"==e?Qa().Ri:e;b.prototype=new n;b.prototype.constructor=b;b.prototype.$classData=c;var e="["+a.Xi,h=a.tg||a,m=a.ug+1;c.Lk=b;c.Dj=p;c.p={c:1,Nh:1,h:1};c.nh=a;c.tg=h;c.ug=m;c.Ul=null;c.Xi=e;c.Ti=void 0;c.Si=void 0;c.ld=void 0;c.name=e;c.isPrimitive=!1;c.isInterface=!1;c.isArrayClass=!0;c.isInstance=function(a){return h.ld(a,m)};return c}
function ma(a){if(!a.Ti){var b=new Ua;b.sb=a;a.Ti=b}return a.Ti}function r(a){a.Si||(a.Si=Ta(a));return a.Si}Ra.prototype.getFakeInstance=function(){return this===na?"some string":this===va?!1:this===pa||this===ra||this===sa||this===ta||this===ua?0:this===ya?Qa().Ri:this===wa?void 0:{$classData:this}};Ra.prototype.getSuperclass=function(){return this.Dj?ma(this.Dj):null};Ra.prototype.getComponentType=function(){return this.nh?ma(this.nh):null};
Ra.prototype.newArrayOfThisClass=function(a){for(var b=this,c=0;c<a.length;c++)b=r(b);return k(b,a)};var Va=Sa(!1,"Z","boolean"),Wa=Sa(0,"C","char"),Xa=Sa(0,"B","byte"),Ya=Sa(0,"S","short"),Za=Sa(0,"I","int"),$a=Sa("longZero","J","long"),ab=Sa(0,"F","float"),bb=Sa(0,"D","double"),cb=ga(Va);Va.ld=cb;var db=ga(Wa);Wa.ld=db;var eb=ga(Xa);Xa.ld=eb;var fb=ga(Ya);Ya.ld=fb;var gb=ga(Za);Za.ld=gb;var hb=ga($a);$a.ld=hb;var ib=ga(ab);ab.ld=ib;var jb=ga(bb);bb.ld=jb;var kb=__webpack_require__(4),lb=__webpack_require__(20);function mb(a,b){return t(new u,function(a,b){return function(f){return a.g(b.g(f))}}(a,b))}function nb(a,b){return t(new u,function(a,b){return function(f){return b.g(a.g(f))}}(a,b))}function pb(a){return a.vb().mountedImpure}function qb(a){var b=rb(),c=sb();return tb(b,ub(c,v(new w,function(a){return function(){return a.Lh}}(a)),vb().Kd),wb(sb(),v(new w,function(a){return function(){a.Lh=xb()}}(a))))}
function yb(a,b){return wb(sb(),v(new w,function(a,b){return function(){a.Lh=zb(new Ab,(new Bb).oa(b),a.Lh)}}(a,b)))}function Cb(a,b){Db();return Fb(function(a,b){return function(a,c){a.g(b.g(c))}}(a,b))}function Gb(a){Db();a.tz=Fb(function(){return function(a,c){var e=Pa(c);c=e.bb;e=e.ic;c=Hb(Qa(),c,e);a.g(c)}}(a));a.uz=Cb(a,t(new u,function(){return function(a){return a}}(a)))}function Ib(){}function n(){}n.prototype=Ib.prototype;Ib.prototype.b=function(){return this};
Ib.prototype.n=function(a){return this===a};Ib.prototype.m=function(){var a=Jb(la(this)),b=(+(this.r()>>>0)).toString(16);return a+"@"+b};Ib.prototype.r=function(){return Fa(this)};Ib.prototype.toString=function(){return this.m()};function Kb(a,b){if(a=a&&a.$classData){var c=a.ug||0;return!(c<b)&&(c>b||!a.tg.isPrimitive)}return!1}var p=l({c:0},!1,"java.lang.Object",{c:1},void 0,void 0,function(a){return null!==a},Kb);Ib.prototype.$classData=p;
function Lb(a,b){var c=a.pc();return b.Ga().We(c,Fb(function(){return function(a,b){return a.nb(b)}}(a)))}function Mb(a){var b=k(r(p),[a.e.length]);Na(a,0,b,0,a.e.length);return b}
function Nb(a,b,c){if(32>c)return a.Pa().e[31&b];if(1024>c)return a.v().e[31&(b>>>5|0)].e[31&b];if(32768>c)return a.G().e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];if(1048576>c)return a.Q().e[31&(b>>>15|0)].e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];if(33554432>c)return a.Ca().e[31&(b>>>20|0)].e[31&(b>>>15|0)].e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];if(1073741824>c)return a.gc().e[31&(b>>>25|0)].e[31&(b>>>20|0)].e[31&(b>>>15|0)].e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];throw(new Ob).b();}
function Pb(a,b,c,e){if(32<=e)if(1024>e)1===a.gb()&&(a.S(k(r(p),[32])),a.v().e[31&(b>>>5|0)]=a.Pa(),a.Nc(1+a.gb()|0)),a.la(k(r(p),[32]));else if(32768>e)2===a.gb()&&(a.ua(k(r(p),[32])),a.G().e[31&(b>>>10|0)]=a.v(),a.Nc(1+a.gb()|0)),a.S(a.G().e[31&(c>>>10|0)]),null===a.v()&&a.S(k(r(p),[32])),a.la(k(r(p),[32]));else if(1048576>e)3===a.gb()&&(a.Qa(k(r(p),[32])),a.Q().e[31&(b>>>15|0)]=a.G(),a.Nc(1+a.gb()|0)),a.ua(a.Q().e[31&(c>>>15|0)]),null===a.G()&&a.ua(k(r(p),[32])),a.S(a.G().e[31&(c>>>10|0)]),null===
a.v()&&a.S(k(r(p),[32])),a.la(k(r(p),[32]));else if(33554432>e)4===a.gb()&&(a.$b(k(r(p),[32])),a.Ca().e[31&(b>>>20|0)]=a.Q(),a.Nc(1+a.gb()|0)),a.Qa(a.Ca().e[31&(c>>>20|0)]),null===a.Q()&&a.Qa(k(r(p),[32])),a.ua(a.Q().e[31&(c>>>15|0)]),null===a.G()&&a.ua(k(r(p),[32])),a.S(a.G().e[31&(c>>>10|0)]),null===a.v()&&a.S(k(r(p),[32])),a.la(k(r(p),[32]));else if(1073741824>e)5===a.gb()&&(a.df(k(r(p),[32])),a.gc().e[31&(b>>>25|0)]=a.Ca(),a.Nc(1+a.gb()|0)),a.$b(a.gc().e[31&(c>>>25|0)]),null===a.Ca()&&a.$b(k(r(p),
[32])),a.Qa(a.Ca().e[31&(c>>>20|0)]),null===a.Q()&&a.Qa(k(r(p),[32])),a.ua(a.Q().e[31&(c>>>15|0)]),null===a.G()&&a.ua(k(r(p),[32])),a.S(a.G().e[31&(c>>>10|0)]),null===a.v()&&a.S(k(r(p),[32])),a.la(k(r(p),[32]));else throw(new Ob).b();}function Qb(a,b,c){var e=k(r(p),[32]);Na(a,b,e,c,32-(c>b?c:b)|0);return e}
function Rb(a,b,c){if(32<=c)if(1024>c)a.la(a.v().e[31&(b>>>5|0)]);else if(32768>c)a.S(a.G().e[31&(b>>>10|0)]),a.la(a.v().e[31&(b>>>5|0)]);else if(1048576>c)a.ua(a.Q().e[31&(b>>>15|0)]),a.S(a.G().e[31&(b>>>10|0)]),a.la(a.v().e[31&(b>>>5|0)]);else if(33554432>c)a.Qa(a.Ca().e[31&(b>>>20|0)]),a.ua(a.Q().e[31&(b>>>15|0)]),a.S(a.G().e[31&(b>>>10|0)]),a.la(a.v().e[31&(b>>>5|0)]);else if(1073741824>c)a.$b(a.gc().e[31&(b>>>25|0)]),a.Qa(a.Ca().e[31&(b>>>20|0)]),a.ua(a.Q().e[31&(b>>>15|0)]),a.S(a.G().e[31&(b>>>
10|0)]),a.la(a.v().e[31&(b>>>5|0)]);else throw(new Ob).b();}
function Tb(a,b){var c=-1+a.gb()|0;switch(c){case 5:a.df(Mb(a.gc()));a.$b(Mb(a.Ca()));a.Qa(Mb(a.Q()));a.ua(Mb(a.G()));a.S(Mb(a.v()));a.gc().e[31&(b>>>25|0)]=a.Ca();a.Ca().e[31&(b>>>20|0)]=a.Q();a.Q().e[31&(b>>>15|0)]=a.G();a.G().e[31&(b>>>10|0)]=a.v();a.v().e[31&(b>>>5|0)]=a.Pa();break;case 4:a.$b(Mb(a.Ca()));a.Qa(Mb(a.Q()));a.ua(Mb(a.G()));a.S(Mb(a.v()));a.Ca().e[31&(b>>>20|0)]=a.Q();a.Q().e[31&(b>>>15|0)]=a.G();a.G().e[31&(b>>>10|0)]=a.v();a.v().e[31&(b>>>5|0)]=a.Pa();break;case 3:a.Qa(Mb(a.Q()));
a.ua(Mb(a.G()));a.S(Mb(a.v()));a.Q().e[31&(b>>>15|0)]=a.G();a.G().e[31&(b>>>10|0)]=a.v();a.v().e[31&(b>>>5|0)]=a.Pa();break;case 2:a.ua(Mb(a.G()));a.S(Mb(a.v()));a.G().e[31&(b>>>10|0)]=a.v();a.v().e[31&(b>>>5|0)]=a.Pa();break;case 1:a.S(Mb(a.v()));a.v().e[31&(b>>>5|0)]=a.Pa();break;case 0:break;default:throw(new x).o(c);}}function Ub(a,b){var c=a.e[b];a.e[b]=null;return Mb(c)}
function Vb(a,b,c){a.Nc(c);c=-1+c|0;switch(c){case -1:break;case 0:a.la(b.Pa());break;case 1:a.S(b.v());a.la(b.Pa());break;case 2:a.ua(b.G());a.S(b.v());a.la(b.Pa());break;case 3:a.Qa(b.Q());a.ua(b.G());a.S(b.v());a.la(b.Pa());break;case 4:a.$b(b.Ca());a.Qa(b.Q());a.ua(b.G());a.S(b.v());a.la(b.Pa());break;case 5:a.df(b.gc());a.$b(b.Ca());a.Qa(b.Q());a.ua(b.G());a.S(b.v());a.la(b.Pa());break;default:throw(new x).o(c);}}var Wb=l({zp:0},!0,"scala.collection.mutable.HashEntry",{zp:1});
function Xb(){}Xb.prototype=new n;Xb.prototype.constructor=Xb;Xb.prototype.b=function(){return this};Xb.prototype.$classData=l({Yp:0},!1,"AppCSS$",{Yp:1,c:1});var Yb=void 0;function Zb(){this.Xo=this.Zd=null}Zb.prototype=new n;Zb.prototype.constructor=Zb;
Zb.prototype.b=function(){$b=this;var a=ac(),b;-1!==(a.F.indexOf("github")|0)?(bc||(bc=(new cc).b()),b=bc.Qo):b="";this.Zd=dc(ec(a),b);(new fc).b();a=(new gc).b();b=hc();var c=(new ic).i(""),c=jc(a,kc(lc(mc(),c.F)));b=nc(oc(b,c.g(v(new w,function(a,b){return function(){return pc(b,v(new w,function(){return function(){qc||(qc=(new rc).b());return qc.hh()}}(a)),t(new u,function(){return function(a){sc();return a.ek()}}(a)))}}(this,a)))));var c=tc(),e=uc();this.Xo=vc(b,wc(a,xc(c,e),t(new u,function(){return function(a){Ac();
return(new Pc).o(a)}}(this,a))));return this};Zb.prototype.hh=function(){Qc||(Qc=(new Rc).b());return Sc(Tc(this.Zd,this.Xo)).Mf.U()};Zb.prototype.$classData=l({Zp:0},!1,"AppRouter$",{Zp:1,c:1});var $b=void 0;function cc(){this.Qo=null}cc.prototype=new n;cc.prototype.constructor=cc;cc.prototype.b=function(){this.Qo="scalajs_starter";return this};cc.prototype.$classData=l({aq:0},!1,"Config$",{aq:1,c:1});var bc=void 0;function Uc(){}Uc.prototype=new n;Uc.prototype.constructor=Uc;Uc.prototype.b=function(){return this};
Uc.prototype.$classData=l({bq:0},!1,"ScalaJsApp$",{bq:1,c:1});var Vc=void 0;function Wc(){this.mh=null}Wc.prototype=new n;Wc.prototype.constructor=Wc;Wc.prototype.b=function(){Xc=this;Yc();sc();Zc();sc();$c||($c=(new ad).b());var a=bd().ti,a=[cd(a),(sc(),dd(),(new ed).el("Hello!"))],a=fd((new A).f(a));this.mh=Sc(gd("Title",hd(a)));return this};Wc.prototype.hh=function(){return this.mh.Mf.U()};Wc.prototype.$classData=l({cq:0},!1,"components.Title$",{cq:1,c:1});var Xc=void 0;
function id(){this.Y=null}id.prototype=new n;id.prototype.constructor=id;id.prototype.b=function(){jd=this;this.Y=kd(rb(),void 0);return this};function wb(a,b){return v(new w,function(a,b){return function(){b.U()}}(a,b))}function ub(a,b,c){return wb(sb(),v(new w,function(a,b,c){return function(){b.U().O(t(new u,function(a,b){return function(a){b.g(a).$.U()}}(a,c)))}}(a,b,c)))}id.prototype.$classData=l({eq:0},!1,"japgolly.scalajs.react.Callback$",{eq:1,c:1});var jd=void 0;
function sb(){jd||(jd=(new id).b());return jd}function Bb(){this.$=null}Bb.prototype=new n;Bb.prototype.constructor=Bb;Bb.prototype.oa=function(a){this.$=a;return this};Bb.prototype.n=function(a){rb();return a&&a.$classData&&a.$classData.p.bm?this.$===(null===a?null:a.$):!1};Bb.prototype.r=function(){return Fa(this.$)};Bb.prototype.$classData=l({bm:0},!1,"japgolly.scalajs.react.CallbackTo",{bm:1,c:1});function ld(){}ld.prototype=new n;ld.prototype.constructor=ld;ld.prototype.b=function(){return this};
function md(a,b){return b===sb().Y}function nd(a,b){return function(a){return function(){return a.U()}}(b)}function tb(a,b,c){return md(rb(),b)?c:v(new w,function(a,b,c){return function(){c.U();return b.U()}}(a,c,b))}function od(a,b,c){return v(new w,function(a,b,c){return function(){return c.g(b.U())}}(a,b,c))}function qd(a,b,c){return v(new w,function(a,b,c){return function(){return c.g(b.U()).$.U()}}(a,b,c))}function kd(a,b){return v(new w,function(a,b){return function(){return b}}(a,b))}
ld.prototype.$classData=l({fq:0},!1,"japgolly.scalajs.react.CallbackTo$",{fq:1,c:1});var rd=void 0;function rb(){rd||(rd=(new ld).b());return rd}function sd(){}sd.prototype=new n;sd.prototype.constructor=sd;function td(){}td.prototype=sd.prototype;function ud(){}ud.prototype=new n;ud.prototype.constructor=ud;ud.prototype.b=function(){return this};
function vd(){var a;Xd||(Xd=(new ud).b());a=Xd;Yd||(Yd=(new Zd).b());a=t(new u,function(a,c){return function(e){return $d(new ae,kb.createElement(e,c.s),t(new u,function(a,b,c){return function(a){a=a.pd;return kb.createElement(c,be(ce(),a,b.Ao.U()))}}(a,c,e)),void 0)}}(a,Yd.am));de||(de=(new ee).b());return fe(a)}ud.prototype.$classData=l({jq:0},!1,"japgolly.scalajs.react.CtorType$Summoner$",{jq:1,c:1});var Xd=void 0;function ge(){}ge.prototype=new n;ge.prototype.constructor=ge;ge.prototype.b=function(){return this};
ge.prototype.$classData=l({lq:0},!1,"japgolly.scalajs.react.component.Generic$",{lq:1,c:1});var he=void 0;function ie(){this.$d=null}ie.prototype=new n;ie.prototype.constructor=ie;ie.prototype.b=function(){je=this;ke||(ke=(new le).b());this.$d=ke;me();return this};ie.prototype.$classData=l({Hq:0},!1,"japgolly.scalajs.react.component.Scala$",{Hq:1,c:1});var je=void 0;function ne(){je||(je=(new ie).b())}function oe(){}oe.prototype=new n;oe.prototype.constructor=oe;oe.prototype.b=function(){return this};
oe.prototype.$classData=l({Mq:0},!1,"japgolly.scalajs.react.component.ScalaFn$",{Mq:1,c:1});var pe=void 0;function qe(){this.jo=null}qe.prototype=new n;qe.prototype.constructor=qe;qe.prototype.b=function(){re=this;this.jo=t(new u,function(){return function(){return se().Df}}(this));return this};
function te(a,b,c){ue();return ve(ue(),b,c).hn(t(new u,function(){return function(a){se();return{a:a}}}(a))).xo(t(new u,function(a){return function(b){return b.wo(t(new u,function(){return function(a){return a.a}}(a))).uo(t(new u,function(){return function(a){ne();return we(a)}}(a)))}}(a)))}qe.prototype.$classData=l({Pq:0},!1,"japgolly.scalajs.react.component.builder.Builder$",{Pq:1,c:1});var re=void 0;function xe(){re||(re=(new qe).b());return re}function ye(){this.aa=null}ye.prototype=new n;
ye.prototype.constructor=ye;function ze(a,b){return Ae(a,v(new w,function(a,b){return function(){return b.U()}}(a,b)))}ye.prototype.i=function(a){this.aa=a;return this};function Ae(a,b){return(new Be).Xf(a.aa,t(new u,function(a,b){return function(){se();return{a:b.U()}}}(a,b)))}ye.prototype.$classData=l({Qq:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step1",{Qq:1,c:1});function Be(){this.de=this.aa=null}Be.prototype=new n;Be.prototype.constructor=Be;
function Ce(a,b){var c=a.de,e=new De;e.aa=a.aa;e.de=c;e.Gf=b;return e}function Ee(a){return Ce(a,t(new u,function(){return function(){}}(a)))}Be.prototype.Xf=function(a,b){this.aa=a;this.de=b;return this};Be.prototype.$classData=l({Rq:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step2",{Rq:1,c:1});function De(){this.Gf=this.de=this.aa=null}De.prototype=new n;De.prototype.constructor=De;
function Fe(a,b){var c=new Ge,e=a.de,f=a.Gf,h=He(new Ie,B(),B(),B(),B(),B(),B(),B(),B());c.aa=a.aa;c.de=e;c.Gf=f;c.ge=b;c.Ld=h;return c}function Je(a,b){return Fe(a,t(new u,function(a,b){return function(a){a=(new Ke).Ra(a.I);return b.g(pb(a).Cc())}}(a,b)))}function Le(a,b){return Fe(a,t(new u,function(a,b){return function(){return b}}(a,b)))}De.prototype.$classData=l({Sq:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step3",{Sq:1,c:1});
function Ge(){this.Ld=this.ge=this.Gf=this.de=this.aa=null}Ge.prototype=new n;Ge.prototype.constructor=Ge;function Me(a){var b=kd(rb(),!1),b=t(new u,function(a,b){return function(){return(new Bb).oa(b)}}(a,b));return Ne(a,Oe(),b,Pe().Dn)}function Ne(a,b,c,e){b=Qe(a.Ld,b,c,e);c=new Ge;e=a.de;var f=a.Gf,h=a.ge;c.aa=a.aa;c.de=e;c.Gf=f;c.ge=h;c.Ld=b;return c}function Sc(a){var b=vd();a=Re(Se(),a);return te(xe(),a,b)}
Ge.prototype.$classData=l({Tq:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step4",{Tq:1,c:1});function le(){}le.prototype=new n;le.prototype.constructor=le;le.prototype.b=function(){return this};function gd(a,b){xe();a=(new ye).i(a);b=Le(Ee((xe(),(new Be).Xf(a.aa,xe().jo))),b);return Me(b)}le.prototype.$classData=l({Uq:0},!1,"japgolly.scalajs.react.component.builder.EntryPoint$",{Uq:1,c:1});var ke=void 0;function Te(){}Te.prototype=new n;Te.prototype.constructor=Te;d=Te.prototype;d.b=function(){return this};
d.hg=function(a){me();var b=Ue().rf(a);a=(new Ve).Ra(a);return We(0,"ComponentDidMount(props: "+b+", state: "+pb(a).Cc()+")")};d.rf=function(a){a=(new Ve).Ra(a);return pb(a).rd()};d.Nf=function(a,b){return b&&b.$classData&&b.$classData.p.em?(b=null===b?null:b.I,D(G(),a,b)):!1};d.$classData=l({Xq:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidMount$",{Xq:1,c:1});var Xe=void 0;function Ue(){Xe||(Xe=(new Te).b());return Xe}function Ye(){}Ye.prototype=new n;
Ye.prototype.constructor=Ye;d=Ye.prototype;d.b=function(){return this};d.hg=function(a){me();var b=Ze().rf(a);a=(new bf).Ra(a);return We(0,"ComponentWillMount(props: "+b+", state: "+pb(a).Cc()+")")};d.rf=function(a){a=(new bf).Ra(a);return pb(a).rd()};d.Nf=function(a,b){return b&&b.$classData&&b.$classData.p.fm?(b=null===b?null:b.I,D(G(),a,b)):!1};d.$classData=l({Zq:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillMount$",{Zq:1,c:1});var cf=void 0;
function Ze(){cf||(cf=(new Ye).b());return cf}function df(){}df.prototype=new n;df.prototype.constructor=df;d=df.prototype;d.b=function(){return this};d.hg=function(a){me();var b="ComponentWillUnmount(props: "+ef().rf(a)+", state: ";ef();a=(new ff).Ra(a);a=pb(a).Cc();return We(0,b+a+")")};d.rf=function(a){a=(new ff).Ra(a);return pb(a).rd()};d.Nf=function(a,b){return b&&b.$classData&&b.$classData.p.gm?(b=null===b?null:b.I,D(G(),a,b)):!1};
d.$classData=l({ar:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillUnmount$",{ar:1,c:1});var gf=void 0;function ef(){gf||(gf=(new df).b());return gf}function hf(){}hf.prototype=new n;hf.prototype.constructor=hf;d=hf.prototype;d.b=function(){return this};d.hg=function(a){me();var b=jf().rf(a);a=(new Ke).Ra(a);return We(0,"Render(props: "+b+", state: "+pb(a).Cc()+")")};d.rf=function(a){a=(new Ke).Ra(a);return pb(a).rd()};
d.Nf=function(a,b){return b&&b.$classData&&b.$classData.p.hm?(b=null===b?null:b.I,D(G(),a,b)):!1};d.$classData=l({cr:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$RenderScope$",{cr:1,c:1});var kf=void 0;function jf(){kf||(kf=(new hf).b());return kf}function lf(){this.Ym=this.$l=this.Yl=this.Tl=null}lf.prototype=new n;lf.prototype.constructor=lf;function mf(a,b,c){nf(a,function(a){return function(b,c){return a.Vi(this,b,c)}}(b),c)}
lf.prototype.b=function(){Of=this;var a=g.Object.setPrototypeOf,b=void 0===a?B():(new Pf).o(a);if(Qf(b))a=t(new u,function(){return function(a){return g.Object.getPrototypeOf(a)}}(this)),b=b.F;else{if(B()!==b)throw(new x).o(b);a=t(new u,function(){return function(a){return a.__proto__}}(this));b=function(a,b){Se();a.__proto__=b}}this.Tl=(new H).d(a,b);this.Yl=this.Tl.N;this.$l=this.Tl.P;this.Ym=kb.Component;return this};
function Rf(a,b){for(var c=0,e=b.length|0;c<e;){var f=b[c];f.configurable=!0;"value"in f&&(f.writable=!0);g.Object.defineProperty(a,f.key,f);c=1+c|0}}
function Re(a,b){var c=b.de,e=b.Gf,f=b.ge,h=(new Sf).o(null);h.x=function(a,b,c){return function(e){Se();var f;Se();f=Se().Yl.g(c.x).call(this,e);f=f instanceof g.Object||f instanceof g.Function?f:this;ue();var h=(new Tf).Ra(f);f.mountedImpure=(ne(),we(h));var h=f.mountedImpure,C=Uf().Yn;f.mountedPure=h.Vp(C);f.backend=b.g(f.mountedPure);f.state=a.g(e);return f}}(c,e,h);Vf(a,h.x,a.Ym);c=[];Wf("render",t(new u,function(a,b){return function(a){return b.g((new Ke).Ra(a)).Jj}}(a,f)),c);f=b.Ld.xe;f.l()||
(f=f.R(),mf("componentDidCatch",Xf(function(a,b){return function(a,c){b.g(Yf(a,c)).$.U()}}(a,f)),c));f=b.Ld.ye;f.l()||(f=f.R(),Wf("componentDidMount",t(new u,function(a,b){return function(a){b.g((new Ve).Ra(a)).$.U()}}(a,f)),c));f=b.Ld.ze;f.l()||(f=f.R(),mf("componentDidUpdate",Xf(function(a,b){return function(a,c,e){b.g((new Zf).Gh(a,c.a,e.a)).$.U()}}(a,f)),c));f=b.Ld.Ae;f.l()||(f=f.R(),Wf("componentWillMount",t(new u,function(a,b){return function(a){b.g((new bf).Ra(a)).$.U()}}(a,f)),c));f=b.Ld.Be;
f.l()||(f=f.R(),$f(Fb(function(a,b){return function(a,c){b.g(ag(a,c.a)).$.U()}}(a,f)),c));f=b.Ld.Ce;f.l()||(f=f.R(),Wf("componentWillUnmount",t(new u,function(a,b){return function(a){b.g((new ff).Ra(a)).$.U()}}(a,f)),c));f=b.Ld.De;f.l()||(f=f.R(),mf("componentWillUpdate",Xf(function(a,b){return function(a,c,e){b.g((new bg).Gh(a,c.a,e.a)).$.U()}}(a,f)),c));f=b.Ld.Pe;f.l()||(f=f.R(),mf("shouldComponentUpdate",Xf(function(a,b){return function(a,c,e){return!!b.g((new cg).Gh(a,c.a,e.a)).$.U()}}(a,f)),
c));Rf(h.x.prototype,c);dg||(dg=(new eg).b());a=dg.Ck(b.aa);a.l()||(a=a.R(),h.x.displayName=a);return h.x}function Wf(a,b,c){nf(a,function(a){return function(){return a.g(this)}}(b),c)}function nf(a,b,c){c.push({key:a,value:b})}function $f(a,b){nf("componentWillReceiveProps",function(a){return function(b){return fg(a,this,b)}}(a),b)}function Vf(a,b,c){b.prototype=g.Object.create(c.prototype,{constructor:{value:b,enumerable:!1,writable:!0,configurable:!0}});(0,a.$l)(b,c)}
lf.prototype.$classData=l({er:0},!1,"japgolly.scalajs.react.component.builder.ViaReactComponent$",{er:1,c:1});var Of=void 0;function Se(){Of||(Of=(new lf).b());return Of}function gg(a,b){return v(new w,function(a,b){return function(){var f=(new H).d(b,void 0);a.Hg=zb(new Ab,f,a.Hg);return(new Bb).oa(wb(sb(),v(new w,function(a,b){return function(){var c=a.Hg;hg();for(var e=(new ig).b();!c.l();){var f=c.D();f!==b!==!1&&jg(e,f);c=c.u()}a.Hg=kg(e)}}(a,f))))}}(a,b))}
function lg(a){return ub(sb(),v(new w,function(a){return function(){return a.Hg}}(a)),t(new u,function(a,c){return function(a){return(new Bb).oa(a.N.g(c).$)}}(a,void 0)))}function mg(){}mg.prototype=new n;mg.prototype.constructor=mg;mg.prototype.b=function(){return this};function ng(a,b,c,e){return og(pg(),b,t(new u,function(a,b){return function(c){c=b.g(c).$;return t(new u,function(a,b){return function(){return(new Bb).oa(b)}}(a,c))}}(a,c)),e)}
mg.prototype.$classData=l({fr:0},!1,"japgolly.scalajs.react.extra.EventListener$",{fr:1,c:1});var qg=void 0;function rg(){qg||(qg=(new mg).b());return qg}function sg(){}sg.prototype=new n;sg.prototype.constructor=sg;sg.prototype.b=function(){return this};
function og(a,b,c,e){return tg().Gd(t(new u,function(a,b,c,e,y){return function(N){var z=t(new u,function(a,b,c,e,f){return function(h){h=h.I;var m=(new Ve).Ra(h),q=e.g(pb(m)),m=(new Ve).Ra(h),y=function(a){return function(b){pg();a.g(b).$.U()}}(c.g(m.vb().mountedPure)),m=wb(sb(),v(new w,function(a,b,c,e,f){return function(){b.addEventListener(e,c,f)}}(a,q,y,b,f))),q=wb(sb(),v(new w,function(a,b,c,e,f){return function(){b.removeEventListener(e,c,f)}}(a,q,y,b,f))),y=rb();h=(new Ve).Ra(h);return(new Bb).oa(tb(y,
m,yb(h.vb().backend,q)))}}(a,b,c,e,y));return Ne(N,ug(),z,Pe().vg)}}(a,b,c,e,!1)))}sg.prototype.$classData=l({gr:0},!1,"japgolly.scalajs.react.extra.EventListener$OfEventType$",{gr:1,c:1});var vg=void 0;function pg(){vg||(vg=(new sg).b());return vg}function wg(){}wg.prototype=new n;wg.prototype.constructor=wg;wg.prototype.b=function(){return this};
function xg(a,b,c){return yg(a,b,t(new u,function(a,b){return function(c){return t(new u,function(a,b,c){return function(){return(new Bb).oa(b.g((new Ve).Ra(c)).$)}}(a,b,c.I))}}(a,c)))}
function yg(a,b,c){return tg().Gd(t(new u,function(a,b,c){return function(m){var q=t(new u,function(a,b,c){return function(e){e=e.I;var f=b.g(Ue().rf(e)),h=c.g((new Ve).Ra(e)),f=gg(f,h);e=(new Ve).Ra(e);e=t(new u,function(a,b){return function(a){return(new Bb).oa(yb(b,a.$))}}(a,e.vb().backend));return(new Bb).oa(qd(rb(),f,e))}}(a,b,c));return Ne(m,ug(),q,Pe().vg)}}(a,b,c)))}wg.prototype.$classData=l({hr:0},!1,"japgolly.scalajs.react.extra.Listenable$",{hr:1,c:1});var zg=void 0;function Ag(){}
Ag.prototype=new n;Ag.prototype.constructor=Ag;Ag.prototype.b=function(){return this};function tg(){Bg||(Bg=(new Ag).b());return t(new u,function(a){return function(b){var c=t(new u,function(){return function(a){a=(new ff).Ra(a.I);return(new Bb).oa(qb(a.vb().backend))}}(a));return Ne(b,Cg(),c,Pe().vg)}}(Bg))}Ag.prototype.$classData=l({ir:0},!1,"japgolly.scalajs.react.extra.OnUnmount$",{ir:1,c:1});var Bg=void 0;function Dg(){}Dg.prototype=new n;Dg.prototype.constructor=Dg;function Eg(){}
Eg.prototype=Dg.prototype;function ec(a){var b=a.Zj(a);return a.uj(Fg(Ca(),b,"/*$","/"))}function dc(a,b){return a.uj(""+a.Zj(a)+b)}function Gg(){}Gg.prototype=new n;Gg.prototype.constructor=Gg;function Hg(){}Hg.prototype=Gg.prototype;function Ig(a,b){a=Jg(a)?Kg(a.Gg,a.Ig):Kg(Ac().sc.Vb,a);var c=new Lg;c.Gg=a;c.Ig=b;return c}function Rc(){}Rc.prototype=new n;Rc.prototype.constructor=Rc;Rc.prototype.b=function(){return this};function Tc(a,b){return Mg(Qc,b,Ng(a,b))}
function Mg(a,b,c){var e=Je(Ce(ze((Yc(),(new ye).i("Router")),c.Pl),t(new u,function(){return function(){return(new Og).b()}}(a))),t(new u,function(a,b){return function(a){return fg(b.Lc.ge,b.ph,a)}}(a,c))),f=t(new u,function(a,b){return function(a){var c=a.I;a=b.Og;var e=B(),c=(new Ve).Ra(c);return(new Bb).oa(fg(a,e,pb(c).Cc().Hc).$)}}(a,b)),e=Ne(e,ug(),f,Pe().vg);b=t(new u,function(a,b){return function(a){return(new Bb).oa(fg(b.Og,(new Pf).o(a.yl.Hc),pb(a).Cc().Hc).$)}}(a,b));var h=Ne(e,Pg(),b,
Pe().vg);b=ng(rg(),"popstate",t(new u,function(a,b){return function(){return(new Bb).oa(b.ph.Kj())}}(a,c)),t(new u,function(){return function(){return Qg(Rg())}}(a)));zg||(zg=(new wg).b());b=[b,xg(zg,t(new u,function(a,b){return function(){return b}}(a,c)),t(new u,function(a,b){return function(c){c=c.I;return(new Bb).oa(qd(rb(),b.Pl,t(new u,function(a,b){return function(a){var c=(new Ve).Ra(b);return(new Bb).oa(c.Ug(a,sb().Y).$)}}(a,c))))}}(a,c)))];var e=b.length|0,f=0,m=h;a:for(;;){if(f!==e){h=1+
f|0;m=b[f].g(m);f=h;continue a}e=m;break}a=[-1!==(Qg(Rg()).navigator.userAgent.indexOf("Trident")|0)?ng(rg(),"hashchange",t(new u,function(a,b){return function(){return(new Bb).oa(b.ph.Kj())}}(a,c)),t(new u,function(){return function(){return Qg(Rg())}}(a))):vb().Kd];c=a.length|0;b=0;f=e;a:for(;;){if(b!==c){e=1+b|0;f=a[b].g(f);b=e;continue a}return f}}Rc.prototype.$classData=l({sr:0},!1,"japgolly.scalajs.react.extra.router.Router$",{sr:1,c:1});var Qc=void 0;function gc(){}gc.prototype=new n;
gc.prototype.constructor=gc;function Sg(a,b,c){return t(new u,function(a,b,c){return function(m){return(new Tg).Eh(t(new u,function(a,b){return function(a){a=a.F;a=Ug(new Vg,b.Ej,a,a.length|0);a=Wg(a)?b.wl.g(a):B();if(a.l())return B();a=a.R();Ac();return(new Pf).o((new Xg).o(a))}}(a,b)),t(new u,function(a,b,c){return function(a){return Yg(c,a,b)}}(a,c,t(new u,function(a,b){return function(a){return b.Ik.g(a)}}(a,b)))),Fb(function(a,b,c){return function(a,e){return Yg(c,e,b)}}(a,c,m)))}}(a,b,c))}
gc.prototype.b=function(){return this};function wc(a,b,c){return t(new u,function(a,b,c){return function(){return c.g(b)}}(a,b,c))}function Yg(a,b,c){b=c.g(b);return b.l()?B():(new Pf).o(a.g(b.R()))}function Zg(a,b){return t(new u,function(a,b){return function(){return b.U()}}(a,b))}function pc(a,b,c){return(new $g).zb(t(new u,function(a,b,c){return function(){return c.g(b.U())}}(a,b,c)))}
function jc(a,b){var c=tc(),e=I().fp;b=ah(b,t(new u,function(a,b){return function(){return b}}(b,c)),t(new u,function(){return function(){}}(b,e)));c=Sg(a,b,bh(new ch,dh(c)));return t(new u,function(a,b){return function(c){c=Zg(a,c);return b.g(c)}}(a,c))}gc.prototype.$classData=l({ur:0},!1,"japgolly.scalajs.react.extra.router.RouterConfigDsl",{ur:1,c:1});function fc(){}fc.prototype=new n;fc.prototype.constructor=fc;fc.prototype.b=function(){return this};
fc.prototype.$classData=l({wr:0},!1,"japgolly.scalajs.react.extra.router.RouterConfigDsl$BuildInterface",{wr:1,c:1});function eh(){}eh.prototype=new n;eh.prototype.constructor=eh;function fh(){}fh.prototype=eh.prototype;function gh(){this.Uo=this.To=null}gh.prototype=new n;gh.prototype.constructor=gh;gh.prototype.b=function(){hh=this;var a=(new ih).i("([-()\\[\\]{}+?*.$\\^|,:#\x3c!\\\\])");xb();this.To=jh(a.cb);a=(new ih).i("\\x08");xb();this.Uo=jh(a.cb);return this};
function kh(a){var b=hh;a=lh(Ug(new Vg,b.To.Ng,a,Ga(a)),"\\\\$1");return a=lh(Ug(new Vg,b.Uo.Ng,a,Ga(a)),"\\\\x08")}gh.prototype.$classData=l({Ar:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$",{Ar:1,c:1});var hh=void 0;function mh(){this.Gp=null}mh.prototype=new n;mh.prototype.constructor=mh;mh.prototype.b=function(){nh=this;this.Gp=(new Pf).o(void 0);lc(this,"/");return this};
function lc(a,b){var c=new oh;hh||(hh=(new gh).b());return ph(c,kh(b),0,t(new u,function(){return function(){return mc().Gp}}(a)),t(new u,function(a,b){return function(){return b}}(a,b)))}mh.prototype.$classData=l({Dr:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$RouteB$",{Dr:1,c:1});var nh=void 0;function mc(){nh||(nh=(new mh).b());return nh}function qh(){}qh.prototype=new n;qh.prototype.constructor=qh;function rh(){}rh.prototype=qh.prototype;
function ah(a,b,c){return a.Jo(t(new u,function(a,b){return function(a){return(new Pf).o(b.g(a))}}(a,b)),c)}function sh(){}sh.prototype=new n;sh.prototype.constructor=sh;sh.prototype.b=function(){return this};function th(a,b){return Fb(function(a,b,f){return function(a,c){var q=fg(b,a,c);return q.l()?fg(f,a,c):q.R()}}(uh,a,b))}sh.prototype.$classData=l({Gr:0},!1,"japgolly.scalajs.react.extra.router.package$OptionFn2Ext$",{Gr:1,c:1});var uh=void 0;function vh(){}vh.prototype=new n;
vh.prototype.constructor=vh;vh.prototype.b=function(){return this};function wh(a,b,c){return t(new u,function(a,b,c){return function(a){var e=b.g(a);return e.l()?c.g(a):e}}(a,b,c))}function xh(a,b,c){return t(new u,function(a,b,c){return function(a){var e=b.g(a);return e.l()?c.g(a):e.R()}}(a,b,c))}vh.prototype.$classData=l({Hr:0},!1,"japgolly.scalajs.react.extra.router.package$OptionFnExt$",{Hr:1,c:1});var yh=void 0;function zh(){yh||(yh=(new vh).b());return yh}function Ah(){this.Df=null}
Ah.prototype=new n;Ah.prototype.constructor=Ah;Ah.prototype.b=function(){Bh=this;this.Df=(se(),{a:void 0});return this};Ah.prototype.$classData=l({Ir:0},!1,"japgolly.scalajs.react.internal.Box$",{Ir:1,c:1});var Bh=void 0;function se(){Bh||(Bh=(new Ah).b());return Bh}function Ch(){}Ch.prototype=new n;Ch.prototype.constructor=Ch;function Dh(){}Dh.prototype=Ch.prototype;function Eh(){this.Jk=this.Vf=null}Eh.prototype=new n;Eh.prototype.constructor=Eh;
Eh.prototype.b=function(){Fh=this;this.Vf=(new Gh).b();this.Jk=(new Hh).b();return this};Eh.prototype.$classData=l({Kr:0},!1,"japgolly.scalajs.react.internal.Effect$",{Kr:1,c:1});var Fh=void 0;function Ih(){Fh||(Fh=(new Eh).b());return Fh}function Jh(){this.bk=this.be=null}Jh.prototype=new n;Jh.prototype.constructor=Jh;function Kh(){}Kh.prototype=Jh.prototype;Jh.prototype.gh=function(a){a=this.be.Nn(a);return this.bk.Ko(a)};Jh.prototype.pj=function(a,b){this.be=a;this.bk=b;return this};
Jh.prototype.$classData=l({tm:0},!1,"japgolly.scalajs.react.internal.Effect$Trans",{tm:1,c:1});function Lh(){this.Yn=this.Xk=null}Lh.prototype=new n;Lh.prototype.constructor=Lh;Lh.prototype.b=function(){Mh=this;Uf();var a=Ih().Vf;this.Xk=Nh(a);Uf();Ih();this.Yn=Oh(Uf(),Ih().Vf,Ih().Jk);Oh(Uf(),Ih().Jk,Ih().Vf);return this};function Oh(a,b,c){return null===(Uf(),null)?(new Jh).pj(b,c):Nh(b)}Lh.prototype.$classData=l({Nr:0},!1,"japgolly.scalajs.react.internal.Effect$Trans$",{Nr:1,c:1});var Mh=void 0;
function Uf(){Mh||(Mh=(new Lh).b());return Mh}function Ph(){}Ph.prototype=new n;Ph.prototype.constructor=Ph;Ph.prototype.b=function(){return this};function Qh(a){var b=Rh,c=Ma(a),c=(new Sh).f(c),c=Th(new Uh,c,0,c.Ib.length|0);return(new Vh).lb(c,t(new u,function(a,b){return function(a){try{var c=b[a]}catch(e){if(c=Wh(Xh(),e),null!==c)c=c.m();else throw e;}return(new H).d(a,c)}}(b,a)))}Ph.prototype.$classData=l({Pr:0},!1,"japgolly.scalajs.react.internal.JsUtil$",{Pr:1,c:1});var Rh=void 0;
function Yh(){this.pd=this.Tg=this.Bg=null}Yh.prototype=new n;Yh.prototype.constructor=Yh;function Zh(a,b,c){a.Bg=b;a.Tg=c;a.pd=t(new u,function(a){return function(b){return t(new u,function(a,b){return function(c){return a.Tg.g(b.g(a.Bg.g(c))).g(c)}}(a,b))}}(a));return a}Yh.prototype.$classData=l({Qr:0},!1,"japgolly.scalajs.react.internal.Lens",{Qr:1,c:1});function $h(){this.Vf=null}$h.prototype=new n;$h.prototype.constructor=$h;$h.prototype.b=function(){ai=this;this.Vf=bi(this);return this};
function bi(a){return Zh(new Yh,t(new u,function(){return function(a){return a}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(){return b}}(a,c))}}(a)))}$h.prototype.$classData=l({Rr:0},!1,"japgolly.scalajs.react.internal.Lens$",{Rr:1,c:1});var ai=void 0;function ci(){ai||(ai=(new $h).b());return ai}function di(){this.Cj=this.hf=null}di.prototype=new n;di.prototype.constructor=di;
di.prototype.$classData=l({Sr:0},!1,"japgolly.scalajs.react.internal.Profunctor$Ops",{Sr:1,c:1});function ei(){this.Ao=this.s=null}ei.prototype=new n;ei.prototype.constructor=ei;function fi(a,b,c,e){a.s=b;a.Ao=e;return a}ei.prototype.$classData=l({Ur:0},!1,"japgolly.scalajs.react.internal.Singleton",{Ur:1,c:1});function Zd(){this.am=this.Df=null}Zd.prototype=new n;Zd.prototype.constructor=Zd;
Zd.prototype.b=function(){Yd=this;this.Df=fi(new ei,void 0,v(new w,function(){return function(){}}(this)),v(new w,function(){return function(){return{}}}(this)));this.am=fi(new ei,se().Df,v(new w,function(){return function(){se();return{a:void 0}}}(this)),v(new w,function(){return function(){se();return{a:void 0}}}(this)));return this};Zd.prototype.$classData=l({Vr:0},!1,"japgolly.scalajs.react.internal.Singleton$",{Vr:1,c:1});var Yd=void 0;function gi(){this.Kd=null}gi.prototype=new n;
gi.prototype.constructor=gi;gi.prototype.b=function(){hi=this;this.Kd=t(new u,function(){return function(a){return a}}(this));return this};gi.prototype.$classData=l({Wr:0},!1,"japgolly.scalajs.react.internal.package$",{Wr:1,c:1});var hi=void 0;function vb(){hi||(hi=(new gi).b());return hi}function ii(){this.aa=null}ii.prototype=new n;ii.prototype.constructor=ii;function ji(){}ji.prototype=ii.prototype;ii.prototype.n=function(a){return a&&a.$classData&&a.$classData.p.Oi?this.aa===a.aa:!1};
ii.prototype.m=function(){return"VdomAttr{name\x3d"+this.aa+"}"};ii.prototype.i=function(a){this.aa=a;return this};ii.prototype.r=function(){return ki(li(),this.aa)};function mi(){}mi.prototype=new n;mi.prototype.constructor=mi;mi.prototype.b=function(){return this};mi.prototype.$classData=l({Yr:0},!1,"japgolly.scalajs.react.vdom.Attr$",{Yr:1,c:1});var ni=void 0;function oi(){this.Jp=this.rn=null}oi.prototype=new n;oi.prototype.constructor=oi;
oi.prototype.b=function(){pi=this;this.rn=Fb(function(){return function(a,b){a.g(b)}}(this));this.Jp=Fb(function(){return function(a,b){a.g(b)}}(this));return this};function qi(a,b){return Fb(function(a,b){return function(a,c){a.g(b.g(c))}}(a,b))}oi.prototype.$classData=l({bs:0},!1,"japgolly.scalajs.react.vdom.Attr$ValueType$",{bs:1,c:1});var pi=void 0;function Db(){pi||(pi=(new oi).b());return pi}function ri(){}ri.prototype=new n;ri.prototype.constructor=ri;ri.prototype.b=function(){return this};
ri.prototype.$classData=l({cs:0},!1,"japgolly.scalajs.react.vdom.Builder$",{cs:1,c:1});var si=void 0;function ti(){si||(si=(new ri).b())}
function ui(a){a.gi={};a.Nl={};a.Kk=[];a.Va=void 0;a.zj=void 0;a.Nu=Fb(function(a){return function(c,e){ti();a.gi[c]=e}}(a));a.an=t(new u,function(a){return function(c){var e=a.zj;void 0!==e&&(vi||(vi=(new wi).b()),c=""+(""+e+" ")+c);a.zj=c}}(a));a.bn=Fb(function(a){return function(c,e){ti();a.Nl[c]=e}}(a));a.Ou=t(new u,function(a){return function(c){Rh||(Rh=(new Ph).b());c=Qh(c);for(c=(new xi).lb(c,t(new u,function(){return function(a){return null!==a}}(a)));c.H();){var e=c.t();if(null!==e)fg(a.bn,
e.N,e.P);else throw(new x).o(e);}}}(a));a.Ak=t(new u,function(a){return function(c){a.Kk.push(c)}}(a));a.Ty=t(new u,function(a){return function(c){a.Va=c}}(a))}function yi(){this.en=null}yi.prototype=new n;yi.prototype.constructor=yi;
yi.prototype.b=function(){zi=this;var a;try{a=g.Symbol["for"]("react.element")}catch(b){if(null!==Wh(Xh(),b))a=60103;else throw b;}this.en=Ai(function(a,c){return function(a,b,h,m){if(void 0!==b.ref)return kb.createElement.apply(kb,[a,b].concat(m));var q=m.length|0;0!==q&&(m=1===q?m[0]:m,ti(),b.children=m);return{$$typeof:c,type:a,key:void 0===h?null:""+h,ref:null,props:b,_owner:null}}}(this,a));return this};
yi.prototype.$classData=l({ds:0},!1,"japgolly.scalajs.react.vdom.Builder$ToRawReactElement$",{ds:1,c:1});var zi=void 0;function Bi(){}Bi.prototype=new n;Bi.prototype.constructor=Bi;function Ci(){}Ci.prototype=Bi.prototype;Bi.prototype.b=function(){dd();Di||(Di=(new Ei).b());Fi||(Fi=(new Gi).b());Hi||(Hi=(new Ii).b());Zc();Ji||(Ji=(new Ki).b());Li||(Li=(new Mi).b());dd();ni||(ni=(new mi).b());Ni||(Ni=(new Oi).b());Pi||(Pi=(new Qi).b());return this};
function Ri(a){a.qz=qi(Db(),t(new u,function(){return function(a){return!!a}}(a)));a.Qp=Db().Jp;a.rz=qi(Db(),t(new u,function(){return function(a){return a|0}}(a)));a.sz=Db().rn}function Gi(){}Gi.prototype=new n;Gi.prototype.constructor=Gi;Gi.prototype.b=function(){return this};Gi.prototype.$classData=l({ss:0},!1,"japgolly.scalajs.react.vdom.ReactFragment$",{ss:1,c:1});var Fi=void 0;function Ii(){}Ii.prototype=new n;Ii.prototype.constructor=Ii;Ii.prototype.b=function(){return this};
Ii.prototype.$classData=l({ts:0},!1,"japgolly.scalajs.react.vdom.ReactPortal$",{ts:1,c:1});var Hi=void 0;function Oi(){}Oi.prototype=new n;Oi.prototype.constructor=Oi;Oi.prototype.b=function(){return this};Oi.prototype.$classData=l({us:0},!1,"japgolly.scalajs.react.vdom.Style$",{us:1,c:1});var Ni=void 0;function Mi(){this.Y=null}Mi.prototype=new n;Mi.prototype.constructor=Mi;Mi.prototype.b=function(){Li=this;this.Y=(new Si).b();return this};
Mi.prototype.$classData=l({ws:0},!1,"japgolly.scalajs.react.vdom.TagMod$",{ws:1,c:1});var Li=void 0;function Qi(){}Qi.prototype=new n;Qi.prototype.constructor=Qi;Qi.prototype.b=function(){return this};Qi.prototype.$classData=l({As:0},!1,"japgolly.scalajs.react.vdom.VdomArray$",{As:1,c:1});var Pi=void 0;function Ei(){}Ei.prototype=new n;Ei.prototype.constructor=Ei;Ei.prototype.b=function(){return this};Ei.prototype.$classData=l({Cs:0},!1,"japgolly.scalajs.react.vdom.VdomElement$",{Cs:1,c:1});
var Di=void 0;function Ti(){this.Y=null}Ti.prototype=new n;Ti.prototype.constructor=Ti;Ti.prototype.b=function(){Ui=this;this.Y=(new ed).el(null);return this};Ti.prototype.$classData=l({Ds:0},!1,"japgolly.scalajs.react.vdom.VdomNode$",{Ds:1,c:1});var Ui=void 0;function dd(){Ui||(Ui=(new Ti).b())}function Vi(){}Vi.prototype=new n;Vi.prototype.constructor=Vi;Vi.prototype.b=function(){Wi=this;(new Xi).b();(new Yi).b();(new Zi).b();(new $i).b();(new aj).b();(new bj).b();(new cj).b();(new dj).b();return this};
Vi.prototype.$classData=l({Fs:0},!1,"japgolly.univeq.UnivEq$",{Fs:1,c:1});var Wi=void 0;function ej(){Wi||(Wi=(new Vi).b())}function fj(){this.ln=this.Cn=this.Up=null;this.Z=0}fj.prototype=new n;fj.prototype.constructor=fj;fj.prototype.b=function(){return this};function gj(){var a=Rg();0===(268435456&a.Z)&&0===(268435456&a.Z)&&(a.Cn=Qg(a).document,a.Z|=268435456);return a.Cn}function Qg(a){0===(134217728&a.Z)&&0===(134217728&a.Z)&&(a.Up=g.window,a.Z|=134217728);return a.Up}
function hj(){var a=Rg();0===(536870912&a.Z)&&0===(536870912&a.Z)&&(a.ln=Qg(a).console,a.Z|=536870912);return a.ln}fj.prototype.$classData=l({Qs:0},!1,"org.scalajs.dom.package$",{Qs:1,c:1});var ij=void 0;function Rg(){ij||(ij=(new fj).b());return ij}function rc(){this.mh=null}rc.prototype=new n;rc.prototype.constructor=rc;
rc.prototype.b=function(){qc=this;Yc();sc();Zc();sc();$c||($c=(new ad).b());var a=jj().ti,a=cd(a);sc();Xc||(Xc=(new Wc).b());var b=Xc.hh(),a=fd((new A).f([a,b.ek()]));this.mh=Sc(gd("HomePage",hd(a)));return this};rc.prototype.hh=function(){return this.mh.Mf.U()};rc.prototype.$classData=l({Rs:0},!1,"pages.HomePage$",{Rs:1,c:1});var qc=void 0;function cd(a){var b=kj;sc();lj||(lj=(new Ej).b());return b(lj,a.Xn)}function Fj(){}Fj.prototype=new n;Fj.prototype.constructor=Fj;Fj.prototype.b=function(){return this};
Fj.prototype.$classData=l({Vs:0},!1,"scalacss.defaults.PlatformExports$",{Vs:1,c:1});var Gj=void 0;function Hj(){}Hj.prototype=new n;Hj.prototype.constructor=Hj;Hj.prototype.b=function(){return this};Hj.prototype.$classData=l({Xs:0},!1,"scalacss.defaults.PlatformExports$StyleSheetInlineJsOps$",{Xs:1,c:1});var Ij=void 0;function Jj(){this.sb=this.qd=null}Jj.prototype=new n;Jj.prototype.constructor=Jj;function Kj(a,b,c){a.qd=b;a.sb=c;return a}
function Lj(a){var b=a.E();return(new Mj).lb(b,t(new u,function(a){return function(b){var f=Nj(Oj(b.P));return(new Vh).lb(f,t(new u,function(a,b){return function(a){return Pj(b.N,a)}}(a,b)))}}(a)))}Jj.prototype.m=function(){var a=this.E();return Qj(a,"AVs(",", ",")")};Jj.prototype.E=function(){var a=Nj(Oj(this.qd));return(new Vh).lb(a,t(new u,function(a){return function(c){return(new H).d(c,a.sb.g(c))}}(this)))};
function Rj(a,b,c){var e=Sj(a,b),f=Tj(e)?Uj(a.qd,b):a.qd;a=a.sb.qb(b,Vj(c,e));return Kj(new Jj,f,a)}function Wj(a,b){Xj();var c=Oj(a.qd),c=Yj(0,Zj(c,b));if(c.l())return B();c=c.R();return(new Pf).o(Kj(new Jj,c,ak(a.sb,b)))}function Sj(a,b){a=a.sb.yb(b);return a.l()?Ac().sc.Vb:Oj(a.R())}Jj.prototype.$classData=l({Ys:0},!1,"scalacss.internal.AVs",{Ys:1,c:1});function bk(){}bk.prototype=new n;bk.prototype.constructor=bk;bk.prototype.b=function(){return this};
function ck(a,b,c){return Kj(new Jj,dk(Xj(),b),(new ek).d(b,c))}bk.prototype.$classData=l({Zs:0},!1,"scalacss.internal.AVs$",{Zs:1,c:1});var fk=void 0;function gk(){fk||(fk=(new bk).b());return fk}function hk(){this.Un=this.Uf=null}hk.prototype=new n;hk.prototype.constructor=hk;function ik(){}ik.prototype=hk.prototype;hk.prototype.n=function(a){if(a&&a.$classData&&a.$classData.p.$s){a=a.Uf;var b=this.Uf;ej();return D(G(),a,b)}return!1};hk.prototype.m=function(){return this.Uf};
hk.prototype.r=function(){return ki(li(),this.Uf)};hk.prototype.Xf=function(a,b){this.Uf=a;this.Un=b;return this};function jk(){}jk.prototype=new n;jk.prototype.constructor=jk;jk.prototype.b=function(){return this};function kk(a){var b=lk();return(new mk).Xf(a,nk(b,a))}function nk(a,b){return t(new u,function(a,b){return function(){return t(new u,function(a,b){return function(a){return ok(pk(),qk(b,a))}}(a,b))}}(a,b))}function rk(a,b,c){return(new mk).Xf(b,sk(c,b))}
jk.prototype.$classData=l({at:0},!1,"scalacss.internal.Attr$",{at:1,c:1});var tk=void 0;function lk(){tk||(tk=(new jk).b());return tk}function uk(){}uk.prototype=new n;uk.prototype.constructor=uk;function vk(){}vk.prototype=uk.prototype;function wk(){this.pn=this.gn=this.fn=null}wk.prototype=new n;wk.prototype.constructor=wk;
wk.prototype.b=function(){xk=this;this.fn=Fb(function(){return function(a,b){(ej(),D(G(),a,b))?(yk||(yk=(new zk).b()),a=yk):a=Ak();return a}}(this));this.gn=Fb(function(a){return function(b,c){b.zl.nc(t(new u,function(a,b){return function(a){return b.ca(a)}}(a,c.zl)))?(Bk||(Bk=(new Ck).b()),b=Bk):b=Ak();return b}}(this));this.pn=Dk(this,this.fn,this.gn);return this};
function Dk(a,b,c){return Fb(function(a,b,c){return function(m,q){return fg(b,m,q).Vl(v(new w,function(a,b,c,e){return function(){return fg(b,c,e)}}(a,c,m,q)))}}(a,b,c))}wk.prototype.$classData=l({ct:0},!1,"scalacss.internal.AttrCmp$",{ct:1,c:1});var xk=void 0;function Ek(){xk||(xk=(new wk).b());return xk}function Fk(){this.Tf=this.kj=null}Fk.prototype=new n;Fk.prototype.constructor=Fk;
Fk.prototype.b=function(){Gk=this;var a=I().Kc,b=J().Lb,c=K(I().j,(new A).f([L(),Hk(),M()])),e=(new H).d(b,c),f=J().Mb,h=K(I().j,(new A).f([M()])),m=(new H).d(f,h),q=J().Nb,y=K(I().j,(new A).f([M()])),N=(new H).d(q,y),z=J().Ob,E=K(I().j,(new A).f([Hk()])),C=(new H).d(z,E),F=J().ec,X=K(I().j,(new A).f([Hk()])),Sb=(new H).d(F,X),yc=J().Pb,ob=K(I().j,(new A).f([Hk(),M()])),Eb=(new H).d(yc,ob),$e=J().Qb,zc=K(I().j,(new A).f([M()])),pd=(new H).d($e,zc),fn=J().Rb,gn=K(I().j,(new A).f([O(),Hk(),M()])),af=
(new H).d(fn,gn),wd=J().Sb,of=K(I().j,(new A).f([O(),M()])),Bc=(new H).d(wd,of),xd=J().Tb,sn=K(I().j,(new A).f([M()])),tn=(new H).d(xd,sn),pf=J().Ub,yd=K(I().j,(new A).f([Hk(),M()])),qf=(new H).d(pf,yd),Cc=J().Wb,zd=K(I().j,(new A).f([O(),Hk(),M()])),un=(new H).d(Cc,zd),vn=J().Xb,rf=K(I().j,(new A).f([O()])),Ad=(new H).d(vn,rf),sf=J().Yb,Dc=K(I().j,(new A).f([O(),M()])),Bd=(new H).d(sf,Dc),wn=J().Zb,xn=K(I().j,(new A).f([O(),Hk(),M()])),tf=(new H).d(wn,xn),Cd=J().fc,uf=K(I().j,(new A).f([M()]));Ik(a,
(new A).f([e,m,N,C,Sb,Eb,pd,af,Bc,tn,qf,un,Ad,Bd,tf,(new H).d(Cd,uf)]));var Ec=I().Kc,Dd=J().Lb,yn=K(I().j,(new A).f([Jk()])),zn=(new H).d(Dd,yn),vf=J().Mb,Ed=K(I().j,(new A).f([Jk()])),wf=(new H).d(vf,Ed),Fc=J().Nb,Fd=K(I().j,(new A).f([Jk()])),An=(new H).d(Fc,Fd),Bn=J().Ob,xf=K(I().j,(new A).f([Jk()])),Gd=(new H).d(Bn,xf),yf=J().ec,Gc=K(I().j,(new A).f([Jk()])),Hd=(new H).d(yf,Gc),Cn=J().Pb,Dn=K(I().j,(new A).f([L(),Jk()])),zf=(new H).d(Cn,Dn),Id=J().Qb,Af=K(I().j,(new A).f([M()])),Hc=(new H).d(Id,
Af),Jd=J().Rb,En=K(I().j,(new A).f([O(),L(),Jk()])),Fn=(new H).d(Jd,En),Bf=J().Sb,Kd=K(I().j,(new A).f([O(),M()])),Cf=(new H).d(Bf,Kd),Ic=J().Tb,Ld=K(I().j,(new A).f([O(),M()])),Gn=(new H).d(Ic,Ld),Hn=J().Ub,Df=K(I().j,(new A).f([L(),Jk(),M()])),Md=(new H).d(Hn,Df),Ef=J().Wb,Jc=K(I().j,(new A).f([O(),Jk(),L()])),Nd=(new H).d(Ef,Jc),In=J().Xb,Jn=K(I().j,(new A).f([L()])),Ff=(new H).d(In,Jn),Od=J().Yb,Gf=K(I().j,(new A).f([O(),L(),Jk()])),Kc=(new H).d(Od,Gf),Pd=J().Zb,Kn=K(I().j,(new A).f([L(),Jk(),
M()])),Ln=(new H).d(Pd,Kn),Hf=J().fc,Qd=K(I().j,(new A).f([Jk()]));Ik(Ec,(new A).f([zn,wf,An,Gd,Hd,zf,Hc,Fn,Cf,Gn,Md,Nd,Ff,Kc,Ln,(new H).d(Hf,Qd)]));var If=I().Kc,Lc=J().Lb,Rd=K(I().j,(new A).f([Hk(),M()])),Mn=(new H).d(Lc,Rd),Nn=J().Mb,Jf=K(I().j,(new A).f([M()])),Sd=(new H).d(Nn,Jf),Kf=J().Nb,Mc=K(I().j,(new A).f([M()])),Td=(new H).d(Kf,Mc),On=J().Ob,Pn=K(I().j,(new A).f([M()])),Lf=(new H).d(On,Pn),Ud=J().ec,Mf=K(I().j,(new A).f([M()])),Nc=(new H).d(Ud,Mf),Vd=J().Pb,mj=K(I().j,(new A).f([Hk(),M()])),
Nf=(new H).d(Vd,mj),Oc=J().Qb,Wd=K(I().j,(new A).f([M()])),Qn=(new H).d(Oc,Wd),tv=J().Rb,uv=K(I().j,(new A).f([L(),Hk(),M()])),vv=(new H).d(tv,uv),wv=J().Sb,Rn=K(I().j,(new A).f([O(),M()])),ur=(new H).d(wv,Rn),vr=J().Tb,nj=K(I().j,(new A).f([M()])),xv=(new H).d(vr,nj),yv=J().Ub,zv=K(I().j,(new A).f([Hk(),M()])),Sn=(new H).d(yv,zv),wr=J().Wb,xr=K(I().j,(new A).f([O(),M()])),oj=(new H).d(wr,xr),Av=J().Xb,Bv=K(I().j,(new A).f([O()])),Cv=(new H).d(Av,Bv),Tn=J().Yb,yr=K(I().j,(new A).f([O(),M()])),zr=
(new H).d(Tn,yr),pj=J().Zb,Dv=K(I().j,(new A).f([Hk(),M()])),Ev=(new H).d(pj,Dv),Fv=J().fc,Un=K(I().j,(new A).f([M()]));Ik(If,(new A).f([Mn,Sd,Td,Lf,Nc,Nf,Qn,vv,ur,xv,Sn,oj,Cv,zr,Ev,(new H).d(Fv,Un)]));var Ar=I().Kc,Br=J().Lb,qj=K(I().j,(new A).f([M()])),Gv=(new H).d(Br,qj),Hv=J().Mb,Iv=K(I().j,(new A).f([M()])),Vn=(new H).d(Hv,Iv),Cr=J().Nb,Dr=K(I().j,(new A).f([M()])),rj=(new H).d(Cr,Dr),Jv=J().Ob,Kv=K(I().j,(new A).f([M()])),Lv=(new H).d(Jv,Kv),Wn=J().ec,Er=K(I().j,(new A).f([M()])),Fr=(new H).d(Wn,
Er),sj=J().Pb,Mv=K(I().j,(new A).f([M()])),Nv=(new H).d(sj,Mv),Ov=J().Qb,Xn=K(I().j,(new A).f([M()])),Gr=(new H).d(Ov,Xn),Hr=J().Rb,tj=K(I().j,(new A).f([M()])),Pv=(new H).d(Hr,tj),Qv=J().Sb,Rv=K(I().j,(new A).f([O(),M()])),Yn=(new H).d(Qv,Rv),Ir=J().Tb,Jr=K(I().j,(new A).f([M()])),uj=(new H).d(Ir,Jr),Sv=J().Ub,Tv=K(I().j,(new A).f([M()])),Uv=(new H).d(Sv,Tv),Zn=J().Wb,Kr=K(I().j,(new A).f([M()])),Lr=(new H).d(Zn,Kr),vj=J().Xb,Vv=K(I().j,(new A).f([M()])),Wv=(new H).d(vj,Vv),Xv=J().Yb,$n=K(I().j,
(new A).f([M()])),Mr=(new H).d(Xv,$n),Nr=J().Zb,wj=K(I().j,(new A).f([M()])),Yv=(new H).d(Nr,wj),Zv=J().fc,$v=K(I().j,(new A).f([M()]));Ik(Ar,(new A).f([Gv,Vn,rj,Lv,Fr,Nv,Gr,Pv,Yn,uj,Uv,Lr,Wv,Mr,Yv,(new H).d(Zv,$v)]));var ao=I().Kc,Or=J().Lb,Pr=K(I().j,(new A).f([L(),M()])),xj=(new H).d(Or,Pr),aw=J().Mb,bw=K(I().j,(new A).f([M()])),cw=(new H).d(aw,bw),bo=J().Nb,Qr=K(I().j,(new A).f([M()])),Rr=(new H).d(bo,Qr),yj=J().Ob,dw=K(I().j,(new A).f([L()])),ew=(new H).d(yj,dw),fw=J().ec,co=K(I().j,(new A).f([L(),
M()])),Sr=(new H).d(fw,co),Tr=J().Pb,zj=K(I().j,(new A).f([L(),Hk(),M()])),gw=(new H).d(Tr,zj),hw=J().Qb,iw=K(I().j,(new A).f([M()])),eo=(new H).d(hw,iw),Ur=J().Rb,Vr=K(I().j,(new A).f([L(),Jk(),M()])),Aj=(new H).d(Ur,Vr),jw=J().Sb,kw=K(I().j,(new A).f([O(),L(),Jk()])),lw=(new H).d(jw,kw),fo=J().Tb,Wr=K(I().j,(new A).f([L(),M()])),Xr=(new H).d(fo,Wr),Bj=J().Ub,mw=K(I().j,(new A).f([L(),Hk(),M()])),nw=(new H).d(Bj,mw),ow=J().Wb,go=K(I().j,(new A).f([O(),M(),Hk()])),Yr=(new H).d(ow,go),Zr=J().Xb,Cj=
K(I().j,(new A).f([M()])),pw=(new H).d(Zr,Cj),$r=J().Yb,as=K(I().j,(new A).f([O(),M()])),Dj=(new H).d($r,as),qw=J().Zb,xF=K(I().j,(new A).f([L(),Hk(),M()])),yF=(new H).d(qw,xF),zF=J().fc,AF=K(I().j,(new A).f([M()]));this.kj=Ik(ao,(new A).f([xj,cw,Rr,ew,Sr,gw,eo,Aj,lw,Xr,nw,Yr,pw,Dj,yF,(new H).d(zF,AF)]));var BF=I().Kc,CF=J().Lb,DF=K(I().j,(new A).f([O()])),EF=(new H).d(CF,DF),FF=J().Mb,GF=K(I().j,(new A).f([O()])),HF=(new H).d(FF,GF),IF=J().Nb,JF=K(I().j,(new A).f([O()])),KF=(new H).d(IF,JF),LF=J().Ob,
MF=K(I().j,(new A).f([O()])),NF=(new H).d(LF,MF),OF=J().ec,PF=K(I().j,(new A).f([O()])),QF=(new H).d(OF,PF),RF=J().Pb,SF=K(I().j,(new A).f([O()])),TF=(new H).d(RF,SF),UF=J().Qb,VF=K(I().j,(new A).f([L()])),WF=(new H).d(UF,VF),XF=J().Rb,YF=K(I().j,(new A).f([O()])),ZF=(new H).d(XF,YF),$F=J().Sb,aG=K(I().j,(new A).f([O(),L()])),bG=(new H).d($F,aG),cG=J().Tb,dG=K(I().j,(new A).f([L()])),eG=(new H).d(cG,dG),fG=J().Ub,gG=K(I().j,(new A).f([O()])),hG=(new H).d(fG,gG),iG=J().Wb,jG=K(I().j,(new A).f([O()])),
kG=(new H).d(iG,jG),lG=J().Xb,mG=K(I().j,(new A).f([O()])),nG=(new H).d(lG,mG),oG=J().Yb,pG=K(I().j,(new A).f([O()])),qG=(new H).d(oG,pG),rG=J().Zb,sG=K(I().j,(new A).f([O(),Kk(),M()])),tG=(new H).d(rG,sG),uG=J().fc,vG=K(I().j,(new A).f([O()]));this.Tf=Ik(BF,(new A).f([EF,HF,KF,NF,QF,TF,WF,ZF,bG,eG,hG,kG,nG,qG,tG,(new H).d(uG,vG)]));var wG=I().Kc,xG=J().Lb,yG=K(I().j,(new A).f([L()])),zG=(new H).d(xG,yG),AG=J().Mb,BG=K(I().j,(new A).f([L()])),CG=(new H).d(AG,BG),DG=J().Nb,EG=K(I().j,(new A).f([Jk()])),
FG=(new H).d(DG,EG),GG=J().Ob,HG=K(I().j,(new A).f([L()])),IG=(new H).d(GG,HG),JG=J().ec,KG=K(I().j,(new A).f([L()])),LG=(new H).d(JG,KG),MG=J().Pb,NG=K(I().j,(new A).f([L()])),OG=(new H).d(MG,NG),PG=J().Qb,QG=K(I().j,(new A).f([O()])),RG=(new H).d(PG,QG),SG=J().Rb,TG=K(I().j,(new A).f([O(),Jk()])),UG=(new H).d(SG,TG),VG=J().Sb,WG=K(I().j,(new A).f([O()])),XG=(new H).d(VG,WG),YG=J().Tb,ZG=K(I().j,(new A).f([O()])),$G=(new H).d(YG,ZG),aH=J().Ub,bH=K(I().j,(new A).f([L()])),cH=(new H).d(aH,bH),dH=J().Wb,
eH=K(I().j,(new A).f([O(),L()])),fH=(new H).d(dH,eH),gH=J().Xb,hH=K(I().j,(new A).f([O()])),iH=(new H).d(gH,hH),jH=J().Yb,kH=K(I().j,(new A).f([O(),L()])),lH=(new H).d(jH,kH),mH=J().Zb,nH=K(I().j,(new A).f([O(),L()])),oH=(new H).d(mH,nH),pH=J().fc,qH=K(I().j,(new A).f([L()]));Ik(wG,(new A).f([zG,CG,FG,IG,LG,OG,RG,UG,XG,$G,cH,fH,iH,lH,oH,(new H).d(pH,qH)]));var rH=I().Kc,sH=J().Lb,tH=K(I().j,(new A).f([L()])),uH=(new H).d(sH,tH),vH=J().Mb,wH=K(I().j,(new A).f([Jk()])),xH=(new H).d(vH,wH),yH=J().Nb,
zH=K(I().j,(new A).f([L()])),AH=(new H).d(yH,zH),BH=J().Ob,CH=K(I().j,(new A).f([L()])),DH=(new H).d(BH,CH),EH=J().ec,FH=K(I().j,(new A).f([L()])),GH=(new H).d(EH,FH),HH=J().Pb,IH=K(I().j,(new A).f([L(),Jk()])),JH=(new H).d(HH,IH),KH=J().Qb,LH=K(I().j,(new A).f([M()])),MH=(new H).d(KH,LH),NH=J().Rb,OH=K(I().j,(new A).f([L()])),PH=(new H).d(NH,OH),QH=J().Sb,RH=K(I().j,(new A).f([O(),M()])),SH=(new H).d(QH,RH),TH=J().Tb,UH=K(I().j,(new A).f([M()])),VH=(new H).d(TH,UH),WH=J().Ub,XH=K(I().j,(new A).f([L(),
Jk()])),YH=(new H).d(WH,XH),ZH=J().Wb,$H=K(I().j,(new A).f([O(),M(),L(),Jk()])),aI=(new H).d(ZH,$H),bI=J().Xb,cI=K(I().j,(new A).f([M()])),dI=(new H).d(bI,cI),eI=J().Yb,fI=K(I().j,(new A).f([O(),M(),L()])),gI=(new H).d(eI,fI),hI=J().Zb,iI=K(I().j,(new A).f([L(),Jk()])),jI=(new H).d(hI,iI),kI=J().fc,lI=K(I().j,(new A).f([L()]));Ik(rH,(new A).f([uH,xH,AH,DH,GH,JH,MH,PH,SH,VH,YH,aI,dI,gI,jI,(new H).d(kI,lI)]));var mI=I().Kc,nI=J().Lb,oI=K(I().j,(new A).f([M()])),pI=(new H).d(nI,oI),qI=J().Mb,rI=K(I().j,
(new A).f([M()])),sI=(new H).d(qI,rI),tI=J().Nb,uI=K(I().j,(new A).f([M()])),vI=(new H).d(tI,uI),wI=J().Ob,xI=K(I().j,(new A).f([M()])),yI=(new H).d(wI,xI),zI=J().ec,AI=K(I().j,(new A).f([M()])),BI=(new H).d(zI,AI),CI=J().Pb,DI=K(I().j,(new A).f([M()])),EI=(new H).d(CI,DI),FI=J().Qb,GI=K(I().j,(new A).f([M()])),HI=(new H).d(FI,GI),II=J().Rb,JI=K(I().j,(new A).f([M()])),KI=(new H).d(II,JI),LI=J().Sb,MI=K(I().j,(new A).f([O(),M()])),NI=(new H).d(LI,MI),OI=J().Tb,PI=K(I().j,(new A).f([M()])),QI=(new H).d(OI,
PI),RI=J().Ub,SI=K(I().j,(new A).f([M()])),TI=(new H).d(RI,SI),UI=J().Wb,VI=K(I().j,(new A).f([M()])),WI=(new H).d(UI,VI),XI=J().Xb,YI=K(I().j,(new A).f([O()])),ZI=(new H).d(XI,YI),$I=J().Yb,aJ=K(I().j,(new A).f([M()])),bJ=(new H).d($I,aJ),cJ=J().Zb,dJ=K(I().j,(new A).f([M()])),eJ=(new H).d(cJ,dJ),fJ=J().fc,gJ=K(I().j,(new A).f([M()]));Ik(mI,(new A).f([pI,sI,vI,yI,BI,EI,HI,KI,NI,QI,TI,WI,ZI,bJ,eJ,(new H).d(fJ,gJ)]));var hJ=I().Kc,iJ=J().Lb,jJ=K(I().j,(new A).f([O(),M()])),kJ=(new H).d(iJ,jJ),lJ=J().Mb,
mJ=K(I().j,(new A).f([M()])),nJ=(new H).d(lJ,mJ),oJ=J().Nb,pJ=K(I().j,(new A).f([O()])),qJ=(new H).d(oJ,pJ),rJ=J().Ob,sJ=K(I().j,(new A).f([O()])),tJ=(new H).d(rJ,sJ),uJ=J().ec,vJ=K(I().j,(new A).f([O()])),wJ=(new H).d(uJ,vJ),xJ=J().Pb,yJ=K(I().j,(new A).f([O(),M()])),zJ=(new H).d(xJ,yJ),AJ=J().Qb,BJ=K(I().j,(new A).f([O()])),CJ=(new H).d(AJ,BJ),DJ=J().Rb,EJ=K(I().j,(new A).f([O()])),FJ=(new H).d(DJ,EJ),GJ=J().Sb,HJ=K(I().j,(new A).f([O()])),IJ=(new H).d(GJ,HJ),JJ=J().Tb,KJ=K(I().j,(new A).f([O()])),
LJ=(new H).d(JJ,KJ),MJ=J().Ub,NJ=K(I().j,(new A).f([O(),Hk()])),OJ=(new H).d(MJ,NJ),PJ=J().Wb,QJ=K(I().j,(new A).f([O(),M()])),RJ=(new H).d(PJ,QJ),SJ=J().Xb,TJ=K(I().j,(new A).f([O()])),UJ=(new H).d(SJ,TJ),VJ=J().Yb,WJ=K(I().j,(new A).f([O(),M()])),XJ=(new H).d(VJ,WJ),YJ=J().Zb,ZJ=K(I().j,(new A).f([O(),Hk()])),$J=(new H).d(YJ,ZJ),aK=J().fc,bK=K(I().j,(new A).f([M()]));Ik(hJ,(new A).f([kJ,nJ,qJ,tJ,wJ,zJ,CJ,FJ,IJ,LJ,OJ,RJ,UJ,XJ,$J,(new H).d(aK,bK)]));var cK=I().Kc,dK=J().Lb,eK=K(I().j,(new A).f([O()])),
fK=(new H).d(dK,eK),gK=J().Mb,hK=K(I().j,(new A).f([O()])),iK=(new H).d(gK,hK),jK=J().Nb,kK=K(I().j,(new A).f([M()])),lK=(new H).d(jK,kK),mK=J().Ob,nK=K(I().j,(new A).f([O()])),oK=(new H).d(mK,nK),pK=J().ec,qK=K(I().j,(new A).f([O()])),rK=(new H).d(pK,qK),sK=J().Pb,tK=K(I().j,(new A).f([O()])),uK=(new H).d(sK,tK),vK=J().Qb,wK=K(I().j,(new A).f([O()])),xK=(new H).d(vK,wK),yK=J().Rb,zK=K(I().j,(new A).f([O(),Hk(),M()])),AK=(new H).d(yK,zK),BK=J().Sb,CK=K(I().j,(new A).f([O()])),DK=(new H).d(BK,CK),
EK=J().Tb,FK=K(I().j,(new A).f([O()])),GK=(new H).d(EK,FK),HK=J().Ub,IK=K(I().j,(new A).f([O(),L()])),JK=(new H).d(HK,IK),KK=J().Wb,LK=K(I().j,(new A).f([O()])),MK=(new H).d(KK,LK),NK=J().Xb,OK=K(I().j,(new A).f([O()])),PK=(new H).d(NK,OK),QK=J().Yb,RK=K(I().j,(new A).f([O()])),SK=(new H).d(QK,RK),TK=J().Zb,UK=K(I().j,(new A).f([O(),L()])),VK=(new H).d(TK,UK),WK=J().fc,XK=K(I().j,(new A).f([O()]));Ik(cK,(new A).f([fK,iK,lK,oK,rK,uK,xK,AK,DK,GK,JK,MK,PK,SK,VK,(new H).d(WK,XK)]));var YK=I().Kc,ZK=J().Lb,
$K=K(I().j,(new A).f([Hk(),M()])),aL=(new H).d(ZK,$K),bL=J().Mb,cL=K(I().j,(new A).f([M()])),dL=(new H).d(bL,cL),eL=J().Nb,fL=K(I().j,(new A).f([M()])),gL=(new H).d(eL,fL),hL=J().Ob,iL=K(I().j,(new A).f([Hk()])),jL=(new H).d(hL,iL),kL=J().ec,lL=K(I().j,(new A).f([Hk(),M()])),mL=(new H).d(kL,lL),nL=J().Pb,oL=K(I().j,(new A).f([Hk(),M()])),pL=(new H).d(nL,oL),qL=J().Qb,rL=K(I().j,(new A).f([M()])),sL=(new H).d(qL,rL),tL=J().Rb,uL=K(I().j,(new A).f([O(),Hk(),M()])),vL=(new H).d(tL,uL),wL=J().Sb,xL=K(I().j,
(new A).f([O(),M()])),yL=(new H).d(wL,xL),zL=J().Tb,AL=K(I().j,(new A).f([M()])),BL=(new H).d(zL,AL),CL=J().Ub,DL=K(I().j,(new A).f([Hk(),M()])),EL=(new H).d(CL,DL),FL=J().Wb,GL=K(I().j,(new A).f([O(),Hk(),M()])),HL=(new H).d(FL,GL),IL=J().Xb,JL=K(I().j,(new A).f([O()])),KL=(new H).d(IL,JL),LL=J().Yb,ML=K(I().j,(new A).f([Hk(),M()])),NL=(new H).d(LL,ML),OL=J().Zb,PL=K(I().j,(new A).f([Hk(),M()])),QL=(new H).d(OL,PL),RL=J().fc,SL=K(I().j,(new A).f([M()]));Ik(YK,(new A).f([aL,dL,gL,jL,mL,pL,sL,vL,yL,
BL,EL,HL,KL,NL,QL,(new H).d(RL,SL)]));return this};Fk.prototype.$classData=l({rt:0},!1,"scalacss.internal.CanIUse$",{rt:1,c:1});var Gk=void 0;function Lk(){Gk||(Gk=(new Fk).b());return Gk}function Mk(){this.oc=this.aa=null}Mk.prototype=new n;Mk.prototype.constructor=Mk;function Nk(){}Nk.prototype=Mk.prototype;Mk.prototype.i=function(a){this.aa=a;this.oc="-"+a+"-";return this};function Ok(){this.jg=null}Ok.prototype=new n;Ok.prototype.constructor=Ok;
Ok.prototype.b=function(){Pk=this;var a=Qk(),b=(new A).f([Rk(),Sk(),Tk()]);Uk();var c=Vk().Ja,b=Wk(b,c);this.jg=Xk(new Yk,a,b);return this};Ok.prototype.$classData=l({tt:0},!1,"scalacss.internal.CanIUse$Prefix$",{tt:1,c:1});var Pk=void 0;function Zk(){this.No=this.Lo=this.sl=this.ho=null;this.Z=0}Zk.prototype=new n;Zk.prototype.constructor=Zk;
function $k(a){if(-1!==(al(P(),a).toLowerCase().indexOf("android")|0)){a=a.aa;if(a.l())return J().jg;a=a.R();if("Chrome"===a){a=J().Mb;var b=(new A).f([]);Uk();var c=Vk().Ja,b=Wk(b,c);return Xk(new Yk,a,b)}if("Firefox"===a)return a=J().Nb,b=(new A).f([]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b);a=J().Lb;b=(new A).f([J().Ob]);Uk();c=Vk().Ja;b=Wk(b,c);return Xk(new Yk,a,b)}a=a.aa;if(a.l())return J().jg;a=a.R();if("Chrome"===a)return a=J().Pb,b=(new A).f([]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b);if("Firefox"===
a)return a=J().Rb,b=(new A).f([]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b);if("IE"===a)return a=J().Sb,b=(new A).f([J().Tb]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b);if("Opera"===a)return a=J().Wb,b=(new A).f([J().Xb,J().Yb]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b);if("Safari"===a)return a=J().Ub,b=(new A).f([J().Zb]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b);Ca();return"Edge"===a.substring((a.length|0)-4|0)?(a=J().Qb,b=(new A).f([]),Uk(),c=Vk().Ja,b=Wk(b,c),Xk(new Yk,a,b)):J().jg}
Zk.prototype.b=function(){bl=this;this.sl=t(new u,function(){return function(a){if(O()===a||M()===a||Jk()===a)return!1;if(Hk()===a||L()===a)return!0;if(Kk()===a)return!1;throw(new x).o(a);}}(this));var a=pk(),b=t(new u,function(a){return function(b){var f=cl(a,b);Uk();var h=Vk().Ja,h=Wk(f,h);Uk();f=Vk().Ja;f=dl(h,f);for(h=Nj(h);h.Cb;){var m=h.t();f.rb((new Pf).o(m))}f=f.La();b=b.E();for(h=!1;!h&&b.H();)h=b.t().P.nc(t(new u,function(a){return function(b){return!a.sl.g(b)}}(a)));return h?Kg(f,B()):
f}}(this));ej();J();ej();ej();ej();this.Lo=el(a,b);Pk||(Pk=(new Ok).b());a=Nj(Oj(Pk.jg));a=(new Vh).lb(a,t(new u,function(){return function(a){return a.oc}}(this)));a=Qj(a,".*(?:","|",").*");a=(new ih).i(a);xb();a=jh(a.cb).Ng;this.No=t(new u,function(a,b){return function(a){return Wg(Ug(new Vg,b,a,a.length|0))}}(this,a));return this};
function fl(){var a=gl();if(0===(1&a.Z)&&0===(1&a.Z)){var b=hl,c=il();Lk();var e=J().Lb,f=[O(),Hk(),M()];if(0===(f.length|0))var h=Q();else{for(var m=R(new S,Q()),q=0,y=f.length|0;q<y;)T(m,f[q]),q=1+q|0;h=m.w}var N=(new H).d(e,h),z=J().Mb,E=[M()];if(0===(E.length|0))var C=Q();else{for(var F=R(new S,Q()),X=0,Sb=E.length|0;X<Sb;)T(F,E[X]),X=1+X|0;C=F.w}var yc=(new H).d(z,C),ob=J().Nb,Eb=[L()];if(0===(Eb.length|0))var $e=Q();else{for(var zc=R(new S,Q()),pd=0,fn=Eb.length|0;pd<fn;)T(zc,Eb[pd]),pd=1+pd|
0;$e=zc.w}var gn=(new H).d(ob,$e),af=J().Ob,wd=[O()];if(0===(wd.length|0))var of=Q();else{for(var Bc=R(new S,Q()),xd=0,sn=wd.length|0;xd<sn;)T(Bc,wd[xd]),xd=1+xd|0;of=Bc.w}var tn=(new H).d(af,of),pf=J().ec,yd=[O(),Hk()];if(0===(yd.length|0))var qf=Q();else{for(var Cc=R(new S,Q()),zd=0,un=yd.length|0;zd<un;)T(Cc,yd[zd]),zd=1+zd|0;qf=Cc.w}var vn=(new H).d(pf,qf),rf=J().Pb,Ad=[O(),Hk(),M()];if(0===(Ad.length|0))var sf=Q();else{for(var Dc=R(new S,Q()),Bd=0,wn=Ad.length|0;Bd<wn;)T(Dc,Ad[Bd]),Bd=1+Bd|0;
sf=Dc.w}var xn=(new H).d(rf,sf),tf=J().Qb,Cd=[O()];if(0===(Cd.length|0))var uf=Q();else{for(var Ec=R(new S,Q()),Dd=0,yn=Cd.length|0;Dd<yn;)T(Ec,Cd[Dd]),Dd=1+Dd|0;uf=Ec.w}var zn=(new H).d(tf,uf),vf=J().Rb,Ed=[O(),L()];if(0===(Ed.length|0))var wf=Q();else{for(var Fc=R(new S,Q()),Fd=0,An=Ed.length|0;Fd<An;)T(Fc,Ed[Fd]),Fd=1+Fd|0;wf=Fc.w}var Bn=(new H).d(vf,wf),xf=J().Sb,Gd=[O()];if(0===(Gd.length|0))var yf=Q();else{for(var Gc=R(new S,Q()),Hd=0,Cn=Gd.length|0;Hd<Cn;)T(Gc,Gd[Hd]),Hd=1+Hd|0;yf=Gc.w}var Dn=
(new H).d(xf,yf),zf=J().Tb,Id=[O()];if(0===(Id.length|0))var Af=Q();else{for(var Hc=R(new S,Q()),Jd=0,En=Id.length|0;Jd<En;)T(Hc,Id[Jd]),Jd=1+Jd|0;Af=Hc.w}var Fn=(new H).d(zf,Af),Bf=J().Ub,Kd=[O(),L()];if(0===(Kd.length|0))var Cf=Q();else{for(var Ic=R(new S,Q()),Ld=0,Gn=Kd.length|0;Ld<Gn;)T(Ic,Kd[Ld]),Ld=1+Ld|0;Cf=Ic.w}var Hn=(new H).d(Bf,Cf),Df=J().Wb,Md=[O(),Hk(),M()];if(0===(Md.length|0))var Ef=Q();else{for(var Jc=R(new S,Q()),Nd=0,In=Md.length|0;Nd<In;)T(Jc,Md[Nd]),Nd=1+Nd|0;Ef=Jc.w}var Jn=(new H).d(Df,
Ef),Ff=J().Xb,Od=[O()];if(0===(Od.length|0))var Gf=Q();else{for(var Kc=R(new S,Q()),Pd=0,Kn=Od.length|0;Pd<Kn;)T(Kc,Od[Pd]),Pd=1+Pd|0;Gf=Kc.w}var Ln=(new H).d(Ff,Gf),Hf=J().Yb,Qd=[O(),M()];if(0===(Qd.length|0))var If=Q();else{for(var Lc=R(new S,Q()),Rd=0,Mn=Qd.length|0;Rd<Mn;)T(Lc,Qd[Rd]),Rd=1+Rd|0;If=Lc.w}var Nn=(new H).d(Hf,If),Jf=J().Zb,Sd=[O(),L()];if(0===(Sd.length|0))var Kf=Q();else{for(var Mc=R(new S,Q()),Td=0,On=Sd.length|0;Td<On;)T(Mc,Sd[Td]),Td=1+Td|0;Kf=Mc.w}var Pn=(new H).d(Jf,Kf),Lf=
J().fc,Ud=[Hk()];if(0===(Ud.length|0))var Mf=Q();else{for(var Nc=R(new S,Q()),Vd=0,mj=Ud.length|0;Vd<mj;)T(Nc,Ud[Vd]),Vd=1+Vd|0;Mf=Nc.w}for(var Nf=[N,yc,gn,tn,vn,xn,zn,Bn,Dn,Fn,Hn,Jn,Ln,Nn,Pn,(new H).d(Lf,Mf)],Oc=jl(new kl,ll()),Wd=0,Qn=Nf.length|0;Wd<Qn;)ml(Oc,Nf[Wd]),Wd=1+Wd|0;a.ho=b(c,Oc.w,"fill-available",(new A).f(["max-content","min-content","fit-content","contain-floats"]));a.Z=(1|a.Z)<<24>>24}return a.ho}
function cl(a,b){b=b.E();b=(new xi).lb(b,t(new u,function(a){return function(b){return b.P.nc(a.sl)}}(a)));a=(new Vh).lb(b,t(new u,function(a){return function(b){return nl(a).g(b.N)}}(a)));b=null;for(b=Q();a.H();){var c=a.t();b=b.kg(c)}return b}function nl(a){return t(new u,function(){return function(a){var c=[a.oc];if(0===(c.length|0))c=Q();else{for(var e=R(new S,Q()),f=0,h=c.length|0;f<h;)T(e,c[f]),f=1+f|0;c=e.w}return c.kg((new ol).Yf(a.fi))}}(a))}
function pl(a,b,c){Uk();a=(new ql).b();for(c=Nj(c);c.Cb;){var e=c.t(),f=e;f.l()?f=!0:(f=f.R(),f=b.ca(f));!1!==f&&rl(a,e)}return sl(a)}Zk.prototype.$classData=l({Et:0},!1,"scalacss.internal.CanIUse2$",{Et:1,c:1});var bl=void 0;function gl(){bl||(bl=(new Zk).b());return bl}function tl(){this.Hj=null}tl.prototype=new n;tl.prototype.constructor=tl;
tl.prototype.b=function(){ul=this;this.Hj=t(new u,function(a){return function(b){return(new Pf).o(t(new u,function(a,b){return function(a){return""+a.oc+b}}(a,b)))}}(this));return this};function vl(a,b){return t(new u,function(a,b){return function(f){return b.g(f)?a.Hj.g(f):B()}}(a,b))}tl.prototype.$classData=l({Ft:0},!1,"scalacss.internal.CanIUse2$PrefixApply$",{Ft:1,c:1});var ul=void 0;function wl(){ul||(ul=(new tl).b());return ul}function xl(){}xl.prototype=new n;xl.prototype.constructor=xl;
xl.prototype.b=function(){return this};function yl(a){if(Tj(a.Wa))return a.Qc;a=Oj(a);return Qj(a,"[","; ","]")}function zl(){var a=Al();return Fb(function(){return function(a,c){for(c=c.E();c.H();){var e=c.t();a=Rj(a,e.N,e.P)}return a}}(a))}
function Bl(){var a=Al();return Fb(function(a){return function(c,e){e=e.E();c=(new Vh).lb(e,t(new u,function(a,b){return function(c){return Cl(Dl((new A).f(["{","} conflicts with ",""])),(new A).f([El(c.N,c.P),Fl(a,b)]))}}(a,c)));Uk();e=Vk().Ja;return Gl(c,e)}}(a))}function Fl(a,b){b=b.E();a=(new Vh).lb(b,t(new u,function(){return function(a){return El(a.N,a.P)}}(a)));return Qj(a,"{",", ","}")}function El(a,b){return Cl(Dl((new A).f(["",": ",""])),(new A).f([a.Uf,yl(b)]))}
xl.prototype.$classData=l({Ht:0},!1,"scalacss.internal.Compose$Rules$",{Ht:1,c:1});var Hl=void 0;function Al(){Hl||(Hl=(new xl).b());return Hl}function Il(){}Il.prototype=new n;Il.prototype.constructor=Il;Il.prototype.b=function(){return this};function Jl(a,b,c){return Kl(0,"."+b.s,c)}function Ll(a,b,c,e){a=Ml(a,b,c,e).ik(v(new w,function(a,b,c,e){return function(){return Nl(a,b,c,e)}}(a,b,c,e)));Uk();b=Vk().Ja;return Gl(a,b)}
function Ol(a,b){b=b.Ue();a=(new Vh).lb(b,t(new u,function(){return function(a){var b=a.Bc,f=a.Qf,h=a.Rf,m=a.Sf,q=a.ig,y=new Pl;y.xb=a.xb;y.Bc=b;y.zh=f;y.Ah=h;y.Bh=m;y.Bi=q;return y}}(a)));b=hg().Ka;return Gl(a,b)}function Ql(a,b,c){b=b.Ue();a=(new Mj).lb(b,t(new u,function(a,b){return function(c){return Rl(a,c,b)}}(a,c)));Uk();c=Vk().Ja;return Gl(a,c)}function Rl(a,b,c){return Ll(a,"."+b.Hf.s,b.wi,c)}
function Sl(a,b,c,e,f){Ac();var h=(new ql).b();b=Ql(a,b,f);Tl(h,b);c=Ul(a,c,f);Tl(h,c);a=Ol(a,e);Tl(h,a);return sl(h)}
function Ml(a,b,c,e){c=c.Hd.E();return(new Mj).lb(c,t(new u,function(a,b,c){return function(e){if(null!==e){var y=e.N;e=Lj(e.P);e=(new Vh).lb(e,t(new u,function(a,b){return function(a){return a.Ef.Un.g(b).g(a.s)}}(a,c)));for(var N=null,N=Ac().sc.Vb;e.H();)var z=e.t(),N=Vl(N,z);if(Tj(N))return hg(),xb();e=N.D();N=Wl(N);e=Xk(new Yk,e,N);N=Xl(y);y=Kl(0,b,y);y=Yl(N,y,e);e=xb();return zb(new Ab,y,e)}throw(new x).o(e);}}(a,b,e)))}
function Xl(a){a=Yj(Xj(),a.Jg);if(a.l())return B();a=a.R();return(new Pf).o(Zl($l(),a))}function am(a,b){Ac();a=(new ql).b();hg();var c=(new ig).b();hg();var e=(new ig).b();for(b=Nj(b);b.Cb;){var f=b.t();if(bm(f))rl(a,f);else if(cm(f))jg(c,f);else if(dm(f))jg(e,f);else throw(new x).o(f);}return(new em).nf(sl(a),kg(c),kg(e))}function fm(a,b){a=ll();for(b=Nj(b);b.Cb;){var c=b.t(),e=c,c=(new H).d(e.Sg,e.Jf),e=e.Ne;a=a.qb(e,gm(Xj(),a.yb(e),c))}return a}
function hm(a,b,c){var e=b.Nv();b=b.JA().E();a=(new Vh).lb(b,t(new u,function(a,b){return function(c){return(new H).d(c.N,Rl(a,c.P,b))}}(a,c)));for(c=jl(new kl,ll());a.H();)b=a.t(),ml(c,b);return im(e,c.w)}function Nl(a,b,c,e){c=Nj(c.Vd);return(new Mj).lb(c,t(new u,function(a,b,c){return function(e){var y=e.BB().g(""+b+e.HA());return Ll(a,y,e.GB(),c)}}(a,b,e)))}function Ul(a,b,c){b=b.Ue();a=(new Vh).lb(b,t(new u,function(a,b){return function(c){return hm(a,c,b)}}(a,c)));c=hg().Ka;return Gl(a,c)}
function Kl(a,b,c){a=c.hi;return a.l()?b:a.R().SA(b)}Il.prototype.$classData=l({Kt:0},!1,"scalacss.internal.Css$",{Kt:1,c:1});var jm=void 0;function km(){jm||(jm=(new Il).b());return jm}function lm(){this.Tg=this.Bg=null}lm.prototype=new n;lm.prototype.constructor=lm;function mm(a,b,c){a.Bg=b;a.Tg=c;return a}lm.prototype.$classData=l({Mt:0},!1,"scalacss.internal.CssKV$Lens",{Mt:1,c:1});function nm(){this.Z=!1}nm.prototype=new n;nm.prototype.constructor=nm;
nm.prototype.b=function(){om=this;var a=[!0,!1];I();pm();Uk();for(var b=(new ql).b(),c=0,e=a.length|0;c<e;)rl(b,a[c]),c=1+c|0;sl(b).W();return this};nm.prototype.$classData=l({Nt:0},!1,"scalacss.internal.Domain$",{Nt:1,c:1});var om=void 0;function qm(){this.ma=null}qm.prototype=new n;qm.prototype.constructor=qm;qm.prototype.n=function(a){rm||(rm=(new sm).b());var b;b=this.ma;a&&a.$classData&&a.$classData.p.Jm?(a=null===a?null:a.ma,b=null===b?null===a:b.n(a)):b=!1;return b};
function tm(a,b){a.ma=b;return a}qm.prototype.r=function(){var a=this.ma;return um(U(),a)};qm.prototype.$classData=l({Jm:0},!1,"scalacss.internal.DslBase$ToStyle",{Jm:1,c:1});function sm(){}sm.prototype=new n;sm.prototype.constructor=sm;sm.prototype.b=function(){return this};sm.prototype.$classData=l({Rt:0},!1,"scalacss.internal.DslBase$ToStyle$",{Rt:1,c:1});var rm=void 0;function vm(){this.xb=null}vm.prototype=new n;vm.prototype.constructor=vm;
vm.prototype.n=function(a){var b;wm||(wm=(new xm).b());b=this.xb;a&&a.$classData&&a.$classData.p.Om?(a=null===a?null:a.xb,b=null===b?null===a:b.n(a)):b=!1;return b};vm.prototype.r=function(){return this.xb.r()};function ym(){var a=new vm,b=(new Pf).o("myFont3");a.xb=b;return a}vm.prototype.$classData=l({Om:0},!1,"scalacss.internal.FontFace$FontSrcSelector",{Om:1,c:1});function xm(){}xm.prototype=new n;xm.prototype.constructor=xm;xm.prototype.b=function(){return this};
function zm(a){var b=(new A).f([]).Af(),c=new Am,b=Xk(new Yk,"local(Pacifico)",b),e=B(),f=B(),h=B(),m=B();c.xb=a;c.Bc=b;c.Qf=e;c.Rf=f;c.Sf=h;c.ig=m;return c}xm.prototype.$classData=l({Tt:0},!1,"scalacss.internal.FontFace$FontSrcSelector$",{Tt:1,c:1});var wm=void 0;function Bm(){this.s=null}Bm.prototype=new n;Bm.prototype.constructor=Bm;function Cm(){}Cm.prototype=Bm.prototype;Bm.prototype.i=function(a){this.s=a;return this};function Dm(){this.s=null}Dm.prototype=new n;Dm.prototype.constructor=Dm;
function Em(){}Em.prototype=Dm.prototype;Dm.prototype.m=function(){return Cl(Dl((new A).f(["Literal(",")"])),(new A).f([this.s]))};Dm.prototype.i=function(a){this.s=a;return this};function Fm(){this.nn=this.Nk=null}Fm.prototype=new n;Fm.prototype.constructor=Fm;
Fm.prototype.b=function(){Gm=this;this.Nk=t(new u,function(a,b){return function(a){a=b.g(a);V();return""+W(V(),"(",a)+")"}}(this,t(new u,function(a){return function(b){if(b&&b.$classData&&b.$classData.p.dA){var c=b.Ov();b=function(){return function(a){return a.mc()}}(a);if(Hm(c)){c=c.Ge();V();var e=W(V(),"aspect-ratio",":");return W(0,e,b(c))}if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","aspect-ratio"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-","aspect-ratio"),W(0,""+e+
":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.lA){c=b.jl();b=function(){return function(a){return a.mc()}}(a);if(Hm(c))return c=c.Ge(),V(),e=W(V(),"height",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","height"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-","height"),W(0,""+e+":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.vA){c=b.jl();b=function(){return function(a){return a.mc()}}(a);if(Hm(c))return c=c.Ge(),V(),e=W(V(),"width",
":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","width"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-","width"),W(0,""+e+":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.gA){c=b.Ov();b=function(){return function(a){return a.mc()}}(a);if(Hm(c))return c=c.Ge(),V(),e=W(V(),"device-aspect-ratio",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","device-aspect-ratio"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-",
"device-aspect-ratio"),W(0,""+e+":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.hA){c=b.jl();b=function(){return function(a){return a.mc()}}(a);if(Hm(c))return c=c.Ge(),V(),e=W(V(),"device-height",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","device-height"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-","device-height"),W(0,""+e+":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.iA){c=b.jl();b=function(){return function(a){return a.mc()}}(a);
if(Hm(c))return c=c.Ge(),V(),e=W(V(),"device-width",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","device-width"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-","device-width"),W(0,""+e+":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.eA){c=b.GA();b=function(){return function(a){return""+(a|0)}}(a);if(c.l())return"color";c=c.R();if(Hm(c))return c=c.Ge(),V(),e=W(V(),"color",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","color"),
W(0,""+e+":",b(c));if(!Jm(c))throw(new x).o(c);c=c.Le();V();V();e=W(V(),"max-","color");return W(0,""+e+":",b(c))}if(b&&b.$classData&&b.$classData.p.fA){c=b.MA();b=function(){return function(a){return""+(a|0)}}(a);if(c.l())return"color-index";c=c.R();if(Hm(c))return c=c.Ge(),V(),e=W(V(),"color-index",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","color-index"),W(0,""+e+":",b(c));if(!Jm(c))throw(new x).o(c);c=c.Le();V();V();e=W(V(),"max-","color-index");return W(0,""+e+":",b(c))}if(b&&
b.$classData&&b.$classData.p.tA){c=b.UA();b=function(){return function(a){return a.mc()}}(a);if(Hm(c))return c=c.Ge(),V(),e=W(V(),"resolution",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","resolution"),W(0,""+e+":",b(c));if(Jm(c))return c=c.Le(),V(),V(),e=W(V(),"max-","resolution"),W(0,""+e+":",b(c));throw(new x).o(c);}if(b&&b.$classData&&b.$classData.p.pA){c=b.FA();b=function(){return function(a){return""+(a|0)}}(a);if(c.l())return"monochrome";c=c.R();if(Hm(c))return c=c.Ge(),
V(),e=W(V(),"monochrome",":"),W(0,e,b(c));if(Im(c))return c=c.Me(),V(),V(),e=W(V(),"min-","monochrome"),W(0,""+e+":",b(c));if(!Jm(c))throw(new x).o(c);c=c.Le();V();V();e=W(V(),"max-","monochrome");return W(0,""+e+":",b(c))}if(b&&b.$classData&&b.$classData.p.sA)return b=b.KB(),W(V(),"orientation:",b.mc());if(b&&b.$classData&&b.$classData.p.uA)return b=b.LB(),W(V(),"scan:",b.mc());if(b&&b.$classData&&b.$classData.p.kA){b=b.MB();if(b.l())return"grid";b=b.R();V();return""+W(V(),"grid",":")+(""+(b|0))}throw(new x).o(b);
}}(this))));this.nn=t(new u,function(){return function(a){if(a&&a.$classData&&a.$classData.p.mA)return a.Mp().mc();if(a&&a.$classData&&a.$classData.p.qA)return a=a.Mp(),V(),"not "+a.mc();if(a&&a.$classData&&a.$classData.p.rA)return a=a.Mp(),V(),"only "+a.mc();throw(new x).o(a);}}(this));return this};
function Km(a,b){a=function(a){return function(b){var c;c=b.LA();var e=a.nn;if(Lm(c))c=a.Nk.g(c.F);else if(Mm(c))c=e.g(c.F);else throw(new x).o(c);b=Wl(b);for(b=Nj(b);b.Cb;)e=b.t(),V(),c=W(V(),c," and "),c=W(0,c,a.Nk.g(e));return c}}(a);var c=b.Wa,e=null,e=a(b.Qc);for(b=Nj(c);b.Cb;)c=b.t(),c=a(c),V(),e=""+W(V(),e,", ")+c;return e}function Zl(a,b){V();return"@media "+Km(a,b)}Fm.prototype.$classData=l({$t:0},!1,"scalacss.internal.Media$",{$t:1,c:1});var Gm=void 0;
function $l(){Gm||(Gm=(new Fm).b());return Gm}function Nm(){}Nm.prototype=new n;Nm.prototype.constructor=Nm;Nm.prototype.b=function(){return this};function W(a,b,c){return""+b+c}Nm.prototype.$classData=l({au:0},!1,"scalacss.internal.Media$StringExt$",{au:1,c:1});var Om=void 0;function V(){Om||(Om=(new Nm).b());return Om}function Yk(){this.Wa=this.Qc=null}Yk.prototype=new n;Yk.prototype.constructor=Yk;function Uj(a,b){var c=a.Qc;a=Kg(a.Wa,b);return Xk(new Yk,c,a)}
function Vj(a,b){if(Tj(b))return a;var c=b.D();a=Vl(Wl(b),Oj(a));return Xk(new Yk,c,a)}Yk.prototype.n=function(a){if(a&&a.$classData&&a.$classData.p.Qm&&D(G(),this.Qc,a.Qc)){var b=this.Wa;a=a.Wa;return null===b?null===a:Pm(b,a)}return!1};Yk.prototype.m=function(){var a=Oj(this);return"NonEmpty"+Qm(a)};function Oj(a){return Rm(a.Wa,a.Qc)}Yk.prototype.r=function(){return ca(31,ki(li(),this.Qc))+ki(li(),this.Wa)|0};function Xk(a,b,c){a.Qc=b;a.Wa=c;return a}
Yk.prototype.$classData=l({Qm:0},!1,"scalacss.internal.NonEmptyVector",{Qm:1,c:1});function Sm(){}Sm.prototype=new n;Sm.prototype.constructor=Sm;Sm.prototype.b=function(){return this};function Tm(a,b,c){return Tj(b)?(a=Ac().sc,Xk(new Yk,c,a.Vb)):Xk(new Yk,b.D(),Kg(Wl(b),c))}function Yj(a,b){if(Tj(b))return B();a=b.D();b=Wl(b);return(new Pf).o(Xk(new Yk,a,b))}function dk(a,b){a=Ac().sc;return Xk(new Yk,b,a.Vb)}function gm(a,b,c){return b.l()?dk(0,c):Uj(b.R(),c)}
Sm.prototype.$classData=l({bu:0},!1,"scalacss.internal.NonEmptyVector$",{bu:1,c:1});var Um=void 0;function Xj(){Um||(Um=(new Sm).b());return Um}function Vm(){this.qd=null}Vm.prototype=new n;Vm.prototype.constructor=Vm;Vm.prototype.b=function(){Wm=this;this.qd=(new Xm).b();return this};Vm.prototype.$classData=l({cu:0},!1,"scalacss.internal.Pseudo$",{cu:1,c:1});var Wm=void 0;function Ym(){}Ym.prototype=new n;Ym.prototype.constructor=Ym;Ym.prototype.b=function(){Zm=this;(new $m).b();return this};
Ym.prototype.$classData=l({eu:0},!1,"scalacss.internal.PseudoType$",{eu:1,c:1});var Zm=void 0;function an(){this.qn=null;this.Z=0}an.prototype=new n;an.prototype.constructor=an;an.prototype.b=function(){return this};function bn(){var a;cn||(cn=(new an).b());a=cn;if(0===(2&a.Z)&&0===(2&a.Z)){var b=dn(a);a.qn=(new en).zb(b);a.Z=(2|a.Z)<<24>>24}return a.qn}
function hn(a,b,c,e,f,h,m){!a.l()&&jn(m,f);!b.l()&&jn(m,f);jn(m,f);jn(m,c.Va);kn(m.wb,58);jn(m,h);a=c.s;e&&-1!==(a.indexOf(" ")|0)?(kn(m.wb,34),jn(m,a),kn(m.wb,34)):jn(m,a);jn(m,";\n")}
function dn(a){return t(new u,function(a,c,e){return function(f){return ln(new mn,t(new u,function(a,b){return function(a){jn(b,"@keyframes ");jn(b,a);jn(b," {\n")}}(a,f)),t(new u,function(a,b,c){return function(a){jn(c,b);jn(c,a);jn(c," {\n")}}(a,c,f)),t(new u,function(a,b){return function(a){jn(b,a);jn(b," {\n")}}(a,f)),Fb(function(a,b,c){return function(a,e){!a.l()&&jn(c,b);jn(c,e);jn(c," {\n")}}(a,c,f)),Xf(function(a,b,c,e){return function(a,f,h){hn(a,f,h,!1,b,c,e)}}(a,c,e,f)),Xf(function(a,b,
c,e){return function(a,f,h){hn(a,f,h,!1,b,c,e)}}(a,c,e,f)),Fb(function(a,b,c){return function(a){!a.l()&&jn(c,b);jn(c,"}\n");a.l()&&kn(c.wb,10)}}(a,c,f)),t(new u,function(a,b){return function(){jn(b,"}\n\n")}}(a,f)),t(new u,function(a,b,c){return function(){jn(c,b);jn(c,"}\n\n")}}(a,c,f)),t(new u,function(a,b){return function(){jn(b,"}\n\n")}}(a,f)),t(new u,function(a,b,c,e){return function(f){var z=function(a,b,c,e){return function(a,f,h){h=!!h;hn(B(),B(),qk(a,f),h,b,c,e)}}(a,b,c,e);jn(e,"@font-face {\n");
var E=f.xb;hn(B(),B(),qk("font-family",E),!0,b,c,e);E=Oj(f.Bc);E=Qj(E,"",",","");z("src",E,!1);E=f.zh;E.l()||(E=E.R(),z("font-stretch",E,!1));E=f.Ah;E.l()||(E=E.R(),z("font-style",E,!1));E=f.Bh;E.l()||(E=E.R(),z("font-weight",E,!1));f=f.Bi;f.l()||(f=f.R().m(),z("unicode-range",f,!1));jn(e,"}\n\n")}}(a,c,e,f)),v(new w,function(){return function(){}}(a)))}}(a,"  "," "))}an.prototype.$classData=l({iu:0},!1,"scalacss.internal.StringRenderer$",{iu:1,c:1});var cn=void 0;function nn(){}nn.prototype=new n;
nn.prototype.constructor=nn;nn.prototype.b=function(){return this};nn.prototype.$classData=l({ku:0},!1,"scalacss.internal.Style$",{ku:1,c:1});var on=void 0;function pn(){}pn.prototype=new n;pn.prototype.constructor=pn;pn.prototype.b=function(){return this};pn.prototype.$classData=l({mu:0},!1,"scalacss.internal.StyleF$",{mu:1,c:1});var qn=void 0;function rn(){this.Oj=null}rn.prototype=new n;rn.prototype.constructor=rn;
function ho(a,b){return(new rn).zb(t(new u,function(a,b){return function(f){var h=a.Oj.g(f);f=b.Oj.g(f);return t(new u,function(a,b,c){return function(a){return b.g(a).Pf(c,(Uk(),Vk().Ja))}}(a,h,f))}}(a,b)))}rn.prototype.zb=function(a){this.Oj=a;return this};function sk(a,b){return t(new u,function(a,b){return function(f){f=a.Oj.g(f);return t(new u,function(a,b,c){return function(a){return c.g(qk(b,a))}}(a,b,f))}}(a,b))}rn.prototype.$classData=l({ou:0},!1,"scalacss.internal.Transform",{ou:1,c:1});
function io(){}io.prototype=new n;io.prototype.constructor=io;io.prototype.b=function(){return this};function hl(a,b,c,e){c=e.Pp().hd(c);c=vl(wl(),t(new u,function(a,b){return function(a){return b.ca(a)}}(a,c)));return jo(a,b,c,ko().s)}
function jo(a,b,c,e){b=gl().Lo.g(b);return(new rn).zb(t(new u,function(a,b,c,e){return function(y){y=pl(gl(),y.Mo,e);return t(new u,function(a,b,c,e){return function(a){var f=gl(),h=c.Bg.g(a);if(f.No.g(h))a=ok(pk(),a);else if(f=b.g(h),f.l())a=ok(pk(),a);else{f=f.R();Uk();for(var h=Vk().Ja,h=dl(e,h),m=Nj(e);m.Cb;){var q=m.t();q.l()?q=a:(q=q.R(),q=fg(c.Tg,a,f.g(q)));h.rb(q)}a=h.La()}return a}}(a,b,c,y))}}(a,c,e,b)))}io.prototype.$classData=l({pu:0},!1,"scalacss.internal.Transform$",{pu:1,c:1});
var lo=void 0;function il(){lo||(lo=(new io).b());return lo}function mo(){}mo.prototype=new n;mo.prototype.constructor=mo;function no(){}no.prototype=mo.prototype;function oo(a,b){return(new po).zb(t(new u,function(a,b){return function(f){return qo(b,qo(a,f))}}(a,b)))}function ro(){}ro.prototype=new n;ro.prototype.constructor=ro;ro.prototype.b=function(){return this};function so(a,b){return(new po).zb(t(new u,function(a,b){return function(a){return(new to).i(b.g(a))}}(a,b)))}
ro.prototype.$classData=l({su:0},!1,"scalacss.internal.ValueT$Rule$",{su:1,c:1});var uo=void 0;function vo(){uo||(uo=(new ro).b());return uo}function wo(){}wo.prototype=new n;wo.prototype.constructor=wo;function xo(){}xo.prototype=wo.prototype;function yo(){}yo.prototype=new n;yo.prototype.constructor=yo;function zo(){}zo.prototype=yo.prototype;yo.prototype.m=function(){return this.Yd().Uf};function Ao(){}Ao.prototype=new n;Ao.prototype.constructor=Ao;Ao.prototype.b=function(){return this};
Ao.prototype.$classData=l({yu:0},!1,"scalacss.internal.mutable.Mutex",{yu:1,c:1});function Bo(){this.dg=null}Bo.prototype=new n;Bo.prototype.constructor=Bo;Bo.prototype.b=function(){Co=this;this.dg=(new Ao).b();return this};Bo.prototype.$classData=l({zu:0},!1,"scalacss.internal.mutable.Mutex$",{zu:1,c:1});var Co=void 0;function Do(){Co||(Co=(new Bo).b());return Co}function Eo(){this.Ji=this.jk=this.ah=this.kk=this.dg=this.Kn=this.to=null;this.$g=!1}Eo.prototype=new n;Eo.prototype.constructor=Eo;
function Fo(a){a.$g=!0;return a.ah}function Go(a,b,c,e){if(c.Hd.l()&&Tj(c.Vd))return c;var f=a.to;if(null===b)throw(new Ho).b();""===b?e=B():(b=lh(Ug(new Vg,f.mn.Ng,b,Ga(b)),"_"),e=(new Pf).o((new Io).i(Cl(Dl((new A).f(["","-",""])),(new A).f([e.s,b])))));e.l()?a=B():(e=e.R(),a=(new Pf).o(Jo(a,e)));return Ko(new Lo,c.Hd,c.Vd,a,c.id,c.Ve)}
function Mo(a,b){for(var c=Nj(a.ah),e=!1;!e&&c.Cb;)e=c.t().Hf,ej(),e=D(G(),e,b);if(e)return!0;a=Nj(a.jk);for(c=!1;!c&&a.Cb;)c=a.t().Nv(),ej(),c=D(G(),c,b);return c}function No(a,b,c,e){var f=Do().dg;a.to=c;a.Kn=e;a.dg=f;a.kk=b;a.ah=Ac().sc.Vb;a.jk=Ac().sc.Vb;a.Ji=Ac().sc.Vb;a.$g=!1;return a}function Jo(a,b){if(Mo(a,b))a:{var c=2;for(;;){var e=(new Io).i(Cl(Dl((new A).f(["","-",""])),(new A).f([b.s,c])));if(Mo(a,e))c=1+c|0;else{a=e;break a}}}else a=b;return a}function Oo(a){a.$g=!0;return a.Ji}
function Po(a,b,c){var e=b.xb;c=e.l()?Qo(a,c).s:e.R();var e=new Am,f=b.Bc,h=b.Qf,m=b.Rf,q=b.Sf;b=b.ig;e.xb=c;e.Bc=f;e.Qf=h;e.Rf=m;e.Sf=q;e.ig=b;a.Ji=Kg(a.Ji,e);return e}function Ro(a){a.$g=!0;return a.jk}
function So(a,b,c){if(!b.id.l()&&b.Hd.l()&&b.If.l()&&Tj(b.Vd)){var e=b.id.D(),f=Wl(b.id),e=(new Pf).o(e);b=Ko(new Lo,b.Hd,b.Vd,e,f,b.Ve)}f=b.If;c=f.l()?Qo(a,c):f.R();f=To(a.Kn).F;a.$g&&fg(f,c,Uo(new Vo,Wo().Y,"Style being registered after stylesheet has been rendered (via .render), or extracted (via .styles)."));Mo(a,c)&&fg(f,c,Uo(new Vo,Wo().Y,"Another style in the register has the same classname."));for(e=Nj(b.Ve);e.Cb;){var h=e.t();fg(f,c,h)}c=Xo(new Yo,c,b.id,b);a.ah=Kg(a.ah,c);return c}
function Qo(a,b){var c;c=a.kk;Ca();b=b.s;var e=c.tl;b=(new Io).i(Zo(c.Yk,ia(r(p),[b,e])));c=(new $o).Ih(c.Yk,1+c.tl|0);c=(new H).d(b,c);if(null===c)throw(new x).o(c);b=c.N;a.kk=c.P;return b}Eo.prototype.$classData=l({Au:0},!1,"scalacss.internal.mutable.Register",{Au:1,c:1});function ap(){}ap.prototype=new n;ap.prototype.constructor=ap;ap.prototype.b=function(){return this};
function bp(){var a;cp||(cp=(new ap).b());a=cp;dp||(dp=(new ep).b());return(new fp).zb(t(new u,function(a,c){return function(a){gp(c,null===a?"null":a);gp(c,"\n")}}(a,dp.Ln.Yg)))}ap.prototype.$classData=l({Bu:0},!1,"scalacss.internal.mutable.Register$ErrorHandler$",{Bu:1,c:1});var cp=void 0;function hp(){this.wg=this.ij=null}hp.prototype=new n;hp.prototype.constructor=hp;function ip(){}ip.prototype=hp.prototype;
hp.prototype.b=function(){Ca();Ca();Ca();var a=Jb(la(this));this.wg=(new jp).i(kp(0,Fg(0,Fg(0,Fg(Ca(),a,"^\\$line.+[.$]\\$iw[.$]",""),"\\$+$",""),"^(?:.+\\.)(.+?)$","$1"),"\\$+","_"));return this};function lp(a){if(null===a.ij&&null===a.ij){var b=new mp;if(null===a)throw np(Xh(),null);b.dc=a;op.prototype.b.call(b);a.ij=b}return a.ij}function pp(){this.Li=this.Zg=this.dg=null}pp.prototype=new n;pp.prototype.constructor=pp;function qp(){}qp.prototype=pp.prototype;
function rp(a,b){sp(a,t(new u,function(a,b){return function(a){a=Nj(a);tp(a,b)}}(a,b)))}pp.prototype.Su=function(a){this.dg=a;this.Zg=Ac().sc.Vb;this.Li=t(new u,function(){return function(){}}(this));return this};function sp(a,b){!a.Zg.l()&&b.g(a.Zg);a.Li=t(new u,function(a,b,f){return function(a){f.g(a);b.g(a)}}(a,b,a.Li))}function up(a,b){b=b.Af();a.Zg=Vl(a.Zg,b);a.Li.g(b)}function vp(){}vp.prototype=new n;vp.prototype.constructor=vp;vp.prototype.b=function(){return this};
function ok(a,b){return Kg(Ac().sc.Vb,b)}function el(a,b){var c=(new wp).b();return t(new u,function(a,b,c){return function(a){var e=c.yb(a);if(e.l()){e=b.g(a);a=xp(c,a,e);if(null===a)B();else{var y=a.s;a.s=e;(new Pf).o(y)}return e}return e.R()}}(a,b,c))}vp.prototype.$classData=l({Ku:0},!1,"scalacss.internal.package$",{Ku:1,c:1});var yp=void 0;function pk(){yp||(yp=(new vp).b());return yp}function Ua(){this.sb=null}Ua.prototype=new n;Ua.prototype.constructor=Ua;function Jb(a){return a.sb.name}
Ua.prototype.m=function(){return(this.sb.isInterface?"interface ":this.sb.isPrimitive?"":"class ")+Jb(this)};Ua.prototype.$classData=l({bv:0},!1,"java.lang.Class",{bv:1,c:1});function zp(){this.Jn=this.Eo=null}zp.prototype=new n;zp.prototype.constructor=zp;zp.prototype.b=function(){Ap=this;this.Eo=Bp(!1);this.Jn=Bp(!0);return this};zp.prototype.$classData=l({sv:0},!1,"java.lang.System$",{sv:1,c:1});var Ap=void 0;function Cp(){Ap||(Ap=(new zp).b());return Ap}function Dp(){}Dp.prototype=new n;
Dp.prototype.constructor=Dp;Dp.prototype.b=function(){return this};Dp.prototype.$classData=l({uv:0},!1,"java.util.Arrays$",{uv:1,c:1});var Ep=void 0;function Fp(){this.ko=null}Fp.prototype=new n;Fp.prototype.constructor=Fp;Fp.prototype.b=function(){Gp=this;this.ko=new g.RegExp("(?:(\\d+)\\$)?([-#+ 0,\\(\x3c]*)(\\d+)?(?:\\.(\\d+))?[%A-Za-z]","g");return this};Fp.prototype.$classData=l({zv:0},!1,"java.util.Formatter$",{zv:1,c:1});var Gp=void 0;function Hp(){}Hp.prototype=new n;
Hp.prototype.constructor=Hp;function Ip(){}Ip.prototype=Hp.prototype;function Jp(){}Jp.prototype=new n;Jp.prototype.constructor=Jp;function Kp(){}Kp.prototype=Jp.prototype;function Lp(){}Lp.prototype=new n;Lp.prototype.constructor=Lp;function Mp(){}Mp.prototype=Lp.prototype;function Np(a,b,c){return a.Gc(b)?a.g(b):c.g(b)}function Op(){this.Hn=this.Pg=null}Op.prototype=new n;Op.prototype.constructor=Op;Op.prototype.b=function(){Pp=this;this.Pg=(new Qp).b();this.Hn=(new Rp).b();return this};
Op.prototype.$classData=l({Vv:0},!1,"scala.PartialFunction$",{Vv:1,c:1});var Pp=void 0;function Sp(){Pp||(Pp=(new Op).b());return Pp}function wi(){}wi.prototype=new n;wi.prototype.constructor=wi;wi.prototype.b=function(){return this};wi.prototype.$classData=l({dw:0},!1,"scala.Predef$any2stringadd$",{dw:1,c:1});var vi=void 0;function Tp(){}Tp.prototype=new n;Tp.prototype.constructor=Tp;Tp.prototype.b=function(){return this};Tp.prototype.$classData=l({lw:0},!1,"scala.math.Ordered$",{lw:1,c:1});
var Up=void 0;function Vp(){this.sc=null;this.Z=0}Vp.prototype=new n;Vp.prototype.constructor=Vp;
Vp.prototype.b=function(){Wp=this;(new Xp).b();Yp||(Yp=(new Zp).b());$p();aq();Vk();bq();hg();xb();cq||(cq=(new dq).b());eq||(eq=(new fq).b());gq||(gq=(new hq).b());iq();jq||(jq=(new kq).b());this.sc=Uk();lq||(lq=(new mq).b());nq||(nq=(new oq).b());pq||(pq=(new qq).b());rq||(rq=(new sq).b());tq||(tq=(new uq).b());vq||(vq=(new wq).b());Up||(Up=(new Tp).b());xq||(xq=(new yq).b());zq||(zq=(new Aq).b());Bq||(Bq=(new Cq).b());Dq||(Dq=(new Eq).b());return this};
Vp.prototype.$classData=l({qw:0},!1,"scala.package$",{qw:1,c:1});var Wp=void 0;function Ac(){Wp||(Wp=(new Vp).b());return Wp}function Fq(){this.Df=null}Fq.prototype=new n;Fq.prototype.constructor=Fq;
Fq.prototype.b=function(){Gq=this;Hq||(Hq=(new Iq).b());Jq||(Jq=(new Kq).b());Lq||(Lq=(new Mq).b());Nq||(Nq=(new Oq).b());Pq||(Pq=(new Qq).b());Rq||(Rq=(new Sq).b());Tq||(Tq=(new Uq).b());Vq||(Vq=(new Wq).b());Xq||(Xq=(new Yq).b());this.Df=Xq;Zq||(Zq=(new $q).b());ar||(ar=(new br).b());cr||(cr=(new dr).b());er||(er=(new fr).b());gr||(gr=(new hr).b());return this};Fq.prototype.$classData=l({sw:0},!1,"scala.reflect.ClassManifestFactory$",{sw:1,c:1});var Gq=void 0;function ir(){}ir.prototype=new n;
ir.prototype.constructor=ir;ir.prototype.b=function(){return this};ir.prototype.$classData=l({tw:0},!1,"scala.reflect.ManifestFactory$",{tw:1,c:1});var jr=void 0;function kr(){}kr.prototype=new n;kr.prototype.constructor=kr;kr.prototype.b=function(){lr=this;Gq||(Gq=(new Fq).b());jr||(jr=(new ir).b());return this};kr.prototype.$classData=l({Jw:0},!1,"scala.reflect.package$",{Jw:1,c:1});var lr=void 0;function mr(){}mr.prototype=new n;mr.prototype.constructor=mr;mr.prototype.b=function(){return this};
mr.prototype.$classData=l({Kw:0},!1,"scala.sys.package$",{Kw:1,c:1});var nr=void 0;function or(){this.Yg=null}or.prototype=new n;or.prototype.constructor=or;or.prototype.m=function(){return"DynamicVariable("+this.Yg+")"};or.prototype.o=function(a){this.Yg=a;return this};or.prototype.$classData=l({Lw:0},!1,"scala.util.DynamicVariable",{Lw:1,c:1});function pr(){}pr.prototype=new n;pr.prototype.constructor=pr;pr.prototype.b=function(){(new qr).b();return this};
pr.prototype.$classData=l({Rw:0},!1,"scala.util.control.Breaks",{Rw:1,c:1});function rr(){}rr.prototype=new n;rr.prototype.constructor=rr;function sr(){}sr.prototype=rr.prototype;function tr(a,b){b=ca(-862048943,b);b=ca(461845907,b<<15|b>>>17|0);return a^b}function bs(a,b){a=tr(a,b);return-430675100+ca(5,a<<13|a>>>19|0)|0}function cs(a){a=ca(-2048144789,a^(a>>>16|0));a=ca(-1028477387,a^(a>>>13|0));return a^(a>>>16|0)}
function ds(a,b,c){var e=(new es).kb(0),f=(new es).kb(0),h=(new es).kb(0),m=(new es).kb(1);b.O(t(new u,function(a,b,c,e,f){return function(a){a=ki(li(),a);b.x=b.x+a|0;c.x^=a;0!==a&&(f.x=ca(f.x,a));e.x=1+e.x|0}}(a,e,f,h,m)));a=bs(c,e.x);a=bs(a,f.x);a=tr(a,m.x);return cs(a^h.x)}function um(a,b){a=b.y();if(0===a)return b=b.A(),Ba(Ca(),b);for(var c=-889275714,e=0;e<a;)c=bs(c,ki(li(),b.z(e))),e=1+e|0;return cs(c^a)}
function fs(a,b,c){var e=(new es).kb(0);c=(new es).kb(c);b.O(t(new u,function(a,b,c){return function(a){c.x=bs(c.x,ki(li(),a));b.x=1+b.x|0}}(a,e,c)));return cs(c.x^e.x)}function gs(){}gs.prototype=new n;gs.prototype.constructor=gs;gs.prototype.b=function(){return this};gs.prototype.$classData=l({Uw:0},!1,"scala.util.hashing.package$",{Uw:1,c:1});var hs=void 0;function hq(){}hq.prototype=new n;hq.prototype.constructor=hq;hq.prototype.b=function(){return this};
hq.prototype.$classData=l({Ww:0},!1,"scala.collection.$colon$plus$",{Ww:1,c:1});var gq=void 0;function fq(){}fq.prototype=new n;fq.prototype.constructor=fq;fq.prototype.b=function(){return this};fq.prototype.$classData=l({Xw:0},!1,"scala.collection.$plus$colon$",{Xw:1,c:1});var eq=void 0;function is(){this.Y=null}is.prototype=new n;is.prototype.constructor=is;is.prototype.b=function(){js=this;this.Y=(new ks).b();return this};is.prototype.$classData=l({bx:0},!1,"scala.collection.Iterator$",{bx:1,c:1});
var js=void 0;function bq(){js||(js=(new is).b());return js}function ls(){this.Wa=this.Qc=null}ls.prototype=new n;ls.prototype.constructor=ls;ls.prototype.$classData=l({hx:0},!1,"scala.collection.Iterator$ConcatIteratorCell",{hx:1,c:1});function Gl(a,b){b=b.sg();b.fb(a.Ga());return b.La()}function Qj(a,b,c,e){return a.Ye((new ms).b(),b,c,e).wb.va}function ns(a,b,c){b=(new Sf).o(b);a.O(t(new u,function(a,b,c){return function(a){c.x=fg(b,c.x,a)}}(a,c,b)));return b.x}
function os(a){var b=(new es).kb(0);a.O(t(new u,function(a,b){return function(){b.x=1+b.x|0}}(a,b)));return b.x}function ps(a,b,c,e,f){var h=qs(!0);jn(b,c);a.O(t(new u,function(a,b,c,e){return function(a){if(e.x)rs(b,a),e.x=!1;else return jn(b,c),rs(b,a)}}(a,b,e,h)));jn(b,f);return b}function ss(){}ss.prototype=new n;ss.prototype.constructor=ss;function ts(){}ts.prototype=ss.prototype;function Ik(a,b){return a.da().fb(b).La()}ss.prototype.da=function(){return jl(new kl,this.In())};
function us(){}us.prototype=new n;us.prototype.constructor=us;function vs(){}vs.prototype=us.prototype;function K(a,b){if(b.l())return a.vh();a=a.da();a.fb(b);return a.La()}us.prototype.vh=function(){return this.da().La()};function ws(a,b){a:for(;;){if(!b.l()){a.rc(b.D());b=b.u();continue a}break}}function Tl(a,b){b&&b.$classData&&b.$classData.p.ni?ws(a,b):b.O(t(new u,function(a){return function(b){return a.rc(b)}}(a)));return a}function xs(){}xs.prototype=new n;xs.prototype.constructor=xs;
function ys(){}ys.prototype=xs.prototype;function kq(){}kq.prototype=new n;kq.prototype.constructor=kq;kq.prototype.b=function(){return this};kq.prototype.$classData=l({my:0},!1,"scala.collection.immutable.Stream$$hash$colon$colon$",{my:1,c:1});var jq=void 0;function zs(){this.Ml=this.Yg=null;this.Z=!1;this.ya=null}zs.prototype=new n;zs.prototype.constructor=zs;function As(a,b,c){a.Ml=c;if(null===b)throw np(Xh(),null);a.ya=b;return a}
function Bs(a){a.Z||(a.Z||(a.Yg=a.Ml.U(),a.Z=!0),a.Ml=null);return a.Yg}zs.prototype.$classData=l({qy:0},!1,"scala.collection.immutable.StreamIterator$LazyCell",{qy:1,c:1});function Cs(){}Cs.prototype=new n;Cs.prototype.constructor=Cs;Cs.prototype.b=function(){return this};function Ds(a,b,c,e){a=0>c?0:c;return e<=a||a>=(b.length|0)?"":b.substring(a,e>(b.length|0)?b.length|0:e)}Cs.prototype.$classData=l({ry:0},!1,"scala.collection.immutable.StringOps$",{ry:1,c:1});var Es=void 0;
function Fs(){Es||(Es=(new Cs).b());return Es}function Gs(){}Gs.prototype=new n;Gs.prototype.constructor=Gs;Gs.prototype.b=function(){return this};Gs.prototype.da=function(){var a=(new ms).b();return Hs(new Is,a,t(new u,function(){return function(a){return(new Js).i(a)}}(this)))};Gs.prototype.$classData=l({zy:0},!1,"scala.collection.immutable.WrappedString$",{zy:1,c:1});var Ks=void 0;function Ls(a,b,c){for(a=a.Jb.e[c];;)if(null!==a?(c=a.Va,c=!D(G(),c,b)):c=!1,c)a=a.xc;else break;return a}
function Ms(a,b){var c=-1+a.Jb.e.length|0,e=da(c);a=a.Kl;hs||(hs=(new gs).b());b=ca(-1640532531,b);Ns();b=ca(-1640532531,b<<24|16711680&b<<8|65280&(b>>>8|0)|b>>>24|0);return((b>>>a|0|b<<(-a|0))>>>e|0)&c}function Os(a){for(var b=-1+a.Jb.e.length|0;null===a.Jb.e[b]&&0<b;)b=-1+b|0;return b}
function xp(a,b,c){var e=ki(li(),b),e=Ms(a,e),f=Ls(a,b,e);if(null!==f)return f;b=(new Ps).d(b,c);b.xc=a.Jb.e[e];a.Jb.e[e]=b;a.me=1+a.me|0;Qs(a,e);if(a.me>a.$j){b=a.Jb.e.length<<1;c=a.Jb;a.Jb=k(r(Wb),[b]);if(null!==a.le)if(e=1+(a.Jb.e.length>>5)|0,a.le.e.length!==e)a.le=k(r(Za),[e]);else{Ep||(Ep=(new Dp).b());for(var e=a.le,f=e.e.length,h=0;h!==f;)e.e[h]=0,h=1+h|0}for(e=-1+c.e.length|0;0<=e;){for(f=c.e[e];null!==f;){var h=f.Va,h=ki(li(),h),h=Ms(a,h),m=f.xc;f.xc=a.Jb.e[h];a.Jb.e[h]=f;f=m;Qs(a,h)}e=
-1+e|0}a.$j=Rs(Ss(),a.Ki,b)}return null}function Ts(a,b){var c=ki(li(),b),c=Ms(a,c);return Ls(a,b,c)}function Us(a,b){null!==a.le&&(a=a.le,b>>=5,a.e[b]=-1+a.e[b]|0)}function Qs(a,b){null!==a.le&&(a=a.le,b>>=5,a.e[b]=1+a.e[b]|0)}function Vs(){}Vs.prototype=new n;Vs.prototype.constructor=Vs;Vs.prototype.b=function(){return this};
function Rs(a,b,c){var e=c>>31,f=b>>31;a=65535&c;var h=c>>>16|0,m=65535&b,q=b>>>16|0,y=ca(a,m),m=ca(h,m),N=ca(a,q);a=y+((m+N|0)<<16)|0;y=(y>>>16|0)+N|0;f=(((ca(c,f)+ca(e,b)|0)+ca(h,q)|0)+(y>>>16|0)|0)+(((65535&y)+m|0)>>>16|0)|0;b=Qa();f===a>>31?(a=a/1E3|0,b.he=a>>31):((c=0>f)?(e=-a|0,a=0!==a?~f:-f|0):(e=a,a=f),0===(-2097152&a)?(a=(4294967296*a+ +(e>>>0))/1E3,b.he=a/4294967296|0,a|=0):a=Ws(b,e,a,1E3,0)|0,!1!==c&&(c=b.he,b.he=0!==a?~c:-c|0,a=-a|0));return a}
Vs.prototype.$classData=l({Jy:0},!1,"scala.collection.mutable.HashTable$",{Jy:1,c:1});var Xs=void 0;function Ss(){Xs||(Xs=(new Vs).b());return Xs}function Ys(){this.gg=!1;this.Pn=this.qj=this.ih=null;this.Fk=!1;this.ro=this.Wn=0}Ys.prototype=new n;Ys.prototype.constructor=Ys;
Ys.prototype.b=function(){Zs=this;this.ih=(this.gg=!!(g.ArrayBuffer&&g.Int32Array&&g.Float32Array&&g.Float64Array))?new g.ArrayBuffer(8):null;this.qj=this.gg?new g.Int32Array(this.ih,0,2):null;this.gg&&new g.Float32Array(this.ih,0,2);this.Pn=this.gg?new g.Float64Array(this.ih,0,1):null;if(this.gg)this.qj[0]=16909060,a=1===((new g.Int8Array(this.ih,0,8))[0]|0);else var a=!0;this.Wn=(this.Fk=a)?0:1;this.ro=this.Fk?1:0;return this};
function Da(a,b){var c=b|0;if(c===b&&-Infinity!==1/b)return c;if(a.gg)a.Pn[0]=b,a=(new $s).Ie(a.qj[a.ro]|0,a.qj[a.Wn]|0);else{if(b!==b)a=!1,b=2047,c=+g.Math.pow(2,51);else if(Infinity===b||-Infinity===b)a=0>b,b=2047,c=0;else if(0===b)a=-Infinity===1/b,c=b=0;else{var e=(a=0>b)?-b:b;if(e>=+g.Math.pow(2,-1022)){b=+g.Math.pow(2,52);var c=+g.Math.log(e)/.6931471805599453,c=+g.Math.floor(c)|0,c=1023>c?c:1023,f=+g.Math.pow(2,c);f>e&&(c=-1+c|0,f/=2);f=e/f*b;e=+g.Math.floor(f);f-=e;e=.5>f?e:.5<f?1+e:0!==e%
2?1+e:e;2<=e/b&&(c=1+c|0,e=1);1023<c?(c=2047,e=0):(c=1023+c|0,e-=b);b=c;c=e}else b=e/+g.Math.pow(2,-1074),c=+g.Math.floor(b),e=b-c,b=0,c=.5>e?c:.5<e?1+c:0!==c%2?1+c:c}c=+c;a=(new $s).Ie(c|0,(a?-2147483648:0)|(b|0)<<20|c/4294967296|0)}return a.bb^a.ic}Ys.prototype.$classData=l({bz:0},!1,"scala.scalajs.runtime.Bits$",{bz:1,c:1});var Zs=void 0;function Ea(){Zs||(Zs=(new Ys).b());return Zs}function at(){this.Z=!1}at.prototype=new n;at.prototype.constructor=at;at.prototype.b=function(){return this};
function Fg(a,b,c,e){if(null===b)throw(new Ho).b();a=bt(ct(),c);b=Ug(new Vg,a,b,b.length|0);dt(b);et(b)?(a=(new ft).b(),gt(b,a,e),ht(b,a),e=a.m()):e=b.Zf;return e}function it(){return g.String.fromCharCode(92)}function Ba(a,b){a=0;for(var c=1,e=-1+(b.length|0)|0;0<=e;)a=a+ca(65535&(b.charCodeAt(e)|0),c)|0,c=ca(31,c),e=-1+e|0;return a}
function Zo(a,b){var c=(new jt).b();if(c.Mh)throw(new kt).b();for(var e=0,f=0,h=a.length|0,m=0;m!==h;){var q=a.indexOf("%",m)|0;if(0>q){lt(c,a.substring(m));break}lt(c,a.substring(m,q));m=1+q|0;Gp||(Gp=(new Fp).b());var y=Gp.ko;y.lastIndex=m;q=y.exec(a);if(null===q||(q.index|0)!==m)throw c=m===h?"%":a.substring(m,1+m|0),(new mt).i(c);for(var m=y.lastIndex|0,y=65535&(a.charCodeAt(-1+m|0)|0),N,z=q[2],E=90>=y?256:0,C=z.length|0,F=0;F!==C;){N=65535&(z.charCodeAt(F)|0);switch(N){case 45:var X=1;break;
case 35:X=2;break;case 43:X=4;break;case 32:X=8;break;case 48:X=16;break;case 44:X=32;break;case 40:X=64;break;case 60:X=128;break;default:throw(new x).o(nt(N));}if(0!==(E&X))throw(new ot).i(g.String.fromCharCode(N));E|=X;F=1+F|0}N=E;C=pt(q[3],-1);E=pt(q[4],-1);if(37===y||110===y)q=null;else{if(0!==(1&N)&&0>C)throw(new qt).i("%"+q[0]);0!==(128&N)?z=f:(z=pt(q[1],0),z=0===z?e=1+e|0:0>z?f:z);if(0>=z||z>b.e.length){c=g.String.fromCharCode(y);if(0>("bBhHsHcCdoxXeEgGfn%".indexOf(c)|0))throw(new mt).i(c);
throw(new rt).i("%"+q[0]);}f=z;q=b.e[-1+z|0]}z=c;F=q;X=y;q=N;y=C;C=E;switch(X){case 98:case 66:0!==(126&q)&&st(q,X,126);tt(z,q,y,C,!1===F||null===F?"false":"true");break;case 104:case 72:0!==(126&q)&&st(q,X,126);E=null===F?"null":(+(Aa(F)>>>0)).toString(16);tt(z,q,y,C,E);break;case 115:case 83:F&&F.$classData&&F.$classData.p.OA?(0!==(124&q)&&st(q,X,124),F.IA(z,(0!==(1&q)?1:0)|(0!==(2&q)?4:0)|(0!==(256&q)?2:0),y,C)):(0!==(126&q)&&st(q,X,126),tt(z,q,y,C,""+F));break;case 99:case 67:0!==(126&q)&&st(q,
X,126);if(0<=C)throw(new ut).kb(C);if(vt(F))tt(z,q,y,-1,g.String.fromCharCode(null===F?0:F.s));else if(Oa(F)){E=F|0;if(!(0<=E&&1114111>=E))throw(new wt).kb(E);E=65536>E?g.String.fromCharCode(E):g.String.fromCharCode(-64+(E>>10)|55296,56320|1023&E);tt(z,q,y,-1,E)}else xt(z,F,X,q,y,C);break;case 100:0!==(2&q)&&st(q,X,2);17!==(17&q)&&12!==(12&q)||yt(q);if(0<=C)throw(new ut).kb(C);Oa(F)?zt(z,q,y,""+(F|0)):xa(F)?(C=Pa(F),E=C.bb,C=C.ic,zt(z,q,y,Hb(Qa(),E,C))):xt(z,F,X,q,y,C);break;case 111:0!==(108&q)&&
st(q,X,108);17===(17&q)&&yt(q);if(0<=C)throw(new ut).kb(C);E=0!==(2&q)?"0":"";Oa(F)?(C=(+((F|0)>>>0)).toString(8),At(z,q,y,E,C)):xa(F)?(C=Pa(F),F=C.bb,N=C.ic,Bt||(Bt=(new Ct).b()),C=1073741823&F,X=1073741823&((F>>>30|0)+(N<<2)|0),F=N>>>28|0,0!==F?(F=(+(F>>>0)).toString(8),N=(+(X>>>0)).toString(8),X="0000000000".substring(N.length|0),C=(+(C>>>0)).toString(8),C=F+(""+X+N)+(""+"0000000000".substring(C.length|0)+C)):0!==X?(F=(+(X>>>0)).toString(8),C=(+(C>>>0)).toString(8),C=F+(""+"0000000000".substring(C.length|
0)+C)):C=(+(C>>>0)).toString(8),At(z,q,y,E,C)):xt(z,F,X,q,y,C);break;case 120:case 88:0!==(108&q)&&st(q,X,108);17===(17&q)&&yt(q);if(0<=C)throw(new ut).kb(C);E=0===(2&q)?"":0!==(256&q)?"0X":"0x";Oa(F)?(C=(+((F|0)>>>0)).toString(16),At(z,q,y,E,Dt(q,C))):xa(F)?(C=Pa(F),F=C.bb,N=C.ic,Bt||(Bt=(new Ct).b()),C=q,0!==N?(N=(+(N>>>0)).toString(16),F=(+(F>>>0)).toString(16),F=N+(""+"00000000".substring(F.length|0)+F)):F=(+(F>>>0)).toString(16),At(z,C,y,E,Dt(q,F))):xt(z,F,X,q,y,C);break;case 101:case 69:0!==
(32&q)&&st(q,X,32);17!==(17&q)&&12!==(12&q)||yt(q);"number"===typeof F?(E=+F,E!==E||Infinity===E||-Infinity===E?Et(z,q,y,E):zt(z,q,y,Ft(E,0<=C?C:6,0!==(2&q)))):xt(z,F,X,q,y,C);break;case 103:case 71:0!==(2&q)&&st(q,X,2);17!==(17&q)&&12!==(12&q)||yt(q);"number"===typeof F?(F=+F,F!==F||Infinity===F||-Infinity===F?Et(z,q,y,F):(E=q,N=0<=C?C:6,q=0!==(2&q),C=+g.Math.abs(F),N=0===N?1:N,1E-4<=C&&C<+g.Math.pow(10,N)?(X=void 0!==g.Math.log10?+g.Math.log10(C):+g.Math.log(C)/2.302585092994046,X=Ia(+g.Math.ceil(X)),
C=+g.Math.pow(10,X)<=C?1+X|0:X,C=N-C|0,q=Gt(F,0<C?C:0,q)):q=Ft(F,-1+N|0,q),zt(z,E,y,q))):xt(z,F,X,q,y,C);break;case 102:17!==(17&q)&&12!==(12&q)||yt(q);"number"===typeof F?(E=+F,E!==E||Infinity===E||-Infinity===E?Et(z,q,y,E):zt(z,q,y,Gt(E,0<=C?C:6,0!==(2&q)))):xt(z,F,X,q,y,C);break;case 37:if(0!==(254&q))throw(new Ht).i(It(q));if(0<=C)throw(new ut).kb(C);if(0!==(1&q)&&0>y)throw(new qt).i("%-%");Jt(z,q,y,"%");break;case 110:if(0!==(255&q))throw(new Ht).i(It(q));if(0<=C)throw(new ut).kb(C);if(0<=y)throw(new Kt).kb(y);
lt(z,"\n");break;default:throw(new mt).i(g.String.fromCharCode(X));}}a=c.m();c.$i();return a}function kp(a,b,c,e){if(null===b)throw(new Ho).b();a=bt(ct(),c);return lh(Ug(new Vg,a,b,b.length|0),e)}at.prototype.$classData=l({dz:0},!1,"scala.scalajs.runtime.RuntimeString$",{dz:1,c:1});var Lt=void 0;function Ca(){Lt||(Lt=(new at).b());return Lt}function Mt(){}Mt.prototype=new n;Mt.prototype.constructor=Mt;Mt.prototype.b=function(){return this};function np(a,b){return Nt(b)?b.gf:b}
function Wh(a,b){return b&&b.$classData&&b.$classData.p.Ua?b:(new Ot).o(b)}Mt.prototype.$classData=l({ez:0},!1,"scala.scalajs.runtime.package$",{ez:1,c:1});var Pt=void 0;function Xh(){Pt||(Pt=(new Mt).b());return Pt}function Qt(){}Qt.prototype=new n;Qt.prototype.constructor=Qt;Qt.prototype.b=function(){return this};
function Rt(a,b){if(vt(b))return a.s===b.s;if(St(b)){if("number"===typeof b)return+b===a.s;if(xa(b)){b=Pa(b);var c=b.ic;a=a.s;return b.bb===a&&c===a>>31}return null===b?null===a:za(b,a)}return null===a&&null===b}function D(a,b,c){if(b===c)c=!0;else if(St(b))a:if(St(c))c=Tt(b,c);else{if(vt(c)){if("number"===typeof b){c=+b===c.s;break a}if(xa(b)){a=Pa(b);b=a.ic;c=c.s;c=a.bb===c&&b===c>>31;break a}}c=null===b?null===c:za(b,c)}else c=vt(b)?Rt(b,c):null===b?null===c:za(b,c);return c}
function Tt(a,b){if("number"===typeof a){a=+a;if("number"===typeof b)return a===+b;if(xa(b)){var c=Pa(b);b=c.bb;c=c.ic;return a===Ut(Qa(),b,c)}return b&&b.$classData&&b.$classData.p.pw?b.n(a):!1}if(xa(a)){c=Pa(a);a=c.bb;c=c.ic;if(xa(b)){b=Pa(b);var e=b.ic;return a===b.bb&&c===e}return"number"===typeof b?(b=+b,Ut(Qa(),a,c)===b):b&&b.$classData&&b.$classData.p.pw?b.n((new $s).Ie(a,c)):!1}return null===a?null===b:za(a,b)}Qt.prototype.$classData=l({iz:0},!1,"scala.runtime.BoxesRunTime$",{iz:1,c:1});
var Vt=void 0;function G(){Vt||(Vt=(new Qt).b());return Vt}var Wt=l({lz:0},!1,"scala.runtime.Null$",{lz:1,c:1});function Xt(){}Xt.prototype=new n;Xt.prototype.constructor=Xt;Xt.prototype.b=function(){return this};function Yt(a,b){if(Kb(b,1)||gb(b,1)||jb(b,1)||hb(b,1)||ib(b,1)||db(b,1)||eb(b,1)||fb(b,1)||cb(b,1)||Zt(b))return b.e.length;if(null===b)throw(new Ho).b();throw(new x).o(b);}
function $t(a,b,c,e){if(Kb(b,1))b.e[c]=e;else if(gb(b,1))b.e[c]=e|0;else if(jb(b,1))b.e[c]=+e;else if(hb(b,1))b.e[c]=Pa(e);else if(ib(b,1))b.e[c]=+e;else if(db(b,1))b.e[c]=null===e?0:e.s;else if(eb(b,1))b.e[c]=e|0;else if(fb(b,1))b.e[c]=e|0;else if(cb(b,1))b.e[c]=!!e;else if(Zt(b))b.e[c]=void 0;else{if(null===b)throw(new Ho).b();throw(new x).o(b);}}function al(a,b){a=b.B();return Qj(a,b.A()+"(",",",")")}Xt.prototype.$classData=l({nz:0},!1,"scala.runtime.ScalaRunTime$",{nz:1,c:1});var au=void 0;
function P(){au||(au=(new Xt).b());return au}function bu(){}bu.prototype=new n;bu.prototype.constructor=bu;bu.prototype.b=function(){return this};
function ki(a,b){if(null===b)return 0;if("number"===typeof b){a=+b;b=Ia(a);if(b===a)a=b;else{b=Qa();var c;if(-9223372036854775808>a)b.he=-2147483648,c=0;else if(0x7fffffffffffffff<=a)b.he=2147483647,c=-1;else{c=a|0;var e=a/4294967296|0;b.he=0>a&&0!==c?-1+e|0:e}b=b.he;a=Ut(Qa(),c,b)===a?c^b:Da(Ea(),a)}return a}return xa(b)?(a=Pa(b),b=(new $s).Ie(a.bb,a.ic),a=b.bb,b=b.ic,b===a>>31?a:a^b):Aa(b)}bu.prototype.$classData=l({pz:0},!1,"scala.runtime.Statics$",{pz:1,c:1});var cu=void 0;
function li(){cu||(cu=(new bu).b());return cu}function ae(){this.xj=this.cj=this.ck=null}ae.prototype=new td;ae.prototype.constructor=ae;function $d(a,b,c,e){a.ck=b;a.cj=c;a.xj=e;return a}ae.prototype.U=function(){var a=this.xj,b=this.cj;return void 0===a?this.ck:b.g(a)};ae.prototype.$classData=l({hq:0},!1,"japgolly.scalajs.react.CtorType$Nullary",{hq:1,zz:1,c:1});function ee(){}ee.prototype=new n;ee.prototype.constructor=ee;ee.prototype.b=function(){return this};
function du(a,b){var c=b.g(a.ck);return $d(new ae,c,mb(b,a.cj),a.xj)}ee.prototype.$classData=l({iq:0},!1,"japgolly.scalajs.react.CtorType$ProfunctorN$",{iq:1,c:1,Gz:1});var de=void 0;function eu(){this.eg=this.Lp=null}eu.prototype=new n;eu.prototype.constructor=eu;function fe(a){var b=de,c=new eu;c.Lp=a;c.eg=b;return c}eu.prototype.$classData=l({kq:0},!1,"japgolly.scalajs.react.CtorType$Summoner$$anon$1",{kq:1,c:1,Az:1});function fu(){}fu.prototype=new n;fu.prototype.constructor=fu;
fu.prototype.b=function(){gu=this;return this};function ve(a,b,c){var e=c.Lp.g(b),e=du(e,t(new u,function(){return function(a){return hu(ue(),a)}}(a)));return iu(a,b,e,c.eg)}function hu(a,b){return ju(new ku,b,t(new u,function(){return function(a){ue();return(new Tf).Ra(a)}}(a)))}fu.prototype.$classData=l({sq:0},!1,"japgolly.scalajs.react.component.Js$",{sq:1,c:1,Bq:1});var gu=void 0;function ue(){gu||(gu=(new fu).b());return gu}function lu(){}lu.prototype=new n;lu.prototype.constructor=lu;
lu.prototype.b=function(){mu=this;return this};lu.prototype.$classData=l({Gq:0},!1,"japgolly.scalajs.react.component.JsFn$",{Gq:1,c:1,Bq:1});var mu=void 0;function ff(){this.I=null}ff.prototype=new n;ff.prototype.constructor=ff;d=ff.prototype;d.n=function(a){return ef().Nf(this.I,a)};d.m=function(){return ef().hg(this.I)};d.Ra=function(a){this.I=a;return this};d.r=function(){return Aa(this.I)};d.vb=function(){return this.I};
d.$classData=l({gm:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillUnmount",{gm:1,c:1,Xe:1});function bg(){this.ci=this.pf=this.I=null}bg.prototype=new n;bg.prototype.constructor=bg;bg.prototype.m=function(){return We(me(),"ComponentWillUpdate(props: "+pb(this).rd()+" \u2192 "+this.pf+", state: "+pb(this).Cc()+" \u2192 "+this.ci+")")};bg.prototype.Gh=function(a,b,c){this.I=a;this.pf=b;this.ci=c;return this};bg.prototype.vb=function(){return this.I};
bg.prototype.$classData=l({br:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillUpdate",{br:1,c:1,Xe:1});function cg(){this.ci=this.pf=this.I=null}cg.prototype=new n;cg.prototype.constructor=cg;cg.prototype.m=function(){return We(me(),"ShouldComponentUpdate(props: "+pb(this).rd()+" \u2192 "+this.pf+", state: "+pb(this).Cc()+" \u2192 "+this.ci+")")};cg.prototype.Gh=function(a,b,c){this.I=a;this.pf=b;this.ci=c;return this};cg.prototype.vb=function(){return this.I};
cg.prototype.$classData=l({dr:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ShouldComponentUpdate",{dr:1,c:1,Xe:1});function Og(){this.Lh=null}Og.prototype=new n;Og.prototype.constructor=Og;Og.prototype.b=function(){this.Lh=xb();return this};Og.prototype.$classData=l({jr:0},!1,"japgolly.scalajs.react.extra.OnUnmount$Backend",{jr:1,c:1,Ez:1});function nu(){this.Oa=this.So=null}nu.prototype=new fh;nu.prototype.constructor=nu;
function ou(a){var b=new nu;if(null===a)throw np(Xh(),null);b.Oa=a;b.So=pu(a,qu());return b}nu.prototype.Kj=function(){return this.So};nu.prototype.$classData=l({zr:0},!1,"japgolly.scalajs.react.extra.router.RouterLogic$$anon$1",{zr:1,xr:1,c:1});function ru(){this.Ik=this.wl=this.Ej=null}ru.prototype=new rh;ru.prototype.constructor=ru;ru.prototype.m=function(){return"Route("+this.Ej+")"};function su(a,b,c,e){a.Ej=b;a.wl=c;a.Ik=e;return a}
function tu(a,b,c){return su(new ru,a.Ej,t(new u,function(a,b){return function(c){c=a.wl.g(c);return c.l()?B():b.g(c.R())}}(a,b)),mb(a.Ik,c))}ru.prototype.Jo=function(a,b){return tu(this,a,b)};ru.prototype.$classData=l({Br:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Route",{Br:1,Er:1,c:1});function oh(){this.Lj=null;this.yo=0;this.Hk=this.vl=null}oh.prototype=new rh;oh.prototype.constructor=oh;oh.prototype.m=function(){return"RouteB("+this.Lj+")"};
function uu(a,b,c){return ph(new oh,a.Lj,a.yo,t(new u,function(a,b){return function(c){c=a.vl.g(c);return c.l()?B():b.g(c.R())}}(a,b)),mb(a.Hk,c))}oh.prototype.Jo=function(a,b){return uu(this,a,b)};function kc(a){var b=bt(ct(),"^"+a.Lj+"$");return su(new ru,b,t(new u,function(a){return function(b){return a.vl.g(t(new u,function(a,b){return function(a){return vu(b,1+(a|0)|0)}}(a,b)))}}(a)),t(new u,function(a){return function(b){return(new ic).i(a.Hk.g(b))}}(a)))}
function ph(a,b,c,e,f){a.Lj=b;a.yo=c;a.vl=e;a.Hk=f;return a}oh.prototype.$classData=l({Cr:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$RouteB",{Cr:1,Er:1,c:1});function Gh(){}Gh.prototype=new Dh;Gh.prototype.constructor=Gh;Gh.prototype.b=function(){return this};Gh.prototype.Nn=function(a){return a};Gh.prototype.Ko=function(a){return a.U()};Gh.prototype.$classData=l({Lr:0},!1,"japgolly.scalajs.react.internal.Effect$$anon$1",{Lr:1,Jr:1,c:1});function Hh(){}Hh.prototype=new Dh;
Hh.prototype.constructor=Hh;Hh.prototype.b=function(){return this};Hh.prototype.Nn=function(a){return a.U().$};Hh.prototype.Ko=function(a){return(new Bb).oa(a)};Hh.prototype.$classData=l({Mr:0},!1,"japgolly.scalajs.react.internal.Effect$$anon$2",{Mr:1,Jr:1,c:1});function wu(){Jh.call(this)}wu.prototype=new Kh;wu.prototype.constructor=wu;wu.prototype.gh=function(a){return a.U()};function Nh(a){var b=new wu;Jh.prototype.pj.call(b,a,a);return b}
wu.prototype.$classData=l({Or:0},!1,"japgolly.scalajs.react.internal.Effect$Trans$Id",{Or:1,tm:1,c:1});function xu(){}xu.prototype=new n;xu.prototype.constructor=xu;xu.prototype.b=function(){yu=this;he||(he=(new ge).b());ue();mu||(mu=(new lu).b());ne();pe||(pe=(new oe).b());return this};xu.prototype.$classData=l({Xr:0},!1,"japgolly.scalajs.react.package$",{Xr:1,c:1,Bz:1});var yu=void 0;function Yc(){yu||(yu=(new xu).b())}function Ej(){this.aa=null}Ej.prototype=new ji;Ej.prototype.constructor=Ej;
Ej.prototype.b=function(){ii.prototype.i.call(this,"class");return this};function kj(a,b){var c=sc().Qp;Li||(Li=(new Mi).b());return(new zu).zb(t(new u,function(a,b,c){return function(a){fg(c,a.an,b)}}(a,b,c)))}Ej.prototype.$classData=l({Zr:0},!1,"japgolly.scalajs.react.vdom.Attr$ClassName$",{Zr:1,Oi:1,c:1});var lj=void 0;function Au(){this.aa=null}Au.prototype=new ji;Au.prototype.constructor=Au;function Bu(){}Bu.prototype=Au.prototype;Au.prototype.i=function(a){ii.prototype.i.call(this,a);return this};
Au.prototype.$classData=l({um:0},!1,"japgolly.scalajs.react.vdom.Attr$Generic",{um:1,Oi:1,c:1});function Cu(){this.aa=null}Cu.prototype=new ji;Cu.prototype.constructor=Cu;Cu.prototype.b=function(){ii.prototype.i.call(this,"key");return this};Cu.prototype.$classData=l({as:0},!1,"japgolly.scalajs.react.vdom.Attr$Key$",{as:1,Oi:1,c:1});var Du=void 0;function Eu(){}Eu.prototype=new n;Eu.prototype.constructor=Eu;Eu.prototype.b=function(){return this};
Eu.prototype.$classData=l({is:0},!1,"japgolly.scalajs.react.vdom.HtmlTags$",{is:1,c:1,Mz:1});var Fu=void 0;function zu(){this.wh=null}zu.prototype=new n;zu.prototype.constructor=zu;zu.prototype.zb=function(a){this.wh=a;return this};zu.prototype.Ui=function(a){this.wh.g(a)};zu.prototype.$classData=l({xs:0},!1,"japgolly.scalajs.react.vdom.TagMod$$anon$2",{xs:1,c:1,Pi:1});function Si(){}Si.prototype=new n;Si.prototype.constructor=Si;Si.prototype.b=function(){return this};Si.prototype.Ui=function(){};
Si.prototype.$classData=l({ys:0},!1,"japgolly.scalajs.react.vdom.TagMod$$anon$3",{ys:1,c:1,Pi:1});function Gu(){this.ol=this.Np=this.sf=null;this.Z=!1}Gu.prototype=new n;Gu.prototype.constructor=Gu;Gu.prototype.m=function(){return hd(this).m()};Gu.prototype.Ui=function(a){a=a.Ak;var b=hd(this).Ro;a.g(b)};
function hd(a){if(!a.Z&&!a.Z){for(var b=(new Hu).b(),c=a.ol,e=a.ol,e=k(r(Iu),[Ju(e)]),f=0;;){var h=c,m=xb();if(null!==h&&h.n(m))break;else e.e[f]=c.D(),c=c.u(),f=1+f|0}for(c=e.e.length;0<c;)for(c=-1+c|0,f=e.e[c],h=0;h<f.W();)f.Xa(h).Ui(b),h=1+h|0;e=a.Np;c=b.zj;void 0!==c&&(ti(),b.gi.className=c);ti();c=b.Nl;c=0===(g.Object.keys(c).length|0)?void 0:c;void 0!==c&&(ti(),b.gi.style=c);zi||(zi=(new yi).b());c=zi.en;f=b.gi;h=b.Va;b=b.Kk;b=(0,c.Fb)(e,f,h,b);b=Ku(b);a.sf=b;a.Z=!0}return a.sf}
Gu.prototype.$classData=l({zs:0},!1,"japgolly.scalajs.react.vdom.TagOf",{zs:1,c:1,Pi:1});function ed(){this.Jj=null}ed.prototype=new n;ed.prototype.constructor=ed;function Lu(){}Lu.prototype=ed.prototype;ed.prototype.Ui=function(a){a.Ak.g(this.Jj)};ed.prototype.el=function(a){this.Jj=a;return this};ed.prototype.$classData=l({vm:0},!1,"japgolly.scalajs.react.vdom.VdomNode",{vm:1,c:1,Pi:1});function Xi(){}Xi.prototype=new n;Xi.prototype.constructor=Xi;Xi.prototype.b=function(){return this};
Xi.prototype.$classData=l({Ns:0},!1,"japgolly.univeq.UnivEq$$anon$8",{Ns:1,c:1,Cf:1});function Mu(){this.ma=null}Mu.prototype=new n;Mu.prototype.constructor=Mu;Mu.prototype.Wi=function(a){Gj||(Gj=(new Fj).b());a=this.ma.Wi(a);var b=gj().createElement("style");b.type="text/css";b.appendChild(gj().createTextNode(a));return b};Mu.prototype.$classData=l({Ws:0},!1,"scalacss.defaults.PlatformExports$StyleElementRenderer",{Ws:1,c:1,hu:1});function Nu(){}Nu.prototype=new xo;Nu.prototype.constructor=Nu;
function Ou(){}Ou.prototype=Nu.prototype;function Pu(){}Pu.prototype=new vk;Pu.prototype.constructor=Pu;function Qu(){}Qu.prototype=Pu.prototype;Pu.prototype.kn=function(){return!0};Pu.prototype.Vl=function(){return this};function Ru(){this.Eb=null}Ru.prototype=new zo;Ru.prototype.constructor=Ru;Ru.prototype.b=function(){Su=this;this.Eb=rk(lk(),"display",ho(hl(il(),Lk().kj,"flex",(new A).f(["inline-flex"])),hl(il(),Lk().Tf,"grid",(new A).f(["inline-grid"]))));return this};Ru.prototype.Yd=function(){return this.Eb};
Ru.prototype.$classData=l({jt:0},!1,"scalacss.internal.Attrs$display$",{jt:1,we:1,c:1});var Su=void 0;function Tu(){this.Eb=null}Tu.prototype=new zo;Tu.prototype.constructor=Tu;Tu.prototype.b=function(){Uu=this;lk();var a=il(),b=Lk().kj;this.Eb=rk(0,"flex-direction",jo(a,b,wl().Hj,ko().Va));return this};Tu.prototype.Yd=function(){return this.Eb};Tu.prototype.$classData=l({kt:0},!1,"scalacss.internal.Attrs$flexDirection$",{kt:1,we:1,c:1});var Uu=void 0;function Vu(){this.Eb=null}Vu.prototype=new zo;
Vu.prototype.constructor=Vu;Vu.prototype.b=function(){Wu=this;this.Eb=kk("font-family");return this};Vu.prototype.Yd=function(){return this.Eb};Vu.prototype.$classData=l({lt:0},!1,"scalacss.internal.Attrs$fontFamily$",{lt:1,we:1,c:1});var Wu=void 0;function Xu(){this.Eb=null}Xu.prototype=new zo;Xu.prototype.constructor=Xu;Xu.prototype.b=function(){Yu=this;lk();var a=il(),b=Lk().kj;this.Eb=rk(0,"justify-content",jo(a,b,wl().Hj,ko().Va));return this};Xu.prototype.Yd=function(){return this.Eb};
Xu.prototype.$classData=l({ot:0},!1,"scalacss.internal.Attrs$justifyContent$",{ot:1,we:1,c:1});var Yu=void 0;function Zu(){this.Eb=null}Zu.prototype=new zo;Zu.prototype.constructor=Zu;
Zu.prototype.b=function(){$u=this;lk();var a=hl,b=il();Lk();var c=J().Lb,e=[L()];if(0===(e.length|0))var f=Q();else{for(var h=R(new S,Q()),m=0,q=e.length|0;m<q;)T(h,e[m]),m=1+m|0;f=h.w}var y=(new H).d(c,f),N=J().Mb,z=[L()];if(0===(z.length|0))var E=Q();else{for(var C=R(new S,Q()),F=0,X=z.length|0;F<X;)T(C,z[F]),F=1+F|0;E=C.w}var Sb=(new H).d(N,E),yc=J().Nb,ob=[L()];if(0===(ob.length|0))var Eb=Q();else{for(var $e=R(new S,Q()),zc=0,pd=ob.length|0;zc<pd;)T($e,ob[zc]),zc=1+zc|0;Eb=$e.w}var fn=(new H).d(yc,
Eb),gn=J().Ob,af=[L()];if(0===(af.length|0))var wd=Q();else{for(var of=R(new S,Q()),Bc=0,xd=af.length|0;Bc<xd;)T(of,af[Bc]),Bc=1+Bc|0;wd=of.w}var sn=(new H).d(gn,wd),tn=J().ec,pf=[L()];if(0===(pf.length|0))var yd=Q();else{for(var qf=R(new S,Q()),Cc=0,zd=pf.length|0;Cc<zd;)T(qf,pf[Cc]),Cc=1+Cc|0;yd=qf.w}var un=(new H).d(tn,yd),vn=J().Pb,rf=[L()];if(0===(rf.length|0))var Ad=Q();else{for(var sf=R(new S,Q()),Dc=0,Bd=rf.length|0;Dc<Bd;)T(sf,rf[Dc]),Dc=1+Dc|0;Ad=sf.w}var wn=(new H).d(vn,Ad),xn=J().Qb,tf=
[O()];if(0===(tf.length|0))var Cd=Q();else{for(var uf=R(new S,Q()),Ec=0,Dd=tf.length|0;Ec<Dd;)T(uf,tf[Ec]),Ec=1+Ec|0;Cd=uf.w}var yn=(new H).d(xn,Cd),zn=J().Rb,vf=[O(),L()];if(0===(vf.length|0))var Ed=Q();else{for(var wf=R(new S,Q()),Fc=0,Fd=vf.length|0;Fc<Fd;)T(wf,vf[Fc]),Fc=1+Fc|0;Ed=wf.w}var An=(new H).d(zn,Ed),Bn=J().Sb,xf=[O()];if(0===(xf.length|0))var Gd=Q();else{for(var yf=R(new S,Q()),Gc=0,Hd=xf.length|0;Gc<Hd;)T(yf,xf[Gc]),Gc=1+Gc|0;Gd=yf.w}var Cn=(new H).d(Bn,Gd),Dn=J().Tb,zf=[O()];if(0===
(zf.length|0))var Id=Q();else{for(var Af=R(new S,Q()),Hc=0,Jd=zf.length|0;Hc<Jd;)T(Af,zf[Hc]),Hc=1+Hc|0;Id=Af.w}var En=(new H).d(Dn,Id),Fn=J().Ub,Bf=[L()];if(0===(Bf.length|0))var Kd=Q();else{for(var Cf=R(new S,Q()),Ic=0,Ld=Bf.length|0;Ic<Ld;)T(Cf,Bf[Ic]),Ic=1+Ic|0;Kd=Cf.w}var Gn=(new H).d(Fn,Kd),Hn=J().Wb,Df=[O(),L()];if(0===(Df.length|0))var Md=Q();else{for(var Ef=R(new S,Q()),Jc=0,Nd=Df.length|0;Jc<Nd;)T(Ef,Df[Jc]),Jc=1+Jc|0;Md=Ef.w}var In=(new H).d(Hn,Md),Jn=J().Xb,Ff=[O()];if(0===(Ff.length|
0))var Od=Q();else{for(var Gf=R(new S,Q()),Kc=0,Pd=Ff.length|0;Kc<Pd;)T(Gf,Ff[Kc]),Kc=1+Kc|0;Od=Gf.w}var Kn=(new H).d(Jn,Od),Ln=J().Yb,Hf=[O(),L()];if(0===(Hf.length|0))var Qd=Q();else{for(var If=R(new S,Q()),Lc=0,Rd=Hf.length|0;Lc<Rd;)T(If,Hf[Lc]),Lc=1+Lc|0;Qd=If.w}var Mn=(new H).d(Ln,Qd),Nn=J().Zb,Jf=[L()];if(0===(Jf.length|0))var Sd=Q();else{for(var Kf=R(new S,Q()),Mc=0,Td=Jf.length|0;Mc<Td;)T(Kf,Jf[Mc]),Mc=1+Mc|0;Sd=Kf.w}var On=(new H).d(Nn,Sd),Pn=J().fc,Lf=[L()];if(0===(Lf.length|0))var Ud=Q();
else{for(var Mf=R(new S,Q()),Nc=0,Vd=Lf.length|0;Nc<Vd;)T(Mf,Lf[Nc]),Nc=1+Nc|0;Ud=Mf.w}for(var mj=[y,Sb,fn,sn,un,wn,yn,An,Cn,En,Gn,In,Kn,Mn,On,(new H).d(Pn,Ud)],Nf=jl(new kl,ll()),Oc=0,Wd=mj.length|0;Oc<Wd;)ml(Nf,mj[Oc]),Oc=1+Oc|0;var Qn=Nf.w;av||(av=(new bv).b());var tv=a(b,Qn,av.s,(new A).f([cv().s])),uv=hl,vv=il();Lk();var wv=J().Lb,Rn=[O()];if(0===(Rn.length|0))var ur=Q();else{for(var vr=R(new S,Q()),nj=0,xv=Rn.length|0;nj<xv;)T(vr,Rn[nj]),nj=1+nj|0;ur=vr.w}var yv=(new H).d(wv,ur),zv=J().Mb,Sn=
[O()];if(0===(Sn.length|0))var wr=Q();else{for(var xr=R(new S,Q()),oj=0,Av=Sn.length|0;oj<Av;)T(xr,Sn[oj]),oj=1+oj|0;wr=xr.w}var Bv=(new H).d(zv,wr),Cv=J().Nb,Tn=[O()];if(0===(Tn.length|0))var yr=Q();else{for(var zr=R(new S,Q()),pj=0,Dv=Tn.length|0;pj<Dv;)T(zr,Tn[pj]),pj=1+pj|0;yr=zr.w}var Ev=(new H).d(Cv,yr),Fv=J().Ob,Un=[O()];if(0===(Un.length|0))var Ar=Q();else{for(var Br=R(new S,Q()),qj=0,Gv=Un.length|0;qj<Gv;)T(Br,Un[qj]),qj=1+qj|0;Ar=Br.w}var Hv=(new H).d(Fv,Ar),Iv=J().ec,Vn=[O()];if(0===(Vn.length|
0))var Cr=Q();else{for(var Dr=R(new S,Q()),rj=0,Jv=Vn.length|0;rj<Jv;)T(Dr,Vn[rj]),rj=1+rj|0;Cr=Dr.w}var Kv=(new H).d(Iv,Cr),Lv=J().Pb,Wn=[O()];if(0===(Wn.length|0))var Er=Q();else{for(var Fr=R(new S,Q()),sj=0,Mv=Wn.length|0;sj<Mv;)T(Fr,Wn[sj]),sj=1+sj|0;Er=Fr.w}var Nv=(new H).d(Lv,Er),Ov=J().Qb,Xn=[Jk()];if(0===(Xn.length|0))var Gr=Q();else{for(var Hr=R(new S,Q()),tj=0,Pv=Xn.length|0;tj<Pv;)T(Hr,Xn[tj]),tj=1+tj|0;Gr=Hr.w}var Qv=(new H).d(Ov,Gr),Rv=J().Rb,Yn=[O(),Kk()];if(0===(Yn.length|0))var Ir=
Q();else{for(var Jr=R(new S,Q()),uj=0,Sv=Yn.length|0;uj<Sv;)T(Jr,Yn[uj]),uj=1+uj|0;Ir=Jr.w}var Tv=(new H).d(Rv,Ir),Uv=J().Sb,Zn=[Kk(),Jk()];if(0===(Zn.length|0))var Kr=Q();else{for(var Lr=R(new S,Q()),vj=0,Vv=Zn.length|0;vj<Vv;)T(Lr,Zn[vj]),vj=1+vj|0;Kr=Lr.w}var Wv=(new H).d(Uv,Kr),Xv=J().Tb,$n=[Jk()];if(0===($n.length|0))var Mr=Q();else{for(var Nr=R(new S,Q()),wj=0,Yv=$n.length|0;wj<Yv;)T(Nr,$n[wj]),wj=1+wj|0;Mr=Nr.w}var Zv=(new H).d(Xv,Mr),$v=J().Ub,ao=[O()];if(0===(ao.length|0))var Or=Q();else{for(var Pr=
R(new S,Q()),xj=0,aw=ao.length|0;xj<aw;)T(Pr,ao[xj]),xj=1+xj|0;Or=Pr.w}var bw=(new H).d($v,Or),cw=J().Wb,bo=[O()];if(0===(bo.length|0))var Qr=Q();else{for(var Rr=R(new S,Q()),yj=0,dw=bo.length|0;yj<dw;)T(Rr,bo[yj]),yj=1+yj|0;Qr=Rr.w}var ew=(new H).d(cw,Qr),fw=J().Xb,co=[O()];if(0===(co.length|0))var Sr=Q();else{for(var Tr=R(new S,Q()),zj=0,gw=co.length|0;zj<gw;)T(Tr,co[zj]),zj=1+zj|0;Sr=Tr.w}var hw=(new H).d(fw,Sr),iw=J().Yb,eo=[O()];if(0===(eo.length|0))var Ur=Q();else{for(var Vr=R(new S,Q()),Aj=
0,jw=eo.length|0;Aj<jw;)T(Vr,eo[Aj]),Aj=1+Aj|0;Ur=Vr.w}var kw=(new H).d(iw,Ur),lw=J().Zb,fo=[O()];if(0===(fo.length|0))var Wr=Q();else{for(var Xr=R(new S,Q()),Bj=0,mw=fo.length|0;Bj<mw;)T(Xr,fo[Bj]),Bj=1+Bj|0;Wr=Xr.w}var nw=(new H).d(lw,Wr),ow=J().fc,go=[O()];if(0===(go.length|0))var Yr=Q();else{for(var Zr=R(new S,Q()),Cj=0,pw=go.length|0;Cj<pw;)T(Zr,go[Cj]),Cj=1+Cj|0;Yr=Zr.w}for(var $r=[yv,Bv,Ev,Hv,Kv,Nv,Qv,Tv,Wv,Zv,bw,ew,hw,kw,nw,(new H).d(ow,Yr)],as=jl(new kl,ll()),Dj=0,qw=$r.length|0;Dj<qw;)ml(as,
$r[Dj]),Dj=1+Dj|0;this.Eb=rk(0,"text-align",ho(tv,uv(vv,as.w,"justify",(new A).f([]))));return this};Zu.prototype.Yd=function(){return this.Eb};Zu.prototype.$classData=l({pt:0},!1,"scalacss.internal.Attrs$textAlign$",{pt:1,we:1,c:1});var $u=void 0;function dv(){this.Sp=this.so=null}dv.prototype=new n;dv.prototype.constructor=dv;function ev(a,b,c){return(new H).d(fg(a.so,b,c),fg(a.Sp,b,c))}dv.prototype.$classData=l({It:0},!1,"scalacss.internal.Compose$Rules$$anon$1",{It:1,c:1,Zz:1});
l({Ot:0},!1,"scalacss.internal.Domain$OverSeq",{Ot:1,c:1,$z:1});function mk(){hk.call(this);this.zl=null}mk.prototype=new ik;mk.prototype.constructor=mk;mk.prototype.Xf=function(a,b){hk.prototype.Xf.call(this,a,b);a=[this];if(0===(a.length|0))a=Q();else{b=R(new S,Q());for(var c=0,e=a.length|0;c<e;)T(b,a[c]),c=1+c|0;a=b.w}this.zl=a;return this};mk.prototype.$classData=l({gu:0},!1,"scalacss.internal.RealAttr",{gu:1,$s:1,c:1});function en(){this.Rn=null}en.prototype=new n;en.prototype.constructor=en;
en.prototype.Wi=function(a){return fv(this,a)};en.prototype.zb=function(a){this.Rn=a;return this};function fv(a,b){var c=(new ms).b(),e=a.Rn.g(c),f=am(km(),b);if(null===f)throw(new x).o(f);var h=f.ng;b=f.og;f=f.pg;for(h=fm(km(),h);!f.l();){var m=f.D();gv(e,m);f=f.u()}for(;!b.l();)f=b.D(),gv(e,f),b=b.u();h.O(t(new u,function(a,b){return function(a){a.N.l()&&hv(b,B(),a.P)}}(a,e)));h.O(t(new u,function(a,b){return function(a){!a.N.l()&&hv(b,a.N,a.P)}}(a,e)));e.th.U();return c.wb.va}
en.prototype.$classData=l({ju:0},!1,"scalacss.internal.StringRenderer$Default",{ju:1,c:1,hu:1});function iv(){}iv.prototype=new n;iv.prototype.constructor=iv;function jv(){}jv.prototype=iv.prototype;function po(){this.xh=null}po.prototype=new no;po.prototype.constructor=po;function qo(a,b){return a.xh.g(b)}po.prototype.zb=function(a){this.xh=a;return this};po.prototype.$classData=l({tu:0},!1,"scalacss.internal.ValueT$Rule$$anon$1",{tu:1,zA:1,c:1});function kv(){}kv.prototype=new zo;
kv.prototype.constructor=kv;function lv(){}lv.prototype=kv.prototype;function mv(){pp.call(this)}mv.prototype=new qp;mv.prototype.constructor=mv;mv.prototype.b=function(){pp.prototype.Su.call(this,Do().dg);return this};mv.prototype.$classData=l({xu:0},!1,"scalacss.internal.mutable.GlobalRegistry$",{xu:1,EA:1,c:1});var nv=void 0;function ov(){nv||(nv=(new mv).b());return nv}function fp(){this.Po=null}fp.prototype=new n;fp.prototype.constructor=fp;
function To(a){return(new Pf).o(Fb(function(a){return function(c,e){a.Po.g(Cl(Dl((new A).f(["[CSS WARNING] "," -- ",""])),(new A).f([Jl(km(),c,e.oh),e.ai])))}}(a)))}fp.prototype.zb=function(a){this.Po=a;return this};fp.prototype.$classData=l({Cu:0},!1,"scalacss.internal.mutable.Register$ErrorHandler$Noisy",{Cu:1,c:1,AA:1});function pv(){this.mn=null}pv.prototype=new n;pv.prototype.constructor=pv;
pv.prototype.b=function(){qv=this;var a=(new ih).i("[^_a-zA-Z0-9\u00a0-\u00ff-]");xb();this.mn=jh(a.cb);return this};pv.prototype.$classData=l({Du:0},!1,"scalacss.internal.mutable.Register$MacroName$Use$",{Du:1,c:1,BA:1});var qv=void 0;function rv(){qv||(qv=(new pv).b());return qv}function $o(){this.Yk=null;this.tl=0}$o.prototype=new n;$o.prototype.constructor=$o;$o.prototype.Ih=function(a,b){this.Yk=a;this.tl=b;return this};
$o.prototype.$classData=l({Eu:0},!1,"scalacss.internal.mutable.Register$NameGen$IncFmt",{Eu:1,c:1,CA:1});function sv(){this.ya=null}sv.prototype=new n;sv.prototype.constructor=sv;function rw(a){var b=new sv;if(null===a)throw np(Xh(),null);b.ya=a;return b}sv.prototype.$classData=l({Iu:0},!1,"scalacss.internal.mutable.StyleSheet$Inline$MFontFace",{Iu:1,c:1,aA:1});function sw(){this.ya=this.aa=null}sw.prototype=new n;sw.prototype.constructor=sw;
function tw(a){var b=new sw;b.aa="self";if(null===a)throw np(Xh(),null);b.ya=a;return b}sw.prototype.$classData=l({Ju:0},!1,"scalacss.internal.mutable.StyleSheet$Inline$MStyle",{Ju:1,c:1,bA:1});function uw(){}uw.prototype=new n;uw.prototype.constructor=uw;function vw(){}vw.prototype=uw.prototype;function St(a){return!!(a&&a.$classData&&a.$classData.p.$f||"number"===typeof a)}function ww(){this.ma=null}ww.prototype=new n;ww.prototype.constructor=ww;function xw(){}xw.prototype=ww.prototype;
ww.prototype.jj=function(){if(void 0===g.Error.captureStackTrace){try{var a={}.undef()}catch(b){if(a=Wh(Xh(),b),null!==a)if(Nt(a))a=a.gf;else throw np(Xh(),a);else throw b;}this.stackdata=a}else g.Error.captureStackTrace(this),this.stackdata=this;return this};ww.prototype.Pc=function(){return this.ma};ww.prototype.m=function(){var a=Jb(la(this)),b=this.Pc();return null===b?a:a+": "+b};ww.prototype.Sa=function(a,b,c,e){this.ma=a;e&&this.jj();return this};
function Vg(){this.go=this.Ho=null;this.Vo=this.Wo=0;this.ee=this.Zf=this.Mj=null;this.Zi=!1;this.rg=0}Vg.prototype=new n;Vg.prototype.constructor=Vg;function et(a){if(a.Zi){a.ee=a.Mj.exec(a.Zf);if(null!==a.ee){var b=a.ee[0];if(void 0===b)throw(new yw).i("undefined.get");if(null===b)throw(new Ho).b();""===b&&(b=a.Mj,b.lastIndex=1+(b.lastIndex|0)|0)}else a.Zi=!1;B();return null!==a.ee}return!1}function zw(a){if(null===a.ee)throw(new Aw).i("No match available");return a.ee}
function vu(a,b){a=zw(a)[b];return void 0===a?null:a}function Wg(a){dt(a);et(a);null===a.ee||0===(zw(a).index|0)&&Bw(a)===(a.Zf.length|0)||dt(a);return null!==a.ee}function ht(a,b){Cw(b,a.Zf.substring(a.rg));a.rg=a.Zf.length|0}function Bw(a){var b=zw(a).index|0;a=zw(a)[0];if(void 0===a)throw(new yw).i("undefined.get");return b+(a.length|0)|0}
function Ug(a,b,c,e){a.Ho=b;a.go=c;a.Wo=0;a.Vo=e;b=a.Ho;c=new g.RegExp(b.ag);b=c!==b.ag?c:new g.RegExp(b.ag.source,(b.ag.global?"g":"")+(b.ag.ignoreCase?"i":"")+(b.ag.multiline?"m":""));a.Mj=b;a.Zf=ka(Ha(a.go,a.Wo,a.Vo));a.ee=null;a.Zi=!0;a.rg=0;B();return a}
function gt(a,b,c){var e=a.Zf,f=a.rg,h=zw(a).index|0;Cw(b,e.substring(f,h));e=c.length|0;for(f=0;f<e;)switch(h=65535&(c.charCodeAt(f)|0),h){case 36:for(h=f=1+f|0;;){if(f<e)var m=65535&(c.charCodeAt(f)|0),m=48<=m&&57>=m;else m=!1;if(m)f=1+f|0;else break}Ns();h=c.substring(h,f);m=null===h?0:h.length|0;0===m&&Dw(h);var q=65535&(h.charCodeAt(0)|0),y=45===q,N=y?2147483648:2147483647,q=y||43===q?1:0;q>=(h.length|0)&&Dw(h);for(var z=0;q!==m;){Ew||(Ew=(new Fw).b());var E;E=Ew;var C=65535&(h.charCodeAt(q)|
0);if(256>C)E=48<=C&&57>=C?-48+C|0:65<=C&&90>=C?-55+C|0:97<=C&&122>=C?-87+C|0:-1;else if(65313<=C&&65338>=C)E=-65303+C|0;else if(65345<=C&&65370>=C)E=-65335+C|0;else{var F;a:{Ep||(Ep=(new Dp).b());F=Gw(E);var X=C,Sb=0,yc=F.e.length;for(;;){if(Sb===yc){F=-1-Sb|0;break a}var ob=(Sb+yc|0)>>>1|0,Eb=F.e[ob];if(X<Eb)yc=ob;else{if(D(G(),X,Eb)){F=ob;break a}Sb=1+ob|0}}}F=0>F?-2-F|0:F;0>F?E=-1:(E=C-Gw(E).e[F]|0,E=9<E?-1:E)}E=10>E?E:-1;z=10*z+E;(-1===E||z>N)&&Dw(h);q=1+q|0}Cw(b,vu(a,y?-z|0:z|0));break;case 92:f=
1+f|0;f<e&&(h=65535&(c.charCodeAt(f)|0),kn(b.$d,h));f=1+f|0;break;default:kn(b.$d,h),f=1+f|0}a.rg=Bw(a)}function lh(a,b){dt(a);for(var c=(new ft).b();et(a);)gt(a,c,b);ht(a,c);return c.m()}function dt(a){a.Mj.lastIndex=0;a.ee=null;a.Zi=!0;a.rg=0;B()}Vg.prototype.$classData=l({Kv:0},!1,"java.util.regex.Matcher",{Kv:1,c:1,PA:1});function Hw(){}Hw.prototype=new n;Hw.prototype.constructor=Hw;Hw.prototype.b=function(){return this};Hw.prototype.sg=function(){return(new ms).b()};Hw.prototype.$e=function(){return(new ms).b()};
Hw.prototype.$classData=l({cw:0},!1,"scala.Predef$$anon$3",{cw:1,c:1,Vj:1});function Xp(){}Xp.prototype=new n;Xp.prototype.constructor=Xp;Xp.prototype.b=function(){return this};Xp.prototype.m=function(){return"object AnyRef"};Xp.prototype.$classData=l({rw:0},!1,"scala.package$$anon$1",{rw:1,c:1,dB:1});function Iw(){this.Dp=this.vo=this.Ll=0}Iw.prototype=new sr;Iw.prototype.constructor=Iw;Iw.prototype.b=function(){Jw=this;this.Ll=Ba(Ca(),"Seq");this.vo=Ba(Ca(),"Map");this.Dp=Ba(Ca(),"Set");return this};
function Kw(a,b){var c;if(b&&b.$classData&&b.$classData.p.pp){c=0;a=a.Ll;for(var e=b;!e.l();)b=e.D(),e=e.u(),a=bs(a,ki(li(),b)),c=1+c|0;c=cs(a^c)}else c=fs(a,b,a.Ll);return c}Iw.prototype.$classData=l({Tw:0},!1,"scala.util.hashing.MurmurHash3$",{Tw:1,jB:1,c:1});var Jw=void 0;function U(){Jw||(Jw=(new Iw).b());return Jw}function Lw(a){return(a.H()?"non-empty":"empty")+" iterator"}function Mw(a,b){for(var c=!0;c&&a.H();)c=!!b.g(a.t());return c}function tp(a,b){for(;a.H();)b.g(a.t())}
function Nw(a,b){var c=new Ow;c.Mc=a;c.Wa=null;c.Uh=null;c.cf=!1;return c.ik(b)}function Pw(a){if(a.H()){var b=a.t();return(new Qw).Hh(b,v(new w,function(a){return function(){return a.kc()}}(a)))}iq();return Rw()}function Sw(a,b){for(var c=0;c<b&&a.H();)a.t(),c=1+c|0;return a}function Tw(){this.ya=this.Cj=null}Tw.prototype=new n;Tw.prototype.constructor=Tw;Tw.prototype.O=function(a){this.ya.O(t(new u,function(a,c){return function(e){return a.Cj.g(e)?c.g(e):void 0}}(this,a)))};
function Uw(a,b,c){a.Cj=c;if(null===b)throw np(Xh(),null);a.ya=b;return a}Tw.prototype.$classData=l({ox:0},!1,"scala.collection.TraversableLike$WithFilter",{ox:1,c:1,ea:1});function Vw(){}Vw.prototype=new vs;Vw.prototype.constructor=Vw;function Ww(){}Ww.prototype=Vw.prototype;function Xw(){this.ya=null}Xw.prototype=new n;Xw.prototype.constructor=Xw;Xw.prototype.sg=function(){return R(new S,this.ya.yg())};Xw.prototype.$e=function(a){return a&&a.$classData&&a.$classData.p.cd?a.ob().da():R(new S,this.ya.yg())};
function Yw(a){var b=new Xw;if(null===a)throw np(Xh(),null);b.ya=a;return b}Xw.prototype.$classData=l({qx:0},!1,"scala.collection.generic.GenSetFactory$$anon$1",{qx:1,c:1,Vj:1});function Zw(){this.Ka=null}Zw.prototype=new vs;Zw.prototype.constructor=Zw;function $w(){}$w.prototype=Zw.prototype;Zw.prototype.b=function(){this.Ka=(new ax).Jh(this);return this};function bx(){this.ya=null}bx.prototype=new n;bx.prototype.constructor=bx;function cx(){}cx.prototype=bx.prototype;bx.prototype.sg=function(){return this.ya.da()};
bx.prototype.$e=function(a){return a.ob().da()};bx.prototype.Jh=function(a){if(null===a)throw np(Xh(),null);this.ya=a;return this};function dx(){}dx.prototype=new ts;dx.prototype.constructor=dx;function ex(){}ex.prototype=dx.prototype;function fx(){this.ll=null}fx.prototype=new ys;fx.prototype.constructor=fx;function gx(a,b){a.ll=b;b=new hx;if(null===a)throw np(Xh(),null);b.Oa=a}fx.prototype.Ek=function(a,b){return fg(this.ll,a,b)};
fx.prototype.$classData=l({xx:0},!1,"scala.collection.immutable.HashMap$$anon$2",{xx:1,Cx:1,c:1});function hx(){this.Oa=null}hx.prototype=new ys;hx.prototype.constructor=hx;hx.prototype.Ek=function(a,b){return fg(this.Oa.ll,b,a)};hx.prototype.$classData=l({yx:0},!1,"scala.collection.immutable.HashMap$$anon$2$$anon$3",{yx:1,Cx:1,c:1});function ix(){}ix.prototype=new n;ix.prototype.constructor=ix;d=ix.prototype;d.b=function(){return this};d.g=function(){return this};d.m=function(){return"\x3cfunction1\x3e"};
d.Gd=function(a){return nb(this,a)};d.$classData=l({Mx:0},!1,"scala.collection.immutable.List$$anon$1",{Mx:1,c:1,T:1});function jx(a,b){b=b.ke();switch(b){case -1:break;default:a.qc(b)}}function kx(a,b,c){b=b.ke();switch(b){case -1:break;default:a.qc(b+c|0)}}function lx(a,b,c){c=c.ke();switch(c){case -1:break;default:a.qc(b<c?b:c)}}function mx(){}mx.prototype=new n;mx.prototype.constructor=mx;function nx(){}nx.prototype=mx.prototype;mx.prototype.m=function(){return"\x3cfunction0\x3e"};
function ox(){}ox.prototype=new n;ox.prototype.constructor=ox;function px(){}px.prototype=ox.prototype;ox.prototype.m=function(){return"\x3cfunction1\x3e"};ox.prototype.Gd=function(a){return nb(this,a)};function qx(){}qx.prototype=new n;qx.prototype.constructor=qx;function rx(){}rx.prototype=qx.prototype;qx.prototype.m=function(){return"\x3cfunction2\x3e"};function sx(){}sx.prototype=new n;sx.prototype.constructor=sx;function tx(){}tx.prototype=sx.prototype;sx.prototype.m=function(){return"\x3cfunction3\x3e"};
function ux(){}ux.prototype=new n;ux.prototype.constructor=ux;function vx(){}vx.prototype=ux.prototype;ux.prototype.m=function(){return"\x3cfunction4\x3e"};function wx(){this.x=!1}wx.prototype=new n;wx.prototype.constructor=wx;wx.prototype.m=function(){return""+this.x};function qs(a){var b=new wx;b.x=a;return b}wx.prototype.$classData=l({hz:0},!1,"scala.runtime.BooleanRef",{hz:1,c:1,h:1});function Zt(a){return!!(a&&a.$classData&&1===a.$classData.ug&&a.$classData.tg.p.Ip)}
var wa=l({Ip:0},!1,"scala.runtime.BoxedUnit",{Ip:1,c:1,h:1},void 0,void 0,function(a){return void 0===a});function es(){this.x=0}es.prototype=new n;es.prototype.constructor=es;es.prototype.m=function(){return""+this.x};es.prototype.kb=function(a){this.x=a;return this};es.prototype.$classData=l({jz:0},!1,"scala.runtime.IntRef",{jz:1,c:1,h:1});function Sf(){this.x=null}Sf.prototype=new n;Sf.prototype.constructor=Sf;Sf.prototype.m=function(){return""+this.x};Sf.prototype.o=function(a){this.x=a;return this};
Sf.prototype.$classData=l({mz:0},!1,"scala.runtime.ObjectRef",{mz:1,c:1,h:1});function xx(){}xx.prototype=new n;xx.prototype.constructor=xx;xx.prototype.b=function(){return this};function Oe(){var a=me();ci();return Zh(new Yh,t(new u,function(){return function(a){return a.Pe}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return He(new Ie,a.ye,a.ze,a.Ae,a.Be,a.Ce,a.De,b,a.xe)}}(a,c))}}(a)))}
function Pg(){var a=me();ci();return Zh(new Yh,t(new u,function(){return function(a){return a.ze}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return He(new Ie,a.ye,b,a.Ae,a.Be,a.Ce,a.De,a.Pe,a.xe)}}(a,c))}}(a)))}
function Cg(){var a=me();ci();return Zh(new Yh,t(new u,function(){return function(a){return a.Ce}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return He(new Ie,a.ye,a.ze,a.Ae,a.Be,b,a.De,a.Pe,a.xe)}}(a,c))}}(a)))}function We(a,b){return kp(Ca(),b,"undefined \u2192 undefined","undefined").split("props: undefined, ").join("").split("state: undefined)").join(")").split(", )").join(")")}
function ug(){var a=me();ci();return Zh(new Yh,t(new u,function(){return function(a){return a.ye}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return He(new Ie,b,a.ze,a.Ae,a.Be,a.Ce,a.De,a.Pe,a.xe)}}(a,c))}}(a)))}xx.prototype.$classData=l({Vq:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$",{Vq:1,c:1,k:1,h:1});var yx=void 0;function me(){yx||(yx=(new xx).b());return yx}function zx(){}zx.prototype=new n;zx.prototype.constructor=zx;
zx.prototype.b=function(){return this};zx.prototype.$classData=l({kr:0},!1,"japgolly.scalajs.react.extra.router.AbsUrl$",{kr:1,c:1,k:1,h:1});var Ax=void 0;function Bx(){}Bx.prototype=new n;Bx.prototype.constructor=Bx;Bx.prototype.b=function(){return this};function ac(){Cx||(Cx=(new Bx).b());var a=Qg(Rg()).location,b=a.protocol+"//"+a.hostname,c=a.port;if(null===c)throw(new Ho).b();var e=ct(),e=bt(e,"^(?:80)?$");Wg(Ug(new Vg,e,c,Ga(c)))||(b=b+":"+a.port);return(new Dx).i(b)}
Bx.prototype.$classData=l({mr:0},!1,"japgolly.scalajs.react.extra.router.BaseUrl$",{mr:1,c:1,k:1,h:1});var Cx=void 0;function Ex(){this.Co=null}Ex.prototype=new n;Ex.prototype.constructor=Ex;Ex.prototype.b=function(){Fx=this;this.Co=t(new u,function(){return function(){var a=(new Bb).oa(sb().Y);return(new Bb).oa(a.$)}}(this));return this};
function Gx(a){var b=wb(sb(),v(new w,function(){return function(){Qg(Rg()).scrollTo(0,0)}}(a)));return Fb(function(a,b){return function(){return(new Bb).oa(b)}}(a,b))}function Hx(a){return Fb(function(){return function(a,c){return c.sf.U()}}(a))}Ex.prototype.$classData=l({tr:0},!1,"japgolly.scalajs.react.extra.router.RouterConfig$",{tr:1,c:1,k:1,h:1});var Fx=void 0;function Ix(){this.Hg=this.ph=this.on=this.Pl=this.Lc=this.Zd=null}Ix.prototype=new n;Ix.prototype.constructor=Ix;
function Ng(a,b){var c=new Ix;c.Zd=a;c.Lc=b;c.Hg=(hg(),xb());c.Pl=qd(rb(),v(new w,function(){return function(){Ax||(Ax=(new zx).b());return(new Jx).i(Qg(Rg()).location.href)}}(c)),t(new u,function(a){return function(b){return(new Bb).oa(qd(rb(),a.Lc.od.g(v(new w,function(a,b){return function(){return"Syncing to "+b+"."}}(a,b))).$,t(new u,function(a,b){return function(){return(new Bb).oa(qd(rb(),pu(a,Kx(a,b)),t(new u,function(a){return function(b){return(new Bb).oa(qd(rb(),a.Lc.od.g(v(new w,function(a,
b){return function(){return"Resolved to page "+b.Hc+"."}}(a,b))).$,t(new u,function(a,b){return function(){return(new Bb).oa(od(rb(),a.Lc.od.g(v(new w,function(){return function(){return""}}(a))).$,t(new u,function(a,b){return function(){return b}}(a,b))))}}(a,b))))}}(a))))}}(a,b))))}}(c)));c.on=ou(c);c.ph=Lx(c.on,b.ub);return c}
function Mx(a,b,c){return Ig(Ig((new Nx).oa(v(new w,function(a,b,c){return function(){return"Redirecting to "+Ox(a.Zd,b)+" via "+c+"."}}(a,b,c))),Px(a,b,c)),Kx(a,Ox(a.Zd,b)))}function Kx(a,b){var c;c=b.F;var e=a.Zd.F;c=0<=(c.length|0)&&c.substring(0,e.length|0)===e?(new Pf).o((new ic).i(b.F.substring(a.Zd.F.length|0))):B();if(Qf(c))return Qx(a,c.F);if(B()===c)return Rx(a,b);throw(new x).o(c);}
function pu(a,b){if(b&&b.$classData&&b.$classData.p.tk){var c=b.lc;b=v(new w,function(a,b){return function(){Qg(Rg()).history.pushState({},"",b.F)}}(a,c));a=a.Lc.od.g(v(new w,function(a,b){return function(){return"PushState: ["+b.F+"]"}}(a,c))).$;return(new Bb).oa(tb(rb(),a,b)).$}if(b&&b.$classData&&b.$classData.p.uk)return c=b.lc,b=v(new w,function(a,b){return function(){Qg(Rg()).history.replaceState({},"",b.F)}}(a,c)),a=a.Lc.od.g(v(new w,function(a,b){return function(){return"ReplaceState: ["+b.F+
"]"}}(a,c))).$,(new Bb).oa(tb(rb(),a,b)).$;if(b&&b.$classData&&b.$classData.p.wk)return c=b.lc,b=v(new w,function(a,b){return function(){Qg(Rg()).location.href=b.F}}(a,c)),a=a.Lc.od.g(v(new w,function(a,b){return function(){return"SetWindowLocation: ["+b.F+"]"}}(a,c))).$,(new Bb).oa(tb(rb(),a,b)).$;if(qu()===b)return b=lg(a),a=a.Lc.od.g(v(new w,function(){return function(){return"Broadcasting sync request."}}(a))).$,(new Bb).oa(tb(rb(),a,b)).$;if(b&&b.$classData&&b.$classData.p.vk)return a=b.eh,kd(rb(),
a);if(b&&b.$classData&&b.$classData.p.sk)return a=a.Lc.od.g(b.bi),null===a?null:a.$;if(Jg(b)){var e=b.Gg;b=b.Ig;for(var c=rb(),f=null,f=(new Bb).oa(sb().Y),e=Nj(e);e.Cb;)var h=e.t(),f=f.$,f=(new Bb).oa(tb(rb(),f,pu(a,h)));return tb(c,f.$,pu(a,b))}throw(new x).o(b);}function Rx(a,b){var c=(new ic).i("");return Ig((new Nx).oa(v(new w,function(a,b,c){return function(){return"Wrong base: "+b+" is outside of "+Ox(a.Zd,c)+"."}}(a,b,c))),Mx(a,c,Sx()))}
function Tx(a){var b=vb().Kd;if(Lm(a))return(new Ux).o(a.F);if(!Mm(a))throw(new x).o(a);return b.g(a.F)}function Px(a,b,c){a=Ox(a.Zd,b);if(Sx()===c)return(new Vx).Fh(a);if(uc()===c)return(new Wx).Fh(a);Xx||(Xx=(new Yx).b());if(Xx===c)return(new Zx).Fh(a);throw(new x).o(c);}
function Qx(a,b){var c=a.Lc.Gb.g(b);if(Lm(c))var e=c.F,f=fg(a.Lc.Db,b,e),e=Ig((new Nx).oa(v(new w,function(a,b,c,e){return function(){return"Action for page "+c+" at "+b+" is "+e+"."}}(a,b,e,f))),$x(a,e,f));else{if(!Mm(c))throw(new x).o(c);e=ay(a,c.F)}return Ig((new Nx).oa(v(new w,function(a,b,c){return function(){return"Parsed "+b+" to "+c+"."}}(a,b,c))),e)}
function ay(a,b){if(b&&b.$classData&&b.$classData.p.qk){var c=b.Xh;return Mx(a,a.Lc.ub.g(b.Hc),c)}if(b&&b.$classData&&b.$classData.p.Fz)return c=b.TA(),b=b.RA(),Mx(a,c,b);throw(new x).o(b);}function by(a,b){if(b&&b.$classData&&b.$classData.p.rk)return Ac(),(new Xg).o(b);if(b&&b.$classData&&b.$classData.p.nr)return Ac(),a=ay(a,b),(new Pc).o(a);throw(new x).o(b);}
function $x(a,b,c){c=by(a,c);a=Lm(c)?(new Xg).o((new cy).Hh(b,v(new w,function(a,b){return function(){return b.hf.g(a.ph)}}(a,c.F)))):c;return Tx(a)}Ix.prototype.$classData=l({yr:0},!1,"japgolly.scalajs.react.extra.router.RouterLogic",{yr:1,c:1,Cz:1,Dz:1});function dy(){}dy.prototype=new n;dy.prototype.constructor=dy;dy.prototype.b=function(){return this};
function hc(){var a;ey||(ey=(new dy).b());a=ey;return(new Tg).Eh(t(new u,function(){return function(){return B()}}(a)),t(new u,function(){return function(){return B()}}(a)),Fb(function(){return function(){return B()}}(a)))}dy.prototype.$classData=l({Fr:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Rule$",{Fr:1,c:1,k:1,h:1});var ey=void 0;function fy(){this.Dn=this.vg=null}fy.prototype=new n;fy.prototype.constructor=fy;
fy.prototype.b=function(){gy=this;this.vg=Fb(function(){return function(a,b){a=a.$;return(new Bb).oa(tb(rb(),a,b.U().$))}}(this));this.Dn=Fb(function(){return function(a,b){a=a.$;var c=rb();b=b.U().$;var e=rb();return(new Bb).oa(v(new w,function(a,b,c,e){return function(){return!!fg(e,b,c)}}(e,(new Bb).oa(a).$,b,Fb(function(){return function(a,b){return!!a.U()||!!b.U()}}(c)))))}}(this));return this};fy.prototype.$classData=l({Tr:0},!1,"japgolly.scalajs.react.internal.Semigroup$",{Tr:1,c:1,k:1,h:1});
var gy=void 0;function Pe(){gy||(gy=(new fy).b());return gy}function hy(){this.aa=null}hy.prototype=new Bu;hy.prototype.constructor=hy;hy.prototype.i=function(a){ii.prototype.i.call(this,a);return this};hy.prototype.$classData=l({$r:0},!1,"japgolly.scalajs.react.vdom.Attr$Event",{$r:1,um:1,Oi:1,c:1});function Hu(){this.Ty=this.Ak=this.Ou=this.bn=this.an=this.Nu=this.zj=this.Va=this.Kk=this.Nl=this.gi=null}Hu.prototype=new n;Hu.prototype.constructor=Hu;Hu.prototype.b=function(){ui(this);return this};
Hu.prototype.$classData=l({es:0},!1,"japgolly.scalajs.react.vdom.Builder$ToVdomElement",{es:1,c:1,Iz:1,Hz:1});function iy(){}iy.prototype=new n;iy.prototype.constructor=iy;iy.prototype.b=function(){return this};function fd(a){var b=xb(),c=new Gu;a=zb(new Ab,a,b);jy||(jy=(new ky).b());c.Np="div";c.ol=a;return c}iy.prototype.$classData=l({hs:0},!1,"japgolly.scalajs.react.vdom.HtmlTagOf$",{hs:1,c:1,k:1,h:1});var ly=void 0;function Zc(){ly||(ly=(new iy).b())}function ky(){}ky.prototype=new n;
ky.prototype.constructor=ky;ky.prototype.b=function(){return this};ky.prototype.$classData=l({ps:0},!1,"japgolly.scalajs.react.vdom.Namespace$",{ps:1,c:1,k:1,h:1});var jy=void 0;function Ki(){}Ki.prototype=new n;Ki.prototype.constructor=Ki;Ki.prototype.b=function(){return this};Ki.prototype.$classData=l({vs:0},!1,"japgolly.scalajs.react.vdom.SvgTagOf$",{vs:1,c:1,k:1,h:1});var Ji=void 0;function my(){this.Ro=this.Jj=null}my.prototype=new Lu;my.prototype.constructor=my;
function Ku(a){var b=new my;b.Ro=a;ed.prototype.el.call(b,a);return b}my.prototype.$classData=l({Bs:0},!1,"japgolly.scalajs.react.vdom.VdomElement",{Bs:1,vm:1,c:1,Pi:1});function Yi(){}Yi.prototype=new n;Yi.prototype.constructor=Yi;Yi.prototype.b=function(){return this};Yi.prototype.$classData=l({Gs:0},!1,"japgolly.univeq.UnivEq$$anon$1",{Gs:1,c:1,Sz:1,Cf:1});function Zi(){}Zi.prototype=new n;Zi.prototype.constructor=Zi;Zi.prototype.b=function(){return this};
Zi.prototype.$classData=l({Hs:0},!1,"japgolly.univeq.UnivEq$$anon$2",{Hs:1,c:1,Oz:1,Cf:1});function $i(){}$i.prototype=new n;$i.prototype.constructor=$i;$i.prototype.b=function(){return this};$i.prototype.$classData=l({Is:0},!1,"japgolly.univeq.UnivEq$$anon$3",{Is:1,c:1,Qz:1,Cf:1});function aj(){}aj.prototype=new n;aj.prototype.constructor=aj;aj.prototype.b=function(){return this};aj.prototype.$classData=l({Js:0},!1,"japgolly.univeq.UnivEq$$anon$4",{Js:1,c:1,Pz:1,Cf:1});function bj(){}
bj.prototype=new n;bj.prototype.constructor=bj;bj.prototype.b=function(){return this};bj.prototype.$classData=l({Ks:0},!1,"japgolly.univeq.UnivEq$$anon$5",{Ks:1,c:1,Rz:1,Cf:1});function cj(){}cj.prototype=new n;cj.prototype.constructor=cj;cj.prototype.b=function(){return this};cj.prototype.$classData=l({Ls:0},!1,"japgolly.univeq.UnivEq$$anon$6",{Ls:1,c:1,Tz:1,Cf:1});function dj(){}dj.prototype=new n;dj.prototype.constructor=dj;dj.prototype.b=function(){return this};
dj.prototype.$classData=l({Ms:0},!1,"japgolly.univeq.UnivEq$$anon$7",{Ms:1,c:1,Nz:1,Cf:1});function ad(){}ad.prototype=new n;ad.prototype.constructor=ad;ad.prototype.b=function(){return this};ad.prototype.$classData=l({Ts:0},!1,"scalacss.ScalaCssReact$",{Ts:1,c:1,Wz:1,Us:1});var $c=void 0;function ny(){this.jg=this.fc=this.Zb=this.Yb=this.Xb=this.Wb=this.Ub=this.Tb=this.Sb=this.Rb=this.Qb=this.Pb=this.ec=this.Ob=this.Nb=this.Mb=this.Lb=null}ny.prototype=new n;ny.prototype.constructor=ny;
ny.prototype.b=function(){oy=this;this.Lb=py(new qy,Tk(),ll());this.Mb=py(new qy,Tk(),ll());this.Nb=py(new qy,Qk(),ll());for(var a=Tk(),b=Tk(),b=[(new H).d("9.9",b)],c=jl(new kl,ll()),e=0,f=b.length|0;e<f;)ml(c,b[e]),e=1+e|0;this.Ob=py(new qy,a,c.w);this.ec=py(new qy,Tk(),ll());this.Pb=py(new qy,Tk(),ll());this.Qb=py(new qy,Rk(),ll());this.Rb=py(new qy,Qk(),ll());this.Sb=py(new qy,Rk(),ll());this.Tb=py(new qy,Rk(),ll());this.Ub=py(new qy,Tk(),ll());a=Tk();b=Sk();b=[(new H).d("9,9.5-9.6,10.0-10.1,10.5,10.6,11,11.1,11.5,11.6,12,12.1",
b)];c=jl(new kl,ll());e=0;for(f=b.length|0;e<f;)ml(c,b[e]),e=1+e|0;this.Wb=py(new qy,a,c.w);this.Xb=py(new qy,Sk(),ll());a=Sk();b=Tk();b=[(new H).d("37",b)];c=jl(new kl,ll());e=0;for(f=b.length|0;e<f;)ml(c,b[e]),e=1+e|0;this.Yb=py(new qy,a,c.w);this.Zb=py(new qy,Tk(),ll());this.fc=py(new qy,Tk(),ll());a=this.Lb;b=(new A).f([this.Mb,this.Nb,this.Ob,this.ec,this.Pb,this.Qb,this.Rb,this.Sb,this.Tb,this.Ub,this.Wb,this.Xb,this.Yb,this.Zb,this.fc]);Uk();c=Vk().Ja;b=Wk(b,c);this.jg=Xk(new Yk,a,b);return this};
ny.prototype.$classData=l({st:0},!1,"scalacss.internal.CanIUse$Agent$",{st:1,c:1,k:1,h:1});var oy=void 0;function J(){oy||(oy=(new ny).b());return oy}function ry(){this.cp=null;this.Z=0}ry.prototype=new n;ry.prototype.constructor=ry;ry.prototype.b=function(){return this};function sy(){var a=ty();if(0===(1&a.Z)&&0===(1&a.Z)){var b=zl(),c=Bl(),e=new uy,f=new dv;f.so=b;f.Sp=c;e.ki=f;a.cp=e;a.Z=(1|a.Z)<<24>>24}return a.cp}
ry.prototype.$classData=l({Gt:0},!1,"scalacss.internal.Compose$",{Gt:1,c:1,k:1,h:1});var vy=void 0;function ty(){vy||(vy=(new ry).b());return vy}function wy(){this.Y=null}wy.prototype=new n;wy.prototype.constructor=wy;wy.prototype.b=function(){xy=this;var a=B(),b=Ac().sc.Vb,c=new yy;c.hi=a;c.Jg=b;this.Y=c;return this};wy.prototype.$classData=l({Jt:0},!1,"scalacss.internal.Cond$",{Jt:1,c:1,k:1,h:1});var xy=void 0;function Wo(){xy||(xy=(new wy).b());return xy}function zy(){this.s=this.Va=null}
zy.prototype=new n;zy.prototype.constructor=zy;zy.prototype.b=function(){Ay=this;this.Va=mm(new lm,t(new u,function(){return function(a){return a.Va}}(this)),Fb(function(){return function(a,b){return qk(b,a.s)}}(this)));this.s=mm(new lm,t(new u,function(){return function(a){return a.s}}(this)),Fb(function(){return function(a,b){return qk(a.Va,b)}}(this)));return this};zy.prototype.$classData=l({Lt:0},!1,"scalacss.internal.CssKV$",{Lt:1,c:1,k:1,h:1});var Ay=void 0;
function ko(){Ay||(Ay=(new zy).b());return Ay}function By(){this.Y=null}By.prototype=new n;By.prototype.constructor=By;By.prototype.b=function(){Cy=this;var a=B(),b=(new Dy).Wf(a,a,a,a,a,a,a,a),c=B(),a=new Ey,e=new Fy;e.gk=c;e.oj=c;e.Yi=c;e.hj=c;e.gj=c;e.fj=c;e.yj=c;e.aj=c;e.bj=c;e.Aj=c;e.Nj=c;e.Uj=c;e.Tf=c;a.Gj=b;a.wj=e;for(var e=gl(),c=$k(b),b=nl(e),f=c.Wa,e=b.g(c.Qc),c=Nj(f);c.Cb;)f=c.t(),f=b.g(f),e=e.kg(f);a.Mo=e;this.Y=a;return this};
By.prototype.$classData=l({St:0},!1,"scalacss.internal.Env$",{St:1,c:1,k:1,h:1});var Cy=void 0;function Gy(){Cy||(Cy=(new By).b());return Cy}function Hy(){this.s=null}Hy.prototype=new Em;Hy.prototype.constructor=Hy;Hy.prototype.b=function(){Dm.prototype.i.call(this,"end");return this};Hy.prototype.$classData=l({Xt:0},!1,"scalacss.internal.Literal$Typed$end$",{Xt:1,Vt:1,c:1,Wt:1});var Iy=void 0;function cv(){Iy||(Iy=(new Hy).b());return Iy}function bv(){this.s=null}bv.prototype=new Em;
bv.prototype.constructor=bv;bv.prototype.b=function(){Dm.prototype.i.call(this,"start");return this};bv.prototype.$classData=l({Yt:0},!1,"scalacss.internal.Literal$Typed$start$",{Yt:1,Vt:1,c:1,Wt:1});var av=void 0;function Jy(){this.Y=null}Jy.prototype=new n;Jy.prototype.constructor=Jy;Jy.prototype.b=function(){Ky=this;this.Y=Ly(ll());return this};function Ly(a){var b=Ac().sc.Vb,c=B(),e=Ac().sc.Vb,f=Ac().sc;return Ko(new Lo,a,b,c,e,f.Vb)}
Jy.prototype.$classData=l({nu:0},!1,"scalacss.internal.StyleS$",{nu:1,c:1,k:1,h:1});var Ky=void 0;function My(){hp.call(this);this.Lf=null}My.prototype=new ip;My.prototype.constructor=My;function Ny(){}Ny.prototype=My.prototype;My.prototype.eo=function(a){this.Lf=a;hp.prototype.b.call(this);return this};var va=l({Zu:0},!1,"java.lang.Boolean",{Zu:1,c:1,h:1,md:1},void 0,void 0,function(a){return"boolean"===typeof a});function Oy(){this.s=0}Oy.prototype=new n;Oy.prototype.constructor=Oy;
Oy.prototype.n=function(a){return vt(a)?this.s===a.s:!1};Oy.prototype.m=function(){return g.String.fromCharCode(this.s)};function nt(a){var b=new Oy;b.s=a;return b}Oy.prototype.r=function(){return this.s};function vt(a){return!!(a&&a.$classData&&a.$classData.p.po)}Oy.prototype.$classData=l({po:0},!1,"java.lang.Character",{po:1,c:1,h:1,md:1});function Fw(){this.Bo=null;this.Z=0}Fw.prototype=new n;Fw.prototype.constructor=Fw;Fw.prototype.b=function(){return this};
function Gw(a){if(0===(16&a.Z)<<24>>24&&0===(16&a.Z)<<24>>24){var b=(new A).f([1632,1776,1984,2406,2534,2662,2790,2918,3046,3174,3302,3430,3664,3792,3872,4160,4240,6112,6160,6470,6608,6784,6800,6992,7088,7232,7248,42528,43216,43264,43472,43600,44016,65296,66720,69734,69872,69942,70096,71360,120782,120792,120802,120812,120822]),c=b.na.length|0,c=k(r(Za),[c]),e;e=0;for(b=Th(new Uh,b,0,b.na.length|0);b.H();){var f=b.t();c.e[e]=f|0;e=1+e|0}a.Bo=c;a.Z=(16|a.Z)<<24>>24}return a.Bo}
Fw.prototype.$classData=l({av:0},!1,"java.lang.Character$",{av:1,c:1,k:1,h:1});var Ew=void 0;function Py(){this.ma=null}Py.prototype=new xw;Py.prototype.constructor=Py;function Qy(){}Qy.prototype=Py.prototype;function Ry(){this.ma=null}Ry.prototype=new xw;Ry.prototype.constructor=Ry;function Sy(){}Sy.prototype=Ry.prototype;function Ty(){}Ty.prototype=new n;Ty.prototype.constructor=Ty;Ty.prototype.b=function(){return this};function Dw(a){throw(new Uy).i('For input string: "'+a+'"');}
function Vy(a,b){a=b-(1431655765&b>>1)|0;a=(858993459&a)+(858993459&a>>2)|0;return ca(16843009,252645135&(a+(a>>4)|0))>>24}Ty.prototype.$classData=l({hv:0},!1,"java.lang.Integer$",{hv:1,c:1,k:1,h:1});var Wy=void 0;function Ns(){Wy||(Wy=(new Ty).b());return Wy}function Ct(){this.Z=!1}Ct.prototype=new n;Ct.prototype.constructor=Ct;Ct.prototype.b=function(){return this};Ct.prototype.$classData=l({lv:0},!1,"java.lang.Long$",{lv:1,c:1,k:1,h:1});var Bt=void 0;function Xy(){this.lk=this.ag=null}
Xy.prototype=new n;Xy.prototype.constructor=Xy;Xy.prototype.m=function(){return this.lk};Xy.prototype.$classData=l({Lv:0},!1,"java.util.regex.Pattern",{Lv:1,c:1,k:1,h:1});function Yy(){this.lo=this.mo=null}Yy.prototype=new n;Yy.prototype.constructor=Yy;Yy.prototype.b=function(){Zy=this;this.mo=new g.RegExp("^\\\\Q(.|\\n|\\r)\\\\E$");this.lo=new g.RegExp("^\\(\\?([idmsuxU]*)(?:-([idmsuxU]*))?\\)");return this};
function bt(a,b){a=a.mo.exec(b);if(null!==a){a=a[1];if(void 0===a)throw(new yw).i("undefined.get");a=(new Pf).o((new H).d($y(a),0))}else a=B();if(a.l()){var c=ct().lo.exec(b);if(null!==c){a=c[0];if(void 0===a)throw(new yw).i("undefined.get");a=b.substring(a.length|0);var e=c[1];if(void 0===e)var f=0;else{var e=(new ih).i(e),f=e.cb.length|0,h=0,m=0;a:for(;;){if(h!==f){var q=1+h|0,h=e.Xa(h),h=null===h?0:h.s,m=m|0|az(ct(),h),h=q;continue a}break}f=m|0}c=c[2];if(void 0===c)c=f;else{c=(new ih).i(c);e=
c.cb.length|0;q=0;h=f;a:for(;;){if(q!==e){f=1+q|0;q=c.Xa(q);q=null===q?0:q.s;h=(h|0)&~az(ct(),q);q=f;continue a}break}c=h|0}a=(new Pf).o((new H).d(a,c))}else a=B()}a=a.l()?(new H).d(b,0):a.R();if(null===a)throw(new x).o(a);c=a.P|0;a=new g.RegExp(a.N,"g"+(0!==(2&c)?"i":"")+(0!==(8&c)?"m":""));c=new Xy;c.ag=a;c.lk=b;return c}
function $y(a){for(var b="",c=0;c<(a.length|0);){var e=65535&(a.charCodeAt(c)|0);switch(e){case 92:case 46:case 40:case 41:case 91:case 93:case 123:case 125:case 124:case 63:case 42:case 43:case 94:case 36:e="\\"+nt(e);break;default:e=nt(e)}b=""+b+e;c=1+c|0}return b}function az(a,b){switch(b){case 105:return 2;case 100:return 1;case 109:return 8;case 115:return 32;case 117:return 64;case 120:return 4;case 85:return 256;default:throw(new Ob).i("bad in-pattern flag");}}
Yy.prototype.$classData=l({Mv:0},!1,"java.util.regex.Pattern$",{Mv:1,c:1,k:1,h:1});var Zy=void 0;function ct(){Zy||(Zy=(new Yy).b());return Zy}function ep(){this.Ln=null}ep.prototype=new Ip;ep.prototype.constructor=ep;ep.prototype.b=function(){dp=this;(new or).o(Cp().Eo);this.Ln=(new or).o(Cp().Jn);(new or).o(null);return this};ep.prototype.$classData=l({Qv:0},!1,"scala.Console$",{Qv:1,VA:1,c:1,eB:1});var dp=void 0;function eg(){}eg.prototype=new n;eg.prototype.constructor=eg;eg.prototype.b=function(){return this};
eg.prototype.Ck=function(a){return null===a?B():(new Pf).o(a)};eg.prototype.$classData=l({Uv:0},!1,"scala.Option$",{Uv:1,c:1,k:1,h:1});var dg=void 0;function bz(){this.fp=this.j=this.Kc=null}bz.prototype=new Mp;bz.prototype.constructor=bz;bz.prototype.b=function(){cz=this;Ac();hg();dz||(dz=(new ez).b());this.Kc=dz;this.j=fz();lr||(lr=(new kr).b());lr||(lr=(new kr).b());gz||(gz=(new hz).b());(new Hw).b();(new iz).b();this.fp=(new jz).b();return this};
function kz(a,b){if(!b)throw(new Ob).i("requirement failed");}bz.prototype.$classData=l({$v:0},!1,"scala.Predef$",{$v:1,YA:1,c:1,WA:1});var cz=void 0;function I(){cz||(cz=(new bz).b());return cz}function lz(){}lz.prototype=new n;lz.prototype.constructor=lz;lz.prototype.b=function(){return this};lz.prototype.$classData=l({ew:0},!1,"scala.StringContext$",{ew:1,c:1,k:1,h:1});var mz=void 0;function sq(){}sq.prototype=new n;sq.prototype.constructor=sq;sq.prototype.b=function(){return this};
sq.prototype.$classData=l({iw:0},!1,"scala.math.Fractional$",{iw:1,c:1,k:1,h:1});var rq=void 0;function uq(){}uq.prototype=new n;uq.prototype.constructor=uq;uq.prototype.b=function(){return this};uq.prototype.$classData=l({jw:0},!1,"scala.math.Integral$",{jw:1,c:1,k:1,h:1});var tq=void 0;function wq(){}wq.prototype=new n;wq.prototype.constructor=wq;wq.prototype.b=function(){return this};wq.prototype.$classData=l({kw:0},!1,"scala.math.Numeric$",{kw:1,c:1,k:1,h:1});var vq=void 0;function Aq(){}
Aq.prototype=new n;Aq.prototype.constructor=Aq;Aq.prototype.b=function(){return this};Aq.prototype.$classData=l({Nw:0},!1,"scala.util.Either$",{Nw:1,c:1,k:1,h:1});var zq=void 0;function Cq(){}Cq.prototype=new n;Cq.prototype.constructor=Cq;Cq.prototype.b=function(){return this};Cq.prototype.m=function(){return"Left"};Cq.prototype.$classData=l({Ow:0},!1,"scala.util.Left$",{Ow:1,c:1,k:1,h:1});var Bq=void 0;function Eq(){}Eq.prototype=new n;Eq.prototype.constructor=Eq;Eq.prototype.b=function(){return this};
Eq.prototype.m=function(){return"Right"};Eq.prototype.$classData=l({Pw:0},!1,"scala.util.Right$",{Pw:1,c:1,k:1,h:1});var Dq=void 0;function nz(){this.Zl=!1}nz.prototype=new n;nz.prototype.constructor=nz;nz.prototype.b=function(){this.Zl=!1;return this};nz.prototype.$classData=l({Sw:0},!1,"scala.util.control.NoStackTrace$",{Sw:1,c:1,k:1,h:1});var oz=void 0;function pz(){this.Ng=null}pz.prototype=new n;pz.prototype.constructor=pz;
function jh(a){var b=new pz,c=ct();pz.prototype.Uu.call(b,bt(c,a));return b}pz.prototype.Uu=function(a){this.Ng=a;return this};pz.prototype.m=function(){return this.Ng.lk};pz.prototype.$classData=l({Vw:0},!1,"scala.util.matching.Regex",{Vw:1,c:1,k:1,h:1});function qz(){this.ya=null}qz.prototype=new cx;qz.prototype.constructor=qz;qz.prototype.b=function(){bx.prototype.Jh.call(this,Vk());return this};qz.prototype.sg=function(){Vk();pm();Uk();return(new ql).b()};
qz.prototype.$classData=l({Zw:0},!1,"scala.collection.IndexedSeq$$anon$1",{Zw:1,hp:1,c:1,Vj:1});function rz(){this.Ka=null}rz.prototype=new $w;rz.prototype.constructor=rz;function sz(){}sz.prototype=rz.prototype;function ax(){this.Oa=this.ya=null}ax.prototype=new cx;ax.prototype.constructor=ax;ax.prototype.sg=function(){return this.Oa.da()};ax.prototype.Jh=function(a){if(null===a)throw np(Xh(),null);this.Oa=a;bx.prototype.Jh.call(this,a);return this};
ax.prototype.$classData=l({rx:0},!1,"scala.collection.generic.GenTraversableFactory$$anon$1",{rx:1,hp:1,c:1,Vj:1});function tz(){}tz.prototype=new ex;tz.prototype.constructor=tz;function uz(){}uz.prototype=tz.prototype;function dq(){}dq.prototype=new n;dq.prototype.constructor=dq;dq.prototype.b=function(){return this};dq.prototype.m=function(){return"::"};dq.prototype.$classData=l({vx:0},!1,"scala.collection.immutable.$colon$colon$",{vx:1,c:1,k:1,h:1});var cq=void 0;function oq(){}oq.prototype=new n;
oq.prototype.constructor=oq;oq.prototype.b=function(){return this};oq.prototype.$classData=l({cy:0},!1,"scala.collection.immutable.Range$",{cy:1,c:1,k:1,h:1});var nq=void 0;function vz(){this.ya=null}vz.prototype=new cx;vz.prototype.constructor=vz;vz.prototype.b=function(){bx.prototype.Jh.call(this,iq());return this};vz.prototype.$classData=l({oy:0},!1,"scala.collection.immutable.Stream$StreamCanBuildFrom",{oy:1,hp:1,c:1,Vj:1});function mq(){}mq.prototype=new n;mq.prototype.constructor=mq;
mq.prototype.b=function(){return this};mq.prototype.$classData=l({Sy:0},!1,"scala.collection.mutable.StringBuilder$",{Sy:1,c:1,k:1,h:1});var lq=void 0;function w(){this.Fb=null}w.prototype=new nx;w.prototype.constructor=w;w.prototype.U=function(){return(0,this.Fb)()};function v(a,b){a.Fb=b;return a}w.prototype.$classData=l({Xy:0},!1,"scala.scalajs.runtime.AnonFunction0",{Xy:1,DB:1,c:1,vz:1});function u(){this.Fb=null}u.prototype=new px;u.prototype.constructor=u;
u.prototype.g=function(a){return(0,this.Fb)(a)};function t(a,b){a.Fb=b;return a}u.prototype.$classData=l({Yy:0},!1,"scala.scalajs.runtime.AnonFunction1",{Yy:1,Hp:1,c:1,T:1});function wz(){this.Fb=null}wz.prototype=new rx;wz.prototype.constructor=wz;function Fb(a){var b=new wz;b.Fb=a;return b}function fg(a,b,c){return(0,a.Fb)(b,c)}wz.prototype.$classData=l({Zy:0},!1,"scala.scalajs.runtime.AnonFunction2",{Zy:1,EB:1,c:1,wz:1});function xz(){this.Fb=null}xz.prototype=new tx;xz.prototype.constructor=xz;
function Xf(a){var b=new xz;b.Fb=a;return b}xz.prototype.Vi=function(a,b,c){return(0,this.Fb)(a,b,c)};xz.prototype.$classData=l({$y:0},!1,"scala.scalajs.runtime.AnonFunction3",{$y:1,fz:1,c:1,Xp:1});function yz(){this.Fb=null}yz.prototype=new vx;yz.prototype.constructor=yz;function Ai(a){var b=new yz;b.Fb=a;return b}yz.prototype.$classData=l({az:0},!1,"scala.scalajs.runtime.AnonFunction4",{az:1,FB:1,c:1,xz:1});function zz(){this.he=0;this.Ri=null}zz.prototype=new n;zz.prototype.constructor=zz;
zz.prototype.b=function(){Az=this;this.Ri=(new $s).Ie(0,0);return this};function Bz(a,b,c){return 0===(-2097152&c)?""+(4294967296*c+ +(b>>>0)):Ws(a,b,c,1E9,2)}function Ut(a,b,c){return 0>c?-(4294967296*+((0!==b?~c:-c|0)>>>0)+ +((-b|0)>>>0)):4294967296*c+ +(b>>>0)}function Hb(a,b,c){return c===b>>31?""+b:0>c?"-"+Bz(a,-b|0,0!==b?~c:-c|0):Bz(a,b,c)}
function Ws(a,b,c,e,f){var h=(32+da(e)|0)-(0!==c?da(c):32+da(b)|0)|0,m=h,q=0===(32&m)?e<<m:0,y=0===(32&m)?(e>>>1|0)>>>(31-m|0)|0|0<<m:e<<m,m=b,N=c;for(b=c=0;0<=h&&0!==(-2097152&N);){var z=m,E=N,C=q,F=y;if(E===F?(-2147483648^z)>=(-2147483648^C):(-2147483648^E)>=(-2147483648^F))z=N,E=y,N=m-q|0,z=(-2147483648^N)>(-2147483648^m)?-1+(z-E|0)|0:z-E|0,m=N,N=z,32>h?c|=1<<h:b|=1<<h;h=-1+h|0;z=y>>>1|0;q=q>>>1|0|y<<31;y=z}h=N;if(0===h?(-2147483648^m)>=(-2147483648^e):-2147483648<=(-2147483648^h))h=4294967296*
N+ +(m>>>0),e=0+ +(e>>>0),1!==f&&(z=h/e,q=z/4294967296|0,y=c,c=z=y+(z|0)|0,b=(-2147483648^z)<(-2147483648^y)?1+(b+q|0)|0:b+q|0),0!==f&&(e=h%e,m=e|0,N=e/4294967296|0);if(0===f)return a.he=b,c;if(1===f)return a.he=N,m;a=""+m;return""+(4294967296*b+ +(c>>>0))+"000000000".substring(a.length|0)+a}zz.prototype.$classData=l({cz:0},!1,"scala.scalajs.runtime.RuntimeLong$",{cz:1,c:1,k:1,h:1});var Az=void 0;function Qa(){Az||(Az=(new zz).b());return Az}function Cz(){}Cz.prototype=new n;
Cz.prototype.constructor=Cz;function Dz(){}Dz.prototype=Cz.prototype;Cz.prototype.g=function(a){return this.Ze(a,Sp().Hn)};Cz.prototype.m=function(){return"\x3cfunction1\x3e"};Cz.prototype.Gd=function(a){return Ez(this,a)};var Fz=l({kz:0},!1,"scala.runtime.Nothing$",{kz:1,Ua:1,c:1,h:1});function Gz(){My.call(this);this.ti=this.Qn=null}Gz.prototype=new Ny;Gz.prototype.constructor=Gz;
Gz.prototype.b=function(){var a=Hz();My.prototype.eo.call(this,No(new Eo,a.Pk,rv(),a.Ok));Iz=this;var a=rw(this),b=a.ya.Lf,c=ym().xb;wm||(wm=(new xm).b());this.Qn=Po(b,zm(c),a.ya.wg);a=tw(this);lp(this);lp(this);Jz||(Jz=(new Kz).b());b=Jz;lp(this);lp(this);Lz||(Lz=(new Mz).b());c=Nz();lp(this);var e=lp(this),e=so(vo(),t(new u,function(){return function(a){return a.mc()}}(e)));lp(this);var f=vo(),c=qo(oo(e,(new po).zb(t(new u,function(){return function(a){return a}}(f)))),c).s,b=tm(new qm,Oz(Pj(b.Xd,
c)));lp(this);lp(this);Wu||(Wu=(new Vu).b());c=tm(new qm,Oz(Pj(Wu.Eb,this.Qn.xb)));lp(this);lp(this);Pz||(Pz=(new Qz).b());e=tm(new qm,Oz(Pj(Pz.Ff,"white")));lp(this);lp(this);$u||($u=(new Zu).b());c=[b,c,e,tm(new qm,Oz(Pj($u.Eb,"center")))];Hz();e=sy();f=Rz();if(0===(c.length|0))b=f.dh.Y;else{aq();Sz();for(var b=[],h=0,m=c.length|0;h<m;)b.push(c[h].ma),h=1+h|0;c=function(a,b){return function(a,c){return Tz(b,a,c)}}(f,e);if(0<(b.length|0))a:for(e=b.length|0,f=1,m=b[0];;){if(f!==e){h=1+f|0;m=c(m,b[f]);
f=h;continue a}b=m;break}else{if(0===(b.length|0))throw(new Uz).i("empty.reduceLeft");e=!1;e=!0;f=null;h=f=0;for(m=b.length|0;h<m;){var q=b[h];e?(f=q,e=!1):f=c(f,q);h=1+h|0}b=f}}b=Go(a.ya.Lf,a.aa,b,a.ya.wg);this.ti=So(a.ya.Lf,b,a.ya.wg);return this};Gz.prototype.$classData=l({dq:0},!1,"components.Title$Style$",{dq:1,Hu:1,Fu:1,c:1,Zt:1});var Iz=void 0;function bd(){Iz||(Iz=(new Gz).b());return Iz}function Vz(){this.s=this.Bc=this.Va=null}Vz.prototype=new n;Vz.prototype.constructor=Vz;
Vz.prototype.b=function(){Wz=this;Du||(Du=(new Cu).b());this.Va=Du;(new hy).i("onChange");(new hy).i("onClick");(new hy).i("onClickCapture");this.Bc=(Xz(),(new Au).i("src"));Xz();(new Au).i("title");Xz();(new Au).i("type");this.s=(Xz(),(new Au).i("value"));return this};Vz.prototype.$classData=l({gs:0},!1,"japgolly.scalajs.react.vdom.HtmlAttrAndStyles$",{gs:1,c:1,Jz:1,Kz:1,Lz:1});var Wz=void 0;function Yz(){}Yz.prototype=new n;Yz.prototype.constructor=Yz;function Zz(){}Zz.prototype=Yz.prototype;
Yz.prototype.$i=function(){};function $z(){My.call(this);this.ti=null}$z.prototype=new Ny;$z.prototype.constructor=$z;
$z.prototype.b=function(){var a=Hz();My.prototype.eo.call(this,No(new Eo,a.Pk,rv(),a.Ok));aA=this;a=tw(this);lp(this);lp(this);var b;bA||(bA=(new cA).b());b=bA;lp(this);lp(this);var c=(new dA).o(100);lp(this);var e=lp(this),e=so(vo(),t(new u,function(){return function(a){return a.mc()}}(e)));lp(this);var f=vo(),c=qo(oo(e,(new po).zb(t(new u,function(){return function(a){return a}}(f)))),c).s;b=tm(new qm,Oz(Pj(b.Xd,c)));lp(this);lp(this);eA||(eA=(new fA).b());c=eA;lp(this);lp(this);e=(new dA).o(100);
lp(this);f=lp(this);f=so(vo(),t(new u,function(){return function(a){return a.mc()}}(f)));lp(this);var h=vo(),e=qo(oo(f,(new po).zb(t(new u,function(){return function(a){return a}}(h)))),e).s,c=tm(new qm,Oz(Pj(c.Xd,e)));lp(this);lp(this);gA||(gA=(new hA).b());e=gA;f=lp(this).zo(Cl(Dl((new A).f(["rgb(",",",",",")"])),(new A).f([79,112,165]))).s;e=tm(new qm,Oz(Pj(e.Ff,f)));lp(this);lp(this);Su||(Su=(new Ru).b());f=tm(new qm,Oz(Pj(Su.Eb,"flex")));lp(this);lp(this);Uu||(Uu=(new Tu).b());h=tm(new qm,Oz(Pj(Uu.Eb,
"column")));lp(this);lp(this);Yu||(Yu=(new Xu).b());c=[b,c,e,f,h,tm(new qm,Oz(Pj(Yu.Eb,"center")))];Hz();e=sy();f=Rz();if(0===(c.length|0))b=f.dh.Y;else{aq();Sz();b=[];for(var h=0,m=c.length|0;h<m;)b.push(c[h].ma),h=1+h|0;c=function(a,b){return function(a,c){return Tz(b,a,c)}}(f,e);if(0<(b.length|0))a:for(e=b.length|0,f=1,m=b[0];;){if(f!==e){h=1+f|0;m=c(m,b[f]);f=h;continue a}b=m;break}else{if(0===(b.length|0))throw(new Uz).i("empty.reduceLeft");e=!1;e=!0;f=null;h=f=0;for(m=b.length|0;h<m;){var q=
b[h];e?(f=q,e=!1):f=c(f,q);h=1+h|0}b=f}}b=Go(a.ya.Lf,a.aa,b,a.ya.wg);this.ti=So(a.ya.Lf,b,a.ya.wg);return this};$z.prototype.$classData=l({Ss:0},!1,"pages.HomePage$Style$",{Ss:1,Hu:1,Fu:1,c:1,Zt:1});var aA=void 0;function jj(){aA||(aA=(new $z).b());return aA}function Kz(){this.Xd=null}Kz.prototype=new lv;Kz.prototype.constructor=Kz;Kz.prototype.b=function(){Jz=this;this.Xd=kk("font-size");return this};Kz.prototype.Yd=function(){return this.Xd};
Kz.prototype.$classData=l({mt:0},!1,"scalacss.internal.Attrs$fontSize$",{mt:1,Qi:1,we:1,c:1,Wm:1});var Jz=void 0;function iA(){this.Xd=null}iA.prototype=new lv;iA.prototype.constructor=iA;function jA(){}jA.prototype=iA.prototype;iA.prototype.b=function(){this.Xd=this.dn().g(fl());return this};iA.prototype.Yd=function(){return this.Xd};var na=l({Mu:0},!1,"java.lang.String",{Mu:1,c:1,h:1,gl:1,md:1},void 0,void 0,function(a){return"string"===typeof a});function kA(){this.ma=null}kA.prototype=new Qy;
kA.prototype.constructor=kA;kA.prototype.o=function(a){ww.prototype.Sa.call(this,""+a,0,0,!0);return this};kA.prototype.$classData=l({Yu:0},!1,"java.lang.AssertionError",{Yu:1,NA:1,Ua:1,c:1,h:1});
var pa=l({$u:0},!1,"java.lang.Byte",{$u:1,$f:1,c:1,h:1,md:1},void 0,void 0,function(a){return oa(a)}),ua=l({dv:0},!1,"java.lang.Double",{dv:1,$f:1,c:1,h:1,md:1},void 0,void 0,function(a){return"number"===typeof a}),ta=l({ev:0},!1,"java.lang.Float",{ev:1,$f:1,c:1,h:1,md:1},void 0,void 0,function(a){return"number"===typeof a}),sa=l({gv:0},!1,"java.lang.Integer",{gv:1,$f:1,c:1,h:1,md:1},void 0,void 0,function(a){return Oa(a)}),ya=l({kv:0},!1,"java.lang.Long",{kv:1,$f:1,c:1,h:1,md:1},void 0,void 0,function(a){return xa(a)});
function lA(){this.ma=null}lA.prototype=new Sy;lA.prototype.constructor=lA;function mA(){}mA.prototype=lA.prototype;lA.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};lA.prototype.$classData=l({ib:0},!1,"java.lang.RuntimeException",{ib:1,mb:1,Ua:1,c:1,h:1});var ra=l({pv:0},!1,"java.lang.Short",{pv:1,$f:1,c:1,h:1,md:1},void 0,void 0,function(a){return qa(a)});function ft(){this.$d=null}ft.prototype=new n;ft.prototype.constructor=ft;d=ft.prototype;
d.b=function(){ft.prototype.cl.call(this,(new nA).b());return this};d.Ol=function(a,b){return this.$d.va.substring(a,b)};d.m=function(){return this.$d.va};d.Bk=function(a){var b=this.$d;b.va=""+b.va+a;return this};function Cw(a,b){a=a.$d;a.va=""+a.va+b}d.W=function(){return this.$d.W()};d.cl=function(a){this.$d=a;return this};d.$classData=l({qv:0},!1,"java.lang.StringBuffer",{qv:1,c:1,gl:1,no:1,h:1});function nA(){this.va=null}nA.prototype=new n;nA.prototype.constructor=nA;d=nA.prototype;
d.b=function(){this.va="";return this};d.Ol=function(a,b){return this.va.substring(a,b)};d.m=function(){return this.va};d.Bk=function(a){this.va=""+this.va+a;return this};d.kb=function(a){nA.prototype.b.call(this);if(0>a)throw(new oA).b();return this};d.W=function(){return this.va.length|0};function kn(a,b){b=g.String.fromCharCode(b);a.va=""+a.va+b}d.i=function(a){nA.prototype.b.call(this);if(null===a)throw(new Ho).b();this.va=a;return this};
d.$classData=l({rv:0},!1,"java.lang.StringBuilder",{rv:1,c:1,gl:1,no:1,h:1});function jt(){this.zf=this.Fe=null;this.Mh=!1}jt.prototype=new n;jt.prototype.constructor=jt;d=jt.prototype;d.b=function(){jt.prototype.Tu.call(this,null);return this};function pA(a,b,c){null===a.Fe?a.zf=""+a.zf+b+c:qA(a,[b,c])}
function Ft(a,b,c){b=a.toExponential(b);a=0===a&&0>1/a?"-"+b:b;b=a.length|0;a=101!==(65535&(a.charCodeAt(-3+b|0)|0))?a:a.substring(0,-1+b|0)+"0"+a.substring(-1+b|0);if(!c||0<=(a.indexOf(".")|0))return a;c=a.indexOf("e")|0;return a.substring(0,c)+"."+a.substring(c)}function rA(a,b){for(var c="",e=0;e!==b;)c=""+c+a,e=1+e|0;return c}function Jt(a,b,c,e){var f=e.length|0;f>=c?lt(a,e):0!==(1&b)?pA(a,e,rA(" ",c-f|0)):pA(a,rA(" ",c-f|0),e)}function Dt(a,b){return 0!==(256&a)?b.toUpperCase():b}
d.m=function(){if(this.Mh)throw(new kt).b();return null===this.Fe?this.zf:this.Fe.m()};function It(a){return(0!==(1&a)?"-":"")+(0!==(2&a)?"#":"")+(0!==(4&a)?"+":"")+(0!==(8&a)?" ":"")+(0!==(16&a)?"0":"")+(0!==(32&a)?",":"")+(0!==(64&a)?"(":"")+(0!==(128&a)?"\x3c":"")}d.Tu=function(a){this.Fe=a;this.zf="";this.Mh=!1;return this};function pt(a,b){if(void 0===a)return b;a=+g.parseInt(a,10);return 2147483647>=a?Ia(a):-1}function sA(a,b,c,e){null===a.Fe?a.zf=a.zf+(""+b+c)+e:qA(a,[b,c,e])}
function At(a,b,c,e,f){var h=(e.length|0)+(f.length|0)|0;h>=c?pA(a,e,f):0!==(16&b)?sA(a,e,rA("0",c-h|0),f):0!==(1&b)?sA(a,e,f,rA(" ",c-h|0)):sA(a,rA(" ",c-h|0),e,f)}function Et(a,b,c,e){Jt(a,b,c,Dt(b,e!==e?"NaN":0<e?0!==(4&b)?"+Infinity":0!==(8&b)?" Infinity":"Infinity":0!==(64&b)?"(Infinity)":"-Infinity"))}function xt(a,b,c,e,f,h){if(null===b)tt(a,e,f,h,"null");else{a=new tA;b=la(b);a.bf=c;a.cn=b;ww.prototype.Sa.call(a,null,0,0,!0);if(null===b)throw(new Ho).b();throw a;}}
function qA(a,b){try{for(var c=0,e=b.length|0;c<e;)a.Fe.Bk(b[c]),c=1+c|0}catch(f){if(!(f&&f.$classData&&f.$classData.p.Os))throw f;}}function Gt(a,b,c){b=a.toFixed(b);a=0===a&&0>1/a?"-"+b:b;return c&&0>(a.indexOf(".")|0)?a+".":a}function tt(a,b,c,e,f){e=0>e?f:f.substring(0,e);Jt(a,b,c,Dt(b,e))}function yt(a){throw(new Ht).i(It(a));}function lt(a,b){null===a.Fe?a.zf=""+a.zf+b:qA(a,[b])}
function st(a,b,c){a=It(a&c);c=new uA;c.jf=a;c.bf=b;ww.prototype.Sa.call(c,null,0,0,!0);if(null===a)throw(new Ho).b();throw c;}
function zt(a,b,c,e){if((e.length|0)>=c&&0===(108&b))lt(a,Dt(b,e));else if(0===(124&b))tt(a,b,c,-1,e);else{if(45!==(65535&(e.charCodeAt(0)|0)))var f=0!==(4&b)?"+":0!==(8&b)?" ":"";else 0!==(64&b)?(e=e.substring(1)+")",f="("):(e=e.substring(1),f="-");if(0!==(32&b)){for(var h=e.length|0,m=0;;){if(m!==h)var q=65535&(e.charCodeAt(m)|0),q=48<=q&&57>=q;else q=!1;if(q)m=1+m|0;else break}m=-3+m|0;if(!(0>=m)){for(h=e.substring(m);3<m;)q=-3+m|0,h=e.substring(q,m)+","+h,m=q;e=e.substring(0,m)+","+h}}At(a,b,
c,f,Dt(b,e))}}d.$i=function(){if(!this.Mh&&null!==this.Fe){var a=this.Fe;if(a&&a.$classData&&a.$classData.p.xk)try{a.$i()}catch(b){if(!(b&&b.$classData&&b.$classData.p.Os))throw b;}}this.Mh=!0};d.$classData=l({yv:0},!1,"java.util.Formatter",{yv:1,c:1,xk:1,oo:1,wm:1});function vA(){}vA.prototype=new Kp;vA.prototype.constructor=vA;vA.prototype.b=function(){return this};
function wA(a,b,c,e,f,h){a=la(b);var m;if(m=!!a.sb.isArrayClass)m=la(e),m.sb.isPrimitive||a.sb.isPrimitive?a=m===a||(m===ma(Ya)?a===ma(Xa):m===ma(Za)?a===ma(Xa)||a===ma(Ya):m===ma(ab)?a===ma(Xa)||a===ma(Ya)||a===ma(Za):m===ma(bb)&&(a===ma(Xa)||a===ma(Ya)||a===ma(Za)||a===ma(ab))):(a=a.sb.getFakeInstance(),a=!!m.sb.isInstance(a)),m=a;if(m)Na(b,c,e,f,h);else for(a=c,c=c+h|0;a<c;){P();h=f;P();m=b;var q=a;if(Kb(m,1)||gb(m,1)||jb(m,1)||hb(m,1)||ib(m,1))m=m.e[q];else if(db(m,1))m=nt(m.e[q]);else if(eb(m,
1)||fb(m,1)||cb(m,1)||Zt(m))m=m.e[q];else{if(null===m)throw(new Ho).b();throw(new x).o(m);}$t(0,e,h,m);a=1+a|0;f=1+f|0}}vA.prototype.$classData=l({Pv:0},!1,"scala.Array$",{Pv:1,XA:1,c:1,k:1,h:1});var xA=void 0;function yA(){xA||(xA=(new vA).b());return xA}function zA(){}zA.prototype=new n;zA.prototype.constructor=zA;function AA(){}AA.prototype=zA.prototype;zA.prototype.m=function(){return"\x3cfunction1\x3e"};
zA.prototype.Gd=function(a){return t(new u,function(a,c){return function(e){return c.g(a.g(e))}}(this,a))};function BA(){}BA.prototype=new n;BA.prototype.constructor=BA;function CA(){}CA.prototype=BA.prototype;BA.prototype.m=function(){return"\x3cfunction1\x3e"};BA.prototype.Gd=function(a){return t(new u,function(a,c){return function(e){return c.g(a.g(e))}}(this,a))};function qq(){}qq.prototype=new n;qq.prototype.constructor=qq;qq.prototype.b=function(){return this};
qq.prototype.$classData=l({hw:0},!1,"scala.math.Equiv$",{hw:1,c:1,fB:1,k:1,h:1});var pq=void 0;function yq(){}yq.prototype=new n;yq.prototype.constructor=yq;yq.prototype.b=function(){return this};yq.prototype.$classData=l({nw:0},!1,"scala.math.Ordering$",{nw:1,c:1,gB:1,k:1,h:1});var xq=void 0;function hz(){}hz.prototype=new n;hz.prototype.constructor=hz;hz.prototype.b=function(){return this};hz.prototype.m=function(){return"\x3c?\x3e"};
hz.prototype.$classData=l({Iw:0},!1,"scala.reflect.NoManifest$",{Iw:1,c:1,Ic:1,k:1,h:1});var gz=void 0;function DA(){}DA.prototype=new n;DA.prototype.constructor=DA;function EA(){}d=EA.prototype=DA.prototype;d.Ga=function(){return this};d.Ue=function(){return this};d.l=function(){return!this.H()};d.m=function(){return Lw(this)};d.O=function(a){tp(this,a)};d.ik=function(a){return Nw(this,a)};d.Af=function(){Uk();var a=Vk().Ja;return Gl(this,a)};d.L=function(){return os(this)};d.kc=function(){return Pw(this)};
d.Ye=function(a,b,c,e){return ps(this,a,b,c,e)};d.We=function(a,b){return ns(this,a,b)};d.Kh=function(){return!1};d.Sk=function(a){return Sw(this,a)};function Ow(){this.Uh=this.Wa=this.Mc=null;this.cf=!1}Ow.prototype=new n;Ow.prototype.constructor=Ow;d=Ow.prototype;d.Ga=function(){return this};d.t=function(){return this.H()?(this.cf=!1,this.Mc.t()):bq().Y.t()};d.Ue=function(){return this};d.l=function(){return!this.H()};d.m=function(){return Lw(this)};d.O=function(a){tp(this,a)};
d.ik=function(a){var b=new ls;b.Qc=a;b.Wa=null;null===this.Wa?this.Wa=b:this.Uh.Wa=b;this.Uh=b;null===this.Mc&&(this.Mc=bq().Y);return this};d.Af=function(){Uk();var a=Vk().Ja;return Gl(this,a)};d.L=function(){return os(this)};
d.H=function(){if(this.cf)return!0;if(null!==this.Mc){if(this.Mc.H())return this.cf=!0;var a;a:for(;;){if(null===this.Wa){this.Uh=this.Mc=null;a=!1;break a}this.Mc=this.Wa.Qc.U().Ue();this.Wa=this.Wa.Wa;c:for(;;){if((a=this.Mc)&&a.$classData&&a.$classData.p.dp){a=this.Mc;this.Mc=a.Mc;this.cf=a.cf;null!==a.Wa&&(a.Uh.Wa=this.Wa,this.Wa=a.Wa);continue c}break}if(this.cf){a=!0;break a}if(this.Mc.H()){a=this.cf=!0;break a}}return a}return!1};d.kc=function(){return Pw(this)};
d.Ye=function(a,b,c,e){return ps(this,a,b,c,e)};d.We=function(a,b){return ns(this,a,b)};d.Kh=function(){return!1};d.Sk=function(a){return Sw(this,a)};d.$classData=l({dp:0},!1,"scala.collection.Iterator$ConcatIterator",{dp:1,c:1,Hb:1,K:1,J:1});function FA(){}FA.prototype=new Ww;FA.prototype.constructor=FA;function GA(){}GA.prototype=FA.prototype;function ez(){}ez.prototype=new uz;ez.prototype.constructor=ez;ez.prototype.b=function(){return this};ez.prototype.In=function(){return ll()};
ez.prototype.$classData=l({Ux:0},!1,"scala.collection.immutable.Map$",{Ux:1,sx:1,tx:1,px:1,c:1});var dz=void 0;function Ps(){this.xc=this.s=this.Va=null}Ps.prototype=new n;Ps.prototype.constructor=Ps;function HA(a){return"(kv: "+a.Va+", "+a.s+")"+(null!==a.xc?" -\x3e "+HA(a.xc):"")}Ps.prototype.d=function(a,b){this.Va=a;this.s=b;return this};Ps.prototype.m=function(){return HA(this)};Ps.prototype.$classData=l({Ey:0},!1,"scala.collection.mutable.DefaultEntry",{Ey:1,c:1,zp:1,k:1,h:1});
function IA(){this.w=this.Y=null}IA.prototype=new n;IA.prototype.constructor=IA;function JA(a,b){a.Y=b;a.w=b;return a}d=IA.prototype;d.rc=function(a){this.w.rc(a);return this};d.La=function(){return this.w};d.fd=function(a,b){lx(this,a,b)};d.rb=function(a){this.w.rc(a);return this};d.qc=function(){};d.fb=function(a){return Tl(this,a)};d.$classData=l({Fy:0},!1,"scala.collection.mutable.GrowingBuilder",{Fy:1,c:1,Sd:1,Qd:1,Pd:1});function $s(){this.ic=this.bb=0}$s.prototype=new vw;
$s.prototype.constructor=$s;d=$s.prototype;d.n=function(a){return xa(a)?this.bb===a.bb&&this.ic===a.ic:!1};d.vc=function(a,b,c){$s.prototype.Ie.call(this,a|b<<22,b>>10|c<<12);return this};d.m=function(){return Hb(Qa(),this.bb,this.ic)};d.Ie=function(a,b){this.bb=a;this.ic=b;return this};d.kb=function(a){$s.prototype.Ie.call(this,a,a>>31);return this};d.r=function(){return this.bb^this.ic};function xa(a){return!!(a&&a.$classData&&a.$classData.p.Fp)}
d.$classData=l({Fp:0},!1,"scala.scalajs.runtime.RuntimeLong",{Fp:1,$f:1,c:1,h:1,md:1});function KA(){this.pd=null}KA.prototype=new n;KA.prototype.constructor=KA;d=KA.prototype;d.A=function(){return"Mod"};d.y=function(){return 1};d.n=function(a){var b;ce();b=this.pd;a&&a.$classData&&a.$classData.p.cm?(a=null===a?null:a.pd,b=null===b?null===a:b.n(a)):b=!1;return b};d.z=function(a){a:switch(ce(),a){case 0:a=this.pd;break a;default:throw(new Y).i(""+a);}return a};
d.m=function(){ce();var a=this.pd;return al(P(),(new KA).zb(a))};d.zb=function(a){this.pd=a;return this};d.r=function(){return this.pd.r()};d.B=function(){ce();return Z((new KA).zb(this.pd))};d.$classData=l({cm:0},!1,"japgolly.scalajs.react.CtorType$Mod",{cm:1,c:1,C:1,q:1,k:1,h:1});function LA(){}LA.prototype=new px;LA.prototype.constructor=LA;LA.prototype.b=function(){return this};LA.prototype.g=function(a){return(new KA).zb(a)};LA.prototype.m=function(){return"Mod"};
function be(a,b,c){b.g(c);return c}LA.prototype.$classData=l({gq:0},!1,"japgolly.scalajs.react.CtorType$Mod$",{gq:1,Hp:1,c:1,T:1,k:1,h:1});var MA=void 0;function ce(){MA||(MA=(new LA).b());return MA}function Ie(){this.xe=this.Pe=this.De=this.Ce=this.Be=this.Ae=this.ze=this.ye=null}Ie.prototype=new n;Ie.prototype.constructor=Ie;d=Ie.prototype;d.A=function(){return"Lifecycle"};d.y=function(){return 8};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.dm){var b=this.ye,c=a.ye;(null===b?null===c:b.n(c))?(b=this.ze,c=a.ze,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Ae,c=a.Ae,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Be,c=a.Be,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Ce,c=a.Ce,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.De,c=a.De,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Pe,c=a.Pe,b=null===b?null===c:b.n(c)):b=!1;if(b)return b=this.xe,a=a.xe,null===b?null===a:b.n(a)}return!1};
d.z=function(a){switch(a){case 0:return this.ye;case 1:return this.ze;case 2:return this.Ae;case 3:return this.Be;case 4:return this.Ce;case 5:return this.De;case 6:return this.Pe;case 7:return this.xe;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};function He(a,b,c,e,f,h,m,q,y){a.ye=b;a.ze=c;a.Ae=e;a.Be=f;a.Ce=h;a.De=m;a.Pe=q;a.xe=y;return a}
function Qe(a,b,c,e){return b.pd.g(t(new u,function(a,b,c){return function(e){e.l()?e=b:(e=e.R(),e=t(new u,function(a,b,c,e){return function(f){return fg(c,e.g(f),v(new w,function(a,b,c){return function(){return b.g(c)}}(a,b,f)))}}(a,b,c,e)));return(new Pf).o(e)}}(a,c,e))).g(a)}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({dm:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle",{dm:1,c:1,C:1,q:1,k:1,h:1});function cy(){this.sf=this.Hc=null}cy.prototype=new n;
cy.prototype.constructor=cy;d=cy.prototype;d.A=function(){return"Resolution"};d.y=function(){return 2};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.nm?D(G(),this.Hc,a.Hc)?this.sf===a.sf:!1:!1};d.z=function(a){switch(a){case 0:return this.Hc;case 1:return this.sf;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.Hh=function(a,b){this.Hc=a;this.sf=b;return this};
d.$classData=l({nm:0},!1,"japgolly.scalajs.react.extra.router.Resolution",{nm:1,c:1,C:1,q:1,k:1,h:1});function NA(){this.od=this.Og=this.ge=this.Db=this.ub=this.Gb=null}NA.prototype=new n;NA.prototype.constructor=NA;d=NA.prototype;d.A=function(){return"RouterConfig"};d.y=function(){return 6};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.pm){var b=this.Gb,c=a.Gb;(null===b?null===c:b.n(c))?(b=this.ub,c=a.ub,b=null===b?null===c:b.n(c)):b=!1;if(b&&this.Db===a.Db&&this.ge===a.ge&&this.Og===a.Og)return b=this.od,a=a.od,null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.Gb;case 1:return this.ub;case 2:return this.Db;case 3:return this.ge;case 4:return this.Og;case 5:return this.od;default:throw(new Y).i(""+a);}};
d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({pm:0},!1,"japgolly.scalajs.react.extra.router.RouterConfig",{pm:1,c:1,C:1,q:1,k:1,h:1});function Tg(){this.Db=this.ub=this.Gb=null}Tg.prototype=new n;Tg.prototype.constructor=Tg;d=Tg.prototype;d.Eh=function(a,b,c){this.Gb=a;this.ub=b;this.Db=c;return this};d.A=function(){return"Rule"};d.y=function(){return 3};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.rm){var b=this.Gb,c=a.Gb;(null===b?null===c:b.n(c))?(b=this.ub,c=a.ub,b=null===b?null===c:b.n(c)):b=!1;return b?this.Db===a.Db:!1}return!1};d.z=function(a){switch(a){case 0:return this.Gb;case 1:return this.ub;case 2:return this.Db;default:throw(new Y).i(""+a);}};
function nc(a){return OA(a,t(new u,function(){return function(a){nr||(nr=(new mr).b());a="Unspecified path for page "+a+".";throw np(Xh(),(new lA).i(a));}}(a)),Fb(function(){return function(a,c){nr||(nr=(new mr).b());a="Unspecified action for page "+c+" at "+a+".";throw np(Xh(),(new lA).i(a));}}(a)))}d.m=function(){return al(P(),this)};function OA(a,b,c){var e=a.Gb;b=xh(zh(),a.ub,b);uh||(uh=(new sh).b());return(new PA).Eh(e,b,th(a.Db,c))}
function oc(a,b){var c=wh(zh(),a.Gb,b.Gb),e=zh();return(new Tg).Eh(c,wh(e,a.ub,b.ub),Fb(function(a,b){return function(c,e){return fg(a.ub.g(e).l()?b.Db:a.Db,c,e)}}(a,b)))}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({rm:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Rule",{rm:1,c:1,C:1,q:1,k:1,h:1});function PA(){this.Db=this.ub=this.Gb=null}PA.prototype=new n;PA.prototype.constructor=PA;d=PA.prototype;d.Eh=function(a,b,c){this.Gb=a;this.ub=b;this.Db=c;return this};
d.A=function(){return"Rules"};d.y=function(){return 3};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.sm){var b=this.Gb,c=a.Gb;(null===b?null===c:b.n(c))?(b=this.ub,c=a.ub,b=null===b?null===c:b.n(c)):b=!1;return b?this.Db===a.Db:!1}return!1};d.z=function(a){switch(a){case 0:return this.Gb;case 1:return this.ub;case 2:return this.Db;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};
function vc(a,b){var c;Fx||(Fx=(new Ex).b());c=Fx;b=xh(zh(),a.Gb,b);var e=new NA,f=a.ub;a=a.Db;var h=Hx(c),m=Gx(c);c=c.Co;e.Gb=b;e.ub=f;e.Db=a;e.ge=h;e.Og=m;e.od=c;return e}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({sm:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Rules",{sm:1,c:1,C:1,q:1,k:1,h:1});function QA(){}QA.prototype=new Zz;QA.prototype.constructor=QA;function RA(){}RA.prototype=QA.prototype;QA.prototype.Qu=function(){return this};
function SA(){this.s=this.Ef=null}SA.prototype=new n;SA.prototype.constructor=SA;d=SA.prototype;d.A=function(){return"AV"};d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.xm){var b=this.Ef,c=a.Ef;return(null===b?null===c:b.n(c))?this.s===a.s:!1}return!1};d.z=function(a){switch(a){case 0:return this.Ef;case 1:return this.s;default:throw(new Y).i(""+a);}};function Pj(a,b){var c=new SA;c.Ef=a;c.s=b;return c}d.m=function(){return al(P(),this)};
d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({xm:0},!1,"scalacss.internal.AV",{xm:1,c:1,C:1,q:1,k:1,h:1});function fA(){this.Xd=null}fA.prototype=new jA;fA.prototype.constructor=fA;fA.prototype.b=function(){iA.prototype.b.call(this);return this};fA.prototype.dn=function(){return t(new u,function(){return function(a){return rk(lk(),"height",a)}}(this))};fA.prototype.$classData=l({nt:0},!1,"scalacss.internal.Attrs$height$",{nt:1,wu:1,Qi:1,we:1,c:1,Wm:1});
var eA=void 0;function cA(){this.Xd=null}cA.prototype=new jA;cA.prototype.constructor=cA;cA.prototype.b=function(){iA.prototype.b.call(this);return this};cA.prototype.dn=function(){return t(new u,function(){return function(a){return rk(lk(),"width",a)}}(this))};cA.prototype.$classData=l({qt:0},!1,"scalacss.internal.Attrs$width$",{qt:1,wu:1,Qi:1,we:1,c:1,Wm:1});var bA=void 0;function qy(){this.fi=this.oc=null}qy.prototype=new n;qy.prototype.constructor=qy;d=qy.prototype;d.A=function(){return"Agent"};
d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.zm&&this.oc===a.oc){var b=this.fi;a=a.fi;return null===b?null===a:TA(b,a)}return!1};d.z=function(a){switch(a){case 0:return this.oc;case 1:return this.fi;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};function py(a,b,c){a.oc=b;a.fi=c;return a}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({zm:0},!1,"scalacss.internal.CanIUse$Agent",{zm:1,c:1,C:1,q:1,k:1,h:1});function Io(){this.s=null}Io.prototype=new n;Io.prototype.constructor=Io;d=Io.prototype;d.A=function(){return"ClassName"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Am?this.s===a.s:!1};d.z=function(a){switch(a){case 0:return this.s;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.i=function(a){this.s=a;return this};
d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Am:0},!1,"scalacss.internal.ClassName",{Am:1,c:1,C:1,q:1,k:1,h:1});function jp(){this.s=null}jp.prototype=new n;jp.prototype.constructor=jp;d=jp.prototype;d.A=function(){return"ClassNameHint"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Bm?this.s===a.s:!1};d.z=function(a){switch(a){case 0:return this.s;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};
d.i=function(a){this.s=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Bm:0},!1,"scalacss.internal.ClassNameHint",{Bm:1,c:1,C:1,q:1,k:1,h:1});function uy(){this.ki=null}uy.prototype=new n;uy.prototype.constructor=uy;d=uy.prototype;d.A=function(){return"Compose"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Cm?this.ki===a.ki:!1};
d.z=function(a){switch(a){case 0:return this.ki;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};
function UA(a,b,c,e,f){c=(new Sf).o(c.sb);Uw(new Tw,c.x,t(new u,function(){return function(a){return null!==a}}(a))).O(t(new u,function(a,b,c){return function(a){if(null!==a){var e=a.N;a=a.P;var f=b.sb.yb(e);if(!f.l()){var f=Oj(f.R()),h=fz(),h=Yw(h),f=Wk(f,h),h=Oj(a);Uk();a=(new ql).b();for(h=Nj(h);h.Cb;){var C=h.t();!0!==f.ca(C)&&rl(a,C)}f=sl(a);Tj(f)?c.x=c.x.ne(e):(a=f.D(),f=Wl(f),c.x=c.x.qb(e,Xk(new Yk,a,f)))}}else throw(new x).o(a);}}(a,e,c)));return ns(c.x,e,Fb(function(a,b,c){return function(e,
f){f=(new H).d(e,f);e=f.N;var z=f.P;if(null!==z){f=z.N;var z=z.P,E=Wj(e,t(new u,function(a,b){return function(a){return fg(Ek().pn,b,a).kn()}}(a,f)));if(B()===E)return Rj(e,f,z);if(Qf(E)){E=E.F;f=VA(a,c,ev(a.ki,E,ck(gk(),f,z)),b);z=WA(E.sb);E=Oj(f.qd);z=Lb(z,E);e=Wj(e,t(new u,function(a,b){return function(a){return!b.ca(a)}}(a,z)));if(B()===e)return f;if(Qf(e))return ns(f.sb,e.F,Fb(function(){return function(a,b){var c=b.N,e=Sj(a,c),e=Tj(e)?Uj(a.qd,c):a.qd;a=a.sb.qb(c,b.P);return Kj(new Jj,e,a)}}(a)));
throw(new x).o(e);}throw(new x).o(E);}throw(new x).o(f);}}(a,f,b)))}function VA(a,b,c,e){c.P.O(t(new u,function(a,b,c){return function(a){b.x=Kg(b.x,Uo(new Vo,c,a))}}(a,e,b)));return c.N}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
function Tz(a,b,c){var e=Vl(b.Ve,c.Ve),e=(new Sf).o(e);a=ns(c.Hd,b.Hd,Fb(function(a,b){return function(c,e){e=(new H).d(c,e);c=e.N;var f=e.P;if(null!==f){e=f.N;var f=f.P,z=c.yb(e);z.l()||(z=z.R(),f=UA(a,e,f,z,b));return c.qb(e,f)}throw(new x).o(e);}}(a,e)));var f=Vl(b.Vd,c.Vd);b=Vl(b.id,c.id);return Ko(new Lo,a,f,B(),b,e.x)}d.$classData=l({Cm:0},!1,"scalacss.internal.Compose",{Cm:1,c:1,C:1,q:1,k:1,h:1});function XA(){this.s=this.Va=null}XA.prototype=new n;XA.prototype.constructor=XA;
function qk(a,b){var c=new XA;c.Va=a;c.s=b;return c}d=XA.prototype;d.A=function(){return"CssKV"};d.y=function(){return 2};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Im?this.Va===a.Va&&this.s===a.s:!1};d.z=function(a){switch(a){case 0:return this.Va;case 1:return this.s;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({Im:0},!1,"scalacss.internal.CssKV",{Im:1,c:1,C:1,q:1,k:1,h:1});function op(){this.dh=null}op.prototype=new Ou;op.prototype.constructor=op;function YA(){}YA.prototype=op.prototype;op.prototype.b=function(){Zm||(Zm=(new Ym).b());Wm||(Wm=(new Vm).b());om||(om=(new nm).b());on||(on=(new nn).b());Ky||(Ky=(new Jy).b());this.dh=Ky;qn||(qn=(new pn).b());return this};
function Oz(a){var b=[],c;gk();c=a.s;c=ck(0,a.Ef,dk(Xj(),c));a=b.length|0;var e=0,f=c;a:for(;;){if(e!==a){c=1+e|0;var h=b[e],e=f,f=h.Ef,m=h.s,q=Sj(e,f),h=Tj(q)?Uj(e.qd,f):e.qd,e=e.sb.qb(f,Tm(Xj(),q,m)),f=Kj(new Jj,h,e),e=c;continue a}b=f;break}a=Wo().Y;return Ly((new ek).d(a,b))}op.prototype.zo=function(a){return(new to).i(a)};function Ey(){this.Mo=this.wj=this.Gj=null}Ey.prototype=new n;Ey.prototype.constructor=Ey;d=Ey.prototype;d.A=function(){return"Env"};d.y=function(){return 2};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Km){var b=this.Gj,c=a.Gj;if(null===b?null===c:b.n(c))return b=this.wj,a=a.wj,null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.Gj;case 1:return this.wj;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Km:0},!1,"scalacss.internal.Env",{Km:1,c:1,C:1,q:1,k:1,h:1});
function Fy(){this.Tf=this.Uj=this.Nj=this.Aj=this.bj=this.aj=this.yj=this.fj=this.gj=this.hj=this.Yi=this.oj=this.gk=null}Fy.prototype=new n;Fy.prototype.constructor=Fy;d=Fy.prototype;d.A=function(){return"Media"};d.y=function(){return 13};
d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Lm?D(G(),this.gk,a.gk)&&D(G(),this.oj,a.oj)&&D(G(),this.Yi,a.Yi)&&D(G(),this.hj,a.hj)&&D(G(),this.gj,a.gj)&&D(G(),this.fj,a.fj)&&D(G(),this.yj,a.yj)&&D(G(),this.aj,a.aj)&&D(G(),this.bj,a.bj)&&D(G(),this.Aj,a.Aj)&&D(G(),this.Nj,a.Nj)&&D(G(),this.Uj,a.Uj)&&D(G(),this.Tf,a.Tf):!1};
d.z=function(a){switch(a){case 0:return this.gk;case 1:return this.oj;case 2:return this.Yi;case 3:return this.hj;case 4:return this.gj;case 5:return this.fj;case 6:return this.yj;case 7:return this.aj;case 8:return this.bj;case 9:return this.Aj;case 10:return this.Nj;case 11:return this.Uj;case 12:return this.Tf;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({Lm:0},!1,"scalacss.internal.Env$Media",{Lm:1,c:1,C:1,q:1,k:1,h:1});function Dy(){this.Bj=this.dk=this.Ij=this.vj=this.ej=this.fk=this.tj=this.aa=null}Dy.prototype=new n;Dy.prototype.constructor=Dy;d=Dy.prototype;d.A=function(){return"Platform"};d.y=function(){return 8};
d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Mm?D(G(),this.aa,a.aa)&&D(G(),this.tj,a.tj)&&D(G(),this.fk,a.fk)&&D(G(),this.ej,a.ej)&&D(G(),this.vj,a.vj)&&D(G(),this.Ij,a.Ij)&&D(G(),this.dk,a.dk)&&D(G(),this.Bj,a.Bj):!1};d.z=function(a){switch(a){case 0:return this.aa;case 1:return this.tj;case 2:return this.fk;case 3:return this.ej;case 4:return this.vj;case 5:return this.Ij;case 6:return this.dk;case 7:return this.Bj;default:throw(new Y).i(""+a);}};
d.m=function(){return al(P(),this)};d.Wf=function(a,b,c,e,f,h,m,q){this.aa=a;this.tj=b;this.fk=c;this.ej=e;this.vj=f;this.Ij=h;this.dk=m;this.Bj=q;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Mm:0},!1,"scalacss.internal.Env$Platform",{Mm:1,c:1,C:1,q:1,k:1,h:1});function Am(){this.ig=this.Sf=this.Rf=this.Qf=this.Bc=this.xb=null}Am.prototype=new n;Am.prototype.constructor=Am;d=Am.prototype;d.A=function(){return"FontFace"};d.y=function(){return 6};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Nm){if(D(G(),this.xb,a.xb))var b=this.Bc,c=a.Bc,b=null===b?null===c:b.n(c);else b=!1;b?(b=this.Qf,c=a.Qf,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Rf,c=a.Rf,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Sf,c=a.Sf,b=null===b?null===c:b.n(c)):b=!1;if(b)return b=this.ig,a=a.ig,null===b?null===a:b.n(a)}return!1};
d.z=function(a){switch(a){case 0:return this.xb;case 1:return this.Bc;case 2:return this.Qf;case 3:return this.Rf;case 4:return this.Sf;case 5:return this.ig;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Nm:0},!1,"scalacss.internal.FontFace",{Nm:1,c:1,C:1,q:1,k:1,h:1});function ZA(){this.zi=this.Md=null}ZA.prototype=new n;ZA.prototype.constructor=ZA;d=ZA.prototype;d.A=function(){return"Length"};
d.y=function(){return 2};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Pm?D(G(),this.Md,a.Md)?this.zi===a.zi:!1:!1};function Nz(){var a=Lz,b=new ZA;b.Md=20;b.zi=a;return b}d.z=function(a){switch(a){case 0:return this.Md;case 1:return this.zi;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.mc=function(){var a=ka(this.Md);return"0"===a?"0":""+a+this.zi.s};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({Pm:0},!1,"scalacss.internal.Length",{Pm:1,c:1,C:1,q:1,k:1,h:1});function dA(){this.Md=null}dA.prototype=new n;dA.prototype.constructor=dA;d=dA.prototype;d.A=function(){return"Percentage"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Rm?D(G(),this.Md,a.Md):!1};d.z=function(a){switch(a){case 0:return this.Md;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.mc=function(){return ka(this.Md)+"%"};
d.o=function(a){this.Md=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Rm:0},!1,"scalacss.internal.Percentage",{Rm:1,c:1,C:1,q:1,k:1,h:1});function mn(){this.th=this.yh=this.Oh=this.Qh=this.Kg=this.ri=this.Th=this.Sh=this.si=this.Lg=this.Rh=this.Ph=null}mn.prototype=new n;mn.prototype.constructor=mn;
function hv(a,b,c){var e=a.Lg;b.l()||e.g(b.R());c=Oj(c);Uw(new Tw,c,t(new u,function(){return function(a){return null!==a}}(a))).O(t(new u,function(a,b){return function(c){if(null!==c){var e=c.N;c=c.P;$A(a,B(),b,e,c)}else throw(new x).o(c);}}(a,b)));a=a.Kg;b.l()||a.g(b.R())}d=mn.prototype;d.A=function(){return"FormatSB"};d.y=function(){return 12};
function gv(a,b){if(bm(b)){var c=b.Ne,e=a.Lg;c.l()||e.g(c.R());$A(a,B(),b.Ne,b.Sg,b.Jf);b=b.Ne;a=a.Kg;b.l()||a.g(b.R())}else if(cm(b))a.Ph.g(b.aa.s),Uw(new Tw,b.Dh,t(new u,function(){return function(a){return null!==a}}(a))).O(t(new u,function(a){return function(b){if(null!==b){var c=b.N,e=b.P;a.Rh.g(c.mc());b=(new Pf).o(c);for(e=Nj(e);e.Cb;){var y=e.t();aB(a,b,y.Ne,y.Jf)}a.Qh.g(c)}else throw(new x).o(b);}}(a))),a.Oh.g(b.aa);else if(dm(b))a.yh.g(b);else throw(new x).o(b);}
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Sm){var b=this.Ph,c=a.Ph;(null===b?null===c:b.n(c))?(b=this.Rh,c=a.Rh,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Lg,c=a.Lg,b=null===b?null===c:b.n(c)):b=!1;b&&this.si===a.si&&this.Sh===a.Sh&&this.Th===a.Th&&this.ri===a.ri?(b=this.Kg,c=a.Kg,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Qh,c=a.Qh,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Oh,c=a.Oh,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.yh,c=a.yh,b=null===b?null===c:b.n(c)):b=
!1;return b?this.th===a.th:!1}return!1};d.z=function(a){switch(a){case 0:return this.Ph;case 1:return this.Rh;case 2:return this.Lg;case 3:return this.si;case 4:return this.Sh;case 5:return this.Th;case 6:return this.ri;case 7:return this.Kg;case 8:return this.Qh;case 9:return this.Oh;case 10:return this.yh;case 11:return this.th;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};
function ln(a,b,c,e,f,h,m,q,y,N,z,E,C){a.Ph=b;a.Rh=c;a.Lg=e;a.si=f;a.Sh=h;a.Th=m;a.ri=q;a.Kg=y;a.Qh=N;a.Oh=z;a.yh=E;a.th=C;return a}function $A(a,b,c,e,f){fg(a.si,c,e);aB(a,b,c,f);fg(a.ri,c,e)}d.r=function(){return um(U(),this)};function aB(a,b,c,e){a.Sh.Vi(b,c,e.Qc);for(e=Nj(e.Wa);e.Cb;){var f=e.t();a.Th.Vi(b,c,f)}}d.B=function(){return Z(this)};d.$classData=l({Sm:0},!1,"scalacss.internal.StringRenderer$FormatSB",{Sm:1,c:1,C:1,q:1,k:1,h:1});function Yo(){this.Xn=this.wi=this.fh=this.Hf=null}
Yo.prototype=new n;Yo.prototype.constructor=Yo;d=Yo.prototype;d.A=function(){return"StyleA"};d.y=function(){return 3};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Tm){var b=this.Hf,c=a.Hf;(null===b?null===c:b.n(c))?(b=this.fh,c=a.fh,b=null===b?null===c:Pm(b,c)):b=!1;if(b)return b=this.wi,a=a.wi,null===b?null===a:b.n(a)}return!1};
function Xo(a,b,c,e){a.Hf=b;a.fh=c;a.wi=e;b=bB(a);b=(new Vh).lb(b,t(new u,function(){return function(a){return a.s}}(a)));a.Xn=Qj(b,""," ","");return a}d.z=function(a){switch(a){case 0:return this.Hf;case 1:return this.fh;case 2:return this.wi;default:throw(new Y).i(""+a);}};function bB(a){Ac();var b=(new cB).o(a.Hf);return Nw(b,v(new w,function(a){return function(){return a.fh}}(a)))}d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({Tm:0},!1,"scalacss.internal.StyleA",{Tm:1,c:1,C:1,q:1,k:1,h:1});function dB(){}dB.prototype=new tx;dB.prototype.constructor=dB;dB.prototype.b=function(){return this};dB.prototype.m=function(){return"StyleA"};dB.prototype.Vi=function(a,b,c){return Xo(new Yo,a,b,c)};dB.prototype.$classData=l({lu:0},!1,"scalacss.internal.StyleA$",{lu:1,fz:1,c:1,Xp:1,k:1,h:1});var eB=void 0;function to(){this.s=null}to.prototype=new n;to.prototype.constructor=to;d=to.prototype;d.A=function(){return"ValueT"};
d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Vm?this.s===a.s:!1};d.z=function(a){switch(a){case 0:return this.s;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.i=function(a){this.s=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Vm:0},!1,"scalacss.internal.ValueT",{Vm:1,c:1,C:1,q:1,k:1,h:1});function fB(){}fB.prototype=new lv;fB.prototype.constructor=fB;function gB(){}
gB.prototype=fB.prototype;fB.prototype.zo=function(a){return Pj(this.Yd(),a)};function Vo(){this.ai=this.oh=null}Vo.prototype=new n;Vo.prototype.constructor=Vo;d=Vo.prototype;d.A=function(){return"Warning"};d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Xm){var b=this.oh,c=a.oh;return(null===b?null===c:b.n(c))?this.ai===a.ai:!1}return!1};d.z=function(a){switch(a){case 0:return this.oh;case 1:return this.ai;default:throw(new Y).i(""+a);}};
function Uo(a,b,c){a.oh=b;a.ai=c;return a}d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Xm:0},!1,"scalacss.internal.package$Warning",{Xm:1,c:1,C:1,q:1,k:1,h:1});function hB(){this.Ok=this.Pk=null}hB.prototype=new n;hB.prototype.constructor=hB;hB.prototype.b=function(){iB=this;Zm||(Zm=(new Ym).b());Wm||(Wm=(new Vm).b());om||(om=(new nm).b());eB||(eB=(new dB).b());ty();Gy();this.Pk=(new $o).Ih("%s-%04d",1);this.Ok=bp();return this};
hB.prototype.$classData=l({Lu:0},!1,"scalacss.package$DevDefaults$",{Lu:1,c:1,Yz:1,Us:1,Xz:1,DA:1});var iB=void 0;function Hz(){iB||(iB=(new hB).b());return iB}l({Xu:0},!1,"java.lang.ArithmeticException",{Xu:1,ib:1,mb:1,Ua:1,c:1,h:1});function Ob(){this.ma=null}Ob.prototype=new mA;Ob.prototype.constructor=Ob;function jB(){}jB.prototype=Ob.prototype;Ob.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};Ob.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};
Ob.prototype.$classData=l({Rc:0},!1,"java.lang.IllegalArgumentException",{Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function Aw(){this.ma=null}Aw.prototype=new mA;Aw.prototype.constructor=Aw;function kB(){}kB.prototype=Aw.prototype;Aw.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};Aw.prototype.$classData=l({qo:0},!1,"java.lang.IllegalStateException",{qo:1,ib:1,mb:1,Ua:1,c:1,h:1});function Y(){this.ma=null}Y.prototype=new mA;Y.prototype.constructor=Y;
Y.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};Y.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};Y.prototype.$classData=l({fv:0},!1,"java.lang.IndexOutOfBoundsException",{fv:1,ib:1,mb:1,Ua:1,c:1,h:1});function lB(){}lB.prototype=new Zz;lB.prototype.constructor=lB;lB.prototype.b=function(){return this};lB.prototype.$classData=l({jv:0},!1,"java.lang.JSConsoleBasedPrintStream$DummyOutputStream",{jv:1,Ps:1,c:1,xk:1,oo:1,wm:1});
function oA(){this.ma=null}oA.prototype=new mA;oA.prototype.constructor=oA;oA.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};oA.prototype.$classData=l({mv:0},!1,"java.lang.NegativeArraySizeException",{mv:1,ib:1,mb:1,Ua:1,c:1,h:1});function Ho(){this.ma=null}Ho.prototype=new mA;Ho.prototype.constructor=Ho;Ho.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};
Ho.prototype.$classData=l({nv:0},!1,"java.lang.NullPointerException",{nv:1,ib:1,mb:1,Ua:1,c:1,h:1});function Uz(){this.ma=null}Uz.prototype=new mA;Uz.prototype.constructor=Uz;Uz.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};Uz.prototype.$classData=l({tv:0},!1,"java.lang.UnsupportedOperationException",{tv:1,ib:1,mb:1,Ua:1,c:1,h:1});function yw(){this.ma=null}yw.prototype=new mA;yw.prototype.constructor=yw;yw.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};
yw.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};yw.prototype.$classData=l({Iv:0},!1,"java.util.NoSuchElementException",{Iv:1,ib:1,mb:1,Ua:1,c:1,h:1});function x(){this.di=this.Do=this.ma=null;this.Gk=!1}x.prototype=new mA;x.prototype.constructor=x;
x.prototype.Pc=function(){if(!this.Gk&&!this.Gk){var a;if(null===this.di)a="null";else try{a=ka(this.di)+" ("+("of class "+Jb(la(this.di)))+")"}catch(b){if(null!==Wh(Xh(),b))a="an instance of class "+Jb(la(this.di));else throw b;}this.Do=a;this.Gk=!0}return this.Do};x.prototype.o=function(a){this.di=a;ww.prototype.Sa.call(this,null,0,0,!0);return this};x.prototype.$classData=l({Rv:0},!1,"scala.MatchError",{Rv:1,ib:1,mb:1,Ua:1,c:1,h:1});function mB(){}mB.prototype=new n;mB.prototype.constructor=mB;
function nB(){}nB.prototype=mB.prototype;function Rp(){}Rp.prototype=new n;Rp.prototype.constructor=Rp;d=Rp.prototype;d.b=function(){return this};d.g=function(a){this.Dk(a)};d.m=function(){return"\x3cfunction1\x3e"};d.Gc=function(){return!1};d.Ze=function(a,b){return Np(this,a,b)};d.Dk=function(a){throw(new x).o(a);};d.Gd=function(){return this};d.$classData=l({Wv:0},!1,"scala.PartialFunction$$anon$1",{Wv:1,c:1,Ta:1,T:1,k:1,h:1});function oB(){this.hl=this.eg=null}oB.prototype=new n;
oB.prototype.constructor=oB;function Ez(a,b){var c=new oB;c.eg=a;c.hl=b;return c}d=oB.prototype;d.g=function(a){return this.hl.g(this.eg.g(a))};d.m=function(){return"\x3cfunction1\x3e"};d.Gc=function(a){return this.eg.Gc(a)};d.Ze=function(a,b){var c=this.eg.Ze(a,Sp().Pg);return Sp().Pg===c?b.g(a):this.hl.g(c)};d.Gd=function(a){return Ez(this,a)};d.$classData=l({Yv:0},!1,"scala.PartialFunction$AndThen",{Yv:1,c:1,Ta:1,T:1,k:1,h:1});function ch(){this.Io=null}ch.prototype=new px;
ch.prototype.constructor=ch;ch.prototype.g=function(a){return this.Ck(a)};function bh(a,b){a.Io=b;return a}ch.prototype.Ck=function(a){a=this.Io.Ze(a,Sp().Pg);return Sp().Pg===a?B():(new Pf).o(a)};ch.prototype.$classData=l({Zv:0},!1,"scala.PartialFunction$Lifted",{Zv:1,Hp:1,c:1,T:1,k:1,h:1});function iz(){}iz.prototype=new CA;iz.prototype.constructor=iz;iz.prototype.b=function(){return this};iz.prototype.g=function(a){return a};
iz.prototype.$classData=l({aw:0},!1,"scala.Predef$$anon$1",{aw:1,$A:1,c:1,T:1,k:1,h:1});function jz(){}jz.prototype=new AA;jz.prototype.constructor=jz;jz.prototype.b=function(){return this};jz.prototype.g=function(a){return a};jz.prototype.$classData=l({bw:0},!1,"scala.Predef$$anon$2",{bw:1,ZA:1,c:1,T:1,k:1,h:1});function pB(){this.Uc=null}pB.prototype=new n;pB.prototype.constructor=pB;d=pB.prototype;d.A=function(){return"StringContext"};d.y=function(){return 1};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Zo){var b=this.Uc;a=a.Uc;return null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.Uc;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};function qB(a,b){if(a.Uc.W()!==(1+b.W()|0))throw(new Ob).i("wrong number of arguments ("+b.W()+") for interpolated string with "+a.Uc.W()+" parts");}
function Cl(a,b){var c=function(){return function(a){mz||(mz=(new lz).b());a:{var b=a.length|0,c;Ca();var e=it();c=a.indexOf(e)|0;switch(c){case -1:break a;default:e=(new nA).b();b:{var f=c;c=0;for(;;)if(0<=f){if(f>c){var z=e;c=Ha(null===a?"null":a,c,f);z.va=""+z.va+c}c=1+f|0;if(c>=b)throw(new rB).Ih(a,f);z=65535&(a.charCodeAt(c)|0);switch(z){case 98:f=8;break;case 116:f=9;break;case 110:f=10;break;case 102:f=12;break;case 114:f=13;break;case 34:f=34;break;case 39:f=39;break;case 92:f=92;break;default:if(48<=
z&&55>=z)f=65535&(a.charCodeAt(c)|0),z=-48+f|0,c=1+c|0,c<b&&48<=(65535&(a.charCodeAt(c)|0))&&55>=(65535&(a.charCodeAt(c)|0))&&(z=-48+((z<<3)+(65535&(a.charCodeAt(c)|0))|0)|0,c=1+c|0,c<b&&51>=f&&48<=(65535&(a.charCodeAt(c)|0))&&55>=(65535&(a.charCodeAt(c)|0))&&(z=-48+((z<<3)+(65535&(a.charCodeAt(c)|0))|0)|0,c=1+c|0)),c=-1+c|0,f=65535&z;else throw(new rB).Ih(a,f);}c=1+c|0;kn(e,f);f=c;Ca();var z=a,E=it(),z=z.indexOf(E,c)|0;c=f;f=z}else{c<b&&(f=e,a=Ha(null===a?"null":a,c,b),f.va=""+f.va+a);a=e.va;break b}}}}return a}}(a);
qB(a,b);a=a.Uc.E();b=b.E();for(var e=a.t(),e=(new nA).i(c(e));b.H();){var f=b.t();e.va=""+e.va+f;f=a.t();f=c(f);e.va=""+e.va+f}return e.va}function Dl(a){var b=new pB;b.Uc=a;return b}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Zo:0},!1,"scala.StringContext",{Zo:1,c:1,C:1,q:1,k:1,h:1});function sB(){}sB.prototype=new n;sB.prototype.constructor=sB;function tB(){}tB.prototype=sB.prototype;function qr(){this.ma=null}qr.prototype=new xw;qr.prototype.constructor=qr;
qr.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};qr.prototype.jj=function(){oz||(oz=(new nz).b());return oz.Zl?ww.prototype.jj.call(this):this};qr.prototype.$classData=l({Qw:0},!1,"scala.util.control.BreakControl",{Qw:1,Ua:1,c:1,h:1,hB:1,iB:1});
function TA(a,b){if(b&&b.$classData&&b.$classData.p.Jc){var c;if(!(c=a===b)&&(c=a.L()===b.L()))try{var e=a.E();for(a=!0;a&&e.H();){var f=e.t();if(null===f)throw(new x).o(f);var h=f.P,m=b.yb(f.N);b:{if(Qf(m)){var q=m.F;if(D(G(),h,q)){a=!0;break b}}a=!1}}c=a}catch(y){if(y&&y.$classData&&y.$classData.p.cv)c=!1;else throw y;}b=c}else b=!1;return b}function uB(a,b,c){return a.ce(t(new u,function(a,b){return function(a){return D(G(),b,a)}}(a,b)),c)}
function Pm(a,b){return b&&b.$classData&&b.$classData.p.ud?a.Nd(b):!1}function vB(a,b){return 0<=b&&b<a.W()}function wB(){this.Ka=null}wB.prototype=new $w;wB.prototype.constructor=wB;wB.prototype.b=function(){Zw.prototype.b.call(this);return this};wB.prototype.da=function(){xB();return(new ig).b()};wB.prototype.$classData=l({ax:0},!1,"scala.collection.Iterable$",{ax:1,zc:1,bc:1,c:1,Ac:1,cc:1});var yB=void 0;function $p(){yB||(yB=(new wB).b());return yB}function Vh(){this.On=this.Oa=null}
Vh.prototype=new EA;Vh.prototype.constructor=Vh;Vh.prototype.t=function(){return this.On.g(this.Oa.t())};Vh.prototype.lb=function(a,b){if(null===a)throw np(Xh(),null);this.Oa=a;this.On=b;return this};Vh.prototype.H=function(){return this.Oa.H()};Vh.prototype.$classData=l({cx:0},!1,"scala.collection.Iterator$$anon$10",{cx:1,ac:1,c:1,Hb:1,K:1,J:1});function Mj(){this.xh=this.Oa=this.dj=null}Mj.prototype=new EA;Mj.prototype.constructor=Mj;Mj.prototype.t=function(){return(this.H()?this.dj:bq().Y).t()};
Mj.prototype.lb=function(a,b){if(null===a)throw np(Xh(),null);this.Oa=a;this.xh=b;this.dj=bq().Y;return this};Mj.prototype.H=function(){for(;!this.dj.H();){if(!this.Oa.H())return!1;this.dj=this.xh.g(this.Oa.t()).Ue()}return!0};Mj.prototype.$classData=l({dx:0},!1,"scala.collection.Iterator$$anon$11",{dx:1,ac:1,c:1,Hb:1,K:1,J:1});function xi(){this.Zk=null;this.nj=!1;this.Fo=this.Oa=null}xi.prototype=new EA;xi.prototype.constructor=xi;xi.prototype.t=function(){return this.H()?(this.nj=!1,this.Zk):bq().Y.t()};
xi.prototype.lb=function(a,b){if(null===a)throw np(Xh(),null);this.Oa=a;this.Fo=b;this.nj=!1;return this};xi.prototype.H=function(){if(!this.nj){do{if(!this.Oa.H())return!1;this.Zk=this.Oa.t()}while(!this.Fo.g(this.Zk));this.nj=!0}return!0};xi.prototype.$classData=l({ex:0},!1,"scala.collection.Iterator$$anon$12",{ex:1,ac:1,c:1,Hb:1,K:1,J:1});function ks(){}ks.prototype=new EA;ks.prototype.constructor=ks;ks.prototype.b=function(){return this};
ks.prototype.t=function(){throw(new yw).i("next on empty iterator");};ks.prototype.H=function(){return!1};ks.prototype.$classData=l({fx:0},!1,"scala.collection.Iterator$$anon$2",{fx:1,ac:1,c:1,Hb:1,K:1,J:1});function cB(){this.lj=!1;this.En=null}cB.prototype=new EA;cB.prototype.constructor=cB;cB.prototype.t=function(){return this.lj?(this.lj=!1,this.En):bq().Y.t()};cB.prototype.o=function(a){this.En=a;this.lj=!0;return this};cB.prototype.H=function(){return this.lj};
cB.prototype.$classData=l({gx:0},!1,"scala.collection.Iterator$$anon$3",{gx:1,ac:1,c:1,Hb:1,K:1,J:1});function zB(){this.Ed=null}zB.prototype=new EA;zB.prototype.constructor=zB;function AB(a){var b=new zB;b.Ed=a;return b}zB.prototype.t=function(){if(this.H()){var a=this.Ed.D();this.Ed=this.Ed.u();return a}return bq().Y.t()};zB.prototype.H=function(){return!this.Ed.l()};zB.prototype.$classData=l({ix:0},!1,"scala.collection.LinearSeqLike$$anon$1",{ix:1,ac:1,c:1,Hb:1,K:1,J:1});
function BB(){this.wc=null}BB.prototype=new EA;BB.prototype.constructor=BB;BB.prototype.t=function(){return this.wc.t().N};BB.prototype.H=function(){return this.wc.H()};BB.prototype.Yf=function(a){this.wc=a.E();return this};BB.prototype.$classData=l({jx:0},!1,"scala.collection.MapLike$$anon$1",{jx:1,ac:1,c:1,Hb:1,K:1,J:1});function CB(){this.wc=null}CB.prototype=new EA;CB.prototype.constructor=CB;CB.prototype.t=function(){return this.wc.t().P};CB.prototype.H=function(){return this.wc.H()};
CB.prototype.Yf=function(a){this.wc=a.E();return this};CB.prototype.$classData=l({kx:0},!1,"scala.collection.MapLike$$anon$2",{kx:1,ac:1,c:1,Hb:1,K:1,J:1});function Zp(){this.Ka=null}Zp.prototype=new $w;Zp.prototype.constructor=Zp;Zp.prototype.b=function(){Zw.prototype.b.call(this);Yp=this;(new pr).b();return this};Zp.prototype.da=function(){DB||(DB=(new EB).b());return(new ig).b()};Zp.prototype.$classData=l({nx:0},!1,"scala.collection.Traversable$",{nx:1,zc:1,bc:1,c:1,Ac:1,cc:1});var Yp=void 0;
function FB(){}FB.prototype=new GA;FB.prototype.constructor=FB;function GB(){}GB.prototype=FB.prototype;FB.prototype.vh=function(){return this.yg()};FB.prototype.da=function(){return R(new S,this.yg())};function HB(){this.Ka=null}HB.prototype=new $w;HB.prototype.constructor=HB;HB.prototype.b=function(){Zw.prototype.b.call(this);return this};HB.prototype.da=function(){return(new ig).b()};HB.prototype.$classData=l({Kx:0},!1,"scala.collection.immutable.Iterable$",{Kx:1,zc:1,bc:1,c:1,Ac:1,cc:1});
var IB=void 0;function xB(){IB||(IB=(new HB).b());return IB}function JB(){this.Ed=null}JB.prototype=new EA;JB.prototype.constructor=JB;JB.prototype.t=function(){if(!this.H())return bq().Y.t();var a=Bs(this.Ed),b=a.D();this.Ed=As(new zs,this,v(new w,function(a,b){return function(){return b.u()}}(this,a)));return b};function KB(a){var b=new JB;b.Ed=As(new zs,b,v(new w,function(a,b){return function(){return b}}(b,a)));return b}JB.prototype.H=function(){return!Bs(this.Ed).l()};
JB.prototype.kc=function(){var a=Bs(this.Ed);this.Ed=As(new zs,this,v(new w,function(){return function(){iq();return Rw()}}(this)));return a};JB.prototype.$classData=l({py:0},!1,"scala.collection.immutable.StreamIterator",{py:1,ac:1,c:1,Hb:1,K:1,J:1});function EB(){this.Ka=null}EB.prototype=new $w;EB.prototype.constructor=EB;EB.prototype.b=function(){Zw.prototype.b.call(this);return this};EB.prototype.da=function(){return(new ig).b()};
EB.prototype.$classData=l({sy:0},!1,"scala.collection.immutable.Traversable$",{sy:1,zc:1,bc:1,c:1,Ac:1,cc:1});var DB=void 0;function LB(){this.Gn=null;this.dd=0;this.Qg=this.Dl=this.Tj=null;this.xf=0;this.fg=null}LB.prototype=new EA;LB.prototype.constructor=LB;function MB(){}MB.prototype=LB.prototype;
LB.prototype.t=function(){if(null!==this.fg){var a=this.fg.t();this.fg.H()||(this.fg=null);return a}a:{var a=this.Qg,b=this.xf;for(;;){b===(-1+a.e.length|0)?(this.dd=-1+this.dd|0,0<=this.dd?(this.Qg=this.Tj.e[this.dd],this.xf=this.Dl.e[this.dd],this.Tj.e[this.dd]=null):(this.Qg=null,this.xf=0)):this.xf=1+this.xf|0;if((a=a.e[b])&&a.$classData&&a.$classData.p.lp||a&&a.$classData&&a.$classData.p.np){a=this.Vn(a);break a}if(NB(a)||OB(a))0<=this.dd&&(this.Tj.e[this.dd]=this.Qg,this.Dl.e[this.dd]=this.xf),
this.dd=1+this.dd|0,this.Qg=PB(a),this.xf=0,a=PB(a),b=0;else{this.fg=a.E();a=this.t();break a}}}return a};LB.prototype.H=function(){return null!==this.fg||0<=this.dd};function PB(a){if(NB(a))return a.ab;if(!OB(a))throw(new x).o(a);return a.$a}LB.prototype.ao=function(a){this.Gn=a;this.dd=0;this.Tj=k(r(r(QB)),[6]);this.Dl=k(r(Za),[6]);this.Qg=this.Gn;this.xf=0;this.fg=null;return this};function RB(){this.Eg=0;this.Oa=null}RB.prototype=new EA;RB.prototype.constructor=RB;
RB.prototype.t=function(){return 0<this.Eg?(this.Eg=-1+this.Eg|0,this.Oa.Xa(this.Eg)):bq().Y.t()};RB.prototype.H=function(){return 0<this.Eg};function SB(a){var b=new RB;if(null===a)throw np(Xh(),null);b.Oa=a;b.Eg=a.W();return b}RB.prototype.$classData=l({vy:0},!1,"scala.collection.immutable.Vector$$anon$1",{vy:1,ac:1,c:1,Hb:1,K:1,J:1});function Is(){this.wh=this.Td=null}Is.prototype=new n;Is.prototype.constructor=Is;function Hs(a,b,c){a.wh=c;a.Td=b;return a}d=Is.prototype;
d.n=function(a){return null!==a&&(a===this||a===this.Td||za(a,this.Td))};d.rc=function(a){this.Td.rb(a);return this};d.m=function(){return""+this.Td};d.La=function(){return this.wh.g(this.Td.La())};d.fd=function(a,b){this.Td.fd(a,b)};d.rb=function(a){this.Td.rb(a);return this};d.r=function(){return this.Td.r()};d.qc=function(a){this.Td.qc(a)};d.fb=function(a){this.Td.fb(a);return this};d.$classData=l({Dy:0},!1,"scala.collection.mutable.Builder$$anon$1",{Dy:1,c:1,Sd:1,Qd:1,Pd:1,cB:1});
function TB(){this.wc=null}TB.prototype=new EA;TB.prototype.constructor=TB;TB.prototype.t=function(){return this.wc.t().Va};TB.prototype.dl=function(a){this.wc=UB(a);return this};TB.prototype.H=function(){return this.wc.H()};TB.prototype.$classData=l({Hy:0},!1,"scala.collection.mutable.HashMap$$anon$3",{Hy:1,ac:1,c:1,Hb:1,K:1,J:1});function VB(){this.wc=null}VB.prototype=new EA;VB.prototype.constructor=VB;VB.prototype.t=function(){return this.wc.t().s};VB.prototype.dl=function(a){this.wc=UB(a);return this};
VB.prototype.H=function(){return this.wc.H()};VB.prototype.$classData=l({Iy:0},!1,"scala.collection.mutable.HashMap$$anon$4",{Iy:1,ac:1,c:1,Hb:1,K:1,J:1});function WB(){this.fl=null;this.Fg=0;this.Of=null}WB.prototype=new EA;WB.prototype.constructor=WB;WB.prototype.t=function(){var a=this.Of;for(this.Of=this.Of.xc;null===this.Of&&0<this.Fg;)this.Fg=-1+this.Fg|0,this.Of=this.fl.e[this.Fg];return a};function UB(a){var b=new WB;b.fl=a.Jb;b.Fg=Os(a);b.Of=b.fl.e[b.Fg];return b}
WB.prototype.H=function(){return null!==this.Of};WB.prototype.$classData=l({Ky:0},!1,"scala.collection.mutable.HashTable$$anon$1",{Ky:1,ac:1,c:1,Hb:1,K:1,J:1});function XB(){this.Ka=null}XB.prototype=new $w;XB.prototype.constructor=XB;XB.prototype.b=function(){Zw.prototype.b.call(this);return this};XB.prototype.da=function(){return(new YB).b()};XB.prototype.$classData=l({My:0},!1,"scala.collection.mutable.Iterable$",{My:1,zc:1,bc:1,c:1,Ac:1,cc:1});var ZB=void 0;function $B(){this.Uc=null}
$B.prototype=new n;$B.prototype.constructor=$B;function aC(){}d=aC.prototype=$B.prototype;d.b=function(){this.Uc=(new ig).b();return this};d.rc=function(a){return bC(this,a)};function bC(a,b){var c=a.Uc;hg();b=(new A).f([b]);var e=hg().Ka;jg(c,Wk(b,e));return a}d.fd=function(a,b){lx(this,a,b)};d.rb=function(a){return bC(this,a)};d.qc=function(){};d.fb=function(a){jg(this.Uc,a);return this};function cC(){this.qh=null}cC.prototype=new EA;cC.prototype.constructor=cC;
cC.prototype.t=function(){if(this.H()){var a=this.qh.D();this.qh=this.qh.u();return a}throw(new yw).i("next on empty Iterator");};cC.prototype.H=function(){return this.qh!==xb()};cC.prototype.$classData=l({Oy:0},!1,"scala.collection.mutable.ListBuffer$$anon$1",{Oy:1,ac:1,c:1,Hb:1,K:1,J:1});function kl(){this.w=this.Y=null}kl.prototype=new n;kl.prototype.constructor=kl;function ml(a,b){a.w=a.w.Wd(b);return a}d=kl.prototype;d.rc=function(a){return ml(this,a)};d.La=function(){return this.w};
d.fd=function(a,b){lx(this,a,b)};function jl(a,b){a.Y=b;a.w=b;return a}d.rb=function(a){return ml(this,a)};d.qc=function(){};d.fb=function(a){return Tl(this,a)};d.$classData=l({Py:0},!1,"scala.collection.mutable.MapBuilder",{Py:1,c:1,qi:1,Sd:1,Qd:1,Pd:1});function S(){this.w=this.Y=null}S.prototype=new n;S.prototype.constructor=S;d=S.prototype;d.rc=function(a){return T(this,a)};d.La=function(){return this.w};d.fd=function(a,b){lx(this,a,b)};function T(a,b){a.w=a.w.hd(b);return a}
function R(a,b){a.Y=b;a.w=b;return a}d.rb=function(a){return T(this,a)};d.qc=function(){};d.fb=function(a){return Tl(this,a)};d.$classData=l({Qy:0},!1,"scala.collection.mutable.SetBuilder",{Qy:1,c:1,qi:1,Sd:1,Qd:1,Pd:1});function dC(){this.jn=this.lh=0;this.Wp=null}dC.prototype=new EA;dC.prototype.constructor=dC;dC.prototype.t=function(){var a=this.Wp.z(this.lh);this.lh=1+this.lh|0;return a};function Z(a){var b=new dC;b.Wp=a;b.lh=0;b.jn=a.y();return b}dC.prototype.H=function(){return this.lh<this.jn};
dC.prototype.$classData=l({oz:0},!1,"scala.runtime.ScalaRunTime$$anon$1",{oz:1,ac:1,c:1,Hb:1,K:1,J:1});function eC(){}eC.prototype=new n;eC.prototype.constructor=eC;d=eC.prototype;d.b=function(){return this};d.A=function(){return"Home"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Home"};d.r=function(){return 2255103};d.B=function(){return Z(this)};d.$classData=l({$p:0},!1,"AppRouter$Home$",{$p:1,c:1,yz:1,C:1,q:1,k:1,h:1});var fC=void 0;
function tc(){fC||(fC=(new eC).b());return fC}function ku(){this.Rp=this.$h=this.I=null}ku.prototype=new n;ku.prototype.constructor=ku;ku.prototype.ek=function(){return this.Rp};ku.prototype.uo=function(a){ue();var b=vb().Kd;return gC(this,b,a)};ku.prototype.wo=function(a){ue();var b=vb().Kd;return gC(this,a,b)};function ju(a,b,c){a.I=b;a.$h=c;a.Rp=Ku(a.I);return a}ku.prototype.$classData=l({vq:0},!1,"japgolly.scalajs.react.component.Js$$anon$3",{vq:1,c:1,Aq:1,zq:1,qq:1,pq:1,rq:1});
function hC(){this.ml=this.ql=this.lf=this.$h=this.I=null}hC.prototype=new n;hC.prototype.constructor=hC;hC.prototype.ek=function(){return this.lf.ek()};hC.prototype.uo=function(a){ue();var b=this.lf,c=this.ql;a=mb(a,this.ml);return gC(b,c,a)};function gC(a,b,c){var e=new hC;e.lf=a;e.ql=b;e.ml=c;e.I=a.I;e.$h=mb(c,a.$h);return e}hC.prototype.wo=function(a){ue();var b=this.lf;a=mb(a,this.ql);return gC(b,a,this.ml)};
hC.prototype.$classData=l({wq:0},!1,"japgolly.scalajs.react.component.Js$$anon$4",{wq:1,c:1,Aq:1,zq:1,qq:1,pq:1,rq:1});function iC(){this.xl=this.ya=this.Mf=this.I=null}iC.prototype=new n;iC.prototype.constructor=iC;function iu(a,b,c,e){var f=new iC;if(null===a)throw np(Xh(),null);f.ya=a;f.xl=e;f.I=b;f.Mf=c;return f}iC.prototype.hn=function(a){var b=this.ya,c=vb().Kd,e=vb().Kd;return jC(b,this,a,c,e,this.xl)};iC.prototype.xo=function(a){var b=this.ya,c=vb().Kd,e=vb().Kd;return jC(b,this,c,e,a,this.xl)};
iC.prototype.$classData=l({Cq:0},!1,"japgolly.scalajs.react.component.JsBaseComponentTemplate$$anon$1",{Cq:1,c:1,Fq:1,Eq:1,nq:1,mq:1,oq:1});function kC(){this.Fj=this.rl=this.kl=this.Mk=this.lf=this.ya=this.Mf=this.I=null}kC.prototype=new n;kC.prototype.constructor=kC;kC.prototype.hn=function(a){var b=this.ya,c=this.lf;a=mb(this.Mk,a);return jC(b,c,a,this.kl,this.rl,this.Fj)};
function jC(a,b,c,e,f,h){var m=new kC;if(null===a)throw np(Xh(),null);m.ya=a;m.lf=b;m.Mk=c;m.kl=e;m.rl=f;m.Fj=h;m.I=b.I;vb();a=e.g(b.Mf);b=m.Fj;c=new di;c.hf=a;c.Cj=b;a=c.hf;b=f.g(a.ck);f=$d(new ae,b,mb(f,a.cj),a.xj);m.Mf=f;return m}kC.prototype.xo=function(a){var b=this.ya,c=this.lf,e=this.Mk,f=this.kl;a=mb(a,this.rl);return jC(b,c,e,f,a,this.Fj)};kC.prototype.$classData=l({Dq:0},!1,"japgolly.scalajs.react.component.JsBaseComponentTemplate$$anon$2",{Dq:1,c:1,Fq:1,Eq:1,nq:1,mq:1,oq:1});
function Jx(){this.F=null}Jx.prototype=new Eg;Jx.prototype.constructor=Jx;d=Jx.prototype;d.A=function(){return"AbsUrl"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.im?this.F===a.F:!1};d.z=function(a){switch(a){case 0:return this.F;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.Zj=function(a){return a.F};
d.i=function(a){this.F=a;-1===(this.F.indexOf("://")|0)&&(a=this+" doesn't seem to be a valid URL. It's missing '://'. Consider using AbsUrl.fromWindow.",hj().warn(a));return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.uj=function(a){return(new Jx).i(a)};d.$classData=l({im:0},!1,"japgolly.scalajs.react.extra.router.AbsUrl",{im:1,lm:1,c:1,C:1,q:1,k:1,h:1});function Dx(){this.F=null}Dx.prototype=new Eg;Dx.prototype.constructor=Dx;d=Dx.prototype;d.A=function(){return"BaseUrl"};
d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.jm?this.F===a.F:!1};d.z=function(a){switch(a){case 0:return this.F;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.Zj=function(a){return a.F};d.i=function(a){this.F=a;-1===(this.F.indexOf("://")|0)&&(a=this+" doesn't seem to be a valid URL. It's missing '://'. Consider using BaseUrl.fromWindowOrigin.",hj().warn(a));return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
function Ox(a,b){return(new Jx).i(""+a.F+b.F)}d.uj=function(a){return(new Dx).i(a)};d.$classData=l({jm:0},!1,"japgolly.scalajs.react.extra.router.BaseUrl",{jm:1,lm:1,c:1,C:1,q:1,k:1,h:1});function ic(){this.F=null}ic.prototype=new Eg;ic.prototype.constructor=ic;d=ic.prototype;d.A=function(){return"Path"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.km?this.F===a.F:!1};d.z=function(a){switch(a){case 0:return this.F;default:throw(new Y).i(""+a);}};
d.m=function(){return al(P(),this)};d.Zj=function(a){return a.F};d.i=function(a){this.F=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.uj=function(a){return(new ic).i(a)};d.$classData=l({km:0},!1,"japgolly.scalajs.react.extra.router.Path",{km:1,lm:1,c:1,C:1,q:1,k:1,h:1});function Yx(){}Yx.prototype=new n;Yx.prototype.constructor=Yx;d=Yx.prototype;d.b=function(){return this};d.A=function(){return"Force"};d.y=function(){return 0};
d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Force"};d.r=function(){return 68065995};d.B=function(){return Z(this)};d.$classData=l({or:0},!1,"japgolly.scalajs.react.extra.router.Redirect$Force$",{or:1,c:1,mm:1,C:1,q:1,k:1,h:1});var Xx=void 0;function lC(){}lC.prototype=new n;lC.prototype.constructor=lC;d=lC.prototype;d.b=function(){return this};d.A=function(){return"Push"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Push"};d.r=function(){return 2499386};
d.B=function(){return Z(this)};d.$classData=l({pr:0},!1,"japgolly.scalajs.react.extra.router.Redirect$Push$",{pr:1,c:1,mm:1,C:1,q:1,k:1,h:1});var mC=void 0;function Sx(){mC||(mC=(new lC).b());return mC}function nC(){}nC.prototype=new n;nC.prototype.constructor=nC;d=nC.prototype;d.b=function(){return this};d.A=function(){return"Replace"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Replace"};d.r=function(){return-1535817068};d.B=function(){return Z(this)};
d.$classData=l({qr:0},!1,"japgolly.scalajs.react.extra.router.Redirect$Replace$",{qr:1,c:1,mm:1,C:1,q:1,k:1,h:1});var oC=void 0;function uc(){oC||(oC=(new nC).b());return oC}function $g(){this.hf=null}$g.prototype=new n;$g.prototype.constructor=$g;d=$g.prototype;d.A=function(){return"Renderer"};d.y=function(){return 1};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.rk){var b=this.hf;a=a.hf;return null===b?null===a:b.n(a)}return!1};
d.z=function(a){switch(a){case 0:return this.hf;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.zb=function(a){this.hf=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({rk:0},!1,"japgolly.scalajs.react.extra.router.Renderer",{rk:1,c:1,lr:1,C:1,q:1,k:1,h:1});function pC(){}pC.prototype=new Hg;pC.prototype.constructor=pC;d=pC.prototype;d.b=function(){return this};d.A=function(){return"BroadcastSync"};d.y=function(){return 0};
d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"BroadcastSync"};d.r=function(){return-155951396};d.B=function(){return Z(this)};d.$classData=l({rr:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$BroadcastSync$",{rr:1,qg:1,c:1,C:1,q:1,k:1,h:1});var qC=void 0;function qu(){qC||(qC=(new pC).b());return qC}function Nx(){this.bi=null}Nx.prototype=new Hg;Nx.prototype.constructor=Nx;d=Nx.prototype;d.oa=function(a){this.bi=a;return this};d.A=function(){return"Log"};d.y=function(){return 1};
d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.sk?this.bi===a.bi:!1};d.z=function(a){switch(a){case 0:return this.bi;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({sk:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$Log",{sk:1,qg:1,c:1,C:1,q:1,k:1,h:1});function Vx(){this.lc=null}Vx.prototype=new Hg;Vx.prototype.constructor=Vx;d=Vx.prototype;d.A=function(){return"PushState"};
d.y=function(){return 1};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.tk){var b=this.lc;a=a.lc;return null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.lc;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.Fh=function(a){this.lc=a;return this};d.B=function(){return Z(this)};d.$classData=l({tk:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$PushState",{tk:1,qg:1,c:1,C:1,q:1,k:1,h:1});
function Wx(){this.lc=null}Wx.prototype=new Hg;Wx.prototype.constructor=Wx;d=Wx.prototype;d.A=function(){return"ReplaceState"};d.y=function(){return 1};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.uk){var b=this.lc;a=a.lc;return null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.lc;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.Fh=function(a){this.lc=a;return this};d.B=function(){return Z(this)};
d.$classData=l({uk:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$ReplaceState",{uk:1,qg:1,c:1,C:1,q:1,k:1,h:1});function Ux(){this.eh=null}Ux.prototype=new Hg;Ux.prototype.constructor=Ux;d=Ux.prototype;d.A=function(){return"Return"};d.y=function(){return 1};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.vk?D(G(),this.eh,a.eh):!1};d.z=function(a){switch(a){case 0:return this.eh;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.o=function(a){this.eh=a;return this};
d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({vk:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$Return",{vk:1,qg:1,c:1,C:1,q:1,k:1,h:1});function Lg(){this.Ig=this.Gg=null}Lg.prototype=new Hg;Lg.prototype.constructor=Lg;d=Lg.prototype;d.A=function(){return"Sequence"};d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(Jg(a)){var b=this.Gg,c=a.Gg;if(null===b?null===c:Pm(b,c))return b=this.Ig,a=a.Ig,null===b?null===a:b.n(a)}return!1};
d.z=function(a){switch(a){case 0:return this.Gg;case 1:return this.Ig;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};function Jg(a){return!!(a&&a.$classData&&a.$classData.p.om)}d.$classData=l({om:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$Sequence",{om:1,qg:1,c:1,C:1,q:1,k:1,h:1});function Zx(){this.lc=null}Zx.prototype=new Hg;Zx.prototype.constructor=Zx;d=Zx.prototype;d.A=function(){return"SetWindowLocation"};
d.y=function(){return 1};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.wk){var b=this.lc;a=a.lc;return null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.lc;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.Fh=function(a){this.lc=a;return this};d.B=function(){return Z(this)};
d.$classData=l({wk:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$SetWindowLocation",{wk:1,qg:1,c:1,C:1,q:1,k:1,h:1});function rC(){this.ul=null}rC.prototype=new Dz;rC.prototype.constructor=rC;function dh(a){var b=new rC;b.ul=a;return b}rC.prototype.Gc=function(a){return D(G(),this.ul,a)};rC.prototype.Ze=function(a,b){return D(G(),this.ul,a)?a:b.g(a)};rC.prototype.$classData=l({vr:0},!1,"japgolly.scalajs.react.extra.router.RouterConfigDsl$$anonfun$1",{vr:1,gz:1,c:1,T:1,Ta:1,k:1,h:1});
function sC(){this.Fb=this.Ai=null}sC.prototype=new fh;sC.prototype.constructor=sC;d=sC.prototype;d.A=function(){return"Contramap"};function Lx(a,b){var c=new sC;c.Ai=a;c.Fb=b;return c}d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.qm){var b=this.Ai,c=a.Ai;if(null===b?null===c:b.n(c))return b=this.Fb,a=a.Fb,null===b?null===a:b.n(a)}return!1};d.z=function(a){switch(a){case 0:return this.Ai;case 1:return this.Fb;default:throw(new Y).i(""+a);}};
d.m=function(){return al(P(),this)};d.Kj=function(){return this.Ai.Kj()};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({qm:0},!1,"japgolly.scalajs.react.extra.router.RouterCtl$Contramap",{qm:1,xr:1,c:1,C:1,q:1,k:1,h:1});function tC(){}tC.prototype=new vk;tC.prototype.constructor=tC;d=tC.prototype;d.b=function(){return this};d.A=function(){return"Unrelated"};d.y=function(){return 0};d.kn=function(){return!1};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Unrelated"};
d.Vl=function(a){return a.U()};d.r=function(){return 511295314};d.B=function(){return Z(this)};d.$classData=l({gt:0},!1,"scalacss.internal.AttrCmp$Unrelated$",{gt:1,ym:1,c:1,C:1,q:1,k:1,h:1});var uC=void 0;function Ak(){uC||(uC=(new tC).b());return uC}function hA(){this.Ff=null}hA.prototype=new gB;hA.prototype.constructor=hA;hA.prototype.b=function(){gA=this;this.Ff=kk("background-color");return this};hA.prototype.Yd=function(){return this.Ff};
hA.prototype.$classData=l({ht:0},!1,"scalacss.internal.Attrs$backgroundColor$",{ht:1,vu:1,Qi:1,we:1,c:1,ru:1,zk:1});var gA=void 0;function Qz(){this.Ff=null}Qz.prototype=new gB;Qz.prototype.constructor=Qz;Qz.prototype.b=function(){Pz=this;this.Ff=kk("color");return this};Qz.prototype.Yd=function(){return this.Ff};Qz.prototype.$classData=l({it:0},!1,"scalacss.internal.Attrs$color$",{it:1,vu:1,Qi:1,we:1,c:1,ru:1,zk:1});var Pz=void 0;function vC(){Mk.call(this)}vC.prototype=new Nk;
vC.prototype.constructor=vC;d=vC.prototype;d.b=function(){Mk.prototype.i.call(this,"moz");return this};d.A=function(){return"moz"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"moz"};d.r=function(){return 108312};d.B=function(){return Z(this)};d.$classData=l({ut:0},!1,"scalacss.internal.CanIUse$Prefix$moz$",{ut:1,yk:1,c:1,C:1,q:1,k:1,h:1});var wC=void 0;function Qk(){wC||(wC=(new vC).b());return wC}function xC(){Mk.call(this)}xC.prototype=new Nk;
xC.prototype.constructor=xC;d=xC.prototype;d.b=function(){Mk.prototype.i.call(this,"ms");return this};d.A=function(){return"ms"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"ms"};d.r=function(){return 3494};d.B=function(){return Z(this)};d.$classData=l({vt:0},!1,"scalacss.internal.CanIUse$Prefix$ms$",{vt:1,yk:1,c:1,C:1,q:1,k:1,h:1});var yC=void 0;function Rk(){yC||(yC=(new xC).b());return yC}function zC(){Mk.call(this)}zC.prototype=new Nk;
zC.prototype.constructor=zC;d=zC.prototype;d.b=function(){Mk.prototype.i.call(this,"o");return this};d.A=function(){return"o"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"o"};d.r=function(){return 111};d.B=function(){return Z(this)};d.$classData=l({wt:0},!1,"scalacss.internal.CanIUse$Prefix$o$",{wt:1,yk:1,c:1,C:1,q:1,k:1,h:1});var AC=void 0;function Sk(){AC||(AC=(new zC).b());return AC}function BC(){Mk.call(this)}BC.prototype=new Nk;
BC.prototype.constructor=BC;d=BC.prototype;d.b=function(){Mk.prototype.i.call(this,"webkit");return this};d.A=function(){return"webkit"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"webkit"};d.r=function(){return-791796990};d.B=function(){return Z(this)};d.$classData=l({xt:0},!1,"scalacss.internal.CanIUse$Prefix$webkit$",{xt:1,yk:1,c:1,C:1,q:1,k:1,h:1});var CC=void 0;function Tk(){CC||(CC=(new BC).b());return CC}function DC(){}DC.prototype=new n;
DC.prototype.constructor=DC;d=DC.prototype;d.b=function(){return this};d.A=function(){return"Full"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Full"};d.r=function(){return 2201263};d.B=function(){return Z(this)};d.$classData=l({yt:0},!1,"scalacss.internal.CanIUse$Support$Full$",{yt:1,c:1,ch:1,C:1,q:1,k:1,h:1});var EC=void 0;function M(){EC||(EC=(new DC).b());return EC}function FC(){}FC.prototype=new n;FC.prototype.constructor=FC;d=FC.prototype;d.b=function(){return this};
d.A=function(){return"FullX"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"FullX"};d.r=function(){return 68239241};d.B=function(){return Z(this)};d.$classData=l({zt:0},!1,"scalacss.internal.CanIUse$Support$FullX$",{zt:1,c:1,ch:1,C:1,q:1,k:1,h:1});var GC=void 0;function Hk(){GC||(GC=(new FC).b());return GC}function HC(){}HC.prototype=new n;HC.prototype.constructor=HC;d=HC.prototype;d.b=function(){return this};d.A=function(){return"Partial"};d.y=function(){return 0};
d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Partial"};d.r=function(){return 871719265};d.B=function(){return Z(this)};d.$classData=l({At:0},!1,"scalacss.internal.CanIUse$Support$Partial$",{At:1,c:1,ch:1,C:1,q:1,k:1,h:1});var IC=void 0;function Jk(){IC||(IC=(new HC).b());return IC}function JC(){}JC.prototype=new n;JC.prototype.constructor=JC;d=JC.prototype;d.b=function(){return this};d.A=function(){return"PartialX"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};
d.m=function(){return"PartialX"};d.r=function(){return 1253493527};d.B=function(){return Z(this)};d.$classData=l({Bt:0},!1,"scalacss.internal.CanIUse$Support$PartialX$",{Bt:1,c:1,ch:1,C:1,q:1,k:1,h:1});var KC=void 0;function L(){KC||(KC=(new JC).b());return KC}function LC(){}LC.prototype=new n;LC.prototype.constructor=LC;d=LC.prototype;d.b=function(){return this};d.A=function(){return"Unknown"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Unknown"};d.r=function(){return 1379812394};
d.B=function(){return Z(this)};d.$classData=l({Ct:0},!1,"scalacss.internal.CanIUse$Support$Unknown$",{Ct:1,c:1,ch:1,C:1,q:1,k:1,h:1});var MC=void 0;function Kk(){MC||(MC=(new LC).b());return MC}function NC(){}NC.prototype=new n;NC.prototype.constructor=NC;d=NC.prototype;d.b=function(){return this};d.A=function(){return"Unsupported"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Unsupported"};d.r=function(){return 1716505109};d.B=function(){return Z(this)};
d.$classData=l({Dt:0},!1,"scalacss.internal.CanIUse$Support$Unsupported$",{Dt:1,c:1,ch:1,C:1,q:1,k:1,h:1});var OC=void 0;function O(){OC||(OC=(new NC).b());return OC}function yy(){this.Jg=this.hi=null}yy.prototype=new n;yy.prototype.constructor=yy;d=yy.prototype;d.A=function(){return"Cond"};d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Dm){var b=this.hi,c=a.hi;if(null===b?null===c:b.n(c))return b=this.Jg,a=a.Jg,null===b?null===a:Pm(b,a)}return!1};
d.z=function(a){switch(a){case 0:return this.hi;case 1:return this.Jg;default:throw(new Y).i(""+a);}};d.m=function(){var a=Yj(Xj(),this.Jg);a.l()?a=B():(a=a.R(),a=(new Pf).o(Zl($l(),a)));return(a.l()?"":a.R()+" ")+Kl(km(),"",this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Dm:0},!1,"scalacss.internal.Cond",{Dm:1,c:1,wA:1,C:1,q:1,k:1,h:1});function Pl(){this.Bi=this.Bh=this.Ah=this.zh=this.Bc=this.xb=null}Pl.prototype=new n;Pl.prototype.constructor=Pl;d=Pl.prototype;
d.A=function(){return"FontFace"};d.y=function(){return 6};d.n=function(a){if(this===a)return!0;if(dm(a)){if(this.xb===a.xb)var b=this.Bc,c=a.Bc,b=null===b?null===c:b.n(c);else b=!1;b?(b=this.zh,c=a.zh,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Ah,c=a.Ah,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.Bh,c=a.Bh,b=null===b?null===c:b.n(c)):b=!1;if(b)return b=this.Bi,a=a.Bi,null===b?null===a:b.n(a)}return!1};
d.z=function(a){switch(a){case 0:return this.xb;case 1:return this.Bc;case 2:return this.zh;case 3:return this.Ah;case 4:return this.Bh;case 5:return this.Bi;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};function dm(a){return!!(a&&a.$classData&&a.$classData.p.Fm)}d.$classData=l({Fm:0},!1,"scalacss.internal.CssEntry$FontFace",{Fm:1,c:1,Em:1,C:1,q:1,k:1,h:1});function PC(){this.Dh=this.aa=null}PC.prototype=new n;
PC.prototype.constructor=PC;function im(a,b){var c=new PC;c.aa=a;c.Dh=b;return c}d=PC.prototype;d.A=function(){return"Keyframes"};d.y=function(){return 2};d.n=function(a){if(this===a)return!0;if(cm(a)){var b=this.aa,c=a.aa;if(null===b?null===c:b.n(c))return b=this.Dh,a=a.Dh,null===b?null===a:TA(b,a)}return!1};d.z=function(a){switch(a){case 0:return this.aa;case 1:return this.Dh;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
function cm(a){return!!(a&&a.$classData&&a.$classData.p.Gm)}d.$classData=l({Gm:0},!1,"scalacss.internal.CssEntry$Keyframes",{Gm:1,c:1,Em:1,C:1,q:1,k:1,h:1});function QC(){this.Jf=this.Sg=this.Ne=null}QC.prototype=new n;QC.prototype.constructor=QC;d=QC.prototype;d.A=function(){return"Style"};d.y=function(){return 3};d.n=function(a){if(this===a)return!0;if(bm(a)){var b=this.Ne,c=a.Ne;if((null===b?null===c:b.n(c))&&this.Sg===a.Sg)return b=this.Jf,a=a.Jf,null===b?null===a:b.n(a)}return!1};
d.z=function(a){switch(a){case 0:return this.Ne;case 1:return this.Sg;case 2:return this.Jf;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};function Yl(a,b,c){var e=new QC;e.Ne=a;e.Sg=b;e.Jf=c;return e}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};function bm(a){return!!(a&&a.$classData&&a.$classData.p.Hm)}d.$classData=l({Hm:0},!1,"scalacss.internal.CssEntry$Style",{Hm:1,c:1,Em:1,C:1,q:1,k:1,h:1});function RC(){this.dh=null}RC.prototype=new YA;
RC.prototype.constructor=RC;RC.prototype.b=function(){op.prototype.b.call(this);return this};RC.prototype.$classData=l({Pt:0},!1,"scalacss.internal.Dsl$",{Pt:1,Qt:1,bt:1,uu:1,c:1,qu:1,zk:1});var SC=void 0;function Rz(){SC||(SC=(new RC).b());return SC}function Mz(){this.s=null}Mz.prototype=new Cm;Mz.prototype.constructor=Mz;d=Mz.prototype;d.b=function(){Bm.prototype.i.call(this,"vw");return this};d.A=function(){return"vw"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"vw"};
d.r=function(){return 3777};d.B=function(){return Z(this)};d.$classData=l({Ut:0},!1,"scalacss.internal.LengthUnit$vw$",{Ut:1,cA:1,c:1,C:1,q:1,k:1,h:1});var Lz=void 0;function Hm(a){return!!(a&&a.$classData&&a.$classData.p.jA)}function Jm(a){return!!(a&&a.$classData&&a.$classData.p.nA)}function Im(a){return!!(a&&a.$classData&&a.$classData.p.oA)}function mp(){this.dc=this.dh=null}mp.prototype=new YA;mp.prototype.constructor=mp;
mp.prototype.$classData=l({Gu:0},!1,"scalacss.internal.mutable.StyleSheet$Base$dsl$",{Gu:1,Qt:1,bt:1,uu:1,c:1,qu:1,zk:1});function H(){this.P=this.N=null}H.prototype=new n;H.prototype.constructor=H;d=H.prototype;d.A=function(){return"Tuple2"};d.y=function(){return 2};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Zm?D(G(),this.N,a.N)&&D(G(),this.P,a.P):!1};d.d=function(a,b){this.N=a;this.P=b;return this};
d.z=function(a){a:switch(a){case 0:a=this.N;break a;case 1:a=this.P;break a;default:throw(new Y).i(""+a);}return a};d.m=function(){return"("+this.N+","+this.P+")"};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};d.$classData=l({Zm:0},!1,"scala.Tuple2",{Zm:1,c:1,aB:1,C:1,q:1,k:1,h:1});function em(){this.pg=this.og=this.ng=null}em.prototype=new n;em.prototype.constructor=em;d=em.prototype;d.A=function(){return"Tuple3"};d.y=function(){return 3};
d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.$m?D(G(),this.ng,a.ng)&&D(G(),this.og,a.og)&&D(G(),this.pg,a.pg):!1};d.z=function(a){a:switch(a){case 0:a=this.ng;break a;case 1:a=this.og;break a;case 2:a=this.pg;break a;default:throw(new Y).i(""+a);}return a};d.m=function(){return"("+this.ng+","+this.og+","+this.pg+")"};d.nf=function(a,b,c){this.ng=a;this.og=b;this.pg=c;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({$m:0},!1,"scala.Tuple3",{$m:1,c:1,bB:1,C:1,q:1,k:1,h:1});function Uy(){this.ma=null}Uy.prototype=new jB;Uy.prototype.constructor=Uy;Uy.prototype.i=function(a){ww.prototype.Sa.call(this,a,0,0,!0);return this};Uy.prototype.$classData=l({ov:0},!1,"java.lang.NumberFormatException",{ov:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function kt(){this.ma=null}kt.prototype=new kB;kt.prototype.constructor=kt;kt.prototype.b=function(){ww.prototype.Sa.call(this,null,0,0,!0);return this};
kt.prototype.$classData=l({Av:0},!1,"java.util.FormatterClosedException",{Av:1,qo:1,ib:1,mb:1,Ua:1,c:1,h:1});function TC(){this.ma=null}TC.prototype=new jB;TC.prototype.constructor=TC;function UC(){}UC.prototype=TC.prototype;function VC(){}VC.prototype=new nB;VC.prototype.constructor=VC;d=VC.prototype;d.b=function(){return this};d.A=function(){return"None"};d.y=function(){return 0};d.l=function(){return!0};d.R=function(){throw(new yw).i("None.get");};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"None"};
d.r=function(){return 2433880};d.B=function(){return Z(this)};d.$classData=l({Sv:0},!1,"scala.None$",{Sv:1,Tv:1,c:1,C:1,q:1,k:1,h:1});var WC=void 0;function B(){WC||(WC=(new VC).b());return WC}function Qp(){}Qp.prototype=new Dz;Qp.prototype.constructor=Qp;Qp.prototype.b=function(){return this};Qp.prototype.Gc=function(){return!0};Qp.prototype.Ze=function(){return Sp().Pg};Qp.prototype.$classData=l({Xv:0},!1,"scala.PartialFunction$$anonfun$1",{Xv:1,gz:1,c:1,T:1,Ta:1,k:1,h:1});
function Pf(){this.F=null}Pf.prototype=new nB;Pf.prototype.constructor=Pf;d=Pf.prototype;d.A=function(){return"Some"};d.y=function(){return 1};d.n=function(a){return this===a?!0:Qf(a)?D(G(),this.F,a.F):!1};d.l=function(){return!1};d.z=function(a){switch(a){case 0:return this.F;default:throw(new Y).i(""+a);}};d.R=function(){return this.F};d.m=function(){return al(P(),this)};d.o=function(a){this.F=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
function Qf(a){return!!(a&&a.$classData&&a.$classData.p.Yo)}d.$classData=l({Yo:0},!1,"scala.Some",{Yo:1,Tv:1,c:1,C:1,q:1,k:1,h:1});function rB(){this.ma=null}rB.prototype=new jB;rB.prototype.constructor=rB;
rB.prototype.Ih=function(a,b){kz(I(),0<=b&&b<(a.length|0));if(b===(-1+(a.length|0)|0))var c="at terminal";else c=65535&(a.charCodeAt(1+b|0)|0),c="'\\"+nt(c)+"' not one of [\\b, \\t, \\n, \\f, \\r, \\\\, \\\", \\'] at";ww.prototype.Sa.call(this,"invalid escape "+c+" index "+b+' in "'+a+'". Use \\\\ for literal \\.',0,0,!0);return this};rB.prototype.$classData=l({fw:0},!1,"scala.StringContext$InvalidEscapeException",{fw:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function Pc(){this.F=null}Pc.prototype=new tB;
Pc.prototype.constructor=Pc;d=Pc.prototype;d.A=function(){return"Left"};d.y=function(){return 1};d.n=function(a){return this===a?!0:Mm(a)?D(G(),this.F,a.F):!1};d.z=function(a){switch(a){case 0:return this.F;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.o=function(a){this.F=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};function Mm(a){return!!(a&&a.$classData&&a.$classData.p.ap)}
d.$classData=l({ap:0},!1,"scala.util.Left",{ap:1,Mw:1,c:1,C:1,q:1,k:1,h:1});function Xg(){this.F=null}Xg.prototype=new tB;Xg.prototype.constructor=Xg;d=Xg.prototype;d.A=function(){return"Right"};d.y=function(){return 1};d.n=function(a){return this===a?!0:Lm(a)?D(G(),this.F,a.F):!1};d.z=function(a){switch(a){case 0:return this.F;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.o=function(a){this.F=a;return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
function Lm(a){return!!(a&&a.$classData&&a.$classData.p.bp)}d.$classData=l({bp:0},!1,"scala.util.Right",{bp:1,Mw:1,c:1,C:1,q:1,k:1,h:1});function XC(a,b,c){c=c.$e(a.tf());a.O(t(new u,function(a,b,c){return function(a){return c.fb(b.g(a).Ga())}}(a,b,c)));return c.La()}function Wk(a,b){b=b.sg();jx(b,a);b.fb(a.db());return b.La()}function Qm(a){return a.Yh(a.Dd()+"(",", ",")")}
function Zj(a,b){var c=a.da();a.O(t(new u,function(a,b,c,m){return function(a){return!!b.g(a)!==c?m.rb(a):void 0}}(a,b,!1,c)));return c.La()}function YC(a,b,c){c=c.$e(a.tf());if(b&&b.$classData&&b.$classData.p.wf){var e=b.Ga().L();kx(c,a,e)}c.fb(a.db());c.fb(b.Ga());return c.La()}function ZC(a){if(a.l())throw(new Uz).i("empty.tail");return a.ae(1)}function dl(a,b){b=b.$e(a.tf());jx(b,a);return b}
function $C(a){a=Jb(la(a.tf()));for(var b=-1+(a.length|0)|0;;)if(-1!==b&&36===(65535&(a.charCodeAt(b)|0)))b=-1+b|0;else break;if(-1===b||46===(65535&(a.charCodeAt(b)|0)))return"";for(var c="";;){for(var e=1+b|0;;)if(-1!==b&&57>=(65535&(a.charCodeAt(b)|0))&&48<=(65535&(a.charCodeAt(b)|0)))b=-1+b|0;else break;for(var f=b;;)if(-1!==b&&36!==(65535&(a.charCodeAt(b)|0))&&46!==(65535&(a.charCodeAt(b)|0)))b=-1+b|0;else break;var h=1+b|0;if(b===f&&e!==(a.length|0))return c;for(;;)if(-1!==b&&36===(65535&(a.charCodeAt(b)|
0)))b=-1+b|0;else break;var f=-1===b?!0:46===(65535&(a.charCodeAt(b)|0)),m;(m=f)||(m=65535&(a.charCodeAt(h)|0),m=!(90<m&&127>m||65>m));if(m){e=a.substring(h,e);h=c;if(null===h)throw(new Ho).b();c=""===h?e:""+e+nt(46)+c;if(f)return c}}}function aD(){this.Ka=null}aD.prototype=new sz;aD.prototype.constructor=aD;function bD(){}bD.prototype=aD.prototype;function cD(){LB.call(this)}cD.prototype=new MB;cD.prototype.constructor=cD;cD.prototype.Vn=function(a){return dD(a)};
cD.prototype.$classData=l({Bx:0},!1,"scala.collection.immutable.HashMap$HashTrieMap$$anon$1",{Bx:1,ty:1,ac:1,c:1,Hb:1,K:1,J:1});function eD(){LB.call(this)}eD.prototype=new MB;eD.prototype.constructor=eD;eD.prototype.Vn=function(a){return a.Ab};eD.prototype.$classData=l({Gx:0},!1,"scala.collection.immutable.HashSet$HashTrieSet$$anon$1",{Gx:1,ty:1,ac:1,c:1,Hb:1,K:1,J:1});function fD(){}fD.prototype=new GB;fD.prototype.constructor=fD;fD.prototype.b=function(){return this};fD.prototype.yg=function(){return Q()};
fD.prototype.$classData=l({ey:0},!1,"scala.collection.immutable.Set$",{ey:1,ip:1,kp:1,gp:1,bc:1,c:1,cc:1});var gD=void 0;function fz(){gD||(gD=(new fD).b());return gD}function hD(){this.Uc=null}hD.prototype=new aC;hD.prototype.constructor=hD;hD.prototype.b=function(){$B.prototype.b.call(this);return this};hD.prototype.La=function(){return iD(this)};function iD(a){return a.Uc.Ea.kc().Pf(t(new u,function(){return function(a){return a.kc()}}(a)),(iq(),(new vz).b()))}
function jD(a){return!!(a&&a.$classData&&a.$classData.p.rp)}hD.prototype.$classData=l({rp:0},!1,"scala.collection.immutable.Stream$StreamBuilder",{rp:1,wB:1,c:1,qi:1,Sd:1,Qd:1,Pd:1});function ql(){this.rh=this.cg=this.jh=0;this.An=this.yn=this.wn=this.un=this.sn=this.sh=null}ql.prototype=new n;ql.prototype.constructor=ql;d=ql.prototype;d.Q=function(){return this.wn};d.b=function(){this.sh=k(r(p),[32]);this.rh=1;this.cg=this.jh=0;return this};d.gb=function(){return this.rh};
d.rc=function(a){return rl(this,a)};d.df=function(a){this.An=a};d.Pa=function(){return this.sh};d.ua=function(a){this.un=a};d.Ca=function(){return this.yn};
function rl(a,b){if(a.cg>=a.sh.e.length){var c=32+a.jh|0,e=a.jh^c;if(1024>e)1===a.gb()&&(a.S(k(r(p),[32])),a.v().e[0]=a.Pa(),a.Nc(1+a.gb()|0)),a.la(k(r(p),[32])),a.v().e[31&(c>>>5|0)]=a.Pa();else if(32768>e)2===a.gb()&&(a.ua(k(r(p),[32])),a.G().e[0]=a.v(),a.Nc(1+a.gb()|0)),a.la(k(r(p),[32])),a.S(k(r(p),[32])),a.v().e[31&(c>>>5|0)]=a.Pa(),a.G().e[31&(c>>>10|0)]=a.v();else if(1048576>e)3===a.gb()&&(a.Qa(k(r(p),[32])),a.Q().e[0]=a.G(),a.Nc(1+a.gb()|0)),a.la(k(r(p),[32])),a.S(k(r(p),[32])),a.ua(k(r(p),
[32])),a.v().e[31&(c>>>5|0)]=a.Pa(),a.G().e[31&(c>>>10|0)]=a.v(),a.Q().e[31&(c>>>15|0)]=a.G();else if(33554432>e)4===a.gb()&&(a.$b(k(r(p),[32])),a.Ca().e[0]=a.Q(),a.Nc(1+a.gb()|0)),a.la(k(r(p),[32])),a.S(k(r(p),[32])),a.ua(k(r(p),[32])),a.Qa(k(r(p),[32])),a.v().e[31&(c>>>5|0)]=a.Pa(),a.G().e[31&(c>>>10|0)]=a.v(),a.Q().e[31&(c>>>15|0)]=a.G(),a.Ca().e[31&(c>>>20|0)]=a.Q();else if(1073741824>e)5===a.gb()&&(a.df(k(r(p),[32])),a.gc().e[0]=a.Ca(),a.Nc(1+a.gb()|0)),a.la(k(r(p),[32])),a.S(k(r(p),[32])),a.ua(k(r(p),
[32])),a.Qa(k(r(p),[32])),a.$b(k(r(p),[32])),a.v().e[31&(c>>>5|0)]=a.Pa(),a.G().e[31&(c>>>10|0)]=a.v(),a.Q().e[31&(c>>>15|0)]=a.G(),a.Ca().e[31&(c>>>20|0)]=a.Q(),a.gc().e[31&(c>>>25|0)]=a.Ca();else throw(new Ob).b();a.jh=c;a.cg=0}a.sh.e[a.cg]=b;a.cg=1+a.cg|0;return a}d.La=function(){return sl(this)};d.fd=function(a,b){lx(this,a,b)};d.S=function(a){this.sn=a};d.$b=function(a){this.yn=a};d.v=function(){return this.sn};d.gc=function(){return this.An};
function sl(a){var b=a.jh+a.cg|0;if(0===b)return Uk().Vb;var c=(new kD).vc(0,b,0);Vb(c,a,a.rh);1<a.rh&&Rb(c,0,-1+b|0);return c}d.rb=function(a){return rl(this,a)};d.qc=function(){};d.Nc=function(a){this.rh=a};d.G=function(){return this.un};d.la=function(a){this.sh=a};d.fb=function(a){return Tl(this,a)};d.Qa=function(a){this.wn=a};d.$classData=l({wy:0},!1,"scala.collection.immutable.VectorBuilder",{wy:1,c:1,qi:1,Sd:1,Qd:1,Pd:1,vp:1});
function lD(){this.Wk=this.bb=this.af=this.Vk=0;this.Cb=!1;this.Qk=0;this.Bn=this.zn=this.xn=this.vn=this.tn=this.Rk=null}lD.prototype=new EA;lD.prototype.constructor=lD;d=lD.prototype;
d.t=function(){if(!this.Cb)throw(new yw).i("reached iterator end");var a=this.Rk.e[this.bb];this.bb=1+this.bb|0;if(this.bb===this.Wk)if((this.af+this.bb|0)<this.Vk){var b=32+this.af|0,c=this.af^b;if(1024>c)this.la(this.v().e[31&(b>>>5|0)]);else if(32768>c)this.S(this.G().e[31&(b>>>10|0)]),this.la(this.v().e[0]);else if(1048576>c)this.ua(this.Q().e[31&(b>>>15|0)]),this.S(this.G().e[0]),this.la(this.v().e[0]);else if(33554432>c)this.Qa(this.Ca().e[31&(b>>>20|0)]),this.ua(this.Q().e[0]),this.S(this.G().e[0]),
this.la(this.v().e[0]);else if(1073741824>c)this.$b(this.gc().e[31&(b>>>25|0)]),this.Qa(this.Ca().e[0]),this.ua(this.Q().e[0]),this.S(this.G().e[0]),this.la(this.v().e[0]);else throw(new Ob).b();this.af=b;b=this.Vk-this.af|0;this.Wk=32>b?b:32;this.bb=0}else this.Cb=!1;return a};d.Q=function(){return this.xn};d.gb=function(){return this.Qk};d.df=function(a){this.Bn=a};d.Ie=function(a,b){this.Vk=b;this.af=-32&a;this.bb=31&a;a=b-this.af|0;this.Wk=32>a?a:32;this.Cb=(this.af+this.bb|0)<b;return this};
d.Pa=function(){return this.Rk};d.ua=function(a){this.vn=a};d.Ca=function(){return this.zn};d.S=function(a){this.tn=a};d.H=function(){return this.Cb};d.$b=function(a){this.zn=a};d.v=function(){return this.tn};d.gc=function(){return this.Bn};d.Nc=function(a){this.Qk=a};d.G=function(){return this.vn};d.la=function(a){this.Rk=a};d.Qa=function(a){this.xn=a};d.$classData=l({xy:0},!1,"scala.collection.immutable.VectorIterator",{xy:1,ac:1,c:1,Hb:1,K:1,J:1,vp:1});function mD(){this.Xh=this.Hc=null}
mD.prototype=new n;mD.prototype.constructor=mD;d=mD.prototype;d.A=function(){return"RedirectToPage"};d.y=function(){return 2};d.n=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.qk?D(G(),this.Hc,a.Hc)?this.Xh===a.Xh:!1:!1};d.z=function(a){switch(a){case 0:return this.Hc;case 1:return this.Xh;default:throw(new Y).i(""+a);}};d.m=function(){return al(P(),this)};d.r=function(){return um(U(),this)};function xc(a,b){var c=new mD;c.Hc=a;c.Xh=b;return c}d.B=function(){return Z(this)};
d.$classData=l({qk:0},!1,"japgolly.scalajs.react.extra.router.RedirectToPage",{qk:1,c:1,nr:1,lr:1,C:1,q:1,k:1,h:1});function nD(){}nD.prototype=new RA;nD.prototype.constructor=nD;function oD(){}oD.prototype=nD.prototype;nD.prototype.Bk=function(a){a=null===a?"null":ka(a);gp(this,null===a?"null":a);return this};nD.prototype.Ru=function(){QA.prototype.Qu.call(this);return this};function Ck(){}Ck.prototype=new Qu;Ck.prototype.constructor=Ck;d=Ck.prototype;d.b=function(){return this};d.A=function(){return"Overlap"};
d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Overlap"};d.r=function(){return 573365287};d.B=function(){return Z(this)};d.$classData=l({et:0},!1,"scalacss.internal.AttrCmp$Overlap$",{et:1,dt:1,ym:1,c:1,C:1,q:1,k:1,h:1});var Bk=void 0;function zk(){}zk.prototype=new Qu;zk.prototype.constructor=zk;d=zk.prototype;d.b=function(){return this};d.A=function(){return"Same"};d.y=function(){return 0};d.z=function(a){throw(new Y).i(""+a);};d.m=function(){return"Same"};
d.r=function(){return 2569350};d.B=function(){return Z(this)};d.$classData=l({ft:0},!1,"scalacss.internal.AttrCmp$Same$",{ft:1,dt:1,ym:1,c:1,C:1,q:1,k:1,h:1});var yk=void 0;function Xm(){}Xm.prototype=new n;Xm.prototype.constructor=Xm;Xm.prototype.b=function(){return this};Xm.prototype.$classData=l({du:0},!1,"scalacss.internal.Pseudo$$anon$2",{du:1,c:1,mw:1,vv:1,ow:1,gw:1,k:1,h:1});function $m(){}$m.prototype=new n;$m.prototype.constructor=$m;$m.prototype.b=function(){return this};
$m.prototype.$classData=l({fu:0},!1,"scalacss.internal.PseudoType$$anon$1",{fu:1,c:1,mw:1,vv:1,ow:1,gw:1,k:1,h:1});function Lo(){this.Ve=this.id=this.If=this.Vd=this.Hd=null}Lo.prototype=new jv;Lo.prototype.constructor=Lo;d=Lo.prototype;d.A=function(){return"StyleS"};d.y=function(){return 5};
d.n=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Um){var b=this.Hd,c=a.Hd;(null===b?null===c:TA(b,c))?(b=this.Vd,c=a.Vd,b=null===b?null===c:Pm(b,c)):b=!1;b?(b=this.If,c=a.If,b=null===b?null===c:b.n(c)):b=!1;b?(b=this.id,c=a.id,b=null===b?null===c:Pm(b,c)):b=!1;if(b)return b=this.Ve,a=a.Ve,null===b?null===a:Pm(b,a)}return!1};
d.z=function(a){switch(a){case 0:return this.Hd;case 1:return this.Vd;case 2:return this.If;case 3:return this.id;case 4:return this.Ve;default:throw(new Y).i(""+a);}};function Ko(a,b,c,e,f,h){a.Hd=b;a.Vd=c;a.If=e;a.id=f;a.Ve=h;return a}d.m=function(){return bn().Wi(pD(this))};function pD(a){var b=km(),c=a.If;return Rl(b,Xo(new Yo,c.l()?(new Io).i("???"):c.R(),a.id,a),Gy().Y)}d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};
d.$classData=l({Um:0},!1,"scalacss.internal.StyleS",{Um:1,yA:1,c:1,xA:1,C:1,q:1,k:1,h:1});function ot(){this.jf=this.ma=null}ot.prototype=new UC;ot.prototype.constructor=ot;ot.prototype.Pc=function(){return"Flags \x3d '"+this.jf+"'"};ot.prototype.i=function(a){this.jf=a;ww.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ho).b();return this};ot.prototype.$classData=l({wv:0},!1,"java.util.DuplicateFormatFlagsException",{wv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});
function uA(){this.jf=this.ma=null;this.bf=0}uA.prototype=new UC;uA.prototype.constructor=uA;uA.prototype.Pc=function(){return"Conversion \x3d "+nt(this.bf)+", Flags \x3d "+this.jf};uA.prototype.$classData=l({xv:0},!1,"java.util.FormatFlagsConversionMismatchException",{xv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function wt(){this.ma=null;this.bf=0}wt.prototype=new UC;wt.prototype.constructor=wt;wt.prototype.Pc=function(){return"Code point \x3d 0x"+(+(this.bf>>>0)).toString(16)};
wt.prototype.kb=function(a){this.bf=a;ww.prototype.Sa.call(this,null,0,0,!0);return this};wt.prototype.$classData=l({Bv:0},!1,"java.util.IllegalFormatCodePointException",{Bv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function tA(){this.ma=null;this.bf=0;this.cn=null}tA.prototype=new UC;tA.prototype.constructor=tA;tA.prototype.Pc=function(){return g.String.fromCharCode(this.bf)+" !\x3d "+Jb(this.cn)};
tA.prototype.$classData=l({Cv:0},!1,"java.util.IllegalFormatConversionException",{Cv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function Ht(){this.jf=this.ma=null}Ht.prototype=new UC;Ht.prototype.constructor=Ht;Ht.prototype.Pc=function(){return"Flags \x3d '"+this.jf+"'"};Ht.prototype.i=function(a){this.jf=a;ww.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ho).b();return this};Ht.prototype.$classData=l({Dv:0},!1,"java.util.IllegalFormatFlagsException",{Dv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});
function ut(){this.ma=null;this.Go=0}ut.prototype=new UC;ut.prototype.constructor=ut;ut.prototype.Pc=function(){return""+this.Go};ut.prototype.kb=function(a){this.Go=a;ww.prototype.Sa.call(this,null,0,0,!0);return this};ut.prototype.$classData=l({Ev:0},!1,"java.util.IllegalFormatPrecisionException",{Ev:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function Kt(){this.ma=null;this.Tp=0}Kt.prototype=new UC;Kt.prototype.constructor=Kt;Kt.prototype.Pc=function(){return""+this.Tp};
Kt.prototype.kb=function(a){this.Tp=a;ww.prototype.Sa.call(this,null,0,0,!0);return this};Kt.prototype.$classData=l({Fv:0},!1,"java.util.IllegalFormatWidthException",{Fv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function rt(){this.uf=this.ma=null}rt.prototype=new UC;rt.prototype.constructor=rt;rt.prototype.Pc=function(){return"Format specifier '"+this.uf+"'"};rt.prototype.i=function(a){this.uf=a;ww.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ho).b();return this};
rt.prototype.$classData=l({Gv:0},!1,"java.util.MissingFormatArgumentException",{Gv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function qt(){this.uf=this.ma=null}qt.prototype=new UC;qt.prototype.constructor=qt;qt.prototype.Pc=function(){return this.uf};qt.prototype.i=function(a){this.uf=a;ww.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ho).b();return this};qt.prototype.$classData=l({Hv:0},!1,"java.util.MissingFormatWidthException",{Hv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});
function mt(){this.uf=this.ma=null}mt.prototype=new UC;mt.prototype.constructor=mt;mt.prototype.Pc=function(){return"Conversion \x3d '"+this.uf+"'"};mt.prototype.i=function(a){this.uf=a;ww.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ho).b();return this};mt.prototype.$classData=l({Jv:0},!1,"java.util.UnknownFormatConversionException",{Jv:1,Je:1,Rc:1,ib:1,mb:1,Ua:1,c:1,h:1});function qD(){this.Ka=null}qD.prototype=new bD;qD.prototype.constructor=qD;
qD.prototype.b=function(){Zw.prototype.b.call(this);return this};qD.prototype.da=function(){rD();return(new ig).b()};qD.prototype.$classData=l({mx:0},!1,"scala.collection.Seq$",{mx:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1});var sD=void 0;function aq(){sD||(sD=(new qD).b());return sD}function tD(){this.Ka=null}tD.prototype=new bD;tD.prototype.constructor=tD;function uD(){}uD.prototype=tD.prototype;function vD(){}vD.prototype=new uz;vD.prototype.constructor=vD;
vD.prototype.b=function(){wD=this;gx(new fx,Fb(function(){return function(a){return a}}(this)));return this};function xD(a,b,c,e,f,h,m){var q=31&(b>>>h|0),y=31&(e>>>h|0);if(q!==y)return a=1<<q|1<<y,b=k(r(yD),[2]),q<y?(b.e[0]=c,b.e[1]=f):(b.e[0]=f,b.e[1]=c),zD(new AD,a,b,m);y=k(r(yD),[1]);q=1<<q;y.e[0]=xD(a,b,c,e,f,5+h|0,m);return zD(new AD,q,y,m)}vD.prototype.In=function(){return BD()};vD.prototype.$classData=l({wx:0},!1,"scala.collection.immutable.HashMap$",{wx:1,sx:1,tx:1,px:1,c:1,oB:1,k:1,h:1});
var wD=void 0;function CD(){wD||(wD=(new vD).b());return wD}function DD(){this.Ka=null}DD.prototype=new bD;DD.prototype.constructor=DD;DD.prototype.b=function(){Zw.prototype.b.call(this);return this};DD.prototype.da=function(){return(new ig).b()};DD.prototype.$classData=l({dy:0},!1,"scala.collection.immutable.Seq$",{dy:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1});var ED=void 0;function rD(){ED||(ED=(new DD).b());return ED}function FD(){this.Ka=null}FD.prototype=new bD;FD.prototype.constructor=FD;
FD.prototype.b=function(){Zw.prototype.b.call(this);return this};FD.prototype.da=function(){return(new YB).b()};FD.prototype.$classData=l({Ly:0},!1,"scala.collection.mutable.IndexedSeq$",{Ly:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1});var GD=void 0;function HD(){this.Ka=null}HD.prototype=new bD;HD.prototype.constructor=HD;HD.prototype.b=function(){Zw.prototype.b.call(this);return this};HD.prototype.da=function(){return(new A).b()};
HD.prototype.$classData=l({Wy:0},!1,"scala.scalajs.js.WrappedArray$",{Wy:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1});var ID=void 0;function Sz(){ID||(ID=(new HD).b());return ID}function JD(){this.uz=this.tz=this.sz=this.rz=this.Qp=this.qz=null}JD.prototype=new Ci;JD.prototype.constructor=JD;function KD(){}KD.prototype=JD.prototype;function LD(){this.kh=this.io=null}LD.prototype=new oD;LD.prototype.constructor=LD;function Bp(a){var b=new LD;b.io=a;(new lB).b();nD.prototype.Ru.call(b);b.kh="";return b}
function gp(a,b){for(;""!==b;){var c=b.indexOf("\n")|0;if(0>c)a.kh=""+a.kh+b,b="";else{var e=""+a.kh+b.substring(0,c);g.console&&(a.io&&g.console.error?g.console.error(e):g.console.log(e));a.kh="";b=b.substring(1+c|0)}}}LD.prototype.$i=function(){};LD.prototype.$classData=l({iv:0},!1,"java.lang.JSConsoleBasedPrintStream",{iv:1,Vz:1,Uz:1,Ps:1,c:1,xk:1,oo:1,wm:1,no:1});function MD(){this.Kb=null}MD.prototype=new n;MD.prototype.constructor=MD;function ND(){}ND.prototype=MD.prototype;
MD.prototype.n=function(a){return this===a};MD.prototype.m=function(){return this.Kb};MD.prototype.r=function(){return Fa(this)};function OD(){this.oc=null}OD.prototype=new n;OD.prototype.constructor=OD;function PD(){}PD.prototype=OD.prototype;function QD(){this.Ja=this.Ka=null}QD.prototype=new uD;QD.prototype.constructor=QD;QD.prototype.b=function(){Zw.prototype.b.call(this);RD=this;this.Ja=(new qz).b();return this};QD.prototype.da=function(){pm();Uk();return(new ql).b()};
QD.prototype.$classData=l({Yw:0},!1,"scala.collection.IndexedSeq$",{Yw:1,jp:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1});var RD=void 0;function Vk(){RD||(RD=(new QD).b());return RD}function Uh(){this.He=this.ff=0;this.Oa=null}Uh.prototype=new EA;Uh.prototype.constructor=Uh;Uh.prototype.t=function(){this.He>=this.ff&&bq().Y.t();var a=this.Oa.Xa(this.He);this.He=1+this.He|0;return a};function Th(a,b,c,e){a.ff=e;if(null===b)throw np(Xh(),null);a.Oa=b;a.He=c;return a}
Uh.prototype.H=function(){return this.He<this.ff};Uh.prototype.Sk=function(a){return 0>=a?Th(new Uh,this.Oa,this.He,this.ff):(this.He+a|0)>=this.ff?Th(new Uh,this.Oa,this.ff,this.ff):Th(new Uh,this.Oa,this.He+a|0,this.ff)};Uh.prototype.$classData=l({$w:0},!1,"scala.collection.IndexedSeqLike$Elements",{$w:1,ac:1,c:1,Hb:1,K:1,J:1,kB:1,k:1,h:1});function SD(){}SD.prototype=new GB;SD.prototype.constructor=SD;SD.prototype.b=function(){return this};
function TD(a,b,c,e,f,h){var m=31&(b>>>h|0),q=31&(e>>>h|0);if(m!==q)return a=1<<m|1<<q,b=k(r(UD),[2]),m<q?(b.e[0]=c,b.e[1]=f):(b.e[0]=f,b.e[1]=c),VD(new WD,a,b,c.L()+f.L()|0);q=k(r(UD),[1]);m=1<<m;c=TD(a,b,c,e,f,5+h|0);q.e[0]=c;return VD(new WD,m,q,c.Qe)}SD.prototype.yg=function(){return XD()};SD.prototype.$classData=l({Dx:0},!1,"scala.collection.immutable.HashSet$",{Dx:1,ip:1,kp:1,gp:1,bc:1,c:1,cc:1,k:1,h:1});var YD=void 0;function ZD(){YD||(YD=(new SD).b());return YD}
function $D(){this.Ka=null}$D.prototype=new uD;$D.prototype.constructor=$D;$D.prototype.b=function(){Zw.prototype.b.call(this);return this};$D.prototype.da=function(){Uk();return(new ql).b()};$D.prototype.$classData=l({Jx:0},!1,"scala.collection.immutable.IndexedSeq$",{Jx:1,jp:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1});var aE=void 0;function pm(){aE||(aE=(new $D).b());return aE}function bE(){}bE.prototype=new GB;bE.prototype.constructor=bE;bE.prototype.b=function(){return this};bE.prototype.yg=function(){return cE()};
bE.prototype.$classData=l({Rx:0},!1,"scala.collection.immutable.ListSet$",{Rx:1,ip:1,kp:1,gp:1,bc:1,c:1,cc:1,k:1,h:1});var dE=void 0;function Ot(){this.gf=this.ma=null}Ot.prototype=new mA;Ot.prototype.constructor=Ot;d=Ot.prototype;d.A=function(){return"JavaScriptException"};d.y=function(){return 1};d.jj=function(){this.stackdata=this.gf;return this};d.n=function(a){return this===a?!0:Nt(a)?D(G(),this.gf,a.gf):!1};d.z=function(a){switch(a){case 0:return this.gf;default:throw(new Y).i(""+a);}};
d.Pc=function(){return ka(this.gf)};d.o=function(a){this.gf=a;ww.prototype.Sa.call(this,null,0,0,!0);return this};d.r=function(){return um(U(),this)};d.B=function(){return Z(this)};function Nt(a){return!!(a&&a.$classData&&a.$classData.p.Ep)}d.$classData=l({Ep:0},!1,"scala.scalajs.js.JavaScriptException",{Ep:1,ib:1,mb:1,Ua:1,c:1,h:1,C:1,q:1,k:1});function Zf(){this.yl=this.Oo=this.I=null}Zf.prototype=new n;Zf.prototype.constructor=Zf;
Zf.prototype.m=function(){return We(me(),"ComponentDidUpdate(props: "+this.Oo+" \u2192 "+pb(this).rd()+", state: "+this.yl+" \u2192 "+pb(this).Cc()+")")};Zf.prototype.Gh=function(a,b,c){this.I=a;this.Oo=b;this.yl=c;return this};Zf.prototype.vb=function(){return this.I};Zf.prototype.$classData=l({Yq:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidUpdate",{Yq:1,c:1,bh:1,Xe:1,ve:1,ue:1,te:1,re:1,se:1,Mi:1});function bf(){this.I=null}bf.prototype=new n;bf.prototype.constructor=bf;
d=bf.prototype;d.n=function(a){return Ze().Nf(this.I,a)};d.m=function(){return Ze().hg(this.I)};d.Ra=function(a){this.I=a;return this};d.r=function(){return Aa(this.I)};d.vb=function(){return this.I};d.$classData=l({fm:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillMount",{fm:1,c:1,Ni:1,bh:1,Xe:1,ve:1,ue:1,te:1,re:1,se:1});function eE(){JD.call(this)}eE.prototype=new KD;eE.prototype.constructor=eE;eE.prototype.b=function(){Bi.prototype.b.call(this);Gb(this);Ri(this);return this};
eE.prototype.$classData=l({rs:0},!1,"japgolly.scalajs.react.vdom.PackageBase$",{rs:1,qs:1,fs:1,c:1,js:1,ks:1,ls:1,ms:1,os:1,ns:1});var fE=void 0;function Xz(){fE||(fE=(new eE).b())}function gE(){JD.call(this)}gE.prototype=new KD;gE.prototype.constructor=gE;gE.prototype.b=function(){Bi.prototype.b.call(this);Gb(this);Ri(this);hE=this;Fu||(Fu=(new Eu).b());Wz||(Wz=(new Vz).b());return this};
gE.prototype.$classData=l({Es:0},!1,"japgolly.scalajs.react.vdom.html_$less$up$",{Es:1,qs:1,fs:1,c:1,js:1,ks:1,ls:1,ms:1,os:1,ns:1});var hE=void 0;function sc(){hE||(hE=(new gE).b());return hE}function Wq(){this.Kb=null}Wq.prototype=new ND;Wq.prototype.constructor=Wq;Wq.prototype.b=function(){this.Kb="Boolean";return this};Wq.prototype.$classData=l({ww:0},!1,"scala.reflect.ManifestFactory$BooleanManifest$",{ww:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Vq=void 0;function Iq(){this.Kb=null}
Iq.prototype=new ND;Iq.prototype.constructor=Iq;Iq.prototype.b=function(){this.Kb="Byte";return this};Iq.prototype.$classData=l({xw:0},!1,"scala.reflect.ManifestFactory$ByteManifest$",{xw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Hq=void 0;function Mq(){this.Kb=null}Mq.prototype=new ND;Mq.prototype.constructor=Mq;Mq.prototype.b=function(){this.Kb="Char";return this};
Mq.prototype.$classData=l({yw:0},!1,"scala.reflect.ManifestFactory$CharManifest$",{yw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Lq=void 0;function Uq(){this.Kb=null}Uq.prototype=new ND;Uq.prototype.constructor=Uq;Uq.prototype.b=function(){this.Kb="Double";return this};Uq.prototype.$classData=l({zw:0},!1,"scala.reflect.ManifestFactory$DoubleManifest$",{zw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Tq=void 0;function Sq(){this.Kb=null}Sq.prototype=new ND;Sq.prototype.constructor=Sq;
Sq.prototype.b=function(){this.Kb="Float";return this};Sq.prototype.$classData=l({Aw:0},!1,"scala.reflect.ManifestFactory$FloatManifest$",{Aw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Rq=void 0;function Oq(){this.Kb=null}Oq.prototype=new ND;Oq.prototype.constructor=Oq;Oq.prototype.b=function(){this.Kb="Int";return this};Oq.prototype.$classData=l({Bw:0},!1,"scala.reflect.ManifestFactory$IntManifest$",{Bw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Nq=void 0;function Qq(){this.Kb=null}
Qq.prototype=new ND;Qq.prototype.constructor=Qq;Qq.prototype.b=function(){this.Kb="Long";return this};Qq.prototype.$classData=l({Cw:0},!1,"scala.reflect.ManifestFactory$LongManifest$",{Cw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Pq=void 0;function iE(){this.Wg=this.oc=null}iE.prototype=new PD;iE.prototype.constructor=iE;function jE(){}jE.prototype=iE.prototype;iE.prototype.n=function(a){return this===a};iE.prototype.m=function(){return this.Wg};iE.prototype.r=function(){return Fa(this)};
function Kq(){this.Kb=null}Kq.prototype=new ND;Kq.prototype.constructor=Kq;Kq.prototype.b=function(){this.Kb="Short";return this};Kq.prototype.$classData=l({Gw:0},!1,"scala.reflect.ManifestFactory$ShortManifest$",{Gw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Jq=void 0;function Yq(){this.Kb=null}Yq.prototype=new ND;Yq.prototype.constructor=Yq;Yq.prototype.b=function(){this.Kb="Unit";return this};
Yq.prototype.$classData=l({Hw:0},!1,"scala.reflect.ManifestFactory$UnitManifest$",{Hw:1,vf:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Xq=void 0;function kE(a,b){a=a.E();for(b=b.E();a.H()&&b.H();)if(!D(G(),a.t(),b.t()))return!1;return!a.H()&&!b.H()}function lE(){this.Ka=null}lE.prototype=new bD;lE.prototype.constructor=lE;lE.prototype.b=function(){Zw.prototype.b.call(this);mE=this;(new ix).b();return this};lE.prototype.vh=function(){return xb()};lE.prototype.da=function(){return(new ig).b()};
lE.prototype.$classData=l({Lx:0},!1,"scala.collection.immutable.List$",{Lx:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1,k:1,h:1});var mE=void 0;function hg(){mE||(mE=(new lE).b());return mE}function nE(){this.Ka=null}nE.prototype=new bD;nE.prototype.constructor=nE;nE.prototype.b=function(){Zw.prototype.b.call(this);return this};nE.prototype.vh=function(){return Rw()};nE.prototype.da=function(){return(new hD).b()};
nE.prototype.$classData=l({ly:0},!1,"scala.collection.immutable.Stream$",{ly:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1,k:1,h:1});var oE=void 0;function iq(){oE||(oE=(new nE).b());return oE}function pE(){this.Ka=null}pE.prototype=new bD;pE.prototype.constructor=pE;pE.prototype.b=function(){Zw.prototype.b.call(this);return this};pE.prototype.da=function(){return(new YB).b()};pE.prototype.$classData=l({By:0},!1,"scala.collection.mutable.ArrayBuffer$",{By:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1,k:1,h:1});
var qE=void 0;function rE(){this.Ka=null}rE.prototype=new bD;rE.prototype.constructor=rE;rE.prototype.b=function(){Zw.prototype.b.call(this);return this};rE.prototype.da=function(){return JA(new IA,(new ig).b())};rE.prototype.$classData=l({Ny:0},!1,"scala.collection.mutable.ListBuffer$",{Ny:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1,k:1,h:1});var sE=void 0;function tE(){this.Jd=this.Vh=this.pl=this.be=null}tE.prototype=new n;tE.prototype.constructor=tE;function uE(){}d=uE.prototype=tE.prototype;
d.Vp=function(a){return this.Wh(this.pl,this.Vh,(new Jh).pj(this.Jd.be,a.bk))};d.rd=function(){return this.Jd.gh(v(new w,function(a){return function(){return a.pl.g(a.be.rd())}}(this)))};d.Zh=function(a,b){return this.Jd.gh(v(new w,function(a,b,f){return function(){a.be.Zh(a.Vh.pd.g(b),f)}}(this,a,b)))};d.Ug=function(a,b){return this.Jd.gh(v(new w,function(a,b,f){return function(){a.be.Zh(a.Vh.Tg.g(b),f)}}(this,a,b)))};d.bo=function(a,b,c,e){this.be=a;this.pl=b;this.Vh=c;this.Jd=e;return this};
d.Cc=function(){return this.Jd.gh(v(new w,function(a){return function(){return a.Vh.Bg.g(a.be.Cc())}}(this)))};function vE(){this.Jd=null}vE.prototype=new n;vE.prototype.constructor=vE;function wE(){}wE.prototype=vE.prototype;vE.prototype.Vp=function(a){var b=vb().Kd,c=ci();return this.Wh(b,c.Vf,(new Jh).pj(this.Jd.be,a.bk))};vE.prototype.co=function(a){this.Jd=a;return this};function xE(){this.Mn=this.I=null}xE.prototype=new n;xE.prototype.constructor=xE;
xE.prototype.m=function(){return We(me(),"ComponentDidCatch("+this.Mn+")")};function Yf(a,b){var c=new xE;c.I=a;c.Mn=b;return c}xE.prototype.vb=function(){return this.I};xE.prototype.$classData=l({Wq:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidCatch",{Wq:1,c:1,Ni:1,bh:1,Xe:1,ve:1,ue:1,te:1,re:1,se:1,Mi:1});function Ve(){this.I=null}Ve.prototype=new n;Ve.prototype.constructor=Ve;d=Ve.prototype;d.n=function(a){return Ue().Nf(this.I,a)};d.m=function(){return Ue().hg(this.I)};
d.Ra=function(a){this.I=a;return this};d.Ug=function(a,b){return(new Bb).oa(this.vb().mountedPure.Ug(a,b).$)};d.r=function(){return Aa(this.I)};d.vb=function(){return this.I};d.$classData=l({em:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidMount",{em:1,c:1,Ni:1,bh:1,Xe:1,ve:1,ue:1,te:1,re:1,se:1,Mi:1});function yE(){this.pf=this.I=null}yE.prototype=new n;yE.prototype.constructor=yE;function ag(a,b){var c=new yE;c.I=a;c.pf=b;return c}
yE.prototype.m=function(){return We(me(),"ComponentWillReceiveProps(props: "+pb(this).rd()+" \u2192 "+this.pf+", state: "+pb(this).Cc()+")")};yE.prototype.vb=function(){return this.I};yE.prototype.$classData=l({$q:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillReceiveProps",{$q:1,c:1,Ni:1,bh:1,Xe:1,ve:1,ue:1,te:1,re:1,se:1,Mi:1});function Ke(){this.I=null}Ke.prototype=new n;Ke.prototype.constructor=Ke;d=Ke.prototype;d.n=function(a){return jf().Nf(this.I,a)};d.m=function(){return jf().hg(this.I)};
d.Ra=function(a){this.I=a;return this};d.r=function(){return Aa(this.I)};d.vb=function(){return this.I};d.$classData=l({hm:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$RenderScope",{hm:1,c:1,Ni:1,bh:1,Xe:1,ve:1,ue:1,te:1,re:1,se:1,Mi:1});function $q(){iE.call(this)}$q.prototype=new jE;$q.prototype.constructor=$q;$q.prototype.b=function(){this.Wg="Any";var a=B();xb();this.oc=a;ma(p);return this};
$q.prototype.$classData=l({uw:0},!1,"scala.reflect.ManifestFactory$AnyManifest$",{uw:1,Sj:1,Rj:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var Zq=void 0;function dr(){iE.call(this)}dr.prototype=new jE;dr.prototype.constructor=dr;dr.prototype.b=function(){this.Wg="AnyVal";var a=B();xb();this.oc=a;ma(p);return this};dr.prototype.$classData=l({vw:0},!1,"scala.reflect.ManifestFactory$AnyValManifest$",{vw:1,Sj:1,Rj:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var cr=void 0;function fr(){iE.call(this)}
fr.prototype=new jE;fr.prototype.constructor=fr;fr.prototype.b=function(){this.Wg="Nothing";var a=B();xb();this.oc=a;ma(Fz);return this};fr.prototype.$classData=l({Dw:0},!1,"scala.reflect.ManifestFactory$NothingManifest$",{Dw:1,Sj:1,Rj:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var er=void 0;function hr(){iE.call(this)}hr.prototype=new jE;hr.prototype.constructor=hr;hr.prototype.b=function(){this.Wg="Null";var a=B();xb();this.oc=a;ma(Wt);return this};
hr.prototype.$classData=l({Ew:0},!1,"scala.reflect.ManifestFactory$NullManifest$",{Ew:1,Sj:1,Rj:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var gr=void 0;function br(){iE.call(this)}br.prototype=new jE;br.prototype.constructor=br;br.prototype.b=function(){this.Wg="Object";var a=B();xb();this.oc=a;ma(p);return this};br.prototype.$classData=l({Fw:0},!1,"scala.reflect.ManifestFactory$ObjectManifest$",{Fw:1,Sj:1,Rj:1,c:1,Xc:1,Wc:1,Vc:1,Ic:1,k:1,h:1,q:1});var ar=void 0;function zE(){this.Vb=this.Ka=null}
zE.prototype=new uD;zE.prototype.constructor=zE;zE.prototype.b=function(){Zw.prototype.b.call(this);AE=this;this.Vb=(new kD).vc(0,0,0);return this};zE.prototype.vh=function(){return this.Vb};zE.prototype.da=function(){return(new ql).b()};zE.prototype.$classData=l({uy:0},!1,"scala.collection.immutable.Vector$",{uy:1,jp:1,je:1,ie:1,zc:1,bc:1,c:1,Ac:1,cc:1,k:1,h:1});var AE=void 0;function Uk(){AE||(AE=(new zE).b());return AE}function BE(){}BE.prototype=new n;BE.prototype.constructor=BE;
function CE(){}d=CE.prototype=BE.prototype;d.Pf=function(a,b){return XC(this,a,b)};d.Yh=function(a,b,c){return Qj(this,a,b,c)};d.m=function(){return Qm(this)};d.kf=function(a,b){return ns(this,a,b)};d.Af=function(){Uk();var a=Vk().Ja;return Wk(this,a)};d.L=function(){return os(this)};d.ke=function(){return-1};d.u=function(){return ZC(this)};d.Ye=function(a,b,c,e){return ps(this,a,b,c,e)};d.tf=function(){return this};d.We=function(a,b){return this.kf(a,b)};
d.Pp=function(){var a=fz(),a=Yw(a);return Wk(this,a)};d.Kh=function(){return!0};d.da=function(){return this.ob().da()};d.Dd=function(){return $C(this)};function Tj(a){return 0===a.fe(0)}function Tf(){this.yc=this.Jd=null}Tf.prototype=new wE;Tf.prototype.constructor=Tf;d=Tf.prototype;d.nl=function(a,b){this.yc.setState(function(a){return function(b){return a.g(b)}}(a),nd(rb(),b))};d.Wh=function(a,b,c){ue();return DE(this,a,b,c)};d.rd=function(){return this.yc.props};d.Zh=function(a,b){this.nl(a,b)};
d.Ug=function(a,b){this.yc.setState(a,nd(rb(),b))};d.Ra=function(a){vE.prototype.co.call(this,Uf().Xk);this.yc=a;return this};d.Cc=function(){return this.yc.state};d.vb=function(){return this.yc};d.$classData=l({tq:0},!1,"japgolly.scalajs.react.component.Js$$anon$1",{tq:1,Oq:1,c:1,pk:1,ok:1,nk:1,mk:1,ue:1,te:1,re:1,ve:1,se:1,yq:1,xq:1});function EE(){tE.call(this);this.Tn=this.yc=null}EE.prototype=new uE;EE.prototype.constructor=EE;EE.prototype.Wh=function(a,b,c){ue();return DE(this.Tn,a,b,c)};
function DE(a,b,c,e){var f=new EE;f.Tn=a;tE.prototype.bo.call(f,a,b,c,e);f.yc=a.vb();return f}EE.prototype.vb=function(){return this.yc};EE.prototype.$classData=l({uq:0},!1,"japgolly.scalajs.react.component.Js$$anon$2",{uq:1,Nq:1,c:1,pk:1,ok:1,nk:1,mk:1,ue:1,te:1,re:1,ve:1,se:1,yq:1,xq:1});function FE(){this.Fi=this.yc=this.rj=this.Jd=null}FE.prototype=new wE;FE.prototype.constructor=FE;d=FE.prototype;
d.nl=function(a,b){this.Fi.Zh(t(new u,function(a,b){return function(a){se();return{a:b.g(a.a)}}}(this,a)),b)};d.Wh=function(a,b,c){ne();return GE(this,a,b,c)};d.rd=function(){return this.Fi.rd().a};d.Zh=function(a,b){this.nl(a,b)};d.Ug=function(a,b){this.Fi.Ug((se(),{a:a}),b)};d.Cc=function(){return this.Fi.Cc().a};function we(a){var b=new FE;b.Fi=a;vE.prototype.co.call(b,Uf().Xk);b.rj=a;b.yc=a.vb();return b}d.vb=function(){return this.yc};
d.$classData=l({Iq:0},!1,"japgolly.scalajs.react.component.Scala$$anon$1",{Iq:1,Oq:1,c:1,pk:1,ok:1,nk:1,mk:1,ue:1,te:1,re:1,ve:1,se:1,Lq:1,Kq:1});function HE(){tE.call(this);this.Sn=this.yc=this.rj=null}HE.prototype=new uE;HE.prototype.constructor=HE;function GE(a,b,c,e){var f=new HE;f.Sn=a;tE.prototype.bo.call(f,a,b,c,e);f.rj=a.rj;f.yc=a.vb();return f}HE.prototype.Wh=function(a,b,c){ne();return GE(this.Sn,a,b,c)};HE.prototype.vb=function(){return this.yc};
HE.prototype.$classData=l({Jq:0},!1,"japgolly.scalajs.react.component.Scala$$anon$2",{Jq:1,Nq:1,c:1,pk:1,ok:1,nk:1,mk:1,ue:1,te:1,re:1,ve:1,se:1,Lq:1,Kq:1});function IE(a){return JE(a)?Th(new Uh,a,0,a.W()).t():a.Xa(0)}function KE(a,b){return a.W()-b|0}function LE(a,b){for(var c=0;c<a.W()&&!1===!!b.g(a.Xa(c));)c=1+c|0;return c!==a.W()}
function ME(a,b){if(b&&b.$classData&&b.$classData.p.li){var c=a.W();if(c===b.W()){for(var e=0;e<c&&D(G(),a.Xa(e),b.Xa(e));)e=1+e|0;return e===c}return!1}return kE(a,b)}function JE(a){return 0===a.W()}function NE(a,b){for(var c=0,e=a.W();c<e;)b.g(a.Xa(c)),c=1+c|0}function OE(a,b,c){c=0<c?c:0;for(var e=a.W(),f=c;;){if(f<e)var h=a.Xa(f),h=!b.g(h);else h=!1;if(h)f=1+f|0;else break}b=c+(f-c|0)|0;return b>=a.W()?-1:b}
function PE(a,b,c){b=0<b?b:0;c=0<c?c:0;var e=a.W();c=c<e?c:e;var e=c-b|0,f=0<e?e:0,e=a.da();for(e.qc(f);b<c;)e.rb(a.Xa(b)),b=1+b|0;return e.La()}function QE(a,b,c,e){var f=0;for(;;){if(f===b)return c;var h=1+f|0;c=fg(e,c,a.Xa(f));f=h}}function RE(a){return JE(a)?ZC(a):a.Vg(1,a.W())}function SE(a,b,c,e){var f=0,h=c,m=a.W();e=m<e?m:e;c=Yt(P(),b)-c|0;for(c=e<c?e:c;f<c;)$t(P(),b,h,a.Xa(f)),f=1+f|0,h=1+h|0}
function TE(a,b){if(b&&b.$classData&&b.$classData.p.ni){if(a===b)return!0;for(;!a.l()&&!b.l()&&D(G(),a.D(),b.D());)a=a.u(),b=b.u();return a.l()&&b.l()}return kE(a,b)}function UE(a,b){for(;!a.l();){if(b.g(a.D()))return!0;a=a.u()}return!1}function VE(a,b){a=a.Tk(b);if(0>b||a.l())throw(new Y).i(""+b);return a.D()}function WE(a,b,c){var e=0<c?c:0;for(a=a.Tk(c);!a.l();){if(b.g(a.D()))return e;e=1+e|0;a=a.u()}return-1}function XE(a,b,c){for(;!a.l();)b=fg(c,b,a.D()),a=a.u();return b}
function Ju(a){for(var b=0;!a.l();)b=1+b|0,a=a.u();return b}function YE(a){if(a.l())throw(new yw).b();for(var b=a.u();!b.l();)a=b,b=b.u();return a.D()}function ZE(a,b){return 0<=b&&0<(0>b?1:$E(a,b))}function $E(a,b){var c=0;for(;;){if(c===b)return a.l()?0:1;if(a.l())return-1;c=1+c|0;a=a.u()}}function aF(a,b){return b.Ga().We(a,Fb(function(){return function(a,b){return a.hd(b)}}(a)))}function bF(a){throw(new yw).i("key not found: "+a);}
function cF(a,b,c,e,f){var h=a.E();a=(new Vh).lb(h,t(new u,function(){return function(a){if(null!==a){var b=a.N;a=a.P;vi||(vi=(new wi).b());return""+(""+b+" -\x3e ")+a}throw(new x).o(a);}}(a)));return ps(a,b,c,e,f)}function dF(a,b,c){return a.Dg(b,v(new w,function(a,b,c){return function(){return c.g(b)}}(a,b,c)))}function eF(a,b,c){b=0<b?b:0;var e=a.W(),e=c<e?c:e;if(b>=e)return a.da().La();c=a.da();a=a.m().substring(b,e);return c.fb((new ih).i(a)).La()}function fF(){}fF.prototype=new CE;
fF.prototype.constructor=fF;function gF(){}d=gF.prototype=fF.prototype;d.Ga=function(){return this.Bb()};d.D=function(){return this.E().t()};d.Se=function(){return this};d.Ue=function(){return this.E()};d.nc=function(a){for(var b=this.E(),c=!1;!c&&b.H();)c=!!a.g(b.t());return c};d.Nd=function(a){return kE(this,a)};d.db=function(){return this.Se()};d.Bb=function(){return this};d.l=function(){return!this.E().H()};d.ob=function(){return $p()};d.Ch=function(a){var b=this.E();return Mw(b,a)};
d.O=function(a){var b=this.E();tp(b,a)};d.kc=function(){return this.E().kc()};d.ae=function(a){var b=this.da();kx(b,this,-(0>a?0:a)|0);for(var c=0,e=this.E();c<a&&e.H();)e.t(),c=1+c|0;return b.fb(e).La()};d.Kf=function(a,b,c){var e=b;b=b+c|0;c=Yt(P(),a);b=b<c?b:c;for(c=this.E();e<b&&c.H();)$t(P(),a,e,c.t()),e=1+e|0};var QB=l({Fa:0},!0,"scala.collection.immutable.Iterable",{Fa:1,Ia:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,Ha:1,ra:1,pa:1,fa:1,ha:1,q:1});function ih(){this.cb=null}
ih.prototype=new n;ih.prototype.constructor=ih;d=ih.prototype;d.Ga=function(){return(new Js).i(this.cb)};d.D=function(){return IE(this)};d.Xa=function(a){a=65535&(this.cb.charCodeAt(a)|0);return nt(a)};d.fe=function(a){return KE(this,a)};d.Ue=function(){return Th(new Uh,this,0,this.cb.length|0)};d.Nd=function(a){return ME(this,a)};d.nc=function(a){return LE(this,a)};d.l=function(){return JE(this)};d.db=function(){return(new Js).i(this.cb)};d.Pf=function(a,b){return XC(this,a,b)};
d.n=function(a){Fs();return a&&a.$classData&&a.$classData.p.tp?this.cb===(null===a?null:a.cb):!1};d.Yh=function(a,b,c){return Qj(this,a,b,c)};d.m=function(){return this.cb};d.O=function(a){NE(this,a)};d.ce=function(a,b){return OE(this,a,b)};d.Vg=function(a,b){return Ds(Fs(),this.cb,a,b)};d.Af=function(){Uk();var a=Vk().Ja;return Wk(this,a)};d.L=function(){return this.cb.length|0};d.E=function(){return Th(new Uh,this,0,this.cb.length|0)};d.W=function(){return this.cb.length|0};
d.ke=function(){return this.cb.length|0};d.kc=function(){var a=Th(new Uh,this,0,this.cb.length|0);return Pw(a)};d.ae=function(a){var b=this.cb.length|0;return Ds(Fs(),this.cb,a,b)};d.Ud=function(){return(new Js).i(this.cb)};d.u=function(){return RE(this)};d.Ye=function(a,b,c,e){return ps(this,a,b,c,e)};d.tf=function(){return this.cb};d.We=function(a,b){return QE(this,this.cb.length|0,a,b)};d.Kf=function(a,b,c){SE(this,a,b,c)};d.Kh=function(){return!0};d.r=function(){var a=this.cb;return Ba(Ca(),a)};
d.i=function(a){this.cb=a;return this};d.da=function(){return(new ms).b()};d.Dd=function(){return $C(this)};d.$classData=l({tp:0},!1,"scala.collection.immutable.StringOps",{tp:1,c:1,sp:1,mi:1,wf:1,xd:1,ha:1,q:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,fa:1,vd:1,$o:1,md:1});function ol(){this.Ii=null}ol.prototype=new gF;ol.prototype.constructor=ol;d=ol.prototype;d.O=function(a){var b=this.Ii.Sl();tp(b,a)};d.L=function(){return this.Ii.L()};d.E=function(){return this.Ii.Sl()};
d.Yf=function(a){if(null===a)throw np(Xh(),null);this.Ii=a;return this};d.$classData=l({lx:0},!1,"scala.collection.MapLike$DefaultValuesIterable",{lx:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,k:1,h:1});var Iu=l({Od:0},!0,"scala.collection.Seq",{Od:1,Ta:1,T:1,ra:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,pa:1,fa:1,ha:1,q:1,ud:1,vd:1,xd:1});function hF(a){var b=a.da();b.fb(a);return b.La()}function Sh(){this.Ib=null}Sh.prototype=new n;
Sh.prototype.constructor=Sh;d=Sh.prototype;d.Ga=function(){return(new A).f(this.Ib)};d.b=function(){Sh.prototype.f.call(this,[]);return this};d.D=function(){return IE(this)};d.Xa=function(a){return this.Ib[a]};d.fe=function(a){return KE(this,a)};d.Ue=function(){return Th(new Uh,this,0,this.Ib.length|0)};d.Nd=function(a){return ME(this,a)};d.nc=function(a){return LE(this,a)};d.l=function(){return JE(this)};d.db=function(){return(new A).f(this.Ib)};d.n=function(a){return Pm(this,a)};
d.Pf=function(a,b){return XC(this,a,b)};d.Yh=function(a,b,c){return Qj(this,a,b,c)};d.rc=function(a){this.Ib.push(a);return this};d.m=function(){return Qm(this)};d.O=function(a){NE(this,a)};d.ce=function(a,b){return OE(this,a,b)};d.Vg=function(a,b){return PE(this,a,b)};d.Af=function(){Uk();var a=Vk().Ja;return Wk(this,a)};d.L=function(){return this.Ib.length|0};d.La=function(){return this.Ib};d.E=function(){return Th(new Uh,this,0,this.Ib.length|0)};d.fd=function(a,b){lx(this,a,b)};
d.W=function(){return this.Ib.length|0};d.ke=function(){return this.Ib.length|0};d.kc=function(){var a=Th(new Uh,this,0,this.Ib.length|0);return Pw(a)};d.ae=function(a){return PE(this,a,this.Ib.length|0)};d.Ud=function(){return(new A).f(this.Ib)};d.u=function(){return RE(this)};d.Ye=function(a,b,c,e){return ps(this,a,b,c,e)};d.tf=function(){return this.Ib};d.We=function(a,b){return QE(this,this.Ib.length|0,a,b)};d.rb=function(a){this.Ib.push(a);return this};d.Kf=function(a,b,c){SE(this,a,b,c)};
d.qc=function(){};d.r=function(){return Kw(U(),(new A).f(this.Ib))};d.Kh=function(){return!0};d.f=function(a){this.Ib=a;return this};d.da=function(){return(new Sh).b()};d.fb=function(a){return Tl(this,a)};d.Dd=function(){return $C(this)};d.$classData=l({Uy:0},!1,"scala.scalajs.js.ArrayOps",{Uy:1,c:1,Cy:1,Bp:1,Hl:1,wf:1,xd:1,ha:1,q:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,fa:1,vd:1,mi:1,Sd:1,Qd:1,Pd:1});function iF(){}iF.prototype=new gF;iF.prototype.constructor=iF;function jF(){}d=jF.prototype=iF.prototype;
d.$n=function(a,b){return uB(this,a,b)};d.l=function(){return Tj(this)};d.n=function(a){return Pm(this,a)};d.m=function(){return Qm(this)};d.ce=function(a,b){a:{var c=0<b?b:0;for(b=this.E().Sk(b);b.H();){if(a.g(b.t())){a=c;break a}c=1+c|0}a=-1}return a};d.L=function(){return this.W()};d.Ud=function(){return this};d.Zn=function(a){return this.$n(a,0)};d.Ze=function(a,b){return Np(this,a,b)};d.r=function(){return Kw(U(),this.yf())};d.Gd=function(a){return Ez(this,a)};function kF(){}kF.prototype=new gF;
kF.prototype.constructor=kF;function lF(){}d=lF.prototype=kF.prototype;d.g=function(a){var b=this.yb(a);if(B()===b)a=bF(a);else if(Qf(b))a=b.F;else throw(new x).o(b);return a};d.l=function(){return 0===this.L()};d.n=function(a){return TA(this,a)};d.Dg=function(a,b){a=this.yb(a);if(Qf(a))b=a.F;else if(B()===a)b=b.U();else throw(new x).o(a);return b};d.m=function(){return Qm(this)};d.il=function(){return(new BB).Yf(this)};d.Sl=function(){return(new CB).Yf(this)};d.ca=function(a){return!this.yb(a).l()};
d.Ye=function(a,b,c,e){return cF(this,a,b,c,e)};d.Gc=function(a){return this.ca(a)};d.r=function(){var a=U();return ds(a,this.ui(),a.vo)};d.Ze=function(a,b){return dF(this,a,b)};d.Gd=function(a){return Ez(this,a)};d.da=function(){return jl(new kl,this.zg())};d.Dd=function(){return"Map"};function mF(){}mF.prototype=new gF;mF.prototype.constructor=mF;function nF(){}d=nF.prototype=mF.prototype;d.l=function(){return 0===this.L()};
d.n=function(a){if(a&&a.$classData&&a.$classData.p.$c){var b;if(!(b=this===a)&&(b=this.L()===a.L()))try{b=this.Kp(a)}catch(c){if(c&&c.$classData&&c.$classData.p.cv)b=!1;else throw c;}a=b}else a=!1;return a};d.m=function(){return Qm(this)};d.Kp=function(a){return this.Ch(a)};d.r=function(){var a=U();return ds(a,this,a.Dp)};d.Gd=function(a){return nb(this,a)};d.kg=function(a){return aF(this,a)};d.da=function(){return R(new S,this.ef())};d.Dd=function(){return"Set"};
function oF(){this.dc=this.ei=null}oF.prototype=new lF;oF.prototype.constructor=oF;function pF(){}d=pF.prototype=oF.prototype;d.O=function(a){this.dc.O(t(new u,function(a,c){return function(e){return a.ei.g(e.N)?c.g(e):void 0}}(this,a)))};d.Vu=function(a,b){this.ei=b;if(null===a)throw np(Xh(),null);this.dc=a;return this};d.E=function(){var a=this.dc.E();return(new xi).lb(a,t(new u,function(a){return function(c){return!!a.ei.g(c.N)}}(this)))};d.yb=function(a){return this.ei.g(a)?this.dc.yb(a):B()};
d.ca=function(a){return!!this.ei.g(a)&&this.dc.ca(a)};function qF(a,b){var c=jl(new kl,ll());Tl(c,a);ml(c,(new H).d(b.N,b.P));return c.w}function rF(a,b){var c=jl(new kl,ll());Uw(new Tw,a,t(new u,function(a,b){return function(a){return!D(G(),a.N,b)}}(a,b))).O(t(new u,function(a,b){return function(a){return b.rb(a)}}(a,c)));return c.w}function sF(){this.dc=null}sF.prototype=new nF;sF.prototype.constructor=sF;function tF(){}d=tF.prototype=sF.prototype;d.O=function(a){var b=this.dc.il();tp(b,a)};
d.L=function(){return this.dc.L()};d.E=function(){return this.dc.il()};d.Yf=function(a){if(null===a)throw np(Xh(),null);this.dc=a;return this};d.ca=function(a){return this.dc.ca(a)};function uF(){}uF.prototype=new lF;uF.prototype.constructor=uF;function vF(){}d=vF.prototype=uF.prototype;d.Ga=function(){return this};d.pc=function(){return this};d.Bb=function(){return this};d.db=function(){return this};d.ob=function(){return xB()};d.zg=function(){return this.Uk()};d.Uk=function(){return ll()};
d.ui=function(){return this};function wF(){}wF.prototype=new nF;wF.prototype.constructor=wF;function TL(){}d=TL.prototype=wF.prototype;d.Ga=function(){return this};d.pc=function(){return this};d.Mg=function(){throw(new yw).i("next of empty set");};d.g=function(a){return this.ca(a)};d.nb=function(a){return this.Hi(a)};d.l=function(){return!0};d.Bb=function(){return this};d.db=function(){return this};d.lg=function(a){return UL(new VL,this,a)};d.ob=function(){dE||(dE=(new bE).b());return dE};d.L=function(){return 0};
d.E=function(){var a=WL(this);return AB(a)};d.oe=function(a){return this.Hi(a)};d.ef=function(){return cE()};function WL(a){for(var b=xb();!a.l();){var c=a.uh(),b=zb(new Ab,c,b);a=a.Mg()}return b}d.uh=function(){throw(new yw).i("elem of empty set");};d.ca=function(){return!1};function XL(a,b){return b.l()?a:b.We(a,Fb(function(){return function(a,b){return a.lg(b)}}(a)))}d.Hi=function(){return this};d.hd=function(a){return this.lg(a)};d.kg=function(a){return XL(this,a)};d.Dd=function(){return"ListSet"};
function YL(){}YL.prototype=new nF;YL.prototype.constructor=YL;d=YL.prototype;d.Ga=function(){return this};d.pc=function(){return this};d.b=function(){return this};d.g=function(){return!1};d.nb=function(){return this};d.Bb=function(){return this};d.db=function(){return this};d.ob=function(){return fz()};d.O=function(){};d.L=function(){return 0};d.E=function(){return bq().Y};d.oe=function(){return this};d.ef=function(){return Q()};d.ca=function(){return!1};d.hd=function(a){return(new ZL).o(a)};
d.$classData=l({fy:0},!1,"scala.collection.immutable.Set$EmptySet$",{fy:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});var $L=void 0;function Q(){$L||($L=(new YL).b());return $L}function ZL(){this.X=null}ZL.prototype=new nF;ZL.prototype.constructor=ZL;d=ZL.prototype;d.Ga=function(){return this};d.pc=function(){return this};d.D=function(){return this.X};d.g=function(a){return this.ca(a)};
d.nc=function(a){return!!a.g(this.X)};d.nb=function(a){return this.Dc(a)};d.Bb=function(){return this};d.db=function(){return this};d.Ch=function(a){return!!a.g(this.X)};d.ob=function(){return fz()};d.O=function(a){a.g(this.X)};d.L=function(){return 1};d.E=function(){bq();var a=(new A).f([this.X]);return Th(new Uh,a,0,a.na.length|0)};d.o=function(a){this.X=a;return this};d.oe=function(a){return this.Dc(a)};d.ef=function(){return Q()};d.pe=function(a){return this.ca(a)?this:(new aM).d(this.X,a)};
d.ca=function(a){return D(G(),a,this.X)};d.u=function(){return Q()};d.hd=function(a){return this.pe(a)};d.Dc=function(a){return D(G(),a,this.X)?Q():this};d.$classData=l({gy:0},!1,"scala.collection.immutable.Set$Set1",{gy:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});function aM(){this.za=this.X=null}aM.prototype=new nF;aM.prototype.constructor=aM;d=aM.prototype;d.Ga=function(){return this};
d.pc=function(){return this};d.D=function(){return this.X};d.g=function(a){return this.ca(a)};d.nc=function(a){return!!a.g(this.X)||!!a.g(this.za)};d.nb=function(a){return this.Dc(a)};d.yi=function(){return(new ZL).o(this.za)};d.Bb=function(){return this};d.db=function(){return this};d.d=function(a,b){this.X=a;this.za=b;return this};d.Ch=function(a){return!!a.g(this.X)&&!!a.g(this.za)};d.ob=function(){return fz()};d.O=function(a){a.g(this.X);a.g(this.za)};d.L=function(){return 2};
d.E=function(){bq();var a=(new A).f([this.X,this.za]);return Th(new Uh,a,0,a.na.length|0)};d.oe=function(a){return this.Dc(a)};d.ef=function(){return Q()};d.pe=function(a){return this.ca(a)?this:(new bM).nf(this.X,this.za,a)};d.ca=function(a){return D(G(),a,this.X)||D(G(),a,this.za)};d.u=function(){return this.yi()};d.hd=function(a){return this.pe(a)};d.Dc=function(a){return D(G(),a,this.X)?(new ZL).o(this.za):D(G(),a,this.za)?(new ZL).o(this.X):this};
d.$classData=l({hy:0},!1,"scala.collection.immutable.Set$Set2",{hy:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});function bM(){this.hb=this.za=this.X=null}bM.prototype=new nF;bM.prototype.constructor=bM;d=bM.prototype;d.Ga=function(){return this};d.pc=function(){return this};d.D=function(){return this.X};d.g=function(a){return this.ca(a)};
d.nc=function(a){return!!a.g(this.X)||!!a.g(this.za)||!!a.g(this.hb)};d.nb=function(a){return this.Dc(a)};d.yi=function(){return(new aM).d(this.za,this.hb)};d.Bb=function(){return this};d.db=function(){return this};d.Ch=function(a){return!!a.g(this.X)&&!!a.g(this.za)&&!!a.g(this.hb)};d.ob=function(){return fz()};d.O=function(a){a.g(this.X);a.g(this.za);a.g(this.hb)};d.L=function(){return 3};d.nf=function(a,b,c){this.X=a;this.za=b;this.hb=c;return this};
d.E=function(){bq();var a=(new A).f([this.X,this.za,this.hb]);return Th(new Uh,a,0,a.na.length|0)};d.oe=function(a){return this.Dc(a)};d.ef=function(){return Q()};d.pe=function(a){return this.ca(a)?this:(new cM).of(this.X,this.za,this.hb,a)};d.ca=function(a){return D(G(),a,this.X)||D(G(),a,this.za)||D(G(),a,this.hb)};d.u=function(){return this.yi()};d.hd=function(a){return this.pe(a)};
d.Dc=function(a){return D(G(),a,this.X)?(new aM).d(this.za,this.hb):D(G(),a,this.za)?(new aM).d(this.X,this.hb):D(G(),a,this.hb)?(new aM).d(this.X,this.za):this};d.$classData=l({iy:0},!1,"scala.collection.immutable.Set$Set3",{iy:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});function cM(){this.kd=this.hb=this.za=this.X=null}cM.prototype=new nF;cM.prototype.constructor=cM;d=cM.prototype;
d.Ga=function(){return this};d.pc=function(){return this};d.D=function(){return this.X};d.g=function(a){return this.ca(a)};d.nc=function(a){return!!a.g(this.X)||!!a.g(this.za)||!!a.g(this.hb)||!!a.g(this.kd)};d.nb=function(a){return this.Dc(a)};d.yi=function(){return(new bM).nf(this.za,this.hb,this.kd)};d.Bb=function(){return this};d.db=function(){return this};d.Ch=function(a){return!!a.g(this.X)&&!!a.g(this.za)&&!!a.g(this.hb)&&!!a.g(this.kd)};d.ob=function(){return fz()};
d.O=function(a){a.g(this.X);a.g(this.za);a.g(this.hb);a.g(this.kd)};d.L=function(){return 4};d.E=function(){bq();var a=(new A).f([this.X,this.za,this.hb,this.kd]);return Th(new Uh,a,0,a.na.length|0)};d.oe=function(a){return this.Dc(a)};d.ef=function(){return Q()};d.pe=function(a){return this.ca(a)?this:dM(dM(dM(dM(dM((new eM).b(),this.X),this.za),this.hb),this.kd),a)};d.ca=function(a){return D(G(),a,this.X)||D(G(),a,this.za)||D(G(),a,this.hb)||D(G(),a,this.kd)};d.u=function(){return this.yi()};
d.of=function(a,b,c,e){this.X=a;this.za=b;this.hb=c;this.kd=e;return this};d.hd=function(a){return this.pe(a)};d.Dc=function(a){return D(G(),a,this.X)?(new bM).nf(this.za,this.hb,this.kd):D(G(),a,this.za)?(new bM).nf(this.X,this.hb,this.kd):D(G(),a,this.hb)?(new bM).nf(this.X,this.za,this.kd):D(G(),a,this.kd)?(new bM).nf(this.X,this.za,this.hb):this};
d.$classData=l({jy:0},!1,"scala.collection.immutable.Set$Set4",{jy:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});function eM(){}eM.prototype=new nF;eM.prototype.constructor=eM;function fM(){}d=fM.prototype=eM.prototype;d.Ci=function(a,b){return gM(new hM,a,b)};d.Ee=function(a){return this.bl(ki(li(),a))};d.Ga=function(){return this};d.pc=function(){return this};d.b=function(){return this};
d.g=function(a){return this.ca(a)};d.nb=function(a){return iM(this,a)};function dM(a,b){return a.Ci(b,a.Ee(b),0)}d.Bb=function(){return this};d.db=function(){return this};d.ob=function(){return ZD()};d.O=function(){};d.Kp=function(a){if(a&&a.$classData&&a.$classData.p.Rg)return this.xi(a,0);var b=this.E();return Mw(b,a)};d.L=function(){return 0};d.E=function(){return bq().Y};d.oe=function(a){return iM(this,a)};d.ef=function(){return XD()};d.ji=function(){return this};
function iM(a,b){a=a.ji(b,a.Ee(b),0);return null===a?XD():a}d.bl=function(a){a=a+~(a<<9)|0;a^=a>>>14|0;a=a+(a<<4)|0;return a^(a>>>10|0)};d.u=function(){return this.Rl()};d.ca=function(a){return this.mf(a,this.Ee(a),0)};d.Rl=function(){return iM(this,this.D())};d.hd=function(a){return dM(this,a)};d.mf=function(){return!1};d.xi=function(){return!0};
var UD=l({Rg:0},!1,"scala.collection.immutable.HashSet",{Rg:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,td:1,k:1,h:1});eM.prototype.$classData=UD;function jM(){}jM.prototype=new TL;jM.prototype.constructor=jM;jM.prototype.b=function(){return this};
jM.prototype.$classData=l({Sx:0},!1,"scala.collection.immutable.ListSet$EmptyListSet$",{Sx:1,Qx:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});var kM=void 0;function cE(){kM||(kM=(new jM).b());return kM}function VL(){this.Wl=this.Fn=null}VL.prototype=new TL;VL.prototype.constructor=VL;d=VL.prototype;d.Mg=function(){return this.Wl};d.nb=function(a){return lM(a,this)};d.l=function(){return!1};
d.lg=function(a){return mM(this,a)?this:UL(new VL,this,a)};d.L=function(){a:{var a=this,b=0;for(;;){if(a.l())break a;a=a.Mg();b=1+b|0}}return b};function lM(a,b){var c=xb();for(;;){if(b.l())return YE(c);if(D(G(),a,b.uh())){b=b.Mg();for(a=c;!a.l();)c=a.D(),b=UL(new VL,b,c.uh()),a=a.u();return b}var e=b.Mg(),c=zb(new Ab,b,c);b=e}}d.oe=function(a){return lM(a,this)};function UL(a,b,c){a.Fn=c;if(null===b)throw np(Xh(),null);a.Wl=b;return a}d.ca=function(a){return mM(this,a)};d.uh=function(){return this.Fn};
d.Hi=function(a){return lM(a,this)};function mM(a,b){for(;;){if(a.l())return!1;if(D(G(),a.uh(),b))return!0;a=a.Mg()}}d.hd=function(a){return this.lg(a)};d.$classData=l({Tx:0},!1,"scala.collection.immutable.ListSet$Node",{Tx:1,Qx:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,k:1,h:1});function nM(){this.dc=null}nM.prototype=new tF;nM.prototype.constructor=nM;d=nM.prototype;d.Ga=function(){return this};
d.pc=function(){return this};d.g=function(a){return this.dc.ca(a)};d.nb=function(a){return this.Dc(a)};d.Bb=function(){return this};d.db=function(){return this};d.ob=function(){return fz()};function WA(a){var b=new nM;sF.prototype.Yf.call(b,a);return b}d.oe=function(a){return this.Dc(a)};d.ef=function(){return Q()};d.pe=function(a){return this.dc.ca(a)?this:K(fz(),xb()).kg(this).hd(a)};d.hd=function(a){return this.pe(a)};d.Dc=function(a){return this.dc.ca(a)?K(fz(),xb()).kg(this).oe(a):this};
d.$classData=l({ay:0},!1,"scala.collection.immutable.MapLike$ImmutableDefaultKeySet",{ay:1,mB:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,k:1,h:1,Cd:1,Fa:1,Ia:1,Ha:1});function oM(){}oM.prototype=new jF;oM.prototype.constructor=oM;function pM(){}pM.prototype=oM.prototype;oM.prototype.Ga=function(){return this.vi()};oM.prototype.Bb=function(){return this.vi()};oM.prototype.vi=function(){return this};function qM(){}
qM.prototype=new fM;qM.prototype.constructor=qM;d=qM.prototype;d.b=function(){return this};d.D=function(){throw(new yw).i("Empty Set");};d.u=function(){return this.Rl()};d.Rl=function(){throw(new yw).i("Empty Set");};d.$classData=l({Ex:0},!1,"scala.collection.immutable.HashSet$EmptyHashSet$",{Ex:1,Rg:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,td:1,k:1,h:1});var rM=void 0;
function XD(){rM||(rM=(new qM).b());return rM}function WD(){this.Ec=0;this.$a=null;this.Qe=0}WD.prototype=new fM;WD.prototype.constructor=WD;d=WD.prototype;
d.Ci=function(a,b,c){var e=1<<(31&(b>>>c|0)),f=Vy(Ns(),this.Ec&(-1+e|0));if(0!==(this.Ec&e)){e=this.$a.e[f];a=e.Ci(a,b,5+c|0);if(e===a)return this;b=k(r(UD),[this.$a.e.length]);wA(yA(),this.$a,0,b,0,this.$a.e.length);b.e[f]=a;return VD(new WD,this.Ec,b,this.Qe+(a.L()-e.L()|0)|0)}c=k(r(UD),[1+this.$a.e.length|0]);wA(yA(),this.$a,0,c,0,f);c.e[f]=gM(new hM,a,b);wA(yA(),this.$a,f,c,1+f|0,this.$a.e.length-f|0);return VD(new WD,this.Ec|e,c,1+this.Qe|0)};
d.O=function(a){for(var b=0;b<this.$a.e.length;)this.$a.e[b].O(a),b=1+b|0};d.L=function(){return this.Qe};d.E=function(){var a=new eD;LB.prototype.ao.call(a,this.$a);return a};
d.ji=function(a,b,c){var e=1<<(31&(b>>>c|0)),f=Vy(Ns(),this.Ec&(-1+e|0));if(0!==(this.Ec&e)){var h=this.$a.e[f];a=h.ji(a,b,5+c|0);return h===a?this:null===a?(e^=this.Ec,0!==e?(a=k(r(UD),[-1+this.$a.e.length|0]),wA(yA(),this.$a,0,a,0,f),wA(yA(),this.$a,1+f|0,a,f,-1+(this.$a.e.length-f|0)|0),f=this.Qe-h.L()|0,1!==a.e.length||OB(a.e[0])?VD(new WD,e,a,f):a.e[0]):null):1!==this.$a.e.length||OB(a)?(e=k(r(UD),[this.$a.e.length]),wA(yA(),this.$a,0,e,0,this.$a.e.length),e.e[f]=a,f=this.Qe+(a.L()-h.L()|0)|
0,VD(new WD,this.Ec,e,f)):a}return this};function VD(a,b,c,e){a.Ec=b;a.$a=c;a.Qe=e;I();if(Vy(Ns(),b)!==c.e.length)throw(new kA).o("assertion failed");return a}d.mf=function(a,b,c){var e=31&(b>>>c|0),f=1<<e;return-1===this.Ec?this.$a.e[31&e].mf(a,b,5+c|0):0!==(this.Ec&f)?(e=Vy(Ns(),this.Ec&(-1+f|0)),this.$a.e[e].mf(a,b,5+c|0)):!1};
d.xi=function(a,b){if(a===this)return!0;if(OB(a)&&this.Qe<=a.Qe){var c=this.Ec,e=this.$a,f=0,h=a.$a;a=a.Ec;var m=0;if((c&a)===c){for(;0!==c;){var q=c^c&(-1+c|0),y=a^a&(-1+a|0);if(q===y){if(!e.e[f].xi(h.e[m],5+b|0))return!1;c&=~q;f=1+f|0}a&=~y;m=1+m|0}return!0}}return!1};function OB(a){return!!(a&&a.$classData&&a.$classData.p.op)}
d.$classData=l({op:0},!1,"scala.collection.immutable.HashSet$HashTrieSet",{op:1,Rg:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,td:1,k:1,h:1});function sM(){}sM.prototype=new fM;sM.prototype.constructor=sM;function tM(){}tM.prototype=sM.prototype;function uM(){}uM.prototype=new vF;uM.prototype.constructor=uM;function vM(){}d=vM.prototype=uM.prototype;d.pc=function(){return this};d.nb=function(a){return this.Gi(a)};
d.Ei=function(){throw(new yw).i("value of empty map");};d.db=function(){return this};d.l=function(){return!0};d.qe=function(a){return this.mg(a)};d.zg=function(){return wM()};d.ne=function(a){return this.Gi(a)};d.Uk=function(){return wM()};d.ui=function(){return this};d.L=function(){return 0};d.mg=function(a){return xM(new yM,this,a.N,a.P)};d.E=function(){var a=zM(this);return AB(a)};d.bg=function(){throw(new yw).i("key of empty map");};d.qb=function(a,b){return this.Di(a,b)};
d.Di=function(a,b){return xM(new yM,this,a,b)};d.Gi=function(){return this};d.yb=function(){return B()};function zM(a){for(var b=xb();!a.l();){var c=(new H).d(a.bg(),a.Ei()),b=zb(new Ab,c,b);a=a.qf()}return b}d.qf=function(){throw(new yw).i("next of empty map");};d.Wd=function(a){return this.mg(a)};d.Dd=function(){return"ListMap"};function AM(){}AM.prototype=new vF;AM.prototype.constructor=AM;d=AM.prototype;d.b=function(){return this};d.g=function(a){this.Dk(a)};d.nb=function(){return this};
d.Dg=function(a,b){return b.U()};d.qe=function(a){return(new ek).d(a.N,a.P)};d.ne=function(){return this};d.L=function(){return 0};d.E=function(){return bq().Y};d.qb=function(a,b){return(new ek).d(a,b)};d.yb=function(){return B()};d.ca=function(){return!1};d.Dk=function(a){throw(new yw).i("key not found: "+a);};d.Wd=function(a){return(new ek).d(a.N,a.P)};
d.$classData=l({Vx:0},!1,"scala.collection.immutable.Map$EmptyMap$",{Vx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});var BM=void 0;function ll(){BM||(BM=(new AM).b());return BM}function ek(){this.ba=this.M=null}ek.prototype=new vF;ek.prototype.constructor=ek;d=ek.prototype;d.g=function(a){if(D(G(),a,this.M))return this.ba;throw(new yw).i("key not found: "+a);};d.nb=function(a){return this.Fd(a)};
d.Dg=function(a,b){return D(G(),a,this.M)?this.ba:b.U()};d.d=function(a,b){this.M=a;this.ba=b;return this};d.qe=function(a){return this.qb(a.N,a.P)};d.O=function(a){a.g((new H).d(this.M,this.ba))};d.ne=function(a){return this.Fd(a)};d.L=function(){return 1};d.E=function(){bq();var a=(new A).f([(new H).d(this.M,this.ba)]);return Th(new Uh,a,0,a.na.length|0)};d.qb=function(a,b){return D(G(),a,this.M)?(new ek).d(this.M,b):(new CM).of(this.M,this.ba,a,b)};
d.yb=function(a){return D(G(),a,this.M)?(new Pf).o(this.ba):B()};d.ca=function(a){return D(G(),a,this.M)};d.Fd=function(a){return D(G(),a,this.M)?ll():this};d.Wd=function(a){return this.qb(a.N,a.P)};d.$classData=l({Wx:0},!1,"scala.collection.immutable.Map$Map1",{Wx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});function CM(){this.Ba=this.V=this.ba=this.M=null}CM.prototype=new vF;
CM.prototype.constructor=CM;d=CM.prototype;d.g=function(a){if(D(G(),a,this.M))return this.ba;if(D(G(),a,this.V))return this.Ba;throw(new yw).i("key not found: "+a);};d.nb=function(a){return this.Fd(a)};d.Dg=function(a,b){return D(G(),a,this.M)?this.ba:D(G(),a,this.V)?this.Ba:b.U()};d.qe=function(a){return this.qb(a.N,a.P)};d.O=function(a){a.g((new H).d(this.M,this.ba));a.g((new H).d(this.V,this.Ba))};d.ne=function(a){return this.Fd(a)};d.L=function(){return 2};
d.E=function(){bq();var a=(new A).f([(new H).d(this.M,this.ba),(new H).d(this.V,this.Ba)]);return Th(new Uh,a,0,a.na.length|0)};d.qb=function(a,b){return D(G(),a,this.M)?(new CM).of(this.M,b,this.V,this.Ba):D(G(),a,this.V)?(new CM).of(this.M,this.ba,this.V,b):DM(this.M,this.ba,this.V,this.Ba,a,b)};d.yb=function(a){return D(G(),a,this.M)?(new Pf).o(this.ba):D(G(),a,this.V)?(new Pf).o(this.Ba):B()};d.ca=function(a){return D(G(),a,this.M)||D(G(),a,this.V)};
d.of=function(a,b,c,e){this.M=a;this.ba=b;this.V=c;this.Ba=e;return this};d.Fd=function(a){return D(G(),a,this.M)?(new ek).d(this.V,this.Ba):D(G(),a,this.V)?(new ek).d(this.M,this.ba):this};d.Wd=function(a){return this.qb(a.N,a.P)};d.$classData=l({Xx:0},!1,"scala.collection.immutable.Map$Map2",{Xx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});
function EM(){this.eb=this.Da=this.Ba=this.V=this.ba=this.M=null}EM.prototype=new vF;EM.prototype.constructor=EM;d=EM.prototype;d.g=function(a){if(D(G(),a,this.M))return this.ba;if(D(G(),a,this.V))return this.Ba;if(D(G(),a,this.Da))return this.eb;throw(new yw).i("key not found: "+a);};d.nb=function(a){return this.Fd(a)};d.Dg=function(a,b){return D(G(),a,this.M)?this.ba:D(G(),a,this.V)?this.Ba:D(G(),a,this.Da)?this.eb:b.U()};d.qe=function(a){return this.qb(a.N,a.P)};
d.O=function(a){a.g((new H).d(this.M,this.ba));a.g((new H).d(this.V,this.Ba));a.g((new H).d(this.Da,this.eb))};d.ne=function(a){return this.Fd(a)};function DM(a,b,c,e,f,h){var m=new EM;m.M=a;m.ba=b;m.V=c;m.Ba=e;m.Da=f;m.eb=h;return m}d.L=function(){return 3};d.E=function(){bq();var a=(new A).f([(new H).d(this.M,this.ba),(new H).d(this.V,this.Ba),(new H).d(this.Da,this.eb)]);return Th(new Uh,a,0,a.na.length|0)};
d.qb=function(a,b){return D(G(),a,this.M)?DM(this.M,b,this.V,this.Ba,this.Da,this.eb):D(G(),a,this.V)?DM(this.M,this.ba,this.V,b,this.Da,this.eb):D(G(),a,this.Da)?DM(this.M,this.ba,this.V,this.Ba,this.Da,b):(new FM).Wf(this.M,this.ba,this.V,this.Ba,this.Da,this.eb,a,b)};d.yb=function(a){return D(G(),a,this.M)?(new Pf).o(this.ba):D(G(),a,this.V)?(new Pf).o(this.Ba):D(G(),a,this.Da)?(new Pf).o(this.eb):B()};d.ca=function(a){return D(G(),a,this.M)||D(G(),a,this.V)||D(G(),a,this.Da)};
d.Fd=function(a){return D(G(),a,this.M)?(new CM).of(this.V,this.Ba,this.Da,this.eb):D(G(),a,this.V)?(new CM).of(this.M,this.ba,this.Da,this.eb):D(G(),a,this.Da)?(new CM).of(this.M,this.ba,this.V,this.Ba):this};d.Wd=function(a){return this.qb(a.N,a.P)};d.$classData=l({Yx:0},!1,"scala.collection.immutable.Map$Map3",{Yx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});
function FM(){this.gd=this.jc=this.eb=this.Da=this.Ba=this.V=this.ba=this.M=null}FM.prototype=new vF;FM.prototype.constructor=FM;d=FM.prototype;d.g=function(a){if(D(G(),a,this.M))return this.ba;if(D(G(),a,this.V))return this.Ba;if(D(G(),a,this.Da))return this.eb;if(D(G(),a,this.jc))return this.gd;throw(new yw).i("key not found: "+a);};d.nb=function(a){return this.Fd(a)};d.Dg=function(a,b){return D(G(),a,this.M)?this.ba:D(G(),a,this.V)?this.Ba:D(G(),a,this.Da)?this.eb:D(G(),a,this.jc)?this.gd:b.U()};
d.qe=function(a){return this.qb(a.N,a.P)};d.O=function(a){a.g((new H).d(this.M,this.ba));a.g((new H).d(this.V,this.Ba));a.g((new H).d(this.Da,this.eb));a.g((new H).d(this.jc,this.gd))};d.ne=function(a){return this.Fd(a)};d.L=function(){return 4};d.E=function(){bq();var a=(new A).f([(new H).d(this.M,this.ba),(new H).d(this.V,this.Ba),(new H).d(this.Da,this.eb),(new H).d(this.jc,this.gd)]);return Th(new Uh,a,0,a.na.length|0)};
d.qb=function(a,b){return D(G(),a,this.M)?(new FM).Wf(this.M,b,this.V,this.Ba,this.Da,this.eb,this.jc,this.gd):D(G(),a,this.V)?(new FM).Wf(this.M,this.ba,this.V,b,this.Da,this.eb,this.jc,this.gd):D(G(),a,this.Da)?(new FM).Wf(this.M,this.ba,this.V,this.Ba,this.Da,b,this.jc,this.gd):D(G(),a,this.jc)?(new FM).Wf(this.M,this.ba,this.V,this.Ba,this.Da,this.eb,this.jc,b):GM(GM(GM(GM(GM((new HM).b(),this.M,this.ba),this.V,this.Ba),this.Da,this.eb),this.jc,this.gd),a,b)};
d.Wf=function(a,b,c,e,f,h,m,q){this.M=a;this.ba=b;this.V=c;this.Ba=e;this.Da=f;this.eb=h;this.jc=m;this.gd=q;return this};d.yb=function(a){return D(G(),a,this.M)?(new Pf).o(this.ba):D(G(),a,this.V)?(new Pf).o(this.Ba):D(G(),a,this.Da)?(new Pf).o(this.eb):D(G(),a,this.jc)?(new Pf).o(this.gd):B()};d.ca=function(a){return D(G(),a,this.M)||D(G(),a,this.V)||D(G(),a,this.Da)||D(G(),a,this.jc)};
d.Fd=function(a){return D(G(),a,this.M)?DM(this.V,this.Ba,this.Da,this.eb,this.jc,this.gd):D(G(),a,this.V)?DM(this.M,this.ba,this.Da,this.eb,this.jc,this.gd):D(G(),a,this.Da)?DM(this.M,this.ba,this.V,this.Ba,this.jc,this.gd):D(G(),a,this.jc)?DM(this.M,this.ba,this.V,this.Ba,this.Da,this.eb):this};d.Wd=function(a){return this.qb(a.N,a.P)};
d.$classData=l({Zx:0},!1,"scala.collection.immutable.Map$Map4",{Zx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});function IM(){oF.call(this)}IM.prototype=new pF;IM.prototype.constructor=IM;d=IM.prototype;d.Ga=function(){return this};function ak(a,b){var c=new IM;oF.prototype.Vu.call(c,a,b);return c}d.pc=function(){return this};d.nb=function(a){return rF(this,a)};d.Bb=function(){return this};
d.db=function(){return this};d.qe=function(a){return qF(this,a)};d.ob=function(){return xB()};d.zg=function(){return ll()};d.ne=function(a){return rF(this,a)};d.ui=function(){return this};d.qb=function(a,b){return this.qe((new H).d(a,b))};d.Wd=function(a){return qF(this,a)};
d.$classData=l({$x:0},!1,"scala.collection.immutable.MapLike$$anon$1",{$x:1,nB:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,lB:1,sB:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1});function HM(){}HM.prototype=new vF;HM.prototype.constructor=HM;function JM(){}d=JM.prototype=HM.prototype;d.Ee=function(a){return this.bl(ki(li(),a))};d.Ga=function(){return this};d.pc=function(){return this};d.b=function(){return this};
d.nb=function(a){return KM(this,a)};d.db=function(){return this};d.Xg=function(a,b,c,e,f){return LM(a,b,e,f)};d.Cg=function(){return B()};d.qe=function(a){return MM(this,a)};function MM(a,b){return a.Xg(b.N,a.Ee(b.N),0,b.P,b,null)}d.O=function(){};d.zg=function(){CD();return BD()};function GM(a,b,c){return a.Xg(b,a.Ee(b),0,c,null,null)}d.ne=function(a){return KM(this,a)};d.ii=function(){return this};function KM(a,b){return a.ii(b,a.Ee(b),0)}d.Uk=function(){CD();return BD()};d.L=function(){return 0};
d.ui=function(){return this};d.E=function(){return bq().Y};d.qb=function(a,b){return GM(this,a,b)};d.Ql=function(){return KM(this,this.D().N)};d.bl=function(a){a=a+~(a<<9)|0;a^=a>>>14|0;a=a+(a<<4)|0;return a^(a>>>10|0)};d.yb=function(a){return this.Cg(a,this.Ee(a),0)};d.xg=function(){return!1};d.ca=function(a){return this.xg(a,this.Ee(a),0)};d.u=function(){return this.Ql()};d.Wd=function(a){return MM(this,a)};
var yD=l({oi:0},!1,"scala.collection.immutable.HashMap",{oi:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1,td:1});HM.prototype.$classData=yD;function hM(){this.Ab=null;this.Ma=0}hM.prototype=new tM;hM.prototype.constructor=hM;d=hM.prototype;
d.Ci=function(a,b,c){if(b===this.Ma&&D(G(),a,this.Ab))return this;if(b!==this.Ma)return TD(ZD(),this.Ma,this,b,gM(new hM,a,b),c);c=cE();return NM(new OM,b,UL(new VL,c,this.Ab).lg(a))};function gM(a,b,c){a.Ab=b;a.Ma=c;return a}d.O=function(a){a.g(this.Ab)};d.E=function(){bq();var a=(new A).f([this.Ab]);return Th(new Uh,a,0,a.na.length|0)};d.L=function(){return 1};d.ji=function(a,b){return b===this.Ma&&D(G(),a,this.Ab)?null:this};d.mf=function(a,b){return b===this.Ma&&D(G(),a,this.Ab)};
d.xi=function(a,b){return a.mf(this.Ab,this.Ma,b)};d.$classData=l({np:0},!1,"scala.collection.immutable.HashSet$HashSet1",{np:1,Hx:1,Rg:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,td:1,k:1,h:1});function OM(){this.Ma=0;this.Ke=null}OM.prototype=new tM;OM.prototype.constructor=OM;d=OM.prototype;
d.Ci=function(a,b,c){return b===this.Ma?NM(new OM,b,this.Ke.lg(a)):TD(ZD(),this.Ma,this,b,gM(new hM,a,b),c)};d.O=function(a){var b=WL(this.Ke);tp(AB(b),a)};d.E=function(){var a=WL(this.Ke);return AB(a)};d.L=function(){return this.Ke.L()};d.ji=function(a,b){if(b===this.Ma){a=this.Ke.Hi(a);var c=a.L();switch(c){case 0:return null;case 1:return a=WL(a),gM(new hM,AB(a).t(),b);default:return c===this.Ke.L()?this:NM(new OM,b,a)}}else return this};function NM(a,b,c){a.Ma=b;a.Ke=c;return a}
d.mf=function(a,b){return b===this.Ma&&this.Ke.ca(a)};d.xi=function(a,b){for(var c=WL(this.Ke),c=AB(c),e=!0;e&&c.H();)e=c.t(),e=a.mf(e,this.Ma,b);return e};d.$classData=l({Fx:0},!1,"scala.collection.immutable.HashSet$HashSetCollision1",{Fx:1,Hx:1,Rg:1,sd:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,cd:1,T:1,$c:1,wd:1,zd:1,yd:1,Na:1,Cd:1,Fa:1,Ia:1,Ha:1,td:1,k:1,h:1});function PM(){}PM.prototype=new vM;PM.prototype.constructor=PM;PM.prototype.b=function(){return this};
PM.prototype.$classData=l({Ox:0},!1,"scala.collection.immutable.ListMap$EmptyListMap$",{Ox:1,Nx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});var QM=void 0;function wM(){QM||(QM=(new PM).b());return QM}function yM(){this.Xl=this.Bf=this.Ab=null}yM.prototype=new vM;yM.prototype.constructor=yM;
function RM(a,b){var c=xb();for(;;){if(b.l())return YE(c);if(D(G(),a,b.bg())){b=b.qf();for(a=c;!a.l();)c=a.D(),b=xM(new yM,b,c.bg(),c.Ei()),a=a.u();return b}var e=b.qf(),c=zb(new Ab,b,c);b=e}}d=yM.prototype;d.g=function(a){a:{var b=this;for(;;){if(b.l())throw(new yw).i("key not found: "+a);if(D(G(),a,b.bg())){a=b.Ei();break a}b=b.qf()}}return a};d.Ei=function(){return this.Bf};d.nb=function(a){return RM(a,this)};d.l=function(){return!1};d.qe=function(a){return this.mg(a)};
d.ne=function(a){return RM(a,this)};d.L=function(){a:{var a=this,b=0;for(;;){if(a.l())break a;a=a.qf();b=1+b|0}}return b};d.bg=function(){return this.Ab};d.mg=function(a){var b=RM(a.N,this);return xM(new yM,b,a.N,a.P)};d.qb=function(a,b){return this.Di(a,b)};d.Di=function(a,b){var c=RM(a,this);return xM(new yM,c,a,b)};d.Gi=function(a){return RM(a,this)};d.yb=function(a){a:{var b=this;for(;;){if(b.l()){a=B();break a}if(D(G(),a,b.bg())){a=(new Pf).o(b.Ei());break a}b=b.qf()}}return a};
d.ca=function(a){a:{var b=this;for(;;){if(b.l()){a=!1;break a}if(D(G(),a,b.bg())){a=!0;break a}b=b.qf()}}return a};function xM(a,b,c,e){a.Ab=c;a.Bf=e;if(null===b)throw np(Xh(),null);a.Xl=b;return a}d.qf=function(){return this.Xl};d.Wd=function(a){return this.mg(a)};
d.$classData=l({Px:0},!1,"scala.collection.immutable.ListMap$Node",{Px:1,Nx:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1});function SM(){}SM.prototype=new jF;SM.prototype.constructor=SM;function TM(){}d=TM.prototype=SM.prototype;d.Ga=function(){return this};d.Xa=function(a){return VE(this,a)};d.Se=function(){return this};d.fe=function(a){return 0>a?1:$E(this,a)};
d.Nd=function(a){return TE(this,a)};d.g=function(a){return VE(this,a|0)};d.nc=function(a){return UE(this,a)};d.Bb=function(){return this};d.db=function(){return this};d.Pf=function(a,b){if(jD(b.$e(this))){if(this.l())a=Rw();else{b=(new Sf).o(this);for(var c=a.g(b.x.D()).kc();!b.x.l()&&c.l();)b.x=b.x.u(),b.x.l()||(c=a.g(b.x.D()).kc());a=b.x.l()?(iq(),Rw()):UM(c,v(new w,function(a,b,c){return function(){return c.x.u().Pf(b,(iq(),(new vz).b()))}}(this,a,b)))}return a}return XC(this,a,b)};
d.n=function(a){return this===a||Pm(this,a)};d.Tk=function(a){return VM(this,a)};d.Yh=function(a,b,c){var e=this,f=this;for(e.l()||(e=e.u());f!==e&&!e.l();){e=e.u();if(e.l())break;e=e.u();if(e===f)break;f=f.u()}return Qj(this,a,b,c)};d.ob=function(){return iq()};d.m=function(){return Qj(this,"Stream(",", ",")")};d.O=function(a){var b=this;a:for(;;){if(!b.l()){a.g(b.D());b=b.u();continue a}break}};d.kf=function(a,b){var c=this;for(;;){if(c.l())return a;var e=c.u();a=fg(b,a,c.D());c=e}};
d.ce=function(a,b){return WE(this,a,b)};d.E=function(){return KB(this)};d.W=function(){for(var a=0,b=this;!b.l();)a=1+a|0,b=b.u();return a};d.yf=function(){return this};d.kc=function(){return this};d.ae=function(a){return VM(this,a)};function VM(a,b){for(;;){if(0>=b||a.l())return a;a=a.u();b=-1+b|0}}d.Ud=function(){return this};
d.Ye=function(a,b,c,e){jn(a,b);if(!this.l()){rs(a,this.D());b=this;if(b.Re()){var f=this.u();if(f.l())return jn(a,e),a;if(b!==f&&(b=f,f.Re()))for(f=f.u();b!==f&&f.Re();)rs(jn(a,c),b.D()),b=b.u(),f=f.u(),f.Re()&&(f=f.u());if(f.Re()){for(var h=this,m=0;h!==f;)h=h.u(),f=f.u(),m=1+m|0;b===f&&0<m&&(rs(jn(a,c),b.D()),b=b.u());for(;b!==f;)rs(jn(a,c),b.D()),b=b.u()}else{for(;b!==f;)rs(jn(a,c),b.D()),b=b.u();!b.l()&&rs(jn(a,c),b.D())}}b.l()||(b.Re()?jn(jn(a,c),"..."):jn(jn(a,c),"?"))}jn(a,e);return a};
d.Gc=function(a){return ZE(this,a|0)};d.r=function(){return Kw(U(),this)};function UM(a,b){if(a.l())return b.U().kc();var c=a.D();return(new Qw).Hh(c,v(new w,function(a,b){return function(){return UM(a.u(),b)}}(a,b)))}d.Dd=function(){return"Stream"};function WM(a,b){if(b>=a.Aa)throw(new Y).i(""+b);return a.na.e[b]}
function XM(a,b){var c=a.na.e.length,e=c>>31,f=b>>31;if(f===e?(-2147483648^b)>(-2147483648^c):f>e){f=c<<1;for(c=c>>>31|0|e<<1;;){var e=b>>31,h=f,m=c;if(e===m?(-2147483648^b)>(-2147483648^h):e>m)c=f>>>31|0|c<<1,f<<=1;else break}b=c;if(0===b?-1<(-2147483648^f):0<b)f=2147483647;b=f;b=k(r(p),[b]);Na(a.na,0,b,0,a.Aa);a.na=b}}function YM(){}YM.prototype=new JM;YM.prototype.constructor=YM;d=YM.prototype;d.b=function(){return this};d.D=function(){throw(new yw).i("Empty Map");};
d.Ql=function(){throw(new yw).i("Empty Map");};d.u=function(){return this.Ql()};d.$classData=l({zx:0},!1,"scala.collection.immutable.HashMap$EmptyHashMap$",{zx:1,oi:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1,td:1});var ZM=void 0;function BD(){ZM||(ZM=(new YM).b());return ZM}function $M(){this.Ab=null;this.Ma=0;this.sj=this.Bf=null}$M.prototype=new JM;$M.prototype.constructor=$M;
function dD(a){null===a.sj&&(a.sj=(new H).d(a.Ab,a.Bf));return a.sj}function LM(a,b,c,e){var f=new $M;f.Ab=a;f.Ma=b;f.Bf=c;f.sj=e;return f}d=$M.prototype;d.Xg=function(a,b,c,e,f,h){if(b===this.Ma&&D(G(),a,this.Ab)){if(null===h)return this.Bf===e?this:LM(a,b,e,f);a=h.Ek(dD(this),null!==f?f:(new H).d(a,e));return LM(a.N,b,a.P,a)}if(b!==this.Ma)return a=LM(a,b,e,f),xD(CD(),this.Ma,this,b,a,c,2);c=wM();return aN(new bN,b,xM(new yM,c,this.Ab,this.Bf).Di(a,e))};
d.Cg=function(a,b){return b===this.Ma&&D(G(),a,this.Ab)?(new Pf).o(this.Bf):B()};d.O=function(a){a.g(dD(this))};d.ii=function(a,b){return b===this.Ma&&D(G(),a,this.Ab)?(CD(),BD()):this};d.L=function(){return 1};d.E=function(){bq();var a=[dD(this)],a=(new A).f(a);return Th(new Uh,a,0,a.na.length|0)};d.xg=function(a,b){return b===this.Ma&&D(G(),a,this.Ab)};
d.$classData=l({lp:0},!1,"scala.collection.immutable.HashMap$HashMap1",{lp:1,oi:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1,td:1});function bN(){this.Ma=0;this.Sc=null}bN.prototype=new JM;bN.prototype.constructor=bN;d=bN.prototype;
d.Xg=function(a,b,c,e,f,h){if(b===this.Ma)return null!==h&&this.Sc.ca(a)?aN(new bN,b,this.Sc.mg(h.Ek((new H).d(a,this.Sc.g(a)),f))):aN(new bN,b,this.Sc.Di(a,e));a=LM(a,b,e,f);return xD(CD(),this.Ma,this,b,a,c,1+this.Sc.L()|0)};d.Cg=function(a,b){return b===this.Ma?this.Sc.yb(a):B()};d.O=function(a){var b=zM(this.Sc);tp(AB(b),a)};
d.ii=function(a,b){if(b===this.Ma){a=this.Sc.Gi(a);var c=a.L();switch(c){case 0:return CD(),BD();case 1:return a=zM(a),a=AB(a).t(),LM(a.N,b,a.P,a);default:return c===this.Sc.L()?this:aN(new bN,b,a)}}else return this};d.E=function(){var a=zM(this.Sc);return AB(a)};d.L=function(){return this.Sc.L()};function aN(a,b,c){a.Ma=b;a.Sc=c;return a}d.xg=function(a,b){return b===this.Ma&&this.Sc.ca(a)};
d.$classData=l({Ax:0},!1,"scala.collection.immutable.HashMap$HashMapCollision1",{Ax:1,oi:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1,td:1});function AD(){this.tc=0;this.ab=null;this.Aa=0}AD.prototype=new JM;AD.prototype.constructor=AD;d=AD.prototype;
d.Xg=function(a,b,c,e,f,h){var m=1<<(31&(b>>>c|0)),q=Vy(Ns(),this.tc&(-1+m|0));if(0!==(this.tc&m)){m=this.ab.e[q];a=m.Xg(a,b,5+c|0,e,f,h);if(a===m)return this;b=k(r(yD),[this.ab.e.length]);wA(yA(),this.ab,0,b,0,this.ab.e.length);b.e[q]=a;return zD(new AD,this.tc,b,this.Aa+(a.L()-m.L()|0)|0)}c=k(r(yD),[1+this.ab.e.length|0]);wA(yA(),this.ab,0,c,0,q);c.e[q]=LM(a,b,e,f);wA(yA(),this.ab,q,c,1+q|0,this.ab.e.length-q|0);return zD(new AD,this.tc|m,c,1+this.Aa|0)};
d.Cg=function(a,b,c){var e=31&(b>>>c|0);if(-1===this.tc)return this.ab.e[e].Cg(a,b,5+c|0);e=1<<e;return 0!==(this.tc&e)?(e=Vy(Ns(),this.tc&(-1+e|0)),this.ab.e[e].Cg(a,b,5+c|0)):B()};d.O=function(a){for(var b=0;b<this.ab.e.length;)this.ab.e[b].O(a),b=1+b|0};
d.ii=function(a,b,c){var e=1<<(31&(b>>>c|0)),f=Vy(Ns(),this.tc&(-1+e|0));if(0!==(this.tc&e)){var h=this.ab.e[f];a=h.ii(a,b,5+c|0);if(a===h)return this;if(0===a.L()){e^=this.tc;if(0!==e)return a=k(r(yD),[-1+this.ab.e.length|0]),wA(yA(),this.ab,0,a,0,f),wA(yA(),this.ab,1+f|0,a,f,-1+(this.ab.e.length-f|0)|0),f=this.Aa-h.L()|0,1!==a.e.length||NB(a.e[0])?zD(new AD,e,a,f):a.e[0];CD();return BD()}return 1!==this.ab.e.length||NB(a)?(e=k(r(yD),[this.ab.e.length]),wA(yA(),this.ab,0,e,0,this.ab.e.length),e.e[f]=
a,f=this.Aa+(a.L()-h.L()|0)|0,zD(new AD,this.tc,e,f)):a}return this};d.E=function(){var a=new cD;LB.prototype.ao.call(a,this.ab);return a};d.L=function(){return this.Aa};function zD(a,b,c,e){a.tc=b;a.ab=c;a.Aa=e;return a}d.xg=function(a,b,c){var e=31&(b>>>c|0);if(-1===this.tc)return this.ab.e[e].xg(a,b,5+c|0);e=1<<e;return 0!==(this.tc&e)?(e=Vy(Ns(),this.tc&(-1+e|0)),this.ab.e[e].xg(a,b,5+c|0)):!1};function NB(a){return!!(a&&a.$classData&&a.$classData.p.mp)}
d.$classData=l({mp:0},!1,"scala.collection.immutable.HashMap$HashTrieMap",{mp:1,oi:1,Rd:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,Ad:1,Fa:1,Ia:1,Ha:1,Bd:1,k:1,h:1,td:1});function cN(){}cN.prototype=new jF;cN.prototype.constructor=cN;function dN(){}d=dN.prototype=cN.prototype;d.Ga=function(){return this};d.Se=function(){return this};d.Xa=function(a){return VE(this,a)};d.fe=function(a){return 0>a?1:$E(this,a)};
d.g=function(a){return VE(this,a|0)};d.Nd=function(a){return TE(this,a)};d.nc=function(a){return UE(this,a)};d.Bb=function(){return this};d.db=function(){return this};
d.Pf=function(a,b){if(b===hg().Ka){if(this===xb())return xb();b=this;for(var c=qs(!1),e=(new Sf).o(null),f=(new Sf).o(null);b!==xb();)a.g(b.D()).Ga().O(t(new u,function(a,b,c,e){return function(a){b.x?(a=zb(new Ab,a,xb()),e.x.Te=a,e.x=a):(c.x=zb(new Ab,a,xb()),e.x=c.x,b.x=!0)}}(this,c,e,f))),b=b.u();return c.x?e.x:xb()}return XC(this,a,b)};d.Tk=function(a){return eN(this,a)};d.ob=function(){return hg()};d.O=function(a){for(var b=this;!b.l();)a.g(b.D()),b=b.u()};
d.kf=function(a,b){return XE(this,a,b)};d.ce=function(a,b){return WE(this,a,b)};d.E=function(){return AB(this)};function eN(a,b){for(;!a.l()&&0<b;)a=a.u(),b=-1+b|0;return a}d.yf=function(){return this};d.W=function(){return Ju(this)};d.kc=function(){return this.l()?Rw():(new Qw).Hh(this.D(),v(new w,function(a){return function(){return a.u().kc()}}(this)))};d.ae=function(a){return eN(this,a)};d.Ud=function(){return this};d.Gc=function(a){return ZE(this,a|0)};d.r=function(){return Kw(U(),this)};
d.Dd=function(){return"List"};function Qw(){this.ak=this.Op=this.mj=null}Qw.prototype=new TM;Qw.prototype.constructor=Qw;d=Qw.prototype;d.D=function(){return this.mj};function fN(a){a.Re()||a.Re()||(a.Op=a.ak.U(),a.ak=null);return a.Op}d.Nd=function(a){return gN(a)?hN(this,a):TE(this,a)};d.l=function(){return!1};d.Re=function(){return null===this.ak};function hN(a,b){for(;;)if(D(G(),a.mj,b.mj))if(a=fN(a),gN(a))if(b=fN(b),gN(b)){if(a===b)return!0}else return!1;else return fN(b).l();else return!1}
d.u=function(){return fN(this)};d.Hh=function(a,b){this.mj=a;this.ak=b;return this};function gN(a){return!!(a&&a.$classData&&a.$classData.p.qp)}d.$classData=l({qp:0},!1,"scala.collection.immutable.Stream$Cons",{qp:1,ky:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Fl:1,pi:1,Fa:1,Ia:1,Ha:1,ni:1,Bl:1,Cl:1,k:1,h:1});function iN(){}iN.prototype=new TM;iN.prototype.constructor=iN;d=iN.prototype;d.D=function(){this.al()};d.b=function(){return this};
d.l=function(){return!0};d.Re=function(){return!1};d.al=function(){throw(new yw).i("head of empty stream");};d.u=function(){throw(new Uz).i("tail of empty stream");};d.$classData=l({ny:0},!1,"scala.collection.immutable.Stream$Empty$",{ny:1,ky:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Fl:1,pi:1,Fa:1,Ia:1,Ha:1,ni:1,Bl:1,Cl:1,k:1,h:1});var jN=void 0;function Rw(){jN||(jN=(new iN).b());return jN}
function kD(){this.hc=this.pb=this.jb=0;this.Za=!1;this.Ya=0;this.Id=this.jd=this.Oc=this.Fc=this.uc=this.tb=null}kD.prototype=new jF;kD.prototype.constructor=kD;d=kD.prototype;d.Ga=function(){return this};d.Q=function(){return this.Oc};
function kN(a,b,c,e){if(a.Za)if(32>e)a.la(Mb(a.Pa()));else if(1024>e)a.S(Mb(a.v())),a.v().e[31&(b>>>5|0)]=a.Pa(),a.la(Ub(a.v(),31&(c>>>5|0)));else if(32768>e)a.S(Mb(a.v())),a.ua(Mb(a.G())),a.v().e[31&(b>>>5|0)]=a.Pa(),a.G().e[31&(b>>>10|0)]=a.v(),a.S(Ub(a.G(),31&(c>>>10|0))),a.la(Ub(a.v(),31&(c>>>5|0)));else if(1048576>e)a.S(Mb(a.v())),a.ua(Mb(a.G())),a.Qa(Mb(a.Q())),a.v().e[31&(b>>>5|0)]=a.Pa(),a.G().e[31&(b>>>10|0)]=a.v(),a.Q().e[31&(b>>>15|0)]=a.G(),a.ua(Ub(a.Q(),31&(c>>>15|0))),a.S(Ub(a.G(),31&
(c>>>10|0))),a.la(Ub(a.v(),31&(c>>>5|0)));else if(33554432>e)a.S(Mb(a.v())),a.ua(Mb(a.G())),a.Qa(Mb(a.Q())),a.$b(Mb(a.Ca())),a.v().e[31&(b>>>5|0)]=a.Pa(),a.G().e[31&(b>>>10|0)]=a.v(),a.Q().e[31&(b>>>15|0)]=a.G(),a.Ca().e[31&(b>>>20|0)]=a.Q(),a.Qa(Ub(a.Ca(),31&(c>>>20|0))),a.ua(Ub(a.Q(),31&(c>>>15|0))),a.S(Ub(a.G(),31&(c>>>10|0))),a.la(Ub(a.v(),31&(c>>>5|0)));else if(1073741824>e)a.S(Mb(a.v())),a.ua(Mb(a.G())),a.Qa(Mb(a.Q())),a.$b(Mb(a.Ca())),a.df(Mb(a.gc())),a.v().e[31&(b>>>5|0)]=a.Pa(),a.G().e[31&
(b>>>10|0)]=a.v(),a.Q().e[31&(b>>>15|0)]=a.G(),a.Ca().e[31&(b>>>20|0)]=a.Q(),a.gc().e[31&(b>>>25|0)]=a.Ca(),a.$b(Ub(a.gc(),31&(c>>>25|0))),a.Qa(Ub(a.Ca(),31&(c>>>20|0))),a.ua(Ub(a.Q(),31&(c>>>15|0))),a.S(Ub(a.G(),31&(c>>>10|0))),a.la(Ub(a.v(),31&(c>>>5|0)));else throw(new Ob).b();else{b=-1+a.gb()|0;switch(b){case 5:a.df(Mb(a.gc()));a.$b(Ub(a.gc(),31&(c>>>25|0)));a.Qa(Ub(a.Ca(),31&(c>>>20|0)));a.ua(Ub(a.Q(),31&(c>>>15|0)));a.S(Ub(a.G(),31&(c>>>10|0)));a.la(Ub(a.v(),31&(c>>>5|0)));break;case 4:a.$b(Mb(a.Ca()));
a.Qa(Ub(a.Ca(),31&(c>>>20|0)));a.ua(Ub(a.Q(),31&(c>>>15|0)));a.S(Ub(a.G(),31&(c>>>10|0)));a.la(Ub(a.v(),31&(c>>>5|0)));break;case 3:a.Qa(Mb(a.Q()));a.ua(Ub(a.Q(),31&(c>>>15|0)));a.S(Ub(a.G(),31&(c>>>10|0)));a.la(Ub(a.v(),31&(c>>>5|0)));break;case 2:a.ua(Mb(a.G()));a.S(Ub(a.G(),31&(c>>>10|0)));a.la(Ub(a.v(),31&(c>>>5|0)));break;case 1:a.S(Mb(a.v()));a.la(Ub(a.v(),31&(c>>>5|0)));break;case 0:a.la(Mb(a.Pa()));break;default:throw(new x).o(b);}a.Za=!0}}
d.D=function(){if(Tj(this))throw(new Uz).i("empty.head");return this.Xa(0)};d.Xa=function(a){var b=a+this.jb|0;if(0<=a&&b<this.pb)a=b;else throw(new Y).i(""+a);return Nb(this,a,a^this.hc)};d.Se=function(){return this};d.gb=function(){return this.Ya};d.fe=function(a){return this.W()-a|0};d.g=function(a){return this.Xa(a|0)};d.Bb=function(){return this};d.db=function(){return this};d.vc=function(a,b,c){this.jb=a;this.pb=b;this.hc=c;this.Za=!1;return this};d.df=function(a){this.Id=a};
function Kg(a,b){var c=(Uk(),Vk().Ja);c===(pm(),Vk().Ja)||c===rD().Ka||c===aq().Ka?a=lN(a,b):(c=c.$e(a.tf()),c.fb(a.Ud()),c.rb(b),a=c.La());return a}d.ob=function(){return Uk()};d.Pa=function(){return this.tb};d.ua=function(a){this.Fc=a};d.Ca=function(){return this.jd};
function mN(a,b,c){var e=-1+a.Ya|0;switch(e){case 0:a.tb=Qb(a.tb,b,c);break;case 1:a.uc=Qb(a.uc,b,c);break;case 2:a.Fc=Qb(a.Fc,b,c);break;case 3:a.Oc=Qb(a.Oc,b,c);break;case 4:a.jd=Qb(a.jd,b,c);break;case 5:a.Id=Qb(a.Id,b,c);break;default:throw(new x).o(e);}}function Wl(a){if(Tj(a))throw(new Uz).i("empty.tail");return nN(a,1)}d.Af=function(){return this};
function lN(a,b){if(a.pb!==a.jb){var c=-32&a.pb,e=31&a.pb;if(a.pb!==c){var f=(new kD).vc(a.jb,1+a.pb|0,c);Vb(f,a,a.Ya);f.Za=a.Za;kN(f,a.hc,c,a.hc^c);f.tb.e[e]=b;return f}var h=a.jb&~(-1+(1<<ca(5,-1+a.Ya|0))|0),f=a.jb>>>ca(5,-1+a.Ya|0)|0;if(0!==h){if(1<a.Ya){var c=c-h|0,m=a.hc-h|0,h=(new kD).vc(a.jb-h|0,(1+a.pb|0)-h|0,c);Vb(h,a,a.Ya);h.Za=a.Za;mN(h,f,0);oN(h,m,c,m^c);h.tb.e[e]=b;return h}e=-32+c|0;c=a.hc;m=(new kD).vc(a.jb-h|0,(1+a.pb|0)-h|0,e);Vb(m,a,a.Ya);m.Za=a.Za;mN(m,f,0);kN(m,c,e,c^e);m.tb.e[32-
h|0]=b;return m}f=a.hc;h=(new kD).vc(a.jb,1+a.pb|0,c);Vb(h,a,a.Ya);h.Za=a.Za;oN(h,f,c,f^c);h.tb.e[e]=b;return h}a=k(r(p),[32]);a.e[0]=b;b=(new kD).vc(0,1,0);b.Ya=1;b.tb=a;return b}function Rm(a,b){var c=(Uk(),Vk().Ja);c===(pm(),Vk().Ja)||c===rD().Ka||c===aq().Ka?a=pN(a,b):(c=c.$e(a.tf()),c.rb(b),c.fb(a.Ud()),a=c.La());return a}d.E=function(){return Nj(this)};d.S=function(a){this.uc=a};
function Vl(a,b){var c=(Uk(),Vk().Ja);if(c===(pm(),Vk().Ja)||c===rD().Ka||c===aq().Ka){if(b.l())return a;b=b.Kh()?b.Ga():b.Af();var e=b.L();if(2>=e||e<(a.W()>>>5|0))return c=(new Sf).o(a),b.O(t(new u,function(a,b){return function(a){b.x=Kg(b.x,a)}}(a,c))),c.x;if(a.W()<(e>>>5|0)&&b&&b.$classData&&b.$classData.p.up){c=b;for(a=SB(a);a.H();)b=a.t(),c=Rm(c,b);return c}return YC(a,b,c)}return YC(a,b.Ga(),c)}d.W=function(){return this.pb-this.jb|0};d.yf=function(){return this};d.$b=function(a){this.jd=a};
function oN(a,b,c,e){a.Za?(Tb(a,b),Pb(a,b,c,e)):(Pb(a,b,c,e),a.Za=!0)}d.ke=function(){return this.W()};d.v=function(){return this.uc};d.ae=function(a){return nN(this,a)};d.gc=function(){return this.Id};d.u=function(){return Wl(this)};d.Ud=function(){return this};function qN(a){if(32>a)return 1;if(1024>a)return 2;if(32768>a)return 3;if(1048576>a)return 4;if(33554432>a)return 5;if(1073741824>a)return 6;throw(new Ob).b();}
function Nj(a){var b=(new lD).Ie(a.jb,a.pb);Vb(b,a,a.Ya);a.Za&&Tb(b,a.hc);1<b.Qk&&Rb(b,a.jb,a.jb^a.hc);return b}d.Gc=function(a){return vB(this,a|0)};function rN(a,b){for(var c=0;c<b;)a.e[c]=null,c=1+c|0}d.r=function(){return Kw(U(),this)};d.Nc=function(a){this.Ya=a};d.G=function(){return this.Fc};d.la=function(a){this.tb=a};
function pN(a,b){if(a.pb!==a.jb){var c=-32&(-1+a.jb|0),e=31&(-1+a.jb|0);if(a.jb!==(32+c|0)){var f=(new kD).vc(-1+a.jb|0,a.pb,c);Vb(f,a,a.Ya);f.Za=a.Za;kN(f,a.hc,c,a.hc^c);f.tb.e[e]=b;return f}var h=(1<<ca(5,a.Ya))-a.pb|0,f=h&~(-1+(1<<ca(5,-1+a.Ya|0))|0),h=h>>>ca(5,-1+a.Ya|0)|0;if(0!==f){if(1<a.Ya){var c=c+f|0,m=a.hc+f|0,f=(new kD).vc((-1+a.jb|0)+f|0,a.pb+f|0,c);Vb(f,a,a.Ya);f.Za=a.Za;mN(f,0,h);oN(f,m,c,m^c);f.tb.e[e]=b;return f}e=32+c|0;c=a.hc;m=(new kD).vc((-1+a.jb|0)+f|0,a.pb+f|0,e);Vb(m,a,a.Ya);
m.Za=a.Za;mN(m,0,h);kN(m,c,e,c^e);m.tb.e[-1+f|0]=b;return m}if(0>c)return f=(1<<ca(5,1+a.Ya|0))-(1<<ca(5,a.Ya))|0,h=c+f|0,c=a.hc+f|0,f=(new kD).vc((-1+a.jb|0)+f|0,a.pb+f|0,h),Vb(f,a,a.Ya),f.Za=a.Za,oN(f,c,h,c^h),f.tb.e[e]=b,f;f=a.hc;h=(new kD).vc(-1+a.jb|0,a.pb,c);Vb(h,a,a.Ya);h.Za=a.Za;oN(h,f,c,f^c);h.tb.e[e]=b;return h}a=k(r(p),[32]);a.e[31]=b;b=(new kD).vc(31,32,0);b.Ya=1;b.tb=a;return b}
function nN(a,b){if(0>=b)b=a;else if(a.jb<(a.pb-b|0)){var c=a.jb+b|0,e=-32&c,f=qN(c^(-1+a.pb|0)),h=c&~(-1+(1<<ca(5,f))|0);b=(new kD).vc(c-h|0,a.pb-h|0,e-h|0);Vb(b,a,a.Ya);b.Za=a.Za;kN(b,a.hc,e,a.hc^e);b.Ya=f;a=-1+f|0;switch(a){case 0:b.uc=null;b.Fc=null;b.Oc=null;b.jd=null;b.Id=null;break;case 1:b.Fc=null;b.Oc=null;b.jd=null;b.Id=null;break;case 2:b.Oc=null;b.jd=null;b.Id=null;break;case 3:b.jd=null;b.Id=null;break;case 4:b.Id=null;break;case 5:break;default:throw(new x).o(a);}a=c-h|0;if(32>a)rN(b.tb,
a);else if(1024>a)rN(b.tb,31&a),b.uc=sN(b.uc,a>>>5|0);else if(32768>a)rN(b.tb,31&a),b.uc=sN(b.uc,31&(a>>>5|0)),b.Fc=sN(b.Fc,a>>>10|0);else if(1048576>a)rN(b.tb,31&a),b.uc=sN(b.uc,31&(a>>>5|0)),b.Fc=sN(b.Fc,31&(a>>>10|0)),b.Oc=sN(b.Oc,a>>>15|0);else if(33554432>a)rN(b.tb,31&a),b.uc=sN(b.uc,31&(a>>>5|0)),b.Fc=sN(b.Fc,31&(a>>>10|0)),b.Oc=sN(b.Oc,31&(a>>>15|0)),b.jd=sN(b.jd,a>>>20|0);else if(1073741824>a)rN(b.tb,31&a),b.uc=sN(b.uc,31&(a>>>5|0)),b.Fc=sN(b.Fc,31&(a>>>10|0)),b.Oc=sN(b.Oc,31&(a>>>15|0)),
b.jd=sN(b.jd,31&(a>>>20|0)),b.Id=sN(b.Id,a>>>25|0);else throw(new Ob).b();}else b=Uk().Vb;return b}function sN(a,b){var c=k(r(p),[a.e.length]);Na(a,b,c,b,c.e.length-b|0);return c}d.Qa=function(a){this.Oc=a};d.$classData=l({up:0},!1,"scala.collection.immutable.Vector",{up:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Ix:1,pi:1,Fa:1,Ia:1,Ha:1,li:1,wf:1,vp:1,k:1,h:1,td:1});function Js(){this.ed=null}Js.prototype=new jF;
Js.prototype.constructor=Js;d=Js.prototype;d.Ga=function(){return this};d.D=function(){return IE(this)};d.Xa=function(a){a=65535&(this.ed.charCodeAt(a)|0);return nt(a)};d.Se=function(){return this};d.fe=function(a){return KE(this,a)};d.Nd=function(a){return ME(this,a)};d.g=function(a){a=65535&(this.ed.charCodeAt(a|0)|0);return nt(a)};d.nc=function(a){return LE(this,a)};d.l=function(){return JE(this)};d.Bb=function(){return this};d.db=function(){return this};d.ob=function(){return pm()};d.m=function(){return this.ed};
d.O=function(a){NE(this,a)};d.kf=function(a,b){return QE(this,this.ed.length|0,a,b)};d.ce=function(a,b){return OE(this,a,b)};d.Vg=function(a,b){return tN(this,a,b)};d.E=function(){return Th(new Uh,this,0,this.ed.length|0)};d.W=function(){return this.ed.length|0};d.yf=function(){return this};d.ke=function(){return this.ed.length|0};d.ae=function(a){return tN(this,a,this.ed.length|0)};d.u=function(){return RE(this)};d.Ud=function(){return this};d.Gc=function(a){return vB(this,a|0)};
d.Kf=function(a,b,c){SE(this,a,b,c)};d.r=function(){return Kw(U(),this)};d.i=function(a){this.ed=a;return this};function tN(a,b,c){b=0>b?0:b;if(c<=b||b>=(a.ed.length|0))return(new Js).i("");c=c>(a.ed.length|0)?a.ed.length|0:c;I();return(new Js).i((null!==a?a.ed:null).substring(b,c))}d.da=function(){Ks||(Ks=(new Gs).b());return Ks.da()};
d.$classData=l({yy:0},!1,"scala.collection.immutable.WrappedString",{yy:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Ix:1,pi:1,Fa:1,Ia:1,Ha:1,li:1,wf:1,sp:1,mi:1,$o:1,md:1});function Ab(){this.Te=this.$k=null}Ab.prototype=new dN;Ab.prototype.constructor=Ab;d=Ab.prototype;d.A=function(){return"::"};d.D=function(){return this.$k};d.y=function(){return 2};d.l=function(){return!1};
d.z=function(a){switch(a){case 0:return this.$k;case 1:return this.Te;default:throw(new Y).i(""+a);}};d.u=function(){return this.Te};function zb(a,b,c){a.$k=b;a.Te=c;return a}d.B=function(){return Z(this)};d.$classData=l({ux:0},!1,"scala.collection.immutable.$colon$colon",{ux:1,pp:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Fl:1,pi:1,Fa:1,Ia:1,Ha:1,ni:1,Bl:1,C:1,Cl:1,k:1,h:1});function uN(){}uN.prototype=new dN;
uN.prototype.constructor=uN;d=uN.prototype;d.b=function(){return this};d.D=function(){this.al()};d.A=function(){return"Nil"};d.y=function(){return 0};d.l=function(){return!0};d.n=function(a){return a&&a.$classData&&a.$classData.p.ud?a.l():!1};d.z=function(a){throw(new Y).i(""+a);};d.al=function(){throw(new yw).i("head of empty list");};d.u=function(){throw(new Uz).i("tail of empty list");};d.B=function(){return Z(this)};
d.$classData=l({by:0},!1,"scala.collection.immutable.Nil$",{by:1,pp:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Fl:1,pi:1,Fa:1,Ia:1,Ha:1,ni:1,Bl:1,C:1,Cl:1,k:1,h:1});var vN=void 0;function xb(){vN||(vN=(new uN).b());return vN}function wN(){}wN.prototype=new lF;wN.prototype.constructor=wN;function xN(){}d=xN.prototype=wN.prototype;d.Bb=function(){return this};d.ob=function(){ZB||(ZB=(new XB).b());return ZB};
d.fd=function(a,b){lx(this,a,b)};d.qc=function(){};d.da=function(){return this.zg()};d.fb=function(a){return Tl(this,a)};function yN(){}yN.prototype=new pM;yN.prototype.constructor=yN;function zN(){}zN.prototype=yN.prototype;yN.prototype.hk=function(a){a=this.Zn(a);-1!==a&&this.Al(a);return this};yN.prototype.fb=function(a){return Tl(this,a)};function wp(){this.Ki=0;this.Jb=null;this.$j=this.me=0;this.le=null;this.Kl=0}wp.prototype=new xN;wp.prototype.constructor=wp;d=wp.prototype;d.Ga=function(){return this};
d.pc=function(){return this};d.b=function(){wp.prototype.Wu.call(this,null);return this};d.g=function(a){var b=Ts(this,a);return null===b?bF(a):b.s};d.nb=function(a){var b=(new wp).b(),b=Tl(b,this);var c=ki(li(),a),c=Ms(b,c),e=b.Jb.e[c];if(null!==e){var f=e.Va;if(D(G(),f,a))b.Jb.e[c]=e.xc,b.me=-1+b.me|0,Us(b,c),e.xc=null;else{for(f=e.xc;;){if(null!==f)var h=f.Va,h=!D(G(),h,a);else h=!1;if(h)e=f,f=f.xc;else break}null!==f&&(e.xc=f.xc,b.me=-1+b.me|0,Us(b,c),f.xc=null)}}return b};d.db=function(){return this};
function AN(a,b){var c=xp(a,b.N,b.P);null!==c&&(c.s=b.P);return a}d.rc=function(a){return AN(this,a)};d.O=function(a){for(var b=this.Jb,c=Os(this),e=b.e[c];null!==e;){var f=e.xc;a.g((new H).d(e.Va,e.s));for(e=f;null===e&&0<c;)c=-1+c|0,e=b.e[c]}};d.zg=function(){return(new wp).b()};d.il=function(){return(new TB).dl(this)};d.L=function(){return this.me};d.ui=function(){return this};d.La=function(){return this};
d.E=function(){return(new Vh).lb(UB(this),t(new u,function(){return function(a){return(new H).d(a.Va,a.s)}}(this)))};d.Sl=function(){return(new VB).dl(this)};d.Wu=function(a){this.Ki=750;Ss();this.Jb=k(r(Wb),[1<<(-da(15)|0)]);this.me=0;var b=this.Ki;Ss();Ss();this.$j=Rs(0,b,1<<(-da(15)|0));this.le=null;this.Kl=Vy(Ns(),-1+this.Jb.e.length|0);null!==a&&(this.Ki=a.QA(),this.Jb=a.IB(),this.me=a.HB(),this.$j=a.JB(),this.Kl=a.AB(),this.le=a.CB());return this};
d.yb=function(a){a=Ts(this,a);return null===a?B():(new Pf).o(a.s)};d.ca=function(a){return null!==Ts(this,a)};d.rb=function(a){return AN(this,a)};d.Wd=function(a){var b=(new wp).b(),b=Tl(b,this);return AN(b,a)};d.$classData=l({Gy:0},!1,"scala.collection.mutable.HashMap",{Gy:1,tB:1,Yc:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,ad:1,Jc:1,Zc:1,bd:1,Ta:1,T:1,Na:1,xB:1,Xj:1,Yj:1,Qj:1,yB:1,Sd:1,Qd:1,Pd:1,El:1,Wj:1,Pj:1,Nh:1,uB:1,vB:1,td:1,k:1,h:1});
function ig(){this.nd=this.Ea=null;this.Ag=!1;this.Tc=0}ig.prototype=new zN;ig.prototype.constructor=ig;d=ig.prototype;d.$n=function(a,b){return uB(this.Ea,a,b)};function BN(a){if(!a.l()){var b=a.Ea,c=a.nd.Te;a.Ea=xb();a.nd=null;a.Ag=!1;for(a.Tc=0;b!==c;)jg(a,b.D()),b=b.u()}}d.pc=function(){return this};d.b=function(){this.Ea=xb();this.Ag=!1;this.Tc=0;return this};d.D=function(){return this.Ea.D()};d.Xa=function(a){if(0>a||a>=this.Tc)throw(new Y).i(""+a);return VE(this.Ea,a)};d.Se=function(){return this};
d.fe=function(a){return 0>a?1:$E(this.Ea,a)};d.g=function(a){return this.Xa(a|0)};d.Nd=function(a){return TE(this.Ea,a)};d.nb=function(a){return CN(DN((new ig).b(),this),a)};d.nc=function(a){return UE(this.Ea,a)};d.l=function(){return 0===this.Tc};function kg(a){a.Ag=!a.l();return a.Ea}d.db=function(){return this};d.n=function(a){return a&&a.$classData&&a.$classData.p.Cp?this.Ea.n(a.Ea):Pm(this,a)};d.Yh=function(a,b,c){return Qj(this.Ea,a,b,c)};d.rc=function(a){return jg(this,a)};
d.ob=function(){sE||(sE=(new rE).b());return sE};d.O=function(a){for(var b=this.Ea;!b.l();)a.g(b.D()),b=b.u()};d.kf=function(a,b){return XE(this.Ea,a,b)};d.ce=function(a,b){return WE(this.Ea,a,b)};d.L=function(){return this.Tc};d.La=function(){return kg(this)};d.E=function(){var a=new cC;a.qh=this.l()?xb():this.Ea;return a};d.fd=function(a,b){lx(this,a,b)};d.W=function(){return this.Tc};d.yf=function(){return this};
d.Al=function(a){if(0>a||a>=this.Tc)throw(new Y).i(""+a);this.Ag&&BN(this);var b=this.Ea.D();if(0===a)this.Ea=this.Ea.u();else{for(var c=this.Ea,b=1;b<a;)c=c.u(),b=1+b|0;b=c.u().D();this.nd===c.u()&&(this.nd=c);c.Te=c.u().u()}EN(this);return b};function CN(a,b){a.Ag&&BN(a);if(!a.l())if(D(G(),a.Ea.D(),b))a.Ea=a.Ea.u(),EN(a);else{for(var c=a.Ea;!c.u().l()&&!D(G(),c.u().D(),b);)c=c.u();if(!c.u().l()){b=c;var e=b.Te,f=a.nd;if(null===e?null===f:e.n(f))a.nd=b;b.Te=c.u().u();EN(a)}}return a}d.kc=function(){return this.Ea.kc()};
function EN(a){a.Tc=a.Tc-1|0;0>=a.Tc&&(a.nd=null)}d.Ye=function(a,b,c,e){return ps(this.Ea,a,b,c,e)};function jg(a,b){a.Ag&&BN(a);if(a.l())a.nd=zb(new Ab,b,xb()),a.Ea=a.nd;else{var c=a.nd;a.nd=zb(new Ab,b,xb());c.Te=a.nd}a.Tc=1+a.Tc|0;return a}d.hk=function(a){return CN(this,a)};d.Gc=function(a){return ZE(this.Ea,a|0)};d.Pp=function(){var a=this.Ea,b=fz(),b=Yw(b);return Wk(a,b)};d.We=function(a,b){return XE(this.Ea,a,b)};d.rb=function(a){return jg(this,a)};d.Zn=function(a){return uB(this.Ea,a,0)};
d.qc=function(){};function DN(a,b){a:for(;;){var c=b;if(null!==c&&c===a){var e=a;b=a.Tc;c=e.da();if(!(0>=b)){c.fd(b,e);for(var f=0,e=e.E();f<b&&e.H();)c.rb(e.t()),f=1+f|0}b=c.La();continue a}return Tl(a,b)}}d.fb=function(a){return DN(this,a)};d.Dd=function(){return"ListBuffer"};
d.$classData=l({Cp:0},!1,"scala.collection.mutable.ListBuffer",{Cp:1,wp:1,Gl:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Il:1,Xj:1,Yj:1,Qj:1,Jl:1,Wj:1,Pj:1,Nh:1,xp:1,yp:1,Qd:1,Pd:1,El:1,ep:1,Na:1,qi:1,Sd:1,qB:1,pB:1,rB:1,k:1,h:1});function ms(){this.wb=null}ms.prototype=new pM;ms.prototype.constructor=ms;d=ms.prototype;d.Ga=function(){return this};d.b=function(){ms.prototype.Pu.call(this,16,"");return this};d.D=function(){return IE(this)};
d.Xa=function(a){a=65535&(this.wb.va.charCodeAt(a)|0);return nt(a)};d.Se=function(){return this};d.fe=function(a){return KE(this,a)};d.g=function(a){a=65535&(this.wb.va.charCodeAt(a|0)|0);return nt(a)};d.Nd=function(a){return ME(this,a)};d.nc=function(a){return LE(this,a)};d.l=function(){return JE(this)};d.Bb=function(){return this};d.db=function(){return this};d.Ol=function(a,b){return this.wb.va.substring(a,b)};d.rc=function(a){kn(this.wb,null===a?0:a.s);return this};
d.ob=function(){GD||(GD=(new FD).b());return GD};d.m=function(){return this.wb.va};d.O=function(a){NE(this,a)};d.kf=function(a,b){var c=this.wb.W();return QE(this,c,a,b)};d.ce=function(a,b){return OE(this,a,b)};d.Vg=function(a,b){return eF(this,a,b)};d.La=function(){return this.wb.va};function jn(a,b){var c=a.wb;c.va=""+c.va+b;return a}d.E=function(){return Th(new Uh,this,0,this.wb.W())};d.vi=function(){return this};d.fd=function(a,b){lx(this,a,b)};
d.Pu=function(a,b){a=(new nA).kb((b.length|0)+a|0);a.va=""+a.va+b;ms.prototype.cl.call(this,a);return this};d.W=function(){return this.wb.W()};d.yf=function(){return this};d.ke=function(){return this.wb.W()};d.ae=function(a){var b=this.wb.W();return eF(this,a,b)};d.Ud=function(){return this};d.u=function(){return RE(this)};d.cl=function(a){this.wb=a;return this};function rs(a,b){var c=a.wb;c.va+=""+b;return a}d.Gc=function(a){return vB(this,a|0)};d.rb=function(a){kn(this.wb,null===a?0:a.s);return this};
d.Kf=function(a,b,c){SE(this,a,b,c)};d.qc=function(){};d.r=function(){return Kw(U(),this)};d.da=function(){return JA(new IA,(new ms).b())};d.fb=function(a){return Tl(this,a)};d.$classData=l({Ry:0},!1,"scala.collection.mutable.StringBuilder",{Ry:1,Gl:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Il:1,Xj:1,Yj:1,Qj:1,Jl:1,Wj:1,Pj:1,Nh:1,gl:1,Ap:1,li:1,wf:1,Hl:1,sp:1,mi:1,$o:1,md:1,qi:1,Sd:1,Qd:1,Pd:1,k:1,h:1});
function A(){this.na=null}A.prototype=new zN;A.prototype.constructor=A;d=A.prototype;d.Ga=function(){return this};d.pc=function(){return this};d.b=function(){A.prototype.f.call(this,[]);return this};d.D=function(){return IE(this)};d.Xa=function(a){return this.na[a]};d.Se=function(){return this};d.fe=function(a){return KE(this,a)};d.g=function(a){return this.na[a|0]};d.Nd=function(a){return ME(this,a)};d.nc=function(a){return LE(this,a)};d.nb=function(a){return hF(this).hk(a)};d.l=function(){return JE(this)};
d.Bb=function(){return this};d.db=function(){return this};d.rc=function(a){this.na.push(a);return this};d.ob=function(){return Sz()};d.O=function(a){NE(this,a)};d.kf=function(a,b){return QE(this,this.na.length|0,a,b)};d.ce=function(a,b){return OE(this,a,b)};d.Vg=function(a,b){return PE(this,a,b)};d.La=function(){return this};d.E=function(){return Th(new Uh,this,0,this.na.length|0)};d.vi=function(){return this};d.fd=function(a,b){lx(this,a,b)};d.W=function(){return this.na.length|0};d.yf=function(){return this};
d.ke=function(){return this.na.length|0};d.Al=function(a){if(0>a||a>=(this.na.length|0))throw(new Y).b();return this.na.splice(a,1)[0]};d.ae=function(a){return PE(this,a,this.na.length|0)};d.u=function(){return RE(this)};d.Ud=function(){return this};d.Gc=function(a){return vB(this,a|0)};d.rb=function(a){this.na.push(a);return this};d.Kf=function(a,b,c){SE(this,a,b,c)};d.qc=function(){};d.r=function(){return Kw(U(),this)};d.f=function(a){this.na=a;return this};d.Dd=function(){return"WrappedArray"};
d.$classData=l({Vy:0},!1,"scala.scalajs.js.WrappedArray",{Vy:1,wp:1,Gl:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Il:1,Xj:1,Yj:1,Qj:1,Jl:1,Wj:1,Pj:1,Nh:1,xp:1,yp:1,Qd:1,Pd:1,El:1,ep:1,Na:1,Ap:1,li:1,wf:1,Hl:1,Cy:1,Bp:1,mi:1,Sd:1});function YB(){this.fo=0;this.na=null;this.Aa=0}YB.prototype=new zN;YB.prototype.constructor=YB;d=YB.prototype;d.Ga=function(){return this};d.pc=function(){return this};
function FN(a,b){XM(a,1+a.Aa|0);a.na.e[a.Aa]=b;a.Aa=1+a.Aa|0;return a}d.b=function(){YB.prototype.kb.call(this,16);return this};d.D=function(){return IE(this)};d.Xa=function(a){return WM(this,a)};d.Se=function(){return this};d.fe=function(a){return KE(this,a)};d.g=function(a){return WM(this,a|0)};d.Nd=function(a){return ME(this,a)};d.nc=function(a){return LE(this,a)};d.nb=function(a){return hF(this).hk(a)};d.l=function(){return JE(this)};d.Bb=function(){return this};d.db=function(){return this};
d.rc=function(a){return FN(this,a)};d.ob=function(){qE||(qE=(new pE).b());return qE};d.O=function(a){for(var b=0,c=this.Aa;b<c;)a.g(this.na.e[b]),b=1+b|0};d.kf=function(a,b){return QE(this,this.Aa,a,b)};d.ce=function(a,b){return OE(this,a,b)};d.Vg=function(a,b){return PE(this,a,b)};d.La=function(){return this};d.vi=function(){return this};d.E=function(){return Th(new Uh,this,0,this.Aa)};d.fd=function(a,b){lx(this,a,b)};d.kb=function(a){a=this.fo=a;this.na=k(r(p),[1<a?a:1]);this.Aa=0;return this};
d.W=function(){return this.Aa};d.yf=function(){return this};d.ke=function(){return this.Aa};d.Al=function(a){var b=WM(this,a);if(0>a||a>(this.Aa-1|0))throw(new Y).i("at "+a+" deleting 1");Na(this.na,a+1|0,this.na,a,this.Aa-(a+1|0)|0);a=this.Aa-1|0;for(kz(I(),a<=this.Aa);this.Aa>a;)this.Aa=-1+this.Aa|0,this.na.e[this.Aa]=null;return b};d.ae=function(a){return PE(this,a,this.Aa)};d.Ud=function(){return this};d.u=function(){return RE(this)};d.Gc=function(a){return vB(this,a|0)};
d.rb=function(a){return FN(this,a)};d.Kf=function(a,b,c){var e=Yt(P(),a)-b|0;c=c<e?c:e;e=this.Aa;c=c<e?c:e;0<c&&wA(yA(),this.na,0,a,b,c)};d.qc=function(a){a>this.Aa&&1<=a&&(a=k(r(p),[a]),Na(this.na,0,a,0,this.Aa),this.na=a)};d.r=function(){return Kw(U(),this)};d.fb=function(a){if(a&&a.$classData&&a.$classData.p.wf){var b=a.W();XM(this,this.Aa+b|0);a.Kf(this.na,this.Aa,b);this.Aa=this.Aa+b|0;a=this}else a=Tl(this,a);return a};d.Dd=function(){return"ArrayBuffer"};
d.$classData=l({Ay:0},!1,"scala.collection.mutable.ArrayBuffer",{Ay:1,wp:1,Gl:1,Oe:1,wa:1,xa:1,c:1,sa:1,ja:1,ka:1,ea:1,K:1,J:1,ga:1,ia:1,qa:1,ta:1,ra:1,pa:1,fa:1,ha:1,q:1,Od:1,Ta:1,T:1,ud:1,vd:1,xd:1,Il:1,Xj:1,Yj:1,Qj:1,Jl:1,Wj:1,Pj:1,Nh:1,xp:1,yp:1,Qd:1,Pd:1,El:1,ep:1,Na:1,Bp:1,Hl:1,wf:1,mi:1,Sd:1,zB:1,Ap:1,li:1,td:1,k:1,h:1});Vc||(Vc=(new Uc).b());ia(r(na),[]);
(function(){var a;Yb||(Yb=(new Xb).b());a=Yb;var b=ov(),c=[jj(),bd()];up(b,(new A).f(c));rp(ov(),t(new u,function(){return function(a){Ij||(Ij=(new Hj).b());Hz();Hz();Hz();var b=bn(),c=new Mu;c.ma=b;b=(Hz(),Gy().Y);Gj||(Gj=(new Fj).b());a=a.Lf;c=c.Wi(Sl(km(),Fo(a),Ro(a),Oo(a),b));gj().head.appendChild(c)}}(a)))})();$b||($b=(new Zb).b());var GN=$b.hh(),HN=gj().getElementById("container"),IN=sb().Y;GN.$h.g(lb.render(GN.I,HN,nd(rb(),IN)));

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(16)))

/***/ }),
/* 16 */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || Function("return this")() || (1,eval)("this");
} catch(e) {
	// This works if the window reference is available
	if(typeof window === "object")
		g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.2.0
 * react.production.min.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var m=__webpack_require__(2),n=__webpack_require__(3),p=__webpack_require__(1),q="function"===typeof Symbol&&Symbol["for"],r=q?Symbol["for"]("react.element"):60103,t=q?Symbol["for"]("react.call"):60104,u=q?Symbol["for"]("react.return"):60105,v=q?Symbol["for"]("react.portal"):60106,w=q?Symbol["for"]("react.fragment"):60107,x="function"===typeof Symbol&&Symbol.iterator;
function y(a){for(var b=arguments.length-1,e="Minified React error #"+a+"; visit http://facebook.github.io/react/docs/error-decoder.html?invariant\x3d"+a,c=0;c<b;c++)e+="\x26args[]\x3d"+encodeURIComponent(arguments[c+1]);b=Error(e+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings.");b.name="Invariant Violation";b.framesToPop=1;throw b;}
var z={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}};function A(a,b,e){this.props=a;this.context=b;this.refs=n;this.updater=e||z}A.prototype.isReactComponent={};A.prototype.setState=function(a,b){"object"!==typeof a&&"function"!==typeof a&&null!=a?y("85"):void 0;this.updater.enqueueSetState(this,a,b,"setState")};A.prototype.forceUpdate=function(a){this.updater.enqueueForceUpdate(this,a,"forceUpdate")};
function B(a,b,e){this.props=a;this.context=b;this.refs=n;this.updater=e||z}function C(){}C.prototype=A.prototype;var D=B.prototype=new C;D.constructor=B;m(D,A.prototype);D.isPureReactComponent=!0;function E(a,b,e){this.props=a;this.context=b;this.refs=n;this.updater=e||z}var F=E.prototype=new C;F.constructor=E;m(F,A.prototype);F.unstable_isAsyncReactComponent=!0;F.render=function(){return this.props.children};var G={current:null},H=Object.prototype.hasOwnProperty,I={key:!0,ref:!0,__self:!0,__source:!0};


function escape(a){var b={"\x3d":"\x3d0",":":"\x3d2"};return"$"+(""+a).replace(/[=:]/g,function(a){return b[a]})}var L=/\/+/g,M=[];function N(a,b,e,c){if(M.length){var d=M.pop();d.result=a;d.keyPrefix=b;d.func=e;d.context=c;d.count=0;return d}return{result:a,keyPrefix:b,func:e,context:c,count:0}}function O(a){a.result=null;a.keyPrefix=null;a.func=null;a.context=null;a.count=0;10>M.length&&M.push(a)}
function P(a,b,e,c){var d=typeof a;if("undefined"===d||"boolean"===d)a=null;var g=!1;if(null===a)g=!0;else switch(d){case "string":case "number":g=!0;break;case "object":switch(a.$$typeof){case r:case t:case u:case v:g=!0}}if(g)return e(c,a,""===b?"."+Q(a,0):b),1;g=0;b=""===b?".":b+":";if(Array.isArray(a))for(var k=0;

$$typeof:r,type:a.type,key:b,ref:a.ref,props:a.props,_owner:a._owner}),c.push(a))}function T(a,b,e,c,d){var g="";null!=e&&(g=(""+e).replace(L,"$\x26/")+"/");b=N(b,g,c,d);null==a||P(a,"",S,b);O(b)}
var U={Children:{map:function(a,b,e){if(null==a)return a;var c=[];T(a,c,null,b,e);return c},forEach:function(a,b,e){if(null==a)return a;b=N(null,null,b,e);null==a||P(a,"",R,b);O(b)},count:function(a){return null==a?0:P(a,"",p.thatReturnsNull,null)},toArray:function(a){var b=[];T(a,b,null,p.thatReturnsArgument);return b},only:function(a){K(a)?void 0:y("143");return a}},Component:A,PureComponent:B,unstable_AsyncComponent:E,Fragment:w,createElement:J,cloneElement:function(a,b,e){var c=m({},a.props),
d=a.key,g=a.ref,k=a._owner;if(null!=b){void 0!==b.ref&&(g=b.ref,k=G.current);void 0!==b.key&&(d=""+b.key);if(a.type&&a.type.defaultProps)var f=a.type.defaultProps;



/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/** @license React v16.2.0
 * react.development.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (process.env.NODE_ENV !== "production") {
  (function() {
'use strict';

var _assign = __webpack_require__(2);
var emptyObject = __webpack_require__(3);
var invariant = __webpack_require__(5);
var warning = __webpack_require__(6);
var emptyFunction = __webpack_require__(1);
var checkPropTypes = __webpack_require__(7);

// TODO: this is special because it gets imported during build.

var ReactVersion = '16.2.0';

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol['for'];

var REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;
var REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;
var REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;

var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';

function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable === 'undefined') {
    return null;
  }
  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }
  return null;
}

/**
 * WARNING: DO NOT manually require this module.
 * This is a replacement for `invariant(...)` used by the error code system
 * and will _only_ be required by the corresponding babel pass.
 * It always throws.
 */

/**
 * Forked from fbjs/warning:
 * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
 *
 * Only change is we use console.warn instead of console.error,
 * and do nothing when 'console' is not supported.
 * This really simplifies the code.
 * ---
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var lowPriorityWarning = function () {};

{
  var printWarning = function (format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.warn(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  lowPriorityWarning = function (condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }
    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

var lowPriorityWarning$1 = lowPriorityWarning;

var didWarnStateUpdateForUnmountedComponent = {};

function warnNoop(publicInstance, callerName) {
  {
    var constructor = publicInstance.constructor;
    var componentName = constructor && (constructor.displayName || constructor.name) || 'ReactClass';
    var warningKey = componentName + '.' + callerName;
    if (didWarnStateUpdateForUnmountedComponent[warningKey]) {
      return;
    }
    warning(false, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op.\n\nPlease check the code for the %s component.', callerName, callerName, componentName);
    didWarnStateUpdateForUnmountedComponent[warningKey] = true;
  }
}

/**
 * This is the abstract API for an update queue.
 */
var ReactNoopUpdateQueue = {
  /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function (publicInstance) {
    return false;
  },

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueForceUpdate: function (publicInstance, callback, callerName) {
    warnNoop(publicInstance, 'forceUpdate');
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueReplaceState: function (publicInstance, completeState, callback, callerName) {
    warnNoop(publicInstance, 'replaceState');
  },

  /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} Name of the calling function in the public API.
   * @internal
   */
  enqueueSetState: function (publicInstance, partialState, callback, callerName) {
    warnNoop(publicInstance, 'setState');
  }
};

/**
 * Base class helpers for the updating state of a component.
 */
function Component(props, context, updater) {
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

Component.prototype.isReactComponent = {};

/**
 * Sets a subset of the state. Always use this to mutate
 * state. You should treat `this.state` as immutable.
 *
 * There is no guarantee that `this.state` will be immediately updated, so
 * accessing `this.state` after calling this method may return the old value.
 *
 * There is no guarantee that calls to `setState` will run synchronously,
 * as they may eventually be batched together.  You can provide an optional
 * callback that will be executed when the call to setState is actually
 * completed.
 *
 * When a function is provided to setState, it will be called at some point in
 * the future (not synchronously). It will be called with the up to date
 * component arguments (state, props, context). These values can be different
 * from this.* because your function may be called after receiveProps but before
 * shouldComponentUpdate, and this new state, props, and context will not yet be
 * assigned to this.
 *
 * @param {object|function} partialState Next partial state or function to
 *        produce next partial state to be merged with current state.
 * @param {?function} callback Called after state is updated.
 * @final
 * @protected
 */
Component.prototype.setState = function (partialState, callback) {
  !(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null) ? invariant(false, 'setState(...): takes an object of state variables to update or a function which returns an object of state variables.') : void 0;
  this.updater.enqueueSetState(this, partialState, callback, 'setState');
};

/**
 * Forces an update. This should only be invoked when it is known with
 * certainty that we are **not** in a DOM transaction.
 *
 * You may want to call this when you know that some deeper aspect of the
 * component's state has changed but `setState` was not called.
 *
 * This will not invoke `shouldComponentUpdate`, but it will invoke
 * `componentWillUpdate` and `componentDidUpdate`.
 *
 * @param {?function} callback Called after update is complete.
 * @final
 * @protected
 */
Component.prototype.forceUpdate = function (callback) {
  this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');
};

/**
 * Deprecated APIs. These APIs used to exist on classic React classes but since
 * we would like to deprecate them, we're not going to move them over to this
 * modern base class. Instead, we define a getter that warns if it's accessed.
 */
{
  var deprecatedAPIs = {
    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']
  };
  var defineDeprecationWarning = function (methodName, info) {
    Object.defineProperty(Component.prototype, methodName, {
      get: function () {
        lowPriorityWarning$1(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);
        return undefined;
      }
    });
  };
  for (var fnName in deprecatedAPIs) {
    if (deprecatedAPIs.hasOwnProperty(fnName)) {
      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    }
  }
}

/**
 * Base class helpers for the updating state of a component.
 */
function PureComponent(props, context, updater) {
  // Duplicated from Component.
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

function ComponentDummy() {}
ComponentDummy.prototype = Component.prototype;
var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
pureComponentPrototype.constructor = PureComponent;
// Avoid an extra prototype jump for these methods.
_assign(pureComponentPrototype, Component.prototype);
pureComponentPrototype.isPureReactComponent = true;

function AsyncComponent(props, context, updater) {
  // Duplicated from Component.
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

var asyncComponentPrototype = AsyncComponent.prototype = new ComponentDummy();
asyncComponentPrototype.constructor = AsyncComponent;
// Avoid an extra prototype jump for these methods.
_assign(asyncComponentPrototype, Component.prototype);
asyncComponentPrototype.unstable_isAsyncReactComponent = true;
asyncComponentPrototype.render = function () {
  return this.props.children;
};

/**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 */
var ReactCurrentOwner = {
  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null
};

var hasOwnProperty = Object.prototype.hasOwnProperty;

var RESERVED_PROPS = {
  key: true,
  ref: true,
  __self: true,
  __source: true
};

var specialPropKeyWarningShown;
var specialPropRefWarningShown;

function hasValidRef(config) {
  {
    if (hasOwnProperty.call(config, 'ref')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;
      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }
  return config.ref !== undefined;
}

function hasValidKey(config) {
  {
    if (hasOwnProperty.call(config, 'key')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'key').get;
      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }
  return config.key !== undefined;
}

function defineKeyPropWarningGetter(props, displayName) {
  var warnAboutAccessingKey = function () {
    if (!specialPropKeyWarningShown) {
      specialPropKeyWarningShown = true;
      warning(false, '%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
    }
  };
  warnAboutAccessingKey.isReactWarning = true;
  Object.defineProperty(props, 'key', {
    get: warnAboutAccessingKey,
    configurable: true
  });
}

function defineRefPropWarningGetter(props, displayName) {
  var warnAboutAccessingRef = function () {
    if (!specialPropRefWarningShown) {
      specialPropRefWarningShown = true;
      warning(false, '%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
    }
  };
  warnAboutAccessingRef.isReactWarning = true;
  Object.defineProperty(props, 'ref', {
    get: warnAboutAccessingRef,
    configurable: true
  });
}

/**
 * Factory method to create a new React element. This no longer adheres to
 * the class pattern, so do not use new to call it. Also, no instanceof check
 * will work. Instead test $$typeof field against Symbol.for('react.element') to check
 * if something is a React Element.
 *
 * @param {*} type
 * @param {*} key
 * @param {string|object} ref
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @param {*} owner
 * @param {*} props
 * @internal
 */
var ReactElement = function (type, key, ref, self, source, owner, props) {
  var element = {
    // This tag allow us to uniquely identify this as a React Element
    $$typeof: REACT_ELEMENT_TYPE,

    // Built-in properties that belong on the element
    type: type,
    key: key,
    ref: ref,
    props: props,

    // Record the component responsible for creating this element.
    _owner: owner
  };

  {
    // The validation flag is currently mutative. We put it on
    // an external backing store so that we can freeze the whole object.
    // This can be replaced with a WeakMap once they are implemented in
    // commonly used development environments.
    element._store = {};

    // To make comparing ReactElements easier for testing purposes, we make
    // the validation flag non-enumerable (where possible, which should
    // include every environment we run tests in), so the test framework
    // ignores it.
    Object.defineProperty(element._store, 'validated', {
      configurable: false,
      enumerable: false,
      writable: true,
      value: false
    });
    // self and source are DEV only properties.
    Object.defineProperty(element, '_self', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: self
    });
    // Two elements created in two different places should be considered
    // equal for testing purposes and therefore we hide it from enumeration.
    Object.defineProperty(element, '_source', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: source
    });
    if (Object.freeze) {
      Object.freeze(element.props);
      Object.freeze(element);
    }
  }

  return element;
};

/**
 * Create and return a new ReactElement of the given type.
 * See https://reactjs.org/docs/react-api.html#createelement
 */
function createElement(type, config, children) {
  var propName;

  // Reserved names are extracted
  var props = {};

  var key = null;
  var ref = null;
  var self = null;
  var source = null;

  if (config != null) {
    if (hasValidRef(config)) {
      ref = config.ref;
    }
    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    self = config.__self === undefined ? null : config.__self;
    source = config.__source === undefined ? null : config.__source;
    // Remaining properties are added to a new props object
    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        props[propName] = config[propName];
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    {
      if (Object.freeze) {
        Object.freeze(childArray);
      }
    }
    props.children = childArray;
  }

  // Resolve default props
  if (type && type.defaultProps) {
    var defaultProps = type.defaultProps;
    for (propName in defaultProps) {
      if (props[propName] === undefined) {
        props[propName] = defaultProps[propName];
      }
    }
  }
  {
    if (key || ref) {
      if (typeof props.$$typeof === 'undefined' || props.$$typeof !== REACT_ELEMENT_TYPE) {
        var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;
        if (key) {
          defineKeyPropWarningGetter(props, displayName);
        }
        if (ref) {
          defineRefPropWarningGetter(props, displayName);
        }
      }
    }
  }
  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
}

/**
 * Return a function that produces ReactElements of a given type.
 * See https://reactjs.org/docs/react-api.html#createfactory
 */


function cloneAndReplaceKey(oldElement, newKey) {
  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);

  return newElement;
}

/**
 * Clone and return a new ReactElement using element as the starting point.
 * See https://reactjs.org/docs/react-api.html#cloneelement
 */
function cloneElement(element, config, children) {
  var propName;

  // Original props are copied
  var props = _assign({}, element.props);

  // Reserved names are extracted
  var key = element.key;
  var ref = element.ref;
  // Self is preserved since the owner is preserved.
  var self = element._self;
  // Source is preserved since cloneElement is unlikely to be targeted by a
  // transpiler, and the original source is probably a better indicator of the
  // true owner.
  var source = element._source;

  // Owner will be preserved, unless ref is overridden
  var owner = element._owner;

  if (config != null) {
    if (hasValidRef(config)) {
      // Silently steal the ref from the parent.
      ref = config.ref;
      owner = ReactCurrentOwner.current;
    }
    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    // Remaining properties override existing props
    var defaultProps;
    if (element.type && element.type.defaultProps) {
      defaultProps = element.type.defaultProps;
    }
    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        if (config[propName] === undefined && defaultProps !== undefined) {
          // Resolve default props
          props[propName] = defaultProps[propName];
        } else {
          props[propName] = config[propName];
        }
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    props.children = childArray;
  }

  return ReactElement(element.type, key, ref, self, source, owner, props);
}

/**
 * Verifies the object is a ReactElement.
 * See https://reactjs.org/docs/react-api.html#isvalidelement
 * @param {?object} object
 * @return {boolean} True if `object` is a valid component.
 * @final
 */
function isValidElement(object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
}

var ReactDebugCurrentFrame = {};

{
  // Component that is being worked on
  ReactDebugCurrentFrame.getCurrentStack = null;

  ReactDebugCurrentFrame.getStackAddendum = function () {
    var impl = ReactDebugCurrentFrame.getCurrentStack;
    if (impl) {
      return impl();
    }
    return null;
  };
}

var SEPARATOR = '.';
var SUBSEPARATOR = ':';

/**
 * Escape and wrap key so it is safe to use as a reactid
 *
 * @param {string} key to be escaped.
 * @return {string} the escaped key.
 */
function escape(key) {
  var escapeRegex = /[=:]/g;
  var escaperLookup = {
    '=': '=0',
    ':': '=2'
  };
  var escapedString = ('' + key).replace(escapeRegex, function (match) {
    return escaperLookup[match];
  });

  return '$' + escapedString;
}

/**
 * TODO: Test that a single child and an array with one item have the same key
 * pattern.
 */

var didWarnAboutMaps = false;

var userProvidedKeyEscapeRegex = /\/+/g;
function escapeUserProvidedKey(text) {
  return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');
}

var POOL_SIZE = 10;
var traverseContextPool = [];
function getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {
  if (traverseContextPool.length) {
    var traverseContext = traverseContextPool.pop();
    traverseContext.result = mapResult;
    traverseContext.keyPrefix = keyPrefix;
    traverseContext.func = mapFunction;
    traverseContext.context = mapContext;
    traverseContext.count = 0;
    return traverseContext;
  } else {
    return {
      result: mapResult,
      keyPrefix: keyPrefix,
      func: mapFunction,
      context: mapContext,
      count: 0
    };
  }
}

function releaseTraverseContext(traverseContext) {
  traverseContext.result = null;
  traverseContext.keyPrefix = null;
  traverseContext.func = null;
  traverseContext.context = null;
  traverseContext.count = 0;
  if (traverseContextPool.length < POOL_SIZE) {
    traverseContextPool.push(traverseContext);
  }
}

/**
 * @param {?*} children Children tree container.
 * @param {!string} nameSoFar Name of the key path so far.
 * @param {!function} callback Callback to invoke with each child found.
 * @param {?*} traverseContext Used to pass information throughout the traversal
 * process.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
  var type = typeof children;

  if (type === 'undefined' || type === 'boolean') {
    // All of the above are perceived as null.
    children = null;
  }

  var invokeCallback = false;

  if (children === null) {
    invokeCallback = true;
  } else {
    switch (type) {
      case 'string':
      case 'number':
        invokeCallback = true;
        break;
      case 'object':
        switch (children.$$typeof) {
          case REACT_ELEMENT_TYPE:
          case REACT_CALL_TYPE:
          case REACT_RETURN_TYPE:
          case REACT_PORTAL_TYPE:
            invokeCallback = true;
        }
    }
  }

  if (invokeCallback) {
    callback(traverseContext, children,
    // If it's the only child, treat the name as if it was wrapped in an array
    // so that it's consistent if the number of children grows.
    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
    return 1;
  }

  var child;
  var nextName;
  var subtreeCount = 0; // Count of children found in the current subtree.
  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

  if (Array.isArray(children)) {
    for (var i = 0; i < children.length; i++) {
      child = children[i];
      nextName = nextNamePrefix + getComponentKey(child, i);
      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
    }
  } else {
    var iteratorFn = getIteratorFn(children);
    if (typeof iteratorFn === 'function') {
      {
        // Warn about using Maps as children
        if (iteratorFn === children.entries) {
          warning(didWarnAboutMaps, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', ReactDebugCurrentFrame.getStackAddendum());
          didWarnAboutMaps = true;
        }
      }

      var iterator = iteratorFn.call(children);
      var step;
      var ii = 0;
      while (!(step = iterator.next()).done) {
        child = step.value;
        nextName = nextNamePrefix + getComponentKey(child, ii++);
        subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
      }
    } else if (type === 'object') {
      var addendum = '';
      {
        addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();
      }
      var childrenString = '' + children;
      invariant(false, 'Objects are not valid as a React child (found: %s).%s', childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString, addendum);
    }
  }

  return subtreeCount;
}

/**
 * Traverses children that are typically specified as `props.children`, but
 * might also be specified through attributes:
 *
 * - `traverseAllChildren(this.props.children, ...)`
 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
 *
 * The `traverseContext` is an optional argument that is passed through the
 * entire traversal. It can be used to store accumulations or anything else that
 * the callback might find relevant.
 *
 * @param {?*} children Children tree object.
 * @param {!function} callback To invoke upon traversing each child.
 * @param {?*} traverseContext Context for traversal.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildren(children, callback, traverseContext) {
  if (children == null) {
    return 0;
  }

  return traverseAllChildrenImpl(children, '', callback, traverseContext);
}

/**
 * Generate a key string that identifies a component within a set.
 *
 * @param {*} component A component that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */
function getComponentKey(component, index) {
  // Do some typechecking here since we call this blindly. We want to ensure
  // that we don't block potential future ES APIs.
  if (typeof component === 'object' && component !== null && component.key != null) {
    // Explicit key
    return escape(component.key);
  }
  // Implicit key determined by the index in the set
  return index.toString(36);
}

function forEachSingleChild(bookKeeping, child, name) {
  var func = bookKeeping.func,
      context = bookKeeping.context;

  func.call(context, child, bookKeeping.count++);
}

/**
 * Iterates through children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.foreach
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc
 * @param {*} forEachContext Context for forEachContext.
 */
function forEachChildren(children, forEachFunc, forEachContext) {
  if (children == null) {
    return children;
  }
  var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);
  traverseAllChildren(children, forEachSingleChild, traverseContext);
  releaseTraverseContext(traverseContext);
}

function mapSingleChildIntoContext(bookKeeping, child, childKey) {
  var result = bookKeeping.result,
      keyPrefix = bookKeeping.keyPrefix,
      func = bookKeeping.func,
      context = bookKeeping.context;


  var mappedChild = func.call(context, child, bookKeeping.count++);
  if (Array.isArray(mappedChild)) {
    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, emptyFunction.thatReturnsArgument);
  } else if (mappedChild != null) {
    if (isValidElement(mappedChild)) {
      mappedChild = cloneAndReplaceKey(mappedChild,
      // Keep both the (mapped) and old keys if they differ, just as
      // traverseAllChildren used to do for objects as children
      keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);
    }
    result.push(mappedChild);
  }
}

function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
  var escapedPrefix = '';
  if (prefix != null) {
    escapedPrefix = escapeUserProvidedKey(prefix) + '/';
  }
  var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);
  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
  releaseTraverseContext(traverseContext);
}

/**
 * Maps children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.map
 *
 * The provided mapFunction(child, key, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} func The map function.
 * @param {*} context Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */
function mapChildren(children, func, context) {
  if (children == null) {
    return children;
  }
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, func, context);
  return result;
}

/**
 * Count the number of children that are typically specified as
 * `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.count
 *
 * @param {?*} children Children tree container.
 * @return {number} The number of children.
 */
function countChildren(children, context) {
  return traverseAllChildren(children, emptyFunction.thatReturnsNull, null);
}

/**
 * Flatten a children object (typically specified as `props.children`) and
 * return an array with appropriately re-keyed children.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.toarray
 */
function toArray(children) {
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, emptyFunction.thatReturnsArgument);
  return result;
}

/**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.only
 *
 * The current implementation of this function assumes that a single child gets
 * passed without a wrapper, but the purpose of this helper function is to
 * abstract away the particular structure of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactElement} The first and only `ReactElement` contained in the
 * structure.
 */
function onlyChild(children) {
  !isValidElement(children) ? invariant(false, 'React.Children.only expected to receive a single React element child.') : void 0;
  return children;
}

var describeComponentFrame = function (name, source, ownerName) {
  return '\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');
};

function getComponentName(fiber) {
  var type = fiber.type;

  if (typeof type === 'string') {
    return type;
  }
  if (typeof type === 'function') {
    return type.displayName || type.name;
  }
  return null;
}

/**
 * ReactElementValidator provides a wrapper around a element factory
 * which validates the props passed to the element. This is intended to be
 * used only in DEV and could be replaced by a static type checker for languages
 * that support it.
 */

{
  var currentlyValidatingElement = null;

  var propTypesMisspellWarningShown = false;

  var getDisplayName = function (element) {
    if (element == null) {
      return '#empty';
    } else if (typeof element === 'string' || typeof element === 'number') {
      return '#text';
    } else if (typeof element.type === 'string') {
      return element.type;
    } else if (element.type === REACT_FRAGMENT_TYPE) {
      return 'React.Fragment';
    } else {
      return element.type.displayName || element.type.name || 'Unknown';
    }
  };

  var getStackAddendum = function () {
    var stack = '';
    if (currentlyValidatingElement) {
      var name = getDisplayName(currentlyValidatingElement);
      var owner = currentlyValidatingElement._owner;
      stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner));
    }
    stack += ReactDebugCurrentFrame.getStackAddendum() || '';
    return stack;
  };

  var VALID_FRAGMENT_PROPS = new Map([['children', true], ['key', true]]);
}

function getDeclarationErrorAddendum() {
  if (ReactCurrentOwner.current) {
    var name = getComponentName(ReactCurrentOwner.current);
    if (name) {
      return '\n\nCheck the render method of `' + name + '`.';
    }
  }
  return '';
}

function getSourceInfoErrorAddendum(elementProps) {
  if (elementProps !== null && elementProps !== undefined && elementProps.__source !== undefined) {
    var source = elementProps.__source;
    var fileName = source.fileName.replace(/^.*[\\\/]/, '');
    var lineNumber = source.lineNumber;
    return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
  }
  return '';
}

/**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */
var ownerHasKeyUseWarning = {};

function getCurrentComponentErrorInfo(parentType) {
  var info = getDeclarationErrorAddendum();

  if (!info) {
    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
    if (parentName) {
      info = '\n\nCheck the top-level render call using <' + parentName + '>.';
    }
  }
  return info;
}

/**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it. Error statuses are cached so a warning
 * will only be shown once.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */
function validateExplicitKey(element, parentType) {
  if (!element._store || element._store.validated || element.key != null) {
    return;
  }
  element._store.validated = true;

  var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
  if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
    return;
  }
  ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

  // Usually the current owner is the offender, but if it accepts children as a
  // property, it may be the creator of the child that's responsible for
  // assigning it a key.
  var childOwner = '';
  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
    // Give the component that originally created this child.
    childOwner = ' It was passed a child from ' + getComponentName(element._owner) + '.';
  }

  currentlyValidatingElement = element;
  {
    warning(false, 'Each child in an array or iterator should have a unique "key" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.%s', currentComponentErrorInfo, childOwner, getStackAddendum());
  }
  currentlyValidatingElement = null;
}

/**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */
function validateChildKeys(node, parentType) {
  if (typeof node !== 'object') {
    return;
  }
  if (Array.isArray(node)) {
    for (var i = 0; i < node.length; i++) {
      var child = node[i];
      if (isValidElement(child)) {
        validateExplicitKey(child, parentType);
      }
    }
  } else if (isValidElement(node)) {
    // This element was passed in a valid location.
    if (node._store) {
      node._store.validated = true;
    }
  } else if (node) {
    var iteratorFn = getIteratorFn(node);
    if (typeof iteratorFn === 'function') {
      // Entry iterators used to provide implicit keys,
      // but now we print a separate warning for them later.
      if (iteratorFn !== node.entries) {
        var iterator = iteratorFn.call(node);
        var step;
        while (!(step = iterator.next()).done) {
          if (isValidElement(step.value)) {
            validateExplicitKey(step.value, parentType);
          }
        }
      }
    }
  }
}

/**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */
function validatePropTypes(element) {
  var componentClass = element.type;
  if (typeof componentClass !== 'function') {
    return;
  }
  var name = componentClass.displayName || componentClass.name;
  var propTypes = componentClass.propTypes;
  if (propTypes) {
    currentlyValidatingElement = element;
    checkPropTypes(propTypes, element.props, 'prop', name, getStackAddendum);
    currentlyValidatingElement = null;
  } else if (componentClass.PropTypes !== undefined && !propTypesMisspellWarningShown) {
    propTypesMisspellWarningShown = true;
    warning(false, 'Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');
  }
  if (typeof componentClass.getDefaultProps === 'function') {
    warning(componentClass.getDefaultProps.isReactClassApproved, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.');
  }
}

/**
 * Given a fragment, validate that it can only be provided with fragment props
 * @param {ReactElement} fragment
 */
function validateFragmentProps(fragment) {
  currentlyValidatingElement = fragment;

  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = Object.keys(fragment.props)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var key = _step.value;

      if (!VALID_FRAGMENT_PROPS.has(key)) {
        warning(false, 'Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.%s', key, getStackAddendum());
        break;
      }
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator['return']) {
        _iterator['return']();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  if (fragment.ref !== null) {
    warning(false, 'Invalid attribute `ref` supplied to `React.Fragment`.%s', getStackAddendum());
  }

  currentlyValidatingElement = null;
}

function createElementWithValidation(type, props, children) {
  var validType = typeof type === 'string' || typeof type === 'function' || typeof type === 'symbol' || typeof type === 'number';
  // We warn in this case but don't throw. We expect the element creation to
  // succeed and there will likely be errors in render.
  if (!validType) {
    var info = '';
    if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
      info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
    }

    var sourceInfo = getSourceInfoErrorAddendum(props);
    if (sourceInfo) {
      info += sourceInfo;
    } else {
      info += getDeclarationErrorAddendum();
    }

    info += getStackAddendum() || '';

    warning(false, 'React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', type == null ? type : typeof type, info);
  }

  var element = createElement.apply(this, arguments);

  // The result can be nullish if a mock or a custom function is used.
  // TODO: Drop this when these are no longer allowed as the type argument.
  if (element == null) {
    return element;
  }

  // Skip key warning if the type isn't valid since our key validation logic
  // doesn't expect a non-string/function type and can throw confusing errors.
  // We don't want exception behavior to differ between dev and prod.
  // (Rendering will throw with a helpful message and as soon as the type is
  // fixed, the key warnings will appear.)
  if (validType) {
    for (var i = 2; i < arguments.length; i++) {
      validateChildKeys(arguments[i], type);
    }
  }

  if (typeof type === 'symbol' && type === REACT_FRAGMENT_TYPE) {
    validateFragmentProps(element);
  } else {
    validatePropTypes(element);
  }

  return element;
}

function createFactoryWithValidation(type) {
  var validatedFactory = createElementWithValidation.bind(null, type);
  // Legacy hook TODO: Warn if this is accessed
  validatedFactory.type = type;

  {
    Object.defineProperty(validatedFactory, 'type', {
      enumerable: false,
      get: function () {
        lowPriorityWarning$1(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');
        Object.defineProperty(this, 'type', {
          value: type
        });
        return type;
      }
    });
  }

  return validatedFactory;
}

function cloneElementWithValidation(element, props, children) {
  var newElement = cloneElement.apply(this, arguments);
  for (var i = 2; i < arguments.length; i++) {
    validateChildKeys(arguments[i], newElement.type);
  }
  validatePropTypes(newElement);
  return newElement;
}

var React = {
  Children: {
    map: mapChildren,
    forEach: forEachChildren,
    count: countChildren,
    toArray: toArray,
    only: onlyChild
  },

  Component: Component,
  PureComponent: PureComponent,
  unstable_AsyncComponent: AsyncComponent,

  Fragment: REACT_FRAGMENT_TYPE,

  createElement: createElementWithValidation,
  cloneElement: cloneElementWithValidation,
  createFactory: createFactoryWithValidation,
  isValidElement: isValidElement,

  version: ReactVersion,

  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
    ReactCurrentOwner: ReactCurrentOwner,
    // Used by renderers to avoid bundling object-assign twice in UMD bundles:
    assign: _assign
  }
};

{
  _assign(React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, {
    // These should not be included in production.
    ReactDebugCurrentFrame: ReactDebugCurrentFrame,
    // Shim for React DOM 16.0.0 which still destructured (but not used) this.
    // TODO: remove in React 17.0.
    ReactComponentTreeHook: {}
  });
}



var React$2 = Object.freeze({
	default: React
});

var React$3 = ( React$2 && React ) || React$2;

// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var react = React$3['default'] ? React$3['default'] : React$3;

module.exports = react;
  })();
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;


/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

function checkDCE() {
  /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */
  if (
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined' ||
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== 'function'
  ) {
    return;
  }
  if (process.env.NODE_ENV !== 'production') {
    // This branch is unreachable because this function is only called
    // in production, but the condition is true only in development.
    // Therefore if the branch is still here, dead code elimination wasn't
    // properly applied.
    // Don't change the message. React DevTools relies on it. Also make sure
    // this message doesn't occur elsewhere in this function, or it will cause
    // a false positive.
    throw new Error('^_^');
  }
  try {
    // Verify that the code above has been dead code eliminated (DCE'd).
    __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
  } catch (err) {
    // DevTools shouldn't crash React, no matter what.
    // We should still report in case we break this code.
    console.error(err);
  }
}

if (process.env.NODE_ENV === 'production') {
  // DCE check should happen before ReactDOM bundle executes so that
  // DevTools can report bad minification during injection.
  checkDCE();
  module.exports = __webpack_require__(21);
} else {
  module.exports = __webpack_require__(24);
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 21 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.2.0
 * react-dom.production.min.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

/*
 Modernizr 3.0.0pre (Custom Build) | MIT
*/


d<b;d++)c+="\x26args[]\x3d"+encodeURIComponent(arguments[d+1]);b=Error(c+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings.");b.name="Invariant Violation";b.framesToPop=1;throw b;}aa?void 0:E("227");
var oa={children:!0,dangerouslySetInnerHTML:!0,defaultValue:!0,defaultChecked:!0,innerHTML:!0,suppressContentEditableWarning:!0,suppressHydrationWarning:!0,style:!0};function pa(a,b){return(a&b)===b}
var ta={MUST_USE_PROPERTY:1,HAS_BOOLEAN_VALUE:4,HAS_NUMERIC_VALUE:8,HAS_POSITIVE_NUMERIC_VALUE:24,HAS_OVERLOADED_BOOLEAN_VALUE:32,HAS_STRING_BOOLEAN_VALUE:64,injectDOMPropertyConfig:function(a){var b=ta,c=a.Properties||{},d=a.DOMAttributeNamespaces||{},e=a.DOMAttributeNames||{};a=a.DOMMutationMethods||{};for(var f in c){ua.hasOwnProperty(f)?E("48",f):void 0;var g=f.toLowerCase(),h=c[f];g={attributeName:g,attributeNamespace:null,propertyName:f,mutationMethod:null,mustUseProperty:pa(h,b.MUST_USE_PROPERTY),


;1>=g.hasBooleanValue+g.hasNumericValue+g.hasOverloadedBooleanValue?void 0:E("50",f);e.hasOwnProperty(f)&&(g.attributeName=e[f]);d.hasOwnProperty(f)&&(g.attributeNamespace=d[f]);a.hasOwnProperty(f)&&(g.mutationMethod=a[f]);ua[f]=g}}},ua={};
function va(a,b){if(oa.hasOwnProperty(a)||2<a.length&&("o"===a[0]||"O"===a[0])&&("n"===a[1]||"N"===a[1]))return!1;if(null===b)return!0;switch(typeof b){case "boolean":return oa.hasOwnProperty(a)?a=!0:(b=wa(a))?a=b.hasBooleanValue||b.hasStringBooleanValue||b.hasOverloadedBooleanValue:(a=a.toLowerCase().slice(0,5),a="data-"===a||"aria-"===a),a;case "undefined":case "number":case "string":case "object":return!0;default:return!1}}function wa(a){return ua.hasOwnProperty(a)?ua[a]:null}
var xa=ta,

 x-height xlink:actuate xlink:arcrole xlink:href xlink:role xlink:show xlink:title xlink:type xml:base xmlns:xlink xml:lang xml:space".split(" ").forEach(function(a){var b=a.replace(Ha,
Ia);Ga.Properties[b]=0;Ga.DOMAttributeNames[b]=a});xa.injectDOMPropertyConfig(Da);xa.injectDOMPropertyConfig(Ga);
var P={_caughtError:null,_hasCaughtError:!1,_rethrowError:null,_hasRethrowError:!1,injection:{injectErrorUtils:function(a){"function"!==typeof a.invokeGuardedCallback?E("197"):void 0;Ja=a.invokeGuardedCallback}},invokeGuardedCallback:function(a,b,c,d,e,f,g,h,k){Ja.apply(P,arguments)},invokeGuardedCallbackAndCatchFirstError:function(a,b,c,d,e,f,g,h,k){P.invokeGuardedCallback.apply(this,arguments);if(P.hasCaughtError()){var q=P.clearCaughtError();P._hasRethrowError||(P._hasRethrowError=!0,P._rethrowError=


P._caughtError=null;P._hasCaughtError=!1;return a}E("198")}};function Ja(a,b,c,d,e,f,g,h,k){P._hasCaughtError=!1;P._caughtError=null;var q=Array.prototype.slice.call(arguments,3);try{b.apply(c,q)}catch(v){P._caughtError=v,P._hasCaughtError=!0}}
function Ka(){if(P._hasRethrowError){var a=P._rethrowError;P._rethrowError=null;P._hasRethrowError=!1;throw a;}}var La=null,Ma={};
function Na(){if(La)for(var a in Ma){var b=Ma[a],c=La.indexOf(a);-1<c?void 0:E("96",a);if(!Oa[c]){b.extractEvents?void 0:E("97",a);Oa[c]=b;c=b.eventTypes;for(var d in c){var e=void 0;var f=c[d],g=b,h=d;Pa.hasOwnProperty(h)?E("99",h):void 0;Pa[h]=f;var k=f.phasedRegistrationNames;if(k){for(e in k)k.hasOwnProperty(e)&&Qa(k[e],g,h);e=!0}else f.registrationName?(Qa(f.registrationName,g,h),e=!0):e=!1;e?void 0:E("98",d,a)}}}}
function Qa(a,b,c){Ra[a]?E("100",a):void 0;Ra[a]=b;Sa[a]=b.eventTypes[c].dependencies}var Oa=[],Pa={},Ra={},Sa={};function Ta(a){La?E("101"):void 0;La=Array.prototype.slice.call(a);Na()}function Ua(a){var b=!1,c;for(c in a)if(a.hasOwnProperty(c)){var d=a[c];Ma.hasOwnProperty(c)&&Ma[c]===d||(Ma[c]?E("102",c):void 0,Ma[c]=d,b=!0)}b&&Na()}
var Va=Object.freeze({

function $a(a,b){null==b?E("30"):void 0;if(null==a)return b;if(Array.isArray(a)){if(Array.isArray(b))return a.push.apply(a,b),a;a.push(b);return a}return Array.isArray(b)?[a].concat(b):[a,b]}function ab(a,b,c){Array.isArray(a)?a.forEach(b,c):a&&b.call(c,a)}var bb=null;


a._dispatchListeners=null;a._dispatchInstances=null;a.isPersistent()||a.constructor.release(a)}}function db(a){return cb(a,!0)}function gb(a){return cb(a,!1)}var hb={injectEventPluginOrder:Ta,injectEventPluginsByName:Ua};
function ib(a,b){

return c}function jb(a,b,c,d){for(var e,f=0;f<Oa.length;f++){var g=Oa[f];g&&(g=g.extractEvents(a,b,c,d))&&(e=$a(e,g))}return e}function kb(a){a&&(bb=$a(bb,a))}function lb(a){var b=bb;bb=null;b&&(a?ab(b,db):ab(b,gb),bb?E("95"):void 0,P.rethrowCaughtError())}var mb=Object.freeze({injection:hb,getListener:ib,extractEvents:jb,enqueueEvents:kb,processEventQueue:lb}),nb=Math.random().toString(36).slice(2),Q="__reactInternalInstance$"+nb,ob="__reactEventHandlers$"+nb;
function pb(a){if(a[Q])return a[Q];for(var b=[];!a[Q];)if(b.push(a),a.parentNode)a=a.parentNode;else return null;var c=void 0,d=a[Q];if(5===d.tag||6===d.tag)return d;for(;a&&(d=a[Q]);a=b.pop())c=d;return c}function qb(a){if(5===a.tag||6===a.tag)return a.stateNode;E("33")}function rb(a){return a[ob]||null}
var sb=Object.freeze({

do a=a["return"];while(a&&5!==a.tag);return a?a:null}function ub(a,b,c){for(var d=[];a;)d.push(a),a=tb(a);for(a=d.length;0<a--;)b(d[a],"captured",c);for(a=0;a<d.length;a++)b(d[a],"bubbled",c)}
function vb(a,b,c){if(b=ib(a,c.dispatchConfig.phasedRegistrationNames[b]))c._dispatchListeners=$a(c._dispatchListeners,b),c._dispatchInstances=$a(c._dispatchInstances,a)}function wb(a){a&&a.dispatchConfig.phasedRegistrationNames&&ub(a._targetInst,vb,a)}

if(c&&d)a:{var e=c;for(var f=d,g=0,h=e;h;h=tb(h))g++;h=0;for(var k=f;k;k=tb(k))h++;for(;0<g-h;)e=tb(e),g--;for(;0<h-g;)f=tb(f),h--;for(;g--;){if(e===f||e===f.alternate)break a;e=tb(e);f=tb(f)}e=null}else e=null;f=e;for(e=[];c&&c!==f;){g=c.alternate;if(null!==g&&g===f)break;e.push(c);c=tb(c)}for(c=[];d&&d!==f;){g=d.alternate;if(null!==g&&g===f)break;c.push(d);d=tb(d)}for(d=0;d<e.length;d++)yb(e[d],"bubbled",a);for(a=c.length;0<a--;)yb(c[a],"captured",b)}
var Cb=Object.freeze({accumulateTwoPhaseDispatches:Ab,accumulateTwoPhaseDispatchesSkipTarget:function(a){ab(a,xb)},accumulateEnterLeaveDispatches:Bb,accumulateDirectDispatches:function(a){ab(a,zb)}}),Db=null;function Eb(){!Db&&l.canUseDOM&&(Db="textContent"in document.documentElement?"textContent":"innerText");return Db}var S={_root:null,_startText:null,_fallbackText:null};
function Fb(){if(S._fallbackText)return S._fallbackText;var a,b=S._startText,c=b.length,d,e=Gb(),f=e.length;for(a=0;a<c&&b[a]===e[a];a++);var g=c-a;for(d=1;d<=g&&b[c-d]===e[f-d];d++);S._fallbackText=e.slice(a,1<d?1-d:void 0);return S._fallbackText}function Gb(){return"value"in S._root?S._root.value:S._root[Eb()]}


function T(a,b,c,d){this.dispatchConfig=a;this._targetInst=b;this.nativeEvent=c;a=this.constructor.Interface;for(var e in a)a.hasOwnProperty(e)&&((b=a[e])?this[e]=b(c):"target"===e?this.target=d:this[e]=c[e]);this.isDefaultPrevented=(null!=c.defaultPrevented?c.defaultPrevented:!1===c.returnValue)?C.thatReturnsTrue:C.thatReturnsFalse;this.isPropagationStopped=C.thatReturnsFalse;return this}
B(T.prototype,{

});T.Interface=Ib;T.augmentClass=function(a,b){function c(){}c.prototype=this.prototype;var d=new c;B(d,a.prototype);a.prototype=d;a.prototype.constructor=a;a.Interface=B({},this.Interface,b);a.augmentClass=this.augmentClass;Jb(a)};Jb(T);function Kb(a,b,c,d){if(this.eventPool.length){var e=this.eventPool.pop();this.call(e,a,b,c,d);return e}return new this(a,b,c,d)}
function Lb(a){a instanceof this?void 0:E("223");a.destructor();10>this.eventPool.length&&this.eventPool.push(a)}function Jb(a){a.eventPool=[];a.getPooled=Kb;a.release=Lb}function Mb(a,b,c,d){

if(Xb=l.canUseDOM&&"TextEvent"in window&&!Wb){var Yb=window.opera;Xb=!("object"===typeof Yb&&"function"===typeof Yb.version&&12>=parseInt(Yb.version(),10))}
var Zb=Xb,$b=l.canUseDOM&&(!Vb||Wb&&8<Wb&&11>=Wb),ac=String.fromCharCode(32),bc={

,compositionStart:{phasedRegistrationNames:{bubbled:"onCompositionStart",
captured:"onCompositionStartCapture"},dependencies:"topBlur topCompositionStart topKeyDown topKeyPress topKeyUp topMouseDown".split(" ")},compositionUpdate:{phasedRegistrationNames:{bubbled:"onCompositionUpdate",captured:"onCompositionUpdateCapture"},dependencies:"topBlur topCompositionUpdate topKeyDown topKeyPress topKeyUp topMouseDown".split(" ")}},cc=!1;
function dc(a,b){switch(a){case "topKeyUp":return-1!==Pb.indexOf(b.keyCode);case "topKeyDown":return 229!==b.keyCode;case "topKeyPress":case "topMouseDown":case "topBlur":return!0;default:return!1}}function ec(a){a=a.detail;return"object"===typeof a&&"data"in a?a.data:null}var fc=!1;function gc(a,b){switch(a){case "topCompositionEnd":return ec(b);case "topKeyPress":if(32!==b.which)return null;cc=!0;return ac;case "topTextInput":return a=b.data,a===ac&&cc?null:a;default:return null}}


e:(e=ec(c),null!==e&&(f.data=e)),Ab(f),e=f):e=null;(a=Zb?gc(a,c):hc(a,c))?(b=Nb.getPooled(bc.beforeInput,b,c,d),b.data=a,Ab(b)):b=null;return[e,b]}},jc=null,kc=null,lc=null;function mc(a){if(a=Xa(a)){jc&&"function"===typeof jc.restoreControlledState?void 0:E("194");var b=Wa(a.stateNode);jc.restoreControlledState(a.stateNode,a.type,b)}}var nc={injectFiberControlledHostComponent:function(a){jc=a}};function oc(a){kc?lc?lc.push(a):lc=[a]:kc=a}
function pc(){if(kc){var a=kc,b=lc;lc=kc=null;mc(a);if(b)for(a=0;a<b.length;a++)mc(b[a])}}var qc=Object.freeze({injection:nc,enqueueStateRestore:oc,restoreStateIfNeeded:pc});function rc(a,b){return a(b)}var sc=!1;function tc(a,b){if(sc)return rc(a,b);sc=!0;try{return rc(a,b)}finally{sc=!1,pc()}}

function wc(a){a=a.target||a.srcElement||window;a.correspondingUseElement&&(a=a.correspondingUseElement);return 3===a.nodeType?a.parentNode:a}var xc;l.canUseDOM&&(xc=document.implementation&&document.implementation.hasFeature&&!0!==document.implementation.hasFeature("",""));


var d="";a&&(d=zc(a)?a.checked?"true":"false":a.value);a=d;return a!==c?(b.setValue(a),!0):!1}var Dc={change:{phasedRegistrationNames:{bubbled:"onChange",captured:"onChangeCapture"},dependencies:"topBlur topChange topClick topFocus topInput topKeyDown topKeyUp topSelectionChange".split(" ")}};
function Ec(a,b,c){a=T.getPooled(Dc.change,a,b,c);a.type="change";oc(c);Ab(a);return a}var Fc=null,Gc=null;function Hc(a){kb(a);lb(!1)}

"topMouseOut"===a?(a=b,b=(b=c.relatedTarget||c.toElement)?pb(b):null):a=null;if(a===
b)return null;var f=null==a?e:qb(a);e=null==b?e:qb(b);var g=fd.getPooled(gd.mouseLeave,a,c,d);g.type="mouseleave";g.target=f;g.relatedTarget=e;c=fd.getPooled(gd.mouseEnter,b,c,d);c.type="mouseenter";c.target=e;c.relatedTarget=f;Bb(g,c,a,b);return[g,c]}},id=aa.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner;function jd(a){a=a.type;return"string"===typeof a?a:"function"===typeof a?a.displayName||a.name:null}
function kd(a){var b=a;if(a.alternate)for(;b["return"];)b=b["return"];else{if(0!==(b.effectTag&2))return 1;for(;b["return"];)if(b=b["return"],0!==(b.effectTag&2))return 1}return 3===b.tag?2:3}function ld(a){return(a=a._reactInternalFiber)?2===kd(a):!1}function md(a){2!==kd(a)?E("188"):void 0}
function nd(a){var b=a.alternate;if(!b)return b=kd(a),3===b?E("188"):void 0,1===b?null:a;for(var c=a,d=b;;){var e=c["return"],f=e?e.alternate:null;if(!e||!f)break;if(e.child===f.child){for(var g=e.child;g;){if(g===c)return md(e),a;if(g===d)return md(e),b;g=g.sibling}E("188")}if(c["return"]!==d["return"])c=e,d=f;else{g=!1;for(var h=e.child;h;){if(h===c){g=!0;c=e;d=f;break}if(h===d){g=!0;d=e;c=f;break}h=h.sibling}if(!g){for(h=f.child;h;){if(h===c){g=!0;c=f;d=e;break}if(h===d){g=!0;d=f;c=e;break}h=h.sibling}g?
void 0:E("189")}}c.alternate!==d?E("190"):void 0}3!==c.tag?E("188"):void 0;return c.stateNode.current===c?a:b}function od(a){a=nd(a);if(!a)return null;for(var b=a;;){if(5===b.tag||6===b.tag)return b;if(b.child)b.child["return"]=b,b=b.child;else{if(b===a)break;for(;!b.sibling;){if(!b["return"]||b["return"]===a)return null;b=b["return"]}b.sibling["return"]=b["return"];b=b.sibling}}return null}
function pd(a){a=nd(a);if(!a)return null;for(var b=a;;){if(5===b.tag||6===b.tag)return b;if(b.child&&4!==b.tag)b.child["return"]=b,b=b.child;else{if(b===a)break;for(;!b.sibling;){if(!b["return"]||b["return"]===a)return null;b=b["return"]}b.sibling["return"]=b["return"];b=b.sibling}}return null}var qd=[];
function rd(a){var b=a.targetInst;do{if(!b){a.ancestors.push(b);break}var c;for(c=b;c["return"];)c=c["return"];c=3!==c.tag?null:c.stateNode.containerInfo;if(!c)break;a.ancestors.push(b);b=pb(c)}while(b);for(c=0;c<a.ancestors.length;c++)b=a.ancestors[c],sd(a.topLevelType,b,a.nativeEvent,wc(a.nativeEvent))}var td=!0,sd=void 0;function ud(a){td=!!a}function U(a,b,c){return c?ba.listen(c,b,vd.bind(null,a)):null}function wd(a,b,c){return c?ba.capture(c,b,vd.bind(null,a)):null}
function vd(a,b){if(td){var c=wc(b);c=pb(c);null===c||"number"!==typeof c.tag||2===kd(c)||(c=null);if(qd.length){var d=qd.pop();d.topLevelType=a;d.nativeEvent=b;d.targetInst=c;a=d}else a={topLevelType:a,nativeEvent:b,targetInst:c,ancestors:[]};try{tc(rd,a)}finally{a.topLevelType=null,a.nativeEvent=null,a.targetInst=null,a.ancestors.length=0,10>qd.length&&qd.push(a)}}}
var xd=Object.freeze({get _enabled(){return td},get _handleTopLevel(){return sd},setHandleTopLevel:function(a){sd=a},setEnabled:ud,isEnabled:function(){return td},trapBubbledEvent:U,trapCapturedEvent:wd,dispatchEvent:vd});function yd(a,b){var c={};c[a.toLowerCase()]=b.toLowerCase();c["Webkit"+a]="webkit"+b;c["Moz"+a]="moz"+b;c["ms"+a]="MS"+b;c["O"+a]="o"+b.toLowerCase();return c}
var zd={

var Dd={topAbort:"abort",topAnimationEnd:Cd("animationend")||"animationend",topAnimationIteration:Cd("animationiteration")||"animationiteration",topAnimationStart:Cd("animationstart")||"animationstart",topBlur:"blur",topCancel:"cancel",topCanPlay:"canplay",topCanPlayThrough:"canplaythrough",topChange:"change",topClick:"click",topClose:"close

Object.prototype.hasOwnProperty.call(a,Gd)||(a[Gd]=Fd++,Ed[a[Gd]]={});return Ed[a[Gd]]}function Id(a){for(;a&&a.firstChild;)a=a.firstChild;return a}
function Jd(a,b){var c=Id(a);a=0;for(var d;c;){if(3===c.nodeType){d=a+c.textContent.length;if(a<=b&&d>=b)return{

var Sd={eventTypes:Md,extractEvents:function(a,b,c,d){var e=d.window===d?d.document:9===d.nodeType?d:d.ownerDocument,f;if(!(f=!e)){a:{e=Hd(e);f=Sa.onSelect;for(var g=0;g<f.length;g++){var h=f[g];if(!e.hasOwnProperty(h)||!e[h]){e=!1;break a}}e=!0}f=!e}if(f)return null;e=b?qb(b):window;switch(a){case "topFocus":if(vc(e)||"true"===e.contentEditable)Nd=e,Od=b,Pd=null;break;

animationName:null,elapsedTime:null,pseudoElement:null});function Ud(a,b,c,d){return T.call(this,a,b,c,d)}T.augmentClass(Ud,{clipboardData:function(a){return"clipboardData"in a?a.clipboardData:window.clipboardData}});function Vd(a,b,c,d){return T.call(this,a,b,c,d)}bd.augmentClass(Vd,{relatedTarget:null});
function Wd(a){var b=a.keyCode;"charCode"in a?(a=a.charCode,0===a&&13===b&&(a=13)):a=b;return 32<=a||13===a?a:0}


touches:null,targetTouches:null,changedTouches:null,altKey:null,metaKey:null,ctrlKey:null,shiftKey:null,getModifierState:ed});function be(a,b,c,d){return T.call(this,a,b,c,d)}T.augmentClass(be,{propertyName:null,elapsedTime:null,pseudoElement:null});


"abort animationEnd animationIteration animationStart blur cancel canPlay canPlayThrough click close contextMenu copy cut doubleClick drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error focus input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing progress rateChange reset scroll seeked seeking stalled submit suspend timeUpdate toggle touchCancel touchEnd touchMove touchStart transitionEnd volumeChange waiting wheel".split(" ").forEach(function(a){var b=a[0].toUpperCase()+
a.slice(1),c="on"+b;b="top"+b;c={phasedRegistrationNames:{bubbled:c,captured:c+"Capture"},dependencies:[b]};de[a]=c;ee[b]=c});
var fe={

b=a.getPooled(e,b,c,d);Ab(b);return b}};sd=function(a,b,c,d){a=jb(a,b,c,d);kb(a);lb(!1)};hb.injectEventPluginOrder("ResponderEventPlugin SimpleEventPlugin TapEventPlugin EnterLeaveEventPlugin ChangeEventPlugin SelectEventPlugin BeforeInputEventPlugin".split(" "));
Wa=sb.getFiberCurrentPropsFromNode;Xa=sb.getInstanceFromNode;Ya=sb.getNodeFromInstance;hb.injectEventPluginsByName({

function me(a,b){var c=a.type.contextTypes;if(!c)return D;var d=a.stateNode;if(d&&d.__reactInternalMemoizedUnmaskedChildContext===b)return d.__reactInternalMemoizedMaskedChildContext;var e={},f;for(f in c)e[f]=b[f];d&&(a=a.stateNode,a.__reactInternalMemoizedUnmaskedChildContext=b,a.__reactInternalMemoizedMaskedChildContext=e);return e}function le(a){return 2===a.tag&&null!=a.type.childContextTypes}function ne(a){le(a)&&(V(X,a),V(ie,a))}
function oe(a,b,c){null!=ie.cursor?E("168"):void 0;W(ie,b,a);W(X,c,a)}function pe(a,b){var c=a.stateNode,d=a.type.childContextTypes;if("function"!==typeof c.getChildContext)return b;c=c.getChildContext();for(var e in c)e in d?void 0:E("108",jd(a)||"Unknown",e);return B({},b,c)}function qe(a){if(!le(a))return!1;var b=a.stateNode;b=b&&b.__reactInternalMemoizedMergedChildContext||D;je=ie.current;W(ie,b,a);W(X,X.current,a);return!0}
function re(a,b){var c=a.stateNode;c?void 0:E("169");if(b){var d=pe(a,je);c.__reactInternalMemoizedMergedChildContext=d;V(X,a);V(ie,a);W(ie,d,a)}else V(X,a);W(X,b,a)}
function Y(a,b,c){this.tag=a;this.key=b;this.stateNode=this.type=null;this.sibling=this.child=this["return"]=null;this.index=0;this.memoizedState=this.updateQueue=this.memoizedProps=this.pendingProps=this.ref=null;this.internalContextTag=c;this.effectTag=0;this.lastEffect=this.firstEffect=this.nextEffect=null;this.expirationTime=0;this.alternate=null}
function se(a,b,c){var d=a.alternate;null===d?(d=new Y(a.tag,a.key,a.internalContextTag),d.type=a.type,d.stateNode=a.stateNode,d.alternate=a,a.alternate=d):(d.effectTag=0,d.nextEffect=null,d.firstEffect=null,d.lastEffect=null);d.expirationTime=c;d.pendingProps=b;d.child=a.child;d.memoizedProps=a.memoizedProps;d.memoizedState=a.memoizedState;d.updateQueue=a.updateQueue;d.sibling=a.sibling;d.index=a.index;d.ref=a.ref;return d}
function te(a,b,c){var d=void 0,e=a.type,f=a.key;"function"===typeof e?(d=e.prototype&&e.prototype.isReactComponent?new Y(2,f,b):new Y(0,f,b),d.type=e,d.pendingProps=a.props):"string"===typeof e?(d=new Y(5,f,b),d.type=e,d.pendingProps=a.props):"object"===typeof e&&null!==e&&"number"===typeof e.tag?(d=e,d.pendingProps=a.props):E("130",null==e?e:typeof e,"");d.expirationTime=c;return d}function ue(a,b,c,d){b=new Y(10,d,b);b.pendingProps=a;b.expirationTime=c;return b}
function ve(a,b,c){b=new Y(6,null,b);b.pendingProps=a;b.expirationTime=c;return b}function we(a,b,c){b=new Y(7,a.key,b);b.type=a.handler;b.pendingProps=a;b.expirationTime=c;return b}function xe(a,b,c){a=new Y(9,null,b);a.expirationTime=c;return a}function ye(a,b,c){b=new Y(4,a.key,b);b.pendingProps=a.children||[];b.expirationTime=c;b.stateNode={containerInfo:a.containerInfo,pendingChildren:null,implementation:a.implementation};return b}var ze=null,Ae=null;


return b.onCommitFiberRoot(c,a)});Ae=Be(function(a){return b.onCommitFiberUnmount(c,a)})}catch(d){}return!0}function De(a){"function"===typeof ze&&ze(a)}function Ee(a){"function"===typeof Ae&&Ae(a)}
function Fe(a){return{baseState:a,expirationTime:0,first:null,last:null,callbackList:null,hasForceUpdate:!1,isInitialized:!1}}function Ge(a,b){null===a.last?a.first=a.last=b:(a.last.next=b,a.last=b);if(0===a.expirationTime||a.expirationTime>b.expirationTime)a.expirationTime=b.expirationTime}
function He(a,b){var c=a.alternate,d=a.updateQueue;null===d&&(d=a.updateQueue=Fe(null));null!==c?(a=c.updateQueue,null===a&&(a=c.updateQueue=Fe(null))):a=null;a=a!==d?a:null;null===a?Ge(d,b):null===d.last||null===a.last?(Ge(d,b),Ge(a,b)):(Ge(d,b),a.last=b)}function Ie(a,b,c,d){a=a.partialState;return"function"===typeof a?a.call(b,c,d):a}
function Je(a,b,c,d,e,f){null!==a&&a.updateQueue===c&&(c=b.updateQueue={baseState:c.baseState,expirationTime:c.expirationTime,first:c.first,last:c.last,isInitialized:c.isInitialized,callbackList:null,hasForceUpdate:!1});c.expirationTime=0;c.isInitialized?a=c.baseState:(a=c.baseState=b.memoizedState,c.isInitialized=!0);for(var g=!0,h=c.first,k=!1;null!==h;){var q=h.expirationTime;if(q>f){var v=c.expirationTime;if(0===v||v>q)c.expirationTime=q;k||(k=!0,c.baseState=a)}else{k||(c.first=h.next,null===
c.first&&(c.last=null));if(h.isReplace)a=Ie(h,d,a,e),g=!0;else if(q=Ie(h,d,a,e))a=g?B({},a,q):B(a,q),g=!1;h.isForced&&(c.hasForceUpdate=!0);null!==h.callback&&(q=c.callbackList,null===q&&(q=c.callbackList=[]),q.push(h))}h=h.next}null!==c.callbackList?b.effectTag|=32:null!==c.first||c.hasForceUpdate||(b.updateQueue=null);k||(c.baseState=a);return a}
function Ke(a,b){var c=a.callbackList;if(null!==c)for(a.callbackList=null,a=0;a<c.length;a++){var d=c[a],e=d.callback;d.callback=null;"function"!==typeof e?E("191",e):void 0;e.call(b)}}
function Le(a,b,c,d){function e(a,b){b.updater=f;a.stateNode=b;b._reactInternalFiber=a}var f={isMounted:ld,enqueueSetState:function(c,d,e){c=c._reactInternalFiber;e=void 0===e?null:e;var g=b(c);He(c,{expirationTime:g,partialState:d,callback:e,isReplace:!1,isForced:!1,nextCallback:null,next:null});a(c,g)},enqueueReplaceState:function(c,d,e){c=c._reactInternalFiber;e=void 0===e?null:e;var g=b(c);He(c,{expirationTime:g,partialState:d,callback:e,isReplace:!0,isForced:!1,nextCallback:null,next:null});
a(c,g)},enqueueForceUpdate:function(c,d){c=c._reactInternalFiber;d=void 0===d?null:d;var e=b(c);He(c,{expirationTime:e,partialState:null,callback:d,isReplace:!1,isForced:!0,nextCallback:null,next:null});a(c,e)}};return{adoptClassInstance:e,constructClassInstance:function(a,b){var c=a.type,d=ke(a),f=2===a.tag&&null!=a.type.contextTypes,g=f?me(a,d):D;b=new c(b,g);e(a,b);f&&(a=a.stateNode,a.__reactInternalMemoizedUnmaskedChildContext=d,a.__reactInternalMemoizedMaskedChildContext=g);return b},mountClassInstance:function(a,
b){var c=a.alternate,d=a.stateNode,e=d.state||null,g=a.pendingProps;g?void 0:E("158");var h=ke(a);d.props=g;d.state=a.memoizedState=e;d.refs=D;d.context=me(a,h);null!=a.type&&null!=a.type.prototype&&!0===a.type.prototype.unstable_isAsyncReactComponent&&(a.internalContextTag|=1);"function"===typeof d.componentWillMount&&(e=d.state,d.componentWillMount(),e!==d.state&&f.enqueueReplaceState(d,d.state,null),e=a.updateQueue,null!==e&&(d.state=Je(c,a,e,d,g,b)));"function"===typeof d.componentDidMount&&(a.effectTag|=
4)},updateClassInstance:function(a,b,e){var g=b.stateNode;g.props=b.memoizedProps;g.state=b.memoizedState;var h=b.memoizedProps,k=b.pendingProps;k||(k=h,null==k?E("159"):void 0);var u=g.context,z=ke(b);z=me(b,z);"function"!==typeof g.componentWillReceiveProps||h===k&&u===z||(u=g.state,g.componentWillReceiveProps(k,z),g.state!==u&&f.enqueueReplaceState(g,g.state,null));u=b.memoizedState;e=null!==b.updateQueue?Je(a,b,b.updateQueue,g,k,e):u;if(!(h!==k||u!==e||X.current||null!==b.updateQueue&&b.updateQueue.hasForceUpdate))return"function"!==
typeof g.componentDidUpdate||h===a.memoizedProps&&u===a.memoizedState||(b.effectTag|=4),!1;var G=k;if(null===h||null!==b.updateQueue&&b.updateQueue.hasForceUpdate)G=!0;else{var I=b.stateNode,L=b.type;G="function"===typeof I.shouldComponentUpdate?I.shouldComponentUpdate(G,e,z):L.prototype&&L.prototype.isPureReactComponent?!ea(h,G)||!ea(u,e):!0}G?("function"===typeof g.componentWillUpdate&&g.componentWillUpdate(k,e,z),"function"===typeof g.componentDidUpdate&&(b.effectTag|=4)):("function"!==typeof g.componentDidUpdate||
h===a.memoizedProps&&u===a.memoizedState||(b.effectTag|=4),c(b,k),d(b,e));g.props=k;g.state=e;g.context=z;return G}}}var Qe="function"===typeof Symbol&&Symbol["for"],Re=Qe?Symbol["for"]("react.element"):60103,Se=Qe?Symbol["for"]("react.call"):60104,Te=Qe?Symbol["for"]("react.return"):60105,Ue=Qe?Symbol["for"]("react.portal"):60106,Ve=Qe?Symbol["for"]("react.fragment"):60107,We="function"===typeof Symbol&&Symbol.iterator;
function Xe(a){if(null===a||"undefined"===typeof a)return null;a=We&&a[We]||a["@@iterator"];return"function"===typeof a?a:null}var Ye=Array.isArray;
function Ze(a,b){var c=b.ref;if(null!==c&&"function"!==typeof c){if(b._owner){b=b._owner;var d=void 0;b&&(2!==b.tag?E("110"):void 0,d=b.stateNode);d?void 0:E("147",c);var e=""+c;if(null!==a&&null!==a.ref&&a.ref._stringRef===e)return a.ref;a=function(a){var b=d.refs===D?d.refs={}:d.refs;null===a?delete b[e]:b[e]=a};a._stringRef=e;return a}"string"!==typeof c?E("148"):void 0;b._owner?void 0:E("149",c)}return c}


c.nextEffect=null;c.effectTag=8}}function c(c,d){if(!a)return null;for(;null!==d;)b(c,d),d=d.sibling;return null}function d(a,b){for(a=new Map;null!==b;)null!==b.key?a.set(b.key,b):a.set(b.index,b),b=b.sibling;return a}function e(a,b,c){a=se(a,b,c);a.index=0;a.sibling=null;return a}function f(b,c,d){b.index=d;if(!a)return c;d=b.alternate;if(null!==d)return d=d.index,d<c?(b.effectTag=
2,c):d;b.effectTag=2;return c}function g(b){a&&null===b.alternate&&(b.effectTag=2);return b}function h(a,b,c,d){if(null===b||6!==b.tag)return b=ve(c,a.internalContextTag,d),b["return"]=a,b;b=e(b,c,d);b["return"]=a;return b}function k(a,b,c,d){if(null!==b&&b.type===c.type)return d=e(b,c.props,d),d.ref=Ze(b,c),d["return"]=a,d;d=te(c,a.internalContextTag,d);d.ref=Ze(b,c);d["return"]=a;return d}function q(a,b,c,d){if(null===b||7!==b.tag)return b=we(c,a.internalContextTag,d),b["return"]=a,b;b=e(b,c,d);
b["return"]=a;return b}function v(a,b,c,d){if(null===b||9!==b.tag)return b=xe(c,a.internalContextTag,d),b.type=c.value,b["return"]=a,b;b=e(b,null,d);b.type=c.value;b["return"]=a;return b}function y(a,b,c,d){if(null===b||4!==b.tag||b.stateNode.containerInfo!==c.containerInfo||b.stateNode.implementation!==c.implementation)return b=ye(c,a.internalContextTag,d),b["return"]=a,b;b=e(b,c.children||[],d);b["return"]=a;return b}function u(a,b,c,d,f){if(null===b||10!==b.tag)return b=ue(c,a.internalContextTag,
d,f),b["return"]=a,b;b=e(b,c,d);b["return"]=a;return b}function z(a,b,c){if("string"===typeof b||"number"===typeof b)return b=ve(""+b,a.internalContextTag,c),b["return"]=a,b;if("object"===typeof b&&null!==b){switch(b.$$typeof){case Re:if(b.type===Ve)return b=ue(b.props.children,a.internalContextTag,c,b.key),b["return"]=a,b;c=te(b,a.internalContextTag,c);c.ref=Ze(null,b);c["return"]=a;return c;case Se:return b=we(b,a.internalContextTag,c),b["return"]=a,b;case Te:return c=xe(b,a.internalContextTag,
c),c.type=b.value,c["return"]=a,c;case Ue:return b=ye(b,a.internalContextTag,c),b["return"]=a,b}if(Ye(b)||Xe(b))return b=ue(b,a.internalContextTag,c,null),b["return"]=a,b;

g=f(x,g,w);null===r?h=x:r.sibling=x;r=x;n=k}if(w===m.length)return c(e,n),h;if(null===n){for(;w<m.length;w++)if(n=z(e,m[w],A))g=f(n,g,w),null===r?h=n:r.sibling=n,r=n;return h}for(n=
d(e,n);w<m.length;w++)if(k=I(n,e,w,m[w],A)){if(a&&null!==k.alternate)n["delete"](null===k.key?w:k.key);g=f(k,g,w);null===r?h=k:r.sibling=k;r=k}a&&n.forEach(function(a){return b(e,a)});return h}function N(e,g,m,A){var h=Xe(m);"function"!==typeof h?E("150"):void 0;m=h.call(m);null==m?E("151"):void 0;for(var r=h=null,n=g,w=g=0,k=null,x=m.next();null!==n&&!x.done;w++,x=m.next()){n.index>w?(k=n,n=null):k=n.sibling;var J=G(e,n,x.value,A);if(null===J){n||(n=k);break}a&&n&&null===J.alternate&&b(e,n);g=f(J,
g,w);null===r?h=J:r.sibling=J;r=J;n=k}if(x.done)return c(e,n),h;if(null===n){for(;!x.done;w++,x=m.next())x=z(e,x.value,A),null!==x&&(g=f(x,g,w),null===r?h=x:r.sibling=x,r=x);return h}for(n=d(e,n);!x.done;w++,x=m.next())if(x=I(n,e,w,x.value,A),null!==x){if(a&&null!==x.alternate)n["delete"](null===x.key?w:x.key);g=f(x,g,w);null===r?h=x:r.sibling=x;r=x}a&&n.forEach(function(a){return b(e,a)});return h}return function(a,d,f,h){"object"===typeof f&&null!==f&&f.type===Ve&&null===f.key&&(f=f.props.children);


d.ref=Ze(m,f);d["return"]=a;a=d;break a}else{c(a,m);break}else b(a,m);m=m.sibling}f.type===Ve?(d=ue(f.props.children,a.internalContextTag,h,f.key),d["return"]=a,a=d):(h=te(f,a.internalContextTag,h),h.ref=Ze(d,f),h["return"]=a,a=h)}return g(a);case Se:a:{for(m=f.key;null!==d;){if(d.key===
m)if(7===d.tag){c(a,d.sibling);d=e(d,f,h);d["return"]=a;a=d;break a}else{c(a,d);break}else b(a,d);d=d.sibling}d=we(f,a.internalContextTag,h);d["return"]=a;a=d}return g(a);case Te:a:{if(null!==d)if(9===d.tag){c(a,d.sibling);d=e(d,null,h);d.type=f.value;d["return"]=a;a=d;break a}else c(a,d);d=xe(f,a.internalContextTag,h);d.type=f.value;d["return"]=a;a=d}return g(a);case Ue:a:{for(m=f.key;null!==d;){if(d.key===m)if(4===d.tag&&d.stateNode.containerInfo===f.containerInfo&&d.stateNode.implementation===
f.implementation){c(a,d.sibling);d=e(d,f.children||[],h);d["return"]=a;a=d;break a}else{c(a,d);break}else b(a,d);d=d.sibling}d=ye(f,a.internalContextTag,h);d["return"]=a;a=d}return g(a)}if("string"===typeof f||"number"===typeof f)return f=""+f,null!==d&&6===d.tag?(c(a,d.sibling),d=e(d,f,h)):(c(a,d),d=ve(f,a.internalContextTag,h)),d["return"]=a,a=d,g(a);if(Ye(f))return L(a,d,f,h);if(Xe(f))return N(a,d,f,h);m&&$e(a,f);if("undefined"===typeof f)switch(a.tag){case 2:case 1:h=a.type,E("152",h.displayName||
h.name||"Component")}return c(a,d)}}var bf=af(!0),cf=af(!1);
function df(a,b,c,d,e){function f(a,b,c){var d=b.expirationTime;b.child=null===a?cf(b,null,c,d):bf(b,a.child,c,d)}function g(a,b){var c=b.ref;null===c||a&&a.ref===c||(b.effectTag|=128)}function h(a,b,c,d){g(a,b);if(!c)return d&&re(b,!1),q(a,b);c=b.stateNode;id.current=b;var e=c.render();b.effectTag|=1;f(a,b,e);b.memoizedState=c.state;b.memoizedProps=c.props;d&&re(b,!0);return b.child}function k(a){var b=a.stateNode;b.pendingContext?oe(a,b.pendingContext,b.pendingContext!==b.context):b.context&&oe(a,
b.context,!1);I(a,b.containerInfo)}function q(a,b){null!==a&&b.child!==a.child?E("153"):void 0;if(null!==b.child){a=b.child;var c=se(a,a.pendingProps,a.expirationTime);b.child=c;for(c["return"]=b;null!==a.sibling;)a=a.sibling,c=c.sibling=se(a,a.pendingProps,a.expirationTime),c["return"]=b;c.sibling=null}return b.child}function v(a,b){switch(b.tag){case 3:k(b);break;case 2:qe(b);break;case 4:I(b,b.stateNode.containerInfo)}return null}var y=a.shouldSetTextContent,u=a.useSyncScheduling,z=a.shouldDeprioritizeSubtree,
G=b.pushHostContext,I=b.pushHostContainer,L=c.enterHydrationState,N=c.resetHydrationState,J=c.tryToClaimNextHydratableInstance;a=Le(d,e,function(a,b){a.memoizedProps=b},function(a,b){a.memoizedState=b});var w=a.adoptClassInstance,m=a.constructClassInstance,A=a.mountClassInstance,Ob=a.updateClassInstance;return{beginWork:function(a,b,c){if(0===b.expirationTime||b.expirationTime>c)return v(a,b);switch(b.tag){case 0:null!==a?E("155"):void 0;var d=b.type,e=b.pendingProps,r=ke(b);r=me(b,r);d=d(e,r);b.effectTag|=
1;"object"===typeof d&&null!==d&&"function"===typeof d.render?(b.tag=2,e=qe(b),w(b,d),A(b,c),b=h(a,b,!0,e)):(b.tag=1,f(a,b,d),b.memoizedProps=e,b=b.child);return b;case 1:a:{e=b.type;c=b.pendingProps;d=b.memoizedProps;if(X.current)null===c&&(c=d);else if(null===c||d===c){b=q(a,b);break a}d=ke(b);d=me(b,d);e=e(c,d);b.effectTag|=1;f(a,b,e);b.memoizedProps=c;b=b.child}

null===a&&J(b);e=b.type;var n=b.memoizedProps;d=b.pendingProps;null===d&&(d=n,null===d?E("154"):void 0);r=null!==a?a.memoizedProps:null;X.current||null!==d&&n!==d?(n=d.children,y(e,d)?n=null:r&&y(e,r)&&(b.effectTag|=16),g(a,b),


if(X.current)null===e&&(e=a&&a.memoizedProps,null===e?E("154"):void 0);else if(null===e||b.memoizedProps===e)e=b.memoizedProps;d=e.children;b.stateNode=null===a?cf(b,b.stateNode,d,c):bf(b,b.stateNode,d,c);b.memoizedProps=e;return b.stateNode;
case 9:return null;case 4:a:{I(b,b.stateNode.containerInfo);e=b.pendingProps;if(X.current)null===e&&(e=a&&a.memoizedProps,null==e?E("154"):void 0);else if(null===e||b.memoizedProps===e){b=q(a,b);break a}null===a?b.child=bf(b,null,e,c):f(a,b,e);b.memoizedProps=e;b=b.child}return b;case 10:a:{c=b.pendingProps;if(X.current)null===c&&(c=b.memoizedProps);else if(null===c||b.memoizedProps===c){b=q(a,b);break a}f(a,b,c);b.memoizedProps=c;b=b.child}return b;default:E("156")}},beginFailedWork:function(a,b,
c){switch(b.tag){case 2:qe(b);break;case 3:k(b);break;default:E("157")}b.effectTag|=64;null===a?b.child=null:b.child!==a.child&&(b.child=a.child);if(0===b.expirationTime||b.expirationTime>c)return v(a,b);b.firstEffect=null;b.lastEffect=null;b.child=null===a?cf(b,null,null,c):bf(b,a.child,null,c);2===b.tag&&(a=b.stateNode,b.memoizedProps=a.props,b.memoizedState=a.state);return b.child}}}
function ef(a,b,c){function d(a){a.effectTag|=4}var e=a.createInstance,f=a.createTextInstance,g=a.appendInitialChild,h=a.finalizeInitialChildren,k=a.prepareUpdate,q=a.persistence,v=b.getRootHostContainer,y=b.popHostContext,u=b.getHostContext,z=b.popHostContainer,G=c.prepareToHydrateHostInstance,I=c.prepareToHydrateHostTextInstance,L=c.popHydrationState,N=void 0,J=void 0,w=void 0;a.mutation?(N=function(){},J=function(a,b,c){(b.updateQueue=c)&&d(b)},w=function(a,b,c,e){c!==e&&d(b)}):q?E("235"):E("236");
return{completeWork:function(a,b,c){var m=b.pendingProps;if(null===m)m=b.memoizedProps;else if(2147483647!==b.expirationTime||2147483647===c)b.pendingProps=null;switch(b.tag){case 1:return null;case 2:return ne(b),null;case 3:z(b);V(X,b);V(ie,b);m=b.stateNode;m.pendingContext&&(m.context=m.pendingContext,m.pendingContext=null);if(null===a||null===a.child)L(b),b.effectTag&=-3;N(b);return null;case 5:y(b);c=v();var A=b.type;if(null!==a&&null!=b.stateNode){var p=a.memoizedProps,q=b.stateNode,x=u();q=
k(q,A,p,m,c,x);J(a,b,q,A,p,m,c);a.ref!==b.ref&&(b.effectTag|=128)}else{if(!m)return null===b.stateNode?E("166"):void 0,null;a=u();if(L(b))G(b,c,a)&&d(b);else{a=e(A,m,c,a,b);a:for(p=b.child;null!==p;){if(5===p.tag||6===p.tag)g(a,p.stateNode);else if(4!==p.tag&&null!==p.child){p.child["return"]=p;p=p.child;continue}if(p===b)break;for(;null===p.sibling;){if(null===p["return"]||p["return"]===b)break a;p=p["return"]}p.sibling["return"]=p["return"];p=p.sibling}h(a,A,m,c)&&d(b);b.stateNode=a}null!==b.ref&&
(b.effectTag|=128)}return null;case 6:if(a&&null!=b.stateNode)w(a,b,a.memoizedProps,m);else{if("string"!==typeof m)return null===b.stateNode?E("166"):void 0,null;a=v();c=u();L(b)?I(b)&&d(b):b.stateNode=f(m,a,c,b)}return null;case 7:(m=b.memoizedProps)?void 0:E("165");b.tag=8;A=[];a:for((p=b.stateNode)&&(p["return"]=b);null!==p;){if(5===p.tag||6===p.tag||4===p.tag)E("247");else if(9===p.tag)A.push(p.type);else if(null!==p.child){p.child["return"]=p;p=p.child;continue}for(;null===p.sibling;){if(null===
p["return"]||p["return"]===b)break a;p=p["return"]}p.sibling["return"]=p["return"];p=p.sibling}p=m.handler;m=p(m.props,A);b.child=bf(b,null!==a?a.child:null,m,c);return b.child;case 8:return b.tag=7,null;case 9:return null;case 10:return null;case 4:return z(b),N(b),null;case 0:E("167");default:E("156")}}}}
function ff(a,b){function c(a){var c=a.ref;if(null!==c)try{c(null)}catch(A){b(a,A)}}function d(a){"function"===typeof Ee&&Ee(a);switch(a.tag){case 2:c(a);var d=a.stateNode;if("function"===typeof d.componentWillUnmount)try{d.props=a.memoizedProps,d.state=a.memoizedState,d.componentWillUnmount()}catch(A){b(a,A)}break;case 5:c(a);break;case 7:e(a.stateNode);break;case 4:k&&g(a)}}function e(a){for(var b=a;;)if(d(b),null===b.child||k&&4===b.tag){if(b===a)break;for(;null===b.sibling;){if(null===b["return"]||
b["return"]===a)return;b=b["return"]}b.sibling["return"]=b["return"];b=b.sibling}else b.child["return"]=b,b=b.child}function f(a){return 5===a.tag||3===a.tag||4===a.tag}function g(a){for(var b=a,c=!1,f=void 0,g=void 0;;){if(!c){c=b["return"];a:for(;;){null===c?E("160"):void 0;switch(c.tag){case 5:f=c.stateNode;g=!1;break a;case 3:f=c.stateNode.containerInfo;g=!0;break a;case 4:f=c.stateNode.containerInfo;g=!0;break a}c=c["return"]}c=!0}if(5===b.tag||6===b.tag)e(b),g?J(f,b.stateNode):N(f,b.stateNode);
else if(4===b.tag?f=b.stateNode.containerInfo:d(b),null!==b.child){b.child["return"]=b;b=b.child;continue}if(b===a)break;for(;null===b.sibling;){if(null===b["return"]||b["return"]===a)return;b=b["return"];4===b.tag&&(c=!1)}b.sibling["return"]=b["return"];b=b.sibling}}var h=a.getPublicInstance,k=a.mutation;a=a.persistence;k||(a?E("235"):E("236"));var q=k.commitMount,v=k.commitUpdate,y=k.resetTextContent,u=k.commitTextUpdate,z=k.appendChild,G=k.appendChildToContainer,I=k.insertBefore,L=k.insertInContainerBefore,
N=k.removeChild,J=k.removeChildFromContainer;return{commitResetTextContent:function(a){y(a.stateNode)},commitPlacement:function(a){a:{for(var b=a["return"];null!==b;){if(f(b)){var c=b;break a}b=b["return"]}E("160");c=void 0}var d=b=void 0;switch(c.tag){case 5:b=c.stateNode;d=!1;break;case 3:b=c.stateNode.containerInfo;d=!0;break;case 4:b=c.stateNode.containerInfo;d=!0;break;default:E("161")}c.effectTag&16&&(y(b),c.effectTag&=-17);a:b:for(c=a;;){for(;null===c.sibling;){if(null===c["return"]||f(c["return"])){c=
null;break a}c=c["return"]}c.sibling["return"]=c["return"];for(c=c.sibling;5!==c.tag&&6!==c.tag;){if(c.effectTag&2)continue b;if(null===c.child||4===c.tag)continue b;else c.child["return"]=c,c=c.child}if(!(c.effectTag&2)){c=c.stateNode;break a}}for(var e=a;;){if(5===e.tag||6===e.tag)c?d?L(b,e.stateNode,c):I(b,e.stateNode,c):d?G(b,e.stateNode):z(b,e.stateNode);else if(4!==e.tag&&null!==e.child){e.child["return"]=e;e=e.child;continue}if(e===a)break;for(;null===e.sibling;){if(null===e["return"]||e["return"]===
a)return;e=e["return"]}e.sibling["return"]=e["return"];e=e.sibling}},commitDeletion:function(a){g(a);a["return"]=null;a.child=null;a.alternate&&(a.alternate.child=null,a.alternate["return"]=null)},commitWork:function(a,b){switch(b.tag){case 2:break;case 5:var c=b.stateNode;if(null!=c){var d=b.memoizedProps;a=null!==a?a.memoizedProps:d;var e=b.type,f=b.updateQueue;b.updateQueue=null;null!==f&&v(c,f,e,a,d,b)}break;case 6:null===b.stateNode?E("162"):void 0;c=b.memoizedProps;u(b.stateNode,null!==a?a.memoizedProps:
c,c);break;case 3:break;default:E("163")}},commitLifeCycles:function(a,b){switch(b.tag){case 2:var c=b.stateNode;if(b.effectTag&4)if(null===a)c.props=b.memoizedProps,c.state=b.memoizedState,c.componentDidMount();else{var d=a.memoizedProps;a=a.memoizedState;c.props=b.memoizedProps;c.state=b.memoizedState;c.componentDidUpdate(d,a)}b=b.updateQueue;null!==b&&Ke(b,c);break;case 3:c=b.updateQueue;null!==c&&Ke(c,null!==b.child?b.child.stateNode:null);break;case 5:c=b.stateNode;null===a&&b.effectTag&4&&q(c,
b.type,b.memoizedProps,b);break;case 6:break;case 4:break;default:E("163")}},commitAttachRef:function(a){var b=a.ref;if(null!==b){var c=a.stateNode;switch(a.tag){case 5:b(h(c));break;default:b(c)}}},commitDetachRef:function(a){a=a.ref;null!==a&&a(null)}}}var gf={};
function hf(a){function b(a){a===gf?E("174"):void 0;return a}var c=a.getChildHostContext,d=a.getRootHostContext,e={current:gf},f={current:gf},g={current:gf};return{getHostContext:function(){return b(e.current)},getRootHostContainer:function(){return b(g.current)},popHostContainer:function(a){V(e,a);V(f,a);V(g,a)},popHostContext:function(a){f.current===a&&(V(e,a),V(f,a))},pushHostContainer:function(a,b){W(g,b,a);b=d(b);W(f,a,a);W(e,b,a)},pushHostContext:function(a){var d=b(g.current),h=b(e.current);
d=c(h,a.type,d);h!==d&&(W(f,a,a),W(e,d,a))},resetHostContainer:function(){e.current=gf;g.current=gf}}}
function jf(a){function b(a,b){var c=new Y(5,null,0);c.type="DELETED";c.stateNode=b;c["return"]=a;c.effectTag=8;null!==a.lastEffect?(a.lastEffect.nextEffect=c,a.lastEffect=c):a.firstEffect=a.lastEffect=c}function c(a,b){switch(a.tag){case 5:return b=f(b,a.type,a.pendingProps),null!==b?(a.stateNode=b,!0):!1;case 6:return b=g(b,a.pendingProps),null!==b?(a.stateNode=b,!0):!1;default:return!1}}function d(a){for(a=a["return"];null!==a&&5!==a.tag&&3!==a.tag;)a=a["return"];y=a}var e=a.shouldSetTextContent;
a=a.hydration;if(!a)return{enterHydrationState:function(){return!1},resetHydrationState:function(){},tryToClaimNextHydratableInstance:function(){},prepareToHydrateHostInstance:function(){E("175")},prepareToHydrateHostTextInstance:function(){E("176")},popHydrationState:function(){return!1}};var f=a.canHydrateInstance,g=a.canHydrateTextInstance,h=a.getNextHydratableSibling,k=a.getFirstHydratableChild,q=a.hydrateInstance,v=a.hydrateTextInstance,y=null,u=null,z=!1;return{enterHydrationState:function(a){u=
k(a.stateNode.containerInfo);y=a;return z=!0},resetHydrationState:function(){u=y=null;z=!1},tryToClaimNextHydratableInstance:function(a){if(z){var d=u;if(d){if(!c(a,d)){d=h(d);if(!d||!c(a,d)){a.effectTag|=2;z=!1;y=a;return}b(y,u)}y=a;u=k(d)}else a.effectTag|=2,z=!1,y=a}},prepareToHydrateHostInstance:function(a,b,c){b=q(a.stateNode,a.type,a.memoizedProps,b,c,a);a.updateQueue=b;return null!==b?!0:!1},prepareToHydrateHostTextInstance:function(a){return v(a.stateNode,a.memoizedProps,a)},popHydrationState:function(a){if(a!==
y)return!1;if(!z)return d(a),z=!0,!1;var c=a.type;if(5!==a.tag||"head"!==c&&"body"!==c&&!e(c,a.memoizedProps))for(c=u;c;)b(a,c),c=h(c);d(a);u=y?h(a.stateNode):null;return!0}}}
function kf(a){function b(a){Qb=ja=!0;var b=a.stateNode;b.current===a?E("177"):void 0;b.isReadyForCommit=!1;id.current=null;if(1<a.effectTag)if(null!==a.lastEffect){a.lastEffect.nextEffect=a;var c=a.firstEffect}else c=a;else c=a.firstEffect;yg();for(t=c;null!==t;){var d=!1,e=void 0;try{for(;null!==t;){var f=t.effectTag;f&16&&zg(t);if(f&128){var g=t.alternate;null!==g&&Ag(g)}switch(f&-242){case 2:Ne(t);t.effectTag&=-3;break;case 6:Ne(t);t.effectTag&=-3;Oe(t.alternate,t);break;case 4:Oe(t.alternate,
t);break;case 8:Sc=!0,Bg(t),Sc=!1}t=t.nextEffect}}catch(Tc){d=!0,e=Tc}d&&(null===t?E("178"):void 0,h(t,e),null!==t&&(t=t.nextEffect))}Cg();b.current=a;for(t=c;null!==t;){c=!1;d=void 0;try{for(;null!==t;){var k=t.effectTag;k&36&&Dg(t.alternate,t);k&128&&Eg(t);if(k&64)switch(e=t,f=void 0,null!==R&&(f=R.get(e),R["delete"](e),null==f&&null!==e.alternate&&(e=e.alternate,f=R.get(e),R["delete"](e))),null==f?E("184"):void 0,e.tag){case 2:e.stateNode.componentDidCatch(f.error,{componentStack:f.componentStack});
break;case 3:null===ca&&(ca=f.error);break;default:E("157")}var Qc=t.nextEffect;t.nextEffect=null;t=Qc}}catch(Tc){c=!0,d=Tc}c&&(null===t?E("178"):void 0,h(t,d),null!==t&&(t=t.nextEffect))}ja=Qb=!1;"function"===typeof De&&De(a.stateNode);ha&&(ha.forEach(G),ha=null);null!==ca&&(a=ca,ca=null,Ob(a));b=b.current.expirationTime;0===b&&(qa=R=null);return b}function c(a){for(;;){var b=Fg(a.alternate,a,H),c=a["return"],d=a.sibling;var e=a;if(2147483647===H||2147483647!==e.expirationTime){if(2!==e.tag&&3!==
e.tag)var f=0;else f=e.updateQueue,f=null===f?0:f.expirationTime;for(var g=e.child;null!==g;)0!==g.expirationTime&&(0===f||f>g.expirationTime)&&(f=g.expirationTime),g=g.sibling;

if(null!==c)a=c;else{a.stateNode.isReadyForCommit=!0;break}}return null}function d(a){var b=rg(a.alternate,a,H);null===b&&(b=c(a));id.current=null;return b}function e(a){var b=Gg(a.alternate,a,H);null===b&&(b=c(a));id.current=null;return b}function f(a){if(null!==R){if(!(0===H||H>a))if(H<=Uc)for(;null!==F;)F=k(F)?e(F):d(F);else for(;null!==F&&!A();)F=k(F)?e(F):d(F)}else if(!(0===H||H>a))if(H<=Uc)for(;null!==F;)F=d(F);else for(;null!==F&&!A();)F=d(F)}function g(a,b){ja?E("243"):void 0;ja=!0;a.isReadyForCommit=
!1;if(a!==ra||b!==H||null===F){for(;-1<he;)ge[he]=null,he--;je=D;ie.current=D;X.current=!1;x();ra=a;H=b;F=se(ra.current,null,b)}var c=!1,d=null;try{f(b)}catch(Rc){c=!0,d=Rc}for(;c;){if(eb){ca=d;break}var g=F;if(null===g)eb=!0;else{var k=h(g,d);null===k?E("183"):void 0;if(!eb){try{c=k;d=b;for(k=c;null!==g;){switch(g.tag){case 2:ne(g);break;case 5:qg(g);break;case 3:p(g);break;case 4:p(g)}if(g===k||g.alternate===k)break;g=g["return"]}F=e(c);f(d)}catch(Rc){c=!0;d=Rc;continue}break}}}b=ca;eb=ja=!1;ca=
null;null!==b&&Ob(b);return a.isReadyForCommit?a.current.alternate:null}function h(a,b){var c=id.current=null,d=!1,e=!1,f=null;if(3===a.tag)c=a,q(a)&&(eb=!0);else for(var g=a["return"];null!==g&&null===c;){2===g.tag?"function"===typeof g.stateNode.componentDidCatch&&(d=!0,f=jd(g),c=g,e=!0):3===g.tag&&(c=g);if(q(g)){if(Sc||null!==ha&&(ha.has(g)||null!==g.alternate&&ha.has(g.alternate)))return null;c=null;e=!1}g=g["return"]}if(null!==c){null===qa&&(qa=new Set);qa.add(c);var h="";g=a;do{a:switch(g.tag){case 0:case 1:case 2:case 5:var k=
g._debugOwner,Qc=g._debugSource;var m=jd(g);var n=null;k&&(n=jd(k));k=Qc;m="\n    in "+(m||"Unknown")+(k?" (at "+k.fileName.replace(/^.*[\\\/]/,"")+":"+k.lineNumber+")":n?" (created by "+n+")":"");break a;default:m=""}h+=m;g=g["return"]}while(g);g=h;a=jd(a);null===R&&(R=new Map);b={componentName:a,componentStack:g,error:b,errorBoundary:d?c.stateNode:null,errorBoundaryFound:d,errorBoundaryName:f,willRetry:e};R.set(c,b);try{var p=b.error;p&&p.suppressReactErrorLogging||console.error(p)}catch(Vc){Vc&&
Vc.suppressReactErrorLogging||console.error(Vc)}Qb?(null===ha&&(ha=new Set),ha.add(c)):G(c);return c}null===ca&&(ca=b);return null}function k(a){return null!==R&&(R.has(a)||null!==a.alternate&&R.has(a.alternate))}function q(a){return null!==qa&&(qa.has(a)||null!==a.alternate&&qa.has(a.alternate))}function v(){return 20*(((I()+100)/20|0)+1)}function y(a){return 0!==ka?ka:ja?Qb?1:H:!Hg||a.internalContextTag&1?v():1}function u(a,b){return z(a,b,!1)}function z(a,b){for(;null!==a;){if(0===a.expirationTime||
a.expirationTime>b)a.expirationTime=b;null!==a.alternate&&(0===a.alternate.expirationTime||a.alternate.expirationTime>b)&&(a.alternate.expirationTime=b);if(null===a["return"])if(3===a.tag){var c=a.stateNode;!ja&&c===ra&&b<H&&(F=ra=null,H=0);var d=c,e=b;Rb>Ig&&E("185");if(null===d.nextScheduledRoot)d.remainingExpirationTime=e,null===O?(sa=O=d,d.nextScheduledRoot=d):(O=O.nextScheduledRoot=d,O.nextScheduledRoot=sa);else{var f=d.remainingExpirationTime;if(0===f||e<f)d.remainingExpirationTime=e}Fa||(la?
Sb&&(ma=d,na=1,m(ma,na)):1===e?w(1,null):L(e));!ja&&c===ra&&b<H&&(F=ra=null,H=0)}else break;a=a["return"]}}function G(a){z(a,1,!0)}function I(){return Uc=((Wc()-Pe)/10|0)+2}function L(a){if(0!==Tb){if(a>Tb)return;Jg(Xc)}var b=Wc()-Pe;Tb=a;Xc=Kg(J,{timeout:10*(a-2)-b})}function N(){var a=0,b=null;if(null!==O)for(var c=O,d=sa;null!==d;){var e=d.remainingExpirationTime;if(0===e){null===c||null===O?E("244"):void 0;if(d===d.nextScheduledRoot){sa=O=d.nextScheduledRoot=null;break}else if(d===sa)sa=e=d.nextScheduledRoot,
O.nextScheduledRoot=e,d.nextScheduledRoot=null;else if(d===O){O=c;O.nextScheduledRoot=sa;d.nextScheduledRoot=null;break}else c.nextScheduledRoot=d.nextScheduledRoot,d.nextScheduledRoot=null;d=c.nextScheduledRoot}else{if(0===a||e<a)a=e,b=d;if(d===O)break;c=d;d=d.nextScheduledRoot}}c=ma;null!==c&&c===b?Rb++:Rb=0;ma=b;na=a}function J(a){w(0,a)}function w(a,b){fb=b;for(N();null!==ma&&0!==na&&(0===a||na<=a)&&!Yc;)m(ma,na),N();null!==fb&&(Tb=0,Xc=-1);0!==na&&L(na);fb=null;Yc=!1;Rb=0;if(Ub)throw a=Zc,Zc=
null,Ub=!1,a;}function m(a,c){Fa?E("245"):void 0;Fa=!0;if(c<=I()){var d=a.finishedWork;null!==d?(a.finishedWork=null,a.remainingExpirationTime=b(d)):(a.finishedWork=null,d=g(a,c),null!==d&&(a.remainingExpirationTime=b(d)))}else d=a.finishedWork,null!==d?(a.finishedWork=null,a.remainingExpirationTime=b(d)):(a.finishedWork=null,d=g(a,c),null!==d&&(A()?a.finishedWork=d:a.remainingExpirationTime=b(d)));Fa=!1}function A(){return null===fb||fb.timeRemaining()>Lg?!1:Yc=!0}function Ob(a){null===ma?E("246"):
void 0;ma.remainingExpirationTime=0;Ub||(Ub=!0,Zc=a)}var

return{computeAsyncExpiration:v,computeExpirationForFiber:y,scheduleWork:u,batchedUpdates:function(a,b){var c=la;la=!0;try{return a(b)}finally{(la=c)||Fa||w(1,null)}},unbatchedUpdates:function(a){if(la&&!Sb){Sb=!0;try{return a()}finally{Sb=!1}}return a()},flushSync:function(a){var b=la;la=!0;try{a:{var c=
ka;ka=1;try{var d=a();break a}finally{ka=c}d=void 0}return d}finally{la=b,Fa?E("187"):void 0,w(1,null)}},deferredUpdates:function(a){var b=ka;ka=v();try{return a()}finally{ka=b}}}}
function lf(a){function b(a){a=od(a);return null===a?null:a.stateNode}var c=a.getPublicInstance;a=kf(a);var d=a.computeAsyncExpiration,e=a.computeExpirationForFiber,f=a.scheduleWork;return{createContainer:function(a,b){var c=new Y(3,null,0);a={current:c,containerInfo:a,pendingChildren:null,remainingExpirationTime:0,isReadyForCommit:!1,finishedWork:null,context:null,pendingContext:null,hydrate:b,nextScheduledRoot:null};return c.stateNode=a},updateContainer:function(a,b,c,q){var g=b.current;if(c){c=
c._reactInternalFiber;var h;b:{2===kd(c)&&2===c.tag?void 0:E("170");for(h=c;3!==h.tag;){if(le(h)){h=h.stateNode.__reactInternalMemoizedMergedChildContext;break b}(h=h["return"])?void 0:E("171")}h=h.stateNode.context}c=le(c)?pe(c,h):h}else c=D;null===b.context?b.context=c:b.pendingContext=c;b=q;b=void 0===b?null:b;q=null!=a&&null!=a.type&&null!=a.type.prototype&&!0===a.type.prototype.unstable_isAsyncReactComponent?d():e(g);He(g,{expirationTime:q,partialState:{element:a},callback:b,isReplace:!1,isForced:!1,
nextCallback:null,next:null});f(g,q)},batchedUpdates:a.batchedUpdates,unbatchedUpdates:a.unbatchedUpdates,deferredUpdates:a.deferredUpdates,flushSync:a.flushSync,getPublicRootInstance:function(a){a=a.current;if(!a.child)return null;switch(a.child.tag){case 5:return c(a.child.stateNode);default:return a.child.stateNode}},findHostInstance:b,findHostInstanceWithNoPortals:function(a){a=pd(a);return null===a?null:a.stateNode},injectIntoDevTools:function(a){var c=a.findFiberByHostInstance;return Ce(B({},
a,{findHostInstanceByFiber:function(a){return b(a)},findFiberByHostInstance:function(a){return c?c(a):null}}))}}}var mf=Object.freeze({default:lf}),nf=mf&&lf||mf,of=nf["default"]?nf["default"]:nf;function pf(a,b,c){

a)Bf.didTimeout=!0;else{xf||(xf=!0,requestAnimationFrame(Df));return}else Bf.didTimeout=!1;wf=-1;a=uf;uf=null;null!==a&&a(Bf)}},!1);var Df=function(a){xf=!1;var b=a-yf+Af;b<Af&&zf<Af?(8>b&&(b=8),Af=b<zf?zf:b):zf=b;yf=a+Af;vf||(vf=!0,window.postMessage(Cf,"*"))};sf=function(a,b){uf=a;null!=b&&"number"===typeof b.timeout&&(wf=rf()+b.timeout);xf||(xf=!0,requestAnimationFrame(Df));return 0};tf=function(){uf=null;vf=!1;wf=-1}}else sf=window.requestIdleCallback,tf=window.cancelIdleCallback;else sf=function(a){return setTimeout(function(){a({timeRemaining:function(){return Infinity}})})},
tf=function(a){clearTimeout(a)};var Ef=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\

function Hf(a){if(Gf.hasOwnProperty(a))return!0;if(Ff.hasOwnProperty(a))return!1;if(Ef.test(a))return Gf[a]=!0;Ff[a]=!0;return!1}
function If(a,b,c){var d=wa(b);if(d&&va(b,c)){var e=d.mutationMethod;e?e(a,c):null==c||d.hasBooleanValue&&!c||d.hasNumericValue&&isNaN(c)||d.hasPositiveNumericValue&&1>c||d.hasOverloadedBooleanValue&&!1===c?Jf(a,b):d.mustUseProperty?a[d.propertyName]=c:(b=d.attributeName,(e=d.attributeNamespace)?a.setAttributeNS(e,b,""+c):d.hasBooleanValue||d.hasOverloadedBooleanValue&&!0===c?a.setAttribute(b,""):a.setAttribute(b,""+c))}else Kf(a,b,va(b,c)?c:null)}
function Kf(a,b,c){Hf(b)&&(null==c?a.removeAttribute(b):a.setAttribute(b,""+c))}function Jf(a,b){var c=wa(b);c?(b=c.mutationMethod)?b(a,void 0):c.mustUseProperty?a[c.propertyName]=c.hasBooleanValue?!1:"":a.removeAttribute(c.attributeName):a.removeAttribute(b)}
function Lf(a,b){var c=b.value,d=b.checked;return B({

b=b.checked;null!=b&&If(a,"checked",b)}function Of(a,b){Nf(a,b);var c=b.value;if(null!=c)if(0===c&&""===a.value)a.value="0";else if("number"===b.type){if(b=parseFloat(a.value)||0,c!=b||c==b&&a.value!=c)a.value=""+c}else a.value!==""+c&&(a.value=""+c);else null

a.value=a.defaultValue;break;default:a.value=a.value}b=a.name;""!==b&&(a.name="");a.defaultChecked=!a.defaultChecked;a.defaultChecked=!a.defaultChecked;""!==b&&(a.name=b)}function Qf(a){var b="";aa.Children.forEach(a,function(a){null==a||"string"!==typeof a&&"number"!==typeof a||(b+=a)});return b}
function Rf(a,b){a=B({children:void 0},b);if(b=Qf(b.children))a.children=b;return a}function Sf(a,b,c,d){a=a.options;if(b){b={};for(var e=0;e<c.length;e++)b["$"+c[e]]=!0;for(c=0;c<a.length;c++)e=b.hasOwnProperty("$"+a[c].value),a[c].selected!==e&&(a[c].selected=e),e&&d&&(a[c].defaultSelected=!0)}else{c=""+c;b=null;for(e=0;e<a.length;e++){if(a[e].value===c){a[e].selected=!0;d&&(a[e].defaultSelected=!0);return}null!==b||a[e].disabled||(b=a[e])}null!==b&&(b.selected=!0)}}
function Tf(a,b){var c=b.value;a._wrapperState={initialValue:null!=c?c:b.defaultValue,wasMultiple:!!b.multiple}}function Uf(a,b){null!=b.dangerouslySetInnerHTML?E("91"):void 0;return B({},b,{value:void 0,defaultValue:void 0,children:""+a._wrapperState.initialValue})}function Vf(a,b){var c=b.value;null==c&&(c=b.defaultValue,b=b.children,null!=b&&(null!=c?E("92"):void 0,Array.isArray(b)&&(1>=b.length?void 0:E("93"),b=b[0]),c=""+b),null==c&&(c=""));a._wrapperState={initialValue:""+c}}
function Wf(a,b){var c=b.value;

function fg(a,b){a=a.style;for(var c in b)if(b.hasOwnProperty(c)){var d=0===c.indexOf("--");var e=c;var f=b[c];e=null==f||"boolean"===typeof f||""===f?"":d||"number"!==typeof f||0===f||dg.hasOwnProperty(e)&&dg[e]?(""+f).trim():f+"px";"float"===c&&(c="cssFloat");d?a.setProperty(c,e):a[c]=e}

dangerouslySetInnerHTML?E("137",a,c()):void 0),null!=b.dangerouslySetInnerHTML&&(null!=b.children?E("60"):void 0,"object"===typeof b.dangerouslySetInnerHTML&&"__html"in b.dangerouslySetInnerHTML?void 0:E("61")),null!=b.style&&"object"!==typeof b.style?E("62",c()):void 0)}
function ig(a,b){if(-1===a.indexOf("-"))return"string"===typeof b.is;switch(a){case "annotation-xml":case "color-profile":case "font-face":case "font-face-src":case "font-face-uri":case "font-face-format":case "font-face-name":case "missing-glyph":return!1;default:return!0}}var jg=Yf.html,kg=C.thatReturns("");
function lg(a,b){

c=9===c.nodeType?c:c.ownerDocument;d===jg&&(d=Zf(a));d===jg?"script"===a?(a=c.createElement("div"),a.innerHTML="\x3cscript\x3e\x3c/script\x3e",a=a.removeChild(a.firstChild)):a="string"===typeof b.is?c.createElement(a,{

f=c;break;case "img":case "image":U("topError","error",a);U("topLoad","load",a);f=c;break;case "form":U("topReset","reset",a);U("topSubmit","submit",a);f=c;break;case "details":U("topToggle","toggle",a);f=c;break;case "input":Mf(a,c);f=Lf(a,c);U("topInvalid","invalid",a);
lg(d,"onChange");break;case "option":f=Rf(a,c);break;case "select":Tf(a,c);f=B({},c,{value:void 0});U("topInvalid","invalid",a);lg(d,"onChange");break;case "textarea":Vf(a,c);f=Uf(a,c);U("topInvalid","invalid",a);lg(d,"onChange");break;default:f=c}hg(b,f,kg);var g=f,h;for(h in g)if(g.hasOwnProperty(h)){var k=g[h];"style"===h?fg(a,k,kg):"dangerouslySetInnerHTML"===h?(k=k?k.__html:void 0,null!=k&&bg(a,k)):"children"===h?"string"===typeof k?("textarea"!==b||""!==k)&&cg(a,k):"number"===typeof k&&cg(a,


d=Lf(a,d);f=[];break;case "option":c=Rf(a,c);d=Rf(a,d);f=[];break;case "select":c=B({},c,{value:void 0});d=B({},d,{value:void 0});f=[];break;case "textarea":c=Uf(a,c);d=Uf(a,d);f=[];

U("topLoad","load",a);break;case "form":U("topReset","reset",a);U("topSubmit","submit",a);break;case "details":U("topToggle","toggle",a);break;case "input":Mf(a,c);U("topInvalid","invalid",a);lg(e,"onChange");break;case "select":Tf(a,c);
U("topInvalid","invalid",a);lg(e,"onChange");

c.parentNode;c=c.querySelectorAll("input[name\x3d"+JSON.stringify(""+b)+'][type\x3d"radio"]');for(b=0;b<c.length;b++){var d=c[b];if(d!==a&&d.form===a.form){var e=rb(d);e?void 0:E("90");Cc(d);Of(d,e)}}}break;case "textarea":Wf(a,c);break;case "select":b=c.value,null!=b&&Sf(a,!!c.multiple,b,!1)}}});nc.injectFiberControlledHostComponent(wg);var xg=null,Mg=null;function Ng(a){return!(!a||1!==a.nodeType&&9!==a.nodeType&&11!==a.nodeType&&(8!==a.nodeType||" react-mount-point-unstable "!==a.nodeValue))}
function Og(a){a=a?9===a.nodeType?a.documentElement:a.firstChild:null;return!(!a||1!==a.nodeType||!a.hasAttribute("data-reactroot"))}
var Z=of({getRootHostContext:function(a){var b=a.nodeType;switch(b){

b:for(;;){for(var u;;){v!==b||0!==d&&3!==v.nodeType||(g=f+d);v!==e||0!==c&&3!==v.nodeType||(h=f+c);3===v.nodeType&&(f+=v.nodeValue.length);if(null===(u=v.firstChild))break;y=v;v=u}for(;;){if(v===a)break b;y===b&&++k===d&&(g=f);y===e&&++q===c&&(h=f);if(null!==(u=v.nextSibling))break;v=y;y=v.parentNode}v=u}b=-1===g||-1===h?null:


g.setStart(e.node,e.offset);b.removeAllRanges();a>d?(b.addRange(g),b.extend(f.node,f.offset)):(g.setEnd(f.node,f.offset),b.addRange(g))}}b=[];for(a=c;a=a.parentNode;)1===a.nodeType&&b.push({element:a,left:a.scrollLeft,top:a.scrollTop});ia(c);for(c=0;c<b.length;c++)a=b[c],a.element.scrollLeft=a.left,a.element.scrollTop=
a.top}Mg=null;ud(xg);xg=null},createInstance:function(a,b,c,d,e){a=ng(a,b,c,d);a[Q]=e;a[ob]=b;return a},appendInitialChild:function(a,b){a.appendChild(b)},finalizeInitialChildren:function(a,b,c,d){pg(a,b,c,d);a:{switch(b){case "button":case "input":case "select":case "textarea":a=!!c.autoFocus;break a}a=!1}return a},prepareUpdate:function(a,b,c,d,e){return sg(a,b,c,d,e)},shouldSetTextContent:function(a,b){return"textarea"===a||"string"===typeof b.children||"number"===typeof b.children||"object"===
typeof b.dangerouslySetInnerHTML&&null!==b.dangerouslySetInnerHTML&&"string"===typeof b.dangerouslySetInnerHTML.__html},shouldDeprioritizeSubtree:function(a,b){return!!b.hidden},createTextInstance:function(a,b,c,d){a=og(a,b);a[Q]=d;return a},now:rf,mutation:{commitMount:function(a){a.focus()},commitUpdate:function(a,b,c,d,e){a[ob]=e;tg(a,b,c,d,e)},resetTextContent:function(a){a.textContent=""},commitTextUpdate:function(a,b,c){a.nodeValue=c},appendChild:function(a,b){a.appendChild(b)},appendChildToContainer:function(a,
b){8===a.nodeType?a.parentNode.insertBefore(b,a):a.appendChild(b)}

8===a.nodeType?a.parentNode.removeChild(b):a.removeChild(b)}},hydration:{canHydrateInstance:function(a,b){return 1!==a.nodeType||b.toLowerCase()!==a.nodeName.toLowerCase()?null:a},canHydrateTextInstance:function(a,
b){return""===b||3!==a.nodeType?null:a},getNextHydratableSibling:function(a){for(a=a.nextSibling;a&&1!==a.nodeType&&3!==a.nodeType;)a=a.nextSibling;return a},getFirstHydratableChild:function(a){for(a=a.firstChild;a&&1!==a.nodeType&&3!==a.nodeType;)a=a.nextSibling;return a},hydrateInstance:function(a,b,c,d,e,f){a[Q]=f;a[ob]=c;return ug(a,b,c,e,d)},hydrateTextInstance:function(a,b,c){a[Q]=c;return vg(a,b)},didNotMatchHydratedContainerTextInstance:function(){},didNotMatchHydratedTextInstance:function(){},
didNotHydrateContainerInstance:function(){},didNotHydrateInstance:function(){},didNotFindHydratableContainerInstance:function(){},didNotFindHydratableContainerTextInstance:function(){},didNotFindHydratableInstance:function(){},didNotFindHydratableTextInstance:function(){}},scheduleDeferredCallback:sf,cancelDeferredCallback:tf,useSyncScheduling:!0});rc=Z.batchedUpdates;
function Pg(a,b,c,d,e){Ng(c)?void 0:E("200");var f=c._reactRootContainer;if(f)Z.updateContainer(b,f,a,e);else{d=d||Og(c);if(!d)for(f=void 0;

function Rg(a,b){this._reactRootContainer=Z.createContainer(a,b)}Rg.prototype.render=function(a,b){Z.updateContainer(a,this._reactRootContainer,null,b)};Rg.prototype.unmount=function(a){Z.updateContainer(null,this._reactRootContainer,null,a)};
var Sg={createPortal:Qg,findDOMNode:function(a){if(null==a)return null;if(1===a.nodeType)return a;var b=a._reactInternalFiber;if(b)return Z.findHostInstance(b);"function"===typeof a.render?E("188"):E("213",Object.keys(a))},hydrate:function(a,b,c){return Pg(null,a,b,!0,c)},render:function(a,b,c){return Pg(null,a,b,!1,c)},unstable_renderSubtreeIntoContainer:function(a,b,c,d){null==a||void 0===a._reactInternalFiber?E("38"):void 0;return Pg(a,b,c,!1,d)},unmountComponentAtNode:function(a){Ng(a)?void 0:
E("40");return a._reactRootContainer?(Z.unbatchedUpdates(function(){Pg(null,null,a,!1,function(){a._reactRootContainer=null})}),!0):!1},unstable_createPortal:Qg,unstable_batchedUpdates:tc,unstable_deferredUpdates:Z.deferredUpdates,flushSync:Z.flushSync,__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED:{EventPluginHub:mb,EventPluginRegistry:Va,EventPropagators:Cb,ReactControlledComponent:qc,ReactDOMComponentTree:sb,ReactDOMEventListener:xd}};
Z.injectIntoDevTools({findFiberByHostInstance:pb,bundleType:0,version:"16.2.0",rendererPackageName:"react-dom"});var Tg=Object.freeze({default:Sg}),Ug=Tg&&Sg||Tg;module.exports=Ug["default"]?Ug["default"]:Ug;


/***/ }),
/* 22 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var isNode = __webpack_require__(23);

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM text node.
 */
function isTextNode(object) {
  return isNode(object) && object.nodeType == 3;
}

module.exports = isTextNode;

/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM node.
 */
function isNode(object) {
  var doc = object ? object.ownerDocument || object : document;
  var defaultView = doc.defaultView || window;
  return !!(object && (typeof defaultView.Node === 'function' ? object instanceof defaultView.Node : typeof object === 'object' && typeof object.nodeType === 'number' && typeof object.nodeName === 'string'));
}

module.exports = isNode;

/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/** @license React v16.2.0
 * react-dom.development.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (process.env.NODE_ENV !== "production") {
  (function() {
'use strict';

var React = __webpack_require__(4);
var invariant = __webpack_require__(5);
var warning = __webpack_require__(6);
var ExecutionEnvironment = __webpack_require__(8);
var _assign = __webpack_require__(2);
var emptyFunction = __webpack_require__(1);
var EventListener = __webpack_require__(9);
var getActiveElement = __webpack_require__(10);
var shallowEqual = __webpack_require__(11);
var containsNode = __webpack_require__(12);
var focusNode = __webpack_require__(13);
var emptyObject = __webpack_require__(3);
var checkPropTypes = __webpack_require__(7);
var hyphenateStyleName = __webpack_require__(25);
var camelizeStyleName = __webpack_require__(27);

/**
 * WARNING: DO NOT manually require this module.
 * This is a replacement for `invariant(...)` used by the error code system
 * and will _only_ be required by the corresponding babel pass.
 * It always throws.
 */

!React ? invariant(false, 'ReactDOM was loaded before React. Make sure you load the React package before loading ReactDOM.') : void 0;

// These attributes should be all lowercase to allow for
// case insensitive checks
var RESERVED_PROPS = {
  children: true,
  dangerouslySetInnerHTML: true,
  defaultValue: true,
  defaultChecked: true,
  innerHTML: true,
  suppressContentEditableWarning: true,
  suppressHydrationWarning: true,
  style: true
};

function checkMask(value, bitmask) {
  return (value & bitmask) === bitmask;
}

var DOMPropertyInjection = {
  /**
   * Mapping from normalized, camelcased property names to a configuration that
   * specifies how the associated DOM property should be accessed or rendered.
   */
  MUST_USE_PROPERTY: 0x1,
  HAS_BOOLEAN_VALUE: 0x4,
  HAS_NUMERIC_VALUE: 0x8,
  HAS_POSITIVE_NUMERIC_VALUE: 0x10 | 0x8,
  HAS_OVERLOADED_BOOLEAN_VALUE: 0x20,
  HAS_STRING_BOOLEAN_VALUE: 0x40,

  /**
   * Inject some specialized knowledge about the DOM. This takes a config object
   * with the following properties:
   *
   * Properties: object mapping DOM property name to one of the
   * DOMPropertyInjection constants or null. If your attribute isn't in here,
   * it won't get written to the DOM.
   *
   * DOMAttributeNames: object mapping React attribute name to the DOM
   * attribute name. Attribute names not specified use the **lowercase**
   * normalized name.
   *
   * DOMAttributeNamespaces: object mapping React attribute name to the DOM
   * attribute namespace URL. (Attribute names not specified use no namespace.)
   *
   * DOMPropertyNames: similar to DOMAttributeNames but for DOM properties.
   * Property names not specified use the normalized name.
   *
   * DOMMutationMethods: Properties that require special mutation methods. If
   * `value` is undefined, the mutation method should unset the property.
   *
   * @param {object} domPropertyConfig the config as described above.
   */
  injectDOMPropertyConfig: function (domPropertyConfig) {
    var Injection = DOMPropertyInjection;
    var Properties = domPropertyConfig.Properties || {};
    var DOMAttributeNamespaces = domPropertyConfig.DOMAttributeNamespaces || {};
    var DOMAttributeNames = domPropertyConfig.DOMAttributeNames || {};
    var DOMMutationMethods = domPropertyConfig.DOMMutationMethods || {};

    for (var propName in Properties) {
      !!properties.hasOwnProperty(propName) ? invariant(false, "injectDOMPropertyConfig(...): You're trying to inject DOM property '%s' which has already been injected. You may be accidentally injecting the same DOM property config twice, or you may be injecting two configs that have conflicting property names.", propName) : void 0;

      var lowerCased = propName.toLowerCase();
      var propConfig = Properties[propName];

      var propertyInfo = {
        attributeName: lowerCased,
        attributeNamespace: null,
        propertyName: propName,
        mutationMethod: null,

        mustUseProperty: checkMask(propConfig, Injection.MUST_USE_PROPERTY),
        hasBooleanValue: checkMask(propConfig, Injection.HAS_BOOLEAN_VALUE),
        hasNumericValue: checkMask(propConfig, Injection.HAS_NUMERIC_VALUE),
        hasPositiveNumericValue: checkMask(propConfig, Injection.HAS_POSITIVE_NUMERIC_VALUE),
        hasOverloadedBooleanValue: checkMask(propConfig, Injection.HAS_OVERLOADED_BOOLEAN_VALUE),
        hasStringBooleanValue: checkMask(propConfig, Injection.HAS_STRING_BOOLEAN_VALUE)
      };
      !(propertyInfo.hasBooleanValue + propertyInfo.hasNumericValue + propertyInfo.hasOverloadedBooleanValue <= 1) ? invariant(false, "DOMProperty: Value can be one of boolean, overloaded boolean, or numeric value, but not a combination: %s", propName) : void 0;

      if (DOMAttributeNames.hasOwnProperty(propName)) {
        var attributeName = DOMAttributeNames[propName];

        propertyInfo.attributeName = attributeName;
      }

      if (DOMAttributeNamespaces.hasOwnProperty(propName)) {
        propertyInfo.attributeNamespace = DOMAttributeNamespaces[propName];
      }

      if (DOMMutationMethods.hasOwnProperty(propName)) {
        propertyInfo.mutationMethod = DOMMutationMethods[propName];
      }

      // Downcase references to whitelist properties to check for membership
      // without case-sensitivity. This allows the whitelist to pick up
      // `allowfullscreen`, which should be written using the property configuration
      // for `allowFullscreen`
      properties[propName] = propertyInfo;
    }
  }
};

/* eslint-disable max-len */
var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
/* eslint-enable max-len */
var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";


var ROOT_ATTRIBUTE_NAME = 'data-reactroot';

/**
 * Map from property "standard name" to an object with info about how to set
 * the property in the DOM. Each object contains:
 *
 * attributeName:
 *   Used when rendering markup or with `*Attribute()`.
 * attributeNamespace
 * propertyName:
 *   Used on DOM node instances. (This includes properties that mutate due to
 *   external factors.)
 * mutationMethod:
 *   If non-null, used instead of the property or `setAttribute()` after
 *   initial render.
 * mustUseProperty:
 *   Whether the property must be accessed and mutated as an object property.
 * hasBooleanValue:
 *   Whether the property should be removed when set to a falsey value.
 * hasNumericValue:
 *   Whether the property must be numeric or parse as a numeric and should be
 *   removed when set to a falsey value.
 * hasPositiveNumericValue:
 *   Whether the property must be positive numeric or parse as a positive
 *   numeric and should be removed when set to a falsey value.
 * hasOverloadedBooleanValue:
 *   Whether the property can be used as a flag as well as with a value.
 *   Removed when strictly equal to false; present without a value when
 *   strictly equal to true; present with a value otherwise.
 */
var properties = {};

/**
 * Checks whether a property name is a writeable attribute.
 * @method
 */
function shouldSetAttribute(name, value) {
  if (isReservedProp(name)) {
    return false;
  }
  if (name.length > 2 && (name[0] === 'o' || name[0] === 'O') && (name[1] === 'n' || name[1] === 'N')) {
    return false;
  }
  if (value === null) {
    return true;
  }
  switch (typeof value) {
    case 'boolean':
      return shouldAttributeAcceptBooleanValue(name);
    case 'undefined':
    case 'number':
    case 'string':
    case 'object':
      return true;
    default:
      // function, symbol
      return false;
  }
}

function getPropertyInfo(name) {
  return properties.hasOwnProperty(name) ? properties[name] : null;
}

function shouldAttributeAcceptBooleanValue(name) {
  if (isReservedProp(name)) {
    return true;
  }
  var propertyInfo = getPropertyInfo(name);
  if (propertyInfo) {
    return propertyInfo.hasBooleanValue || propertyInfo.hasStringBooleanValue || propertyInfo.hasOverloadedBooleanValue;
  }
  var prefix = name.toLowerCase().slice(0, 5);
  return prefix === 'data-' || prefix === 'aria-';
}

/**
 * Checks to see if a property name is within the list of properties
 * reserved for internal React operations. These properties should
 * not be set on an HTML element.
 *
 * @private
 * @param {string} name
 * @return {boolean} If the name is within reserved props
 */
function isReservedProp(name) {
  return RESERVED_PROPS.hasOwnProperty(name);
}

var injection = DOMPropertyInjection;

var MUST_USE_PROPERTY = injection.MUST_USE_PROPERTY;
var HAS_BOOLEAN_VALUE = injection.HAS_BOOLEAN_VALUE;
var HAS_NUMERIC_VALUE = injection.HAS_NUMERIC_VALUE;
var HAS_POSITIVE_NUMERIC_VALUE = injection.HAS_POSITIVE_NUMERIC_VALUE;
var HAS_OVERLOADED_BOOLEAN_VALUE = injection.HAS_OVERLOADED_BOOLEAN_VALUE;
var HAS_STRING_BOOLEAN_VALUE = injection.HAS_STRING_BOOLEAN_VALUE;

var HTMLDOMPropertyConfig = {
  // When adding attributes to this list, be sure to also add them to
  // the `possibleStandardNames` module to ensure casing and incorrect
  // name warnings.
  Properties: {
    allowFullScreen: HAS_BOOLEAN_VALUE,
    // specifies target context for links with `preload` type
    async: HAS_BOOLEAN_VALUE,
    // Note: there is a special case that prevents it from being written to the DOM
    // on the client side because the browsers are inconsistent. Instead we call focus().
    autoFocus: HAS_BOOLEAN_VALUE,
    autoPlay: HAS_BOOLEAN_VALUE,
    capture: HAS_OVERLOADED_BOOLEAN_VALUE,
    checked: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    cols: HAS_POSITIVE_NUMERIC_VALUE,
    contentEditable: HAS_STRING_BOOLEAN_VALUE,
    controls: HAS_BOOLEAN_VALUE,
    'default': HAS_BOOLEAN_VALUE,
    defer: HAS_BOOLEAN_VALUE,
    disabled: HAS_BOOLEAN_VALUE,
    download: HAS_OVERLOADED_BOOLEAN_VALUE,
    draggable: HAS_STRING_BOOLEAN_VALUE,
    formNoValidate: HAS_BOOLEAN_VALUE,
    hidden: HAS_BOOLEAN_VALUE,
    loop: HAS_BOOLEAN_VALUE,
    // Caution; `option.selected` is not updated if `select.multiple` is
    // disabled with `removeAttribute`.
    multiple: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    muted: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    noValidate: HAS_BOOLEAN_VALUE,
    open: HAS_BOOLEAN_VALUE,
    playsInline: HAS_BOOLEAN_VALUE,
    readOnly: HAS_BOOLEAN_VALUE,
    required: HAS_BOOLEAN_VALUE,
    reversed: HAS_BOOLEAN_VALUE,
    rows: HAS_POSITIVE_NUMERIC_VALUE,
    rowSpan: HAS_NUMERIC_VALUE,
    scoped: HAS_BOOLEAN_VALUE,
    seamless: HAS_BOOLEAN_VALUE,
    selected: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    size: HAS_POSITIVE_NUMERIC_VALUE,
    start: HAS_NUMERIC_VALUE,
    // support for projecting regular DOM Elements via V1 named slots ( shadow dom )
    span: HAS_POSITIVE_NUMERIC_VALUE,
    spellCheck: HAS_STRING_BOOLEAN_VALUE,
    // Style must be explicitly set in the attribute list. React components
    // expect a style object
    style: 0,
    // Keep it in the whitelist because it is case-sensitive for SVG.
    tabIndex: 0,
    // itemScope is for for Microdata support.
    // See http://schema.org/docs/gs.html
    itemScope: HAS_BOOLEAN_VALUE,
    // These attributes must stay in the white-list because they have
    // different attribute names (see DOMAttributeNames below)
    acceptCharset: 0,
    className: 0,
    htmlFor: 0,
    httpEquiv: 0,
    // Attributes with mutation methods must be specified in the whitelist
    // Set the string boolean flag to allow the behavior
    value: HAS_STRING_BOOLEAN_VALUE
  },
  DOMAttributeNames: {
    acceptCharset: 'accept-charset',
    className: 'class',
    htmlFor: 'for',
    httpEquiv: 'http-equiv'
  },
  DOMMutationMethods: {
    value: function (node, value) {
      if (value == null) {
        return node.removeAttribute('value');
      }

      // Number inputs get special treatment due to some edge cases in
      // Chrome. Let everything else assign the value attribute as normal.
      // https://github.com/facebook/react/issues/7253#issuecomment-236074326
      if (node.type !== 'number' || node.hasAttribute('value') === false) {
        node.setAttribute('value', '' + value);
      } else if (node.validity && !node.validity.badInput && node.ownerDocument.activeElement !== node) {
        // Don't assign an attribute if validation reports bad
        // input. Chrome will clear the value. Additionally, don't
        // operate on inputs that have focus, otherwise Chrome might
        // strip off trailing decimal places and cause the user's
        // cursor position to jump to the beginning of the input.
        //
        // In ReactDOMInput, we have an onBlur event that will trigger
        // this function again when focus is lost.
        node.setAttribute('value', '' + value);
      }
    }
  }
};

var HAS_STRING_BOOLEAN_VALUE$1 = injection.HAS_STRING_BOOLEAN_VALUE;


var NS = {
  xlink: 'http://www.w3.org/1999/xlink',
  xml: 'http://www.w3.org/XML/1998/namespace'
};

/**
 * This is a list of all SVG attributes that need special casing,
 * namespacing, or boolean value assignment.
 *
 * When adding attributes to this list, be sure to also add them to
 * the `possibleStandardNames` module to ensure casing and incorrect
 * name warnings.
 *
 * SVG Attributes List:
 * https://www.w3.org/TR/SVG/attindex.html
 * SMIL Spec:
 * https://www.w3.org/TR/smil
 */
var ATTRS = ['accent-height', 'alignment-baseline', 'arabic-form', 'baseline-shift', 'cap-height', 'clip-path', 'clip-rule', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'dominant-baseline', 'enable-background', 'fill-opacity', 'fill-rule', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'glyph-name', 'glyph-orientation-horizontal', 'glyph-orientation-vertical', 'horiz-adv-x', 'horiz-origin-x', 'image-rendering', 'letter-spacing', 'lighting-color', 'marker-end', 'marker-mid', 'marker-start', 'overline-position', 'overline-thickness', 'paint-order', 'panose-1', 'pointer-events', 'rendering-intent', 'shape-rendering', 'stop-color', 'stop-opacity', 'strikethrough-position', 'strikethrough-thickness', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke-width', 'text-anchor', 'text-decoration', 'text-rendering', 'underline-position', 'underline-thickness', 'unicode-bidi', 'unicode-range', 'units-per-em', 'v-alphabetic', 'v-hanging', 'v-ideographic', 'v-mathematical', 'vector-effect', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'word-spacing', 'writing-mode', 'x-height', 'xlink:actuate', 'xlink:arcrole', 'xlink:href', 'xlink:role', 'xlink:show', 'xlink:title', 'xlink:type', 'xml:base', 'xmlns:xlink', 'xml:lang', 'xml:space'];

var SVGDOMPropertyConfig = {
  Properties: {
    autoReverse: HAS_STRING_BOOLEAN_VALUE$1,
    externalResourcesRequired: HAS_STRING_BOOLEAN_VALUE$1,
    preserveAlpha: HAS_STRING_BOOLEAN_VALUE$1
  },
  DOMAttributeNames: {
    autoReverse: 'autoReverse',
    externalResourcesRequired: 'externalResourcesRequired',
    preserveAlpha: 'preserveAlpha'
  },
  DOMAttributeNamespaces: {


    xmlSpace: NS.xml
  }
};

var CAMELIZE = /[\-\:]([a-z])/g;
var capitalize = function (token) {
  return token[1].toUpperCase();
};

ATTRS.forEach(function (original) {
  var reactName = original.replace(CAMELIZE, capitalize);

  SVGDOMPropertyConfig.Properties[reactName] = 0;
  SVGDOMPropertyConfig.DOMAttributeNames[reactName] = original;
});

injection.injectDOMPropertyConfig(HTMLDOMPropertyConfig);
injection.injectDOMPropertyConfig(SVGDOMPropertyConfig);

var ReactErrorUtils = {
  // Used by Fiber to simulate a try-catch.
  _caughtError: null,
  _hasCaughtError: false,

  // Used by event system to capture/rethrow the first error.
  _rethrowError: null,
  _hasRethrowError: false,

  injection: {
    injectErrorUtils: function (injectedErrorUtils) {
      !(typeof injectedErrorUtils.invokeGuardedCallback === 'function') ? invariant(false, 'Injected invokeGuardedCallback() must be a function.') : void 0;
      invokeGuardedCallback = injectedErrorUtils.invokeGuardedCallback;
    }
  },

  /**
   * Call a function while guarding against errors that happens within it.
   * Returns an error if it throws, otherwise null.
   *
   * In production, this is implemented using a try-catch. The reason we don't
   * use a try-catch directly is so that we can swap out a different
   * implementation in DEV mode.
   *
   * @param {String} name of the guard to use for logging or debugging
   * @param {Function} func The function to invoke
   * @param {*} context The context to use when calling the function
   * @param {...*} args Arguments for function
   */
  invokeGuardedCallback: function (name, func, context, a, b, c, d, e, f) {
    invokeGuardedCallback.apply(ReactErrorUtils, arguments);
  },

  /**
   * Same as invokeGuardedCallback, but instead of returning an error, it stores
   * it in a global so it can be rethrown by `rethrowCaughtError` later.
   * TODO: See if _caughtError and _rethrowError can be unified.
   *
   * @param {String} name of the guard to use for logging or debugging
   * @param {Function} func The function to invoke
   * @param {*} context The context to use when calling the function
   * @param {...*} args Arguments for function
   */
  invokeGuardedCallbackAndCatchFirstError: function (name, func, context, a, b, c, d, e, f) {
    ReactErrorUtils.invokeGuardedCallback.apply(this, arguments);
    if (ReactErrorUtils.hasCaughtError()) {
      var error = ReactErrorUtils.clearCaughtError();
      if (!ReactErrorUtils._hasRethrowError) {
        ReactErrorUtils._hasRethrowError = true;
        ReactErrorUtils._rethrowError = error;
      }
    }
  },

  /**
   * During execution of guarded functions we will capture the first error which
   * we will rethrow to be handled by the top level error handler.
   */
  rethrowCaughtError: function () {
    return rethrowCaughtError.apply(ReactErrorUtils, arguments);
  },

  hasCaughtError: function () {
    return ReactErrorUtils._hasCaughtError;
  },

  clearCaughtError: function () {
    if (ReactErrorUtils._hasCaughtError) {
      var error = ReactErrorUtils._caughtError;
      ReactErrorUtils._caughtError = null;
      ReactErrorUtils._hasCaughtError = false;
      return error;
    } else {
      invariant(false, 'clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue.');
    }
  }
};

var invokeGuardedCallback = function (name, func, context, a, b, c, d, e, f) {
  ReactErrorUtils._hasCaughtError = false;
  ReactErrorUtils._caughtError = null;
  var funcArgs = Array.prototype.slice.call(arguments, 3);
  try {
    func.apply(context, funcArgs);
  } catch (error) {
    ReactErrorUtils._caughtError = error;
    ReactErrorUtils._hasCaughtError = true;
  }
};

{
  // In DEV mode, we swap out invokeGuardedCallback for a special version
  // that plays more nicely with the browser's DevTools. The idea is to preserve
  // "Pause on exceptions" behavior. Because React wraps all user-provided
  // functions in invokeGuardedCallback, and the production version of
  // invokeGuardedCallback uses a try-catch, all user exceptions are treated
  // like caught exceptions, and the DevTools won't pause unless the developer
  // takes the extra step of enabling pause on caught exceptions. This is
  // untintuitive, though, because even though React has caught the error, from
  // the developer's perspective, the error is uncaught.
  //
  // To preserve the expected "Pause on exceptions" behavior, we don't use a
  // try-catch in DEV. Instead, we synchronously dispatch a fake event to a fake
  // DOM node, and call the user-provided callback from inside an event handler
  // for that fake event. If the callback throws, the error is "captured" using
  // a global event handler. But because the error happens in a different
  // event loop context, it does not interrupt the normal program flow.
  // Effectively, this gives us try-catch behavior without actually using
  // try-catch. Neat!

  // Check that the browser supports the APIs we need to implement our special
  // DEV version of invokeGuardedCallback
  if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
    var fakeNode = document.createElement('react');

    var invokeGuardedCallbackDev = function (name, func, context, a, b, c, d, e, f) {
      // Keeps track of whether the user-provided callback threw an error. We
      // set this to true at the beginning, then set it to false right after
      // calling the function. If the function errors, `didError` will never be
      // set to false. This strategy works even if the browser is flaky and
      // fails to call our global error handler, because it doesn't rely on
      // the error event at all.
      var didError = true;

      // Create an event handler for our fake event. We will synchronously
      // dispatch our fake event using `dispatchEvent`. Inside the handler, we
      // call the user-provided callback.
      var funcArgs = Array.prototype.slice.call(arguments, 3);
      function callCallback() {
        // We immediately remove the callback from event listeners so that
        // nested `invokeGuardedCallback` calls do not clash. Otherwise, a
        // nested call would trigger the fake event handlers of any call higher
        // in the stack.
        fakeNode.removeEventListener(evtType, callCallback, false);
        func.apply(context, funcArgs);
        didError = false;
      }

      // Create a global error event handler. We use this to capture the value
      // that was thrown. It's possible that this error handler will fire more
      // than once; for example, if non-React code also calls `dispatchEvent`
      // and a handler for that event throws. We should be resilient to most of
      // those cases. Even if our error event handler fires more than once, the
      // last error event is always used. If the callback actually does error,
      // we know that the last error event is the correct one, because it's not
      // possible for anything else to have happened in between our callback
      // erroring and the code that follows the `dispatchEvent` call below. If
      // the callback doesn't error, but the error event was fired, we know to
      // ignore it because `didError` will be false, as described above.
      var error = void 0;
      // Use this to track whether the error event is ever called.
      var didSetError = false;
      var isCrossOriginError = false;

      function onError(event) {
        error = event.error;
        didSetError = true;
        if (error === null && event.colno === 0 && event.lineno === 0) {
          isCrossOriginError = true;
        }
      }

      // Create a fake event type.
      var evtType = 'react-' + (name ? name : 'invokeguardedcallback');

      // Attach our event handlers
      window.addEventListener('error', onError);
      fakeNode.addEventListener(evtType, callCallback, false);

      // Synchronously dispatch our fake event. If the user-provided function
      // errors, it will trigger our global error handler.
      var evt = document.createEvent('Event');
      evt.initEvent(evtType, false, false);
      fakeNode.dispatchEvent(evt);

      if (didError) {
        if (!didSetError) {
          // The callback errored, but the error event never fired.
          error = new Error('An error was thrown inside one of your components, but React ' + "doesn't know what it was. This is likely due to browser " + 'flakiness. React does its best to preserve the "Pause on ' + 'exceptions" behavior of the DevTools, which requires some ' + "DEV-mode only tricks. It's possible that these don't work in " + 'your browser. Try triggering the error in production mode, ' + 'or switching to a modern browser. If you suspect that this is ' + 'actually an issue with React, please file an issue.');
        } else if (isCrossOriginError) {
          error = new Error("A cross-origin error was thrown. React doesn't have access to " + 'the actual error object in development. ' + 'See https://fb.me/react-crossorigin-error for more information.');
        }
        ReactErrorUtils._hasCaughtError = true;
        ReactErrorUtils._caughtError = error;
      } else {
        ReactErrorUtils._hasCaughtError = false;
        ReactErrorUtils._caughtError = null;
      }

      // Remove our event listeners
      window.removeEventListener('error', onError);
    };

    invokeGuardedCallback = invokeGuardedCallbackDev;
  }
}

var rethrowCaughtError = function () {
  if (ReactErrorUtils._hasRethrowError) {
    var error = ReactErrorUtils._rethrowError;
    ReactErrorUtils._rethrowError = null;
    ReactErrorUtils._hasRethrowError = false;
    throw error;
  }
};

/**
 * Injectable ordering of event plugins.
 */
var eventPluginOrder = null;

/**
 * Injectable mapping from names to event plugin modules.
 */
var namesToPlugins = {};

/**
 * Recomputes the plugin list using the injected plugins and plugin ordering.
 *
 * @private
 */
function recomputePluginOrdering() {
  if (!eventPluginOrder) {
    // Wait until an `eventPluginOrder` is injected.
    return;
  }
  for (var pluginName in namesToPlugins) {
    var pluginModule = namesToPlugins[pluginName];
    var pluginIndex = eventPluginOrder.indexOf(pluginName);
    !(pluginIndex > -1) ? invariant(false, 'EventPluginRegistry: Cannot inject event plugins that do not exist in the plugin ordering, `%s`.', pluginName) : void 0;
    if (plugins[pluginIndex]) {
      continue;
    }
    !pluginModule.extractEvents ? invariant(false, 'EventPluginRegistry: Event plugins must implement an `extractEvents` method, but `%s` does not.', pluginName) : void 0;
    plugins[pluginIndex] = pluginModule;
    var publishedEvents = pluginModule.eventTypes;
    for (var eventName in publishedEvents) {
      !publishEventForPlugin(publishedEvents[eventName], pluginModule, eventName) ? invariant(false, 'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.', eventName, pluginName) : void 0;
    }
  }
}

/**
 * Publishes an event so that it can be dispatched by the supplied plugin.
 *
 * @param {object} dispatchConfig Dispatch configuration for the event.
 * @param {object} PluginModule Plugin publishing the event.
 * @return {boolean} True if the event was successfully published.
 * @private
 */
function publishEventForPlugin(dispatchConfig, pluginModule, eventName) {
  !!eventNameDispatchConfigs.hasOwnProperty(eventName) ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same event name, `%s`.', eventName) : void 0;
  eventNameDispatchConfigs[eventName] = dispatchConfig;

  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
  if (phasedRegistrationNames) {
    for (var phaseName in phasedRegistrationNames) {
      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
        var phasedRegistrationName = phasedRegistrationNames[phaseName];
        publishRegistrationName(phasedRegistrationName, pluginModule, eventName);
      }
    }
    return true;
  } else if (dispatchConfig.registrationName) {
    publishRegistrationName(dispatchConfig.registrationName, pluginModule, eventName);
    return true;
  }
  return false;
}

/**
 * Publishes a registration name that is used to identify dispatched events.
 *
 * @param {string} registrationName Registration name to add.
 * @param {object} PluginModule Plugin publishing the event.
 * @private
 */
function publishRegistrationName(registrationName, pluginModule, eventName) {
  !!registrationNameModules[registrationName] ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same registration name, `%s`.', registrationName) : void 0;
  registrationNameModules[registrationName] = pluginModule;
  registrationNameDependencies[registrationName] = pluginModule.eventTypes[eventName].dependencies;

  {
    var lowerCasedName = registrationName.toLowerCase();
    possibleRegistrationNames[lowerCasedName] = registrationName;

    if (registrationName === 'onDoubleClick') {
      possibleRegistrationNames.ondblclick = registrationName;
    }
  }
}

/**
 * Registers plugins so that they can extract and dispatch events.
 *
 * @see {EventPluginHub}
 */

/**
 * Ordered list of injected plugins.
 */
var plugins = [];

/**
 * Mapping from event name to dispatch config
 */
var eventNameDispatchConfigs = {};

/**
 * Mapping from registration name to plugin module
 */
var registrationNameModules = {};

/**
 * Mapping from registration name to event name
 */
var registrationNameDependencies = {};

/**
 * Mapping from lowercase registration names to the properly cased version,
 * used to warn in the case of missing event handlers. Available
 * only in true.
 * @type {Object}
 */
var possibleRegistrationNames = {};
// Trust the developer to only use possibleRegistrationNames in true

/**
 * Injects an ordering of plugins (by plugin name). This allows the ordering
 * to be decoupled from injection of the actual plugins so that ordering is
 * always deterministic regardless of packaging, on-the-fly injection, etc.
 *
 * @param {array} InjectedEventPluginOrder
 * @internal
 * @see {EventPluginHub.injection.injectEventPluginOrder}
 */
function injectEventPluginOrder(injectedEventPluginOrder) {
  !!eventPluginOrder ? invariant(false, 'EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React.') : void 0;
  // Clone the ordering so it cannot be dynamically mutated.
  eventPluginOrder = Array.prototype.slice.call(injectedEventPluginOrder);
  recomputePluginOrdering();
}

/**
 * Injects plugins to be used by `EventPluginHub`. The plugin names must be
 * in the ordering injected by `injectEventPluginOrder`.
 *
 * Plugins can be injected as part of page initialization or on-the-fly.
 *
 * @param {object} injectedNamesToPlugins Map from names to plugin modules.
 * @internal
 * @see {EventPluginHub.injection.injectEventPluginsByName}
 */
function injectEventPluginsByName(injectedNamesToPlugins) {
  var isOrderingDirty = false;
  for (var pluginName in injectedNamesToPlugins) {
    if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
      continue;
    }
    var pluginModule = injectedNamesToPlugins[pluginName];
    if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== pluginModule) {
      !!namesToPlugins[pluginName] ? invariant(false, 'EventPluginRegistry: Cannot inject two different event plugins using the same name, `%s`.', pluginName) : void 0;
      namesToPlugins[pluginName] = pluginModule;
      isOrderingDirty = true;
    }
  }
  if (isOrderingDirty) {
    recomputePluginOrdering();
  }
}

var EventPluginRegistry = Object.freeze({
	plugins: plugins,
	eventNameDispatchConfigs: eventNameDispatchConfigs,
	registrationNameModules: registrationNameModules,
	registrationNameDependencies: registrationNameDependencies,
	possibleRegistrationNames: possibleRegistrationNames,
	injectEventPluginOrder: injectEventPluginOrder,
	injectEventPluginsByName: injectEventPluginsByName
});

var getFiberCurrentPropsFromNode = null;
var getInstanceFromNode = null;
var getNodeFromInstance = null;

var injection$2 = {
  injectComponentTree: function (Injected) {
    getFiberCurrentPropsFromNode = Injected.getFiberCurrentPropsFromNode;
    getInstanceFromNode = Injected.getInstanceFromNode;
    getNodeFromInstance = Injected.getNodeFromInstance;

    {
      warning(getNodeFromInstance && getInstanceFromNode, 'EventPluginUtils.injection.injectComponentTree(...): Injected ' + 'module is missing getNodeFromInstance or getInstanceFromNode.');
    }
  }
};






var validateEventDispatches;
{
  validateEventDispatches = function (event) {
    var dispatchListeners = event._dispatchListeners;
    var dispatchInstances = event._dispatchInstances;

    var listenersIsArr = Array.isArray(dispatchListeners);
    var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;

    var instancesIsArr = Array.isArray(dispatchInstances);
    var instancesLen = instancesIsArr ? dispatchInstances.length : dispatchInstances ? 1 : 0;

    warning(instancesIsArr === listenersIsArr && instancesLen === listenersLen, 'EventPluginUtils: Invalid `event`.');
  };
}

/**
 * Dispatch the event to the listener.
 * @param {SyntheticEvent} event SyntheticEvent to handle
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @param {function} listener Application-level callback
 * @param {*} inst Internal component instance
 */
function executeDispatch(event, simulated, listener, inst) {
  var type = event.type || 'unknown-event';
  event.currentTarget = getNodeFromInstance(inst);
  ReactErrorUtils.invokeGuardedCallbackAndCatchFirstError(type, listener, undefined, event);
  event.currentTarget = null;
}

/**
 * Standard/simple iteration through an event's collected dispatches.
 */
function executeDispatchesInOrder(event, simulated) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchInstances = event._dispatchInstances;
  {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and Instances are two parallel arrays that are always in sync.
      executeDispatch(event, simulated, dispatchListeners[i], dispatchInstances[i]);
    }
  } else if (dispatchListeners) {
    executeDispatch(event, simulated, dispatchListeners, dispatchInstances);
  }
  event._dispatchListeners = null;
  event._dispatchInstances = null;
}

/**
 * @see executeDispatchesInOrderStopAtTrueImpl
 */


/**
 * Execution of a "direct" dispatch - there must be at most one dispatch
 * accumulated on the event or it is considered an error. It doesn't really make
 * sense for an event with multiple dispatches (bubbled) to keep track of the
 * return values at each dispatch execution, but it does tend to make sense when
 * dealing with "direct" dispatches.
 *
 * @return {*} The return value of executing the single dispatch.
 */


/**
 * @param {SyntheticEvent} event
 * @return {boolean} True iff number of dispatches accumulated is greater than 0.
 */

/**
 * Accumulates items that must not be null or undefined into the first one. This
 * is used to conserve memory by avoiding array allocations, and thus sacrifices
 * API cleanness. Since `current` can be null before being passed in and not
 * null after this function, make sure to assign it back to `current`:
 *
 * `a = accumulateInto(a, b);`
 *
 * This API should be sparingly used. Try `accumulate` for something cleaner.
 *
 * @return {*|array<*>} An accumulation of items.
 */

function accumulateInto(current, next) {
  !(next != null) ? invariant(false, 'accumulateInto(...): Accumulated items must not be null or undefined.') : void 0;

  if (current == null) {
    return next;
  }

  // Both are not empty. Warning: Never call x.concat(y) when you are not
  // certain that x is an Array (x could be a string with concat method).
  if (Array.isArray(current)) {
    if (Array.isArray(next)) {
      current.push.apply(current, next);
      return current;
    }
    current.push(next);
    return current;
  }

  if (Array.isArray(next)) {
    // A bit too dangerous to mutate `next`.
    return [current].concat(next);
  }

  return [current, next];
}

/**
 * @param {array} arr an "accumulation" of items which is either an Array or
 * a single item. Useful when paired with the `accumulate` module. This is a
 * simple utility that allows us to reason about a collection of items, but
 * handling the case when there is exactly one item (and we do not need to
 * allocate an array).
 * @param {function} cb Callback invoked with each element or a collection.
 * @param {?} [scope] Scope used as `this` in a callback.
 */
function forEachAccumulated(arr, cb, scope) {
  if (Array.isArray(arr)) {
    arr.forEach(cb, scope);
  } else if (arr) {
    cb.call(scope, arr);
  }
}

/**
 * Internal queue of events that have accumulated their dispatches and are
 * waiting to have their dispatches executed.
 */
var eventQueue = null;

/**
 * Dispatches an event and releases it back into the pool, unless persistent.
 *
 * @param {?object} event Synthetic event to be dispatched.
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @private
 */
var executeDispatchesAndRelease = function (event, simulated) {
  if (event) {
    executeDispatchesInOrder(event, simulated);

    if (!event.isPersistent()) {
      event.constructor.release(event);
    }
  }
};
var executeDispatchesAndReleaseSimulated = function (e) {
  return executeDispatchesAndRelease(e, true);
};
var executeDispatchesAndReleaseTopLevel = function (e) {
  return executeDispatchesAndRelease(e, false);
};

function isInteractive(tag) {
  return tag === 'button' || tag === 'input' || tag === 'select' || tag === 'textarea';
}

function shouldPreventMouseEvent(name, type, props) {
  switch (name) {
    case 'onClick':
    case 'onClickCapture':
    case 'onDoubleClick':
    case 'onDoubleClickCapture':
    case 'onMouseDown':
    case 'onMouseDownCapture':
    case 'onMouseMove':
    case 'onMouseMoveCapture':
    case 'onMouseUp':
    case 'onMouseUpCapture':
      return !!(props.disabled && isInteractive(type));
    default:
      return false;
  }
}

/**
 * This is a unified interface for event plugins to be installed and configured.
 *
 * Event plugins can implement the following properties:
 *
 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
 *     Required. When a top-level event is fired, this method is expected to
 *     extract synthetic events that will in turn be queued and dispatched.
 *
 *   `eventTypes` {object}
 *     Optional, plugins that fire events must publish a mapping of registration
 *     names that are used to register listeners. Values of this mapping must
 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
 *
 *   `executeDispatch` {function(object, function, string)}
 *     Optional, allows plugins to override how an event gets dispatched. By
 *     default, the listener is simply invoked.
 *
 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
 *
 * @public
 */

/**
 * Methods for injecting dependencies.
 */
var injection$1 = {
  /**
   * @param {array} InjectedEventPluginOrder
   * @public
   */
  injectEventPluginOrder: injectEventPluginOrder,

  /**
   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
   */
  injectEventPluginsByName: injectEventPluginsByName
};

/**
 * @param {object} inst The instance, which is the source of events.
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @return {?function} The stored callback.
 */
function getListener(inst, registrationName) {
  var listener;

  // TODO: shouldPreventMouseEvent is DOM-specific and definitely should not
  // live here; needs to be moved to a better place soon
  var stateNode = inst.stateNode;
  if (!stateNode) {
    // Work in progress (ex: onload events in incremental mode).
    return null;
  }
  var props = getFiberCurrentPropsFromNode(stateNode);
  if (!props) {
    // Work in progress.
    return null;
  }
  listener = props[registrationName];
  if (shouldPreventMouseEvent(registrationName, inst.type, props)) {
    return null;
  }
  !(!listener || typeof listener === 'function') ? invariant(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.', registrationName, typeof listener) : void 0;
  return listener;
}

/**
 * Allows registered plugins an opportunity to extract events from top-level
 * native browser events.
 *
 * @return {*} An accumulation of synthetic events.
 * @internal
 */
function extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var events;
  for (var i = 0; i < plugins.length; i++) {
    // Not every plugin in the ordering may be loaded at runtime.
    var possiblePlugin = plugins[i];
    if (possiblePlugin) {
      var extractedEvents = possiblePlugin.extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget);
      if (extractedEvents) {
        events = accumulateInto(events, extractedEvents);
      }
    }
  }
  return events;
}

/**
 * Enqueues a synthetic event that should be dispatched when
 * `processEventQueue` is invoked.
 *
 * @param {*} events An accumulation of synthetic events.
 * @internal
 */
function enqueueEvents(events) {
  if (events) {
    eventQueue = accumulateInto(eventQueue, events);
  }
}

/**
 * Dispatches all synthetic events on the event queue.
 *
 * @internal
 */
function processEventQueue(simulated) {
  // Set `eventQueue` to null before processing it so that we can tell if more
  // events get enqueued while processing.
  var processingEventQueue = eventQueue;
  eventQueue = null;

  if (!processingEventQueue) {
    return;
  }

  if (simulated) {
    forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseSimulated);
  } else {
    forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);
  }
  !!eventQueue ? invariant(false, 'processEventQueue(): Additional events were enqueued while processing an event queue. Support for this has not yet been implemented.') : void 0;
  // This would be a good time to rethrow if any of the event handlers threw.
  ReactErrorUtils.rethrowCaughtError();
}

var EventPluginHub = Object.freeze({
	injection: injection$1,
	getListener: getListener,
	extractEvents: extractEvents,
	enqueueEvents: enqueueEvents,
	processEventQueue: processEventQueue
});

var IndeterminateComponent = 0; // Before we know whether it is functional or class
var FunctionalComponent = 1;
var ClassComponent = 2;
var HostRoot = 3; // Root of a host tree. Could be nested inside another node.
var HostPortal = 4; // A subtree. Could be an entry point to a different renderer.
var HostComponent = 5;
var HostText = 6;
var CallComponent = 7;
var CallHandlerPhase = 8;
var ReturnComponent = 9;
var Fragment = 10;

var randomKey = Math.random().toString(36).slice(2);
var internalInstanceKey = '__reactInternalInstance$' + randomKey;
var internalEventHandlersKey = '__reactEventHandlers$' + randomKey;

function precacheFiberNode$1(hostInst, node) {
  node[internalInstanceKey] = hostInst;
}

/**
 * Given a DOM node, return the closest ReactDOMComponent or
 * ReactDOMTextComponent instance ancestor.
 */
function getClosestInstanceFromNode(node) {
  if (node[internalInstanceKey]) {
    return node[internalInstanceKey];
  }

  // Walk up the tree until we find an ancestor whose instance we have cached.
  var parents = [];
  while (!node[internalInstanceKey]) {
    parents.push(node);
    if (node.parentNode) {
      node = node.parentNode;
    } else {
      // Top of the tree. This node must not be part of a React tree (or is
      // unmounted, potentially).
      return null;
    }
  }

  var closest = void 0;
  var inst = node[internalInstanceKey];
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber, this will always be the deepest root.
    return inst;
  }
  for (; node && (inst = node[internalInstanceKey]); node = parents.pop()) {
    closest = inst;
  }

  return closest;
}

/**
 * Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
 * instance, or null if the node was not rendered by this React.
 */
function getInstanceFromNode$1(node) {
  var inst = node[internalInstanceKey];
  if (inst) {
    if (inst.tag === HostComponent || inst.tag === HostText) {
      return inst;
    } else {
      return null;
    }
  }
  return null;
}

/**
 * Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
 * DOM node.
 */
function getNodeFromInstance$1(inst) {
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber this, is just the state node right now. We assume it will be
    // a host component or host text.
    return inst.stateNode;
  }

  // Without this first invariant, passing a non-DOM-component triggers the next
  // invariant for a missing parent, which is super confusing.
  invariant(false, 'getNodeFromInstance: Invalid argument.');
}

function getFiberCurrentPropsFromNode$1(node) {
  return node[internalEventHandlersKey] || null;
}

function updateFiberProps$1(node, props) {
  node[internalEventHandlersKey] = props;
}

var ReactDOMComponentTree = Object.freeze({
	precacheFiberNode: precacheFiberNode$1,
	getClosestInstanceFromNode: getClosestInstanceFromNode,
	getInstanceFromNode: getInstanceFromNode$1,
	getNodeFromInstance: getNodeFromInstance$1,
	getFiberCurrentPropsFromNode: getFiberCurrentPropsFromNode$1,
	updateFiberProps: updateFiberProps$1
});

function getParent(inst) {
  do {
    inst = inst['return'];
    // TODO: If this is a HostRoot we might want to bail out.
    // That is depending on if we want nested subtrees (layers) to bubble
    // events to their parent. We could also go through parentNode on the
    // host node but that wouldn't work for React Native and doesn't let us
    // do the portal feature.
  } while (inst && inst.tag !== HostComponent);
  if (inst) {
    return inst;
  }
  return null;
}

/**
 * Return the lowest common ancestor of A and B, or null if they are in
 * different trees.
 */
function getLowestCommonAncestor(instA, instB) {
  var depthA = 0;
  for (var tempA = instA; tempA; tempA = getParent(tempA)) {
    depthA++;
  }
  var depthB = 0;
  for (var tempB = instB; tempB; tempB = getParent(tempB)) {
    depthB++;
  }

  // If A is deeper, crawl up.
  while (depthA - depthB > 0) {
    instA = getParent(instA);
    depthA--;
  }

  // If B is deeper, crawl up.
  while (depthB - depthA > 0) {
    instB = getParent(instB);
    depthB--;
  }

  // Walk in lockstep until we find a match.
  var depth = depthA;
  while (depth--) {
    if (instA === instB || instA === instB.alternate) {
      return instA;
    }
    instA = getParent(instA);
    instB = getParent(instB);
  }
  return null;
}

/**
 * Return if A is an ancestor of B.
 */


/**
 * Return the parent instance of the passed-in instance.
 */
function getParentInstance(inst) {
  return getParent(inst);
}

/**
 * Simulates the traversal of a two-phase, capture/bubble event dispatch.
 */
function traverseTwoPhase(inst, fn, arg) {
  var path = [];
  while (inst) {
    path.push(inst);
    inst = getParent(inst);
  }
  var i;
  for (i = path.length; i-- > 0;) {
    fn(path[i], 'captured', arg);
  }
  for (i = 0; i < path.length; i++) {
    fn(path[i], 'bubbled', arg);
  }
}

/**
 * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
 * should would receive a `mouseEnter` or `mouseLeave` event.
 *
 * Does not invoke the callback on the nearest common ancestor because nothing
 * "entered" or "left" that element.
 */
function traverseEnterLeave(from, to, fn, argFrom, argTo) {
  var common = from && to ? getLowestCommonAncestor(from, to) : null;
  var pathFrom = [];
  while (true) {
    if (!from) {
      break;
    }
    if (from === common) {
      break;
    }
    var alternate = from.alternate;
    if (alternate !== null && alternate === common) {
      break;
    }
    pathFrom.push(from);
    from = getParent(from);
  }
  var pathTo = [];
  while (true) {
    if (!to) {
      break;
    }
    if (to === common) {
      break;
    }
    var _alternate = to.alternate;
    if (_alternate !== null && _alternate === common) {
      break;
    }
    pathTo.push(to);
    to = getParent(to);
  }
  for (var i = 0; i < pathFrom.length; i++) {
    fn(pathFrom[i], 'bubbled', argFrom);
  }
  for (var _i = pathTo.length; _i-- > 0;) {
    fn(pathTo[_i], 'captured', argTo);
  }
}

/**
 * Some event types have a notion of different registration names for different
 * "phases" of propagation. This finds listeners by a given phase.
 */
function listenerAtPhase(inst, event, propagationPhase) {
  var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
  return getListener(inst, registrationName);
}

/**
 * A small set of propagation patterns, each of which will accept a small amount
 * of information, and generate a set of "dispatch ready event objects" - which
 * are sets of events that have already been annotated with a set of dispatched
 * listener functions/ids. The API is designed this way to discourage these
 * propagation strategies from actually executing the dispatches, since we
 * always want to collect the entire set of dispatches before executing even a
 * single one.
 */

/**
 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
 * here, allows us to not have to bind or create functions for each event.
 * Mutating the event's members allows us to not have to create a wrapping
 * "dispatch" object that pairs the event with the listener.
 */
function accumulateDirectionalDispatches(inst, phase, event) {
  {
    warning(inst, 'Dispatching inst must not be null');
  }
  var listener = listenerAtPhase(inst, event, phase);
  if (listener) {
    event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
    event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
  }
}

/**
 * Collect dispatches (must be entirely collected before dispatching - see unit
 * tests). Lazily allocate the array to conserve memory.  We must loop through
 * each event and perform the traversal for each one. We cannot perform a
 * single traversal for the entire collection of events because each event may
 * have a different target.
 */
function accumulateTwoPhaseDispatchesSingle(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    traverseTwoPhase(event._targetInst, accumulateDirectionalDispatches, event);
  }
}

/**
 * Same as `accumulateTwoPhaseDispatchesSingle`, but skips over the targetID.
 */
function accumulateTwoPhaseDispatchesSingleSkipTarget(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    var targetInst = event._targetInst;
    var parentInst = targetInst ? getParentInstance(targetInst) : null;
    traverseTwoPhase(parentInst, accumulateDirectionalDispatches, event);
  }
}

/**
 * Accumulates without regard to direction, does not look for phased
 * registration names. Same as `accumulateDirectDispatchesSingle` but without
 * requiring that the `dispatchMarker` be the same as the dispatched ID.
 */
function accumulateDispatches(inst, ignoredDirection, event) {
  if (inst && event && event.dispatchConfig.registrationName) {
    var registrationName = event.dispatchConfig.registrationName;
    var listener = getListener(inst, registrationName);
    if (listener) {
      event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
      event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
    }
  }
}

/**
 * Accumulates dispatches on an `SyntheticEvent`, but only for the
 * `dispatchMarker`.
 * @param {SyntheticEvent} event
 */
function accumulateDirectDispatchesSingle(event) {
  if (event && event.dispatchConfig.registrationName) {
    accumulateDispatches(event._targetInst, null, event);
  }
}

function accumulateTwoPhaseDispatches(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
}

function accumulateTwoPhaseDispatchesSkipTarget(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingleSkipTarget);
}

function accumulateEnterLeaveDispatches(leave, enter, from, to) {
  traverseEnterLeave(from, to, accumulateDispatches, leave, enter);
}

function accumulateDirectDispatches(events) {
  forEachAccumulated(events, accumulateDirectDispatchesSingle);
}

var EventPropagators = Object.freeze({
	accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
	accumulateTwoPhaseDispatchesSkipTarget: accumulateTwoPhaseDispatchesSkipTarget,
	accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches,
	accumulateDirectDispatches: accumulateDirectDispatches
});

var contentKey = null;

/**
 * Gets the key used to access text content on a DOM node.
 *
 * @return {?string} Key used to access text content.
 * @internal
 */
function getTextContentAccessor() {
  if (!contentKey && ExecutionEnvironment.canUseDOM) {
    // Prefer textContent to innerText because many browsers support both but
    // SVG <text> elements don't support innerText even when <div> does.
    contentKey = 'textContent' in document.documentElement ? 'textContent' : 'innerText';
  }
  return contentKey;
}

/**
 * This helper object stores information about text content of a target node,
 * allowing comparison of content before and after a given event.
 *
 * Identify the node where selection currently begins, then observe
 * both its text content and its current position in the DOM. Since the
 * browser may natively replace the target node during composition, we can
 * use its position to find its replacement.
 *
 *
 */
var compositionState = {
  _root: null,
  _startText: null,
  _fallbackText: null
};

function initialize(nativeEventTarget) {
  compositionState._root = nativeEventTarget;
  compositionState._startText = getText();
  return true;
}

function reset() {
  compositionState._root = null;
  compositionState._startText = null;
  compositionState._fallbackText = null;
}

function getData() {
  if (compositionState._fallbackText) {
    return compositionState._fallbackText;
  }

  var start;
  var startValue = compositionState._startText;
  var startLength = startValue.length;
  var end;
  var endValue = getText();
  var endLength = endValue.length;

  for (start = 0; start < startLength; start++) {
    if (startValue[start] !== endValue[start]) {
      break;
    }
  }

  var minEnd = startLength - start;
  for (end = 1; end <= minEnd; end++) {
    if (startValue[startLength - end] !== endValue[endLength - end]) {
      break;
    }
  }

  var sliceTail = end > 1 ? 1 - end : undefined;
  compositionState._fallbackText = endValue.slice(start, sliceTail);
  return compositionState._fallbackText;
}

function getText() {
  if ('value' in compositionState._root) {
    return compositionState._root.value;
  }
  return compositionState._root[getTextContentAccessor()];
}

/* eslint valid-typeof: 0 */

var didWarnForAddedNewProperty = false;
var isProxySupported = typeof Proxy === 'function';
var EVENT_POOL_SIZE = 10;

var shouldBeReleasedProperties = ['dispatchConfig', '_targetInst', 'nativeEvent', 'isDefaultPrevented', 'isPropagationStopped', '_dispatchListeners', '_dispatchInstances'];

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var EventInterface = {
  type: null,
  target: null,
  // currentTarget is set when dispatching; no use in copying it here
  currentTarget: emptyFunction.thatReturnsNull,
  eventPhase: null,
  bubbles: null,
  cancelable: null,
  timeStamp: function (event) {
    return event.timeStamp || Date.now();
  },
  defaultPrevented: null,
  isTrusted: null
};

/**
 * Synthetic events are dispatched by event plugins, typically in response to a
 * top-level event delegation handler.
 *
 * These systems should generally use pooling to reduce the frequency of garbage
 * collection. The system should check `isPersistent` to determine whether the
 * event should be released into the pool after being dispatched. Users that
 * need a persisted event should invoke `persist`.
 *
 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
 * normalizing browser quirks. Subclasses do not necessarily have to implement a
 * DOM interface; custom application-specific events can also subclass this.
 *
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {*} targetInst Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @param {DOMEventTarget} nativeEventTarget Target node.
 */
function SyntheticEvent(dispatchConfig, targetInst, nativeEvent, nativeEventTarget) {
  {
    // these have a getter/setter for warnings
    delete this.nativeEvent;
    delete this.preventDefault;
    delete this.stopPropagation;
  }

  this.dispatchConfig = dispatchConfig;
  this._targetInst = targetInst;
  this.nativeEvent = nativeEvent;

  var Interface = this.constructor.Interface;
  for (var propName in Interface) {
    if (!Interface.hasOwnProperty(propName)) {
      continue;
    }
    {
      delete this[propName]; // this has a getter/setter for warnings
    }
    var normalize = Interface[propName];
    if (normalize) {
      this[propName] = normalize(nativeEvent);
    } else {
      if (propName === 'target') {
        this.target = nativeEventTarget;
      } else {
        this[propName] = nativeEvent[propName];
      }
    }
  }

  var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
  if (defaultPrevented) {
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  } else {
    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
  }
  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
  return this;
}

_assign(SyntheticEvent.prototype, {
  preventDefault: function () {
    this.defaultPrevented = true;
    var event = this.nativeEvent;
    if (!event) {
      return;
    }

    if (event.preventDefault) {
      event.preventDefault();
    } else if (typeof event.returnValue !== 'unknown') {
      event.returnValue = false;
    }
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  },

  stopPropagation: function () {
    var event = this.nativeEvent;
    if (!event) {
      return;
    }

    if (event.stopPropagation) {
      event.stopPropagation();
    } else if (typeof event.cancelBubble !== 'unknown') {
      // The ChangeEventPlugin registers a "propertychange" event for
      // IE. This event does not support bubbling or cancelling, and
      // any references to cancelBubble throw "Member not found".  A
      // typeof check of "unknown" circumvents this issue (and is also
      // IE specific).
      event.cancelBubble = true;
    }

    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
  },

  /**
   * We release all dispatched `SyntheticEvent`s after each event loop, adding
   * them back into the pool. This allows a way to hold onto a reference that
   * won't be added back into the pool.
   */
  persist: function () {
    this.isPersistent = emptyFunction.thatReturnsTrue;
  },

  /**
   * Checks if this event should be released back into the pool.
   *
   * @return {boolean} True if this should not be released, false otherwise.
   */
  isPersistent: emptyFunction.thatReturnsFalse,

  /**
   * `PooledClass` looks for `destructor` on each instance it releases.
   */
  destructor: function () {
    var Interface = this.constructor.Interface;
    for (var propName in Interface) {
      {
        Object.defineProperty(this, propName, getPooledWarningPropertyDefinition(propName, Interface[propName]));
      }
    }
    for (var i = 0; i < shouldBeReleasedProperties.length; i++) {
      this[shouldBeReleasedProperties[i]] = null;
    }
    {
      Object.defineProperty(this, 'nativeEvent', getPooledWarningPropertyDefinition('nativeEvent', null));
      Object.defineProperty(this, 'preventDefault', getPooledWarningPropertyDefinition('preventDefault', emptyFunction));
      Object.defineProperty(this, 'stopPropagation', getPooledWarningPropertyDefinition('stopPropagation', emptyFunction));
    }
  }
});

SyntheticEvent.Interface = EventInterface;

/**
 * Helper to reduce boilerplate when creating subclasses.
 *
 * @param {function} Class
 * @param {?object} Interface
 */
SyntheticEvent.augmentClass = function (Class, Interface) {
  var Super = this;

  var E = function () {};
  E.prototype = Super.prototype;
  var prototype = new E();

  _assign(prototype, Class.prototype);
  Class.prototype = prototype;
  Class.prototype.constructor = Class;

  Class.Interface = _assign({}, Super.Interface, Interface);
  Class.augmentClass = Super.augmentClass;
  addEventPoolingTo(Class);
};

/** Proxying after everything set on SyntheticEvent
 * to resolve Proxy issue on some WebKit browsers
 * in which some Event properties are set to undefined (GH#10010)
 */
{
  if (isProxySupported) {
    /*eslint-disable no-func-assign */
    SyntheticEvent = new Proxy(SyntheticEvent, {
      construct: function (target, args) {
        return this.apply(target, Object.create(target.prototype), args);
      },
      apply: function (constructor, that, args) {
        return new Proxy(constructor.apply(that, args), {
          set: function (target, prop, value) {
            if (prop !== 'isPersistent' && !target.constructor.Interface.hasOwnProperty(prop) && shouldBeReleasedProperties.indexOf(prop) === -1) {
              warning(didWarnForAddedNewProperty || target.isPersistent(), "This synthetic event is reused for performance reasons. If you're " + "seeing this, you're adding a new property in the synthetic event object. " + 'The property is never released. See ' + 'https://fb.me/react-event-pooling for more information.');
              didWarnForAddedNewProperty = true;
            }
            target[prop] = value;
            return true;
          }
        });
      }
    });
    /*eslint-enable no-func-assign */
  }
}

addEventPoolingTo(SyntheticEvent);

/**
 * Helper to nullify syntheticEvent instance properties when destructing
 *
 * @param {String} propName
 * @param {?object} getVal
 * @return {object} defineProperty object
 */
function getPooledWarningPropertyDefinition(propName, getVal) {
  var isFunction = typeof getVal === 'function';
  return {
    configurable: true,
    set: set,
    get: get
  };

  function set(val) {
    var action = isFunction ? 'setting the method' : 'setting the property';
    warn(action, 'This is effectively a no-op');
    return val;
  }

  function get() {
    var action = isFunction ? 'accessing the method' : 'accessing the property';
    var result = isFunction ? 'This is a no-op function' : 'This is set to null';
    warn(action, result);
    return getVal;
  }

  function warn(action, result) {
    var warningCondition = false;
    warning(warningCondition, "This synthetic event is reused for performance reasons. If you're seeing this, " + "you're %s `%s` on a released/nullified synthetic event. %s. " + 'If you must keep the original synthetic event around, use event.persist(). ' + 'See https://fb.me/react-event-pooling for more information.', action, propName, result);
  }
}

function getPooledEvent(dispatchConfig, targetInst, nativeEvent, nativeInst) {
  var EventConstructor = this;
  if (EventConstructor.eventPool.length) {
    var instance = EventConstructor.eventPool.pop();
    EventConstructor.call(instance, dispatchConfig, targetInst, nativeEvent, nativeInst);
    return instance;
  }
  return new EventConstructor(dispatchConfig, targetInst, nativeEvent, nativeInst);
}

function releasePooledEvent(event) {
  var EventConstructor = this;
  !(event instanceof EventConstructor) ? invariant(false, 'Trying to release an event instance  into a pool of a different type.') : void 0;
  event.destructor();
  if (EventConstructor.eventPool.length < EVENT_POOL_SIZE) {
    EventConstructor.eventPool.push(event);
  }
}

function addEventPoolingTo(EventConstructor) {
  EventConstructor.eventPool = [];
  EventConstructor.getPooled = getPooledEvent;
  EventConstructor.release = releasePooledEvent;
}

var SyntheticEvent$1 = SyntheticEvent;

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */
var CompositionEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticCompositionEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticCompositionEvent, CompositionEventInterface);

/**
 * @interface Event
 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
 *      /#events-inputevents
 */
var InputEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticInputEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticInputEvent, InputEventInterface);

var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
var START_KEYCODE = 229;

var canUseCompositionEvent = ExecutionEnvironment.canUseDOM && 'CompositionEvent' in window;

var documentMode = null;
if (ExecutionEnvironment.canUseDOM && 'documentMode' in document) {
  documentMode = document.documentMode;
}

// Webkit offers a very useful `textInput` event that can be used to
// directly represent `beforeInput`. The IE `textinput` event is not as
// useful, so we don't use it.
var canUseTextInputEvent = ExecutionEnvironment.canUseDOM && 'TextEvent' in window && !documentMode && !isPresto();

// In IE9+, we have access to composition events, but the data supplied
// by the native compositionend event may be incorrect. Japanese ideographic
// spaces, for instance (\u3000) are not recorded correctly.
var useFallbackCompositionData = ExecutionEnvironment.canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);

/**
 * Opera <= 12 includes TextEvent in window, but does not fire
 * text input events. Rely on keypress instead.
 */
function isPresto() {
  var opera = window.opera;
  return typeof opera === 'object' && typeof opera.version === 'function' && parseInt(opera.version(), 10) <= 12;
}

var SPACEBAR_CODE = 32;
var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);

// Events and their corresponding property names.
var eventTypes = {
  beforeInput: {
    phasedRegistrationNames: {
      bubbled: 'onBeforeInput',
      captured: 'onBeforeInputCapture'
    },
    dependencies: ['topCompositionEnd', 'topKeyPress', 'topTextInput', 'topPaste']
  },
  compositionEnd: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionEnd',
      captured: 'onCompositionEndCapture'
    },
    dependencies: ['topBlur', 'topCompositionEnd', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  },
  compositionStart: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionStart',
      captured: 'onCompositionStartCapture'
    },
    dependencies: ['topBlur', 'topCompositionStart', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  },
  compositionUpdate: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionUpdate',
      captured: 'onCompositionUpdateCapture'
    },
    dependencies: ['topBlur', 'topCompositionUpdate', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  }
};

// Track whether we've ever handled a keypress on the space key.
var hasSpaceKeypress = false;

/**
 * Return whether a native keypress event is assumed to be a command.
 * This is required because Firefox fires `keypress` events for key commands
 * (cut, copy, select-all, etc.) even though no character is inserted.
 */
function isKeypressCommand(nativeEvent) {
  return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) &&
  // ctrlKey && altKey is equivalent to AltGr, and is not a command.
  !(nativeEvent.ctrlKey && nativeEvent.altKey);
}

/**
 * Translate native top level events into event types.
 *
 * @param {string} topLevelType
 * @return {object}
 */
function getCompositionEventType(topLevelType) {
  switch (topLevelType) {
    case 'topCompositionStart':
      return eventTypes.compositionStart;
    case 'topCompositionEnd':
      return eventTypes.compositionEnd;
    case 'topCompositionUpdate':
      return eventTypes.compositionUpdate;
  }
}

/**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionStart(topLevelType, nativeEvent) {
  return topLevelType === 'topKeyDown' && nativeEvent.keyCode === START_KEYCODE;
}

/**
 * Does our fallback mode think that this event is the end of composition?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionEnd(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case 'topKeyUp':
      // Command keys insert or clear IME input.
      return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
    case 'topKeyDown':
      // Expect IME keyCode on each keydown. If we get any other
      // code we must have exited earlier.
      return nativeEvent.keyCode !== START_KEYCODE;
    case 'topKeyPress':
    case 'topMouseDown':
    case 'topBlur':
      // Events are not possible without cancelling IME.
      return true;
    default:
      return false;
  }
}

/**
 * Google Input Tools provides composition data via a CustomEvent,
 * with the `data` property populated in the `detail` object. If this
 * is available on the event object, use it. If not, this is a plain
 * composition event and we have nothing special to extract.
 *
 * @param {object} nativeEvent
 * @return {?string}
 */
function getDataFromCustomEvent(nativeEvent) {
  var detail = nativeEvent.detail;
  if (typeof detail === 'object' && 'data' in detail) {
    return detail.data;
  }
  return null;
}

// Track the current IME composition status, if any.
var isComposing = false;

/**
 * @return {?object} A SyntheticCompositionEvent.
 */
function extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var eventType;
  var fallbackData;

  if (canUseCompositionEvent) {
    eventType = getCompositionEventType(topLevelType);
  } else if (!isComposing) {
    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
      eventType = eventTypes.compositionStart;
    }
  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
    eventType = eventTypes.compositionEnd;
  }

  if (!eventType) {
    return null;
  }

  if (useFallbackCompositionData) {
    // The current composition is stored statically and must not be
    // overwritten while composition continues.
    if (!isComposing && eventType === eventTypes.compositionStart) {
      isComposing = initialize(nativeEventTarget);
    } else if (eventType === eventTypes.compositionEnd) {
      if (isComposing) {
        fallbackData = getData();
      }
    }
  }

  var event = SyntheticCompositionEvent.getPooled(eventType, targetInst, nativeEvent, nativeEventTarget);

  if (fallbackData) {
    // Inject data generated from fallback path into the synthetic event.
    // This matches the property of native CompositionEventInterface.
    event.data = fallbackData;
  } else {
    var customData = getDataFromCustomEvent(nativeEvent);
    if (customData !== null) {
      event.data = customData;
    }
  }

  accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * @param {TopLevelTypes} topLevelType Record from `BrowserEventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The string corresponding to this `beforeInput` event.
 */
function getNativeBeforeInputChars(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case 'topCompositionEnd':
      return getDataFromCustomEvent(nativeEvent);
    case 'topKeyPress':
      /**
       * If native `textInput` events are available, our goal is to make
       * use of them. However, there is a special case: the spacebar key.
       * In Webkit, preventing default on a spacebar `textInput` event
       * cancels character insertion, but it *also* causes the browser
       * to fall back to its default spacebar behavior of scrolling the
       * page.
       *
       * Tracking at:
       * https://code.google.com/p/chromium/issues/detail?id=355103
       *
       * To avoid this issue, use the keypress event as if no `textInput`
       * event is available.
       */
      var which = nativeEvent.which;
      if (which !== SPACEBAR_CODE) {
        return null;
      }

      hasSpaceKeypress = true;
      return SPACEBAR_CHAR;

    case 'topTextInput':
      // Record the characters to be added to the DOM.
      var chars = nativeEvent.data;

      // If it's a spacebar character, assume that we have already handled
      // it at the keypress level and bail immediately. Android Chrome
      // doesn't give us keycodes, so we need to blacklist it.
      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
        return null;
      }

      return chars;

    default:
      // For other native event types, do nothing.
      return null;
  }
}

/**
 * For browsers that do not provide the `textInput` event, extract the
 * appropriate string to use for SyntheticInputEvent.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The fallback string for this `beforeInput` event.
 */
function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
  // If we are currently composing (IME) and using a fallback to do so,
  // try to extract the composed characters from the fallback object.
  // If composition event is available, we extract a string only at
  // compositionevent, otherwise extract it at fallback events.
  if (isComposing) {
    if (topLevelType === 'topCompositionEnd' || !canUseCompositionEvent && isFallbackCompositionEnd(topLevelType, nativeEvent)) {
      var chars = getData();
      reset();
      isComposing = false;
      return chars;
    }
    return null;
  }

  switch (topLevelType) {
    case 'topPaste':
      // If a paste event occurs after a keypress, throw out the input
      // chars. Paste events should not lead to BeforeInput events.
      return null;
    case 'topKeyPress':
      /**
       * As of v27, Firefox may fire keypress events even when no character
       * will be inserted. A few possibilities:
       *
       * - `which` is `0`. Arrow keys, Esc key, etc.
       *
       * - `which` is the pressed key code, but no char is available.
       *   Ex: 'AltGr + d` in Polish. There is no modified character for
       *   this key combination and no character is inserted into the
       *   document, but FF fires the keypress for char code `100` anyway.
       *   No `input` event will occur.
       *
       * - `which` is the pressed key code, but a command combination is
       *   being used. Ex: `Cmd+C`. No character is inserted, and no
       *   `input` event will occur.
       */
      if (!isKeypressCommand(nativeEvent)) {
        // IE fires the `keypress` event when a user types an emoji via
        // Touch keyboard of Windows.  In such a case, the `char` property
        // holds an emoji character like `\uD83D\uDE0A`.  Because its length
        // is 2, the property `which` does not represent an emoji correctly.
        // In such a case, we directly return the `char` property instead of
        // using `which`.
        if (nativeEvent.char && nativeEvent.char.length > 1) {
          return nativeEvent.char;
        } else if (nativeEvent.which) {
          return String.fromCharCode(nativeEvent.which);
        }
      }
      return null;
    case 'topCompositionEnd':
      return useFallbackCompositionData ? null : nativeEvent.data;
    default:
      return null;
  }
}

/**
 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
 * `textInput` or fallback behavior.
 *
 * @return {?object} A SyntheticInputEvent.
 */
function extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var chars;

  if (canUseTextInputEvent) {
    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
  } else {
    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
  }

  // If no characters are being inserted, no BeforeInput event should
  // be fired.
  if (!chars) {
    return null;
  }

  var event = SyntheticInputEvent.getPooled(eventTypes.beforeInput, targetInst, nativeEvent, nativeEventTarget);

  event.data = chars;
  accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * Create an `onBeforeInput` event to match
 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
 *
 * This event plugin is based on the native `textInput` event
 * available in Chrome, Safari, Opera, and IE. This event fires after
 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
 *
 * `beforeInput` is spec'd but not implemented in any browsers, and
 * the `input` event does not provide any useful information about what has
 * actually been added, contrary to the spec. Thus, `textInput` is the best
 * available event to identify the characters that have actually been inserted
 * into the target node.
 *
 * This plugin is also responsible for emitting `composition` events, thus
 * allowing us to share composition fallback code for both `beforeInput` and
 * `composition` event types.
 */
var BeforeInputEventPlugin = {
  eventTypes: eventTypes,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    return [extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget), extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget)];
  }
};

// Use to restore controlled state after a change event has fired.

var fiberHostComponent = null;

var ReactControlledComponentInjection = {
  injectFiberControlledHostComponent: function (hostComponentImpl) {
    // The fiber implementation doesn't use dynamic dispatch so we need to
    // inject the implementation.
    fiberHostComponent = hostComponentImpl;
  }
};

var restoreTarget = null;
var restoreQueue = null;

function restoreStateOfTarget(target) {
  // We perform this translation at the end of the event loop so that we
  // always receive the correct fiber here
  var internalInstance = getInstanceFromNode(target);
  if (!internalInstance) {
    // Unmounted
    return;
  }
  !(fiberHostComponent && typeof fiberHostComponent.restoreControlledState === 'function') ? invariant(false, 'Fiber needs to be injected to handle a fiber target for controlled events. This error is likely caused by a bug in React. Please file an issue.') : void 0;
  var props = getFiberCurrentPropsFromNode(internalInstance.stateNode);
  fiberHostComponent.restoreControlledState(internalInstance.stateNode, internalInstance.type, props);
}

var injection$3 = ReactControlledComponentInjection;

function enqueueStateRestore(target) {
  if (restoreTarget) {
    if (restoreQueue) {
      restoreQueue.push(target);
    } else {
      restoreQueue = [target];
    }
  } else {
    restoreTarget = target;
  }
}

function restoreStateIfNeeded() {
  if (!restoreTarget) {
    return;
  }
  var target = restoreTarget;
  var queuedTargets = restoreQueue;
  restoreTarget = null;
  restoreQueue = null;

  restoreStateOfTarget(target);
  if (queuedTargets) {
    for (var i = 0; i < queuedTargets.length; i++) {
      restoreStateOfTarget(queuedTargets[i]);
    }
  }
}

var ReactControlledComponent = Object.freeze({
	injection: injection$3,
	enqueueStateRestore: enqueueStateRestore,
	restoreStateIfNeeded: restoreStateIfNeeded
});

// Used as a way to call batchedUpdates when we don't have a reference to
// the renderer. Such as when we're dispatching events or if third party
// libraries need to call batchedUpdates. Eventually, this API will go away when
// everything is batched by default. We'll then have a similar API to opt-out of
// scheduled work and instead do synchronous work.

// Defaults
var fiberBatchedUpdates = function (fn, bookkeeping) {
  return fn(bookkeeping);
};

var isNestingBatched = false;
function batchedUpdates(fn, bookkeeping) {
  if (isNestingBatched) {
    // If we are currently inside another batch, we need to wait until it
    // fully completes before restoring state. Therefore, we add the target to
    // a queue of work.
    return fiberBatchedUpdates(fn, bookkeeping);
  }
  isNestingBatched = true;
  try {
    return fiberBatchedUpdates(fn, bookkeeping);
  } finally {
    // Here we wait until all updates have propagated, which is important
    // when using controlled components within layers:
    // https://github.com/facebook/react/issues/1698
    // Then we restore state of any controlled component.
    isNestingBatched = false;
    restoreStateIfNeeded();
  }
}

var ReactGenericBatchingInjection = {
  injectFiberBatchedUpdates: function (_batchedUpdates) {
    fiberBatchedUpdates = _batchedUpdates;
  }
};

var injection$4 = ReactGenericBatchingInjection;

/**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */
var supportedInputTypes = {
  color: true,
  date: true,
  datetime: true,
  'datetime-local': true,
  email: true,
  month: true,
  number: true,
  password: true,
  range: true,
  search: true,
  tel: true,
  text: true,
  time: true,
  url: true,
  week: true
};

function isTextInputElement(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();

  if (nodeName === 'input') {
    return !!supportedInputTypes[elem.type];
  }

  if (nodeName === 'textarea') {
    return true;
  }

  return false;
}

/**
 * HTML nodeType values that represent the type of the node
 */

var ELEMENT_NODE = 1;
var TEXT_NODE = 3;
var COMMENT_NODE = 8;
var DOCUMENT_NODE = 9;
var DOCUMENT_FRAGMENT_NODE = 11;

/**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */
function getEventTarget(nativeEvent) {
  var target = nativeEvent.target || nativeEvent.srcElement || window;

  // Normalize SVG <use> element events #4963
  if (target.correspondingUseElement) {
    target = target.correspondingUseElement;
  }

  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
  // @see http://www.quirksmode.org/js/events_properties.html
  return target.nodeType === TEXT_NODE ? target.parentNode : target;
}

var useHasFeature;
if (ExecutionEnvironment.canUseDOM) {
  useHasFeature = document.implementation && document.implementation.hasFeature &&
  // always returns true in newer browsers as per the standard.
  // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature
  document.implementation.hasFeature('', '') !== true;
}

/**
 * Checks if an event is supported in the current execution environment.


 */
function isEventSupported(eventNameSuffix, capture) {
  if (!ExecutionEnvironment.canUseDOM || capture && !('addEventListener' in document)) {
    return false;
  }

  var eventName = 'on' + eventNameSuffix;
  var isSupported = eventName in document;

  if (!isSupported) {
    var element = document.createElement('div');
    element.setAttribute(eventName, 'return;');
    isSupported = typeof element[eventName] === 'function';
  }

  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {
    // This is the only way to test support for the `wheel` event in IE9+.
    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');
  }

  return isSupported;
}

function isCheckable(elem) {
  var type = elem.type;
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (type === 'checkbox' || type === 'radio');
}

function getTracker(node) {
  return node._valueTracker;
}

function detachTracker(node) {
  node._valueTracker = null;
}

function getValueFromNode(node) {
  var value = '';
  if (!node) {
    return value;
  }

  if (isCheckable(node)) {
    value = node.checked ? 'true' : 'false';
  } else {
    value = node.value;
  }

  return value;
}

function trackValueOnNode(node) {
  var valueField = isCheckable(node) ? 'checked' : 'value';
  var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);

  var currentValue = '' + node[valueField];

  // if someone has already defined a value or Safari, then bail
  // and don't track value will cause over reporting of changes,
  // but it's better then a hard failure
  // (needed for certain tests that spyOn input values and Safari)
  if (node.hasOwnProperty(valueField) || typeof descriptor.get !== 'function' || typeof descriptor.set !== 'function') {
    return;
  }

  Object.defineProperty(node, valueField, {
    enumerable: descriptor.enumerable,
    configurable: true,
    get: function () {
      return descriptor.get.call(this);
    },
    set: function (value) {
      currentValue = '' + value;
      descriptor.set.call(this, value);
    }
  });

  var tracker = {
    getValue: function () {
      return currentValue;
    },
    setValue: function (value) {
      currentValue = '' + value;
    },
    stopTracking: function () {
      detachTracker(node);
      delete node[valueField];
    }
  };
  return tracker;
}

function track(node) {
  if (getTracker(node)) {
    return;
  }

  // TODO: Once it's just Fiber we can move this to node._wrapperState
  node._valueTracker = trackValueOnNode(node);
}

function updateValueIfChanged(node) {
  if (!node) {
    return false;
  }

  var tracker = getTracker(node);
  // if there is no tracker at this point it's unlikely
  // that trying again will succeed
  if (!tracker) {
    return true;
  }

  var lastValue = tracker.getValue();
  var nextValue = getValueFromNode(node);
  if (nextValue !== lastValue) {
    tracker.setValue(nextValue);
    return true;
  }
  return false;
}

var eventTypes$1 = {
  change: {
    phasedRegistrationNames: {
      bubbled: 'onChange',
      captured: 'onChangeCapture'
    },
    dependencies: ['topBlur', 'topChange', 'topClick', 'topFocus', 'topInput', 'topKeyDown', 'topKeyUp', 'topSelectionChange']
  }
};

function createAndAccumulateChangeEvent(inst, nativeEvent, target) {
  var event = SyntheticEvent$1.getPooled(eventTypes$1.change, inst, nativeEvent, target);
  event.type = 'change';
  // Flag this event loop as needing state restore.
  enqueueStateRestore(target);
  accumulateTwoPhaseDispatches(event);
  return event;
}
/**
 * For IE shims
 */
var activeElement = null;
var activeElementInst = null;

/**
 * SECTION: handle `change` event
 */
function shouldUseChangeEvent(elem) {
  var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
}

function manualDispatchChangeEvent(nativeEvent) {
  var event = createAndAccumulateChangeEvent(activeElementInst, nativeEvent, getEventTarget(nativeEvent));

  // If change and propertychange bubbled, we'd just bind to it like all the
  // other events and have it go through ReactBrowserEventEmitter. Since it
  // doesn't, we manually listen for the events and so we have to enqueue and
  // process the abstract event manually.
  //
  // Batching is necessary here in order to ensure that all event handlers run
  // before the next rerender (including event handlers attached to ancestor
  // elements instead of directly on the input). Without this, controlled
  // components don't work properly in conjunction with event bubbling because
  // the component is rerendered and the value reverted before all the event
  // handlers can run. See https://github.com/facebook/react/issues/708.
  batchedUpdates(runEventInBatch, event);
}

function runEventInBatch(event) {
  enqueueEvents(event);
  processEventQueue(false);
}

function getInstIfValueChanged(targetInst) {
  var targetNode = getNodeFromInstance$1(targetInst);
  if (updateValueIfChanged(targetNode)) {
    return targetInst;
  }
}

function getTargetInstForChangeEvent(topLevelType, targetInst) {
  if (topLevelType === 'topChange') {
    return targetInst;
  }
}

/**
 * SECTION: handle `input` event
 */
var isInputEventSupported = false;
if (ExecutionEnvironment.canUseDOM) {
  // IE9 claims to support the input event but fails to trigger it when
  // deleting text, so we ignore its input events.
  isInputEventSupported = isEventSupported('input') && (!document.documentMode || document.documentMode > 9);
}

/**
 * (For IE <=9) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */
function startWatchingForValueChange(target, targetInst) {
  activeElement = target;
  activeElementInst = targetInst;
  activeElement.attachEvent('onpropertychange', handlePropertyChange);
}

/**
 * (For IE <=9) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */
function stopWatchingForValueChange() {
  if (!activeElement) {
    return;
  }
  activeElement.detachEvent('onpropertychange', handlePropertyChange);
  activeElement = null;
  activeElementInst = null;
}

/**
 * (For IE <=9) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */
function handlePropertyChange(nativeEvent) {
  if (nativeEvent.propertyName !== 'value') {
    return;
  }
  if (getInstIfValueChanged(activeElementInst)) {
    manualDispatchChangeEvent(nativeEvent);
  }
}

function handleEventsForInputEventPolyfill(topLevelType, target, targetInst) {
  if (topLevelType === 'topFocus') {
    // In IE9, propertychange fires for most input events but is buggy and
    // doesn't fire when text is deleted, but conveniently, selectionchange
    // appears to fire in all of the remaining cases so we catch those and
    // forward the event if the value has changed
    // In either case, we don't want to call the event handler if the value
    // is changed from JS so we redefine a setter for `.value` that updates
    // our activeElementValue variable, allowing us to ignore those changes
    //
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForValueChange();
    startWatchingForValueChange(target, targetInst);
  } else if (topLevelType === 'topBlur') {
    stopWatchingForValueChange();
  }
}

// For IE8 and IE9.
function getTargetInstForInputEventPolyfill(topLevelType, targetInst) {
  if (topLevelType === 'topSelectionChange' || topLevelType === 'topKeyUp' || topLevelType === 'topKeyDown') {
    // On the selectionchange event, the target is just document which isn't
    // helpful for us so just check activeElement instead.
    //
    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
    // propertychange on the first input event after setting `value` from a
    // script and fires only keydown, keypress, keyup. Catching keyup usually
    // gets it and catching keydown lets us fire an event for the first
    // keystroke if user does a key repeat (it'll be a little delayed: right
    // before the second keystroke). Other input methods (e.g., paste) seem to
    // fire selectionchange normally.
    return getInstIfValueChanged(activeElementInst);
  }
}

/**
 * SECTION: handle `click` event
 */
function shouldUseClickEvent(elem) {
  // Use the `click` event to detect changes to checkbox and radio inputs.
  // This approach works across all browsers, whereas `change` does not fire
  // until `blur` in IE8.
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
}

function getTargetInstForClickEvent(topLevelType, targetInst) {
  if (topLevelType === 'topClick') {
    return getInstIfValueChanged(targetInst);
  }
}

function getTargetInstForInputOrChangeEvent(topLevelType, targetInst) {
  if (topLevelType === 'topInput' || topLevelType === 'topChange') {
    return getInstIfValueChanged(targetInst);
  }
}

function handleControlledInputBlur(inst, node) {
  // TODO: In IE, inst is occasionally null. Why?
  if (inst == null) {
    return;
  }

  // Fiber and ReactDOM keep wrapper state in separate places
  var state = inst._wrapperState || node._wrapperState;

  if (!state || !state.controlled || node.type !== 'number') {
    return;
  }

  // If controlled, assign the value attribute to the current value on blur
  var value = '' + node.value;
  if (node.getAttribute('value') !== value) {
    node.setAttribute('value', value);
  }
}

/**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */
var ChangeEventPlugin = {
  eventTypes: eventTypes$1,

  _isInputEventSupported: isInputEventSupported,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    var getTargetInstFunc, handleEventFunc;
    if (shouldUseChangeEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForChangeEvent;
    } else if (isTextInputElement(targetNode)) {
      if (isInputEventSupported) {
        getTargetInstFunc = getTargetInstForInputOrChangeEvent;
      } else {
        getTargetInstFunc = getTargetInstForInputEventPolyfill;
        handleEventFunc = handleEventsForInputEventPolyfill;
      }
    } else if (shouldUseClickEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForClickEvent;
    }

    if (getTargetInstFunc) {
      var inst = getTargetInstFunc(topLevelType, targetInst);
      if (inst) {
        var event = createAndAccumulateChangeEvent(inst, nativeEvent, nativeEventTarget);
        return event;
      }
    }

    if (handleEventFunc) {
      handleEventFunc(topLevelType, targetNode, targetInst);
    }

    // When blurring, set the value attribute for number inputs
    if (topLevelType === 'topBlur') {
      handleControlledInputBlur(targetInst, targetNode);
    }
  }
};

/**
 * Module that is injectable into `EventPluginHub`, that specifies a
 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
 * plugins, without having to package every one of them. This is better than
 * having plugins be ordered in the same order that they are injected because
 * that ordering would be influenced by the packaging order.
 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
 */
var DOMEventPluginOrder = ['ResponderEventPlugin', 'SimpleEventPlugin', 'TapEventPlugin', 'EnterLeaveEventPlugin', 'ChangeEventPlugin', 'SelectEventPlugin', 'BeforeInputEventPlugin'];

/**
 * @interface UIEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var UIEventInterface = {
  view: null,
  detail: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticUIEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticUIEvent, UIEventInterface);

/**
 * Translation from modifier key to the associated property in the event.
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
 */

var modifierKeyToProp = {
  Alt: 'altKey',
  Control: 'ctrlKey',
  Meta: 'metaKey',
  Shift: 'shiftKey'
};

// IE8 does not implement getModifierState so we simply map it to the only
// modifier keys exposed by the event itself, does not support Lock-keys.
// Currently, all major browsers except Chrome seems to support Lock-keys.
function modifierStateGetter(keyArg) {
  var syntheticEvent = this;
  var nativeEvent = syntheticEvent.nativeEvent;
  if (nativeEvent.getModifierState) {
    return nativeEvent.getModifierState(keyArg);
  }
  var keyProp = modifierKeyToProp[keyArg];
  return keyProp ? !!nativeEvent[keyProp] : false;
}

function getEventModifierState(nativeEvent) {
  return modifierStateGetter;
}

/**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var MouseEventInterface = {
  screenX: null,
  screenY: null,
  clientX: null,
  clientY: null,
  pageX: null,
  pageY: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  getModifierState: getEventModifierState,
  button: null,
  buttons: null,
  relatedTarget: function (event) {
    return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticMouseEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticMouseEvent, MouseEventInterface);

var eventTypes$2 = {
  mouseEnter: {
    registrationName: 'onMouseEnter',
    dependencies: ['topMouseOut', 'topMouseOver']
  },
  mouseLeave: {
    registrationName: 'onMouseLeave',
    dependencies: ['topMouseOut', 'topMouseOver']
  }
};

var EnterLeaveEventPlugin = {
  eventTypes: eventTypes$2,

  /**
   * For almost every interaction we care about, there will be both a top-level
   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
   * we do not extract duplicate events. However, moving the mouse into the
   * browser from outside will not fire a `mouseout` event. In this case, we use
   * the `mouseover` top-level event.
   */
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    if (topLevelType === 'topMouseOver' && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
      return null;
    }
    if (topLevelType !== 'topMouseOut' && topLevelType !== 'topMouseOver') {
      // Must not be a mouse in or mouse out - ignoring.
      return null;
    }

    var win;
    if (nativeEventTarget.window === nativeEventTarget) {
      // `nativeEventTarget` is probably a window object.
      win = nativeEventTarget;
    } else {
      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
      var doc = nativeEventTarget.ownerDocument;
      if (doc) {
        win = doc.defaultView || doc.parentWindow;
      } else {
        win = window;
      }
    }

    var from;
    var to;
    if (topLevelType === 'topMouseOut') {
      from = targetInst;
      var related = nativeEvent.relatedTarget || nativeEvent.toElement;
      to = related ? getClosestInstanceFromNode(related) : null;
    } else {
      // Moving to a node from outside the window.
      from = null;
      to = targetInst;
    }

    if (from === to) {
      // Nothing pertains to our managed components.
      return null;
    }

    var fromNode = from == null ? win : getNodeFromInstance$1(from);
    var toNode = to == null ? win : getNodeFromInstance$1(to);

    var leave = SyntheticMouseEvent.getPooled(eventTypes$2.mouseLeave, from, nativeEvent, nativeEventTarget);
    leave.type = 'mouseleave';
    leave.target = fromNode;
    leave.relatedTarget = toNode;

    var enter = SyntheticMouseEvent.getPooled(eventTypes$2.mouseEnter, to, nativeEvent, nativeEventTarget);
    enter.type = 'mouseenter';
    enter.target = toNode;
    enter.relatedTarget = fromNode;

    accumulateEnterLeaveDispatches(leave, enter, from, to);

    return [leave, enter];
  }
};

/**
 * `ReactInstanceMap` maintains a mapping from a public facing stateful
 * instance (key) and the internal representation (value). This allows public
 * methods to accept the user facing instance as an argument and map them back
 * to internal methods.
 *
 * Note that this module is currently shared and assumed to be stateless.
 * If this becomes an actual Map, that will break.
 */

/**
 * This API should be called `delete` but we'd have to make sure to always
 * transform these to strings for IE support. When this transform is fully
 * supported we can rename it.
 */


function get(key) {
  return key._reactInternalFiber;
}

function has(key) {
  return key._reactInternalFiber !== undefined;
}

function set(key, value) {
  key._reactInternalFiber = value;
}

var ReactInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;

var ReactCurrentOwner = ReactInternals.ReactCurrentOwner;
var ReactDebugCurrentFrame = ReactInternals.ReactDebugCurrentFrame;

function getComponentName(fiber) {
  var type = fiber.type;

  if (typeof type === 'string') {
    return type;
  }
  if (typeof type === 'function') {
    return type.displayName || type.name;
  }
  return null;
}

// Don't change these two values:
var NoEffect = 0; //           0b00000000
var PerformedWork = 1; //      0b00000001

// You can change the rest (and add more).
var Placement = 2; //          0b00000010
var Update = 4; //             0b00000100
var PlacementAndUpdate = 6; // 0b00000110
var Deletion = 8; //           0b00001000
var ContentReset = 16; //      0b00010000
var Callback = 32; //          0b00100000
var Err = 64; //               0b01000000
var Ref = 128; //              0b10000000

var MOUNTING = 1;
var MOUNTED = 2;
var UNMOUNTED = 3;

function isFiberMountedImpl(fiber) {
  var node = fiber;
  if (!fiber.alternate) {
    // If there is no alternate, this might be a new tree that isn't inserted
    // yet. If it is, then it will have a pending insertion effect on it.
    if ((node.effectTag & Placement) !== NoEffect) {
      return MOUNTING;
    }
    while (node['return']) {
      node = node['return'];
      if ((node.effectTag & Placement) !== NoEffect) {
        return MOUNTING;
      }
    }
  } else {
    while (node['return']) {
      node = node['return'];
    }
  }
  if (node.tag === HostRoot) {
    // TODO: Check if this was a nested HostRoot when used with
    // renderContainerIntoSubtree.
    return MOUNTED;
  }
  // If we didn't hit the root, that means that we're in an disconnected tree
  // that has been unmounted.
  return UNMOUNTED;
}

function isFiberMounted(fiber) {
  return isFiberMountedImpl(fiber) === MOUNTED;
}

function isMounted(component) {
  {
    var owner = ReactCurrentOwner.current;
    if (owner !== null && owner.tag === ClassComponent) {
      var ownerFiber = owner;
      var instance = ownerFiber.stateNode;
      warning(instance._warnedAboutRefsInRender, '%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(ownerFiber) || 'A component');
      instance._warnedAboutRefsInRender = true;
    }
  }

  var fiber = get(component);
  if (!fiber) {
    return false;
  }
  return isFiberMountedImpl(fiber) === MOUNTED;
}

function assertIsMounted(fiber) {
  !(isFiberMountedImpl(fiber) === MOUNTED) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
}

function findCurrentFiberUsingSlowPath(fiber) {
  var alternate = fiber.alternate;
  if (!alternate) {
    // If there is no alternate, then we only need to check if it is mounted.
    var state = isFiberMountedImpl(fiber);
    !(state !== UNMOUNTED) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
    if (state === MOUNTING) {
      return null;
    }
    return fiber;
  }
  // If we have two possible branches, we'll walk backwards up to the root
  // to see what path the root points to. On the way we may hit one of the
  // special cases and we'll deal with them.
  var a = fiber;
  var b = alternate;
  while (true) {
    var parentA = a['return'];
    var parentB = parentA ? parentA.alternate : null;
    if (!parentA || !parentB) {
      // We're at the root.
      break;
    }

    // If both copies of the parent fiber point to the same child, we can
    // assume that the child is current. This happens when we bailout on low
    // priority: the bailed out fiber's child reuses the current child.
    if (parentA.child === parentB.child) {
      var child = parentA.child;
      while (child) {
        if (child === a) {
          // We've determined that A is the current branch.
          assertIsMounted(parentA);
          return fiber;
        }
        if (child === b) {
          // We've determined that B is the current branch.
          assertIsMounted(parentA);
          return alternate;
        }
        child = child.sibling;
      }
      // We should never have an alternate for any mounting node. So the only
      // way this could possibly happen is if this was unmounted, if at all.
      invariant(false, 'Unable to find node on an unmounted component.');
    }

    if (a['return'] !== b['return']) {
      // The return pointer of A and the return pointer of B point to different
      // fibers. We assume that return pointers never criss-cross, so A must
      // belong to the child set of A.return, and B must belong to the child
      // set of B.return.
      a = parentA;
      b = parentB;
    } else {
      // The return pointers point to the same fiber. We'll have to use the
      // default, slow path: scan the child sets of each parent alternate to see
      // which child belongs to which set.
      //
      // Search parent A's child set
      var didFindChild = false;
      var _child = parentA.child;
      while (_child) {
        if (_child === a) {
          didFindChild = true;
          a = parentA;
          b = parentB;
          break;
        }
        if (_child === b) {
          didFindChild = true;
          b = parentA;
          a = parentB;
          break;
        }
        _child = _child.sibling;
      }
      if (!didFindChild) {
        // Search parent B's child set
        _child = parentB.child;
        while (_child) {
          if (_child === a) {
            didFindChild = true;
            a = parentB;
            b = parentA;
            break;
          }
          if (_child === b) {
            didFindChild = true;
            b = parentB;
            a = parentA;
            break;
          }
          _child = _child.sibling;
        }
        !didFindChild ? invariant(false, 'Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.') : void 0;
      }
    }

    !(a.alternate === b) ? invariant(false, 'Return fibers should always be each others\' alternates. This error is likely caused by a bug in React. Please file an issue.') : void 0;
  }
  // If the root is not a host container, we're in a disconnected tree. I.e.
  // unmounted.
  !(a.tag === HostRoot) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
  if (a.stateNode.current === a) {
    // We've determined that A is the current branch.
    return fiber;
  }
  // Otherwise B has to be current branch.
  return alternate;
}

function findCurrentHostFiber(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);
  if (!currentParent) {
    return null;
  }

  // Next we'll drill down this component to find the first HostComponent/Text.
  var node = currentParent;
  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child) {
      node.child['return'] = node;
      node = node.child;
      continue;
    }
    if (node === currentParent) {
      return null;
    }
    while (!node.sibling) {
      if (!node['return'] || node['return'] === currentParent) {
        return null;
      }
      node = node['return'];
    }
    node.sibling['return'] = node['return'];
    node = node.sibling;
  }
  // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable
  return null;
}

function findCurrentHostFiberWithNoPortals(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);
  if (!currentParent) {
    return null;
  }

  // Next we'll drill down this component to find the first HostComponent/Text.
  var node = currentParent;
  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child && node.tag !== HostPortal) {
      node.child['return'] = node;
      node = node.child;
      continue;
    }
    if (node === currentParent) {
      return null;
    }
    while (!node.sibling) {
      if (!node['return'] || node['return'] === currentParent) {
        return null;
      }
      node = node['return'];
    }
    node.sibling['return'] = node['return'];
    node = node.sibling;
  }
  // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable
  return null;
}

var CALLBACK_BOOKKEEPING_POOL_SIZE = 10;
var callbackBookkeepingPool = [];

/**
 * Find the deepest React component completely containing the root of the
 * passed-in instance (for use when entire React trees are nested within each
 * other). If React trees are not nested, returns null.
 */
function findRootContainerNode(inst) {
  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
  // traversal, but caching is difficult to do correctly without using a
  // mutation observer to listen for all DOM changes.
  while (inst['return']) {
    inst = inst['return'];
  }
  if (inst.tag !== HostRoot) {
    // This can happen if we're in a detached tree.
    return null;
  }
  return inst.stateNode.containerInfo;
}

// Used to store ancestor hierarchy in top level callback
function getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst) {
  if (callbackBookkeepingPool.length) {
    var instance = callbackBookkeepingPool.pop();
    instance.topLevelType = topLevelType;
    instance.nativeEvent = nativeEvent;
    instance.targetInst = targetInst;
    return instance;
  }
  return {
    topLevelType: topLevelType,
    nativeEvent: nativeEvent,
    targetInst: targetInst,
    ancestors: []
  };
}

function releaseTopLevelCallbackBookKeeping(instance) {
  instance.topLevelType = null;
  instance.nativeEvent = null;
  instance.targetInst = null;
  instance.ancestors.length = 0;
  if (callbackBookkeepingPool.length < CALLBACK_BOOKKEEPING_POOL_SIZE) {
    callbackBookkeepingPool.push(instance);
  }
}

function handleTopLevelImpl(bookKeeping) {
  var targetInst = bookKeeping.targetInst;

  // Loop through the hierarchy, in case there's any nested components.
  // It's important that we build the array of ancestors before calling any
  // event handlers, because event handlers can modify the DOM, leading to
  // inconsistencies with ReactMount's node cache. See #1105.
  var ancestor = targetInst;
  do {
    if (!ancestor) {
      bookKeeping.ancestors.push(ancestor);
      break;
    }
    var root = findRootContainerNode(ancestor);
    if (!root) {
      break;
    }
    bookKeeping.ancestors.push(ancestor);
    ancestor = getClosestInstanceFromNode(root);
  } while (ancestor);

  for (var i = 0; i < bookKeeping.ancestors.length; i++) {
    targetInst = bookKeeping.ancestors[i];
    _handleTopLevel(bookKeeping.topLevelType, targetInst, bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
  }
}

// TODO: can we stop exporting these?
var _enabled = true;
var _handleTopLevel = void 0;

function setHandleTopLevel(handleTopLevel) {
  _handleTopLevel = handleTopLevel;
}

function setEnabled(enabled) {
  _enabled = !!enabled;
}

function isEnabled() {
  return _enabled;
}

/**
 * Traps top-level events by using event bubbling.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {object} element Element on which to attach listener.
 * @return {?object} An object with a remove function which will forcefully
 *                  remove the listener.
 * @internal
 */
function trapBubbledEvent(topLevelType, handlerBaseName, element) {
  if (!element) {
    return null;
  }
  return EventListener.listen(element, handlerBaseName, dispatchEvent.bind(null, topLevelType));
}

/**
 * Traps a top-level event by using event capturing.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {object} element Element on which to attach listener.
 * @return {?object} An object with a remove function which will forcefully
 *                  remove the listener.
 * @internal
 */
function trapCapturedEvent(topLevelType, handlerBaseName, element) {
  if (!element) {
    return null;
  }
  return EventListener.capture(element, handlerBaseName, dispatchEvent.bind(null, topLevelType));
}

function dispatchEvent(topLevelType, nativeEvent) {
  if (!_enabled) {
    return;
  }

  var nativeEventTarget = getEventTarget(nativeEvent);
  var targetInst = getClosestInstanceFromNode(nativeEventTarget);
  if (targetInst !== null && typeof targetInst.tag === 'number' && !isFiberMounted(targetInst)) {
    // If we get an event (ex: img onload) before committing that
    // component's mount, ignore it for now (that is, treat it as if it was an
    // event on a non-React tree). We might also consider queueing events and
    // dispatching them after the mount.
    targetInst = null;
  }

  var bookKeeping = getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst);

  try {
    // Event queue being processed in the same cycle allows
    // `preventDefault`.
    batchedUpdates(handleTopLevelImpl, bookKeeping);
  } finally {
    releaseTopLevelCallbackBookKeeping(bookKeeping);
  }
}

var ReactDOMEventListener = Object.freeze({
	get _enabled () { return _enabled; },
	get _handleTopLevel () { return _handleTopLevel; },
	setHandleTopLevel: setHandleTopLevel,
	setEnabled: setEnabled,
	isEnabled: isEnabled,
	trapBubbledEvent: trapBubbledEvent,
	trapCapturedEvent: trapCapturedEvent,
	dispatchEvent: dispatchEvent
});

/**
 * Generate a mapping of standard vendor prefixes using the defined style property and event name.
 *
 * @param {string} styleProp
 * @param {string} eventName
 * @returns {object}
 */
function makePrefixMap(styleProp, eventName) {
  var prefixes = {};

  prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
  prefixes['Webkit' + styleProp] = 'webkit' + eventName;
  prefixes['Moz' + styleProp] = 'moz' + eventName;
  prefixes['ms' + styleProp] = 'MS' + eventName;
  prefixes['O' + styleProp] = 'o' + eventName.toLowerCase();

  return prefixes;
}

/**
 * A list of event names to a configurable list of vendor prefixes.
 */
var vendorPrefixes = {
  animationend: makePrefixMap('Animation', 'AnimationEnd'),
  animationiteration: makePrefixMap('Animation', 'AnimationIteration'),
  animationstart: makePrefixMap('Animation', 'AnimationStart'),
  transitionend: makePrefixMap('Transition', 'TransitionEnd')
};

/**
 * Event names that have already been detected and prefixed (if applicable).
 */
var prefixedEventNames = {};

/**
 * Element to check for prefixes on.
 */
var style = {};

/**
 * Bootstrap if a DOM exists.
 */
if (ExecutionEnvironment.canUseDOM) {
  style = document.createElement('div').style;

  // On some platforms, in particular some releases of Android 4.x,
  // the un-prefixed "animation" and "transition" properties are defined on the
  // style object but the events that fire will still be prefixed, so we need
  // to check if the un-prefixed events are usable, and if not remove them from the map.
  if (!('AnimationEvent' in window)) {
    delete vendorPrefixes.animationend.animation;
    delete vendorPrefixes.animationiteration.animation;
    delete vendorPrefixes.animationstart.animation;
  }

  // Same as above
  if (!('TransitionEvent' in window)) {
    delete vendorPrefixes.transitionend.transition;
  }
}

/**
 * Attempts to determine the correct vendor prefixed event name.
 *
 * @param {string} eventName
 * @returns {string}
 */
function getVendorPrefixedEventName(eventName) {
  if (prefixedEventNames[eventName]) {
    return prefixedEventNames[eventName];
  } else if (!vendorPrefixes[eventName]) {
    return eventName;
  }

  var prefixMap = vendorPrefixes[eventName];

  for (var styleProp in prefixMap) {
    if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) {
      return prefixedEventNames[eventName] = prefixMap[styleProp];
    }
  }

  return '';
}

/**
 * Types of raw signals from the browser caught at the top level.
 *
 * For events like 'submit' which don't consistently bubble (which we
 * trap at a lower node than `document`), binding at `document` would
 * cause duplicate events so we don't include them here.
 */
var topLevelTypes$1 = {
  topAbort: 'abort',
  topAnimationEnd: getVendorPrefixedEventName('animationend') || 'animationend',
  topAnimationIteration: getVendorPrefixedEventName('animationiteration') || 'animationiteration',
  topAnimationStart: getVendorPrefixedEventName('animationstart') || 'animationstart',
  topBlur: 'blur',
  topCancel: 'cancel',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topChange: 'change',
  topClick: 'click',
  topClose: 'close',
  topCompositionEnd: 'compositionend',
  topCompositionStart: 'compositionstart',
  topCompositionUpdate: 'compositionupdate',
  topContextMenu: 'contextmenu',
  topCopy: 'copy',
  topCut: 'cut',
  topDoubleClick: 'dblclick',
  topDrag: 'drag',
  topDragEnd: 'dragend',
  topDragEnter: 'dragenter',
  topDragExit: 'dragexit',
  topDragLeave: 'dragleave',
  topDragOver: 'dragover',
  topDragStart: 'dragstart',
  topDrop: 'drop',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topFocus: 'focus',
  topInput: 'input',
  topKeyDown: 'keydown',
  topKeyPress: 'keypress',
  topKeyUp: 'keyup',
  topLoadedData: 'loadeddata',
  topLoad: 'load',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topMouseDown: 'mousedown',
  topMouseMove: 'mousemove',
  topMouseOut: 'mouseout',
  topMouseOver: 'mouseover',
  topMouseUp: 'mouseup',
  topPaste: 'paste',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topScroll: 'scroll',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topSelectionChange: 'selectionchange',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTextInput: 'textInput',
  topTimeUpdate: 'timeupdate',
  topToggle: 'toggle',
  topTouchCancel: 'touchcancel',
  topTouchEnd: 'touchend',
  topTouchMove: 'touchmove',
  topTouchStart: 'touchstart',
  topTransitionEnd: getVendorPrefixedEventName('transitionend') || 'transitionend',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting',
  topWheel: 'wheel'
};

var BrowserEventConstants = {
  topLevelTypes: topLevelTypes$1
};

function runEventQueueInBatch(events) {
  enqueueEvents(events);
  processEventQueue(false);
}

/**
 * Streams a fired top-level event to `EventPluginHub` where plugins have the
 * opportunity to create `ReactEvent`s to be dispatched.
 */
function handleTopLevel(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var events = extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget);
  runEventQueueInBatch(events);
}

var topLevelTypes = BrowserEventConstants.topLevelTypes;

/**
 * Summary of `ReactBrowserEventEmitter` event handling:
 *
 *  - Top-level delegation is used to trap most native browser events. This
 *    may only occur in the main thread and is the responsibility of
 *    ReactDOMEventListener, which is injected and can therefore support
 *    pluggable event sources. This is the only work that occurs in the main
 *    thread.
 *
 *  - We normalize and de-duplicate events to account for browser quirks. This
 *    may be done in the worker thread.
 *
 *  - Forward these native events (with the associated top-level type used to
 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
 *    to extract any synthetic events.
 *
 *  - The `EventPluginHub` will then process each event by annotating them with
 *    "dispatches", a sequence of listeners and IDs that care about that event.
 *
 *  - The `EventPluginHub` then dispatches the events.
 *
 * Overview of React and the event system:
 *
 * +------------+    .
 * |    DOM     |    .
 * +------------+    .
 *       |           .
 *       v           .
 * +------------+    .
 * | ReactEvent |    .
 * |  Listener  |    .
 * +------------+    .                         +-----------+
 *       |           .               +--------+|SimpleEvent|
 *       |           .               |         |Plugin     |
 * +-----|------+    .               v         +-----------+
 * |     |      |    .    +--------------+                    +------------+
 * |     +-----------.--->|EventPluginHub|                    |    Event   |
 * |            |    .    |              |     +-----------+  | Propagators|
 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
 * |            |    .    |              |     +-----------+  |  utilities |
 * |     +-----------.--->|              |                    +------------+
 * |     |      |    .    +--------------+
 * +-----|------+    .                ^        +-----------+
 *       |           .                |        |Enter/Leave|
 *       +           .                +-------+|Plugin     |
 * +-------------+   .                         +-----------+
 * | application |   .
 * |-------------|   .
 * |             |   .
 * |             |   .
 * +-------------+   .
 *                   .
 *    React Core     .  General Purpose Event Plugin System
 */

var alreadyListeningTo = {};
var reactTopListenersCounter = 0;

/**
 * To ensure no conflicts with other potential React instances on the page
 */
var topListenersIDKey = '_reactListenersID' + ('' + Math.random()).slice(2);

function getListeningForDocument(mountAt) {
  // In IE8, `mountAt` is a host object and doesn't have `hasOwnProperty`
  // directly.
  if (!Object.prototype.hasOwnProperty.call(mountAt, topListenersIDKey)) {
    mountAt[topListenersIDKey] = reactTopListenersCounter++;
    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
  }
  return alreadyListeningTo[mountAt[topListenersIDKey]];
}

/**
 * We listen for bubbled touch events on the document object.
 *
 * Firefox v8.01 (and possibly others) exhibited strange behavior when
 * mounting `onmousemove` events at some node that was not the document
 * element. The symptoms were that if your mouse is not moving over something
 * contained within that mount point (for example on the background) the
 * top-level listeners for `onmousemove` won't be called. However, if you
 * register the `mousemove` on the document object, then it will of course
 * catch all `mousemove`s. This along with iOS quirks, justifies restricting
 * top-level listeners to the document object only, at least for these
 * movement types of events and possibly all events.
 *
 * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
 *
 * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
 * they bubble to document.
 *
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @param {object} contentDocumentHandle Document which owns the container
 */
function listenTo(registrationName, contentDocumentHandle) {
  var mountAt = contentDocumentHandle;
  var isListening = getListeningForDocument(mountAt);
  var dependencies = registrationNameDependencies[registrationName];

  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
      if (dependency === 'topScroll') {
        trapCapturedEvent('topScroll', 'scroll', mountAt);
      } else if (dependency === 'topFocus' || dependency === 'topBlur') {
        trapCapturedEvent('topFocus', 'focus', mountAt);
        trapCapturedEvent('topBlur', 'blur', mountAt);

        // to make sure blur and focus event listeners are only attached once
        isListening.topBlur = true;
        isListening.topFocus = true;
      } else if (dependency === 'topCancel') {
        if (isEventSupported('cancel', true)) {
          trapCapturedEvent('topCancel', 'cancel', mountAt);
        }
        isListening.topCancel = true;
      } else if (dependency === 'topClose') {
        if (isEventSupported('close', true)) {
          trapCapturedEvent('topClose', 'close', mountAt);
        }
        isListening.topClose = true;
      } else if (topLevelTypes.hasOwnProperty(dependency)) {
        trapBubbledEvent(dependency, topLevelTypes[dependency], mountAt);
      }

      isListening[dependency] = true;
    }
  }
}

function isListeningToAllDependencies(registrationName, mountAt) {
  var isListening = getListeningForDocument(mountAt);
  var dependencies = registrationNameDependencies[registrationName];
  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
      return false;
    }
  }
  return true;
}

/**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */
function getLeafNode(node) {
  while (node && node.firstChild) {
    node = node.firstChild;
  }
  return node;
}

/**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */
function getSiblingNode(node) {
  while (node) {
    if (node.nextSibling) {
      return node.nextSibling;
    }
    node = node.parentNode;
  }
}

/**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */
function getNodeForCharacterOffset(root, offset) {
  var node = getLeafNode(root);
  var nodeStart = 0;
  var nodeEnd = 0;

  while (node) {
    if (node.nodeType === TEXT_NODE) {
      nodeEnd = nodeStart + node.textContent.length;

      if (nodeStart <= offset && nodeEnd >= offset) {
        return {
          node: node,
          offset: offset - nodeStart
        };
      }

      nodeStart = nodeEnd;
    }

    node = getLeafNode(getSiblingNode(node));
  }
}

/**
 * @param {DOMElement} outerNode
 * @return {?object}
 */
function getOffsets(outerNode) {
  var selection = window.getSelection && window.getSelection();

  if (!selection || selection.rangeCount === 0) {
    return null;
  }

  var anchorNode = selection.anchorNode,
      anchorOffset = selection.anchorOffset,
      focusNode$$1 = selection.focusNode,
      focusOffset = selection.focusOffset;

  // In Firefox, anchorNode and focusNode can be "anonymous divs", e.g. the
  // up/down buttons on an <input type="number">. Anonymous divs do not seem to
  // expose properties, triggering a "Permission denied error" if any of its
  // properties are accessed. The only seemingly possible way to avoid erroring
  // is to access a property that typically works for non-anonymous divs and
  // catch any error that may otherwise arise. See
  // https://bugzilla.mozilla.org/show_bug.cgi?id=208427

  try {
    /* eslint-disable no-unused-expressions */
    anchorNode.nodeType;
    focusNode$$1.nodeType;
    /* eslint-enable no-unused-expressions */
  } catch (e) {
    return null;
  }

  return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode$$1, focusOffset);
}

/**
 * Returns {start, end} where `start` is the character/codepoint index of
 * (anchorNode, anchorOffset) within the textContent of `outerNode`, and
 * `end` is the index of (focusNode, focusOffset).
 *
 * Returns null if you pass in garbage input but we should probably just crash.
 *
 * Exported only for testing.
 */
function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode$$1, focusOffset) {
  var length = 0;
  var start = -1;
  var end = -1;
  var indexWithinAnchor = 0;
  var indexWithinFocus = 0;
  var node = outerNode;
  var parentNode = null;

  outer: while (true) {
    var next = null;

    while (true) {
      if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
        start = length + anchorOffset;
      }
      if (node === focusNode$$1 && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
        end = length + focusOffset;
      }

      if (node.nodeType === TEXT_NODE) {
        length += node.nodeValue.length;
      }

      if ((next = node.firstChild) === null) {
        break;
      }
      // Moving from `node` to its first child `next`.
      parentNode = node;
      node = next;
    }

    while (true) {
      if (node === outerNode) {
        // If `outerNode` has children, this is always the second time visiting
        // it. If it has no children, this is still the first loop, and the only
        // valid selection is anchorNode and focusNode both equal to this node
        // and both offsets 0, in which case we will have handled above.
        break outer;
      }
      if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) {
        start = length;
      }
      if (parentNode === focusNode$$1 && ++indexWithinFocus === focusOffset) {
        end = length;
      }
      if ((next = node.nextSibling) !== null) {
        break;
      }
      node = parentNode;
      parentNode = node.parentNode;
    }

    // Moving from `node` to its next sibling `next`.
    node = next;
  }

  if (start === -1 || end === -1) {
    // This should never happen. (Would happen if the anchor/focus nodes aren't
    // actually inside the passed-in node.)
    return null;
  }

  return {
    start: start,
    end: end
  };
}

/**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programmatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setOffsets(node, offsets) {
  if (!window.getSelection) {
    return;
  }

  var selection = window.getSelection();
  var length = node[getTextContentAccessor()].length;
  var start = Math.min(offsets.start, length);
  var end = offsets.end === undefined ? start : Math.min(offsets.end, length);

  // IE 11 uses modern selection, but doesn't support the extend method.
  // Flip backward selections, so we can set with a single range.
  if (!selection.extend && start > end) {
    var temp = end;
    end = start;
    start = temp;
  }

  var startMarker = getNodeForCharacterOffset(node, start);
  var endMarker = getNodeForCharacterOffset(node, end);

  if (startMarker && endMarker) {
    if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) {
      return;
    }
    var range = document.createRange();
    range.setStart(startMarker.node, startMarker.offset);
    selection.removeAllRanges();

    if (start > end) {
      selection.addRange(range);
      selection.extend(endMarker.node, endMarker.offset);
    } else {
      range.setEnd(endMarker.node, endMarker.offset);
      selection.addRange(range);
    }
  }
}

function isInDocument(node) {
  return containsNode(document.documentElement, node);
}

/**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */

function hasSelectionCapabilities(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName && (nodeName === 'input' && elem.type === 'text' || nodeName === 'textarea' || elem.contentEditable === 'true');
}

function getSelectionInformation() {
  var focusedElem = getActiveElement();
  return {
    focusedElem: focusedElem,
    selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection$1(focusedElem) : null
  };
}

/**
 * @restoreSelection: If any selection information was potentially lost,
 * restore it. This is useful when performing operations that could remove dom
 * nodes and place them back in, resulting in focus being lost.
 */
function restoreSelection(priorSelectionInformation) {
  var curFocusedElem = getActiveElement();
  var priorFocusedElem = priorSelectionInformation.focusedElem;
  var priorSelectionRange = priorSelectionInformation.selectionRange;
  if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
    if (hasSelectionCapabilities(priorFocusedElem)) {
      setSelection(priorFocusedElem, priorSelectionRange);
    }

    // Focusing a node can change the scroll position, which is undesirable
    var ancestors = [];
    var ancestor = priorFocusedElem;
    while (ancestor = ancestor.parentNode) {
      if (ancestor.nodeType === ELEMENT_NODE) {
        ancestors.push({
          element: ancestor,
          left: ancestor.scrollLeft,
          top: ancestor.scrollTop
        });
      }
    }

    focusNode(priorFocusedElem);

    for (var i = 0; i < ancestors.length; i++) {
      var info = ancestors[i];
      info.element.scrollLeft = info.left;
      info.element.scrollTop = info.top;
    }
  }
}

/**
 * @getSelection: Gets the selection bounds of a focused textarea, input or
 * contentEditable node.
 * -@input: Look up selection bounds of this input
 * -@return {start: selectionStart, end: selectionEnd}
 */
function getSelection$1(input) {
  var selection = void 0;

  if ('selectionStart' in input) {
    // Modern browser with input or textarea.
    selection = {
      start: input.selectionStart,
      end: input.selectionEnd
    };
  } else {
    // Content editable or old IE textarea.
    selection = getOffsets(input);
  }

  return selection || { start: 0, end: 0 };
}

/**
 * @setSelection: Sets the selection bounds of a textarea or input and focuses
 * the input.
 * -@input     Set selection bounds of this input or textarea
 * -@offsets   Object of same form that is returned from get*
 */
function setSelection(input, offsets) {
  var start = offsets.start,
      end = offsets.end;

  if (end === undefined) {
    end = start;
  }

  if ('selectionStart' in input) {
    input.selectionStart = start;
    input.selectionEnd = Math.min(end, input.value.length);
  } else {
    setOffsets(input, offsets);
  }
}

var skipSelectionChangeEvent = ExecutionEnvironment.canUseDOM && 'documentMode' in document && document.documentMode <= 11;

var eventTypes$3 = {
  select: {
    phasedRegistrationNames: {
      bubbled: 'onSelect',
      captured: 'onSelectCapture'
    },
    dependencies: ['topBlur', 'topContextMenu', 'topFocus', 'topKeyDown', 'topKeyUp', 'topMouseDown', 'topMouseUp', 'topSelectionChange']
  }
};

var activeElement$1 = null;
var activeElementInst$1 = null;
var lastSelection = null;
var mouseDown = false;

/**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 *
 * @param {DOMElement} node
 * @return {object}
 */
function getSelection(node) {
  if ('selectionStart' in node && hasSelectionCapabilities(node)) {
    return {
      start: node.selectionStart,
      end: node.selectionEnd
    };
  } else if (window.getSelection) {
    var selection = window.getSelection();
    return {
      anchorNode: selection.anchorNode,
      anchorOffset: selection.anchorOffset,
      focusNode: selection.focusNode,
      focusOffset: selection.focusOffset
    };
  }
}

/**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @return {?SyntheticEvent}
 */
function constructSelectEvent(nativeEvent, nativeEventTarget) {
  // Ensure we have the right element, and that the user is not dragging a
  // selection (this matches native `select` event behavior). In HTML5, select
  // fires only on input and textarea thus if there's no focused element we
  // won't dispatch.
  if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement()) {
    return null;
  }

  // Only fire when selection has actually changed.
  var currentSelection = getSelection(activeElement$1);
  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
    lastSelection = currentSelection;

    var syntheticEvent = SyntheticEvent$1.getPooled(eventTypes$3.select, activeElementInst$1, nativeEvent, nativeEventTarget);

    syntheticEvent.type = 'select';
    syntheticEvent.target = activeElement$1;

    accumulateTwoPhaseDispatches(syntheticEvent);

    return syntheticEvent;
  }

  return null;
}

/**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */
var SelectEventPlugin = {
  eventTypes: eventTypes$3,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : nativeEventTarget.nodeType === DOCUMENT_NODE ? nativeEventTarget : nativeEventTarget.ownerDocument;
    // Track whether all listeners exists for this plugin. If none exist, we do
    // not extract events. See #3639.
    if (!doc || !isListeningToAllDependencies('onSelect', doc)) {
      return null;
    }

    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    switch (topLevelType) {
      // Track the input node that has focus.
      case 'topFocus':
        if (isTextInputElement(targetNode) || targetNode.contentEditable === 'true') {
          activeElement$1 = targetNode;
          activeElementInst$1 = targetInst;
          lastSelection = null;
        }
        break;
      case 'topBlur':
        activeElement$1 = null;
        activeElementInst$1 = null;
        lastSelection = null;
        break;
      // Don't fire the event while the user is dragging. This matches the
      // semantics of the native select event.
      case 'topMouseDown':
        mouseDown = true;
        break;
      case 'topContextMenu':
      case 'topMouseUp':
        mouseDown = false;
        return constructSelectEvent(nativeEvent, nativeEventTarget);
      // Chrome and IE fire non-standard event when selection is changed (and
      // sometimes when it hasn't). IE's event fires out of order with respect
      // to key and input events on deletion, so we discard it.
      //
      // Firefox doesn't support selectionchange, so check selection status
      // after each key entry. The selection changes after keydown and before
      // keyup, but we check on keydown as well in the case of holding down a
      // key, when multiple keydown events are fired but only one keyup is.
      // This is also our approach for IE handling, for the reason above.
      case 'topSelectionChange':
        if (skipSelectionChangeEvent) {
          break;
        }
      // falls through
      case 'topKeyDown':
      case 'topKeyUp':
        return constructSelectEvent(nativeEvent, nativeEventTarget);
    }

    return null;
  }
};

/**
 * @interface Event
 * @see http://www.w3.org/TR/css3-animations/#AnimationEvent-interface
 * @see https://developer.mozilla.org/en-US/docs/Web/API/AnimationEvent
 */
var AnimationEventInterface = {
  animationName: null,
  elapsedTime: null,
  pseudoElement: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticAnimationEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticAnimationEvent, AnimationEventInterface);

/**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */
var ClipboardEventInterface = {
  clipboardData: function (event) {
    return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticClipboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticClipboardEvent, ClipboardEventInterface);

/**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var FocusEventInterface = {
  relatedTarget: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticFocusEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticFocusEvent, FocusEventInterface);

/**
 * `charCode` represents the actual "character code" and is safe to use with
 * `String.fromCharCode`. As such, only keys that correspond to printable
 * characters produce a valid `charCode`, the only exception to this is Enter.
 * The Tab-key is considered non-printable and does not have a `charCode`,
 * presumably because it does not produce a tab-character in browsers.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {number} Normalized `charCode` property.
 */
function getEventCharCode(nativeEvent) {
  var charCode;
  var keyCode = nativeEvent.keyCode;

  if ('charCode' in nativeEvent) {
    charCode = nativeEvent.charCode;

    // FF does not set `charCode` for the Enter-key, check against `keyCode`.
    if (charCode === 0 && keyCode === 13) {
      charCode = 13;
    }
  } else {
    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
    charCode = keyCode;
  }

  // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
  // Must not discard the (non-)printable Enter-key.
  if (charCode >= 32 || charCode === 13) {
    return charCode;
  }

  return 0;
}

/**
 * Normalization of deprecated HTML5 `key` values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var normalizeKey = {
  Esc: 'Escape',
  Spacebar: ' ',
  Left: 'ArrowLeft',
  Up: 'ArrowUp',
  Right: 'ArrowRight',
  Down: 'ArrowDown',
  Del: 'Delete',
  Win: 'OS',
  Menu: 'ContextMenu',
  Apps: 'ContextMenu',
  Scroll: 'ScrollLock',
  MozPrintableKey: 'Unidentified'
};

/**
 * Translation from legacy `keyCode` to HTML5 `key`
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var translateToKey = {
  '8': 'Backspace',
  '9': 'Tab',
  '12': 'Clear',
  '13': 'Enter',
  '16': 'Shift',
  '17': 'Control',
  '18': 'Alt',
  '19': 'Pause',
  '20': 'CapsLock',
  '27': 'Escape',
  '32': ' ',
  '33': 'PageUp',
  '34': 'PageDown',
  '35': 'End',
  '36': 'Home',
  '37': 'ArrowLeft',
  '38': 'ArrowUp',
  '39': 'ArrowRight',
  '40': 'ArrowDown',
  '45': 'Insert',
  '46': 'Delete',
  '112': 'F1',
  '113': 'F2',
  '114': 'F3',
  '115': 'F4',
  '116': 'F5',
  '117': 'F6',
  '118': 'F7',
  '119': 'F8',
  '120': 'F9',
  '121': 'F10',
  '122': 'F11',
  '123': 'F12',
  '144': 'NumLock',
  '145': 'ScrollLock',
  '224': 'Meta'
};

/**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */
function getEventKey(nativeEvent) {
  if (nativeEvent.key) {
    // Normalize inconsistent values reported by browsers due to
    // implementations of a working draft specification.

    // FireFox implements `key` but returns `MozPrintableKey` for all
    // printable characters (normalized to `Unidentified`), ignore it.
    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
    if (key !== 'Unidentified') {
      return key;
    }
  }

  // Browser does not implement `key`, polyfill as much of it as we can.
  if (nativeEvent.type === 'keypress') {
    var charCode = getEventCharCode(nativeEvent);

    // The enter-key is technically both printable and non-printable and can
    // thus be captured by `keypress`, no other non-printable key should.
    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
  }
  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
    // While user keyboard layout determines the actual meaning of each
    // `keyCode` value, almost all function keys have a universal value.
    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
  }
  return '';
}

/**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var KeyboardEventInterface = {
  key: getEventKey,
  location: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  repeat: null,
  locale: null,
  getModifierState: getEventModifierState,
  // Legacy Interface
  charCode: function (event) {
    // `charCode` is the result of a KeyPress event and represents the value of
    // the actual printable character.

    // KeyPress is deprecated, but its replacement is not yet final and not
    // implemented in any major browser. Only KeyPress has charCode.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    return 0;
  },
  keyCode: function (event) {
    // `keyCode` is the result of a KeyDown/Up event and represents the value of
    // physical keyboard key.

    // The actual meaning of the value depends on the users' keyboard layout
    // which cannot be detected. Assuming that it is a US keyboard layout
    // provides a surprisingly accurate mapping for US and European users.
    // Due to this, it is left to the user to implement at this time.
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  },
  which: function (event) {
    // `which` is an alias for either `keyCode` or `charCode` depending on the
    // type of the event.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticKeyboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticKeyboardEvent, KeyboardEventInterface);

/**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var DragEventInterface = {
  dataTransfer: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticMouseEvent}
 */
function SyntheticDragEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticMouseEvent.augmentClass(SyntheticDragEvent, DragEventInterface);

/**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */
var TouchEventInterface = {
  touches: null,
  targetTouches: null,
  changedTouches: null,
  altKey: null,
  metaKey: null,
  ctrlKey: null,
  shiftKey: null,
  getModifierState: getEventModifierState
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticTouchEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticTouchEvent, TouchEventInterface);

/**
 * @interface Event
 * @see http://www.w3.org/TR/2009/WD-css3-transitions-20090320/#transition-events-
 * @see https://developer.mozilla.org/en-US/docs/Web/API/TransitionEvent
 */
var TransitionEventInterface = {
  propertyName: null,
  elapsedTime: null,
  pseudoElement: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticTransitionEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticTransitionEvent, TransitionEventInterface);

/**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var WheelEventInterface = {
  deltaX: function (event) {
    return 'deltaX' in event ? event.deltaX : // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
    'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
  },
  deltaY: function (event) {
    return 'deltaY' in event ? event.deltaY : // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
    'wheelDeltaY' in event ? -event.wheelDeltaY : // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
    'wheelDelta' in event ? -event.wheelDelta : 0;
  },
  deltaZ: null,

  // Browsers without "deltaMode" is reporting in raw wheel delta where one
  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
  deltaMode: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticMouseEvent}
 */
function SyntheticWheelEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticMouseEvent.augmentClass(SyntheticWheelEvent, WheelEventInterface);

/**
 * Turns
 * ['abort', ...]
 * into
 * eventTypes = {
 *   'abort': {
 *     phasedRegistrationNames: {
 *       bubbled: 'onAbort',
 *       captured: 'onAbortCapture',
 *     },
 *     dependencies: ['topAbort'],
 *   },
 *   ...
 * };
 * topLevelEventsToDispatchConfig = {
 *   'topAbort': { sameConfig }
 * };
 */
var eventTypes$4 = {};
var topLevelEventsToDispatchConfig = {};
['abort', 'animationEnd', 'animationIteration', 'animationStart', 'blur', 'cancel', 'canPlay', 'canPlayThrough', 'click', 'close', 'contextMenu', 'copy', 'cut', 'doubleClick', 'drag', 'dragEnd', 'dragEnter', 'dragExit', 'dragLeave', 'dragOver', 'dragStart', 'drop', 'durationChange', 'emptied', 'encrypted', 'ended', 'error', 'focus', 'input', 'invalid', 'keyDown', 'keyPress', 'keyUp', 'load', 'loadedData', 'loadedMetadata', 'loadStart', 'mouseDown', 'mouseMove', 'mouseOut', 'mouseOver', 'mouseUp', 'paste', 'pause', 'play', 'playing', 'progress', 'rateChange', 'reset', 'scroll', 'seeked', 'seeking', 'stalled', 'submit', 'suspend', 'timeUpdate', 'toggle', 'touchCancel', 'touchEnd', 'touchMove', 'touchStart', 'transitionEnd', 'volumeChange', 'waiting', 'wheel'].forEach(function (event) {
  var capitalizedEvent = event[0].toUpperCase() + event.slice(1);
  var onEvent = 'on' + capitalizedEvent;
  var topEvent = 'top' + capitalizedEvent;

  var type = {
    phasedRegistrationNames: {
      bubbled: onEvent,
      captured: onEvent + 'Capture'
    },
    dependencies: [topEvent]
  };
  eventTypes$4[event] = type;
  topLevelEventsToDispatchConfig[topEvent] = type;
});

// Only used in DEV for exhaustiveness validation.
var knownHTMLTopLevelTypes = ['topAbort', 'topCancel', 'topCanPlay', 'topCanPlayThrough', 'topClose', 'topDurationChange', 'topEmptied', 'topEncrypted', 'topEnded', 'topError', 'topInput', 'topInvalid', 'topLoad', 'topLoadedData', 'topLoadedMetadata', 'topLoadStart', 'topPause', 'topPlay', 'topPlaying', 'topProgress', 'topRateChange', 'topReset', 'topSeeked', 'topSeeking', 'topStalled', 'topSubmit', 'topSuspend', 'topTimeUpdate', 'topToggle', 'topVolumeChange', 'topWaiting'];

var SimpleEventPlugin = {
  eventTypes: eventTypes$4,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
    if (!dispatchConfig) {
      return null;
    }
    var EventConstructor;
    switch (topLevelType) {
      case 'topKeyPress':
        // Firefox creates a keypress event for function keys too. This removes
        // the unwanted keypress events. Enter is however both printable and
        // non-printable. One would expect Tab to be as well (but it isn't).
        if (getEventCharCode(nativeEvent) === 0) {
          return null;
        }
      /* falls through */
      case 'topKeyDown':
      case 'topKeyUp':
        EventConstructor = SyntheticKeyboardEvent;
        break;
      case 'topBlur':
      case 'topFocus':
        EventConstructor = SyntheticFocusEvent;
        break;
      case 'topClick':
        // Firefox creates a click event on right mouse clicks. This removes the
        // unwanted click events.
        if (nativeEvent.button === 2) {
          return null;
        }
      /* falls through */
      case 'topDoubleClick':
      case 'topMouseDown':
      case 'topMouseMove':
      case 'topMouseUp':
      // TODO: Disabled elements should not respond to mouse events
      /* falls through */
      case 'topMouseOut':
      case 'topMouseOver':
      case 'topContextMenu':
        EventConstructor = SyntheticMouseEvent;
        break;
      case 'topDrag':
      case 'topDragEnd':
      case 'topDragEnter':
      case 'topDragExit':
      case 'topDragLeave':
      case 'topDragOver':
      case 'topDragStart':
      case 'topDrop':
        EventConstructor = SyntheticDragEvent;
        break;
      case 'topTouchCancel':
      case 'topTouchEnd':
      case 'topTouchMove':
      case 'topTouchStart':
        EventConstructor = SyntheticTouchEvent;
        break;
      case 'topAnimationEnd':
      case 'topAnimationIteration':
      case 'topAnimationStart':
        EventConstructor = SyntheticAnimationEvent;
        break;
      case 'topTransitionEnd':
        EventConstructor = SyntheticTransitionEvent;
        break;
      case 'topScroll':
        EventConstructor = SyntheticUIEvent;
        break;
      case 'topWheel':
        EventConstructor = SyntheticWheelEvent;
        break;
      case 'topCopy':
      case 'topCut':
      case 'topPaste':
        EventConstructor = SyntheticClipboardEvent;
        break;
      default:
        {
          if (knownHTMLTopLevelTypes.indexOf(topLevelType) === -1) {
            warning(false, 'SimpleEventPlugin: Unhandled event type, `%s`. This warning ' + 'is likely caused by a bug in React. Please file an issue.', topLevelType);
          }
        }
        // HTML Events
        // @see http://www.w3.org/TR/html5/index.html#events-0
        EventConstructor = SyntheticEvent$1;
        break;
    }
    var event = EventConstructor.getPooled(dispatchConfig, targetInst, nativeEvent, nativeEventTarget);
    accumulateTwoPhaseDispatches(event);
    return event;
  }
};

setHandleTopLevel(handleTopLevel);

/**
 * Inject modules for resolving DOM hierarchy and plugin ordering.
 */
injection$1.injectEventPluginOrder(DOMEventPluginOrder);
injection$2.injectComponentTree(ReactDOMComponentTree);

/**
 * Some important event plugins included by default (without having to require
 * them).
 */
injection$1.injectEventPluginsByName({
  SimpleEventPlugin: SimpleEventPlugin,
  EnterLeaveEventPlugin: EnterLeaveEventPlugin,
  ChangeEventPlugin: ChangeEventPlugin,
  SelectEventPlugin: SelectEventPlugin,
  BeforeInputEventPlugin: BeforeInputEventPlugin
});

var enableAsyncSubtreeAPI = true;
var enableAsyncSchedulingByDefaultInReactDOM = false;
// Exports ReactDOM.createRoot
var enableCreateRoot = false;
var enableUserTimingAPI = true;

// Mutating mode (React DOM, React ART, React Native):
var enableMutatingReconciler = true;
// Experimental noop mode (currently unused):
var enableNoopReconciler = false;
// Experimental persistent mode (CS):
var enablePersistentReconciler = false;

// Helps identify side effects in begin-phase lifecycle hooks and setState reducers:
var debugRenderPhaseSideEffects = false;

// Only used in www builds.

var valueStack = [];

{
  var fiberStack = [];
}

var index = -1;

function createCursor(defaultValue) {
  return {
    current: defaultValue
  };
}



function pop(cursor, fiber) {
  if (index < 0) {
    {
      warning(false, 'Unexpected pop.');
    }
    return;
  }

  {
    if (fiber !== fiberStack[index]) {
      warning(false, 'Unexpected Fiber popped.');
    }
  }

  cursor.current = valueStack[index];

  valueStack[index] = null;

  {
    fiberStack[index] = null;
  }

  index--;
}

function push(cursor, value, fiber) {
  index++;

  valueStack[index] = cursor.current;

  {
    fiberStack[index] = fiber;
  }

  cursor.current = value;
}

function reset$1() {
  while (index > -1) {
    valueStack[index] = null;

    {
      fiberStack[index] = null;
    }

    index--;
  }
}

var describeComponentFrame = function (name, source, ownerName) {
  return '\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');
};

function describeFiber(fiber) {
  switch (fiber.tag) {
    case IndeterminateComponent:
    case FunctionalComponent:
    case ClassComponent:
    case HostComponent:
      var owner = fiber._debugOwner;
      var source = fiber._debugSource;
      var name = getComponentName(fiber);
      var ownerName = null;
      if (owner) {
        ownerName = getComponentName(owner);
      }
      return describeComponentFrame(name, source, ownerName);
    default:
      return '';
  }
}

// This function can only be called with a work-in-progress fiber and
// only during begin or complete phase. Do not call it under any other
// circumstances.
function getStackAddendumByWorkInProgressFiber(workInProgress) {
  var info = '';
  var node = workInProgress;
  do {
    info += describeFiber(node);
    // Otherwise this return pointer might point to the wrong tree:
    node = node['return'];
  } while (node);
  return info;
}

function getCurrentFiberOwnerName() {
  {
    var fiber = ReactDebugCurrentFiber.current;
    if (fiber === null) {
      return null;
    }
    var owner = fiber._debugOwner;
    if (owner !== null && typeof owner !== 'undefined') {
      return getComponentName(owner);
    }
  }
  return null;
}

function getCurrentFiberStackAddendum() {
  {
    var fiber = ReactDebugCurrentFiber.current;
    if (fiber === null) {
      return null;
    }
    // Safe because if current fiber exists, we are reconciling,
    // and it is guaranteed to be the work-in-progress version.
    return getStackAddendumByWorkInProgressFiber(fiber);
  }
  return null;
}

function resetCurrentFiber() {
  ReactDebugCurrentFrame.getCurrentStack = null;
  ReactDebugCurrentFiber.current = null;
  ReactDebugCurrentFiber.phase = null;
}

function setCurrentFiber(fiber) {
  ReactDebugCurrentFrame.getCurrentStack = getCurrentFiberStackAddendum;
  ReactDebugCurrentFiber.current = fiber;
  ReactDebugCurrentFiber.phase = null;
}

function setCurrentPhase(phase) {
  ReactDebugCurrentFiber.phase = phase;
}

var ReactDebugCurrentFiber = {
  current: null,
  phase: null,
  resetCurrentFiber: resetCurrentFiber,
  setCurrentFiber: setCurrentFiber,
  setCurrentPhase: setCurrentPhase,
  getCurrentFiberOwnerName: getCurrentFiberOwnerName,
  getCurrentFiberStackAddendum: getCurrentFiberStackAddendum
};

// Prefix measurements so that it's possible to filter them.
// Longer prefixes are hard to read in DevTools.
var reactEmoji = '\u269B';
var warningEmoji = '\u26D4';
var supportsUserTiming = typeof performance !== 'undefined' && typeof performance.mark === 'function' && typeof performance.clearMarks === 'function' && typeof performance.measure === 'function' && typeof performance.clearMeasures === 'function';

// Keep track of current fiber so that we know the path to unwind on pause.
// TODO: this looks the same as nextUnitOfWork in scheduler. Can we unify them?
var currentFiber = null;
// If we're in the middle of user code, which fiber and method is it?
// Reusing `currentFiber` would be confusing for this because user code fiber
// can change during commit phase too, but we don't need to unwind it (since
// lifecycles in the commit phase don't resemble a tree).
var currentPhase = null;
var currentPhaseFiber = null;
// Did lifecycle hook schedule an update? This is often a performance problem,
// so we will keep track of it, and include it in the report.
// Track commits caused by cascading updates.
var isCommitting = false;
var hasScheduledUpdateInCurrentCommit = false;
var hasScheduledUpdateInCurrentPhase = false;
var commitCountInCurrentWorkLoop = 0;
var effectCountInCurrentCommit = 0;
var isWaitingForCallback = false;
// During commits, we only show a measurement once per method name
// to avoid stretch the commit phase with measurement overhead.
var labelsInCurrentCommit = new Set();

var formatMarkName = function (markName) {
  return reactEmoji + ' ' + markName;
};

var formatLabel = function (label, warning$$1) {
  var prefix = warning$$1 ? warningEmoji + ' ' : reactEmoji + ' ';
  var suffix = warning$$1 ? ' Warning: ' + warning$$1 : '';
  return '' + prefix + label + suffix;
};

var beginMark = function (markName) {
  performance.mark(formatMarkName(markName));
};

var clearMark = function (markName) {
  performance.clearMarks(formatMarkName(markName));
};

var endMark = function (label, markName, warning$$1) {
  var formattedMarkName = formatMarkName(markName);
  var formattedLabel = formatLabel(label, warning$$1);
  try {
    performance.measure(formattedLabel, formattedMarkName);
  } catch (err) {}
  // If previous mark was missing for some reason, this will throw.
  // This could only happen if React crashed in an unexpected place earlier.
  // Don't pile on with more errors.

  // Clear marks immediately to avoid growing buffer.
  performance.clearMarks(formattedMarkName);
  performance.clearMeasures(formattedLabel);
};

var getFiberMarkName = function (label, debugID) {
  return label + ' (#' + debugID + ')';
};

var getFiberLabel = function (componentName, isMounted, phase) {
  if (phase === null) {
    // These are composite component total time measurements.
    return componentName + ' [' + (isMounted ? 'update' : 'mount') + ']';
  } else {
    // Composite component methods.
    return componentName + '.' + phase;
  }
};

var beginFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);

  if (isCommitting && labelsInCurrentCommit.has(label)) {
    // During the commit phase, we don't show duplicate labels because
    // there is a fixed overhead for every measurement, and we don't
    // want to stretch the commit phase beyond necessary.
    return false;
  }
  labelsInCurrentCommit.add(label);

  var markName = getFiberMarkName(label, debugID);
  beginMark(markName);
  return true;
};

var clearFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  clearMark(markName);
};

var endFiberMark = function (fiber, phase, warning$$1) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  endMark(label, markName, warning$$1);
};

var shouldIgnoreFiber = function (fiber) {
  // Host components should be skipped in the timeline.
  // We could check typeof fiber.type, but does this work with RN?
  switch (fiber.tag) {
    case HostRoot:
    case HostComponent:
    case HostText:
    case HostPortal:
    case ReturnComponent:
    case Fragment:
      return true;
    default:
      return false;
  }
};

var clearPendingPhaseMeasurement = function () {
  if (currentPhase !== null && currentPhaseFiber !== null) {
    clearFiberMark(currentPhaseFiber, currentPhase);
  }
  currentPhaseFiber = null;
  currentPhase = null;
  hasScheduledUpdateInCurrentPhase = false;
};

var pauseTimers = function () {
  // Stops all currently active measurements so that they can be resumed
  // if we continue in a later deferred loop from the same unit of work.
  var fiber = currentFiber;
  while (fiber) {
    if (fiber._debugIsCurrentlyTiming) {
      endFiberMark(fiber, null, null);
    }
    fiber = fiber['return'];
  }
};

var resumeTimersRecursively = function (fiber) {
  if (fiber['return'] !== null) {
    resumeTimersRecursively(fiber['return']);
  }
  if (fiber._debugIsCurrentlyTiming) {
    beginFiberMark(fiber, null);
  }
};

var resumeTimers = function () {
  // Resumes all measurements that were active during the last deferred loop.
  if (currentFiber !== null) {
    resumeTimersRecursively(currentFiber);
  }
};

function recordEffect() {
  if (enableUserTimingAPI) {
    effectCountInCurrentCommit++;
  }
}

function recordScheduleUpdate() {
  if (enableUserTimingAPI) {
    if (isCommitting) {
      hasScheduledUpdateInCurrentCommit = true;
    }
    if (currentPhase !== null && currentPhase !== 'componentWillMount' && currentPhase !== 'componentWillReceiveProps') {
      hasScheduledUpdateInCurrentPhase = true;
    }
  }
}

function startRequestCallbackTimer() {
  if (enableUserTimingAPI) {
    if (supportsUserTiming && !isWaitingForCallback) {
      isWaitingForCallback = true;
      beginMark('(Waiting for async callback...)');
    }
  }
}

function stopRequestCallbackTimer(didExpire) {
  if (enableUserTimingAPI) {
    if (supportsUserTiming) {
      isWaitingForCallback = false;
      var warning$$1 = didExpire ? 'React was blocked by main thread' : null;
      endMark('(Waiting for async callback...)', '(Waiting for async callback...)', warning$$1);
    }
  }
}

function startWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, this is the fiber to unwind from.
    currentFiber = fiber;
    if (!beginFiberMark(fiber, null)) {
      return;
    }
    fiber._debugIsCurrentlyTiming = true;
  }
}

function cancelWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // Remember we shouldn't complete measurement for this fiber.
    // Otherwise flamechart will be deep even for small updates.
    fiber._debugIsCurrentlyTiming = false;
    clearFiberMark(fiber, null);
  }
}

function stopWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, its parent is the fiber to unwind from.
    currentFiber = fiber['return'];
    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }
    fiber._debugIsCurrentlyTiming = false;
    endFiberMark(fiber, null, null);
  }
}

function stopFailedWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, its parent is the fiber to unwind from.
    currentFiber = fiber['return'];
    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }
    fiber._debugIsCurrentlyTiming = false;
    var warning$$1 = 'An error was thrown inside this error boundary';
    endFiberMark(fiber, null, warning$$1);
  }
}

function startPhaseTimer(fiber, phase) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    clearPendingPhaseMeasurement();
    if (!beginFiberMark(fiber, phase)) {
      return;
    }
    currentPhaseFiber = fiber;
    currentPhase = phase;
  }
}

function stopPhaseTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    if (currentPhase !== null && currentPhaseFiber !== null) {
      var warning$$1 = hasScheduledUpdateInCurrentPhase ? 'Scheduled a cascading update' : null;
      endFiberMark(currentPhaseFiber, currentPhase, warning$$1);
    }
    currentPhase = null;
    currentPhaseFiber = null;
  }
}

function startWorkLoopTimer(nextUnitOfWork) {
  if (enableUserTimingAPI) {
    currentFiber = nextUnitOfWork;
    if (!supportsUserTiming) {
      return;
    }
    commitCountInCurrentWorkLoop = 0;
    // This is top level call.
    // Any other measurements are performed within.
    beginMark('(React Tree Reconciliation)');
    // Resume any measurements that were in progress during the last loop.
    resumeTimers();
  }
}

function stopWorkLoopTimer(interruptedBy) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var warning$$1 = null;
    if (interruptedBy !== null) {
      if (interruptedBy.tag === HostRoot) {
        warning$$1 = 'A top-level update interrupted the previous render';
      } else {
        var componentName = getComponentName(interruptedBy) || 'Unknown';
        warning$$1 = 'An update to ' + componentName + ' interrupted the previous render';
      }
    } else if (commitCountInCurrentWorkLoop > 1) {
      warning$$1 = 'There were cascading updates';
    }
    commitCountInCurrentWorkLoop = 0;
    // Pause any measurements until the next loop.
    pauseTimers();
    endMark('(React Tree Reconciliation)', '(React Tree Reconciliation)', warning$$1);
  }
}

function startCommitTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    isCommitting = true;
    hasScheduledUpdateInCurrentCommit = false;
    labelsInCurrentCommit.clear();
    beginMark('(Committing Changes)');
  }
}

function stopCommitTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }

    var warning$$1 = null;
    if (hasScheduledUpdateInCurrentCommit) {
      warning$$1 = 'Lifecycle hook scheduled a cascading update';
    } else if (commitCountInCurrentWorkLoop > 0) {
      warning$$1 = 'Caused by a cascading update in earlier commit';
    }
    hasScheduledUpdateInCurrentCommit = false;
    commitCountInCurrentWorkLoop++;
    isCommitting = false;
    labelsInCurrentCommit.clear();

    endMark('(Committing Changes)', '(Committing Changes)', warning$$1);
  }
}

function startCommitHostEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Committing Host Effects)');
  }
}

function stopCommitHostEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Committing Host Effects: ' + count + ' Total)', '(Committing Host Effects)', null);
  }
}

function startCommitLifeCyclesTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Calling Lifecycle Methods)');
  }
}

function stopCommitLifeCyclesTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Calling Lifecycle Methods: ' + count + ' Total)', '(Calling Lifecycle Methods)', null);
  }
}

{
  var warnedAboutMissingGetChildContext = {};
}

// A cursor to the current merged context object on the stack.
var contextStackCursor = createCursor(emptyObject);
// A cursor to a boolean indicating whether the context has changed.
var didPerformWorkStackCursor = createCursor(false);
// Keep track of the previous context object that was on the stack.
// We use this to get access to the parent context after we have already
// pushed the next context provider, and now need to merge their contexts.
var previousContext = emptyObject;

function getUnmaskedContext(workInProgress) {
  var hasOwnContext = isContextProvider(workInProgress);
  if (hasOwnContext) {
    // If the fiber is a context provider itself, when we read its context
    // we have already pushed its own child context on the stack. A context
    // provider should not "see" its own child context. Therefore we read the
    // previous (parent) context instead for a context provider.
    return previousContext;
  }
  return contextStackCursor.current;
}

function cacheContext(workInProgress, unmaskedContext, maskedContext) {
  var instance = workInProgress.stateNode;
  instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
  instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
}

function getMaskedContext(workInProgress, unmaskedContext) {
  var type = workInProgress.type;
  var contextTypes = type.contextTypes;
  if (!contextTypes) {
    return emptyObject;
  }

  // Avoid recreating masked context unless unmasked context has changed.
  // Failing to do this will result in unnecessary calls to componentWillReceiveProps.
  // This may trigger infinite loops if componentWillReceiveProps calls setState.
  var instance = workInProgress.stateNode;
  if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) {
    return instance.__reactInternalMemoizedMaskedChildContext;
  }

  var context = {};
  for (var key in contextTypes) {
    context[key] = unmaskedContext[key];
  }

  {
    var name = getComponentName(workInProgress) || 'Unknown';
    checkPropTypes(contextTypes, context, 'context', name, ReactDebugCurrentFiber.getCurrentFiberStackAddendum);
  }

  // Cache unmasked context so we can avoid recreating masked context unless necessary.
  // Context is created before the class component is instantiated so check for instance.
  if (instance) {
    cacheContext(workInProgress, unmaskedContext, context);
  }

  return context;
}

function hasContextChanged() {
  return didPerformWorkStackCursor.current;
}

function isContextConsumer(fiber) {
  return fiber.tag === ClassComponent && fiber.type.contextTypes != null;
}

function isContextProvider(fiber) {
  return fiber.tag === ClassComponent && fiber.type.childContextTypes != null;
}

function popContextProvider(fiber) {
  if (!isContextProvider(fiber)) {
    return;
  }

  pop(didPerformWorkStackCursor, fiber);
  pop(contextStackCursor, fiber);
}

function popTopLevelContextObject(fiber) {
  pop(didPerformWorkStackCursor, fiber);
  pop(contextStackCursor, fiber);
}

function pushTopLevelContextObject(fiber, context, didChange) {
  !(contextStackCursor.cursor == null) ? invariant(false, 'Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue.') : void 0;

  push(contextStackCursor, context, fiber);
  push(didPerformWorkStackCursor, didChange, fiber);
}

function processChildContext(fiber, parentContext) {
  var instance = fiber.stateNode;
  var childContextTypes = fiber.type.childContextTypes;

  // TODO (bvaughn) Replace this behavior with an invariant() in the future.
  // It has only been added in Fiber to match the (unintentional) behavior in Stack.
  if (typeof instance.getChildContext !== 'function') {
    {
      var componentName = getComponentName(fiber) || 'Unknown';

      if (!warnedAboutMissingGetChildContext[componentName]) {
        warnedAboutMissingGetChildContext[componentName] = true;
        warning(false, '%s.childContextTypes is specified but there is no getChildContext() method ' + 'on the instance. You can either define getChildContext() on %s or remove ' + 'childContextTypes from it.', componentName, componentName);
      }
    }
    return parentContext;
  }

  var childContext = void 0;
  {
    ReactDebugCurrentFiber.setCurrentPhase('getChildContext');
  }
  startPhaseTimer(fiber, 'getChildContext');
  childContext = instance.getChildContext();
  stopPhaseTimer();
  {
    ReactDebugCurrentFiber.setCurrentPhase(null);
  }
  for (var contextKey in childContext) {
    !(contextKey in childContextTypes) ? invariant(false, '%s.getChildContext(): key "%s" is not defined in childContextTypes.', getComponentName(fiber) || 'Unknown', contextKey) : void 0;
  }
  {
    var name = getComponentName(fiber) || 'Unknown';
    checkPropTypes(childContextTypes, childContext, 'child context', name,
    // In practice, there is one case in which we won't get a stack. It's when
    // somebody calls unstable_renderSubtreeIntoContainer() and we process
    // context from the parent component instance. The stack will be missing
    // because it's outside of the reconciliation, and so the pointer has not
    // been set. This is rare and doesn't matter. We'll also remove that API.
    ReactDebugCurrentFiber.getCurrentFiberStackAddendum);
  }

  return _assign({}, parentContext, childContext);
}

function pushContextProvider(workInProgress) {
  if (!isContextProvider(workInProgress)) {
    return false;
  }

  var instance = workInProgress.stateNode;
  // We push the context as early as possible to ensure stack integrity.
  // If the instance does not exist yet, we will push null at first,
  // and replace it on the stack later when invalidating the context.
  var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyObject;

  // Remember the parent context so we can merge with it later.
  // Inherit the parent's did-perform-work value to avoid inadvertently blocking updates.
  previousContext = contextStackCursor.current;
  push(contextStackCursor, memoizedMergedChildContext, workInProgress);
  push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);

  return true;
}

function invalidateContextProvider(workInProgress, didChange) {
  var instance = workInProgress.stateNode;
  !instance ? invariant(false, 'Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue.') : void 0;

  if (didChange) {
    // Merge parent and own context.
    // Skip this if we're not updating due to sCU.
    // This avoids unnecessarily recomputing memoized values.
    var mergedContext = processChildContext(workInProgress, previousContext);
    instance.__reactInternalMemoizedMergedChildContext = mergedContext;

    // Replace the old (or empty) context with the new one.
    // It is important to unwind the context in the reverse order.
    pop(didPerformWorkStackCursor, workInProgress);
    pop(contextStackCursor, workInProgress);
    // Now push the new context and mark that it has changed.
    push(contextStackCursor, mergedContext, workInProgress);
    push(didPerformWorkStackCursor, didChange, workInProgress);
  } else {
    pop(didPerformWorkStackCursor, workInProgress);
    push(didPerformWorkStackCursor, didChange, workInProgress);
  }
}

function resetContext() {
  previousContext = emptyObject;
  contextStackCursor.current = emptyObject;
  didPerformWorkStackCursor.current = false;
}

function findCurrentUnmaskedContext(fiber) {
  // Currently this is only used with renderSubtreeIntoContainer; not sure if it
  // makes sense elsewhere
  !(isFiberMounted(fiber) && fiber.tag === ClassComponent) ? invariant(false, 'Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue.') : void 0;

  var node = fiber;
  while (node.tag !== HostRoot) {
    if (isContextProvider(node)) {
      return node.stateNode.__reactInternalMemoizedMergedChildContext;
    }
    var parent = node['return'];
    !parent ? invariant(false, 'Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    node = parent;
  }
  return node.stateNode.context;
}

var NoWork = 0; // TODO: Use an opaque type once ESLint et al support the syntax

var Sync = 1;
var Never = 2147483647; // Max int32: Math.pow(2, 31) - 1

var UNIT_SIZE = 10;
var MAGIC_NUMBER_OFFSET = 2;

// 1 unit of expiration time represents 10ms.
function msToExpirationTime(ms) {
  // Always add an offset so that we don't clash with the magic number for NoWork.
  return (ms / UNIT_SIZE | 0) + MAGIC_NUMBER_OFFSET;
}

function expirationTimeToMs(expirationTime) {
  return (expirationTime - MAGIC_NUMBER_OFFSET) * UNIT_SIZE;
}

function ceiling(num, precision) {
  return ((num / precision | 0) + 1) * precision;
}

function computeExpirationBucket(currentTime, expirationInMs, bucketSizeMs) {
  return ceiling(currentTime + expirationInMs / UNIT_SIZE, bucketSizeMs / UNIT_SIZE);
}

var NoContext = 0;
var AsyncUpdates = 1;

{
  var hasBadMapPolyfill = false;
  try {
    var nonExtensibleObject = Object.preventExtensions({});
    /* eslint-disable no-new */
    
    /* eslint-enable no-new */
  } catch (e) {
    // TODO: Consider warning about bad polyfills
    hasBadMapPolyfill = true;
  }
}

// A Fiber is work on a Component that needs to be done or was done. There can
// be more than one per component.


{
  var debugCounter = 1;
}

function FiberNode(tag, key, internalContextTag) {
  // Instance
  this.tag = tag;
  this.key = key;
  this.type = null;
  this.stateNode = null;

  // Fiber
  this['return'] = null;
  this.child = null;
  this.sibling = null;
  this.index = 0;

  this.ref = null;

  this.pendingProps = null;
  this.memoizedProps = null;
  this.updateQueue = null;
  this.memoizedState = null;

  this.internalContextTag = internalContextTag;

  // Effects
  this.effectTag = NoEffect;
  this.nextEffect = null;

  this.firstEffect = null;
  this.lastEffect = null;

  this.expirationTime = NoWork;

  this.alternate = null;

  {
    this._debugID = debugCounter++;
    this._debugSource = null;
    this._debugOwner = null;
    this._debugIsCurrentlyTiming = false;
    if (!hasBadMapPolyfill && typeof Object.preventExtensions === 'function') {
      Object.preventExtensions(this);
    }
  }
}

// This is a constructor function, rather than a POJO constructor, still
// please ensure we do the following:
// 1) Nobody should add any instance methods on this. Instance methods can be
//    more difficult to predict when they get optimized and they are almost
//    never inlined properly in static compilers.
// 2) Nobody should rely on `instanceof Fiber` for type testing. We should
//    always know when it is a fiber.
// 3) We might want to experiment with using numeric keys since they are easier
//    to optimize in a non-JIT environment.
// 4) We can easily go from a constructor to a createFiber object literal if that
//    is faster.
// 5) It should be easy to port this to a C struct and keep a C implementation
//    compatible.
var createFiber = function (tag, key, internalContextTag) {
  // $FlowFixMe: the shapes are exact here but Flow doesn't like constructors
  return new FiberNode(tag, key, internalContextTag);
};

function shouldConstruct(Component) {
  return !!(Component.prototype && Component.prototype.isReactComponent);
}

// This is used to create an alternate fiber to do work on.
function createWorkInProgress(current, pendingProps, expirationTime) {
  var workInProgress = current.alternate;
  if (workInProgress === null) {
    // We use a double buffering pooling technique because we know that we'll
    // only ever need at most two versions of a tree. We pool the "other" unused
    // node that we're free to reuse. This is lazily created to avoid allocating
    // extra objects for things that are never updated. It also allow us to
    // reclaim the extra memory if needed.
    workInProgress = createFiber(current.tag, current.key, current.internalContextTag);
    workInProgress.type = current.type;
    workInProgress.stateNode = current.stateNode;

    {
      // DEV-only fields
      workInProgress._debugID = current._debugID;
      workInProgress._debugSource = current._debugSource;
      workInProgress._debugOwner = current._debugOwner;
    }

    workInProgress.alternate = current;
    current.alternate = workInProgress;
  } else {
    // We already have an alternate.
    // Reset the effect tag.
    workInProgress.effectTag = NoEffect;

    // The effect list is no longer valid.
    workInProgress.nextEffect = null;
    workInProgress.firstEffect = null;
    workInProgress.lastEffect = null;
  }

  workInProgress.expirationTime = expirationTime;
  workInProgress.pendingProps = pendingProps;

  workInProgress.child = current.child;
  workInProgress.memoizedProps = current.memoizedProps;
  workInProgress.memoizedState = current.memoizedState;
  workInProgress.updateQueue = current.updateQueue;

  // These will be overridden during the parent's reconciliation
  workInProgress.sibling = current.sibling;
  workInProgress.index = current.index;
  workInProgress.ref = current.ref;

  return workInProgress;
}

function createHostRootFiber() {
  var fiber = createFiber(HostRoot, null, NoContext);
  return fiber;
}

function createFiberFromElement(element, internalContextTag, expirationTime) {
  var owner = null;
  {
    owner = element._owner;
  }

  var fiber = void 0;
  var type = element.type,
      key = element.key;

  if (typeof type === 'function') {
    fiber = shouldConstruct(type) ? createFiber(ClassComponent, key, internalContextTag) : createFiber(IndeterminateComponent, key, internalContextTag);
    fiber.type = type;
    fiber.pendingProps = element.props;
  } else if (typeof type === 'string') {
    fiber = createFiber(HostComponent, key, internalContextTag);
    fiber.type = type;
    fiber.pendingProps = element.props;
  } else if (typeof type === 'object' && type !== null && typeof type.tag === 'number') {
    // Currently assumed to be a continuation and therefore is a fiber already.
    // TODO: The yield system is currently broken for updates in some cases.
    // The reified yield stores a fiber, but we don't know which fiber that is;
    // the current or a workInProgress? When the continuation gets rendered here
    // we don't know if we can reuse that fiber or if we need to clone it.
    // There is probably a clever way to restructure this.
    fiber = type;
    fiber.pendingProps = element.props;
  } else {
    var info = '';
    {
      if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
        info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
      }
      var ownerName = owner ? getComponentName(owner) : null;
      if (ownerName) {
        info += '\n\nCheck the render method of `' + ownerName + '`.';
      }
    }
    invariant(false, 'Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s', type == null ? type : typeof type, info);
  }

  {
    fiber._debugSource = element._source;
    fiber._debugOwner = element._owner;
  }

  fiber.expirationTime = expirationTime;

  return fiber;
}

function createFiberFromFragment(elements, internalContextTag, expirationTime, key) {
  var fiber = createFiber(Fragment, key, internalContextTag);
  fiber.pendingProps = elements;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromText(content, internalContextTag, expirationTime) {
  var fiber = createFiber(HostText, null, internalContextTag);
  fiber.pendingProps = content;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromHostInstanceForDeletion() {
  var fiber = createFiber(HostComponent, null, NoContext);
  fiber.type = 'DELETED';
  return fiber;
}

function createFiberFromCall(call, internalContextTag, expirationTime) {
  var fiber = createFiber(CallComponent, call.key, internalContextTag);
  fiber.type = call.handler;
  fiber.pendingProps = call;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromReturn(returnNode, internalContextTag, expirationTime) {
  var fiber = createFiber(ReturnComponent, null, internalContextTag);
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromPortal(portal, internalContextTag, expirationTime) {
  var fiber = createFiber(HostPortal, portal.key, internalContextTag);
  fiber.pendingProps = portal.children || [];
  fiber.expirationTime = expirationTime;
  fiber.stateNode = {
    containerInfo: portal.containerInfo,
    pendingChildren: null, // Used by persistent updates
    implementation: portal.implementation
  };
  return fiber;
}

function createFiberRoot(containerInfo, hydrate) {
  // Cyclic construction. This cheats the type system right now because
  // stateNode is any.
  var uninitializedFiber = createHostRootFiber();
  var root = {
    current: uninitializedFiber,
    containerInfo: containerInfo,
    pendingChildren: null,
    remainingExpirationTime: NoWork,
    isReadyForCommit: false,
    finishedWork: null,
    context: null,
    pendingContext: null,
    hydrate: hydrate,
    nextScheduledRoot: null
  };
  uninitializedFiber.stateNode = root;
  return root;
}

var onCommitFiberRoot = null;
var onCommitFiberUnmount = null;
var hasLoggedError = false;

function catchErrors(fn) {
  return function (arg) {
    try {
      return fn(arg);
    } catch (err) {
      if (true && !hasLoggedError) {
        hasLoggedError = true;
        warning(false, 'React DevTools encountered an error: %s', err);
      }
    }
  };
}

function injectInternals(internals) {
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
    // No DevTools
    return false;
  }
  var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
  if (hook.isDisabled) {
    // This isn't a real property on the hook, but it can be set to opt out
    // of DevTools integration and associated warnings and logs.
    // https://github.com/facebook/react/issues/3877
    return true;
  }
  if (!hook.supportsFiber) {
    {
      warning(false, 'The installed version of React DevTools is too old and will not work ' + 'with the current version of React. Please update React DevTools. ' + 'https://fb.me/react-devtools');
    }
    // DevTools exists, even though it doesn't support Fiber.
    return true;
  }
  try {
    var rendererID = hook.inject(internals);
    // We have successfully injected, so now it is safe to set up hooks.
    onCommitFiberRoot = catchErrors(function (root) {
      return hook.onCommitFiberRoot(rendererID, root);
    });
    onCommitFiberUnmount = catchErrors(function (fiber) {
      return hook.onCommitFiberUnmount(rendererID, fiber);
    });
  } catch (err) {
    // Catch all errors because it is unsafe to throw during initialization.
    {
      warning(false, 'React DevTools encountered an error: %s.', err);
    }
  }
  // DevTools exists
  return true;
}

function onCommitRoot(root) {
  if (typeof onCommitFiberRoot === 'function') {
    onCommitFiberRoot(root);
  }
}

function onCommitUnmount(fiber) {
  if (typeof onCommitFiberUnmount === 'function') {
    onCommitFiberUnmount(fiber);
  }
}

{
  var didWarnUpdateInsideUpdate = false;
}

// Callbacks are not validated until invocation


// Singly linked-list of updates. When an update is scheduled, it is added to
// the queue of the current fiber and the work-in-progress fiber. The two queues
// are separate but they share a persistent structure.
//
// During reconciliation, updates are removed from the work-in-progress fiber,
// but they remain on the current fiber. That ensures that if a work-in-progress
// is aborted, the aborted updates are recovered by cloning from current.
//
// The work-in-progress queue is always a subset of the current queue.
//
// When the tree is committed, the work-in-progress becomes the current.


function createUpdateQueue(baseState) {
  var queue = {
    baseState: baseState,
    expirationTime: NoWork,
    first: null,
    last: null,
    callbackList: null,
    hasForceUpdate: false,
    isInitialized: false
  };
  {
    queue.isProcessing = false;
  }
  return queue;
}

function insertUpdateIntoQueue(queue, update) {
  // Append the update to the end of the list.
  if (queue.last === null) {
    // Queue is empty
    queue.first = queue.last = update;
  } else {
    queue.last.next = update;
    queue.last = update;
  }
  if (queue.expirationTime === NoWork || queue.expirationTime > update.expirationTime) {
    queue.expirationTime = update.expirationTime;
  }
}

function insertUpdateIntoFiber(fiber, update) {
  // We'll have at least one and at most two distinct update queues.
  var alternateFiber = fiber.alternate;
  var queue1 = fiber.updateQueue;
  if (queue1 === null) {
    // TODO: We don't know what the base state will be until we begin work.
    // It depends on which fiber is the next current. Initialize with an empty
    // base state, then set to the memoizedState when rendering. Not super
    // happy with this approach.
    queue1 = fiber.updateQueue = createUpdateQueue(null);
  }

  var queue2 = void 0;
  if (alternateFiber !== null) {
    queue2 = alternateFiber.updateQueue;
    if (queue2 === null) {
      queue2 = alternateFiber.updateQueue = createUpdateQueue(null);
    }
  } else {
    queue2 = null;
  }
  queue2 = queue2 !== queue1 ? queue2 : null;

  // Warn if an update is scheduled from inside an updater function.
  {
    if ((queue1.isProcessing || queue2 !== null && queue2.isProcessing) && !didWarnUpdateInsideUpdate) {
      warning(false, 'An update (setState, replaceState, or forceUpdate) was scheduled ' + 'from inside an update function. Update functions should be pure, ' + 'with zero side-effects. Consider using componentDidUpdate or a ' + 'callback.');
      didWarnUpdateInsideUpdate = true;
    }
  }

  // If there's only one queue, add the update to that queue and exit.
  if (queue2 === null) {
    insertUpdateIntoQueue(queue1, update);
    return;
  }

  // If either queue is empty, we need to add to both queues.
  if (queue1.last === null || queue2.last === null) {
    insertUpdateIntoQueue(queue1, update);
    insertUpdateIntoQueue(queue2, update);
    return;
  }

  // If both lists are not empty, the last update is the same for both lists
  // because of structural sharing. So, we should only append to one of
  // the lists.
  insertUpdateIntoQueue(queue1, update);
  // But we still need to update the `last` pointer of queue2.
  queue2.last = update;
}

function getUpdateExpirationTime(fiber) {
  if (fiber.tag !== ClassComponent && fiber.tag !== HostRoot) {
    return NoWork;
  }
  var updateQueue = fiber.updateQueue;
  if (updateQueue === null) {
    return NoWork;
  }
  return updateQueue.expirationTime;
}

function getStateFromUpdate(update, instance, prevState, props) {
  var partialState = update.partialState;
  if (typeof partialState === 'function') {
    var updateFn = partialState;

    // Invoke setState callback an extra time to help detect side-effects.
    if (debugRenderPhaseSideEffects) {
      updateFn.call(instance, prevState, props);
    }

    return updateFn.call(instance, prevState, props);
  } else {
    return partialState;
  }
}

function processUpdateQueue(current, workInProgress, queue, instance, props, renderExpirationTime) {
  if (current !== null && current.updateQueue === queue) {
    // We need to create a work-in-progress queue, by cloning the current queue.
    var currentQueue = queue;
    queue = workInProgress.updateQueue = {
      baseState: currentQueue.baseState,
      expirationTime: currentQueue.expirationTime,
      first: currentQueue.first,
      last: currentQueue.last,
      isInitialized: currentQueue.isInitialized,
      // These fields are no longer valid because they were already committed.
      // Reset them.
      callbackList: null,
      hasForceUpdate: false
    };
  }

  {
    // Set this flag so we can warn if setState is called inside the update
    // function of another setState.
    queue.isProcessing = true;
  }

  // Reset the remaining expiration time. If we skip over any updates, we'll
  // increase this accordingly.
  queue.expirationTime = NoWork;

  // TODO: We don't know what the base state will be until we begin work.
  // It depends on which fiber is the next current. Initialize with an empty
  // base state, then set to the memoizedState when rendering. Not super
  // happy with this approach.
  var state = void 0;
  if (queue.isInitialized) {
    state = queue.baseState;
  } else {
    state = queue.baseState = workInProgress.memoizedState;
    queue.isInitialized = true;
  }
  var dontMutatePrevState = true;
  var update = queue.first;
  var didSkip = false;
  while (update !== null) {
    var updateExpirationTime = update.expirationTime;
    if (updateExpirationTime > renderExpirationTime) {
      // This update does not have sufficient priority. Skip it.
      var remainingExpirationTime = queue.expirationTime;
      if (remainingExpirationTime === NoWork || remainingExpirationTime > updateExpirationTime) {
        // Update the remaining expiration time.
        queue.expirationTime = updateExpirationTime;
      }
      if (!didSkip) {
        didSkip = true;
        queue.baseState = state;
      }
      // Continue to the next update.
      update = update.next;
      continue;
    }

    // This update does have sufficient priority.

    // If no previous updates were skipped, drop this update from the queue by
    // advancing the head of the list.
    if (!didSkip) {
      queue.first = update.next;
      if (queue.first === null) {
        queue.last = null;
      }
    }

    // Process the update
    var _partialState = void 0;
    if (update.isReplace) {
      state = getStateFromUpdate(update, instance, state, props);
      dontMutatePrevState = true;
    } else {
      _partialState = getStateFromUpdate(update, instance, state, props);
      if (_partialState) {
        if (dontMutatePrevState) {
          // $FlowFixMe: Idk how to type this properly.
          state = _assign({}, state, _partialState);
        } else {
          state = _assign(state, _partialState);
        }
        dontMutatePrevState = false;
      }
    }
    if (update.isForced) {
      queue.hasForceUpdate = true;
    }
    if (update.callback !== null) {
      // Append to list of callbacks.
      var _callbackList = queue.callbackList;
      if (_callbackList === null) {
        _callbackList = queue.callbackList = [];
      }
      _callbackList.push(update);
    }
    update = update.next;
  }

  if (queue.callbackList !== null) {
    workInProgress.effectTag |= Callback;
  } else if (queue.first === null && !queue.hasForceUpdate) {
    // The queue is empty. We can reset it.
    workInProgress.updateQueue = null;
  }

  if (!didSkip) {
    didSkip = true;
    queue.baseState = state;
  }

  {
    // No longer processing.
    queue.isProcessing = false;
  }

  return state;
}

function commitCallbacks(queue, context) {
  var callbackList = queue.callbackList;
  if (callbackList === null) {
    return;
  }
  // Set the list to null to make sure they don't get called more than once.
  queue.callbackList = null;
  for (var i = 0; i < callbackList.length; i++) {
    var update = callbackList[i];
    var _callback = update.callback;
    // This update might be processed again. Clear the callback so it's only
    // called once.
    update.callback = null;
    !(typeof _callback === 'function') ? invariant(false, 'Invalid argument passed as callback. Expected a function. Instead received: %s', _callback) : void 0;
    _callback.call(context);
  }
}

var fakeInternalInstance = {};
var isArray = Array.isArray;

{
  var didWarnAboutStateAssignmentForComponent = {};

  var warnOnInvalidCallback = function (callback, callerName) {
    warning(callback === null || typeof callback === 'function', '%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback);
  };

  // This is so gross but it's at least non-critical and can be removed if
  // it causes problems. This is meant to give a nicer error message for
  // ReactDOM15.unstable_renderSubtreeIntoContainer(reactDOM16Component,
  // ...)) which otherwise throws a "_processChildContext is not a function"
  // exception.
  Object.defineProperty(fakeInternalInstance, '_processChildContext', {
    enumerable: false,
    value: function () {
      invariant(false, '_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn\'t supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal).');
    }
  });
  Object.freeze(fakeInternalInstance);
}

var ReactFiberClassComponent = function (scheduleWork, computeExpirationForFiber, memoizeProps, memoizeState) {
  // Class component state updater
  var updater = {
    isMounted: isMounted,
    enqueueSetState: function (instance, partialState, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'setState');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: partialState,
        callback: callback,
        isReplace: false,
        isForced: false,
        nextCallback: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    },
    enqueueReplaceState: function (instance, state, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'replaceState');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: state,
        callback: callback,
        isReplace: true,
        isForced: false,
        nextCallback: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    },
    enqueueForceUpdate: function (instance, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'forceUpdate');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: null,
        callback: callback,
        isReplace: false,
        isForced: true,
        nextCallback: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    }
  };

  function checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext) {
    if (oldProps === null || workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate) {
      // If the workInProgress already has an Update effect, return true
      return true;
    }

    var instance = workInProgress.stateNode;
    var type = workInProgress.type;
    if (typeof instance.shouldComponentUpdate === 'function') {
      startPhaseTimer(workInProgress, 'shouldComponentUpdate');
      var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, newContext);
      stopPhaseTimer();

      // Simulate an async bailout/interruption by invoking lifecycle twice.
      if (debugRenderPhaseSideEffects) {
        instance.shouldComponentUpdate(newProps, newState, newContext);
      }

      {
        warning(shouldUpdate !== undefined, '%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', getComponentName(workInProgress) || 'Unknown');
      }

      return shouldUpdate;
    }

    if (type.prototype && type.prototype.isPureReactComponent) {
      return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
    }

    return true;
  }

  function checkClassInstance(workInProgress) {
    var instance = workInProgress.stateNode;
    var type = workInProgress.type;
    {
      var name = getComponentName(workInProgress);
      var renderPresent = instance.render;

      if (!renderPresent) {
        if (type.prototype && typeof type.prototype.render === 'function') {
          warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: did you accidentally return an object from the constructor?', name);
        } else {
          warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`.', name);
        }
      }

      var noGetInitialStateOnES6 = !instance.getInitialState || instance.getInitialState.isReactClassApproved || instance.state;
      warning(noGetInitialStateOnES6, 'getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', name);
      var noGetDefaultPropsOnES6 = !instance.getDefaultProps || instance.getDefaultProps.isReactClassApproved;
      warning(noGetDefaultPropsOnES6, 'getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', name);
      var noInstancePropTypes = !instance.propTypes;
      warning(noInstancePropTypes, 'propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', name);
      var noInstanceContextTypes = !instance.contextTypes;
      warning(noInstanceContextTypes, 'contextTypes was defined as an instance property on %s. Use a static ' + 'property to define contextTypes instead.', name);
      var noComponentShouldUpdate = typeof instance.componentShouldUpdate !== 'function';
      warning(noComponentShouldUpdate, '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', name);
      if (type.prototype && type.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== 'undefined') {
        warning(false, '%s has a method called shouldComponentUpdate(). ' + 'shouldComponentUpdate should not be used when extending React.PureComponent. ' + 'Please extend React.Component if shouldComponentUpdate is used.', getComponentName(workInProgress) || 'A pure component');
      }
      var noComponentDidUnmount = typeof instance.componentDidUnmount !== 'function';
      warning(noComponentDidUnmount, '%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', name);
      var noComponentDidReceiveProps = typeof instance.componentDidReceiveProps !== 'function';
      warning(noComponentDidReceiveProps, '%s has a method called ' + 'componentDidReceiveProps(). But there is no such lifecycle method. ' + 'If you meant to update the state in response to changing props, ' + 'use componentWillReceiveProps(). If you meant to fetch data or ' + 'run side-effects or mutations after React has updated the UI, use componentDidUpdate().', name);
      var noComponentWillRecieveProps = typeof instance.componentWillRecieveProps !== 'function';
      warning(noComponentWillRecieveProps, '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', name);
      var hasMutatedProps = instance.props !== workInProgress.pendingProps;
      warning(instance.props === undefined || !hasMutatedProps, '%s(...): When calling super() in `%s`, make sure to pass ' + "up the same props that your component's constructor was passed.", name, name);
      var noInstanceDefaultProps = !instance.defaultProps;
      warning(noInstanceDefaultProps, 'Setting defaultProps as an instance property on %s is not supported and will be ignored.' + ' Instead, define defaultProps as a static property on %s.', name, name);
    }

    var state = instance.state;
    if (state && (typeof state !== 'object' || isArray(state))) {
      warning(false, '%s.state: must be set to an object or null', getComponentName(workInProgress));
    }
    if (typeof instance.getChildContext === 'function') {
      warning(typeof workInProgress.type.childContextTypes === 'object', '%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', getComponentName(workInProgress));
    }
  }

  function resetInputPointers(workInProgress, instance) {
    instance.props = workInProgress.memoizedProps;
    instance.state = workInProgress.memoizedState;
  }

  function adoptClassInstance(workInProgress, instance) {
    instance.updater = updater;
    workInProgress.stateNode = instance;
    // The instance needs access to the fiber so that it can schedule updates
    set(instance, workInProgress);
    {
      instance._reactInternalInstance = fakeInternalInstance;
    }
  }

  function constructClassInstance(workInProgress, props) {
    var ctor = workInProgress.type;
    var unmaskedContext = getUnmaskedContext(workInProgress);
    var needsContext = isContextConsumer(workInProgress);
    var context = needsContext ? getMaskedContext(workInProgress, unmaskedContext) : emptyObject;
    var instance = new ctor(props, context);
    adoptClassInstance(workInProgress, instance);

    // Cache unmasked context so we can avoid recreating masked context unless necessary.
    // ReactFiberContext usually updates this cache but can't for newly-created instances.
    if (needsContext) {
      cacheContext(workInProgress, unmaskedContext, context);
    }

    return instance;
  }

  function callComponentWillMount(workInProgress, instance) {
    startPhaseTimer(workInProgress, 'componentWillMount');
    var oldState = instance.state;
    instance.componentWillMount();
    stopPhaseTimer();

    // Simulate an async bailout/interruption by invoking lifecycle twice.
    if (debugRenderPhaseSideEffects) {
      instance.componentWillMount();
    }

    if (oldState !== instance.state) {
      {
        warning(false, '%s.componentWillMount(): Assigning directly to this.state is ' + "deprecated (except inside a component's " + 'constructor). Use setState instead.', getComponentName(workInProgress));
      }
      updater.enqueueReplaceState(instance, instance.state, null);
    }
  }

  function callComponentWillReceiveProps(workInProgress, instance, newProps, newContext) {
    startPhaseTimer(workInProgress, 'componentWillReceiveProps');
    var oldState = instance.state;
    instance.componentWillReceiveProps(newProps, newContext);
    stopPhaseTimer();

    // Simulate an async bailout/interruption by invoking lifecycle twice.
    if (debugRenderPhaseSideEffects) {
      instance.componentWillReceiveProps(newProps, newContext);
    }

    if (instance.state !== oldState) {
      {
        var componentName = getComponentName(workInProgress) || 'Component';
        if (!didWarnAboutStateAssignmentForComponent[componentName]) {
          warning(false, '%s.componentWillReceiveProps(): Assigning directly to ' + "this.state is deprecated (except inside a component's " + 'constructor). Use setState instead.', componentName);
          didWarnAboutStateAssignmentForComponent[componentName] = true;
        }
      }
      updater.enqueueReplaceState(instance, instance.state, null);
    }
  }

  // Invokes the mount life-cycles on a previously never rendered instance.
  function mountClassInstance(workInProgress, renderExpirationTime) {
    var current = workInProgress.alternate;

    {
      checkClassInstance(workInProgress);
    }

    var instance = workInProgress.stateNode;
    var state = instance.state || null;

    var props = workInProgress.pendingProps;
    !props ? invariant(false, 'There must be pending props for an initial mount. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    var unmaskedContext = getUnmaskedContext(workInProgress);

    instance.props = props;
    instance.state = workInProgress.memoizedState = state;
    instance.refs = emptyObject;
    instance.context = getMaskedContext(workInProgress, unmaskedContext);

    if (enableAsyncSubtreeAPI && workInProgress.type != null && workInProgress.type.prototype != null && workInProgress.type.prototype.unstable_isAsyncReactComponent === true) {
      workInProgress.internalContextTag |= AsyncUpdates;
    }

    if (typeof instance.componentWillMount === 'function') {
      callComponentWillMount(workInProgress, instance);
      // If we had additional state updates during this life-cycle, let's
      // process them now.
      var updateQueue = workInProgress.updateQueue;
      if (updateQueue !== null) {
        instance.state = processUpdateQueue(current, workInProgress, updateQueue, instance, props, renderExpirationTime);
      }
    }
    if (typeof instance.componentDidMount === 'function') {
      workInProgress.effectTag |= Update;
    }
  }

  // Called on a preexisting class instance. Returns false if a resumed render
  // could be reused.
  // function resumeMountClassInstance(
  //   workInProgress: Fiber,
  //   priorityLevel: PriorityLevel,
  // ): boolean {
  //   const instance = workInProgress.stateNode;
  //   resetInputPointers(workInProgress, instance);

  //   let newState = workInProgress.memoizedState;
  //   let newProps = workInProgress.pendingProps;
  //   if (!newProps) {
  //     // If there isn't any new props, then we'll reuse the memoized props.
  //     // This could be from already completed work.
  //     newProps = workInProgress.memoizedProps;
  //     invariant(
  //       newProps != null,
  //       'There should always be pending or memoized props. This error is ' +
  //         'likely caused by a bug in React. Please file an issue.',
  //     );
  //   }
  //   const newUnmaskedContext = getUnmaskedContext(workInProgress);
  //   const newContext = getMaskedContext(workInProgress, newUnmaskedContext);

  //   const oldContext = instance.context;
  //   const oldProps = workInProgress.memoizedProps;

  //   if (
  //     typeof instance.componentWillReceiveProps === 'function' &&
  //     (oldProps !== newProps || oldContext !== newContext)
  //   ) {
  //     callComponentWillReceiveProps(
  //       workInProgress,
  //       instance,
  //       newProps,
  //       newContext,
  //     );
  //   }

  //   // Process the update queue before calling shouldComponentUpdate
  //   const updateQueue = workInProgress.updateQueue;
  //   if (updateQueue !== null) {
  //     newState = processUpdateQueue(
  //       workInProgress,
  //       updateQueue,
  //       instance,
  //       newState,
  //       newProps,
  //       priorityLevel,
  //     );
  //   }

  //   // TODO: Should we deal with a setState that happened after the last
  //   // componentWillMount and before this componentWillMount? Probably
  //   // unsupported anyway.

  //   if (
  //     !checkShouldComponentUpdate(
  //       workInProgress,
  //       workInProgress.memoizedProps,
  //       newProps,
  //       workInProgress.memoizedState,
  //       newState,
  //       newContext,
  //     )
  //   ) {
  //     // Update the existing instance's state, props, and context pointers even
  //     // though we're bailing out.
  //     instance.props = newProps;
  //     instance.state = newState;
  //     instance.context = newContext;
  //     return false;
  //   }

  //   // Update the input pointers now so that they are correct when we call
  //   // componentWillMount
  //   instance.props = newProps;
  //   instance.state = newState;
  //   instance.context = newContext;

  //   if (typeof instance.componentWillMount === 'function') {
  //     callComponentWillMount(workInProgress, instance);
  //     // componentWillMount may have called setState. Process the update queue.
  //     const newUpdateQueue = workInProgress.updateQueue;
  //     if (newUpdateQueue !== null) {
  //       newState = processUpdateQueue(
  //         workInProgress,
  //         newUpdateQueue,
  //         instance,
  //         newState,
  //         newProps,
  //         priorityLevel,
  //       );
  //     }
  //   }

  //   if (typeof instance.componentDidMount === 'function') {
  //     workInProgress.effectTag |= Update;
  //   }

  //   instance.state = newState;

  //   return true;
  // }

  // Invokes the update life-cycles and returns false if it shouldn't rerender.
  function updateClassInstance(current, workInProgress, renderExpirationTime) {
    var instance = workInProgress.stateNode;
    resetInputPointers(workInProgress, instance);

    var oldProps = workInProgress.memoizedProps;
    var newProps = workInProgress.pendingProps;
    if (!newProps) {
      // If there aren't any new props, then we'll reuse the memoized props.
      // This could be from already completed work.
      newProps = oldProps;
      !(newProps != null) ? invariant(false, 'There should always be pending or memoized props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    }
    var oldContext = instance.context;
    var newUnmaskedContext = getUnmaskedContext(workInProgress);
    var newContext = getMaskedContext(workInProgress, newUnmaskedContext);

    // Note: During these life-cycles, instance.props/instance.state are what
    // ever the previously attempted to render - not the "current". However,
    // during componentDidUpdate we pass the "current" props.

    if (typeof instance.componentWillReceiveProps === 'function' && (oldProps !== newProps || oldContext !== newContext)) {
      callComponentWillReceiveProps(workInProgress, instance, newProps, newContext);
    }

    // Compute the next state using the memoized state and the update queue.
    var oldState = workInProgress.memoizedState;
    // TODO: Previous state can be null.
    var newState = void 0;
    if (workInProgress.updateQueue !== null) {
      newState = processUpdateQueue(current, workInProgress, workInProgress.updateQueue, instance, newProps, renderExpirationTime);
    } else {
      newState = oldState;
    }

    if (oldProps === newProps && oldState === newState && !hasContextChanged() && !(workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate)) {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Update;
        }
      }
      return false;
    }

    var shouldUpdate = checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext);

    if (shouldUpdate) {
      if (typeof instance.componentWillUpdate === 'function') {
        startPhaseTimer(workInProgress, 'componentWillUpdate');
        instance.componentWillUpdate(newProps, newState, newContext);
        stopPhaseTimer();

        // Simulate an async bailout/interruption by invoking lifecycle twice.
        if (debugRenderPhaseSideEffects) {
          instance.componentWillUpdate(newProps, newState, newContext);
        }
      }
      if (typeof instance.componentDidUpdate === 'function') {
        workInProgress.effectTag |= Update;
      }
    } else {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Update;
        }
      }

      // If shouldComponentUpdate returned false, we should still update the
      // memoized props/state to indicate that this work can be reused.
      memoizeProps(workInProgress, newProps);
      memoizeState(workInProgress, newState);
    }

    // Update the existing instance's state, props, and context pointers even
    // if shouldComponentUpdate returns false.
    instance.props = newProps;
    instance.state = newState;
    instance.context = newContext;

    return shouldUpdate;
  }

  return {
    adoptClassInstance: adoptClassInstance,
    constructClassInstance: constructClassInstance,
    mountClassInstance: mountClassInstance,
    // resumeMountClassInstance,
    updateClassInstance: updateClassInstance
  };
};

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol['for'];

var REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;
var REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;
var REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;

var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';

function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable === 'undefined') {
    return null;
  }
  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }
  return null;
}

var getCurrentFiberStackAddendum$1 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;


{
  var didWarnAboutMaps = false;
  /**
   * Warn if there's no key explicitly set on dynamic arrays of children or
   * object keys are not valid. This allows us to keep track of children between
   * updates.
   */
  var ownerHasKeyUseWarning = {};
  var ownerHasFunctionTypeWarning = {};

  var warnForMissingKey = function (child) {
    if (child === null || typeof child !== 'object') {
      return;
    }
    if (!child._store || child._store.validated || child.key != null) {
      return;
    }
    !(typeof child._store === 'object') ? invariant(false, 'React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    child._store.validated = true;

    var currentComponentErrorInfo = 'Each child in an array or iterator should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.' + (getCurrentFiberStackAddendum$1() || '');
    if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
      return;
    }
    ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

    warning(false, 'Each child in an array or iterator should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.%s', getCurrentFiberStackAddendum$1());
  };
}

var isArray$1 = Array.isArray;

function coerceRef(current, element) {
  var mixedRef = element.ref;
  if (mixedRef !== null && typeof mixedRef !== 'function') {
    if (element._owner) {
      var owner = element._owner;
      var inst = void 0;
      if (owner) {
        var ownerFiber = owner;
        !(ownerFiber.tag === ClassComponent) ? invariant(false, 'Stateless function components cannot have refs.') : void 0;
        inst = ownerFiber.stateNode;
      }
      !inst ? invariant(false, 'Missing owner for string ref %s. This error is likely caused by a bug in React. Please file an issue.', mixedRef) : void 0;
      var stringRef = '' + mixedRef;
      // Check if previous string ref matches new string ref
      if (current !== null && current.ref !== null && current.ref._stringRef === stringRef) {
        return current.ref;
      }
      var ref = function (value) {
        var refs = inst.refs === emptyObject ? inst.refs = {} : inst.refs;
        if (value === null) {
          delete refs[stringRef];
        } else {
          refs[stringRef] = value;
        }
      };
      ref._stringRef = stringRef;
      return ref;
    } else {
      !(typeof mixedRef === 'string') ? invariant(false, 'Expected ref to be a function or a string.') : void 0;
      !element._owner ? invariant(false, 'Element ref was specified as a string (%s) but no owner was set. You may have multiple copies of React loaded. (details: https://fb.me/react-refs-must-have-owner).', mixedRef) : void 0;
    }
  }
  return mixedRef;
}

function throwOnInvalidObjectType(returnFiber, newChild) {
  if (returnFiber.type !== 'textarea') {
    var addendum = '';
    {
      addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + (getCurrentFiberStackAddendum$1() || '');
    }
    invariant(false, 'Objects are not valid as a React child (found: %s).%s', Object.prototype.toString.call(newChild) === '[object Object]' ? 'object with keys {' + Object.keys(newChild).join(', ') + '}' : newChild, addendum);
  }
}

function warnOnFunctionType() {
  var currentComponentErrorInfo = 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.' + (getCurrentFiberStackAddendum$1() || '');

  if (ownerHasFunctionTypeWarning[currentComponentErrorInfo]) {
    return;
  }
  ownerHasFunctionTypeWarning[currentComponentErrorInfo] = true;

  warning(false, 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.%s', getCurrentFiberStackAddendum$1() || '');
}

// This wrapper function exists because I expect to clone the code in each path
// to be able to optimize each path individually by branching early. This needs
// a compiler or we can do it manually. Helpers that don't need this branching
// live outside of this function.
function ChildReconciler(shouldTrackSideEffects) {
  function deleteChild(returnFiber, childToDelete) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return;
    }
    // Deletions are added in reversed order so we add it to the front.
    // At this point, the return fiber's effect list is empty except for
    // deletions, so we can just append the deletion to the list. The remaining
    // effects aren't added until the complete phase. Once we implement
    // resuming, this may not be true.
    var last = returnFiber.lastEffect;
    if (last !== null) {
      last.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }
    childToDelete.nextEffect = null;
    childToDelete.effectTag = Deletion;
  }

  function deleteRemainingChildren(returnFiber, currentFirstChild) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return null;
    }

    // TODO: For the shouldClone case, this could be micro-optimized a bit by
    // assuming that after the first child we've already added everything.
    var childToDelete = currentFirstChild;
    while (childToDelete !== null) {
      deleteChild(returnFiber, childToDelete);
      childToDelete = childToDelete.sibling;
    }
    return null;
  }

  function mapRemainingChildren(returnFiber, currentFirstChild) {
    // Add the remaining children to a temporary map so that we can find them by
    // keys quickly. Implicit (null) keys get added to this set with their index
    var existingChildren = new Map();

    var existingChild = currentFirstChild;
    while (existingChild !== null) {
      if (existingChild.key !== null) {
        existingChildren.set(existingChild.key, existingChild);
      } else {
        existingChildren.set(existingChild.index, existingChild);
      }
      existingChild = existingChild.sibling;
    }
    return existingChildren;
  }

  function useFiber(fiber, pendingProps, expirationTime) {
    // We currently set sibling to null and index to 0 here because it is easy
    // to forget to do before returning it. E.g. for the single child case.
    var clone = createWorkInProgress(fiber, pendingProps, expirationTime);
    clone.index = 0;
    clone.sibling = null;
    return clone;
  }

  function placeChild(newFiber, lastPlacedIndex, newIndex) {
    newFiber.index = newIndex;
    if (!shouldTrackSideEffects) {
      // Noop.
      return lastPlacedIndex;
    }
    var current = newFiber.alternate;
    if (current !== null) {
      var oldIndex = current.index;
      if (oldIndex < lastPlacedIndex) {
        // This is a move.
        newFiber.effectTag = Placement;
        return lastPlacedIndex;
      } else {
        // This item can stay in place.
        return oldIndex;
      }
    } else {
      // This is an insertion.
      newFiber.effectTag = Placement;
      return lastPlacedIndex;
    }
  }

  function placeSingleChild(newFiber) {
    // This is simpler for the single child case. We only need to do a
    // placement for inserting new children.
    if (shouldTrackSideEffects && newFiber.alternate === null) {
      newFiber.effectTag = Placement;
    }
    return newFiber;
  }

  function updateTextNode(returnFiber, current, textContent, expirationTime) {
    if (current === null || current.tag !== HostText) {
      // Insert
      var created = createFiberFromText(textContent, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, textContent, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateElement(returnFiber, current, element, expirationTime) {
    if (current !== null && current.type === element.type) {
      // Move based on index
      var existing = useFiber(current, element.props, expirationTime);
      existing.ref = coerceRef(current, element);
      existing['return'] = returnFiber;
      {
        existing._debugSource = element._source;
        existing._debugOwner = element._owner;
      }
      return existing;
    } else {
      // Insert
      var created = createFiberFromElement(element, returnFiber.internalContextTag, expirationTime);
      created.ref = coerceRef(current, element);
      created['return'] = returnFiber;
      return created;
    }
  }

  function updateCall(returnFiber, current, call, expirationTime) {
    // TODO: Should this also compare handler to determine whether to reuse?
    if (current === null || current.tag !== CallComponent) {
      // Insert
      var created = createFiberFromCall(call, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Move based on index
      var existing = useFiber(current, call, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateReturn(returnFiber, current, returnNode, expirationTime) {
    if (current === null || current.tag !== ReturnComponent) {
      // Insert
      var created = createFiberFromReturn(returnNode, returnFiber.internalContextTag, expirationTime);
      created.type = returnNode.value;
      created['return'] = returnFiber;
      return created;
    } else {
      // Move based on index
      var existing = useFiber(current, null, expirationTime);
      existing.type = returnNode.value;
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updatePortal(returnFiber, current, portal, expirationTime) {
    if (current === null || current.tag !== HostPortal || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) {
      // Insert
      var created = createFiberFromPortal(portal, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, portal.children || [], expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateFragment(returnFiber, current, fragment, expirationTime, key) {
    if (current === null || current.tag !== Fragment) {
      // Insert
      var created = createFiberFromFragment(fragment, returnFiber.internalContextTag, expirationTime, key);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, fragment, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function createChild(returnFiber, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      var created = createFiberFromText('' + newChild, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            if (newChild.type === REACT_FRAGMENT_TYPE) {
              var _created = createFiberFromFragment(newChild.props.children, returnFiber.internalContextTag, expirationTime, newChild.key);
              _created['return'] = returnFiber;
              return _created;
            } else {
              var _created2 = createFiberFromElement(newChild, returnFiber.internalContextTag, expirationTime);
              _created2.ref = coerceRef(null, newChild);
              _created2['return'] = returnFiber;
              return _created2;
            }
          }

        case REACT_CALL_TYPE:
          {
            var _created3 = createFiberFromCall(newChild, returnFiber.internalContextTag, expirationTime);
            _created3['return'] = returnFiber;
            return _created3;
          }

        case REACT_RETURN_TYPE:
          {
            var _created4 = createFiberFromReturn(newChild, returnFiber.internalContextTag, expirationTime);
            _created4.type = newChild.value;
            _created4['return'] = returnFiber;
            return _created4;
          }

        case REACT_PORTAL_TYPE:
          {
            var _created5 = createFiberFromPortal(newChild, returnFiber.internalContextTag, expirationTime);
            _created5['return'] = returnFiber;
            return _created5;
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _created6 = createFiberFromFragment(newChild, returnFiber.internalContextTag, expirationTime, null);
        _created6['return'] = returnFiber;
        return _created6;
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateSlot(returnFiber, oldFiber, newChild, expirationTime) {
    // Update the fiber if the keys match, otherwise return null.

    var key = oldFiber !== null ? oldFiber.key : null;

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      if (key !== null) {
        return null;
      }
      return updateTextNode(returnFiber, oldFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            if (newChild.key === key) {
              if (newChild.type === REACT_FRAGMENT_TYPE) {
                return updateFragment(returnFiber, oldFiber, newChild.props.children, expirationTime, key);
              }
              return updateElement(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_CALL_TYPE:
          {
            if (newChild.key === key) {
              return updateCall(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_RETURN_TYPE:
          {
            // Returns don't have keys. If the previous node is implicitly keyed
            // we can continue to replace it without aborting even if it is not a
            // yield.
            if (key === null) {
              return updateReturn(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_PORTAL_TYPE:
          {
            if (newChild.key === key) {
              return updatePortal(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        if (key !== null) {
          return null;
        }

        return updateFragment(returnFiber, oldFiber, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateFromMap(existingChildren, returnFiber, newIdx, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys, so we neither have to check the old nor
      // new node for the key. If both are text nodes, they match.
      var matchedFiber = existingChildren.get(newIdx) || null;
      return updateTextNode(returnFiber, matchedFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            if (newChild.type === REACT_FRAGMENT_TYPE) {
              return updateFragment(returnFiber, _matchedFiber, newChild.props.children, expirationTime, newChild.key);
            }
            return updateElement(returnFiber, _matchedFiber, newChild, expirationTime);
          }

        case REACT_CALL_TYPE:
          {
            var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            return updateCall(returnFiber, _matchedFiber2, newChild, expirationTime);
          }

        case REACT_RETURN_TYPE:
          {
            // Returns don't have keys, so we neither have to check the old nor
            // new node for the key. If both are returns, they match.
            var _matchedFiber3 = existingChildren.get(newIdx) || null;
            return updateReturn(returnFiber, _matchedFiber3, newChild, expirationTime);
          }

        case REACT_PORTAL_TYPE:
          {
            var _matchedFiber4 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            return updatePortal(returnFiber, _matchedFiber4, newChild, expirationTime);
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _matchedFiber5 = existingChildren.get(newIdx) || null;
        return updateFragment(returnFiber, _matchedFiber5, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  /**
   * Warns if there is a duplicate or missing key
   */
  function warnOnInvalidKey(child, knownKeys) {
    {
      if (typeof child !== 'object' || child === null) {
        return knownKeys;
      }
      switch (child.$$typeof) {
        case REACT_ELEMENT_TYPE:
        case REACT_CALL_TYPE:
        case REACT_PORTAL_TYPE:
          warnForMissingKey(child);
          var key = child.key;
          if (typeof key !== 'string') {
            break;
          }
          if (knownKeys === null) {
            knownKeys = new Set();
            knownKeys.add(key);
            break;
          }
          if (!knownKeys.has(key)) {
            knownKeys.add(key);
            break;
          }
          warning(false, 'Encountered two children with the same key, `%s`. ' + 'Keys should be unique so that components maintain their identity ' + 'across updates. Non-unique keys may cause children to be ' + 'duplicated and/or omitted — the behavior is unsupported and ' + 'could change in a future version.%s', key, getCurrentFiberStackAddendum$1());
          break;
        default:
          break;
      }
    }
    return knownKeys;
  }

  function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, expirationTime) {
    // This algorithm can't optimize by searching from boths ends since we
    // don't have backpointers on fibers. I'm trying to see how far we can get
    // with that model. If it ends up not being worth the tradeoffs, we can
    // add it later.

    // Even with a two ended optimization, we'd want to optimize for the case
    // where there are few changes and brute force the comparison instead of
    // going for the Map. It'd like to explore hitting that path first in
    // forward-only mode and only go for the Map once we notice that we need
    // lots of look ahead. This doesn't handle reversal as well as two ended
    // search but that's unusual. Besides, for the two ended optimization to
    // work on Iterables, we'd need to copy the whole set.

    // In this first iteration, we'll just live with hitting the bad case
    // (adding everything to a Map) in for every insert/move.

    // If you change this code, also update reconcileChildrenIterator() which
    // uses the same algorithm.

    {
      // First, validate keys.
      var knownKeys = null;
      for (var i = 0; i < newChildren.length; i++) {
        var child = newChildren[i];
        knownKeys = warnOnInvalidKey(child, knownKeys);
      }
    }

    var resultingFirstChild = null;
    var previousNewFiber = null;

    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;
    for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }
      var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], expirationTime);
      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (oldFiber === null) {
          oldFiber = nextOldFiber;
        }
        break;
      }
      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }
      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }
      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (newIdx === newChildren.length) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; newIdx < newChildren.length; newIdx++) {
        var _newFiber = createChild(returnFiber, newChildren[newIdx], expirationTime);
        if (!_newFiber) {
          continue;
        }
        lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber;
        } else {
          previousNewFiber.sibling = _newFiber;
        }
        previousNewFiber = _newFiber;
      }
      return resultingFirstChild;
    }

    // Add all children to a key map for quick lookups.
    var existingChildren = mapRemainingChildren(returnFiber, oldFiber);

    // Keep scanning and use the map to restore deleted items as moves.
    for (; newIdx < newChildren.length; newIdx++) {
      var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], expirationTime);
      if (_newFiber2) {
        if (shouldTrackSideEffects) {
          if (_newFiber2.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren['delete'](_newFiber2.key === null ? newIdx : _newFiber2.key);
          }
        }
        lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber2;
        } else {
          previousNewFiber.sibling = _newFiber2;
        }
        previousNewFiber = _newFiber2;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, expirationTime) {
    // This is the same implementation as reconcileChildrenArray(),
    // but using the iterator instead.

    var iteratorFn = getIteratorFn(newChildrenIterable);
    !(typeof iteratorFn === 'function') ? invariant(false, 'An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    {
      // Warn about using Maps as children
      if (typeof newChildrenIterable.entries === 'function') {
        var possibleMap = newChildrenIterable;
        if (possibleMap.entries === iteratorFn) {
          warning(didWarnAboutMaps, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', getCurrentFiberStackAddendum$1());
          didWarnAboutMaps = true;
        }
      }

      // First, validate keys.
      // We'll get a different iterator later for the main pass.
      var _newChildren = iteratorFn.call(newChildrenIterable);
      if (_newChildren) {
        var knownKeys = null;
        var _step = _newChildren.next();
        for (; !_step.done; _step = _newChildren.next()) {
          var child = _step.value;
          knownKeys = warnOnInvalidKey(child, knownKeys);
        }
      }
    }

    var newChildren = iteratorFn.call(newChildrenIterable);
    !(newChildren != null) ? invariant(false, 'An iterable object provided no iterator.') : void 0;

    var resultingFirstChild = null;
    var previousNewFiber = null;

    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;

    var step = newChildren.next();
    for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }
      var newFiber = updateSlot(returnFiber, oldFiber, step.value, expirationTime);
      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (!oldFiber) {
          oldFiber = nextOldFiber;
        }
        break;
      }
      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }
      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }
      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (step.done) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; !step.done; newIdx++, step = newChildren.next()) {
        var _newFiber3 = createChild(returnFiber, step.value, expirationTime);
        if (_newFiber3 === null) {
          continue;
        }
        lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber3;
        } else {
          previousNewFiber.sibling = _newFiber3;
        }
        previousNewFiber = _newFiber3;
      }
      return resultingFirstChild;
    }

    // Add all children to a key map for quick lookups.
    var existingChildren = mapRemainingChildren(returnFiber, oldFiber);

    // Keep scanning and use the map to restore deleted items as moves.
    for (; !step.done; newIdx++, step = newChildren.next()) {
      var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, expirationTime);
      if (_newFiber4 !== null) {
        if (shouldTrackSideEffects) {
          if (_newFiber4.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren['delete'](_newFiber4.key === null ? newIdx : _newFiber4.key);
          }
        }
        lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber4;
        } else {
          previousNewFiber.sibling = _newFiber4;
        }
        previousNewFiber = _newFiber4;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, expirationTime) {
    // There's no need to check for keys on text nodes since we don't have a
    // way to define them.
    if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
      // We already have an existing node so let's just update it and delete
      // the rest.
      deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
      var existing = useFiber(currentFirstChild, textContent, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
    // The existing first child is not a text node so we need to create one
    // and delete the existing ones.
    deleteRemainingChildren(returnFiber, currentFirstChild);
    var created = createFiberFromText(textContent, returnFiber.internalContextTag, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSingleElement(returnFiber, currentFirstChild, element, expirationTime) {
    var key = element.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === Fragment ? element.type === REACT_FRAGMENT_TYPE : child.type === element.type) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, element.type === REACT_FRAGMENT_TYPE ? element.props.children : element.props, expirationTime);
          existing.ref = coerceRef(child, element);
          existing['return'] = returnFiber;
          {
            existing._debugSource = element._source;
            existing._debugOwner = element._owner;
          }
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    if (element.type === REACT_FRAGMENT_TYPE) {
      var created = createFiberFromFragment(element.props.children, returnFiber.internalContextTag, expirationTime, element.key);
      created['return'] = returnFiber;
      return created;
    } else {
      var _created7 = createFiberFromElement(element, returnFiber.internalContextTag, expirationTime);
      _created7.ref = coerceRef(currentFirstChild, element);
      _created7['return'] = returnFiber;
      return _created7;
    }
  }

  function reconcileSingleCall(returnFiber, currentFirstChild, call, expirationTime) {
    var key = call.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === CallComponent) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, call, expirationTime);
          existing['return'] = returnFiber;
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    var created = createFiberFromCall(call, returnFiber.internalContextTag, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSingleReturn(returnFiber, currentFirstChild, returnNode, expirationTime) {
    // There's no need to check for keys on yields since they're stateless.
    var child = currentFirstChild;
    if (child !== null) {
      if (child.tag === ReturnComponent) {
        deleteRemainingChildren(returnFiber, child.sibling);
        var existing = useFiber(child, null, expirationTime);
        existing.type = returnNode.value;
        existing['return'] = returnFiber;
        return existing;
      } else {
        deleteRemainingChildren(returnFiber, child);
      }
    }

    var created = createFiberFromReturn(returnNode, returnFiber.internalContextTag, expirationTime);
    created.type = returnNode.value;
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSinglePortal(returnFiber, currentFirstChild, portal, expirationTime) {
    var key = portal.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, portal.children || [], expirationTime);
          existing['return'] = returnFiber;
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    var created = createFiberFromPortal(portal, returnFiber.internalContextTag, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  // This API will tag the children with the side-effect of the reconciliation
  // itself. They will be added to the side-effect list as we pass through the
  // children and the parent.
  function reconcileChildFibers(returnFiber, currentFirstChild, newChild, expirationTime) {
    // This function is not recursive.
    // If the top level item is an array, we treat it as a set of children,
    // not as a fragment. Nested arrays on the other hand will be treated as
    // fragment nodes. Recursion happens at the normal flow.

    // Handle top level unkeyed fragments as if they were arrays.
    // This leads to an ambiguity between <>{[...]}</> and <>...</>.
    // We treat the ambiguous cases above the same.
    if (typeof newChild === 'object' && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null) {
      newChild = newChild.props.children;
    }

    // Handle object types
    var isObject = typeof newChild === 'object' && newChild !== null;

    if (isObject) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, expirationTime));

        case REACT_CALL_TYPE:
          return placeSingleChild(reconcileSingleCall(returnFiber, currentFirstChild, newChild, expirationTime));
        case REACT_RETURN_TYPE:
          return placeSingleChild(reconcileSingleReturn(returnFiber, currentFirstChild, newChild, expirationTime));
        case REACT_PORTAL_TYPE:
          return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, expirationTime));
      }
    }

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, '' + newChild, expirationTime));
    }

    if (isArray$1(newChild)) {
      return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (getIteratorFn(newChild)) {
      return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (isObject) {
      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }
    if (typeof newChild === 'undefined') {
      // If the new child is undefined, and the return fiber is a composite
      // component, throw an error. If Fiber return types are disabled,
      // we already threw above.
      switch (returnFiber.tag) {
        case ClassComponent:
          {
            {
              var instance = returnFiber.stateNode;
              if (instance.render._isMockFunction) {
                // We allow auto-mocks to proceed as if they're returning null.
                break;
              }
            }
          }
        // Intentionally fall through to the next case, which handles both
        // functions and classes
        // eslint-disable-next-lined no-fallthrough
        case FunctionalComponent:
          {
            var Component = returnFiber.type;
            invariant(false, '%s(...): Nothing was returned from render. This usually means a return statement is missing. Or, to render nothing, return null.', Component.displayName || Component.name || 'Component');
          }
      }
    }

    // Remaining cases are all treated as empty.
    return deleteRemainingChildren(returnFiber, currentFirstChild);
  }

  return reconcileChildFibers;
}

var reconcileChildFibers = ChildReconciler(true);
var mountChildFibers = ChildReconciler(false);

function cloneChildFibers(current, workInProgress) {
  !(current === null || workInProgress.child === current.child) ? invariant(false, 'Resuming work not yet implemented.') : void 0;

  if (workInProgress.child === null) {
    return;
  }

  var currentChild = workInProgress.child;
  var newChild = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
  workInProgress.child = newChild;

  newChild['return'] = workInProgress;
  while (currentChild.sibling !== null) {
    currentChild = currentChild.sibling;
    newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
    newChild['return'] = workInProgress;
  }
  newChild.sibling = null;
}

{
  var warnedAboutStatelessRefs = {};
}

var ReactFiberBeginWork = function (config, hostContext, hydrationContext, scheduleWork, computeExpirationForFiber) {
  var shouldSetTextContent = config.shouldSetTextContent,
      useSyncScheduling = config.useSyncScheduling,
      shouldDeprioritizeSubtree = config.shouldDeprioritizeSubtree;
  var pushHostContext = hostContext.pushHostContext,
      pushHostContainer = hostContext.pushHostContainer;
  var enterHydrationState = hydrationContext.enterHydrationState,
      resetHydrationState = hydrationContext.resetHydrationState,
      tryToClaimNextHydratableInstance = hydrationContext.tryToClaimNextHydratableInstance;

  var _ReactFiberClassCompo = ReactFiberClassComponent(scheduleWork, computeExpirationForFiber, memoizeProps, memoizeState),
      adoptClassInstance = _ReactFiberClassCompo.adoptClassInstance,
      constructClassInstance = _ReactFiberClassCompo.constructClassInstance,
      mountClassInstance = _ReactFiberClassCompo.mountClassInstance,
      updateClassInstance = _ReactFiberClassCompo.updateClassInstance;

  // TODO: Remove this and use reconcileChildrenAtExpirationTime directly.


  function reconcileChildren(current, workInProgress, nextChildren) {
    reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, workInProgress.expirationTime);
  }

  function reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, renderExpirationTime) {
    if (current === null) {
      // If this is a fresh new component that hasn't been rendered yet, we
      // won't update its child set by applying minimal side-effects. Instead,
      // we will add them all to the child before it gets rendered. That means
      // we can optimize this reconciliation pass by not tracking side-effects.
      workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
    } else {
      // If the current child is the same as the work in progress, it means that
      // we haven't yet started any work on these children. Therefore, we use
      // the clone algorithm to create a copy of all the current children.

      // If we had any progressed work already, that is invalid at this point so
      // let's throw it out.
      workInProgress.child = reconcileChildFibers(workInProgress, current.child, nextChildren, renderExpirationTime);
    }
  }

  function updateFragment(current, workInProgress) {
    var nextChildren = workInProgress.pendingProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextChildren === null) {
        nextChildren = workInProgress.memoizedProps;
      }
    } else if (nextChildren === null || workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextChildren);
    return workInProgress.child;
  }

  function markRef(current, workInProgress) {
    var ref = workInProgress.ref;
    if (ref !== null && (!current || current.ref !== ref)) {
      // Schedule a Ref effect
      workInProgress.effectTag |= Ref;
    }
  }

  function updateFunctionalComponent(current, workInProgress) {
    var fn = workInProgress.type;
    var nextProps = workInProgress.pendingProps;

    var memoizedProps = workInProgress.memoizedProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextProps === null) {
        nextProps = memoizedProps;
      }
    } else {
      if (nextProps === null || memoizedProps === nextProps) {
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      }
      // TODO: consider bringing fn.shouldComponentUpdate() back.
      // It used to be here.
    }

    var unmaskedContext = getUnmaskedContext(workInProgress);
    var context = getMaskedContext(workInProgress, unmaskedContext);

    var nextChildren;

    {
      ReactCurrentOwner.current = workInProgress;
      ReactDebugCurrentFiber.setCurrentPhase('render');
      nextChildren = fn(nextProps, context);
      ReactDebugCurrentFiber.setCurrentPhase(null);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextProps);
    return workInProgress.child;
  }

  function updateClassComponent(current, workInProgress, renderExpirationTime) {
    // Push context providers early to prevent context stack mismatches.
    // During mounting we don't know the child context yet as the instance doesn't exist.
    // We will invalidate the child context in finishClassComponent() right after rendering.
    var hasContext = pushContextProvider(workInProgress);

    var shouldUpdate = void 0;
    if (current === null) {
      if (!workInProgress.stateNode) {
        // In the initial pass we might need to construct the instance.
        constructClassInstance(workInProgress, workInProgress.pendingProps);
        mountClassInstance(workInProgress, renderExpirationTime);
        shouldUpdate = true;
      } else {
        invariant(false, 'Resuming work not yet implemented.');
        // In a resume, we'll already have an instance we can reuse.
        // shouldUpdate = resumeMountClassInstance(workInProgress, renderExpirationTime);
      }
    } else {
      shouldUpdate = updateClassInstance(current, workInProgress, renderExpirationTime);
    }
    return finishClassComponent(current, workInProgress, shouldUpdate, hasContext);
  }

  function finishClassComponent(current, workInProgress, shouldUpdate, hasContext) {
    // Refs should update even if shouldComponentUpdate returns false
    markRef(current, workInProgress);

    if (!shouldUpdate) {
      // Context providers should defer to sCU for rendering
      if (hasContext) {
        invalidateContextProvider(workInProgress, false);
      }

      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var instance = workInProgress.stateNode;

    // Rerender
    ReactCurrentOwner.current = workInProgress;
    var nextChildren = void 0;
    {
      ReactDebugCurrentFiber.setCurrentPhase('render');
      nextChildren = instance.render();
      if (debugRenderPhaseSideEffects) {
        instance.render();
      }
      ReactDebugCurrentFiber.setCurrentPhase(null);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;
    reconcileChildren(current, workInProgress, nextChildren);
    // Memoize props and state using the values we just used to render.
    // TODO: Restructure so we never read values from the instance.
    memoizeState(workInProgress, instance.state);
    memoizeProps(workInProgress, instance.props);

    // The context might have changed so we need to recalculate it.
    if (hasContext) {
      invalidateContextProvider(workInProgress, true);
    }

    return workInProgress.child;
  }

  function pushHostRootContext(workInProgress) {
    var root = workInProgress.stateNode;
    if (root.pendingContext) {
      pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
    } else if (root.context) {
      // Should always be set
      pushTopLevelContextObject(workInProgress, root.context, false);
    }
    pushHostContainer(workInProgress, root.containerInfo);
  }

  function updateHostRoot(current, workInProgress, renderExpirationTime) {
    pushHostRootContext(workInProgress);
    var updateQueue = workInProgress.updateQueue;
    if (updateQueue !== null) {
      var prevState = workInProgress.memoizedState;
      var state = processUpdateQueue(current, workInProgress, updateQueue, null, null, renderExpirationTime);
      if (prevState === state) {
        // If the state is the same as before, that's a bailout because we had
        // no work that expires at this time.
        resetHydrationState();
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      }
      var element = state.element;
      var root = workInProgress.stateNode;
      if ((current === null || current.child === null) && root.hydrate && enterHydrationState(workInProgress)) {
        // If we don't have any current children this might be the first pass.
        // We always try to hydrate. If this isn't a hydration pass there won't
        // be any children to hydrate which is effectively the same thing as
        // not hydrating.

        // This is a bit of a hack. We track the host root as a placement to
        // know that we're currently in a mounting state. That way isMounted
        // works as expected. We must reset this before committing.
        // TODO: Delete this when we delete isMounted and findDOMNode.
        workInProgress.effectTag |= Placement;

        // Ensure that children mount into this root without tracking
        // side-effects. This ensures that we don't store Placement effects on
        // nodes that will be hydrated.
        workInProgress.child = mountChildFibers(workInProgress, null, element, renderExpirationTime);
      } else {
        // Otherwise reset hydration state in case we aborted and resumed another
        // root.
        resetHydrationState();
        reconcileChildren(current, workInProgress, element);
      }
      memoizeState(workInProgress, state);
      return workInProgress.child;
    }
    resetHydrationState();
    // If there is no update queue, that's a bailout because the root has no props.
    return bailoutOnAlreadyFinishedWork(current, workInProgress);
  }

  function updateHostComponent(current, workInProgress, renderExpirationTime) {
    pushHostContext(workInProgress);

    if (current === null) {
      tryToClaimNextHydratableInstance(workInProgress);
    }

    var type = workInProgress.type;
    var memoizedProps = workInProgress.memoizedProps;
    var nextProps = workInProgress.pendingProps;
    if (nextProps === null) {
      nextProps = memoizedProps;
      !(nextProps !== null) ? invariant(false, 'We should always have pending or current props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    }
    var prevProps = current !== null ? current.memoizedProps : null;

    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (nextProps === null || memoizedProps === nextProps) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var nextChildren = nextProps.children;
    var isDirectTextChild = shouldSetTextContent(type, nextProps);

    if (isDirectTextChild) {
      // We special case a direct text child of a host node. This is a common
      // case. We won't handle it as a reified child. We will instead handle
      // this in the host environment that also have access to this prop. That
      // avoids allocating another HostText fiber and traversing it.
      nextChildren = null;
    } else if (prevProps && shouldSetTextContent(type, prevProps)) {
      // If we're switching from a direct text child to a normal child, or to
      // empty, we need to schedule the text content to be reset.
      workInProgress.effectTag |= ContentReset;
    }

    markRef(current, workInProgress);

    // Check the host config to see if the children are offscreen/hidden.
    if (renderExpirationTime !== Never && !useSyncScheduling && shouldDeprioritizeSubtree(type, nextProps)) {
      // Down-prioritize the children.
      workInProgress.expirationTime = Never;
      // Bailout and come back to this fiber later.
      return null;
    }

    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextProps);
    return workInProgress.child;
  }

  function updateHostText(current, workInProgress) {
    if (current === null) {
      tryToClaimNextHydratableInstance(workInProgress);
    }
    var nextProps = workInProgress.pendingProps;
    if (nextProps === null) {
      nextProps = workInProgress.memoizedProps;
    }
    memoizeProps(workInProgress, nextProps);
    // Nothing to do here. This is terminal. We'll do the completion step
    // immediately after.
    return null;
  }

  function mountIndeterminateComponent(current, workInProgress, renderExpirationTime) {
    !(current === null) ? invariant(false, 'An indeterminate component should never have mounted. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    var fn = workInProgress.type;
    var props = workInProgress.pendingProps;
    var unmaskedContext = getUnmaskedContext(workInProgress);
    var context = getMaskedContext(workInProgress, unmaskedContext);

    var value;

    {
      if (fn.prototype && typeof fn.prototype.render === 'function') {
        var componentName = getComponentName(workInProgress);
        warning(false, "The <%s /> component appears to have a render method, but doesn't extend React.Component. " + 'This is likely to cause errors. Change %s to extend React.Component instead.', componentName, componentName);
      }
      ReactCurrentOwner.current = workInProgress;
      value = fn(props, context);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;

    if (typeof value === 'object' && value !== null && typeof value.render === 'function') {
      // Proceed under the assumption that this is a class instance
      workInProgress.tag = ClassComponent;

      // Push context providers early to prevent context stack mismatches.
      // During mounting we don't know the child context yet as the instance doesn't exist.
      // We will invalidate the child context in finishClassComponent() right after rendering.
      var hasContext = pushContextProvider(workInProgress);
      adoptClassInstance(workInProgress, value);
      mountClassInstance(workInProgress, renderExpirationTime);
      return finishClassComponent(current, workInProgress, true, hasContext);
    } else {
      // Proceed under the assumption that this is a functional component
      workInProgress.tag = FunctionalComponent;
      {
        var Component = workInProgress.type;

        if (Component) {
          warning(!Component.childContextTypes, '%s(...): childContextTypes cannot be defined on a functional component.', Component.displayName || Component.name || 'Component');
        }
        if (workInProgress.ref !== null) {
          var info = '';
          var ownerName = ReactDebugCurrentFiber.getCurrentFiberOwnerName();
          if (ownerName) {
            info += '\n\nCheck the render method of `' + ownerName + '`.';
          }

          var warningKey = ownerName || workInProgress._debugID || '';
          var debugSource = workInProgress._debugSource;
          if (debugSource) {
            warningKey = debugSource.fileName + ':' + debugSource.lineNumber;
          }
          if (!warnedAboutStatelessRefs[warningKey]) {
            warnedAboutStatelessRefs[warningKey] = true;
            warning(false, 'Stateless function components cannot be given refs. ' + 'Attempts to access this ref will fail.%s%s', info, ReactDebugCurrentFiber.getCurrentFiberStackAddendum());
          }
        }
      }
      reconcileChildren(current, workInProgress, value);
      memoizeProps(workInProgress, props);
      return workInProgress.child;
    }
  }

  function updateCallComponent(current, workInProgress, renderExpirationTime) {
    var nextCall = workInProgress.pendingProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextCall === null) {
        nextCall = current && current.memoizedProps;
        !(nextCall !== null) ? invariant(false, 'We should always have pending or current props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
      }
    } else if (nextCall === null || workInProgress.memoizedProps === nextCall) {
      nextCall = workInProgress.memoizedProps;
      // TODO: When bailing out, we might need to return the stateNode instead
      // of the child. To check it for work.
      // return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var nextChildren = nextCall.children;

    // The following is a fork of reconcileChildrenAtExpirationTime but using
    // stateNode to store the child.
    if (current === null) {
      workInProgress.stateNode = mountChildFibers(workInProgress, workInProgress.stateNode, nextChildren, renderExpirationTime);
    } else {
      workInProgress.stateNode = reconcileChildFibers(workInProgress, workInProgress.stateNode, nextChildren, renderExpirationTime);
    }

    memoizeProps(workInProgress, nextCall);
    // This doesn't take arbitrary time so we could synchronously just begin
    // eagerly do the work of workInProgress.child as an optimization.
    return workInProgress.stateNode;
  }

  function updatePortalComponent(current, workInProgress, renderExpirationTime) {
    pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
    var nextChildren = workInProgress.pendingProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextChildren === null) {
        nextChildren = current && current.memoizedProps;
        !(nextChildren != null) ? invariant(false, 'We should always have pending or current props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
      }
    } else if (nextChildren === null || workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    if (current === null) {
      // Portals are special because we don't append the children during mount
      // but at commit. Therefore we need to track insertions which the normal
      // flow doesn't do during mount. This doesn't happen at the root because
      // the root always starts with a "current" with a null child.
      // TODO: Consider unifying this with how the root works.
      workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
      memoizeProps(workInProgress, nextChildren);
    } else {
      reconcileChildren(current, workInProgress, nextChildren);
      memoizeProps(workInProgress, nextChildren);
    }
    return workInProgress.child;
  }

  /*
  function reuseChildrenEffects(returnFiber : Fiber, firstChild : Fiber) {
    let child = firstChild;
    do {
      // Ensure that the first and last effect of the parent corresponds
      // to the children's first and last effect.
      if (!returnFiber.firstEffect) {
        returnFiber.firstEffect = child.firstEffect;
      }
      if (child.lastEffect) {
        if (returnFiber.lastEffect) {
          returnFiber.lastEffect.nextEffect = child.firstEffect;
        }
        returnFiber.lastEffect = child.lastEffect;
      }
    } while (child = child.sibling);
  }
  */

  function bailoutOnAlreadyFinishedWork(current, workInProgress) {
    cancelWorkTimer(workInProgress);

    // TODO: We should ideally be able to bail out early if the children have no
    // more work to do. However, since we don't have a separation of this
    // Fiber's priority and its children yet - we don't know without doing lots
    // of the same work we do anyway. Once we have that separation we can just
    // bail out here if the children has no more work at this priority level.
    // if (workInProgress.priorityOfChildren <= priorityLevel) {
    //   // If there are side-effects in these children that have not yet been
    //   // committed we need to ensure that they get properly transferred up.
    //   if (current && current.child !== workInProgress.child) {
    //     reuseChildrenEffects(workInProgress, child);
    //   }
    //   return null;
    // }

    cloneChildFibers(current, workInProgress);
    return workInProgress.child;
  }

  function bailoutOnLowPriority(current, workInProgress) {
    cancelWorkTimer(workInProgress);

    // TODO: Handle HostComponent tags here as well and call pushHostContext()?
    // See PR 8590 discussion for context
    switch (workInProgress.tag) {
      case HostRoot:
        pushHostRootContext(workInProgress);
        break;
      case ClassComponent:
        pushContextProvider(workInProgress);
        break;
      case HostPortal:
        pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
        break;
    }
    // TODO: What if this is currently in progress?
    // How can that happen? How is this not being cloned?
    return null;
  }

  // TODO: Delete memoizeProps/State and move to reconcile/bailout instead
  function memoizeProps(workInProgress, nextProps) {
    workInProgress.memoizedProps = nextProps;
  }

  function memoizeState(workInProgress, nextState) {
    workInProgress.memoizedState = nextState;
    // Don't reset the updateQueue, in case there are pending updates. Resetting
    // is handled by processUpdateQueue.
  }

  function beginWork(current, workInProgress, renderExpirationTime) {
    if (workInProgress.expirationTime === NoWork || workInProgress.expirationTime > renderExpirationTime) {
      return bailoutOnLowPriority(current, workInProgress);
    }

    switch (workInProgress.tag) {
      case IndeterminateComponent:
        return mountIndeterminateComponent(current, workInProgress, renderExpirationTime);
      case FunctionalComponent:
        return updateFunctionalComponent(current, workInProgress);
      case ClassComponent:
        return updateClassComponent(current, workInProgress, renderExpirationTime);
      case HostRoot:
        return updateHostRoot(current, workInProgress, renderExpirationTime);
      case HostComponent:
        return updateHostComponent(current, workInProgress, renderExpirationTime);
      case HostText:
        return updateHostText(current, workInProgress);
      case CallHandlerPhase:
        // This is a restart. Reset the tag to the initial phase.
        workInProgress.tag = CallComponent;
      // Intentionally fall through since this is now the same.
      case CallComponent:
        return updateCallComponent(current, workInProgress, renderExpirationTime);
      case ReturnComponent:
        // A return component is just a placeholder, we can just run through the
        // next one immediately.
        return null;
      case HostPortal:
        return updatePortalComponent(current, workInProgress, renderExpirationTime);
      case Fragment:
        return updateFragment(current, workInProgress);
      default:
        invariant(false, 'Unknown unit of work tag. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  function beginFailedWork(current, workInProgress, renderExpirationTime) {
    // Push context providers here to avoid a push/pop context mismatch.
    switch (workInProgress.tag) {
      case ClassComponent:
        pushContextProvider(workInProgress);
        break;
      case HostRoot:
        pushHostRootContext(workInProgress);
        break;
      default:
        invariant(false, 'Invalid type of work. This error is likely caused by a bug in React. Please file an issue.');
    }

    // Add an error effect so we can handle the error during the commit phase
    workInProgress.effectTag |= Err;

    // This is a weird case where we do "resume" work — work that failed on
    // our first attempt. Because we no longer have a notion of "progressed
    // deletions," reset the child to the current child to make sure we delete
    // it again. TODO: Find a better way to handle this, perhaps during a more
    // general overhaul of error handling.
    if (current === null) {
      workInProgress.child = null;
    } else if (workInProgress.child !== current.child) {
      workInProgress.child = current.child;
    }

    if (workInProgress.expirationTime === NoWork || workInProgress.expirationTime > renderExpirationTime) {
      return bailoutOnLowPriority(current, workInProgress);
    }

    // If we don't bail out, we're going be recomputing our children so we need
    // to drop our effect list.
    workInProgress.firstEffect = null;
    workInProgress.lastEffect = null;

    // Unmount the current children as if the component rendered null
    var nextChildren = null;
    reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, renderExpirationTime);

    if (workInProgress.tag === ClassComponent) {
      var instance = workInProgress.stateNode;
      workInProgress.memoizedProps = instance.props;
      workInProgress.memoizedState = instance.state;
    }

    return workInProgress.child;
  }

  return {
    beginWork: beginWork,
    beginFailedWork: beginFailedWork
  };
};

var ReactFiberCompleteWork = function (config, hostContext, hydrationContext) {
  var createInstance = config.createInstance,
      createTextInstance = config.createTextInstance,
      appendInitialChild = config.appendInitialChild,
      finalizeInitialChildren = config.finalizeInitialChildren,
      prepareUpdate = config.prepareUpdate,
      mutation = config.mutation,
      persistence = config.persistence;
  var getRootHostContainer = hostContext.getRootHostContainer,
      popHostContext = hostContext.popHostContext,
      getHostContext = hostContext.getHostContext,
      popHostContainer = hostContext.popHostContainer;
  var prepareToHydrateHostInstance = hydrationContext.prepareToHydrateHostInstance,
      prepareToHydrateHostTextInstance = hydrationContext.prepareToHydrateHostTextInstance,
      popHydrationState = hydrationContext.popHydrationState;


  function markUpdate(workInProgress) {
    // Tag the fiber with an update effect. This turns a Placement into
    // an UpdateAndPlacement.
    workInProgress.effectTag |= Update;
  }

  function markRef(workInProgress) {
    workInProgress.effectTag |= Ref;
  }

  function appendAllReturns(returns, workInProgress) {
    var node = workInProgress.stateNode;
    if (node) {
      node['return'] = workInProgress;
    }
    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText || node.tag === HostPortal) {
        invariant(false, 'A call cannot have host component children.');
      } else if (node.tag === ReturnComponent) {
        returns.push(node.type);
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === workInProgress) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function moveCallToHandlerPhase(current, workInProgress, renderExpirationTime) {
    var call = workInProgress.memoizedProps;
    !call ? invariant(false, 'Should be resolved by now. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    // First step of the call has completed. Now we need to do the second.
    // TODO: It would be nice to have a multi stage call represented by a
    // single component, or at least tail call optimize nested ones. Currently
    // that requires additional fields that we don't want to add to the fiber.
    // So this requires nested handlers.
    // Note: This doesn't mutate the alternate node. I don't think it needs to
    // since this stage is reset for every pass.
    workInProgress.tag = CallHandlerPhase;

    // Build up the returns.
    // TODO: Compare this to a generator or opaque helpers like Children.
    var returns = [];
    appendAllReturns(returns, workInProgress);
    var fn = call.handler;
    var props = call.props;
    var nextChildren = fn(props, returns);

    var currentFirstChild = current !== null ? current.child : null;
    workInProgress.child = reconcileChildFibers(workInProgress, currentFirstChild, nextChildren, renderExpirationTime);
    return workInProgress.child;
  }

  function appendAllChildren(parent, workInProgress) {
    // We only have the top Fiber that was created but we need recurse down its
    // children to find all the terminal nodes.
    var node = workInProgress.child;
    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText) {
        appendInitialChild(parent, node.stateNode);
      } else if (node.tag === HostPortal) {
        // If we have a portal child, then we don't want to traverse
        // down its children. Instead, we'll get insertions from each child in
        // the portal directly.
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === workInProgress) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === workInProgress) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  var updateHostContainer = void 0;
  var updateHostComponent = void 0;
  var updateHostText = void 0;
  if (mutation) {
    if (enableMutatingReconciler) {
      // Mutation mode
      updateHostContainer = function (workInProgress) {
        // Noop
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance) {
        // TODO: Type this specific to this type of component.
        workInProgress.updateQueue = updatePayload;
        // If the update payload indicates that there is a change or if there
        // is a new ref we mark this as an update. All the work is done in commitWork.
        if (updatePayload) {
          markUpdate(workInProgress);
        }
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        // If the text differs, mark it as an update. All the work in done in commitWork.
        if (oldText !== newText) {
          markUpdate(workInProgress);
        }
      };
    } else {
      invariant(false, 'Mutating reconciler is disabled.');
    }
  } else if (persistence) {
    if (enablePersistentReconciler) {
      // Persistent host tree mode
      var cloneInstance = persistence.cloneInstance,
          createContainerChildSet = persistence.createContainerChildSet,
          appendChildToContainerChildSet = persistence.appendChildToContainerChildSet,
          finalizeContainerChildren = persistence.finalizeContainerChildren;

      // An unfortunate fork of appendAllChildren because we have two different parent types.

      var appendAllChildrenToContainer = function (containerChildSet, workInProgress) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;
        while (node !== null) {
          if (node.tag === HostComponent || node.tag === HostText) {
            appendChildToContainerChildSet(containerChildSet, node.stateNode);
          } else if (node.tag === HostPortal) {
            // If we have a portal child, then we don't want to traverse
            // down its children. Instead, we'll get insertions from each child in
            // the portal directly.
          } else if (node.child !== null) {
            node.child['return'] = node;
            node = node.child;
            continue;
          }
          if (node === workInProgress) {
            return;
          }
          while (node.sibling === null) {
            if (node['return'] === null || node['return'] === workInProgress) {
              return;
            }
            node = node['return'];
          }
          node.sibling['return'] = node['return'];
          node = node.sibling;
        }
      };
      updateHostContainer = function (workInProgress) {
        var portalOrRoot = workInProgress.stateNode;
        var childrenUnchanged = workInProgress.firstEffect === null;
        if (childrenUnchanged) {
          // No changes, just reuse the existing instance.
        } else {
          var container = portalOrRoot.containerInfo;
          var newChildSet = createContainerChildSet(container);
          if (finalizeContainerChildren(container, newChildSet)) {
            markUpdate(workInProgress);
          }
          portalOrRoot.pendingChildren = newChildSet;
          // If children might have changed, we have to add them all to the set.
          appendAllChildrenToContainer(newChildSet, workInProgress);
          // Schedule an update on the container to swap out the container.
          markUpdate(workInProgress);
        }
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance) {
        // If there are no effects associated with this node, then none of our children had any updates.
        // This guarantees that we can reuse all of them.
        var childrenUnchanged = workInProgress.firstEffect === null;
        var currentInstance = current.stateNode;
        if (childrenUnchanged && updatePayload === null) {
          // No changes, just reuse the existing instance.
          // Note that this might release a previous clone.
          workInProgress.stateNode = currentInstance;
        } else {
          var recyclableInstance = workInProgress.stateNode;
          var newInstance = cloneInstance(currentInstance, updatePayload, type, oldProps, newProps, workInProgress, childrenUnchanged, recyclableInstance);
          if (finalizeInitialChildren(newInstance, type, newProps, rootContainerInstance)) {
            markUpdate(workInProgress);
          }
          workInProgress.stateNode = newInstance;
          if (childrenUnchanged) {
            // If there are no other effects in this tree, we need to flag this node as having one.
            // Even though we're not going to use it for anything.
            // Otherwise parents won't know that there are new children to propagate upwards.
            markUpdate(workInProgress);
          } else {
            // If children might have changed, we have to add them all to the set.
            appendAllChildren(newInstance, workInProgress);
          }
        }
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        if (oldText !== newText) {
          // If the text content differs, we'll create a new text instance for it.
          var rootContainerInstance = getRootHostContainer();
          var currentHostContext = getHostContext();
          workInProgress.stateNode = createTextInstance(newText, rootContainerInstance, currentHostContext, workInProgress);
          // We'll have to mark it as having an effect, even though we won't use the effect for anything.
          // This lets the parents know that at least one of their children has changed.
          markUpdate(workInProgress);
        }
      };
    } else {
      invariant(false, 'Persistent reconciler is disabled.');
    }
  } else {
    if (enableNoopReconciler) {
      // No host operations
      updateHostContainer = function (workInProgress) {
        // Noop
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance) {
        // Noop
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        // Noop
      };
    } else {
      invariant(false, 'Noop reconciler is disabled.');
    }
  }

  function completeWork(current, workInProgress, renderExpirationTime) {
    // Get the latest props.
    var newProps = workInProgress.pendingProps;
    if (newProps === null) {
      newProps = workInProgress.memoizedProps;
    } else if (workInProgress.expirationTime !== Never || renderExpirationTime === Never) {
      // Reset the pending props, unless this was a down-prioritization.
      workInProgress.pendingProps = null;
    }

    switch (workInProgress.tag) {
      case FunctionalComponent:
        return null;
      case ClassComponent:
        {
          // We are leaving this subtree, so pop context if any.
          popContextProvider(workInProgress);
          return null;
        }
      case HostRoot:
        {
          popHostContainer(workInProgress);
          popTopLevelContextObject(workInProgress);
          var fiberRoot = workInProgress.stateNode;
          if (fiberRoot.pendingContext) {
            fiberRoot.context = fiberRoot.pendingContext;
            fiberRoot.pendingContext = null;
          }

          if (current === null || current.child === null) {
            // If we hydrated, pop so that we can delete any remaining children
            // that weren't hydrated.
            popHydrationState(workInProgress);
            // This resets the hacky state to fix isMounted before committing.
            // TODO: Delete this when we delete isMounted and findDOMNode.
            workInProgress.effectTag &= ~Placement;
          }
          updateHostContainer(workInProgress);
          return null;
        }
      case HostComponent:
        {
          popHostContext(workInProgress);
          var rootContainerInstance = getRootHostContainer();
          var type = workInProgress.type;
          if (current !== null && workInProgress.stateNode != null) {
            // If we have an alternate, that means this is an update and we need to
            // schedule a side-effect to do the updates.
            var oldProps = current.memoizedProps;
            // If we get updated because one of our children updated, we don't
            // have newProps so we'll have to reuse them.
            // TODO: Split the update API as separate for the props vs. children.
            // Even better would be if children weren't special cased at all tho.
            var instance = workInProgress.stateNode;
            var currentHostContext = getHostContext();
            var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, currentHostContext);

            updateHostComponent(current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance);

            if (current.ref !== workInProgress.ref) {
              markRef(workInProgress);
            }
          } else {
            if (!newProps) {
              !(workInProgress.stateNode !== null) ? invariant(false, 'We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.') : void 0;
              // This can happen when we abort work.
              return null;
            }

            var _currentHostContext = getHostContext();
            // TODO: Move createInstance to beginWork and keep it on a context
            // "stack" as the parent. Then append children as we go in beginWork
            // or completeWork depending on we want to add then top->down or
            // bottom->up. Top->down is faster in IE11.
            var wasHydrated = popHydrationState(workInProgress);
            if (wasHydrated) {
              // TODO: Move this and createInstance step into the beginPhase
              // to consolidate.
              if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, _currentHostContext)) {
                // If changes to the hydrated node needs to be applied at the
                // commit-phase we mark this as such.
                markUpdate(workInProgress);
              }
            } else {
              var _instance = createInstance(type, newProps, rootContainerInstance, _currentHostContext, workInProgress);

              appendAllChildren(_instance, workInProgress);

              // Certain renderers require commit-time effects for initial mount.
              // (eg DOM renderer supports auto-focus for certain elements).
              // Make sure such renderers get scheduled for later work.
              if (finalizeInitialChildren(_instance, type, newProps, rootContainerInstance)) {
                markUpdate(workInProgress);
              }
              workInProgress.stateNode = _instance;
            }

            if (workInProgress.ref !== null) {
              // If there is a ref on a host node we need to schedule a callback
              markRef(workInProgress);
            }
          }
          return null;
        }
      case HostText:
        {
          var newText = newProps;
          if (current && workInProgress.stateNode != null) {
            var oldText = current.memoizedProps;
            // If we have an alternate, that means this is an update and we need
            // to schedule a side-effect to do the updates.
            updateHostText(current, workInProgress, oldText, newText);
          } else {
            if (typeof newText !== 'string') {
              !(workInProgress.stateNode !== null) ? invariant(false, 'We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.') : void 0;
              // This can happen when we abort work.
              return null;
            }
            var _rootContainerInstance = getRootHostContainer();
            var _currentHostContext2 = getHostContext();
            var _wasHydrated = popHydrationState(workInProgress);
            if (_wasHydrated) {
              if (prepareToHydrateHostTextInstance(workInProgress)) {
                markUpdate(workInProgress);
              }
            } else {
              workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext2, workInProgress);
            }
          }
          return null;
        }
      case CallComponent:
        return moveCallToHandlerPhase(current, workInProgress, renderExpirationTime);
      case CallHandlerPhase:
        // Reset the tag to now be a first phase call.
        workInProgress.tag = CallComponent;
        return null;
      case ReturnComponent:
        // Does nothing.
        return null;
      case Fragment:
        return null;
      case HostPortal:
        popHostContainer(workInProgress);
        updateHostContainer(workInProgress);
        return null;
      // Error cases
      case IndeterminateComponent:
        invariant(false, 'An indeterminate component should have become determinate before completing. This error is likely caused by a bug in React. Please file an issue.');
      // eslint-disable-next-line no-fallthrough
      default:
        invariant(false, 'Unknown unit of work tag. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  return {
    completeWork: completeWork
  };
};

var invokeGuardedCallback$2 = ReactErrorUtils.invokeGuardedCallback;
var hasCaughtError$1 = ReactErrorUtils.hasCaughtError;
var clearCaughtError$1 = ReactErrorUtils.clearCaughtError;


var ReactFiberCommitWork = function (config, captureError) {
  var getPublicInstance = config.getPublicInstance,
      mutation = config.mutation,
      persistence = config.persistence;


  var callComponentWillUnmountWithTimer = function (current, instance) {
    startPhaseTimer(current, 'componentWillUnmount');
    instance.props = current.memoizedProps;
    instance.state = current.memoizedState;
    instance.componentWillUnmount();
    stopPhaseTimer();
  };

  // Capture errors so they don't interrupt unmounting.
  function safelyCallComponentWillUnmount(current, instance) {
    {
      invokeGuardedCallback$2(null, callComponentWillUnmountWithTimer, null, current, instance);
      if (hasCaughtError$1()) {
        var unmountError = clearCaughtError$1();
        captureError(current, unmountError);
      }
    }
  }

  function safelyDetachRef(current) {
    var ref = current.ref;
    if (ref !== null) {
      {
        invokeGuardedCallback$2(null, ref, null, null);
        if (hasCaughtError$1()) {
          var refError = clearCaughtError$1();
          captureError(current, refError);
        }
      }
    }
  }

  function commitLifeCycles(current, finishedWork) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          var instance = finishedWork.stateNode;
          if (finishedWork.effectTag & Update) {
            if (current === null) {
              startPhaseTimer(finishedWork, 'componentDidMount');
              instance.props = finishedWork.memoizedProps;
              instance.state = finishedWork.memoizedState;
              instance.componentDidMount();
              stopPhaseTimer();
            } else {
              var prevProps = current.memoizedProps;
              var prevState = current.memoizedState;
              startPhaseTimer(finishedWork, 'componentDidUpdate');
              instance.props = finishedWork.memoizedProps;
              instance.state = finishedWork.memoizedState;
              instance.componentDidUpdate(prevProps, prevState);
              stopPhaseTimer();
            }
          }
          var updateQueue = finishedWork.updateQueue;
          if (updateQueue !== null) {
            commitCallbacks(updateQueue, instance);
          }
          return;
        }
      case HostRoot:
        {
          var _updateQueue = finishedWork.updateQueue;
          if (_updateQueue !== null) {
            var _instance = finishedWork.child !== null ? finishedWork.child.stateNode : null;
            commitCallbacks(_updateQueue, _instance);
          }
          return;
        }
      case HostComponent:
        {
          var _instance2 = finishedWork.stateNode;

          // Renderers may schedule work to be done after host components are mounted
          // (eg DOM renderer may schedule auto-focus for inputs and form controls).
          // These effects should only be committed when components are first mounted,
          // aka when there is no current/alternate.
          if (current === null && finishedWork.effectTag & Update) {
            var type = finishedWork.type;
            var props = finishedWork.memoizedProps;
            commitMount(_instance2, type, props, finishedWork);
          }

          return;
        }
      case HostText:
        {
          // We have no life-cycles associated with text.
          return;
        }
      case HostPortal:
        {
          // We have no life-cycles associated with portals.
          return;
        }
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitAttachRef(finishedWork) {
    var ref = finishedWork.ref;
    if (ref !== null) {
      var instance = finishedWork.stateNode;
      switch (finishedWork.tag) {
        case HostComponent:
          ref(getPublicInstance(instance));
          break;
        default:
          ref(instance);
      }
    }
  }

  function commitDetachRef(current) {
    var currentRef = current.ref;
    if (currentRef !== null) {
      currentRef(null);
    }
  }

  // User-originating errors (lifecycles and refs) should not interrupt
  // deletion, so don't let them throw. Host-originating errors should
  // interrupt deletion, so it's okay
  function commitUnmount(current) {
    if (typeof onCommitUnmount === 'function') {
      onCommitUnmount(current);
    }

    switch (current.tag) {
      case ClassComponent:
        {
          safelyDetachRef(current);
          var instance = current.stateNode;
          if (typeof instance.componentWillUnmount === 'function') {
            safelyCallComponentWillUnmount(current, instance);
          }
          return;
        }
      case HostComponent:
        {
          safelyDetachRef(current);
          return;
        }
      case CallComponent:
        {
          commitNestedUnmounts(current.stateNode);
          return;
        }
      case HostPortal:
        {
          // TODO: this is recursive.
          // We are also not using this parent because
          // the portal will get pushed immediately.
          if (enableMutatingReconciler && mutation) {
            unmountHostComponents(current);
          } else if (enablePersistentReconciler && persistence) {
            emptyPortalContainer(current);
          }
          return;
        }
    }
  }

  function commitNestedUnmounts(root) {
    // While we're inside a removed host node we don't want to call
    // removeChild on the inner nodes because they're removed by the top
    // call anyway. We also want to call componentWillUnmount on all
    // composites before this host node is removed from the tree. Therefore
    var node = root;
    while (true) {
      commitUnmount(node);
      // Visit children because they may contain more composite or host nodes.
      // Skip portals because commitUnmount() currently visits them recursively.
      if (node.child !== null && (
      // If we use mutation we drill down into portals using commitUnmount above.
      // If we don't use mutation we drill down into portals here instead.
      !mutation || node.tag !== HostPortal)) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === root) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === root) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function detachFiber(current) {
    // Cut off the return pointers to disconnect it from the tree. Ideally, we
    // should clear the child pointer of the parent alternate to let this
    // get GC:ed but we don't know which for sure which parent is the current
    // one so we'll settle for GC:ing the subtree of this child. This child
    // itself will be GC:ed when the parent updates the next time.
    current['return'] = null;
    current.child = null;
    if (current.alternate) {
      current.alternate.child = null;
      current.alternate['return'] = null;
    }
  }

  if (!mutation) {
    var commitContainer = void 0;
    if (persistence) {
      var replaceContainerChildren = persistence.replaceContainerChildren,
          createContainerChildSet = persistence.createContainerChildSet;

      var emptyPortalContainer = function (current) {
        var portal = current.stateNode;
        var containerInfo = portal.containerInfo;

        var emptyChildSet = createContainerChildSet(containerInfo);
        replaceContainerChildren(containerInfo, emptyChildSet);
      };
      commitContainer = function (finishedWork) {
        switch (finishedWork.tag) {
          case ClassComponent:
            {
              return;
            }
          case HostComponent:
            {
              return;
            }
          case HostText:
            {
              return;
            }
          case HostRoot:
          case HostPortal:
            {
              var portalOrRoot = finishedWork.stateNode;
              var containerInfo = portalOrRoot.containerInfo,
                  _pendingChildren = portalOrRoot.pendingChildren;

              replaceContainerChildren(containerInfo, _pendingChildren);
              return;
            }
          default:
            {
              invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
            }
        }
      };
    } else {
      commitContainer = function (finishedWork) {
        // Noop
      };
    }
    if (enablePersistentReconciler || enableNoopReconciler) {
      return {
        commitResetTextContent: function (finishedWork) {},
        commitPlacement: function (finishedWork) {},
        commitDeletion: function (current) {
          // Detach refs and call componentWillUnmount() on the whole subtree.
          commitNestedUnmounts(current);
          detachFiber(current);
        },
        commitWork: function (current, finishedWork) {
          commitContainer(finishedWork);
        },

        commitLifeCycles: commitLifeCycles,
        commitAttachRef: commitAttachRef,
        commitDetachRef: commitDetachRef
      };
    } else if (persistence) {
      invariant(false, 'Persistent reconciler is disabled.');
    } else {
      invariant(false, 'Noop reconciler is disabled.');
    }
  }
  var commitMount = mutation.commitMount,
      commitUpdate = mutation.commitUpdate,
      resetTextContent = mutation.resetTextContent,
      commitTextUpdate = mutation.commitTextUpdate,
      appendChild = mutation.appendChild,
      appendChildToContainer = mutation.appendChildToContainer,
      insertBefore = mutation.insertBefore,
      insertInContainerBefore = mutation.insertInContainerBefore,
      removeChild = mutation.removeChild,
      removeChildFromContainer = mutation.removeChildFromContainer;


  function getHostParentFiber(fiber) {
    var parent = fiber['return'];
    while (parent !== null) {
      if (isHostParent(parent)) {
        return parent;
      }
      parent = parent['return'];
    }
    invariant(false, 'Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.');
  }

  function isHostParent(fiber) {
    return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
  }

  function getHostSibling(fiber) {
    // We're going to search forward into the tree until we find a sibling host
    // node. Unfortunately, if multiple insertions are done in a row we have to
    // search past them. This leads to exponential search for the next sibling.
    var node = fiber;
    siblings: while (true) {
      // If we didn't find anything, let's try the next sibling.
      while (node.sibling === null) {
        if (node['return'] === null || isHostParent(node['return'])) {
          // If we pop out of the root or hit the parent the fiber we are the
          // last sibling.
          return null;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
      while (node.tag !== HostComponent && node.tag !== HostText) {
        // If it is not host node and, we might have a host node inside it.
        // Try to search down until we find one.
        if (node.effectTag & Placement) {
          // If we don't have a child, try the siblings instead.
          continue siblings;
        }
        // If we don't have a child, try the siblings instead.
        // We also skip portals because they are not part of this host tree.
        if (node.child === null || node.tag === HostPortal) {
          continue siblings;
        } else {
          node.child['return'] = node;
          node = node.child;
        }
      }
      // Check if this host node is stable or about to be placed.
      if (!(node.effectTag & Placement)) {
        // Found it!
        return node.stateNode;
      }
    }
  }

  function commitPlacement(finishedWork) {
    // Recursively insert all host nodes into the parent.
    var parentFiber = getHostParentFiber(finishedWork);
    var parent = void 0;
    var isContainer = void 0;
    switch (parentFiber.tag) {
      case HostComponent:
        parent = parentFiber.stateNode;
        isContainer = false;
        break;
      case HostRoot:
        parent = parentFiber.stateNode.containerInfo;
        isContainer = true;
        break;
      case HostPortal:
        parent = parentFiber.stateNode.containerInfo;
        isContainer = true;
        break;
      default:
        invariant(false, 'Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.');
    }
    if (parentFiber.effectTag & ContentReset) {
      // Reset the text content of the parent before doing any insertions
      resetTextContent(parent);
      // Clear ContentReset from the effect tag
      parentFiber.effectTag &= ~ContentReset;
    }

    var before = getHostSibling(finishedWork);
    // We only have the top Fiber that was inserted but we need recurse down its
    // children to find all the terminal nodes.
    var node = finishedWork;
    while (true) {
      if (node.tag === HostComponent || node.tag === HostText) {
        if (before) {
          if (isContainer) {
            insertInContainerBefore(parent, node.stateNode, before);
          } else {
            insertBefore(parent, node.stateNode, before);
          }
        } else {
          if (isContainer) {
            appendChildToContainer(parent, node.stateNode);
          } else {
            appendChild(parent, node.stateNode);
          }
        }
      } else if (node.tag === HostPortal) {
        // If the insertion itself is a portal, then we don't want to traverse
        // down its children. Instead, we'll get insertions from each child in
        // the portal directly.
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === finishedWork) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === finishedWork) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function unmountHostComponents(current) {
    // We only have the top Fiber that was inserted but we need recurse down its
    var node = current;

    // Each iteration, currentParent is populated with node's host parent if not
    // currentParentIsValid.
    var currentParentIsValid = false;
    var currentParent = void 0;
    var currentParentIsContainer = void 0;

    while (true) {
      if (!currentParentIsValid) {
        var parent = node['return'];
        findParent: while (true) {
          !(parent !== null) ? invariant(false, 'Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          switch (parent.tag) {
            case HostComponent:
              currentParent = parent.stateNode;
              currentParentIsContainer = false;
              break findParent;
            case HostRoot:
              currentParent = parent.stateNode.containerInfo;
              currentParentIsContainer = true;
              break findParent;
            case HostPortal:
              currentParent = parent.stateNode.containerInfo;
              currentParentIsContainer = true;
              break findParent;
          }
          parent = parent['return'];
        }
        currentParentIsValid = true;
      }

      if (node.tag === HostComponent || node.tag === HostText) {
        commitNestedUnmounts(node);
        // After all the children have unmounted, it is now safe to remove the
        // node from the tree.
        if (currentParentIsContainer) {
          removeChildFromContainer(currentParent, node.stateNode);
        } else {
          removeChild(currentParent, node.stateNode);
        }
        // Don't visit children because we already visited them.
      } else if (node.tag === HostPortal) {
        // When we go into a portal, it becomes the parent to remove from.
        // We will reassign it back when we pop the portal on the way up.
        currentParent = node.stateNode.containerInfo;
        // Visit children because portals might contain host components.
        if (node.child !== null) {
          node.child['return'] = node;
          node = node.child;
          continue;
        }
      } else {
        commitUnmount(node);
        // Visit children because we may find more host components below.
        if (node.child !== null) {
          node.child['return'] = node;
          node = node.child;
          continue;
        }
      }
      if (node === current) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === current) {
          return;
        }
        node = node['return'];
        if (node.tag === HostPortal) {
          // When we go out of the portal, we need to restore the parent.
          // Since we don't keep a stack of them, we will search for it.
          currentParentIsValid = false;
        }
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function commitDeletion(current) {
    // Recursively delete all host nodes from the parent.
    // Detach refs and call componentWillUnmount() on the whole subtree.
    unmountHostComponents(current);
    detachFiber(current);
  }

  function commitWork(current, finishedWork) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          return;
        }
      case HostComponent:
        {
          var instance = finishedWork.stateNode;
          if (instance != null) {
            // Commit the work prepared earlier.
            var newProps = finishedWork.memoizedProps;
            // For hydration we reuse the update path but we treat the oldProps
            // as the newProps. The updatePayload will contain the real change in
            // this case.
            var oldProps = current !== null ? current.memoizedProps : newProps;
            var type = finishedWork.type;
            // TODO: Type the updateQueue to be specific to host components.
            var updatePayload = finishedWork.updateQueue;
            finishedWork.updateQueue = null;
            if (updatePayload !== null) {
              commitUpdate(instance, updatePayload, type, oldProps, newProps, finishedWork);
            }
          }
          return;
        }
      case HostText:
        {
          !(finishedWork.stateNode !== null) ? invariant(false, 'This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          var textInstance = finishedWork.stateNode;
          var newText = finishedWork.memoizedProps;
          // For hydration we reuse the update path but we treat the oldProps
          // as the newProps. The updatePayload will contain the real change in
          // this case.
          var oldText = current !== null ? current.memoizedProps : newText;
          commitTextUpdate(textInstance, oldText, newText);
          return;
        }
      case HostRoot:
        {
          return;
        }
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitResetTextContent(current) {
    resetTextContent(current.stateNode);
  }

  if (enableMutatingReconciler) {
    return {
      commitResetTextContent: commitResetTextContent,
      commitPlacement: commitPlacement,
      commitDeletion: commitDeletion,
      commitWork: commitWork,
      commitLifeCycles: commitLifeCycles,
      commitAttachRef: commitAttachRef,
      commitDetachRef: commitDetachRef
    };
  } else {
    invariant(false, 'Mutating reconciler is disabled.');
  }
};

var NO_CONTEXT = {};

var ReactFiberHostContext = function (config) {
  var getChildHostContext = config.getChildHostContext,
      getRootHostContext = config.getRootHostContext;


  var contextStackCursor = createCursor(NO_CONTEXT);
  var contextFiberStackCursor = createCursor(NO_CONTEXT);
  var rootInstanceStackCursor = createCursor(NO_CONTEXT);

  function requiredContext(c) {
    !(c !== NO_CONTEXT) ? invariant(false, 'Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    return c;
  }

  function getRootHostContainer() {
    var rootInstance = requiredContext(rootInstanceStackCursor.current);
    return rootInstance;
  }

  function pushHostContainer(fiber, nextRootInstance) {
    // Push current root instance onto the stack;
    // This allows us to reset root when portals are popped.
    push(rootInstanceStackCursor, nextRootInstance, fiber);

    var nextRootContext = getRootHostContext(nextRootInstance);

    // Track the context and the Fiber that provided it.
    // This enables us to pop only Fibers that provide unique contexts.
    push(contextFiberStackCursor, fiber, fiber);
    push(contextStackCursor, nextRootContext, fiber);
  }

  function popHostContainer(fiber) {
    pop(contextStackCursor, fiber);
    pop(contextFiberStackCursor, fiber);
    pop(rootInstanceStackCursor, fiber);
  }

  function getHostContext() {
    var context = requiredContext(contextStackCursor.current);
    return context;
  }

  function pushHostContext(fiber) {
    var rootInstance = requiredContext(rootInstanceStackCursor.current);
    var context = requiredContext(contextStackCursor.current);
    var nextContext = getChildHostContext(context, fiber.type, rootInstance);

    // Don't push this Fiber's context unless it's unique.
    if (context === nextContext) {
      return;
    }

    // Track the context and the Fiber that provided it.
    // This enables us to pop only Fibers that provide unique contexts.
    push(contextFiberStackCursor, fiber, fiber);
    push(contextStackCursor, nextContext, fiber);
  }

  function popHostContext(fiber) {
    // Do not pop unless this Fiber provided the current context.
    // pushHostContext() only pushes Fibers that provide unique contexts.
    if (contextFiberStackCursor.current !== fiber) {
      return;
    }

    pop(contextStackCursor, fiber);
    pop(contextFiberStackCursor, fiber);
  }

  function resetHostContainer() {
    contextStackCursor.current = NO_CONTEXT;
    rootInstanceStackCursor.current = NO_CONTEXT;
  }

  return {
    getHostContext: getHostContext,
    getRootHostContainer: getRootHostContainer,
    popHostContainer: popHostContainer,
    popHostContext: popHostContext,
    pushHostContainer: pushHostContainer,
    pushHostContext: pushHostContext,
    resetHostContainer: resetHostContainer
  };
};

var ReactFiberHydrationContext = function (config) {
  var shouldSetTextContent = config.shouldSetTextContent,
      hydration = config.hydration;

  // If this doesn't have hydration mode.

  if (!hydration) {
    return {
      enterHydrationState: function () {
        return false;
      },
      resetHydrationState: function () {},
      tryToClaimNextHydratableInstance: function () {},
      prepareToHydrateHostInstance: function () {
        invariant(false, 'Expected prepareToHydrateHostInstance() to never be called. This error is likely caused by a bug in React. Please file an issue.');
      },
      prepareToHydrateHostTextInstance: function () {
        invariant(false, 'Expected prepareToHydrateHostTextInstance() to never be called. This error is likely caused by a bug in React. Please file an issue.');
      },
      popHydrationState: function (fiber) {
        return false;
      }
    };
  }

  var canHydrateInstance = hydration.canHydrateInstance,
      canHydrateTextInstance = hydration.canHydrateTextInstance,
      getNextHydratableSibling = hydration.getNextHydratableSibling,
      getFirstHydratableChild = hydration.getFirstHydratableChild,
      hydrateInstance = hydration.hydrateInstance,
      hydrateTextInstance = hydration.hydrateTextInstance,
      didNotMatchHydratedContainerTextInstance = hydration.didNotMatchHydratedContainerTextInstance,
      didNotMatchHydratedTextInstance = hydration.didNotMatchHydratedTextInstance,
      didNotHydrateContainerInstance = hydration.didNotHydrateContainerInstance,
      didNotHydrateInstance = hydration.didNotHydrateInstance,
      didNotFindHydratableContainerInstance = hydration.didNotFindHydratableContainerInstance,
      didNotFindHydratableContainerTextInstance = hydration.didNotFindHydratableContainerTextInstance,
      didNotFindHydratableInstance = hydration.didNotFindHydratableInstance,
      didNotFindHydratableTextInstance = hydration.didNotFindHydratableTextInstance;

  // The deepest Fiber on the stack involved in a hydration context.
  // This may have been an insertion or a hydration.

  var hydrationParentFiber = null;
  var nextHydratableInstance = null;
  var isHydrating = false;

  function enterHydrationState(fiber) {
    var parentInstance = fiber.stateNode.containerInfo;
    nextHydratableInstance = getFirstHydratableChild(parentInstance);
    hydrationParentFiber = fiber;
    isHydrating = true;
    return true;
  }

  function deleteHydratableInstance(returnFiber, instance) {
    {
      switch (returnFiber.tag) {
        case HostRoot:
          didNotHydrateContainerInstance(returnFiber.stateNode.containerInfo, instance);
          break;
        case HostComponent:
          didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance);
          break;
      }
    }

    var childToDelete = createFiberFromHostInstanceForDeletion();
    childToDelete.stateNode = instance;
    childToDelete['return'] = returnFiber;
    childToDelete.effectTag = Deletion;

    // This might seem like it belongs on progressedFirstDeletion. However,
    // these children are not part of the reconciliation list of children.
    // Even if we abort and rereconcile the children, that will try to hydrate
    // again and the nodes are still in the host tree so these will be
    // recreated.
    if (returnFiber.lastEffect !== null) {
      returnFiber.lastEffect.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }
  }

  function insertNonHydratedInstance(returnFiber, fiber) {
    fiber.effectTag |= Placement;
    {
      switch (returnFiber.tag) {
        case HostRoot:
          {
            var parentContainer = returnFiber.stateNode.containerInfo;
            switch (fiber.tag) {
              case HostComponent:
                var type = fiber.type;
                var props = fiber.pendingProps;
                didNotFindHydratableContainerInstance(parentContainer, type, props);
                break;
              case HostText:
                var text = fiber.pendingProps;
                didNotFindHydratableContainerTextInstance(parentContainer, text);
                break;
            }
            break;
          }
        case HostComponent:
          {
            var parentType = returnFiber.type;
            var parentProps = returnFiber.memoizedProps;
            var parentInstance = returnFiber.stateNode;
            switch (fiber.tag) {
              case HostComponent:
                var _type = fiber.type;
                var _props = fiber.pendingProps;
                didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type, _props);
                break;
              case HostText:
                var _text = fiber.pendingProps;
                didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text);
                break;
            }
            break;
          }
        default:
          return;
      }
    }
  }

  function tryHydrate(fiber, nextInstance) {
    switch (fiber.tag) {
      case HostComponent:
        {
          var type = fiber.type;
          var props = fiber.pendingProps;
          var instance = canHydrateInstance(nextInstance, type, props);
          if (instance !== null) {
            fiber.stateNode = instance;
            return true;
          }
          return false;
        }
      case HostText:
        {
          var text = fiber.pendingProps;
          var textInstance = canHydrateTextInstance(nextInstance, text);
          if (textInstance !== null) {
            fiber.stateNode = textInstance;
            return true;
          }
          return false;
        }
      default:
        return false;
    }
  }

  function tryToClaimNextHydratableInstance(fiber) {
    if (!isHydrating) {
      return;
    }
    var nextInstance = nextHydratableInstance;
    if (!nextInstance) {
      // Nothing to hydrate. Make it an insertion.
      insertNonHydratedInstance(hydrationParentFiber, fiber);
      isHydrating = false;
      hydrationParentFiber = fiber;
      return;
    }
    if (!tryHydrate(fiber, nextInstance)) {
      // If we can't hydrate this instance let's try the next one.
      // We use this as a heuristic. It's based on intuition and not data so it
      // might be flawed or unnecessary.
      nextInstance = getNextHydratableSibling(nextInstance);
      if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
        // Nothing to hydrate. Make it an insertion.
        insertNonHydratedInstance(hydrationParentFiber, fiber);
        isHydrating = false;
        hydrationParentFiber = fiber;
        return;
      }
      // We matched the next one, we'll now assume that the first one was
      // superfluous and we'll delete it. Since we can't eagerly delete it
      // we'll have to schedule a deletion. To do that, this node needs a dummy
      // fiber associated with it.
      deleteHydratableInstance(hydrationParentFiber, nextHydratableInstance);
    }
    hydrationParentFiber = fiber;
    nextHydratableInstance = getFirstHydratableChild(nextInstance);
  }

  function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
    var instance = fiber.stateNode;
    var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber);
    // TODO: Type this specific to this type of component.
    fiber.updateQueue = updatePayload;
    // If the update payload indicates that there is a change or if there
    // is a new ref we mark this as an update.
    if (updatePayload !== null) {
      return true;
    }
    return false;
  }

  function prepareToHydrateHostTextInstance(fiber) {
    var textInstance = fiber.stateNode;
    var textContent = fiber.memoizedProps;
    var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
    {
      if (shouldUpdate) {
        // We assume that prepareToHydrateHostTextInstance is called in a context where the
        // hydration parent is the parent host component of this host text.
        var returnFiber = hydrationParentFiber;
        if (returnFiber !== null) {
          switch (returnFiber.tag) {
            case HostRoot:
              {
                var parentContainer = returnFiber.stateNode.containerInfo;
                didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent);
                break;
              }
            case HostComponent:
              {
                var parentType = returnFiber.type;
                var parentProps = returnFiber.memoizedProps;
                var parentInstance = returnFiber.stateNode;
                didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent);
                break;
              }
          }
        }
      }
    }
    return shouldUpdate;
  }

  function popToNextHostParent(fiber) {
    var parent = fiber['return'];
    while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot) {
      parent = parent['return'];
    }
    hydrationParentFiber = parent;
  }

  function popHydrationState(fiber) {
    if (fiber !== hydrationParentFiber) {
      // We're deeper than the current hydration context, inside an inserted
      // tree.
      return false;
    }
    if (!isHydrating) {
      // If we're not currently hydrating but we're in a hydration context, then
      // we were an insertion and now need to pop up reenter hydration of our
      // siblings.
      popToNextHostParent(fiber);
      isHydrating = true;
      return false;
    }

    var type = fiber.type;

    // If we have any remaining hydratable nodes, we need to delete them now.
    // We only do this deeper than head and body since they tend to have random
    // other nodes in them. We also ignore components with pure text content in
    // side of them.
    // TODO: Better heuristic.
    if (fiber.tag !== HostComponent || type !== 'head' && type !== 'body' && !shouldSetTextContent(type, fiber.memoizedProps)) {
      var nextInstance = nextHydratableInstance;
      while (nextInstance) {
        deleteHydratableInstance(fiber, nextInstance);
        nextInstance = getNextHydratableSibling(nextInstance);
      }
    }

    popToNextHostParent(fiber);
    nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
    return true;
  }

  function resetHydrationState() {
    hydrationParentFiber = null;
    nextHydratableInstance = null;
    isHydrating = false;
  }

  return {
    enterHydrationState: enterHydrationState,
    resetHydrationState: resetHydrationState,
    tryToClaimNextHydratableInstance: tryToClaimNextHydratableInstance,
    prepareToHydrateHostInstance: prepareToHydrateHostInstance,
    prepareToHydrateHostTextInstance: prepareToHydrateHostTextInstance,
    popHydrationState: popHydrationState
  };
};

// This lets us hook into Fiber to debug what it's doing.
// See https://github.com/facebook/react/pull/8033.
// This is not part of the public API, not even for React DevTools.
// You may only inject a debugTool if you work on React Fiber itself.
var ReactFiberInstrumentation = {
  debugTool: null
};

var ReactFiberInstrumentation_1 = ReactFiberInstrumentation;

var defaultShowDialog = function (capturedError) {
  return true;
};

var showDialog = defaultShowDialog;

function logCapturedError(capturedError) {
  var logError = showDialog(capturedError);

  // Allow injected showDialog() to prevent default console.error logging.
  // This enables renderers like ReactNative to better manage redbox behavior.
  if (logError === false) {
    return;
  }

  var error = capturedError.error;
  var suppressLogging = error && error.suppressReactErrorLogging;
  if (suppressLogging) {
    return;
  }

  {
    var componentName = capturedError.componentName,
        componentStack = capturedError.componentStack,
        errorBoundaryName = capturedError.errorBoundaryName,
        errorBoundaryFound = capturedError.errorBoundaryFound,
        willRetry = capturedError.willRetry;


    var componentNameMessage = componentName ? 'The above error occurred in the <' + componentName + '> component:' : 'The above error occurred in one of your React components:';

    var errorBoundaryMessage = void 0;
    // errorBoundaryFound check is sufficient; errorBoundaryName check is to satisfy Flow.
    if (errorBoundaryFound && errorBoundaryName) {
      if (willRetry) {
        errorBoundaryMessage = 'React will try to recreate this component tree from scratch ' + ('using the error boundary you provided, ' + errorBoundaryName + '.');
      } else {
        errorBoundaryMessage = 'This error was initially handled by the error boundary ' + errorBoundaryName + '.\n' + 'Recreating the tree from scratch failed so React will unmount the tree.';
      }
    } else {
      errorBoundaryMessage = 'Consider adding an error boundary to your tree to customize error handling behavior.\n' + 'Visit https://fb.me/react-error-boundaries to learn more about error boundaries.';
    }
    var combinedMessage = '' + componentNameMessage + componentStack + '\n\n' + ('' + errorBoundaryMessage);

    // In development, we provide our own message with just the component stack.
    // We don't include the original error message and JS stack because the browser
    // has already printed it. Even if the application swallows the error, it is still
    // displayed by the browser thanks to the DEV-only fake event trick in ReactErrorUtils.
    console.error(combinedMessage);
  }
}

var invokeGuardedCallback$1 = ReactErrorUtils.invokeGuardedCallback;
var hasCaughtError = ReactErrorUtils.hasCaughtError;
var clearCaughtError = ReactErrorUtils.clearCaughtError;


{
  var didWarnAboutStateTransition = false;
  var didWarnSetStateChildContext = false;
  var didWarnStateUpdateForUnmountedComponent = {};

  var warnAboutUpdateOnUnmounted = function (fiber) {
    var componentName = getComponentName(fiber) || 'ReactClass';
    if (didWarnStateUpdateForUnmountedComponent[componentName]) {
      return;
    }
    warning(false, 'Can only update a mounted or mounting ' + 'component. This usually means you called setState, replaceState, ' + 'or forceUpdate on an unmounted component. This is a no-op.\n\nPlease ' + 'check the code for the %s component.', componentName);
    didWarnStateUpdateForUnmountedComponent[componentName] = true;
  };

  var warnAboutInvalidUpdates = function (instance) {
    switch (ReactDebugCurrentFiber.phase) {
      case 'getChildContext':
        if (didWarnSetStateChildContext) {
          return;
        }
        warning(false, 'setState(...): Cannot call setState() inside getChildContext()');
        didWarnSetStateChildContext = true;
        break;
      case 'render':
        if (didWarnAboutStateTransition) {
          return;
        }
        warning(false, 'Cannot update during an existing state transition (such as within ' + "`render` or another component's constructor). Render methods should " + 'be a pure function of props and state; constructor side-effects are ' + 'an anti-pattern, but can be moved to `componentWillMount`.');
        didWarnAboutStateTransition = true;
        break;
    }
  };
}

var ReactFiberScheduler = function (config) {
  var hostContext = ReactFiberHostContext(config);
  var hydrationContext = ReactFiberHydrationContext(config);
  var popHostContainer = hostContext.popHostContainer,
      popHostContext = hostContext.popHostContext,
      resetHostContainer = hostContext.resetHostContainer;

  var _ReactFiberBeginWork = ReactFiberBeginWork(config, hostContext, hydrationContext, scheduleWork, computeExpirationForFiber),
      beginWork = _ReactFiberBeginWork.beginWork,
      beginFailedWork = _ReactFiberBeginWork.beginFailedWork;

  var _ReactFiberCompleteWo = ReactFiberCompleteWork(config, hostContext, hydrationContext),
      completeWork = _ReactFiberCompleteWo.completeWork;

  var _ReactFiberCommitWork = ReactFiberCommitWork(config, captureError),
      commitResetTextContent = _ReactFiberCommitWork.commitResetTextContent,
      commitPlacement = _ReactFiberCommitWork.commitPlacement,
      commitDeletion = _ReactFiberCommitWork.commitDeletion,
      commitWork = _ReactFiberCommitWork.commitWork,
      commitLifeCycles = _ReactFiberCommitWork.commitLifeCycles,
      commitAttachRef = _ReactFiberCommitWork.commitAttachRef,
      commitDetachRef = _ReactFiberCommitWork.commitDetachRef;

  var now = config.now,
      scheduleDeferredCallback = config.scheduleDeferredCallback,
      cancelDeferredCallback = config.cancelDeferredCallback,
      useSyncScheduling = config.useSyncScheduling,
      prepareForCommit = config.prepareForCommit,
      resetAfterCommit = config.resetAfterCommit;

  // Represents the current time in ms.

  var startTime = now();
  var mostRecentCurrentTime = msToExpirationTime(0);

  // Represents the expiration time that incoming updates should use. (If this
  // is NoWork, use the default strategy: async updates in async mode, sync
  // updates in sync mode.)
  var expirationContext = NoWork;

  var isWorking = false;

  // The next work in progress fiber that we're currently working on.
  var nextUnitOfWork = null;
  var nextRoot = null;
  // The time at which we're currently rendering work.
  var nextRenderExpirationTime = NoWork;

  // The next fiber with an effect that we're currently committing.
  var nextEffect = null;

  // Keep track of which fibers have captured an error that need to be handled.
  // Work is removed from this collection after componentDidCatch is called.
  var capturedErrors = null;
  // Keep track of which fibers have failed during the current batch of work.
  // This is a different set than capturedErrors, because it is not reset until
  // the end of the batch. This is needed to propagate errors correctly if a
  // subtree fails more than once.
  var failedBoundaries = null;
  // Error boundaries that captured an error during the current commit.
  var commitPhaseBoundaries = null;
  var firstUncaughtError = null;
  var didFatal = false;

  var isCommitting = false;
  var isUnmounting = false;

  // Used for performance tracking.
  var interruptedBy = null;

  function resetContextStack() {
    // Reset the stack
    reset$1();
    // Reset the cursors
    resetContext();
    resetHostContainer();
  }

  function commitAllHostEffects() {
    while (nextEffect !== null) {
      {
        ReactDebugCurrentFiber.setCurrentFiber(nextEffect);
      }
      recordEffect();

      var effectTag = nextEffect.effectTag;
      if (effectTag & ContentReset) {
        commitResetTextContent(nextEffect);
      }

      if (effectTag & Ref) {
        var current = nextEffect.alternate;
        if (current !== null) {
          commitDetachRef(current);
        }
      }

      // The following switch statement is only concerned about placement,
      // updates, and deletions. To avoid needing to add a case for every
      // possible bitmap value, we remove the secondary effects from the
      // effect tag and switch on that value.
      var primaryEffectTag = effectTag & ~(Callback | Err | ContentReset | Ref | PerformedWork);
      switch (primaryEffectTag) {
        case Placement:
          {
            commitPlacement(nextEffect);
            // Clear the "placement" from effect tag so that we know that this is inserted, before
            // any life-cycles like componentDidMount gets called.
            // TODO: findDOMNode doesn't rely on this any more but isMounted
            // does and isMounted is deprecated anyway so we should be able
            // to kill this.
            nextEffect.effectTag &= ~Placement;
            break;
          }
        case PlacementAndUpdate:
          {
            // Placement
            commitPlacement(nextEffect);
            // Clear the "placement" from effect tag so that we know that this is inserted, before
            // any life-cycles like componentDidMount gets called.
            nextEffect.effectTag &= ~Placement;

            // Update
            var _current = nextEffect.alternate;
            commitWork(_current, nextEffect);
            break;
          }
        case Update:
          {
            var _current2 = nextEffect.alternate;
            commitWork(_current2, nextEffect);
            break;
          }
        case Deletion:
          {
            isUnmounting = true;
            commitDeletion(nextEffect);
            isUnmounting = false;
            break;
          }
      }
      nextEffect = nextEffect.nextEffect;
    }

    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
  }

  function commitAllLifeCycles() {
    while (nextEffect !== null) {
      var effectTag = nextEffect.effectTag;

      if (effectTag & (Update | Callback)) {
        recordEffect();
        var current = nextEffect.alternate;
        commitLifeCycles(current, nextEffect);
      }

      if (effectTag & Ref) {
        recordEffect();
        commitAttachRef(nextEffect);
      }

      if (effectTag & Err) {
        recordEffect();
        commitErrorHandling(nextEffect);
      }

      var next = nextEffect.nextEffect;
      // Ensure that we clean these up so that we don't accidentally keep them.
      // I'm not actually sure this matters because we can't reset firstEffect
      // and lastEffect since they're on every node, not just the effectful
      // ones. So we have to clean everything as we reuse nodes anyway.
      nextEffect.nextEffect = null;
      // Ensure that we reset the effectTag here so that we can rely on effect
      // tags to reason about the current life-cycle.
      nextEffect = next;
    }
  }

  function commitRoot(finishedWork) {
    // We keep track of this so that captureError can collect any boundaries
    // that capture an error during the commit phase. The reason these aren't
    // local to this function is because errors that occur during cWU are
    // captured elsewhere, to prevent the unmount from being interrupted.
    isWorking = true;
    isCommitting = true;
    startCommitTimer();

    var root = finishedWork.stateNode;
    !(root.current !== finishedWork) ? invariant(false, 'Cannot commit the same tree as before. This is probably a bug related to the return field. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    root.isReadyForCommit = false;

    // Reset this to null before calling lifecycles
    ReactCurrentOwner.current = null;

    var firstEffect = void 0;
    if (finishedWork.effectTag > PerformedWork) {
      // A fiber's effect list consists only of its children, not itself. So if
      // the root has an effect, we need to add it to the end of the list. The
      // resulting list is the set that would belong to the root's parent, if
      // it had one; that is, all the effects in the tree including the root.
      if (finishedWork.lastEffect !== null) {
        finishedWork.lastEffect.nextEffect = finishedWork;
        firstEffect = finishedWork.firstEffect;
      } else {
        firstEffect = finishedWork;
      }
    } else {
      // There is no effect on the root.
      firstEffect = finishedWork.firstEffect;
    }

    prepareForCommit();

    // Commit all the side-effects within a tree. We'll do this in two passes.
    // The first pass performs all the host insertions, updates, deletions and
    // ref unmounts.
    nextEffect = firstEffect;
    startCommitHostEffectsTimer();
    while (nextEffect !== null) {
      var didError = false;
      var _error = void 0;
      {
        invokeGuardedCallback$1(null, commitAllHostEffects, null);
        if (hasCaughtError()) {
          didError = true;
          _error = clearCaughtError();
        }
      }
      if (didError) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        captureError(nextEffect, _error);
        // Clean-up
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }
    stopCommitHostEffectsTimer();

    resetAfterCommit();

    // The work-in-progress tree is now the current tree. This must come after
    // the first pass of the commit phase, so that the previous tree is still
    // current during componentWillUnmount, but before the second pass, so that
    // the finished work is current during componentDidMount/Update.
    root.current = finishedWork;

    // In the second pass we'll perform all life-cycles and ref callbacks.
    // Life-cycles happen as a separate pass so that all placements, updates,
    // and deletions in the entire tree have already been invoked.
    // This pass also triggers any renderer-specific initial effects.
    nextEffect = firstEffect;
    startCommitLifeCyclesTimer();
    while (nextEffect !== null) {
      var _didError = false;
      var _error2 = void 0;
      {
        invokeGuardedCallback$1(null, commitAllLifeCycles, null);
        if (hasCaughtError()) {
          _didError = true;
          _error2 = clearCaughtError();
        }
      }
      if (_didError) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        captureError(nextEffect, _error2);
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }

    isCommitting = false;
    isWorking = false;
    stopCommitLifeCyclesTimer();
    stopCommitTimer();
    if (typeof onCommitRoot === 'function') {
      onCommitRoot(finishedWork.stateNode);
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onCommitWork(finishedWork);
    }

    // If we caught any errors during this commit, schedule their boundaries
    // to update.
    if (commitPhaseBoundaries) {
      commitPhaseBoundaries.forEach(scheduleErrorRecovery);
      commitPhaseBoundaries = null;
    }

    if (firstUncaughtError !== null) {
      var _error3 = firstUncaughtError;
      firstUncaughtError = null;
      onUncaughtError(_error3);
    }

    var remainingTime = root.current.expirationTime;

    if (remainingTime === NoWork) {
      capturedErrors = null;
      failedBoundaries = null;
    }

    return remainingTime;
  }

  function resetExpirationTime(workInProgress, renderTime) {
    if (renderTime !== Never && workInProgress.expirationTime === Never) {
      // The children of this component are hidden. Don't bubble their
      // expiration times.
      return;
    }

    // Check for pending updates.
    var newExpirationTime = getUpdateExpirationTime(workInProgress);

    // TODO: Calls need to visit stateNode

    // Bubble up the earliest expiration time.
    var child = workInProgress.child;
    while (child !== null) {
      if (child.expirationTime !== NoWork && (newExpirationTime === NoWork || newExpirationTime > child.expirationTime)) {
        newExpirationTime = child.expirationTime;
      }
      child = child.sibling;
    }
    workInProgress.expirationTime = newExpirationTime;
  }

  function completeUnitOfWork(workInProgress) {
    while (true) {
      // The current, flushed, state of this fiber is the alternate.
      // Ideally nothing should rely on this, but relying on it here
      // means that we don't need an additional field on the work in
      // progress.
      var current = workInProgress.alternate;
      {
        ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
      }
      var next = completeWork(current, workInProgress, nextRenderExpirationTime);
      {
        ReactDebugCurrentFiber.resetCurrentFiber();
      }

      var returnFiber = workInProgress['return'];
      var siblingFiber = workInProgress.sibling;

      resetExpirationTime(workInProgress, nextRenderExpirationTime);

      if (next !== null) {
        stopWorkTimer(workInProgress);
        if (true && ReactFiberInstrumentation_1.debugTool) {
          ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
        }
        // If completing this work spawned new work, do that next. We'll come
        // back here again.
        return next;
      }

      if (returnFiber !== null) {
        // Append all the effects of the subtree and this fiber onto the effect
        // list of the parent. The completion order of the children affects the
        // side-effect order.
        if (returnFiber.firstEffect === null) {
          returnFiber.firstEffect = workInProgress.firstEffect;
        }
        if (workInProgress.lastEffect !== null) {
          if (returnFiber.lastEffect !== null) {
            returnFiber.lastEffect.nextEffect = workInProgress.firstEffect;
          }
          returnFiber.lastEffect = workInProgress.lastEffect;
        }

        // If this fiber had side-effects, we append it AFTER the children's
        // side-effects. We can perform certain side-effects earlier if
        // needed, by doing multiple passes over the effect list. We don't want
        // to schedule our own side-effect on our own list because if end up
        // reusing children we'll schedule this effect onto itself since we're
        // at the end.
        var effectTag = workInProgress.effectTag;
        // Skip both NoWork and PerformedWork tags when creating the effect list.
        // PerformedWork effect is read by React DevTools but shouldn't be committed.
        if (effectTag > PerformedWork) {
          if (returnFiber.lastEffect !== null) {
            returnFiber.lastEffect.nextEffect = workInProgress;
          } else {
            returnFiber.firstEffect = workInProgress;
          }
          returnFiber.lastEffect = workInProgress;
        }
      }

      stopWorkTimer(workInProgress);
      if (true && ReactFiberInstrumentation_1.debugTool) {
        ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
      }

      if (siblingFiber !== null) {
        // If there is more work to do in this returnFiber, do that next.
        return siblingFiber;
      } else if (returnFiber !== null) {
        // If there's no more work in this returnFiber. Complete the returnFiber.
        workInProgress = returnFiber;
        continue;
      } else {
        // We've reached the root.
        var root = workInProgress.stateNode;
        root.isReadyForCommit = true;
        return null;
      }
    }

    // Without this explicit null return Flow complains of invalid return type
    // TODO Remove the above while(true) loop
    // eslint-disable-next-line no-unreachable
    return null;
  }

  function performUnitOfWork(workInProgress) {
    // The current, flushed, state of this fiber is the alternate.
    // Ideally nothing should rely on this, but relying on it here
    // means that we don't need an additional field on the work in
    // progress.
    var current = workInProgress.alternate;

    // See if beginning this work spawns more work.
    startWorkTimer(workInProgress);
    {
      ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
    }

    var next = beginWork(current, workInProgress, nextRenderExpirationTime);
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onBeginWork(workInProgress);
    }

    if (next === null) {
      // If this doesn't spawn new work, complete the current work.
      next = completeUnitOfWork(workInProgress);
    }

    ReactCurrentOwner.current = null;

    return next;
  }

  function performFailedUnitOfWork(workInProgress) {
    // The current, flushed, state of this fiber is the alternate.
    // Ideally nothing should rely on this, but relying on it here
    // means that we don't need an additional field on the work in
    // progress.
    var current = workInProgress.alternate;

    // See if beginning this work spawns more work.
    startWorkTimer(workInProgress);
    {
      ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
    }
    var next = beginFailedWork(current, workInProgress, nextRenderExpirationTime);
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onBeginWork(workInProgress);
    }

    if (next === null) {
      // If this doesn't spawn new work, complete the current work.
      next = completeUnitOfWork(workInProgress);
    }

    ReactCurrentOwner.current = null;

    return next;
  }

  function workLoop(expirationTime) {
    if (capturedErrors !== null) {
      // If there are unhandled errors, switch to the slow work loop.
      // TODO: How to avoid this check in the fast path? Maybe the renderer
      // could keep track of which roots have unhandled errors and call a
      // forked version of renderRoot.
      slowWorkLoopThatChecksForFailedWork(expirationTime);
      return;
    }
    if (nextRenderExpirationTime === NoWork || nextRenderExpirationTime > expirationTime) {
      return;
    }

    if (nextRenderExpirationTime <= mostRecentCurrentTime) {
      // Flush all expired work.
      while (nextUnitOfWork !== null) {
        nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
      }
    } else {
      // Flush asynchronous work until the deadline runs out of time.
      while (nextUnitOfWork !== null && !shouldYield()) {
        nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
      }
    }
  }

  function slowWorkLoopThatChecksForFailedWork(expirationTime) {
    if (nextRenderExpirationTime === NoWork || nextRenderExpirationTime > expirationTime) {
      return;
    }

    if (nextRenderExpirationTime <= mostRecentCurrentTime) {
      // Flush all expired work.
      while (nextUnitOfWork !== null) {
        if (hasCapturedError(nextUnitOfWork)) {
          // Use a forked version of performUnitOfWork
          nextUnitOfWork = performFailedUnitOfWork(nextUnitOfWork);
        } else {
          nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
        }
      }
    } else {
      // Flush asynchronous work until the deadline runs out of time.
      while (nextUnitOfWork !== null && !shouldYield()) {
        if (hasCapturedError(nextUnitOfWork)) {
          // Use a forked version of performUnitOfWork
          nextUnitOfWork = performFailedUnitOfWork(nextUnitOfWork);
        } else {
          nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
        }
      }
    }
  }

  function renderRootCatchBlock(root, failedWork, boundary, expirationTime) {
    // We're going to restart the error boundary that captured the error.
    // Conceptually, we're unwinding the stack. We need to unwind the
    // context stack, too.
    unwindContexts(failedWork, boundary);

    // Restart the error boundary using a forked version of
    // performUnitOfWork that deletes the boundary's children. The entire
    // failed subree will be unmounted. During the commit phase, a special
    // lifecycle method is called on the error boundary, which triggers
    // a re-render.
    nextUnitOfWork = performFailedUnitOfWork(boundary);

    // Continue working.
    workLoop(expirationTime);
  }

  function renderRoot(root, expirationTime) {
    !!isWorking ? invariant(false, 'renderRoot was called recursively. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    isWorking = true;

    // We're about to mutate the work-in-progress tree. If the root was pending
    // commit, it no longer is: we'll need to complete it again.
    root.isReadyForCommit = false;

    // Check if we're starting from a fresh stack, or if we're resuming from
    // previously yielded work.
    if (root !== nextRoot || expirationTime !== nextRenderExpirationTime || nextUnitOfWork === null) {
      // Reset the stack and start working from the root.
      resetContextStack();
      nextRoot = root;
      nextRenderExpirationTime = expirationTime;
      nextUnitOfWork = createWorkInProgress(nextRoot.current, null, expirationTime);
    }

    startWorkLoopTimer(nextUnitOfWork);

    var didError = false;
    var error = null;
    {
      invokeGuardedCallback$1(null, workLoop, null, expirationTime);
      if (hasCaughtError()) {
        didError = true;
        error = clearCaughtError();
      }
    }

    // An error was thrown during the render phase.
    while (didError) {
      if (didFatal) {
        // This was a fatal error. Don't attempt to recover from it.
        firstUncaughtError = error;
        break;
      }

      var failedWork = nextUnitOfWork;
      if (failedWork === null) {
        // An error was thrown but there's no current unit of work. This can
        // happen during the commit phase if there's a bug in the renderer.
        didFatal = true;
        continue;
      }

      // "Capture" the error by finding the nearest boundary. If there is no
      // error boundary, we use the root.
      var boundary = captureError(failedWork, error);
      !(boundary !== null) ? invariant(false, 'Should have found an error boundary. This error is likely caused by a bug in React. Please file an issue.') : void 0;

      if (didFatal) {
        // The error we just captured was a fatal error. This happens
        // when the error propagates to the root more than once.
        continue;
      }

      didError = false;
      error = null;
      {
        invokeGuardedCallback$1(null, renderRootCatchBlock, null, root, failedWork, boundary, expirationTime);
        if (hasCaughtError()) {
          didError = true;
          error = clearCaughtError();
          continue;
        }
      }
      // We're finished working. Exit the error loop.
      break;
    }

    var uncaughtError = firstUncaughtError;

    // We're done performing work. Time to clean up.
    stopWorkLoopTimer(interruptedBy);
    interruptedBy = null;
    isWorking = false;
    didFatal = false;
    firstUncaughtError = null;

    if (uncaughtError !== null) {
      onUncaughtError(uncaughtError);
    }

    return root.isReadyForCommit ? root.current.alternate : null;
  }

  // Returns the boundary that captured the error, or null if the error is ignored
  function captureError(failedWork, error) {
    // It is no longer valid because we exited the user code.
    ReactCurrentOwner.current = null;
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }

    // Search for the nearest error boundary.
    var boundary = null;

    // Passed to logCapturedError()
    var errorBoundaryFound = false;
    var willRetry = false;
    var errorBoundaryName = null;

    // Host containers are a special case. If the failed work itself is a host
    // container, then it acts as its own boundary. In all other cases, we
    // ignore the work itself and only search through the parents.
    if (failedWork.tag === HostRoot) {
      boundary = failedWork;

      if (isFailedBoundary(failedWork)) {
        // If this root already failed, there must have been an error when
        // attempting to unmount it. This is a worst-case scenario and
        // should only be possible if there's a bug in the renderer.
        didFatal = true;
      }
    } else {
      var node = failedWork['return'];
      while (node !== null && boundary === null) {
        if (node.tag === ClassComponent) {
          var instance = node.stateNode;
          if (typeof instance.componentDidCatch === 'function') {
            errorBoundaryFound = true;
            errorBoundaryName = getComponentName(node);

            // Found an error boundary!
            boundary = node;
            willRetry = true;
          }
        } else if (node.tag === HostRoot) {
          // Treat the root like a no-op error boundary
          boundary = node;
        }

        if (isFailedBoundary(node)) {
          // This boundary is already in a failed state.

          // If we're currently unmounting, that means this error was
          // thrown while unmounting a failed subtree. We should ignore
          // the error.
          if (isUnmounting) {
            return null;
          }

          // If we're in the commit phase, we should check to see if
          // this boundary already captured an error during this commit.
          // This case exists because multiple errors can be thrown during
          // a single commit without interruption.
          if (commitPhaseBoundaries !== null && (commitPhaseBoundaries.has(node) || node.alternate !== null && commitPhaseBoundaries.has(node.alternate))) {
            // If so, we should ignore this error.
            return null;
          }

          // The error should propagate to the next boundary -— we keep looking.
          boundary = null;
          willRetry = false;
        }

        node = node['return'];
      }
    }

    if (boundary !== null) {
      // Add to the collection of failed boundaries. This lets us know that
      // subsequent errors in this subtree should propagate to the next boundary.
      if (failedBoundaries === null) {
        failedBoundaries = new Set();
      }
      failedBoundaries.add(boundary);

      // This method is unsafe outside of the begin and complete phases.
      // We might be in the commit phase when an error is captured.
      // The risk is that the return path from this Fiber may not be accurate.
      // That risk is acceptable given the benefit of providing users more context.
      var _componentStack = getStackAddendumByWorkInProgressFiber(failedWork);
      var _componentName = getComponentName(failedWork);

      // Add to the collection of captured errors. This is stored as a global
      // map of errors and their component stack location keyed by the boundaries
      // that capture them. We mostly use this Map as a Set; it's a Map only to
      // avoid adding a field to Fiber to store the error.
      if (capturedErrors === null) {
        capturedErrors = new Map();
      }

      var capturedError = {
        componentName: _componentName,
        componentStack: _componentStack,
        error: error,
        errorBoundary: errorBoundaryFound ? boundary.stateNode : null,
        errorBoundaryFound: errorBoundaryFound,
        errorBoundaryName: errorBoundaryName,
        willRetry: willRetry
      };

      capturedErrors.set(boundary, capturedError);

      try {
        logCapturedError(capturedError);
      } catch (e) {
        // Prevent cycle if logCapturedError() throws.
        // A cycle may still occur if logCapturedError renders a component that throws.
        var suppressLogging = e && e.suppressReactErrorLogging;
        if (!suppressLogging) {
          console.error(e);
        }
      }

      // If we're in the commit phase, defer scheduling an update on the
      // boundary until after the commit is complete
      if (isCommitting) {
        if (commitPhaseBoundaries === null) {
          commitPhaseBoundaries = new Set();
        }
        commitPhaseBoundaries.add(boundary);
      } else {
        // Otherwise, schedule an update now.
        // TODO: Is this actually necessary during the render phase? Is it
        // possible to unwind and continue rendering at the same priority,
        // without corrupting internal state?
        scheduleErrorRecovery(boundary);
      }
      return boundary;
    } else if (firstUncaughtError === null) {
      // If no boundary is found, we'll need to throw the error
      firstUncaughtError = error;
    }
    return null;
  }

  function hasCapturedError(fiber) {
    // TODO: capturedErrors should store the boundary instance, to avoid needing
    // to check the alternate.
    return capturedErrors !== null && (capturedErrors.has(fiber) || fiber.alternate !== null && capturedErrors.has(fiber.alternate));
  }

  function isFailedBoundary(fiber) {
    // TODO: failedBoundaries should store the boundary instance, to avoid
    // needing to check the alternate.
    return failedBoundaries !== null && (failedBoundaries.has(fiber) || fiber.alternate !== null && failedBoundaries.has(fiber.alternate));
  }

  function commitErrorHandling(effectfulFiber) {
    var capturedError = void 0;
    if (capturedErrors !== null) {
      capturedError = capturedErrors.get(effectfulFiber);
      capturedErrors['delete'](effectfulFiber);
      if (capturedError == null) {
        if (effectfulFiber.alternate !== null) {
          effectfulFiber = effectfulFiber.alternate;
          capturedError = capturedErrors.get(effectfulFiber);
          capturedErrors['delete'](effectfulFiber);
        }
      }
    }

    !(capturedError != null) ? invariant(false, 'No error for given unit of work. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    switch (effectfulFiber.tag) {
      case ClassComponent:
        var instance = effectfulFiber.stateNode;

        var info = {
          componentStack: capturedError.componentStack
        };

        // Allow the boundary to handle the error, usually by scheduling
        // an update to itself
        instance.componentDidCatch(capturedError.error, info);
        return;
      case HostRoot:
        if (firstUncaughtError === null) {
          firstUncaughtError = capturedError.error;
        }
        return;
      default:
        invariant(false, 'Invalid type of work. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  function unwindContexts(from, to) {
    var node = from;
    while (node !== null) {
      switch (node.tag) {
        case ClassComponent:
          popContextProvider(node);
          break;
        case HostComponent:
          popHostContext(node);
          break;
        case HostRoot:
          popHostContainer(node);
          break;
        case HostPortal:
          popHostContainer(node);
          break;
      }
      if (node === to || node.alternate === to) {
        stopFailedWorkTimer(node);
        break;
      } else {
        stopWorkTimer(node);
      }
      node = node['return'];
    }
  }

  function computeAsyncExpiration() {
    // Given the current clock time, returns an expiration time. We use rounding
    // to batch like updates together.
    // Should complete within ~1000ms. 1200ms max.
    var currentTime = recalculateCurrentTime();
    var expirationMs = 1000;
    var bucketSizeMs = 200;
    return computeExpirationBucket(currentTime, expirationMs, bucketSizeMs);
  }

  function computeExpirationForFiber(fiber) {
    var expirationTime = void 0;
    if (expirationContext !== NoWork) {
      // An explicit expiration context was set;
      expirationTime = expirationContext;
    } else if (isWorking) {
      if (isCommitting) {
        // Updates that occur during the commit phase should have sync priority
        // by default.
        expirationTime = Sync;
      } else {
        // Updates during the render phase should expire at the same time as
        // the work that is being rendered.
        expirationTime = nextRenderExpirationTime;
      }
    } else {
      // No explicit expiration context was set, and we're not currently
      // performing work. Calculate a new expiration time.
      if (useSyncScheduling && !(fiber.internalContextTag & AsyncUpdates)) {
        // This is a sync update
        expirationTime = Sync;
      } else {
        // This is an async update
        expirationTime = computeAsyncExpiration();
      }
    }
    return expirationTime;
  }

  function scheduleWork(fiber, expirationTime) {
    return scheduleWorkImpl(fiber, expirationTime, false);
  }

  function checkRootNeedsClearing(root, fiber, expirationTime) {
    if (!isWorking && root === nextRoot && expirationTime < nextRenderExpirationTime) {
      // Restart the root from the top.
      if (nextUnitOfWork !== null) {
        // This is an interruption. (Used for performance tracking.)
        interruptedBy = fiber;
      }
      nextRoot = null;
      nextUnitOfWork = null;
      nextRenderExpirationTime = NoWork;
    }
  }

  function scheduleWorkImpl(fiber, expirationTime, isErrorRecovery) {
    recordScheduleUpdate();

    {
      if (!isErrorRecovery && fiber.tag === ClassComponent) {
        var instance = fiber.stateNode;
        warnAboutInvalidUpdates(instance);
      }
    }

    var node = fiber;
    while (node !== null) {
      // Walk the parent path to the root and update each node's
      // expiration time.
      if (node.expirationTime === NoWork || node.expirationTime > expirationTime) {
        node.expirationTime = expirationTime;
      }
      if (node.alternate !== null) {
        if (node.alternate.expirationTime === NoWork || node.alternate.expirationTime > expirationTime) {
          node.alternate.expirationTime = expirationTime;
        }
      }
      if (node['return'] === null) {
        if (node.tag === HostRoot) {
          var root = node.stateNode;

          checkRootNeedsClearing(root, fiber, expirationTime);
          requestWork(root, expirationTime);
          checkRootNeedsClearing(root, fiber, expirationTime);
        } else {
          {
            if (!isErrorRecovery && fiber.tag === ClassComponent) {
              warnAboutUpdateOnUnmounted(fiber);
            }
          }
          return;
        }
      }
      node = node['return'];
    }
  }

  function scheduleErrorRecovery(fiber) {
    scheduleWorkImpl(fiber, Sync, true);
  }

  function recalculateCurrentTime() {
    // Subtract initial time so it fits inside 32bits
    var ms = now() - startTime;
    mostRecentCurrentTime = msToExpirationTime(ms);
    return mostRecentCurrentTime;
  }

  function deferredUpdates(fn) {
    var previousExpirationContext = expirationContext;
    expirationContext = computeAsyncExpiration();
    try {
      return fn();
    } finally {
      expirationContext = previousExpirationContext;
    }
  }

  function syncUpdates(fn) {
    var previousExpirationContext = expirationContext;
    expirationContext = Sync;
    try {
      return fn();
    } finally {
      expirationContext = previousExpirationContext;
    }
  }

  // TODO: Everything below this is written as if it has been lifted to the
  // renderers. I'll do this in a follow-up.

  // Linked-list of roots
  var firstScheduledRoot = null;
  var lastScheduledRoot = null;

  var callbackExpirationTime = NoWork;
  var callbackID = -1;
  var isRendering = false;
  var nextFlushedRoot = null;
  var nextFlushedExpirationTime = NoWork;
  var deadlineDidExpire = false;
  var hasUnhandledError = false;
  var unhandledError = null;
  var deadline = null;

  var isBatchingUpdates = false;
  var isUnbatchingUpdates = false;

  // Use these to prevent an infinite loop of nested updates
  var NESTED_UPDATE_LIMIT = 1000;
  var nestedUpdateCount = 0;

  var timeHeuristicForUnitOfWork = 1;

  function scheduleCallbackWithExpiration(expirationTime) {
    if (callbackExpirationTime !== NoWork) {
      // A callback is already scheduled. Check its expiration time (timeout).
      if (expirationTime > callbackExpirationTime) {
        // Existing callback has sufficient timeout. Exit.
        return;
      } else {
        // Existing callback has insufficient timeout. Cancel and schedule a
        // new one.
        cancelDeferredCallback(callbackID);
      }
      // The request callback timer is already running. Don't start a new one.
    } else {
      startRequestCallbackTimer();
    }

    // Compute a timeout for the given expiration time.
    var currentMs = now() - startTime;
    var expirationMs = expirationTimeToMs(expirationTime);
    var timeout = expirationMs - currentMs;

    callbackExpirationTime = expirationTime;
    callbackID = scheduleDeferredCallback(performAsyncWork, { timeout: timeout });
  }

  // requestWork is called by the scheduler whenever a root receives an update.
  // It's up to the renderer to call renderRoot at some point in the future.
  function requestWork(root, expirationTime) {
    if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
      invariant(false, 'Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.');
    }

    // Add the root to the schedule.
    // Check if this root is already part of the schedule.
    if (root.nextScheduledRoot === null) {
      // This root is not already scheduled. Add it.
      root.remainingExpirationTime = expirationTime;
      if (lastScheduledRoot === null) {
        firstScheduledRoot = lastScheduledRoot = root;
        root.nextScheduledRoot = root;
      } else {
        lastScheduledRoot.nextScheduledRoot = root;
        lastScheduledRoot = root;
        lastScheduledRoot.nextScheduledRoot = firstScheduledRoot;
      }
    } else {
      // This root is already scheduled, but its priority may have increased.
      var remainingExpirationTime = root.remainingExpirationTime;
      if (remainingExpirationTime === NoWork || expirationTime < remainingExpirationTime) {
        // Update the priority.
        root.remainingExpirationTime = expirationTime;
      }
    }

    if (isRendering) {
      // Prevent reentrancy. Remaining work will be scheduled at the end of
      // the currently rendering batch.
      return;
    }

    if (isBatchingUpdates) {
      // Flush work at the end of the batch.
      if (isUnbatchingUpdates) {
        // ...unless we're inside unbatchedUpdates, in which case we should
        // flush it now.
        nextFlushedRoot = root;
        nextFlushedExpirationTime = Sync;
        performWorkOnRoot(nextFlushedRoot, nextFlushedExpirationTime);
      }
      return;
    }

    // TODO: Get rid of Sync and use current time?
    if (expirationTime === Sync) {
      performWork(Sync, null);
    } else {
      scheduleCallbackWithExpiration(expirationTime);
    }
  }

  function findHighestPriorityRoot() {
    var highestPriorityWork = NoWork;
    var highestPriorityRoot = null;

    if (lastScheduledRoot !== null) {
      var previousScheduledRoot = lastScheduledRoot;
      var root = firstScheduledRoot;
      while (root !== null) {
        var remainingExpirationTime = root.remainingExpirationTime;
        if (remainingExpirationTime === NoWork) {
          // This root no longer has work. Remove it from the scheduler.

          // TODO: This check is redudant, but Flow is confused by the branch
          // below where we set lastScheduledRoot to null, even though we break
          // from the loop right after.
          !(previousScheduledRoot !== null && lastScheduledRoot !== null) ? invariant(false, 'Should have a previous and last root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          if (root === root.nextScheduledRoot) {
            // This is the only root in the list.
            root.nextScheduledRoot = null;
            firstScheduledRoot = lastScheduledRoot = null;
            break;
          } else if (root === firstScheduledRoot) {
            // This is the first root in the list.
            var next = root.nextScheduledRoot;
            firstScheduledRoot = next;
            lastScheduledRoot.nextScheduledRoot = next;
            root.nextScheduledRoot = null;
          } else if (root === lastScheduledRoot) {
            // This is the last root in the list.
            lastScheduledRoot = previousScheduledRoot;
            lastScheduledRoot.nextScheduledRoot = firstScheduledRoot;
            root.nextScheduledRoot = null;
            break;
          } else {
            previousScheduledRoot.nextScheduledRoot = root.nextScheduledRoot;
            root.nextScheduledRoot = null;
          }
          root = previousScheduledRoot.nextScheduledRoot;
        } else {
          if (highestPriorityWork === NoWork || remainingExpirationTime < highestPriorityWork) {
            // Update the priority, if it's higher
            highestPriorityWork = remainingExpirationTime;
            highestPriorityRoot = root;
          }
          if (root === lastScheduledRoot) {
            break;
          }
          previousScheduledRoot = root;
          root = root.nextScheduledRoot;
        }
      }
    }

    // If the next root is the same as the previous root, this is a nested
    // update. To prevent an infinite loop, increment the nested update count.
    var previousFlushedRoot = nextFlushedRoot;
    if (previousFlushedRoot !== null && previousFlushedRoot === highestPriorityRoot) {
      nestedUpdateCount++;
    } else {
      // Reset whenever we switch roots.
      nestedUpdateCount = 0;
    }
    nextFlushedRoot = highestPriorityRoot;
    nextFlushedExpirationTime = highestPriorityWork;
  }

  function performAsyncWork(dl) {
    performWork(NoWork, dl);
  }

  function performWork(minExpirationTime, dl) {
    deadline = dl;

    // Keep working on roots until there's no more work, or until the we reach
    // the deadline.
    findHighestPriorityRoot();

    if (enableUserTimingAPI && deadline !== null) {
      var didExpire = nextFlushedExpirationTime < recalculateCurrentTime();
      stopRequestCallbackTimer(didExpire);
    }

    while (nextFlushedRoot !== null && nextFlushedExpirationTime !== NoWork && (minExpirationTime === NoWork || nextFlushedExpirationTime <= minExpirationTime) && !deadlineDidExpire) {
      performWorkOnRoot(nextFlushedRoot, nextFlushedExpirationTime);
      // Find the next highest priority work.
      findHighestPriorityRoot();
    }

    // We're done flushing work. Either we ran out of time in this callback,
    // or there's no more work left with sufficient priority.

    // If we're inside a callback, set this to false since we just completed it.
    if (deadline !== null) {
      callbackExpirationTime = NoWork;
      callbackID = -1;
    }
    // If there's work left over, schedule a new callback.
    if (nextFlushedExpirationTime !== NoWork) {
      scheduleCallbackWithExpiration(nextFlushedExpirationTime);
    }

    // Clean-up.
    deadline = null;
    deadlineDidExpire = false;
    nestedUpdateCount = 0;

    if (hasUnhandledError) {
      var _error4 = unhandledError;
      unhandledError = null;
      hasUnhandledError = false;
      throw _error4;
    }
  }

  function performWorkOnRoot(root, expirationTime) {
    !!isRendering ? invariant(false, 'performWorkOnRoot was called recursively. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    isRendering = true;

    // Check if this is async work or sync/expired work.
    // TODO: Pass current time as argument to renderRoot, commitRoot
    if (expirationTime <= recalculateCurrentTime()) {
      // Flush sync work.
      var finishedWork = root.finishedWork;
      if (finishedWork !== null) {
        // This root is already complete. We can commit it.
        root.finishedWork = null;
        root.remainingExpirationTime = commitRoot(finishedWork);
      } else {
        root.finishedWork = null;
        finishedWork = renderRoot(root, expirationTime);
        if (finishedWork !== null) {
          // We've completed the root. Commit it.
          root.remainingExpirationTime = commitRoot(finishedWork);
        }
      }
    } else {
      // Flush async work.
      var _finishedWork = root.finishedWork;
      if (_finishedWork !== null) {
        // This root is already complete. We can commit it.
        root.finishedWork = null;
        root.remainingExpirationTime = commitRoot(_finishedWork);
      } else {
        root.finishedWork = null;
        _finishedWork = renderRoot(root, expirationTime);
        if (_finishedWork !== null) {
          // We've completed the root. Check the deadline one more time
          // before committing.
          if (!shouldYield()) {
            // Still time left. Commit the root.
            root.remainingExpirationTime = commitRoot(_finishedWork);
          } else {
            // There's no time left. Mark this root as complete. We'll come
            // back and commit it later.
            root.finishedWork = _finishedWork;
          }
        }
      }
    }

    isRendering = false;
  }

  // When working on async work, the reconciler asks the renderer if it should
  // yield execution. For DOM, we implement this with requestIdleCallback.
  function shouldYield() {
    if (deadline === null) {
      return false;
    }
    if (deadline.timeRemaining() > timeHeuristicForUnitOfWork) {
      // Disregard deadline.didTimeout. Only expired work should be flushed
      // during a timeout. This path is only hit for non-expired work.
      return false;
    }
    deadlineDidExpire = true;
    return true;
  }

  // TODO: Not happy about this hook. Conceptually, renderRoot should return a
  // tuple of (isReadyForCommit, didError, error)
  function onUncaughtError(error) {
    !(nextFlushedRoot !== null) ? invariant(false, 'Should be working on a root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    // Unschedule this root so we don't work on it again until there's
    // another update.
    nextFlushedRoot.remainingExpirationTime = NoWork;
    if (!hasUnhandledError) {
      hasUnhandledError = true;
      unhandledError = error;
    }
  }

  // TODO: Batching should be implemented at the renderer level, not inside
  // the reconciler.
  function batchedUpdates(fn, a) {
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      return fn(a);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      if (!isBatchingUpdates && !isRendering) {
        performWork(Sync, null);
      }
    }
  }

  // TODO: Batching should be implemented at the renderer level, not inside
  // the reconciler.
  function unbatchedUpdates(fn) {
    if (isBatchingUpdates && !isUnbatchingUpdates) {
      isUnbatchingUpdates = true;
      try {
        return fn();
      } finally {
        isUnbatchingUpdates = false;
      }
    }
    return fn();
  }

  // TODO: Batching should be implemented at the renderer level, not within
  // the reconciler.
  function flushSync(fn) {
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      return syncUpdates(fn);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      !!isRendering ? invariant(false, 'flushSync was called from inside a lifecycle method. It cannot be called when React is already rendering.') : void 0;
      performWork(Sync, null);
    }
  }

  return {
    computeAsyncExpiration: computeAsyncExpiration,
    computeExpirationForFiber: computeExpirationForFiber,
    scheduleWork: scheduleWork,
    batchedUpdates: batchedUpdates,
    unbatchedUpdates: unbatchedUpdates,
    flushSync: flushSync,
    deferredUpdates: deferredUpdates
  };
};

{
  var didWarnAboutNestedUpdates = false;
}

// 0 is PROD, 1 is DEV.
// Might add PROFILE later.


function getContextForSubtree(parentComponent) {
  if (!parentComponent) {
    return emptyObject;
  }

  var fiber = get(parentComponent);
  var parentContext = findCurrentUnmaskedContext(fiber);
  return isContextProvider(fiber) ? processChildContext(fiber, parentContext) : parentContext;
}

var ReactFiberReconciler$1 = function (config) {
  var getPublicInstance = config.getPublicInstance;

  var _ReactFiberScheduler = ReactFiberScheduler(config),
      computeAsyncExpiration = _ReactFiberScheduler.computeAsyncExpiration,
      computeExpirationForFiber = _ReactFiberScheduler.computeExpirationForFiber,
      scheduleWork = _ReactFiberScheduler.scheduleWork,
      batchedUpdates = _ReactFiberScheduler.batchedUpdates,
      unbatchedUpdates = _ReactFiberScheduler.unbatchedUpdates,
      flushSync = _ReactFiberScheduler.flushSync,
      deferredUpdates = _ReactFiberScheduler.deferredUpdates;

  function scheduleTopLevelUpdate(current, element, callback) {
    {
      if (ReactDebugCurrentFiber.phase === 'render' && ReactDebugCurrentFiber.current !== null && !didWarnAboutNestedUpdates) {
        didWarnAboutNestedUpdates = true;
        warning(false, 'Render methods should be a pure function of props and state; ' + 'triggering nested component updates from render is not allowed. ' + 'If necessary, trigger nested updates in componentDidUpdate.\n\n' + 'Check the render method of %s.', getComponentName(ReactDebugCurrentFiber.current) || 'Unknown');
      }
    }

    callback = callback === undefined ? null : callback;
    {
      warning(callback === null || typeof callback === 'function', 'render(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callback);
    }

    var expirationTime = void 0;
    // Check if the top-level element is an async wrapper component. If so,
    // treat updates to the root as async. This is a bit weird but lets us
    // avoid a separate `renderAsync` API.
    if (enableAsyncSubtreeAPI && element != null && element.type != null && element.type.prototype != null && element.type.prototype.unstable_isAsyncReactComponent === true) {
      expirationTime = computeAsyncExpiration();
    } else {
      expirationTime = computeExpirationForFiber(current);
    }

    var update = {
      expirationTime: expirationTime,
      partialState: { element: element },
      callback: callback,
      isReplace: false,
      isForced: false,
      nextCallback: null,
      next: null
    };
    insertUpdateIntoFiber(current, update);
    scheduleWork(current, expirationTime);
  }

  function findHostInstance(fiber) {
    var hostFiber = findCurrentHostFiber(fiber);
    if (hostFiber === null) {
      return null;
    }
    return hostFiber.stateNode;
  }

  return {
    createContainer: function (containerInfo, hydrate) {
      return createFiberRoot(containerInfo, hydrate);
    },
    updateContainer: function (element, container, parentComponent, callback) {
      // TODO: If this is a nested container, this won't be the root.
      var current = container.current;

      {
        if (ReactFiberInstrumentation_1.debugTool) {
          if (current.alternate === null) {
            ReactFiberInstrumentation_1.debugTool.onMountContainer(container);
          } else if (element === null) {
            ReactFiberInstrumentation_1.debugTool.onUnmountContainer(container);
          } else {
            ReactFiberInstrumentation_1.debugTool.onUpdateContainer(container);
          }
        }
      }

      var context = getContextForSubtree(parentComponent);
      if (container.context === null) {
        container.context = context;
      } else {
        container.pendingContext = context;
      }

      scheduleTopLevelUpdate(current, element, callback);
    },


    batchedUpdates: batchedUpdates,

    unbatchedUpdates: unbatchedUpdates,

    deferredUpdates: deferredUpdates,

    flushSync: flushSync,

    getPublicRootInstance: function (container) {
      var containerFiber = container.current;
      if (!containerFiber.child) {
        return null;
      }
      switch (containerFiber.child.tag) {
        case HostComponent:
          return getPublicInstance(containerFiber.child.stateNode);
        default:
          return containerFiber.child.stateNode;
      }
    },


    findHostInstance: findHostInstance,

    findHostInstanceWithNoPortals: function (fiber) {
      var hostFiber = findCurrentHostFiberWithNoPortals(fiber);
      if (hostFiber === null) {
        return null;
      }
      return hostFiber.stateNode;
    },
    injectIntoDevTools: function (devToolsConfig) {
      var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;

      return injectInternals(_assign({}, devToolsConfig, {
        findHostInstanceByFiber: function (fiber) {
          return findHostInstance(fiber);
        },
        findFiberByHostInstance: function (instance) {
          if (!findFiberByHostInstance) {
            // Might not be implemented by the renderer.
            return null;
          }
          return findFiberByHostInstance(instance);
        }
      }));
    }
  };
};

var ReactFiberReconciler$2 = Object.freeze({
	default: ReactFiberReconciler$1
});

var ReactFiberReconciler$3 = ( ReactFiberReconciler$2 && ReactFiberReconciler$1 ) || ReactFiberReconciler$2;

// TODO: bundle Flow types with the package.



// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var reactReconciler = ReactFiberReconciler$3['default'] ? ReactFiberReconciler$3['default'] : ReactFiberReconciler$3;

function createPortal$1(children, containerInfo,
// TODO: figure out the API for cross-renderer implementation.
implementation) {
  var key = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;

  return {
    // This tag allow us to uniquely identify this as a React Portal
    $$typeof: REACT_PORTAL_TYPE,
    key: key == null ? null : '' + key,
    children: children,
    containerInfo: containerInfo,
    implementation: implementation
  };
}

// TODO: this is special because it gets imported during build.

var ReactVersion = '16.2.0';

// a requestAnimationFrame, storing the time for the start of the frame, then
// scheduling a postMessage which gets scheduled after paint. Within the
// postMessage handler do as much work as possible until time + frame rate.
// By separating the idle call into a separate event tick we ensure that
// layout, paint and other browser work is counted against the available time.
// The frame rate is dynamically adjusted.

{
  if (ExecutionEnvironment.canUseDOM && typeof requestAnimationFrame !== 'function') {
    warning(false, 'React depends on requestAnimationFrame. Make sure that you load a ' + 'polyfill in older browsers. http://fb.me/react-polyfills');
  }
}

var hasNativePerformanceNow = typeof performance === 'object' && typeof performance.now === 'function';

var now = void 0;
if (hasNativePerformanceNow) {
  now = function () {
    return performance.now();
  };
} else {
  now = function () {
    return Date.now();
  };
}

// TODO: There's no way to cancel, because Fiber doesn't atm.
var rIC = void 0;
var cIC = void 0;

if (!ExecutionEnvironment.canUseDOM) {
  rIC = function (frameCallback) {
    return setTimeout(function () {
      frameCallback({
        timeRemaining: function () {
          return Infinity;
        }
      });
    });
  };
  cIC = function (timeoutID) {
    clearTimeout(timeoutID);
  };
} else if (typeof requestIdleCallback !== 'function' || typeof cancelIdleCallback !== 'function') {
  // Polyfill requestIdleCallback and cancelIdleCallback

  var scheduledRICCallback = null;
  var isIdleScheduled = false;
  var timeoutTime = -1;

  var isAnimationFrameScheduled = false;

  var frameDeadline = 0;
  // We start out assuming that we run at 30fps but then the heuristic tracking
  // will adjust this value to a faster fps if we get more frequent animation
  // frames.
  var previousFrameTime = 33;
  var activeFrameTime = 33;

  var frameDeadlineObject;
  if (hasNativePerformanceNow) {
    frameDeadlineObject = {
      didTimeout: false,
      timeRemaining: function () {
        // We assume that if we have a performance timer that the rAF callback
        // gets a performance timer value. Not sure if this is always true.
        var remaining = frameDeadline - performance.now();
        return remaining > 0 ? remaining : 0;
      }
    };
  } else {
    frameDeadlineObject = {
      didTimeout: false,
      timeRemaining: function () {
        // Fallback to Date.now()
        var remaining = frameDeadline - Date.now();
        return remaining > 0 ? remaining : 0;
      }
    };
  }

  // We use the postMessage trick to defer idle work until after the repaint.
  var messageKey = '__reactIdleCallback$' + Math.random().toString(36).slice(2);
  var idleTick = function (event) {
    if (event.source !== window || event.data !== messageKey) {
      return;
    }

    isIdleScheduled = false;

    var currentTime = now();
    if (frameDeadline - currentTime <= 0) {
      // There's no time left in this idle period. Check if the callback has
      // a timeout and whether it's been exceeded.
      if (timeoutTime !== -1 && timeoutTime <= currentTime) {
        // Exceeded the timeout. Invoke the callback even though there's no
        // time left.
        frameDeadlineObject.didTimeout = true;
      } else {
        // No timeout.
        if (!isAnimationFrameScheduled) {
          // Schedule another animation callback so we retry later.
          isAnimationFrameScheduled = true;
          requestAnimationFrame(animationTick);
        }
        // Exit without invoking the callback.
        return;
      }
    } else {
      // There's still time left in this idle period.
      frameDeadlineObject.didTimeout = false;
    }

    timeoutTime = -1;
    var callback = scheduledRICCallback;
    scheduledRICCallback = null;
    if (callback !== null) {
      callback(frameDeadlineObject);
    }
  };
  // Assumes that we have addEventListener in this environment. Might need
  // something better for old IE.
  window.addEventListener('message', idleTick, false);

  var animationTick = function (rafTime) {
    isAnimationFrameScheduled = false;
    var nextFrameTime = rafTime - frameDeadline + activeFrameTime;
    if (nextFrameTime < activeFrameTime && previousFrameTime < activeFrameTime) {
      if (nextFrameTime < 8) {
        // Defensive coding. We don't support higher frame rates than 120hz.
        // If we get lower than that, it is probably a bug.
        nextFrameTime = 8;
      }
      // If one frame goes long, then the next one can be short to catch up.
      // If two frames are short in a row, then that's an indication that we
      // actually have a higher frame rate than what we're currently optimizing.
      // We adjust our heuristic dynamically accordingly. For example, if we're
      // running on 120hz display or 90hz VR display.
      // Take the max of the two in case one of them was an anomaly due to
      // missed frame deadlines.
      activeFrameTime = nextFrameTime < previousFrameTime ? previousFrameTime : nextFrameTime;
    } else {
      previousFrameTime = nextFrameTime;
    }
    frameDeadline = rafTime + activeFrameTime;
    if (!isIdleScheduled) {
      isIdleScheduled = true;
      window.postMessage(messageKey, '*');
    }
  };

  rIC = function (callback, options) {
    // This assumes that we only schedule one callback at a time because that's
    // how Fiber uses it.
    scheduledRICCallback = callback;
    if (options != null && typeof options.timeout === 'number') {
      timeoutTime = now() + options.timeout;
    }
    if (!isAnimationFrameScheduled) {
      // If rAF didn't already schedule one, we need to schedule a frame.
      // TODO: If this rAF doesn't materialize because the browser throttles, we
      // might want to still have setTimeout trigger rIC as a backup to ensure
      // that we keep performing work.
      isAnimationFrameScheduled = true;
      requestAnimationFrame(animationTick);
    }
    return 0;
  };

  cIC = function () {
    scheduledRICCallback = null;
    isIdleScheduled = false;
    timeoutTime = -1;
  };
} else {
  rIC = window.requestIdleCallback;
  cIC = window.cancelIdleCallback;
}

/**
 * Forked from fbjs/warning:
 * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
 *
 * Only change is we use console.warn instead of console.error,
 * and do nothing when 'console' is not supported.
 * This really simplifies the code.
 * ---
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var lowPriorityWarning = function () {};

{
  var printWarning = function (format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.warn(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  lowPriorityWarning = function (condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }
    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

var lowPriorityWarning$1 = lowPriorityWarning;

// isAttributeNameSafe() is currently duplicated in DOMMarkupOperations.
// TODO: Find a better place for this.
var VALID_ATTRIBUTE_NAME_REGEX = new RegExp('^[' + ATTRIBUTE_NAME_START_CHAR + '][' + ATTRIBUTE_NAME_CHAR + ']*$');
var illegalAttributeNameCache = {};
var validatedAttributeNameCache = {};
function isAttributeNameSafe(attributeName) {
  if (validatedAttributeNameCache.hasOwnProperty(attributeName)) {
    return true;
  }
  if (illegalAttributeNameCache.hasOwnProperty(attributeName)) {
    return false;
  }
  if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
    validatedAttributeNameCache[attributeName] = true;
    return true;
  }
  illegalAttributeNameCache[attributeName] = true;
  {
    warning(false, 'Invalid attribute name: `%s`', attributeName);
  }
  return false;
}

// shouldIgnoreValue() is currently duplicated in DOMMarkupOperations.
// TODO: Find a better place for this.
function shouldIgnoreValue(propertyInfo, value) {
  return value == null || propertyInfo.hasBooleanValue && !value || propertyInfo.hasNumericValue && isNaN(value) || propertyInfo.hasPositiveNumericValue && value < 1 || propertyInfo.hasOverloadedBooleanValue && value === false;
}

/**
 * Operations for dealing with DOM properties.
 */





/**
 * Get the value for a property on a node. Only used in DEV for SSR validation.
 * The "expected" argument is used as a hint of what the expected value is.
 * Some properties have multiple equivalent values.
 */
function getValueForProperty(node, name, expected) {
  {
    var propertyInfo = getPropertyInfo(name);
    if (propertyInfo) {
      var mutationMethod = propertyInfo.mutationMethod;
      if (mutationMethod || propertyInfo.mustUseProperty) {
        return node[propertyInfo.propertyName];
      } else {
        var attributeName = propertyInfo.attributeName;

        var stringValue = null;

        if (propertyInfo.hasOverloadedBooleanValue) {
          if (node.hasAttribute(attributeName)) {
            var value = node.getAttribute(attributeName);
            if (value === '') {
              return true;
            }
            if (shouldIgnoreValue(propertyInfo, expected)) {
              return value;
            }
            if (value === '' + expected) {
              return expected;
            }
            return value;
          }
        } else if (node.hasAttribute(attributeName)) {
          if (shouldIgnoreValue(propertyInfo, expected)) {
            // We had an attribute but shouldn't have had one, so read it
            // for the error message.
            return node.getAttribute(attributeName);
          }
          if (propertyInfo.hasBooleanValue) {
            // If this was a boolean, it doesn't matter what the value is
            // the fact that we have it is the same as the expected.
            return expected;
          }
          // Even if this property uses a namespace we use getAttribute
          // because we assume its namespaced name is the same as our config.
          // To use getAttributeNS we need the local name which we don't have
          // in our config atm.
          stringValue = node.getAttribute(attributeName);
        }

        if (shouldIgnoreValue(propertyInfo, expected)) {
          return stringValue === null ? expected : stringValue;
        } else if (stringValue === '' + expected) {
          return expected;
        } else {
          return stringValue;
        }
      }
    }
  }
}

/**
 * Get the value for a attribute on a node. Only used in DEV for SSR validation.
 * The third argument is used as a hint of what the expected value is. Some
 * attributes have multiple equivalent values.
 */
function getValueForAttribute(node, name, expected) {
  {
    if (!isAttributeNameSafe(name)) {
      return;
    }
    if (!node.hasAttribute(name)) {
      return expected === undefined ? undefined : null;
    }
    var value = node.getAttribute(name);
    if (value === '' + expected) {
      return expected;
    }
    return value;
  }
}

/**
 * Sets the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 * @param {*} value
 */
function setValueForProperty(node, name, value) {
  var propertyInfo = getPropertyInfo(name);

  if (propertyInfo && shouldSetAttribute(name, value)) {
    var mutationMethod = propertyInfo.mutationMethod;
    if (mutationMethod) {
      mutationMethod(node, value);
    } else if (shouldIgnoreValue(propertyInfo, value)) {
      deleteValueForProperty(node, name);
      return;
    } else if (propertyInfo.mustUseProperty) {
      // Contrary to `setAttribute`, object properties are properly
      // `toString`ed by IE8/9.
      node[propertyInfo.propertyName] = value;
    } else {
      var attributeName = propertyInfo.attributeName;
      var namespace = propertyInfo.attributeNamespace;
      // `setAttribute` with objects becomes only `[object]` in IE8/9,
      // ('' + value) makes it output the correct toString()-value.
      if (namespace) {
        node.setAttributeNS(namespace, attributeName, '' + value);
      } else if (propertyInfo.hasBooleanValue || propertyInfo.hasOverloadedBooleanValue && value === true) {
        node.setAttribute(attributeName, '');
      } else {
        node.setAttribute(attributeName, '' + value);
      }
    }
  } else {
    setValueForAttribute(node, name, shouldSetAttribute(name, value) ? value : null);
    return;
  }

  {
    
  }
}

function setValueForAttribute(node, name, value) {
  if (!isAttributeNameSafe(name)) {
    return;
  }
  if (value == null) {
    node.removeAttribute(name);
  } else {
    node.setAttribute(name, '' + value);
  }

  {
    
  }
}

/**
 * Deletes an attributes from a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 */
function deleteValueForAttribute(node, name) {
  node.removeAttribute(name);
}

/**
 * Deletes the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 */
function deleteValueForProperty(node, name) {
  var propertyInfo = getPropertyInfo(name);
  if (propertyInfo) {
    var mutationMethod = propertyInfo.mutationMethod;
    if (mutationMethod) {
      mutationMethod(node, undefined);
    } else if (propertyInfo.mustUseProperty) {
      var propName = propertyInfo.propertyName;
      if (propertyInfo.hasBooleanValue) {
        node[propName] = false;
      } else {
        node[propName] = '';
      }
    } else {
      node.removeAttribute(propertyInfo.attributeName);
    }
  } else {
    node.removeAttribute(name);
  }
}

var ReactControlledValuePropTypes = {
  checkPropTypes: null
};

{
  var hasReadOnlyValue = {
    button: true,
    checkbox: true,
    image: true,
    hidden: true,
    radio: true,
    reset: true,
    submit: true
  };

  var propTypes = {
    value: function (props, propName, componentName) {
      if (!props[propName] || hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled) {
        return null;
      }
      return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    },
    checked: function (props, propName, componentName) {
      if (!props[propName] || props.onChange || props.readOnly || props.disabled) {
        return null;
      }
      return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    }
  };

  /**
   * Provide a linked `value` attribute for controlled forms. You should not use
   * this outside of the ReactDOM controlled form components.
   */
  ReactControlledValuePropTypes.checkPropTypes = function (tagName, props, getStack) {
    checkPropTypes(propTypes, props, 'prop', tagName, getStack);
  };
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$2 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$3 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnValueDefaultValue = false;
var didWarnCheckedDefaultChecked = false;
var didWarnControlledToUncontrolled = false;
var didWarnUncontrolledToControlled = false;

function isControlled(props) {
  var usesChecked = props.type === 'checkbox' || props.type === 'radio';
  return usesChecked ? props.checked != null : props.value != null;
}

/**
 * Implements an <input> host component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */

function getHostProps(element, props) {
  var node = element;
  var value = props.value;
  var checked = props.checked;

  var hostProps = _assign({
    // Make sure we set .type before any other properties (setting .value
    // before .type means .value is lost in IE11 and below)
    type: undefined,
    // Make sure we set .step before .value (setting .value before .step
    // means .value is rounded on mount, based upon step precision)
    step: undefined,
    // Make sure we set .min & .max before .value (to ensure proper order
    // in corner cases such as min or max deriving from value, e.g. Issue #7170)
    min: undefined,
    max: undefined
  }, props, {
    defaultChecked: undefined,
    defaultValue: undefined,
    value: value != null ? value : node._wrapperState.initialValue,
    checked: checked != null ? checked : node._wrapperState.initialChecked
  });

  return hostProps;
}

function initWrapperState(element, props) {
  {
    ReactControlledValuePropTypes.checkPropTypes('input', props, getCurrentFiberStackAddendum$3);

    if (props.checked !== undefined && props.defaultChecked !== undefined && !didWarnCheckedDefaultChecked) {
      warning(false, '%s contains an input of type %s with both checked and defaultChecked props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the checked prop, or the defaultChecked prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerName$2() || 'A component', props.type);
      didWarnCheckedDefaultChecked = true;
    }
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue) {
      warning(false, '%s contains an input of type %s with both value and defaultValue props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerName$2() || 'A component', props.type);
      didWarnValueDefaultValue = true;
    }
  }

  var defaultValue = props.defaultValue;
  var node = element;
  node._wrapperState = {
    initialChecked: props.checked != null ? props.checked : props.defaultChecked,
    initialValue: props.value != null ? props.value : defaultValue,
    controlled: isControlled(props)
  };
}

function updateChecked(element, props) {
  var node = element;
  var checked = props.checked;
  if (checked != null) {
    setValueForProperty(node, 'checked', checked);
  }
}

function updateWrapper(element, props) {
  var node = element;
  {
    var controlled = isControlled(props);

    if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
      warning(false, 'A component is changing an uncontrolled input of type %s to be controlled. ' + 'Input elements should not switch from uncontrolled to controlled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components%s', props.type, getCurrentFiberStackAddendum$3());
      didWarnUncontrolledToControlled = true;
    }
    if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
      warning(false, 'A component is changing a controlled input of type %s to be uncontrolled. ' + 'Input elements should not switch from controlled to uncontrolled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components%s', props.type, getCurrentFiberStackAddendum$3());
      didWarnControlledToUncontrolled = true;
    }
  }

  updateChecked(element, props);

  var value = props.value;
  if (value != null) {
    if (value === 0 && node.value === '') {
      node.value = '0';
      // Note: IE9 reports a number inputs as 'text', so check props instead.
    } else if (props.type === 'number') {
      // Simulate `input.valueAsNumber`. IE9 does not support it
      var valueAsNumber = parseFloat(node.value) || 0;

      if (
      // eslint-disable-next-line
      value != valueAsNumber ||
      // eslint-disable-next-line
      value == valueAsNumber && node.value != value) {
        // Cast `value` to a string to ensure the value is set correctly. While
        // browsers typically do this as necessary, jsdom doesn't.
        node.value = '' + value;
      }
    } else if (node.value !== '' + value) {
      // Cast `value` to a string to ensure the value is set correctly. While
      // browsers typically do this as necessary, jsdom doesn't.
      node.value = '' + value;
    }
  } else {
    if (props.value == null && props.defaultValue != null) {
      // In Chrome, assigning defaultValue to certain input types triggers input validation.
      // For number inputs, the display value loses trailing decimal points. For email inputs,
      // Chrome raises "The specified value <x> is not a valid email address".
      //
      // Here we check to see if the defaultValue has actually changed, avoiding these problems
      // when the user is inputting text
      //
      // https://github.com/facebook/react/issues/7253
      if (node.defaultValue !== '' + props.defaultValue) {
        node.defaultValue = '' + props.defaultValue;
      }
    }
    if (props.checked == null && props.defaultChecked != null) {
      node.defaultChecked = !!props.defaultChecked;
    }
  }
}

function postMountWrapper(element, props) {
  var node = element;

  // Detach value from defaultValue. We won't do anything if we're working on
  // submit or reset inputs as those values & defaultValues are linked. They
  // are not resetable nodes so this operation doesn't matter and actually
  // removes browser-default values (eg "Submit Query") when no value is
  // provided.

  switch (props.type) {
    case 'submit':
    case 'reset':
      break;
    case 'color':
    case 'date':
    case 'datetime':
    case 'datetime-local':
    case 'month':
    case 'time':
    case 'week':
      // This fixes the no-show issue on iOS Safari and Android Chrome:
      // https://github.com/facebook/react/issues/7233
      node.value = '';
      node.value = node.defaultValue;
      break;
    default:
      node.value = node.value;
      break;
  }

  // Normally, we'd just do `node.checked = node.checked` upon initial mount, less this bug
  // this is needed to work around a chrome bug where setting defaultChecked
  // will sometimes influence the value of checked (even after detachment).
  // Reference: https://bugs.chromium.org/p/chromium/issues/detail?id=608416
  // We need to temporarily unset name to avoid disrupting radio button groups.
  var name = node.name;
  if (name !== '') {
    node.name = '';
  }
  node.defaultChecked = !node.defaultChecked;
  node.defaultChecked = !node.defaultChecked;
  if (name !== '') {
    node.name = name;
  }
}

function restoreControlledState$1(element, props) {
  var node = element;
  updateWrapper(node, props);
  updateNamedCousins(node, props);
}

function updateNamedCousins(rootNode, props) {
  var name = props.name;
  if (props.type === 'radio' && name != null) {
    var queryRoot = rootNode;

    while (queryRoot.parentNode) {
      queryRoot = queryRoot.parentNode;
    }

    // If `rootNode.form` was non-null, then we could try `form.elements`,
    // but that sometimes behaves strangely in IE8. We could also try using
    // `form.getElementsByName`, but that will only return direct children
    // and won't include inputs that use the HTML5 `form=` attribute. Since
    // the input might not even be in a form. It might not even be in the
    // document. Let's just use the local `querySelectorAll` to ensure we don't
    // miss anything.
    var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

    for (var i = 0; i < group.length; i++) {
      var otherNode = group[i];
      if (otherNode === rootNode || otherNode.form !== rootNode.form) {
        continue;
      }
      // This will throw if radio buttons rendered by different copies of React
      // and the same name are rendered into the same form (same as #1939).
      // That's probably okay; we don't support it just as we don't support
      // mixing React radio buttons with non-React ones.
      var otherProps = getFiberCurrentPropsFromNode$1(otherNode);
      !otherProps ? invariant(false, 'ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.') : void 0;

      // We need update the tracked value on the named cousin since the value
      // was changed but the input saw no event or value set
      updateValueIfChanged(otherNode);

      // If this is a controlled radio button group, forcing the input that
      // was previously checked to update will cause it to be come re-checked
      // as appropriate.
      updateWrapper(otherNode, otherProps);
    }
  }
}

function flattenChildren(children) {
  var content = '';

  // Flatten children and warn if they aren't strings or numbers;
  // invalid types are ignored.
  // We can silently skip them because invalid DOM nesting warning
  // catches these cases in Fiber.
  React.Children.forEach(children, function (child) {
    if (child == null) {
      return;
    }
    if (typeof child === 'string' || typeof child === 'number') {
      content += child;
    }
  });

  return content;
}

/**
 * Implements an <option> host component that warns when `selected` is set.
 */

function validateProps(element, props) {
  // TODO (yungsters): Remove support for `selected` in <option>.
  {
    warning(props.selected == null, 'Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.');
  }
}

function postMountWrapper$1(element, props) {
  // value="" should make a value attribute (#6219)
  if (props.value != null) {
    element.setAttribute('value', props.value);
  }
}

function getHostProps$1(element, props) {
  var hostProps = _assign({ children: undefined }, props);
  var content = flattenChildren(props.children);

  if (content) {
    hostProps.children = content;
  }

  return hostProps;
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$3 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$4 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;


{
  var didWarnValueDefaultValue$1 = false;
}

function getDeclarationErrorAddendum() {
  var ownerName = getCurrentFiberOwnerName$3();
  if (ownerName) {
    return '\n\nCheck the render method of `' + ownerName + '`.';
  }
  return '';
}

var valuePropNames = ['value', 'defaultValue'];

/**
 * Validation function for `value` and `defaultValue`.
 */
function checkSelectPropTypes(props) {
  ReactControlledValuePropTypes.checkPropTypes('select', props, getCurrentFiberStackAddendum$4);

  for (var i = 0; i < valuePropNames.length; i++) {
    var propName = valuePropNames[i];
    if (props[propName] == null) {
      continue;
    }
    var isArray = Array.isArray(props[propName]);
    if (props.multiple && !isArray) {
      warning(false, 'The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum());
    } else if (!props.multiple && isArray) {
      warning(false, 'The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum());
    }
  }
}

function updateOptions(node, multiple, propValue, setDefaultSelected) {
  var options = node.options;

  if (multiple) {
    var selectedValues = propValue;
    var selectedValue = {};
    for (var i = 0; i < selectedValues.length; i++) {
      // Prefix to avoid chaos with special keys.
      selectedValue['$' + selectedValues[i]] = true;
    }
    for (var _i = 0; _i < options.length; _i++) {
      var selected = selectedValue.hasOwnProperty('$' + options[_i].value);
      if (options[_i].selected !== selected) {
        options[_i].selected = selected;
      }
      if (selected && setDefaultSelected) {
        options[_i].defaultSelected = true;
      }
    }
  } else {
    // Do not set `select.value` as exact behavior isn't consistent across all
    // browsers for all cases.
    var _selectedValue = '' + propValue;
    var defaultSelected = null;
    for (var _i2 = 0; _i2 < options.length; _i2++) {
      if (options[_i2].value === _selectedValue) {
        options[_i2].selected = true;
        if (setDefaultSelected) {
          options[_i2].defaultSelected = true;
        }
        return;
      }
      if (defaultSelected === null && !options[_i2].disabled) {
        defaultSelected = options[_i2];
      }
    }
    if (defaultSelected !== null) {
      defaultSelected.selected = true;
    }
  }
}

/**
 * Implements a <select> host component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * stringable. If `multiple` is true, the prop must be an array of stringables.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */

function getHostProps$2(element, props) {
  return _assign({}, props, {
    value: undefined
  });
}

function initWrapperState$1(element, props) {
  var node = element;
  {
    checkSelectPropTypes(props);
  }

  var value = props.value;
  node._wrapperState = {
    initialValue: value != null ? value : props.defaultValue,
    wasMultiple: !!props.multiple
  };

  {
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue$1) {
      warning(false, 'Select elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled select ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
      didWarnValueDefaultValue$1 = true;
    }
  }
}

function postMountWrapper$2(element, props) {
  var node = element;
  node.multiple = !!props.multiple;
  var value = props.value;
  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (props.defaultValue != null) {
    updateOptions(node, !!props.multiple, props.defaultValue, true);
  }
}

function postUpdateWrapper(element, props) {
  var node = element;
  // After the initial mount, we control selected-ness manually so don't pass
  // this value down
  node._wrapperState.initialValue = undefined;

  var wasMultiple = node._wrapperState.wasMultiple;
  node._wrapperState.wasMultiple = !!props.multiple;

  var value = props.value;
  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (wasMultiple !== !!props.multiple) {
    // For simplicity, reapply `defaultValue` if `multiple` is toggled.
    if (props.defaultValue != null) {
      updateOptions(node, !!props.multiple, props.defaultValue, true);
    } else {
      // Revert the select back to its default unselected state.
      updateOptions(node, !!props.multiple, props.multiple ? [] : '', false);
    }
  }
}

function restoreControlledState$2(element, props) {
  var node = element;
  var value = props.value;

  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  }
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberStackAddendum$5 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnValDefaultVal = false;

/**
 * Implements a <textarea> host component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */

function getHostProps$3(element, props) {
  var node = element;
  !(props.dangerouslySetInnerHTML == null) ? invariant(false, '`dangerouslySetInnerHTML` does not make sense on <textarea>.') : void 0;

  // Always set children to the same thing. In IE9, the selection range will
  // get reset if `textContent` is mutated.  We could add a check in setTextContent
  // to only set the value if/when the value differs from the node value (which would
  // completely solve this IE9 bug), but Sebastian+Sophie seemed to like this
  // solution. The value can be a boolean or object so that's why it's forced
  // to be a string.
  var hostProps = _assign({}, props, {
    value: undefined,
    defaultValue: undefined,
    children: '' + node._wrapperState.initialValue
  });

  return hostProps;
}

function initWrapperState$2(element, props) {
  var node = element;
  {
    ReactControlledValuePropTypes.checkPropTypes('textarea', props, getCurrentFiberStackAddendum$5);
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValDefaultVal) {
      warning(false, 'Textarea elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled textarea ' + 'and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
      didWarnValDefaultVal = true;
    }
  }

  var initialValue = props.value;

  // Only bother fetching default value if we're going to use it
  if (initialValue == null) {
    var defaultValue = props.defaultValue;
    // TODO (yungsters): Remove support for children content in <textarea>.
    var children = props.children;
    if (children != null) {
      {
        warning(false, 'Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.');
      }
      !(defaultValue == null) ? invariant(false, 'If you supply `defaultValue` on a <textarea>, do not pass children.') : void 0;
      if (Array.isArray(children)) {
        !(children.length <= 1) ? invariant(false, '<textarea> can only have at most one child.') : void 0;
        children = children[0];
      }

      defaultValue = '' + children;
    }
    if (defaultValue == null) {
      defaultValue = '';
    }
    initialValue = defaultValue;
  }

  node._wrapperState = {
    initialValue: '' + initialValue
  };
}

function updateWrapper$1(element, props) {
  var node = element;
  var value = props.value;
  if (value != null) {
    // Cast `value` to a string to ensure the value is set correctly. While
    // browsers typically do this as necessary, jsdom doesn't.
    var newValue = '' + value;

    // To avoid side effects (such as losing text selection), only set value if changed
    if (newValue !== node.value) {
      node.value = newValue;
    }
    if (props.defaultValue == null) {
      node.defaultValue = newValue;
    }
  }
  if (props.defaultValue != null) {
    node.defaultValue = props.defaultValue;
  }
}

function postMountWrapper$3(element, props) {
  var node = element;
  // This is in postMount because we need access to the DOM node, which is not
  // available until after the component has mounted.
  var textContent = node.textContent;

  // Only set node.value if textContent is equal to the expected
  // initial value. In IE10/IE11 there is a bug where the placeholder attribute
  // will populate textContent as well.
  // https://developer.microsoft.com/microsoft-edge/platform/issues/101525/
  if (textContent === node._wrapperState.initialValue) {
    node.value = textContent;
  }
}

function restoreControlledState$3(element, props) {
  // DOM component is still mounted; update
  updateWrapper$1(element, props);
}

var HTML_NAMESPACE$1 = 'http://www.w3.org/1999/xhtml';
var MATH_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
var SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

var Namespaces = {
  html: HTML_NAMESPACE$1,
  mathml: MATH_NAMESPACE,
  svg: SVG_NAMESPACE
};

// Assumes there is no parent namespace.
function getIntrinsicNamespace(type) {
  switch (type) {
    case 'svg':
      return SVG_NAMESPACE;
    case 'math':
      return MATH_NAMESPACE;
    default:
      return HTML_NAMESPACE$1;
  }
}

function getChildNamespace(parentNamespace, type) {
  if (parentNamespace == null || parentNamespace === HTML_NAMESPACE$1) {
    // No (or default) parent namespace: potential entry point.
    return getIntrinsicNamespace(type);
  }
  if (parentNamespace === SVG_NAMESPACE && type === 'foreignObject') {
    // We're leaving SVG.
    return HTML_NAMESPACE$1;
  }
  // By default, pass namespace below.
  return parentNamespace;
}

/* globals MSApp */

/**
 * Create a function which has 'unsafe' privileges (required by windows8 apps)
 */
var createMicrosoftUnsafeLocalFunction = function (func) {
  if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
    return function (arg0, arg1, arg2, arg3) {
      MSApp.execUnsafeLocalFunction(function () {
        return func(arg0, arg1, arg2, arg3);
      });
    };
  } else {
    return func;
  }
};

// SVG temp container for IE lacking innerHTML
var reusableSVGContainer = void 0;

/**
 * Set the innerHTML property of a node
 *
 * @param {DOMElement} node
 * @param {string} html
 * @internal
 */
var setInnerHTML = createMicrosoftUnsafeLocalFunction(function (node, html) {
  // IE does not have innerHTML for SVG nodes, so instead we inject the
  // new markup in a temp node and then move the child nodes across into
  // the target node

  if (node.namespaceURI === Namespaces.svg && !('innerHTML' in node)) {
    reusableSVGContainer = reusableSVGContainer || document.createElement('div');
    reusableSVGContainer.innerHTML = '<svg>' + html + '</svg>';
    var svgNode = reusableSVGContainer.firstChild;
    while (node.firstChild) {
      node.removeChild(node.firstChild);
    }
    while (svgNode.firstChild) {
      node.appendChild(svgNode.firstChild);
    }
  } else {
    node.innerHTML = html;
  }
});

/**
 * Set the textContent property of a node, ensuring that whitespace is preserved
 * even in IE8. innerText is a poor substitute for textContent and, among many
 * issues, inserts <br> instead of the literal newline chars. innerHTML behaves
 * as it should.
 *
 * @param {DOMElement} node
 * @param {string} text
 * @internal
 */
var setTextContent = function (node, text) {
  if (text) {
    var firstChild = node.firstChild;

    if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
      firstChild.nodeValue = text;
      return;
    }
  }
  node.textContent = text;
};

/**
 * CSS properties which accept numbers but are not in units of "px".
 */
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,

  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};

/**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */
function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}

/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */
var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.
Object.keys(isUnitlessNumber).forEach(function (prop) {
  prefixes.forEach(function (prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});

/**
 * Convert a value into the proper css writable value. The style name `name`
 * should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} name CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */
function dangerousStyleValue(name, value, isCustomProperty) {
  // Note that we've removed escapeTextForBrowser() calls here since the
  // whole string will be escaped when the attribute is injected into
  // the markup. If you provide unsafe user data here they can inject
  // arbitrary CSS which may be problematic (I couldn't repro this):
  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
  // This is not an XSS hole but instead a potential CSS injection issue
  // which has lead to a greater discussion about how we're going to
  // trust URLs moving forward. See #2115901

  var isEmpty = value == null || typeof value === 'boolean' || value === '';
  if (isEmpty) {
    return '';
  }

  if (!isCustomProperty && typeof value === 'number' && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) {
    return value + 'px'; // Presumes implicit 'px' suffix for unitless numbers
  }

  return ('' + value).trim();
}

var warnValidStyle = emptyFunction;

{
  // 'msTransform' is correct, but the other prefixes should be capitalized
  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;

  // style values shouldn't contain a semicolon
  var badStyleValueWithSemicolonPattern = /;\s*$/;

  var warnedStyleNames = {};
  var warnedStyleValues = {};
  var warnedForNaNValue = false;
  var warnedForInfinityValue = false;

  var warnHyphenatedStyleName = function (name, getStack) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    warning(false, 'Unsupported style property %s. Did you mean %s?%s', name, camelizeStyleName(name), getStack());
  };

  var warnBadVendoredStyleName = function (name, getStack) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    warning(false, 'Unsupported vendor-prefixed style property %s. Did you mean %s?%s', name, name.charAt(0).toUpperCase() + name.slice(1), getStack());
  };

  var warnStyleValueWithSemicolon = function (name, value, getStack) {
    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
      return;
    }

    warnedStyleValues[value] = true;
    warning(false, "Style property values shouldn't contain a semicolon. " + 'Try "%s: %s" instead.%s', name, value.replace(badStyleValueWithSemicolonPattern, ''), getStack());
  };

  var warnStyleValueIsNaN = function (name, value, getStack) {
    if (warnedForNaNValue) {
      return;
    }

    warnedForNaNValue = true;
    warning(false, '`NaN` is an invalid value for the `%s` css style property.%s', name, getStack());
  };

  var warnStyleValueIsInfinity = function (name, value, getStack) {
    if (warnedForInfinityValue) {
      return;
    }

    warnedForInfinityValue = true;
    warning(false, '`Infinity` is an invalid value for the `%s` css style property.%s', name, getStack());
  };

  warnValidStyle = function (name, value, getStack) {
    if (name.indexOf('-') > -1) {
      warnHyphenatedStyleName(name, getStack);
    } else if (badVendoredStyleNamePattern.test(name)) {
      warnBadVendoredStyleName(name, getStack);
    } else if (badStyleValueWithSemicolonPattern.test(value)) {
      warnStyleValueWithSemicolon(name, value, getStack);
    }

    if (typeof value === 'number') {
      if (isNaN(value)) {
        warnStyleValueIsNaN(name, value, getStack);
      } else if (!isFinite(value)) {
        warnStyleValueIsInfinity(name, value, getStack);
      }
    }
  };
}

var warnValidStyle$1 = warnValidStyle;

/**
 * Operations for dealing with CSS properties.
 */

/**
 * This creates a string that is expected to be equivalent to the style
 * attribute generated by server-side rendering. It by-passes warnings and
 * security checks so it's not safe to use this value for anything other than
 * comparison. It is only used in DEV for SSR validation.
 */
function createDangerousStringForStyles(styles) {
  {
    var serialized = '';
    var delimiter = '';
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      var styleValue = styles[styleName];
      if (styleValue != null) {
        var isCustomProperty = styleName.indexOf('--') === 0;
        serialized += delimiter + hyphenateStyleName(styleName) + ':';
        serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);

        delimiter = ';';
      }
    }
    return serialized || null;
  }
}

/**
 * Sets the value for multiple styles on a node.  If a value is specified as
 * '' (empty string), the corresponding style property will be unset.
 *
 * @param {DOMElement} node
 * @param {object} styles
 */
function setValueForStyles(node, styles, getStack) {
  var style = node.style;
  for (var styleName in styles) {
    if (!styles.hasOwnProperty(styleName)) {
      continue;
    }
    var isCustomProperty = styleName.indexOf('--') === 0;
    {
      if (!isCustomProperty) {
        warnValidStyle$1(styleName, styles[styleName], getStack);
      }
    }
    var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);
    if (styleName === 'float') {
      styleName = 'cssFloat';
    }
    if (isCustomProperty) {
      style.setProperty(styleName, styleValue);
    } else {
      style[styleName] = styleValue;
    }
  }
}

// For HTML, certain tags should omit their close tag. We keep a whitelist for
// those special-case tags.

var omittedCloseTags = {
  area: true,
  base: true,
  br: true,
  col: true,
  embed: true,
  hr: true,
  img: true,
  input: true,
  keygen: true,
  link: true,
  meta: true,
  param: true,
  source: true,
  track: true,
  wbr: true
};

// For HTML, certain tags cannot have children. This has the same purpose as
// `omittedCloseTags` except that `menuitem` should still have its closing tag.

var voidElementTags = _assign({
  menuitem: true
}, omittedCloseTags);

var HTML$1 = '__html';

function assertValidProps(tag, props, getStack) {
  if (!props) {
    return;
  }
  // Note the use of `==` which checks for null or undefined.
  if (voidElementTags[tag]) {
    !(props.children == null && props.dangerouslySetInnerHTML == null) ? invariant(false, '%s is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.%s', tag, getStack()) : void 0;
  }
  if (props.dangerouslySetInnerHTML != null) {
    !(props.children == null) ? invariant(false, 'Can only set one of `children` or `props.dangerouslySetInnerHTML`.') : void 0;
    !(typeof props.dangerouslySetInnerHTML === 'object' && HTML$1 in props.dangerouslySetInnerHTML) ? invariant(false, '`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://fb.me/react-invariant-dangerously-set-inner-html for more information.') : void 0;
  }
  {
    warning(props.suppressContentEditableWarning || !props.contentEditable || props.children == null, 'A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.%s', getStack());
  }
  !(props.style == null || typeof props.style === 'object') ? invariant(false, 'The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + \'em\'}} when using JSX.%s', getStack()) : void 0;
}

function isCustomComponent(tagName, props) {
  if (tagName.indexOf('-') === -1) {
    return typeof props.is === 'string';
  }
  switch (tagName) {
    // These are reserved SVG and MathML elements.
    // We don't mind this whitelist too much because we expect it to never grow.
    // The alternative is to track the namespace in a few places which is convoluted.
    // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
    case 'annotation-xml':
    case 'color-profile':
    case 'font-face':
    case 'font-face-src':
    case 'font-face-uri':
    case 'font-face-format':
    case 'font-face-name':
    case 'missing-glyph':
      return false;
    default:
      return true;
  }
}

var ariaProperties = {
  'aria-current': 0, // state
  'aria-details': 0,
  'aria-disabled': 0, // state
  'aria-hidden': 0, // state
  'aria-invalid': 0, // state
  'aria-keyshortcuts': 0,
  'aria-label': 0,
  'aria-roledescription': 0,
  // Widget Attributes
  'aria-autocomplete': 0,
  'aria-checked': 0,
  'aria-expanded': 0,
  'aria-haspopup': 0,
  'aria-level': 0,
  'aria-modal': 0,
  'aria-multiline': 0,
  'aria-multiselectable': 0,
  'aria-orientation': 0,
  'aria-placeholder': 0,
  'aria-pressed': 0,
  'aria-readonly': 0,
  'aria-required': 0,
  'aria-selected': 0,
  'aria-sort': 0,
  'aria-valuemax': 0,
  'aria-valuemin': 0,
  'aria-valuenow': 0,
  'aria-valuetext': 0,
  // Live Region Attributes
  'aria-atomic': 0,
  'aria-busy': 0,
  'aria-live': 0,
  'aria-relevant': 0,
  // Drag-and-Drop Attributes
  'aria-dropeffect': 0,
  'aria-grabbed': 0,
  // Relationship Attributes
  'aria-activedescendant': 0,
  'aria-colcount': 0,
  'aria-colindex': 0,
  'aria-colspan': 0,
  'aria-controls': 0,
  'aria-describedby': 0,
  'aria-errormessage': 0,
  'aria-flowto': 0,
  'aria-labelledby': 0,
  'aria-owns': 0,
  'aria-posinset': 0,
  'aria-rowcount': 0,
  'aria-rowindex': 0,
  'aria-rowspan': 0,
  'aria-setsize': 0
};

var warnedProperties = {};
var rARIA = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
var rARIACamel = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

var hasOwnProperty = Object.prototype.hasOwnProperty;

function getStackAddendum() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

function validateProperty(tagName, name) {
  if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) {
    return true;
  }

  if (rARIACamel.test(name)) {
    var ariaName = 'aria-' + name.slice(4).toLowerCase();
    var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null;

    // If this is an aria-* attribute, but is not listed in the known DOM
    // DOM properties, then it is an invalid aria-* attribute.
    if (correctName == null) {
      warning(false, 'Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.%s', name, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
    // aria-* attributes should be lowercase; suggest the lowercase version.
    if (name !== correctName) {
      warning(false, 'Invalid ARIA attribute `%s`. Did you mean `%s`?%s', name, correctName, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
  }

  if (rARIA.test(name)) {
    var lowerCasedName = name.toLowerCase();
    var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null;

    // If this is an aria-* attribute, but is not listed in the known DOM
    // DOM properties, then it is an invalid aria-* attribute.
    if (standardName == null) {
      warnedProperties[name] = true;
      return false;
    }
    // aria-* attributes should be lowercase; suggest the lowercase version.
    if (name !== standardName) {
      warning(false, 'Unknown ARIA attribute `%s`. Did you mean `%s`?%s', name, standardName, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
  }

  return true;
}

function warnInvalidARIAProps(type, props) {
  var invalidProps = [];

  for (var key in props) {
    var isValid = validateProperty(type, key);
    if (!isValid) {
      invalidProps.push(key);
    }
  }

  var unknownPropString = invalidProps.map(function (prop) {
    return '`' + prop + '`';
  }).join(', ');

  if (invalidProps.length === 1) {
    warning(false, 'Invalid aria prop %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop%s', unknownPropString, type, getStackAddendum());
  } else if (invalidProps.length > 1) {
    warning(false, 'Invalid aria props %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop%s', unknownPropString, type, getStackAddendum());
  }
}

function validateProperties(type, props) {
  if (isCustomComponent(type, props)) {
    return;
  }
  warnInvalidARIAProps(type, props);
}

var didWarnValueNull = false;

function getStackAddendum$1() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

function validateProperties$1(type, props) {
  if (type !== 'input' && type !== 'textarea' && type !== 'select') {
    return;
  }

  if (props != null && props.value === null && !didWarnValueNull) {
    didWarnValueNull = true;
    if (type === 'select' && props.multiple) {
      warning(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty array when `multiple` is set to `true` ' + 'to clear the component or `undefined` for uncontrolled components.%s', type, getStackAddendum$1());
    } else {
      warning(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty string to clear the component or `undefined` ' + 'for uncontrolled components.%s', type, getStackAddendum$1());
    }
  }
}

// When adding attributes to the HTML or SVG whitelist, be sure to
// also add them to this module to ensure casing and incorrect name
// warnings.
var possibleStandardNames = {
  // HTML
  accept: 'accept',
  acceptcharset: 'acceptCharset',
  'accept-charset': 'acceptCharset',
  accesskey: 'accessKey',
  action: 'action',
  allowfullscreen: 'allowFullScreen',
  alt: 'alt',
  as: 'as',
  async: 'async',
  autocapitalize: 'autoCapitalize',
  autocomplete: 'autoComplete',
  autocorrect: 'autoCorrect',
  autofocus: 'autoFocus',
  autoplay: 'autoPlay',
  autosave: 'autoSave',
  capture: 'capture',
  cellpadding: 'cellPadding',
  cellspacing: 'cellSpacing',
  challenge: 'challenge',
  charset: 'charSet',
  checked: 'checked',
  children: 'children',
  cite: 'cite',
  'class': 'className',
  classid: 'classID',
  classname: 'className',
  cols: 'cols',
  colspan: 'colSpan',
  content: 'content',
  contenteditable: 'contentEditable',
  contextmenu: 'contextMenu',
  controls: 'controls',
  controlslist: 'controlsList',
  coords: 'coords',
  crossorigin: 'crossOrigin',
  dangerouslysetinnerhtml: 'dangerouslySetInnerHTML',
  data: 'data',
  datetime: 'dateTime',
  'default': 'default',
  defaultchecked: 'defaultChecked',
  defaultvalue: 'defaultValue',
  defer: 'defer',
  dir: 'dir',
  disabled: 'disabled',
  download: 'download',
  draggable: 'draggable',
  enctype: 'encType',
  'for': 'htmlFor',
  form: 'form',
  formmethod: 'formMethod',
  formaction: 'formAction',
  formenctype: 'formEncType',
  formnovalidate: 'formNoValidate',
  formtarget: 'formTarget',
  frameborder: 'frameBorder',
  headers: 'headers',
  height: 'height',
  hidden: 'hidden',
  high: 'high',
  href: 'href',
  hreflang: 'hrefLang',
  htmlfor: 'htmlFor',
  httpequiv: 'httpEquiv',
  'http-equiv': 'httpEquiv',
  icon: 'icon',
  id: 'id',
  innerhtml: 'innerHTML',
  inputmode: 'inputMode',
  integrity: 'integrity',
  is: 'is',
  itemid: 'itemID',
  itemprop: 'itemProp',
  itemref: 'itemRef',
  itemscope: 'itemScope',
  itemtype: 'itemType',
  keyparams: 'keyParams',
  keytype: 'keyType',
  kind: 'kind',
  label: 'label',
  lang: 'lang',
  list: 'list',
  loop: 'loop',
  low: 'low',
  manifest: 'manifest',
  marginwidth: 'marginWidth',
  marginheight: 'marginHeight',
  max: 'max',
  maxlength: 'maxLength',
  media: 'media',
  mediagroup: 'mediaGroup',
  method: 'method',
  min: 'min',
  minlength: 'minLength',
  multiple: 'multiple',
  muted: 'muted',
  name: 'name',
  nonce: 'nonce',
  novalidate: 'noValidate',
  open: 'open',
  optimum: 'optimum',
  pattern: 'pattern',
  placeholder: 'placeholder',
  playsinline: 'playsInline',
  poster: 'poster',
  preload: 'preload',
  profile: 'profile',
  radiogroup: 'radioGroup',
  readonly: 'readOnly',
  referrerpolicy: 'referrerPolicy',
  rel: 'rel',
  required: 'required',
  reversed: 'reversed',
  role: 'role',
  rows: 'rows',
  rowspan: 'rowSpan',
  sandbox: 'sandbox',
  scope: 'scope',
  scoped: 'scoped',
  scrolling: 'scrolling',
  seamless: 'seamless',
  selected: 'selected',
  shape: 'shape',
  size: 'size',
  sizes: 'sizes',
  span: 'span',
  spellcheck: 'spellCheck',
  src: 'src',
  srcdoc: 'srcDoc',
  srclang: 'srcLang',
  srcset: 'srcSet',
  start: 'start',
  step: 'step',
  style: 'style',
  summary: 'summary',
  tabindex: 'tabIndex',
  target: 'target',
  title: 'title',
  type: 'type',
  usemap: 'useMap',
  value: 'value',
  width: 'width',
  wmode: 'wmode',
  wrap: 'wrap',

  // SVG
  about: 'about',
  accentheight: 'accentHeight',
  'accent-height': 'accentHeight',
  accumulate: 'accumulate',
  additive: 'additive',
  alignmentbaseline: 'alignmentBaseline',
  'alignment-baseline': 'alignmentBaseline',
  allowreorder: 'allowReorder',
  alphabetic: 'alphabetic',
  amplitude: 'amplitude',
  arabicform: 'arabicForm',
  'arabic-form': 'arabicForm',
  ascent: 'ascent',
  attributename: 'attributeName',
  attributetype: 'attributeType',
  autoreverse: 'autoReverse',
  azimuth: 'azimuth',
  basefrequency: 'baseFrequency',
  baselineshift: 'baselineShift',
  'baseline-shift': 'baselineShift',
  baseprofile: 'baseProfile',
  bbox: 'bbox',
  begin: 'begin',
  bias: 'bias',
  by: 'by',
  calcmode: 'calcMode',
  capheight: 'capHeight',
  'cap-height': 'capHeight',
  clip: 'clip',
  clippath: 'clipPath',
  'clip-path': 'clipPath',
  clippathunits: 'clipPathUnits',
  cliprule: 'clipRule',
  'clip-rule': 'clipRule',
  color: 'color',
  colorinterpolation: 'colorInterpolation',
  'color-interpolation': 'colorInterpolation',
  colorinterpolationfilters: 'colorInterpolationFilters',
  'color-interpolation-filters': 'colorInterpolationFilters',
  colorprofile: 'colorProfile',
  'color-profile': 'colorProfile',
  colorrendering: 'colorRendering',
  'color-rendering': 'colorRendering',
  contentscripttype: 'contentScriptType',
  contentstyletype: 'contentStyleType',
  cursor: 'cursor',
  cx: 'cx',
  cy: 'cy',
  d: 'd',
  datatype: 'datatype',
  decelerate: 'decelerate',
  descent: 'descent',
  diffuseconstant: 'diffuseConstant',
  direction: 'direction',
  display: 'display',
  divisor: 'divisor',
  dominantbaseline: 'dominantBaseline',
  'dominant-baseline': 'dominantBaseline',
  dur: 'dur',
  dx: 'dx',
  dy: 'dy',
  edgemode: 'edgeMode',
  elevation: 'elevation',
  enablebackground: 'enableBackground',
  'enable-background': 'enableBackground',
  end: 'end',
  exponent: 'exponent',
  externalresourcesrequired: 'externalResourcesRequired',
  fill: 'fill',
  fillopacity: 'fillOpacity',
  'fill-opacity': 'fillOpacity',
  fillrule: 'fillRule',
  'fill-rule': 'fillRule',
  filter: 'filter',
  filterres: 'filterRes',
  filterunits: 'filterUnits',
  floodopacity: 'floodOpacity',
  'flood-opacity': 'floodOpacity',
  floodcolor: 'floodColor',
  'flood-color': 'floodColor',
  focusable: 'focusable',
  fontfamily: 'fontFamily',
  'font-family': 'fontFamily',
  fontsize: 'fontSize',
  'font-size': 'fontSize',
  fontsizeadjust: 'fontSizeAdjust',
  'font-size-adjust': 'fontSizeAdjust',
  fontstretch: 'fontStretch',
  'font-stretch': 'fontStretch',
  fontstyle: 'fontStyle',
  'font-style': 'fontStyle',
  fontvariant: 'fontVariant',
  'font-variant': 'fontVariant',
  fontweight: 'fontWeight',
  'font-weight': 'fontWeight',
  format: 'format',
  from: 'from',
  fx: 'fx',
  fy: 'fy',
  g1: 'g1',
  g2: 'g2',
  glyphname: 'glyphName',
  'glyph-name': 'glyphName',
  glyphorientationhorizontal: 'glyphOrientationHorizontal',
  'glyph-orientation-horizontal': 'glyphOrientationHorizontal',
  glyphorientationvertical: 'glyphOrientationVertical',
  'glyph-orientation-vertical': 'glyphOrientationVertical',
  glyphref: 'glyphRef',
  gradienttransform: 'gradientTransform',
  gradientunits: 'gradientUnits',
  hanging: 'hanging',
  horizadvx: 'horizAdvX',
  'horiz-adv-x': 'horizAdvX',
  horizoriginx: 'horizOriginX',
  'horiz-origin-x': 'horizOriginX',
  ideographic: 'ideographic',
  imagerendering: 'imageRendering',
  'image-rendering': 'imageRendering',
  in2: 'in2',
  'in': 'in',
  inlist: 'inlist',
  intercept: 'intercept',
  k1: 'k1',
  k2: 'k2',
  k3: 'k3',
  k4: 'k4',
  k: 'k',
  kernelmatrix: 'kernelMatrix',
  kernelunitlength: 'kernelUnitLength',
  kerning: 'kerning',
  keypoints: 'keyPoints',
  keysplines: 'keySplines',
  keytimes: 'keyTimes',
  lengthadjust: 'lengthAdjust',
  letterspacing: 'letterSpacing',
  'letter-spacing': 'letterSpacing',
  lightingcolor: 'lightingColor',
  'lighting-color': 'lightingColor',
  limitingconeangle: 'limitingConeAngle',
  local: 'local',
  markerend: 'markerEnd',
  'marker-end': 'markerEnd',
  markerheight: 'markerHeight',
  markermid: 'markerMid',
  'marker-mid': 'markerMid',
  markerstart: 'markerStart',
  'marker-start': 'markerStart',
  markerunits: 'markerUnits',
  markerwidth: 'markerWidth',
  mask: 'mask',
  maskcontentunits: 'maskContentUnits',
  maskunits: 'maskUnits',
  mathematical: 'mathematical',
  mode: 'mode',
  numoctaves: 'numOctaves',
  offset: 'offset',
  opacity: 'opacity',
  operator: 'operator',
  order: 'order',
  orient: 'orient',
  orientation: 'orientation',
  origin: 'origin',
  overflow: 'overflow',
  overlineposition: 'overlinePosition',
  'overline-position': 'overlinePosition',
  overlinethickness: 'overlineThickness',
  'overline-thickness': 'overlineThickness',
  paintorder: 'paintOrder',
  'paint-order': 'paintOrder',
  panose1: 'panose1',
  'panose-1': 'panose1',
  pathlength: 'pathLength',
  patterncontentunits: 'patternContentUnits',
  patterntransform: 'patternTransform',
  patternunits: 'patternUnits',
  pointerevents: 'pointerEvents',
  'pointer-events': 'pointerEvents',
  points: 'points',
  pointsatx: 'pointsAtX',
  pointsaty: 'pointsAtY',
  pointsatz: 'pointsAtZ',
  prefix: 'prefix',
  preservealpha: 'preserveAlpha',
  preserveaspectratio: 'preserveAspectRatio',
  primitiveunits: 'primitiveUnits',
  property: 'property',
  r: 'r',
  radius: 'radius',
  refx: 'refX',
  refy: 'refY',
  renderingintent: 'renderingIntent',
  'rendering-intent': 'renderingIntent',
  repeatcount: 'repeatCount',
  repeatdur: 'repeatDur',
  requiredextensions: 'requiredExtensions',
  requiredfeatures: 'requiredFeatures',
  resource: 'resource',
  restart: 'restart',
  result: 'result',
  results: 'results',
  rotate: 'rotate',
  rx: 'rx',
  ry: 'ry',
  scale: 'scale',
  security: 'security',
  seed: 'seed',
  shaperendering: 'shapeRendering',
  'shape-rendering': 'shapeRendering',
  slope: 'slope',
  spacing: 'spacing',
  specularconstant: 'specularConstant',
  specularexponent: 'specularExponent',
  speed: 'speed',
  spreadmethod: 'spreadMethod',
  startoffset: 'startOffset',
  stddeviation: 'stdDeviation',
  stemh: 'stemh',
  stemv: 'stemv',
  stitchtiles: 'stitchTiles',
  stopcolor: 'stopColor',
  'stop-color': 'stopColor',
  stopopacity: 'stopOpacity',
  'stop-opacity': 'stopOpacity',
  strikethroughposition: 'strikethroughPosition',
  'strikethrough-position': 'strikethroughPosition',
  strikethroughthickness: 'strikethroughThickness',
  'strikethrough-thickness': 'strikethroughThickness',
  string: 'string',
  stroke: 'stroke',
  strokedasharray: 'strokeDasharray',
  'stroke-dasharray': 'strokeDasharray',
  strokedashoffset: 'strokeDashoffset',
  'stroke-dashoffset': 'strokeDashoffset',
  strokelinecap: 'strokeLinecap',
  'stroke-linecap': 'strokeLinecap',
  strokelinejoin: 'strokeLinejoin',
  'stroke-linejoin': 'strokeLinejoin',
  strokemiterlimit: 'strokeMiterlimit',
  'stroke-miterlimit': 'strokeMiterlimit',
  strokewidth: 'strokeWidth',
  'stroke-width': 'strokeWidth',
  strokeopacity: 'strokeOpacity',
  'stroke-opacity': 'strokeOpacity',
  suppresscontenteditablewarning: 'suppressContentEditableWarning',
  suppresshydrationwarning: 'suppressHydrationWarning',
  surfacescale: 'surfaceScale',
  systemlanguage: 'systemLanguage',
  tablevalues: 'tableValues',
  targetx: 'targetX',
  targety: 'targetY',
  textanchor: 'textAnchor',
  'text-anchor': 'textAnchor',
  textdecoration: 'textDecoration',
  'text-decoration': 'textDecoration',
  textlength: 'textLength',
  textrendering: 'textRendering',
  'text-rendering': 'textRendering',
  to: 'to',
  transform: 'transform',
  'typeof': 'typeof',
  u1: 'u1',
  u2: 'u2',
  underlineposition: 'underlinePosition',
  'underline-position': 'underlinePosition',
  underlinethickness: 'underlineThickness',
  'underline-thickness': 'underlineThickness',
  unicode: 'unicode',
  unicodebidi: 'unicodeBidi',
  'unicode-bidi': 'unicodeBidi',
  unicoderange: 'unicodeRange',
  'unicode-range': 'unicodeRange',
  unitsperem: 'unitsPerEm',
  'units-per-em': 'unitsPerEm',
  unselectable: 'unselectable',
  valphabetic: 'vAlphabetic',
  'v-alphabetic': 'vAlphabetic',
  values: 'values',
  vectoreffect: 'vectorEffect',
  'vector-effect': 'vectorEffect',
  version: 'version',
  vertadvy: 'vertAdvY',
  'vert-adv-y': 'vertAdvY',
  vertoriginx: 'vertOriginX',
  'vert-origin-x': 'vertOriginX',
  vertoriginy: 'vertOriginY',
  'vert-origin-y': 'vertOriginY',
  vhanging: 'vHanging',
  'v-hanging': 'vHanging',
  videographic: 'vIdeographic',
  'v-ideographic': 'vIdeographic',
  viewbox: 'viewBox',
  viewtarget: 'viewTarget',
  visibility: 'visibility',
  vmathematical: 'vMathematical',
  'v-mathematical': 'vMathematical',
  vocab: 'vocab',
  widths: 'widths',
  wordspacing: 'wordSpacing',
  'word-spacing': 'wordSpacing',
  writingmode: 'writingMode',
  'writing-mode': 'writingMode',
  x1: 'x1',
  x2: 'x2',
  x: 'x',
  xchannelselector: 'xChannelSelector',
  xheight: 'xHeight',
  'x-height': 'xHeight',
  xlinkactuate: 'xlinkActuate',
  'xlink:actuate': 'xlinkActuate',
  xlinkarcrole: 'xlinkArcrole',
  'xlink:arcrole': 'xlinkArcrole',
  xlinkhref: 'xlinkHref',
  'xlink:href': 'xlinkHref',
  xlinkrole: 'xlinkRole',
  'xlink:role': 'xlinkRole',
  xlinkshow: 'xlinkShow',
  'xlink:show': 'xlinkShow',
  xlinktitle: 'xlinkTitle',
  'xlink:title': 'xlinkTitle',
  xlinktype: 'xlinkType',
  'xlink:type': 'xlinkType',
  xmlbase: 'xmlBase',
  'xml:base': 'xmlBase',
  xmllang: 'xmlLang',
  'xml:lang': 'xmlLang',
  xmlns: 'xmlns',
  'xml:space': 'xmlSpace',
  xmlnsxlink: 'xmlnsXlink',
  'xmlns:xlink': 'xmlnsXlink',
  xmlspace: 'xmlSpace',
  y1: 'y1',
  y2: 'y2',
  y: 'y',
  ychannelselector: 'yChannelSelector',
  z: 'z',
  zoomandpan: 'zoomAndPan'
};

function getStackAddendum$2() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

{
  var warnedProperties$1 = {};
  var hasOwnProperty$1 = Object.prototype.hasOwnProperty;
  var EVENT_NAME_REGEX = /^on./;
  var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
  var rARIA$1 = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
  var rARIACamel$1 = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

  var validateProperty$1 = function (tagName, name, value, canUseEventSystem) {
    if (hasOwnProperty$1.call(warnedProperties$1, name) && warnedProperties$1[name]) {
      return true;
    }

    var lowerCasedName = name.toLowerCase();
    if (lowerCasedName === 'onfocusin' || lowerCasedName === 'onfocusout') {
      warning(false, 'React uses onFocus and onBlur instead of onFocusIn and onFocusOut. ' + 'All React events are normalized to bubble, so onFocusIn and onFocusOut ' + 'are not needed/supported by React.');
      warnedProperties$1[name] = true;
      return true;
    }

    // We can't rely on the event system being injected on the server.
    if (canUseEventSystem) {
      if (registrationNameModules.hasOwnProperty(name)) {
        return true;
      }
      var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;
      if (registrationName != null) {
        warning(false, 'Invalid event handler property `%s`. Did you mean `%s`?%s', name, registrationName, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
      if (EVENT_NAME_REGEX.test(name)) {
        warning(false, 'Unknown event handler property `%s`. It will be ignored.%s', name, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
    } else if (EVENT_NAME_REGEX.test(name)) {
      // If no event plugins have been injected, we are in a server environment.
      // So we can't tell if the event name is correct for sure, but we can filter
      // out known bad ones like `onclick`. We can't suggest a specific replacement though.
      if (INVALID_EVENT_NAME_REGEX.test(name)) {
        warning(false, 'Invalid event handler property `%s`. ' + 'React events use the camelCase naming convention, for example `onClick`.%s', name, getStackAddendum$2());
      }
      warnedProperties$1[name] = true;
      return true;
    }

    // Let the ARIA attribute hook validate ARIA attributes
    if (rARIA$1.test(name) || rARIACamel$1.test(name)) {
      return true;
    }

    if (lowerCasedName === 'innerhtml') {
      warning(false, 'Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.');
      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'aria') {
      warning(false, 'The `aria` attribute is reserved for future use in React. ' + 'Pass individual `aria-` attributes instead.');
      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'is' && value !== null && value !== undefined && typeof value !== 'string') {
      warning(false, 'Received a `%s` for a string attribute `is`. If this is expected, cast ' + 'the value to a string.%s', typeof value, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'number' && isNaN(value)) {
      warning(false, 'Received NaN for the `%s` attribute. If this is expected, cast ' + 'the value to a string.%s', name, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    var isReserved = isReservedProp(name);

    // Known attributes should match the casing specified in the property config.
    if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
      var standardName = possibleStandardNames[lowerCasedName];
      if (standardName !== name) {
        warning(false, 'Invalid DOM property `%s`. Did you mean `%s`?%s', name, standardName, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
    } else if (!isReserved && name !== lowerCasedName) {
      // Unknown attributes should have lowercase casing since that's how they
      // will be cased anyway with server rendering.
      warning(false, 'React does not recognize the `%s` prop on a DOM element. If you ' + 'intentionally want it to appear in the DOM as a custom ' + 'attribute, spell it as lowercase `%s` instead. ' + 'If you accidentally passed it from a parent component, remove ' + 'it from the DOM element.%s', name, lowerCasedName, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'boolean' && !shouldAttributeAcceptBooleanValue(name)) {
      if (value) {
        warning(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.%s', value, name, name, value, name, getStackAddendum$2());
      } else {
        warning(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.%s', value, name, name, value, name, name, name, getStackAddendum$2());
      }
      warnedProperties$1[name] = true;
      return true;
    }

    // Now that we've validated casing, do not validate
    // data types for reserved props
    if (isReserved) {
      return true;
    }

    // Warn when a known attribute is a bad type
    if (!shouldSetAttribute(name, value)) {
      warnedProperties$1[name] = true;
      return false;
    }

    return true;
  };
}

var warnUnknownProperties = function (type, props, canUseEventSystem) {
  var unknownProps = [];
  for (var key in props) {
    var isValid = validateProperty$1(type, key, props[key], canUseEventSystem);
    if (!isValid) {
      unknownProps.push(key);
    }
  }

  var unknownPropString = unknownProps.map(function (prop) {
    return '`' + prop + '`';
  }).join(', ');
  if (unknownProps.length === 1) {
    warning(false, 'Invalid value for prop %s on <%s> tag. Either remove it from the element, ' + 'or pass a string or number value to keep it in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior%s', unknownPropString, type, getStackAddendum$2());
  } else if (unknownProps.length > 1) {
    warning(false, 'Invalid values for props %s on <%s> tag. Either remove them from the element, ' + 'or pass a string or number value to keep them in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior%s', unknownPropString, type, getStackAddendum$2());
  }
};

function validateProperties$2(type, props, canUseEventSystem) {
  if (isCustomComponent(type, props)) {
    return;
  }
  warnUnknownProperties(type, props, canUseEventSystem);
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$1 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$2 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnInvalidHydration = false;
var didWarnShadyDOM = false;

var DANGEROUSLY_SET_INNER_HTML = 'dangerouslySetInnerHTML';
var SUPPRESS_CONTENT_EDITABLE_WARNING = 'suppressContentEditableWarning';
var SUPPRESS_HYDRATION_WARNING$1 = 'suppressHydrationWarning';
var AUTOFOCUS = 'autoFocus';
var CHILDREN = 'children';
var STYLE = 'style';
var HTML = '__html';

var HTML_NAMESPACE = Namespaces.html;


var getStack = emptyFunction.thatReturns('');

{
  getStack = getCurrentFiberStackAddendum$2;

  var warnedUnknownTags = {
    // Chrome is the only major browser not shipping <time>. But as of July
    // 2017 it intends to ship it due to widespread usage. We intentionally
    // *don't* warn for <time> even if it's unrecognized by Chrome because
    // it soon will be, and many apps have been using it anyway.
    time: true,
    // There are working polyfills for <dialog>. Let people use it.
    dialog: true
  };

  var validatePropertiesInDevelopment = function (type, props) {
    validateProperties(type, props);
    validateProperties$1(type, props);
    validateProperties$2(type, props, /* canUseEventSystem */true);
  };

  // HTML parsing normalizes CR and CRLF to LF.
  // It also can turn \u0000 into \uFFFD inside attributes.
  // https://www.w3.org/TR/html5/single-page.html#preprocessing-the-input-stream
  // If we have a mismatch, it might be caused by that.
  // We will still patch up in this case but not fire the warning.
  var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
  var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;

  var normalizeMarkupForTextOrAttribute = function (markup) {
    var markupString = typeof markup === 'string' ? markup : '' + markup;
    return markupString.replace(NORMALIZE_NEWLINES_REGEX, '\n').replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, '');
  };

  var warnForTextDifference = function (serverText, clientText) {
    if (didWarnInvalidHydration) {
      return;
    }
    var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
    var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);
    if (normalizedServerText === normalizedClientText) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
  };

  var warnForPropDifference = function (propName, serverValue, clientValue) {
    if (didWarnInvalidHydration) {
      return;
    }
    var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
    var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);
    if (normalizedServerValue === normalizedClientValue) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Prop `%s` did not match. Server: %s Client: %s', propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
  };

  var warnForExtraAttributes = function (attributeNames) {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    var names = [];
    attributeNames.forEach(function (name) {
      names.push(name);
    });
    warning(false, 'Extra attributes from the server: %s', names);
  };

  var warnForInvalidEventListener = function (registrationName, listener) {
    if (listener === false) {
      warning(false, 'Expected `%s` listener to be a function, instead got `false`.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.%s', registrationName, registrationName, registrationName, getCurrentFiberStackAddendum$2());
    } else {
      warning(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.%s', registrationName, typeof listener, getCurrentFiberStackAddendum$2());
    }
  };

  // Parse the HTML and read it back to normalize the HTML string so that it
  // can be used for comparison.
  var normalizeHTML = function (parent, html) {
    // We could have created a separate document here to avoid
    // re-initializing custom elements if they exist. But this breaks
    // how <noscript> is being handled. So we use the same document.
    // See the discussion in https://github.com/facebook/react/pull/11157.
    var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
    testElement.innerHTML = html;
    return testElement.innerHTML;
  };
}

function ensureListeningTo(rootContainerElement, registrationName) {
  var isDocumentOrFragment = rootContainerElement.nodeType === DOCUMENT_NODE || rootContainerElement.nodeType === DOCUMENT_FRAGMENT_NODE;
  var doc = isDocumentOrFragment ? rootContainerElement : rootContainerElement.ownerDocument;
  listenTo(registrationName, doc);
}

function getOwnerDocumentFromRootContainer(rootContainerElement) {
  return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
}

// There are so many media events, it makes sense to just
// maintain a list rather than create a `trapBubbledEvent` for each
var mediaEvents = {
  topAbort: 'abort',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topLoadedData: 'loadeddata',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTimeUpdate: 'timeupdate',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting'
};

function trapClickOnNonInteractiveElement(node) {
  // Mobile Safari does not fire properly bubble click events on
  // non-interactive elements, which means delegated click listeners do not
  // fire. The workaround for this bug involves attaching an empty click
  // listener on the target node.
  // http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
  // Just set it using the onclick property so that we don't have to manage any
  // bookkeeping for it. Not sure if we need to clear it when the listener is
  // removed.
  // TODO: Only do this for the relevant Safaris maybe?
  node.onclick = emptyFunction;
}

function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
  for (var propKey in nextProps) {
    if (!nextProps.hasOwnProperty(propKey)) {
      continue;
    }
    var nextProp = nextProps[propKey];
    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }
      // Relies on `updateStylesByID` not mutating `styleUpdates`.
      setValueForStyles(domElement, nextProp, getStack);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML] : undefined;
      if (nextHtml != null) {
        setInnerHTML(domElement, nextHtml);
      }
    } else if (propKey === CHILDREN) {
      if (typeof nextProp === 'string') {
        // Avoid setting initial textContent when the text is empty. In IE11 setting
        // textContent on a <textarea> will cause the placeholder to not
        // show within the <textarea> until it has been focused and blurred again.
        // https://github.com/facebook/react/issues/6731#issuecomment-254874553
        var canSetTextContent = tag !== 'textarea' || nextProp !== '';
        if (canSetTextContent) {
          setTextContent(domElement, nextProp);
        }
      } else if (typeof nextProp === 'number') {
        setTextContent(domElement, '' + nextProp);
      }
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (propKey === AUTOFOCUS) {
      // We polyfill it separately on the client during commit.
      // We blacklist it here rather than in the property list because we emit it in SSR.
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
    } else if (isCustomComponentTag) {
      setValueForAttribute(domElement, propKey, nextProp);
    } else if (nextProp != null) {
      // If we're updating to null or undefined, we should remove the property
      // from the DOM node instead of inadvertently setting to a string. This
      // brings us in line with the same behavior we have on initial render.
      setValueForProperty(domElement, propKey, nextProp);
    }
  }
}

function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
  // TODO: Handle wasCustomComponentTag
  for (var i = 0; i < updatePayload.length; i += 2) {
    var propKey = updatePayload[i];
    var propValue = updatePayload[i + 1];
    if (propKey === STYLE) {
      setValueForStyles(domElement, propValue, getStack);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      setInnerHTML(domElement, propValue);
    } else if (propKey === CHILDREN) {
      setTextContent(domElement, propValue);
    } else if (isCustomComponentTag) {
      if (propValue != null) {
        setValueForAttribute(domElement, propKey, propValue);
      } else {
        deleteValueForAttribute(domElement, propKey);
      }
    } else if (propValue != null) {
      setValueForProperty(domElement, propKey, propValue);
    } else {
      // If we're updating to null or undefined, we should remove the property
      // from the DOM node instead of inadvertently setting to a string. This
      // brings us in line with the same behavior we have on initial render.
      deleteValueForProperty(domElement, propKey);
    }
  }
}

function createElement$1(type, props, rootContainerElement, parentNamespace) {
  // We create tags in the namespace of their parent container, except HTML
  var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
  var domElement;
  var namespaceURI = parentNamespace;
  if (namespaceURI === HTML_NAMESPACE) {
    namespaceURI = getIntrinsicNamespace(type);
  }
  if (namespaceURI === HTML_NAMESPACE) {
    {
      var isCustomComponentTag = isCustomComponent(type, props);
      // Should this check be gated by parent namespace? Not sure we want to
      // allow <SVG> or <mATH>.
      warning(isCustomComponentTag || type === type.toLowerCase(), '<%s /> is using uppercase HTML. Always use lowercase HTML tags ' + 'in React.', type);
    }

    if (type === 'script') {
      // Create the script via .innerHTML so its "parser-inserted" flag is
      // set to true and it does not execute
      var div = ownerDocument.createElement('div');
      div.innerHTML = '<script><' + '/script>'; // eslint-disable-line
      // This is guaranteed to yield a script element.
      var firstChild = div.firstChild;
      domElement = div.removeChild(firstChild);
    } else if (typeof props.is === 'string') {
      // $FlowIssue `createElement` should be updated for Web Components
      domElement = ownerDocument.createElement(type, { is: props.is });
    } else {
      // Separate else branch instead of using `props.is || undefined` above because of a Firefox bug.
      // See discussion in https://github.com/facebook/react/pull/6896
      // and discussion in https://bugzilla.mozilla.org/show_bug.cgi?id=1276240
      domElement = ownerDocument.createElement(type);
    }
  } else {
    domElement = ownerDocument.createElementNS(namespaceURI, type);
  }

  {
    if (namespaceURI === HTML_NAMESPACE) {
      if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === '[object HTMLUnknownElement]' && !Object.prototype.hasOwnProperty.call(warnedUnknownTags, type)) {
        warnedUnknownTags[type] = true;
        warning(false, 'The tag <%s> is unrecognized in this browser. ' + 'If you meant to render a React component, start its name with ' + 'an uppercase letter.', type);
      }
    }
  }

  return domElement;
}

function createTextNode$1(text, rootContainerElement) {
  return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
}

function setInitialProperties$1(domElement, tag, rawProps, rootContainerElement) {
  var isCustomComponentTag = isCustomComponent(tag, rawProps);
  {
    validatePropertiesInDevelopment(tag, rawProps);
    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      warning(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerName$1() || 'A component');
      didWarnShadyDOM = true;
    }
  }

  // TODO: Make sure that we check isMounted before firing any of these events.
  var props;
  switch (tag) {
    case 'iframe':
    case 'object':
      trapBubbledEvent('topLoad', 'load', domElement);
      props = rawProps;
      break;
    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var event in mediaEvents) {
        if (mediaEvents.hasOwnProperty(event)) {
          trapBubbledEvent(event, mediaEvents[event], domElement);
        }
      }
      props = rawProps;
      break;
    case 'source':
      trapBubbledEvent('topError', 'error', domElement);
      props = rawProps;
      break;
    case 'img':
    case 'image':
      trapBubbledEvent('topError', 'error', domElement);
      trapBubbledEvent('topLoad', 'load', domElement);
      props = rawProps;
      break;
    case 'form':
      trapBubbledEvent('topReset', 'reset', domElement);
      trapBubbledEvent('topSubmit', 'submit', domElement);
      props = rawProps;
      break;
    case 'details':
      trapBubbledEvent('topToggle', 'toggle', domElement);
      props = rawProps;
      break;
    case 'input':
      initWrapperState(domElement, rawProps);
      props = getHostProps(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'option':
      validateProps(domElement, rawProps);
      props = getHostProps$1(domElement, rawProps);
      break;
    case 'select':
      initWrapperState$1(domElement, rawProps);
      props = getHostProps$2(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      props = getHostProps$3(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    default:
      props = rawProps;
  }

  assertValidProps(tag, props, getStack);

  setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps);
      break;
    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement, rawProps);
      break;
    case 'option':
      postMountWrapper$1(domElement, rawProps);
      break;
    case 'select':
      postMountWrapper$2(domElement, rawProps);
      break;
    default:
      if (typeof props.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }
}

// Calculate the diff between the two objects.
function diffProperties$1(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
  {
    validatePropertiesInDevelopment(tag, nextRawProps);
  }

  var updatePayload = null;

  var lastProps;
  var nextProps;
  switch (tag) {
    case 'input':
      lastProps = getHostProps(domElement, lastRawProps);
      nextProps = getHostProps(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'option':
      lastProps = getHostProps$1(domElement, lastRawProps);
      nextProps = getHostProps$1(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'select':
      lastProps = getHostProps$2(domElement, lastRawProps);
      nextProps = getHostProps$2(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'textarea':
      lastProps = getHostProps$3(domElement, lastRawProps);
      nextProps = getHostProps$3(domElement, nextRawProps);
      updatePayload = [];
      break;
    default:
      lastProps = lastRawProps;
      nextProps = nextRawProps;
      if (typeof lastProps.onClick !== 'function' && typeof nextProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }

  assertValidProps(tag, nextProps, getStack);

  var propKey;
  var styleName;
  var styleUpdates = null;
  for (propKey in lastProps) {
    if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) {
      continue;
    }
    if (propKey === STYLE) {
      var lastStyle = lastProps[propKey];
      for (styleName in lastStyle) {
        if (lastStyle.hasOwnProperty(styleName)) {
          if (!styleUpdates) {
            styleUpdates = {};
          }
          styleUpdates[styleName] = '';
        }
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) {
      // Noop. This is handled by the clear text mechanism.
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (propKey === AUTOFOCUS) {
      // Noop. It doesn't work on updates anyway.
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      // This is a special case. If any listener updates we need to ensure
      // that the "current" fiber pointer gets updated so we need a commit
      // to update this element.
      if (!updatePayload) {
        updatePayload = [];
      }
    } else {
      // For all other deleted properties we add it to the queue. We use
      // the whitelist in the commit phase instead.
      (updatePayload = updatePayload || []).push(propKey, null);
    }
  }
  for (propKey in nextProps) {
    var nextProp = nextProps[propKey];
    var lastProp = lastProps != null ? lastProps[propKey] : undefined;
    if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) {
      continue;
    }
    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }
      if (lastProp) {
        // Unset styles on `lastProp` but not on `nextProp`.
        for (styleName in lastProp) {
          if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
            if (!styleUpdates) {
              styleUpdates = {};
            }
            styleUpdates[styleName] = '';
          }
        }
        // Update styles that changed since `lastProp`.
        for (styleName in nextProp) {
          if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
            if (!styleUpdates) {
              styleUpdates = {};
            }
            styleUpdates[styleName] = nextProp[styleName];
          }
        }
      } else {
        // Relies on `updateStylesByID` not mutating `styleUpdates`.
        if (!styleUpdates) {
          if (!updatePayload) {
            updatePayload = [];
          }
          updatePayload.push(propKey, styleUpdates);
        }
        styleUpdates = nextProp;
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML] : undefined;
      var lastHtml = lastProp ? lastProp[HTML] : undefined;
      if (nextHtml != null) {
        if (lastHtml !== nextHtml) {
          (updatePayload = updatePayload || []).push(propKey, '' + nextHtml);
        }
      } else {
        // TODO: It might be too late to clear this if we have children
        // inserted already.
      }
    } else if (propKey === CHILDREN) {
      if (lastProp !== nextProp && (typeof nextProp === 'string' || typeof nextProp === 'number')) {
        (updatePayload = updatePayload || []).push(propKey, '' + nextProp);
      }
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        // We eagerly listen to this even though we haven't committed yet.
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
      if (!updatePayload && lastProp !== nextProp) {
        // This is a special case. If any listener updates we need to ensure
        // that the "current" props pointer gets updated so we need a commit
        // to update this element.
        updatePayload = [];
      }
    } else {
      // For any other property we always add it to the queue and then we
      // filter it out using the whitelist during the commit.
      (updatePayload = updatePayload || []).push(propKey, nextProp);
    }
  }
  if (styleUpdates) {
    (updatePayload = updatePayload || []).push(STYLE, styleUpdates);
  }
  return updatePayload;
}

// Apply the diff.
function updateProperties$1(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
  // Update checked *before* name.
  // In the middle of an update, it is possible to have multiple checked.
  // When a checked radio tries to change name, browser makes another radio's checked false.
  if (tag === 'input' && nextRawProps.type === 'radio' && nextRawProps.name != null) {
    updateChecked(domElement, nextRawProps);
  }

  var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
  var isCustomComponentTag = isCustomComponent(tag, nextRawProps);
  // Apply the diff.
  updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag);

  // TODO: Ensure that an update gets scheduled if any of the special props
  // changed.
  switch (tag) {
    case 'input':
      // Update the wrapper around inputs *after* updating props. This has to
      // happen after `updateDOMProperties`. Otherwise HTML5 input validations
      // raise warnings and prevent the new value from being assigned.
      updateWrapper(domElement, nextRawProps);
      break;
    case 'textarea':
      updateWrapper$1(domElement, nextRawProps);
      break;
    case 'select':
      // <select> value update needs to occur after <option> children
      // reconciliation
      postUpdateWrapper(domElement, nextRawProps);
      break;
  }
}

function diffHydratedProperties$1(domElement, tag, rawProps, parentNamespace, rootContainerElement) {
  {
    var suppressHydrationWarning = rawProps[SUPPRESS_HYDRATION_WARNING$1] === true;
    var isCustomComponentTag = isCustomComponent(tag, rawProps);
    validatePropertiesInDevelopment(tag, rawProps);
    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      warning(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerName$1() || 'A component');
      didWarnShadyDOM = true;
    }
  }

  // TODO: Make sure that we check isMounted before firing any of these events.
  switch (tag) {
    case 'iframe':
    case 'object':
      trapBubbledEvent('topLoad', 'load', domElement);
      break;
    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var event in mediaEvents) {
        if (mediaEvents.hasOwnProperty(event)) {
          trapBubbledEvent(event, mediaEvents[event], domElement);
        }
      }
      break;
    case 'source':
      trapBubbledEvent('topError', 'error', domElement);
      break;
    case 'img':
    case 'image':
      trapBubbledEvent('topError', 'error', domElement);
      trapBubbledEvent('topLoad', 'load', domElement);
      break;
    case 'form':
      trapBubbledEvent('topReset', 'reset', domElement);
      trapBubbledEvent('topSubmit', 'submit', domElement);
      break;
    case 'details':
      trapBubbledEvent('topToggle', 'toggle', domElement);
      break;
    case 'input':
      initWrapperState(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'option':
      validateProps(domElement, rawProps);
      break;
    case 'select':
      initWrapperState$1(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
  }

  assertValidProps(tag, rawProps, getStack);

  {
    var extraAttributeNames = new Set();
    var attributes = domElement.attributes;
    for (var i = 0; i < attributes.length; i++) {
      var name = attributes[i].name.toLowerCase();
      switch (name) {
        // Built-in SSR attribute is whitelisted
        case 'data-reactroot':
          break;
        // Controlled attributes are not validated
        // TODO: Only ignore them on controlled tags.
        case 'value':
          break;
        case 'checked':
          break;
        case 'selected':
          break;
        default:
          // Intentionally use the original name.
          // See discussion in https://github.com/facebook/react/pull/10676.
          extraAttributeNames.add(attributes[i].name);
      }
    }
  }

  var updatePayload = null;
  for (var propKey in rawProps) {
    if (!rawProps.hasOwnProperty(propKey)) {
      continue;
    }
    var nextProp = rawProps[propKey];
    if (propKey === CHILDREN) {
      // For text content children we compare against textContent. This
      // might match additional HTML that is hidden when we read it using
      // textContent. E.g. "foo" will match "f<span>oo</span>" but that still
      // satisfies our requirement. Our requirement is not to produce perfect
      // HTML and attributes. Ideally we should preserve structure but it's
      // ok not to if the visible content is still enough to indicate what
      // even listeners these nodes might be wired up to.
      // TODO: Warn if there is more than a single textNode as a child.
      // TODO: Should we use domElement.firstChild.nodeValue to compare?
      if (typeof nextProp === 'string') {
        if (domElement.textContent !== nextProp) {
          if (true && !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }
          updatePayload = [CHILDREN, nextProp];
        }
      } else if (typeof nextProp === 'number') {
        if (domElement.textContent !== '' + nextProp) {
          if (true && !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }
          updatePayload = [CHILDREN, '' + nextProp];
        }
      }
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
    } else {
      // Validate that the properties correspond to their expected values.
      var serverValue;
      var propertyInfo;
      if (suppressHydrationWarning) {
        // Don't bother comparing. We're ignoring all these warnings.
      } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1 ||
      // Controlled attributes are not validated
      // TODO: Only ignore them on controlled tags.
      propKey === 'value' || propKey === 'checked' || propKey === 'selected') {
        // Noop
      } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
        var rawHtml = nextProp ? nextProp[HTML] || '' : '';
        var serverHTML = domElement.innerHTML;
        var expectedHTML = normalizeHTML(domElement, rawHtml);
        if (expectedHTML !== serverHTML) {
          warnForPropDifference(propKey, serverHTML, expectedHTML);
        }
      } else if (propKey === STYLE) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames['delete'](propKey);
        var expectedStyle = createDangerousStringForStyles(nextProp);
        serverValue = domElement.getAttribute('style');
        if (expectedStyle !== serverValue) {
          warnForPropDifference(propKey, serverValue, expectedStyle);
        }
      } else if (isCustomComponentTag) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames['delete'](propKey.toLowerCase());
        serverValue = getValueForAttribute(domElement, propKey, nextProp);

        if (nextProp !== serverValue) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      } else if (shouldSetAttribute(propKey, nextProp)) {
        if (propertyInfo = getPropertyInfo(propKey)) {
          // $FlowFixMe - Should be inferred as not undefined.
          extraAttributeNames['delete'](propertyInfo.attributeName);
          serverValue = getValueForProperty(domElement, propKey, nextProp);
        } else {
          var ownNamespace = parentNamespace;
          if (ownNamespace === HTML_NAMESPACE) {
            ownNamespace = getIntrinsicNamespace(tag);
          }
          if (ownNamespace === HTML_NAMESPACE) {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames['delete'](propKey.toLowerCase());
          } else {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames['delete'](propKey);
          }
          serverValue = getValueForAttribute(domElement, propKey, nextProp);
        }

        if (nextProp !== serverValue) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      }
    }
  }

  {
    // $FlowFixMe - Should be inferred as not undefined.
    if (extraAttributeNames.size > 0 && !suppressHydrationWarning) {
      // $FlowFixMe - Should be inferred as not undefined.
      warnForExtraAttributes(extraAttributeNames);
    }
  }

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps);
      break;
    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement, rawProps);
      break;
    case 'select':
    case 'option':
      // For input and textarea we current always set the value property at
      // post mount to force it to diverge from attributes. However, for
      // option and select we don't quite do the same thing and select
      // is not resilient to the DOM state changing so we don't do that here.
      // TODO: Consider not doing this for input and textarea.
      break;
    default:
      if (typeof rawProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }

  return updatePayload;
}

function diffHydratedText$1(textNode, text) {
  var isDifferent = textNode.nodeValue !== text;
  return isDifferent;
}

function warnForUnmatchedText$1(textNode, text) {
  {
    warnForTextDifference(textNode.nodeValue, text);
  }
}

function warnForDeletedHydratableElement$1(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Did not expect server HTML to contain a <%s> in <%s>.', child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
  }
}

function warnForDeletedHydratableText$1(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
  }
}

function warnForInsertedHydratedElement$1(parentNode, tag, props) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Expected server HTML to contain a matching <%s> in <%s>.', tag, parentNode.nodeName.toLowerCase());
  }
}

function warnForInsertedHydratedText$1(parentNode, text) {
  {
    if (text === '') {
      // We expect to insert empty text nodes since they're not represented in
      // the HTML.
      // TODO: Remove this special case if we can just avoid inserting empty
      // text nodes.
      return;
    }
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
  }
}

function restoreControlledState(domElement, tag, props) {
  switch (tag) {
    case 'input':
      restoreControlledState$1(domElement, props);
      return;
    case 'textarea':
      restoreControlledState$3(domElement, props);
      return;
    case 'select':
      restoreControlledState$2(domElement, props);
      return;
  }
}

var ReactDOMFiberComponent = Object.freeze({
	createElement: createElement$1,
	createTextNode: createTextNode$1,
	setInitialProperties: setInitialProperties$1,
	diffProperties: diffProperties$1,
	updateProperties: updateProperties$1,
	diffHydratedProperties: diffHydratedProperties$1,
	diffHydratedText: diffHydratedText$1,
	warnForUnmatchedText: warnForUnmatchedText$1,
	warnForDeletedHydratableElement: warnForDeletedHydratableElement$1,
	warnForDeletedHydratableText: warnForDeletedHydratableText$1,
	warnForInsertedHydratedElement: warnForInsertedHydratedElement$1,
	warnForInsertedHydratedText: warnForInsertedHydratedText$1,
	restoreControlledState: restoreControlledState
});

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberStackAddendum$6 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var validateDOMNesting = emptyFunction;

{
  // This validation code was written based on the HTML5 parsing spec:
  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  //
  // Note: this does not catch all invalid nesting, nor does it try to (as it's
  // not clear what practical benefit doing so provides); instead, we warn only
  // for cases where the parser will give a parse tree differing from what React
  // intended. For example, <b><div></div></b> is invalid but we don't warn
  // because it still parses correctly; we do warn for other cases like nested
  // <p> tags where the beginning of the second element implicitly closes the
  // first, causing a confusing mess.

  // https://html.spec.whatwg.org/multipage/syntax.html#special
  var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template',

  // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
  // TODO: Distinguish by namespace here -- for <title>, including it here
  // errs on the side of fewer warnings
  'foreignObject', 'desc', 'title'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope
  var buttonScopeTags = inScopeTags.concat(['button']);

  // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags
  var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];

  var emptyAncestorInfo = {
    current: null,

    formTag: null,
    aTagInScope: null,
    buttonTagInScope: null,
    nobrTagInScope: null,
    pTagInButtonScope: null,

    listItemTagAutoclosing: null,
    dlItemTagAutoclosing: null
  };

  var updatedAncestorInfo$1 = function (oldInfo, tag, instance) {
    var ancestorInfo = _assign({}, oldInfo || emptyAncestorInfo);
    var info = { tag: tag, instance: instance };

    if (inScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.aTagInScope = null;
      ancestorInfo.buttonTagInScope = null;
      ancestorInfo.nobrTagInScope = null;
    }
    if (buttonScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.pTagInButtonScope = null;
    }

    // See rules for 'li', 'dd', 'dt' start tags in
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
      ancestorInfo.listItemTagAutoclosing = null;
      ancestorInfo.dlItemTagAutoclosing = null;
    }

    ancestorInfo.current = info;

    if (tag === 'form') {
      ancestorInfo.formTag = info;
    }
    if (tag === 'a') {
      ancestorInfo.aTagInScope = info;
    }
    if (tag === 'button') {
      ancestorInfo.buttonTagInScope = info;
    }
    if (tag === 'nobr') {
      ancestorInfo.nobrTagInScope = info;
    }
    if (tag === 'p') {
      ancestorInfo.pTagInButtonScope = info;
    }
    if (tag === 'li') {
      ancestorInfo.listItemTagAutoclosing = info;
    }
    if (tag === 'dd' || tag === 'dt') {
      ancestorInfo.dlItemTagAutoclosing = info;
    }

    return ancestorInfo;
  };

  /**
   * Returns whether
   */
  var isTagValidWithParent = function (tag, parentTag) {
    // First, let's check if we're in an unusual parsing mode...
    switch (parentTag) {
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
      case 'select':
        return tag === 'option' || tag === 'optgroup' || tag === '#text';
      case 'optgroup':
        return tag === 'option' || tag === '#text';
      // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
      // but
      case 'option':
        return tag === '#text';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
      // No special behavior since these rules fall back to "in body" mode for
      // all except special table nodes which cause bad parsing behavior anyway.

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
      case 'tr':
        return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
      case 'tbody':
      case 'thead':
      case 'tfoot':
        return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
      case 'colgroup':
        return tag === 'col' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
      case 'table':
        return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
      case 'head':
        return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
      case 'html':
        return tag === 'head' || tag === 'body';
      case '#document':
        return tag === 'html';
    }

    // Probably in the "in body" parsing mode, so we outlaw only tag combos
    // where the parsing rules cause implicit opens or closes to be added.
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    switch (tag) {
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

      case 'rp':
      case 'rt':
        return impliedEndTags.indexOf(parentTag) === -1;

      case 'body':
      case 'caption':
      case 'col':
      case 'colgroup':
      case 'frame':
      case 'head':
      case 'html':
      case 'tbody':
      case 'td':
      case 'tfoot':
      case 'th':
      case 'thead':
      case 'tr':
        // These tags are only valid with a few parents that have special child
        // parsing rules -- if we're down here, then none of those matched and
        // so we allow it only if we don't know what the parent is, as all other
        // cases are invalid.
        return parentTag == null;
    }

    return true;
  };

  /**
   * Returns whether
   */
  var findInvalidAncestorForTag = function (tag, ancestorInfo) {
    switch (tag) {
      case 'address':
      case 'article':
      case 'aside':
      case 'blockquote':
      case 'center':
      case 'details':
      case 'dialog':
      case 'dir':
      case 'div':
      case 'dl':
      case 'fieldset':
      case 'figcaption':
      case 'figure':
      case 'footer':
      case 'header':
      case 'hgroup':
      case 'main':
      case 'menu':
      case 'nav':
      case 'ol':
      case 'p':
      case 'section':
      case 'summary':
      case 'ul':
      case 'pre':
      case 'listing':
      case 'table':
      case 'hr':
      case 'xmp':
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return ancestorInfo.pTagInButtonScope;

      case 'form':
        return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

      case 'li':
        return ancestorInfo.listItemTagAutoclosing;

      case 'dd':
      case 'dt':
        return ancestorInfo.dlItemTagAutoclosing;

      case 'button':
        return ancestorInfo.buttonTagInScope;

      case 'a':
        // Spec says something about storing a list of markers, but it sounds
        // equivalent to this check.
        return ancestorInfo.aTagInScope;

      case 'nobr':
        return ancestorInfo.nobrTagInScope;
    }

    return null;
  };

  var didWarn = {};

  validateDOMNesting = function (childTag, childText, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.current;
    var parentTag = parentInfo && parentInfo.tag;

    if (childText != null) {
      warning(childTag == null, 'validateDOMNesting: when childText is passed, childTag should be null');
      childTag = '#text';
    }

    var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
    var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
    var invalidParentOrAncestor = invalidParent || invalidAncestor;
    if (!invalidParentOrAncestor) {
      return;
    }

    var ancestorTag = invalidParentOrAncestor.tag;
    var addendum = getCurrentFiberStackAddendum$6();

    var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + addendum;
    if (didWarn[warnKey]) {
      return;
    }
    didWarn[warnKey] = true;

    var tagDisplayName = childTag;
    var whitespaceInfo = '';
    if (childTag === '#text') {
      if (/\S/.test(childText)) {
        tagDisplayName = 'Text nodes';
      } else {
        tagDisplayName = 'Whitespace text nodes';
        whitespaceInfo = " Make sure you don't have any extra whitespace between tags on " + 'each line of your source code.';
      }
    } else {
      tagDisplayName = '<' + childTag + '>';
    }

    if (invalidParent) {
      var info = '';
      if (ancestorTag === 'table' && childTag === 'tr') {
        info += ' Add a <tbody> to your code to match the DOM tree generated by ' + 'the browser.';
      }
      warning(false, 'validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s%s', tagDisplayName, ancestorTag, whitespaceInfo, info, addendum);
    } else {
      warning(false, 'validateDOMNesting(...): %s cannot appear as a descendant of ' + '<%s>.%s', tagDisplayName, ancestorTag, addendum);
    }
  };

  // TODO: turn this into a named export
  validateDOMNesting.updatedAncestorInfo = updatedAncestorInfo$1;

  // For testing
  validateDOMNesting.isTagValidInContext = function (tag, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.current;
    var parentTag = parentInfo && parentInfo.tag;
    return isTagValidWithParent(tag, parentTag) && !findInvalidAncestorForTag(tag, ancestorInfo);
  };
}

var validateDOMNesting$1 = validateDOMNesting;

// TODO: direct imports like some-package/src/* are bad. Fix me.
var createElement = createElement$1;
var createTextNode = createTextNode$1;
var setInitialProperties = setInitialProperties$1;
var diffProperties = diffProperties$1;
var updateProperties = updateProperties$1;
var diffHydratedProperties = diffHydratedProperties$1;
var diffHydratedText = diffHydratedText$1;
var warnForUnmatchedText = warnForUnmatchedText$1;
var warnForDeletedHydratableElement = warnForDeletedHydratableElement$1;
var warnForDeletedHydratableText = warnForDeletedHydratableText$1;
var warnForInsertedHydratedElement = warnForInsertedHydratedElement$1;
var warnForInsertedHydratedText = warnForInsertedHydratedText$1;
var updatedAncestorInfo = validateDOMNesting$1.updatedAncestorInfo;
var precacheFiberNode = precacheFiberNode$1;
var updateFiberProps = updateFiberProps$1;


{
  var SUPPRESS_HYDRATION_WARNING = 'suppressHydrationWarning';
  if (typeof Map !== 'function' || Map.prototype == null || typeof Map.prototype.forEach !== 'function' || typeof Set !== 'function' || Set.prototype == null || typeof Set.prototype.clear !== 'function' || typeof Set.prototype.forEach !== 'function') {
    warning(false, 'React depends on Map and Set built-in types. Make sure that you load a ' + 'polyfill in older browsers. http://fb.me/react-polyfills');
  }
}

injection$3.injectFiberControlledHostComponent(ReactDOMFiberComponent);

var eventsEnabled = null;
var selectionInformation = null;

/**
 * True if the supplied DOM node is a valid node element.
 *
 * @param {?DOMElement} node The candidate DOM node.
 * @return {boolean} True if the DOM is a valid DOM node.
 * @internal
 */
function isValidContainer(node) {
  return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === ' react-mount-point-unstable '));
}

function getReactRootElementInContainer(container) {
  if (!container) {
    return null;
  }

  if (container.nodeType === DOCUMENT_NODE) {
    return container.documentElement;
  } else {
    return container.firstChild;
  }
}

function shouldHydrateDueToLegacyHeuristic(container) {
  var rootElement = getReactRootElementInContainer(container);
  return !!(rootElement && rootElement.nodeType === ELEMENT_NODE && rootElement.hasAttribute(ROOT_ATTRIBUTE_NAME));
}

function shouldAutoFocusHostComponent(type, props) {
  switch (type) {
    case 'button':
    case 'input':
    case 'select':
    case 'textarea':
      return !!props.autoFocus;
  }
  return false;
}

var DOMRenderer = reactReconciler({
  getRootHostContext: function (rootContainerInstance) {
    var type = void 0;
    var namespace = void 0;
    var nodeType = rootContainerInstance.nodeType;
    switch (nodeType) {
      case DOCUMENT_NODE:
      case DOCUMENT_FRAGMENT_NODE:
        {
          type = nodeType === DOCUMENT_NODE ? '#document' : '#fragment';
          var root = rootContainerInstance.documentElement;
          namespace = root ? root.namespaceURI : getChildNamespace(null, '');
          break;
        }
      default:
        {
          var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
          var ownNamespace = container.namespaceURI || null;
          type = container.tagName;
          namespace = getChildNamespace(ownNamespace, type);
          break;
        }
    }
    {
      var validatedTag = type.toLowerCase();
      var _ancestorInfo = updatedAncestorInfo(null, validatedTag, null);
      return { namespace: namespace, ancestorInfo: _ancestorInfo };
    }
    return namespace;
  },
  getChildHostContext: function (parentHostContext, type) {
    {
      var parentHostContextDev = parentHostContext;
      var _namespace = getChildNamespace(parentHostContextDev.namespace, type);
      var _ancestorInfo2 = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type, null);
      return { namespace: _namespace, ancestorInfo: _ancestorInfo2 };
    }
    var parentNamespace = parentHostContext;
    return getChildNamespace(parentNamespace, type);
  },
  getPublicInstance: function (instance) {
    return instance;
  },
  prepareForCommit: function () {
    eventsEnabled = isEnabled();
    selectionInformation = getSelectionInformation();
    setEnabled(false);
  },
  resetAfterCommit: function () {
    restoreSelection(selectionInformation);
    selectionInformation = null;
    setEnabled(eventsEnabled);
    eventsEnabled = null;
  },
  createInstance: function (type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
    var parentNamespace = void 0;
    {
      // TODO: take namespace into account when validating.
      var hostContextDev = hostContext;
      validateDOMNesting$1(type, null, hostContextDev.ancestorInfo);
      if (typeof props.children === 'string' || typeof props.children === 'number') {
        var string = '' + props.children;
        var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type, null);
        validateDOMNesting$1(null, string, ownAncestorInfo);
      }
      parentNamespace = hostContextDev.namespace;
    }
    var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
    precacheFiberNode(internalInstanceHandle, domElement);
    updateFiberProps(domElement, props);
    return domElement;
  },
  appendInitialChild: function (parentInstance, child) {
    parentInstance.appendChild(child);
  },
  finalizeInitialChildren: function (domElement, type, props, rootContainerInstance) {
    setInitialProperties(domElement, type, props, rootContainerInstance);
    return shouldAutoFocusHostComponent(type, props);
  },
  prepareUpdate: function (domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
    {
      var hostContextDev = hostContext;
      if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === 'string' || typeof newProps.children === 'number')) {
        var string = '' + newProps.children;
        var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type, null);
        validateDOMNesting$1(null, string, ownAncestorInfo);
      }
    }
    return diffProperties(domElement, type, oldProps, newProps, rootContainerInstance);
  },
  shouldSetTextContent: function (type, props) {
    return type === 'textarea' || typeof props.children === 'string' || typeof props.children === 'number' || typeof props.dangerouslySetInnerHTML === 'object' && props.dangerouslySetInnerHTML !== null && typeof props.dangerouslySetInnerHTML.__html === 'string';
  },
  shouldDeprioritizeSubtree: function (type, props) {
    return !!props.hidden;
  },
  createTextInstance: function (text, rootContainerInstance, hostContext, internalInstanceHandle) {
    {
      var hostContextDev = hostContext;
      validateDOMNesting$1(null, text, hostContextDev.ancestorInfo);
    }
    var textNode = createTextNode(text, rootContainerInstance);
    precacheFiberNode(internalInstanceHandle, textNode);
    return textNode;
  },


  now: now,

  mutation: {
    commitMount: function (domElement, type, newProps, internalInstanceHandle) {
      domElement.focus();
    },
    commitUpdate: function (domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
      // Update the props handle so that we know which props are the ones with
      // with current event handlers.
      updateFiberProps(domElement, newProps);
      // Apply the diff to the DOM node.
      updateProperties(domElement, updatePayload, type, oldProps, newProps);
    },
    resetTextContent: function (domElement) {
      domElement.textContent = '';
    },
    commitTextUpdate: function (textInstance, oldText, newText) {
      textInstance.nodeValue = newText;
    },
    appendChild: function (parentInstance, child) {
      parentInstance.appendChild(child);
    },
    appendChildToContainer: function (container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, container);
      } else {
        container.appendChild(child);
      }
    },
    insertBefore: function (parentInstance, child, beforeChild) {
      parentInstance.insertBefore(child, beforeChild);
    },
    insertInContainerBefore: function (container, child, beforeChild) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, beforeChild);
      } else {
        container.insertBefore(child, beforeChild);
      }
    },
    removeChild: function (parentInstance, child) {
      parentInstance.removeChild(child);
    },
    removeChildFromContainer: function (container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.removeChild(child);
      } else {
        container.removeChild(child);
      }
    }
  },

  hydration: {
    canHydrateInstance: function (instance, type, props) {
      if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) {
        return null;
      }
      // This has now been refined to an element node.
      return instance;
    },
    canHydrateTextInstance: function (instance, text) {
      if (text === '' || instance.nodeType !== TEXT_NODE) {
        // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
      }
      // This has now been refined to a text node.
      return instance;
    },
    getNextHydratableSibling: function (instance) {
      var node = instance.nextSibling;
      // Skip non-hydratable nodes.
      while (node && node.nodeType !== ELEMENT_NODE && node.nodeType !== TEXT_NODE) {
        node = node.nextSibling;
      }
      return node;
    },
    getFirstHydratableChild: function (parentInstance) {
      var next = parentInstance.firstChild;
      // Skip non-hydratable nodes.
      while (next && next.nodeType !== ELEMENT_NODE && next.nodeType !== TEXT_NODE) {
        next = next.nextSibling;
      }
      return next;
    },
    hydrateInstance: function (instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, instance);
      // TODO: Possibly defer this until the commit phase where all the events
      // get attached.
      updateFiberProps(instance, props);
      var parentNamespace = void 0;
      {
        var hostContextDev = hostContext;
        parentNamespace = hostContextDev.namespace;
      }
      return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance);
    },
    hydrateTextInstance: function (textInstance, text, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, textInstance);
      return diffHydratedText(textInstance, text);
    },
    didNotMatchHydratedContainerTextInstance: function (parentContainer, textInstance, text) {
      {
        warnForUnmatchedText(textInstance, text);
      }
    },
    didNotMatchHydratedTextInstance: function (parentType, parentProps, parentInstance, textInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForUnmatchedText(textInstance, text);
      }
    },
    didNotHydrateContainerInstance: function (parentContainer, instance) {
      {
        if (instance.nodeType === 1) {
          warnForDeletedHydratableElement(parentContainer, instance);
        } else {
          warnForDeletedHydratableText(parentContainer, instance);
        }
      }
    },
    didNotHydrateInstance: function (parentType, parentProps, parentInstance, instance) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        if (instance.nodeType === 1) {
          warnForDeletedHydratableElement(parentInstance, instance);
        } else {
          warnForDeletedHydratableText(parentInstance, instance);
        }
      }
    },
    didNotFindHydratableContainerInstance: function (parentContainer, type, props) {
      {
        warnForInsertedHydratedElement(parentContainer, type, props);
      }
    },
    didNotFindHydratableContainerTextInstance: function (parentContainer, text) {
      {
        warnForInsertedHydratedText(parentContainer, text);
      }
    },
    didNotFindHydratableInstance: function (parentType, parentProps, parentInstance, type, props) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedElement(parentInstance, type, props);
      }
    },
    didNotFindHydratableTextInstance: function (parentType, parentProps, parentInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedText(parentInstance, text);
      }
    }
  },

  scheduleDeferredCallback: rIC,
  cancelDeferredCallback: cIC,

  useSyncScheduling: !enableAsyncSchedulingByDefaultInReactDOM
});

injection$4.injectFiberBatchedUpdates(DOMRenderer.batchedUpdates);

var warnedAboutHydrateAPI = false;

function renderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
  !isValidContainer(container) ? invariant(false, 'Target container is not a DOM element.') : void 0;

  {
    if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
      var hostInstance = DOMRenderer.findHostInstanceWithNoPortals(container._reactRootContainer.current);
      if (hostInstance) {
        warning(hostInstance.parentNode === container, 'render(...): It looks like the React-rendered content of this ' + 'container was removed without using React. This is not ' + 'supported and will cause errors. Instead, call ' + 'ReactDOM.unmountComponentAtNode to empty a container.');
      }
    }

    var isRootRenderedBySomeReact = !!container._reactRootContainer;
    var rootEl = getReactRootElementInContainer(container);
    var hasNonRootReactChild = !!(rootEl && getInstanceFromNode$1(rootEl));

    warning(!hasNonRootReactChild || isRootRenderedBySomeReact, 'render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.');

    warning(container.nodeType !== ELEMENT_NODE || !container.tagName || container.tagName.toUpperCase() !== 'BODY', 'render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.');
  }

  var root = container._reactRootContainer;
  if (!root) {
    var shouldHydrate = forceHydrate || shouldHydrateDueToLegacyHeuristic(container);
    // First clear any existing content.
    if (!shouldHydrate) {
      var warned = false;
      var rootSibling = void 0;
      while (rootSibling = container.lastChild) {
        {
          if (!warned && rootSibling.nodeType === ELEMENT_NODE && rootSibling.hasAttribute(ROOT_ATTRIBUTE_NAME)) {
            warned = true;
            warning(false, 'render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.');
          }
        }
        container.removeChild(rootSibling);
      }
    }
    {
      if (shouldHydrate && !forceHydrate && !warnedAboutHydrateAPI) {
        warnedAboutHydrateAPI = true;
        lowPriorityWarning$1(false, 'render(): Calling ReactDOM.render() to hydrate server-rendered markup ' + 'will stop working in React v17. Replace the ReactDOM.render() call ' + 'with ReactDOM.hydrate() if you want React to attach to the server HTML.');
      }
    }
    var newRoot = DOMRenderer.createContainer(container, shouldHydrate);
    root = container._reactRootContainer = newRoot;
    // Initial mount should not be batched.
    DOMRenderer.unbatchedUpdates(function () {
      DOMRenderer.updateContainer(children, newRoot, parentComponent, callback);
    });
  } else {
    DOMRenderer.updateContainer(children, root, parentComponent, callback);
  }
  return DOMRenderer.getPublicRootInstance(root);
}

function createPortal(children, container) {
  var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;

  !isValidContainer(container) ? invariant(false, 'Target container is not a DOM element.') : void 0;
  // TODO: pass ReactDOM portal implementation as third argument
  return createPortal$1(children, container, null, key);
}

function ReactRoot(container, hydrate) {
  var root = DOMRenderer.createContainer(container, hydrate);
  this._reactRootContainer = root;
}
ReactRoot.prototype.render = function (children, callback) {
  var root = this._reactRootContainer;
  DOMRenderer.updateContainer(children, root, null, callback);
};
ReactRoot.prototype.unmount = function (callback) {
  var root = this._reactRootContainer;
  DOMRenderer.updateContainer(null, root, null, callback);
};

var ReactDOM = {
  createPortal: createPortal,

  findDOMNode: function (componentOrElement) {
    {
      var owner = ReactCurrentOwner.current;
      if (owner !== null) {
        var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;
        warning(warnedAboutRefsInRender, '%s is accessing findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(owner) || 'A component');
        owner.stateNode._warnedAboutRefsInRender = true;
      }
    }
    if (componentOrElement == null) {
      return null;
    }
    if (componentOrElement.nodeType === ELEMENT_NODE) {
      return componentOrElement;
    }

    var inst = get(componentOrElement);
    if (inst) {
      return DOMRenderer.findHostInstance(inst);
    }

    if (typeof componentOrElement.render === 'function') {
      invariant(false, 'Unable to find node on an unmounted component.');
    } else {
      invariant(false, 'Element appears to be neither ReactComponent nor DOMNode. Keys: %s', Object.keys(componentOrElement));
    }
  },
  hydrate: function (element, container, callback) {
    // TODO: throw or warn if we couldn't hydrate?
    return renderSubtreeIntoContainer(null, element, container, true, callback);
  },
  render: function (element, container, callback) {
    return renderSubtreeIntoContainer(null, element, container, false, callback);
  },
  unstable_renderSubtreeIntoContainer: function (parentComponent, element, containerNode, callback) {
    !(parentComponent != null && has(parentComponent)) ? invariant(false, 'parentComponent must be a valid React Component') : void 0;
    return renderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
  },
  unmountComponentAtNode: function (container) {
    !isValidContainer(container) ? invariant(false, 'unmountComponentAtNode(...): Target container is not a DOM element.') : void 0;

    if (container._reactRootContainer) {
      {
        var rootEl = getReactRootElementInContainer(container);
        var renderedByDifferentReact = rootEl && !getInstanceFromNode$1(rootEl);
        warning(!renderedByDifferentReact, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by another copy of React.');
      }

      // Unmount should not be batched.
      DOMRenderer.unbatchedUpdates(function () {
        renderSubtreeIntoContainer(null, null, container, false, function () {
          container._reactRootContainer = null;
        });
      });
      // If you call unmountComponentAtNode twice in quick succession, you'll
      // get `true` twice. That's probably fine?
      return true;
    } else {
      {
        var _rootEl = getReactRootElementInContainer(container);
        var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode$1(_rootEl));

        // Check if the container itself is a React root node.
        var isContainerReactRoot = container.nodeType === 1 && isValidContainer(container.parentNode) && !!container.parentNode._reactRootContainer;

        warning(!hasNonRootReactChild, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.');
      }

      return false;
    }
  },


  // Temporary alias since we already shipped React 16 RC with it.
  // TODO: remove in React 17.
  unstable_createPortal: createPortal,

  unstable_batchedUpdates: batchedUpdates,

  unstable_deferredUpdates: DOMRenderer.deferredUpdates,

  flushSync: DOMRenderer.flushSync,

  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
    // For TapEventPlugin which is popular in open source
    EventPluginHub: EventPluginHub,
    // Used by test-utils
    EventPluginRegistry: EventPluginRegistry,
    EventPropagators: EventPropagators,
    ReactControlledComponent: ReactControlledComponent,
    ReactDOMComponentTree: ReactDOMComponentTree,
    ReactDOMEventListener: ReactDOMEventListener
  }
};

if (enableCreateRoot) {
  ReactDOM.createRoot = function createRoot(container, options) {
    var hydrate = options != null && options.hydrate === true;
    return new ReactRoot(container, hydrate);
  };
}

var foundDevTools = DOMRenderer.injectIntoDevTools({
  findFiberByHostInstance: getClosestInstanceFromNode,
  bundleType: 1,
  version: ReactVersion,
  rendererPackageName: 'react-dom'
});

{
  if (!foundDevTools && ExecutionEnvironment.canUseDOM && window.top === window.self) {
    // If we're in Chrome or Firefox, provide a download link if not installed.
    if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
      var protocol = window.location.protocol;
      // Don't warn in exotic cases like chrome-extension://.
      if (/^(https?|file):$/.test(protocol)) {
        console.info('%cDownload the React DevTools ' + 'for a better development experience: ' + 'https://fb.me/react-devtools' + (protocol === 'file:' ? '\nYou might need to use a local HTTP server (instead of file://): ' + 'https://fb.me/react-devtools-faq' : ''), 'font-weight:bold');
      }
    }
  }
}



var ReactDOM$2 = Object.freeze({
	default: ReactDOM
});

var ReactDOM$3 = ( ReactDOM$2 && ReactDOM ) || ReactDOM$2;

// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var reactDom = ReactDOM$3['default'] ? ReactDOM$3['default'] : ReactDOM$3;

module.exports = reactDom;
  })();
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 25 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */



var hyphenate = __webpack_require__(26);

var msPattern = /^ms-/;

/**
 * Hyphenates a camelcased CSS property name, for example:
 *
 *   > hyphenateStyleName('backgroundColor')
 *   < "background-color"
 *   > hyphenateStyleName('MozTransition')
 *   < "-moz-transition"
 *   > hyphenateStyleName('msTransition')
 *   < "-ms-transition"
 *
 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
 * is converted to `-ms-`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenateStyleName(string) {
  return hyphenate(string).replace(msPattern, '-ms-');
}

module.exports = hyphenateStyleName;

/***/ }),
/* 26 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var _uppercasePattern = /([A-Z])/g;

/**
 * Hyphenates a camelcased string, for example:
 *
 *   > hyphenate('backgroundColor')
 *   < "background-color"
 *
 * For CSS style names, use `hyphenateStyleName` instead which works properly
 * with all vendor prefixes, including `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenate(string) {
  return string.replace(_uppercasePattern, '-$1').toLowerCase();
}

module.exports = hyphenate;

/***/ }),
/* 27 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */



var camelize = __webpack_require__(28);

var msPattern = /^-ms-/;

/**
 * Camelcases a hyphenated CSS property name, for example:
 *
 *   > camelizeStyleName('background-color')
 *   < "backgroundColor"
 *   > camelizeStyleName('-moz-transition')
 *   < "MozTransition"
 *   > camelizeStyleName('-ms-transition')
 *   < "msTransition"
 *
 * As Andi Smith suggests
 * (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
 * is converted to lowercase `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function camelizeStyleName(string) {
  return camelize(string.replace(msPattern, 'ms-'));
}

module.exports = camelizeStyleName;

/***/ }),
/* 28 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var _hyphenPattern = /-(.)/g;

/**
 * Camelcases a hyphenated string, for example:
 *
 *   > camelize('background-color')
 *   < "backgroundColor"
 *
 * @param {string} string
 * @return {string}
 */
function camelize(string) {
  return string.replace(_hyphenPattern, function (_, character) {
    return character.toUpperCase();
  });
}

module.exports = camelize;

/***/ })
/******/ ]);
//# sourceMappingURL=scala-js-starter-opt-bundle.js.map