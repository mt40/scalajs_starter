/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 14);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {

// shim for using process in browser
var process = module.exports = {};

// cached from whatever global is present so that test runners that stub it
// don't break things.  But we need to wrap it in a try catch in case it is
// wrapped in strict mode code which doesn't define any globals.  It's inside a
// function because try/catches deoptimize in certain engines.

var cachedSetTimeout;
var cachedClearTimeout;

function defaultSetTimout() {
    throw new Error('setTimeout has not been defined');
}
function defaultClearTimeout () {
    throw new Error('clearTimeout has not been defined');
}
(function () {
    try {
        if (typeof setTimeout === 'function') {
            cachedSetTimeout = setTimeout;
        } else {
            cachedSetTimeout = defaultSetTimout;
        }
    } catch (e) {
        cachedSetTimeout = defaultSetTimout;
    }
    try {
        if (typeof clearTimeout === 'function') {
            cachedClearTimeout = clearTimeout;
        } else {
            cachedClearTimeout = defaultClearTimeout;
        }
    } catch (e) {
        cachedClearTimeout = defaultClearTimeout;
    }
} ())
function runTimeout(fun) {
    if (cachedSetTimeout === setTimeout) {
        //normal enviroments in sane situations
        return setTimeout(fun, 0);
    }
    // if setTimeout wasn't available but was latter defined
    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {
        cachedSetTimeout = setTimeout;
        return setTimeout(fun, 0);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedSetTimeout(fun, 0);
    } catch(e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally
            return cachedSetTimeout.call(null, fun, 0);
        } catch(e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error
            return cachedSetTimeout.call(this, fun, 0);
        }
    }


}
function runClearTimeout(marker) {
    if (cachedClearTimeout === clearTimeout) {
        //normal enviroments in sane situations
        return clearTimeout(marker);
    }
    // if clearTimeout wasn't available but was latter defined
    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {
        cachedClearTimeout = clearTimeout;
        return clearTimeout(marker);
    }
    try {
        // when when somebody has screwed with setTimeout but no I.E. maddness
        return cachedClearTimeout(marker);
    } catch (e){
        try {
            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally
            return cachedClearTimeout.call(null, marker);
        } catch (e){
            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.
            // Some versions of I.E. have different rules for clearTimeout vs setTimeout
            return cachedClearTimeout.call(this, marker);
        }
    }



}
var queue = [];
var draining = false;
var currentQueue;
var queueIndex = -1;

function cleanUpNextTick() {
    if (!draining || !currentQueue) {
        return;
    }
    draining = false;
    if (currentQueue.length) {
        queue = currentQueue.concat(queue);
    } else {
        queueIndex = -1;
    }
    if (queue.length) {
        drainQueue();
    }
}

function drainQueue() {
    if (draining) {
        return;
    }
    var timeout = runTimeout(cleanUpNextTick);
    draining = true;

    var len = queue.length;
    while(len) {
        currentQueue = queue;
        queue = [];
        while (++queueIndex < len) {
            if (currentQueue) {
                currentQueue[queueIndex].run();
            }
        }
        queueIndex = -1;
        len = queue.length;
    }
    currentQueue = null;
    draining = false;
    runClearTimeout(timeout);
}

process.nextTick = function (fun) {
    var args = new Array(arguments.length - 1);
    if (arguments.length > 1) {
        for (var i = 1; i < arguments.length; i++) {
            args[i - 1] = arguments[i];
        }
    }
    queue.push(new Item(fun, args));
    if (queue.length === 1 && !draining) {
        runTimeout(drainQueue);
    }
};

// v8 likes predictible objects
function Item(fun, array) {
    this.fun = fun;
    this.array = array;
}
Item.prototype.run = function () {
    this.fun.apply(null, this.array);
};
process.title = 'browser';
process.browser = true;
process.env = {};
process.argv = [];
process.version = ''; // empty string to avoid regexp issues
process.versions = {};

function noop() {}

process.on = noop;
process.addListener = noop;
process.once = noop;
process.off = noop;
process.removeListener = noop;
process.removeAllListeners = noop;
process.emit = noop;
process.prependListener = noop;
process.prependOnceListener = noop;

process.listeners = function (name) { return [] }

process.binding = function (name) {
    throw new Error('process.binding is not supported');
};

process.cwd = function () { return '/' };
process.chdir = function (dir) {
    throw new Error('process.chdir is not supported');
};
process.umask = function() { return 0; };


/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

function makeEmptyFunction(arg) {
  return function () {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
var emptyFunction = function emptyFunction() {};

emptyFunction.thatReturns = makeEmptyFunction;
emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
emptyFunction.thatReturnsNull = makeEmptyFunction(null);
emptyFunction.thatReturnsThis = function () {
  return this;
};
emptyFunction.thatReturnsArgument = function (arg) {
  return arg;
};

module.exports = emptyFunction;

/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/


/* eslint-disable no-unused-vars */
var getOwnPropertySymbols = Object.getOwnPropertySymbols;
var hasOwnProperty = Object.prototype.hasOwnProperty;
var propIsEnumerable = Object.prototype.propertyIsEnumerable;

function toObject(val) {
	if (val === null || val === undefined) {
		throw new TypeError('Object.assign cannot be called with null or undefined');
	}

	return Object(val);
}

function shouldUseNative() {
	try {
		if (!Object.assign) {
			return false;
		}

		// Detect buggy property enumeration order in older V8 versions.

		// https://bugs.chromium.org/p/v8/issues/detail?id=4118
		var test1 = new String('abc');  // eslint-disable-line no-new-wrappers
		test1[5] = 'de';
		if (Object.getOwnPropertyNames(test1)[0] === '5') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test2 = {};
		for (var i = 0; i < 10; i++) {
			test2['_' + String.fromCharCode(i)] = i;
		}
		var order2 = Object.getOwnPropertyNames(test2).map(function (n) {
			return test2[n];
		});
		if (order2.join('') !== '0123456789') {
			return false;
		}

		// https://bugs.chromium.org/p/v8/issues/detail?id=3056
		var test3 = {};
		'abcdefghijklmnopqrst'.split('').forEach(function (letter) {
			test3[letter] = letter;
		});
		if (Object.keys(Object.assign({}, test3)).join('') !==
				'abcdefghijklmnopqrst') {
			return false;
		}

		return true;
	} catch (err) {
		// We don't expect any of the above to throw, but better to be safe.
		return false;
	}
}

module.exports = shouldUseNative() ? Object.assign : function (target, source) {
	var from;
	var to = toObject(target);
	var symbols;

	for (var s = 1; s < arguments.length; s++) {
		from = Object(arguments[s]);

		for (var key in from) {
			if (hasOwnProperty.call(from, key)) {
				to[key] = from[key];
			}
		}

		if (getOwnPropertySymbols) {
			symbols = getOwnPropertySymbols(from);
			for (var i = 0; i < symbols.length; i++) {
				if (propIsEnumerable.call(from, symbols[i])) {
					to[symbols[i]] = from[symbols[i]];
				}
			}
		}
	}

	return to;
};


/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyObject = {};

if (process.env.NODE_ENV !== 'production') {
  Object.freeze(emptyObject);
}

module.exports = emptyObject;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 4 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

if (process.env.NODE_ENV === 'production') {
  module.exports = __webpack_require__(17);
} else {
  module.exports = __webpack_require__(18);
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 5 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var validateFormat = function validateFormat(format) {};

if (process.env.NODE_ENV !== 'production') {
  validateFormat = function validateFormat(format) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }
  };
}

function invariant(condition, format, a, b, c, d, e, f) {
  validateFormat(format);

  if (!condition) {
    var error;
    if (format === undefined) {
      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');
    } else {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      error = new Error(format.replace(/%s/g, function () {
        return args[argIndex++];
      }));
      error.name = 'Invariant Violation';
    }

    error.framesToPop = 1; // we don't care about invariant's own frame
    throw error;
  }
}

module.exports = invariant;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2014-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var emptyFunction = __webpack_require__(1);

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if (process.env.NODE_ENV !== 'production') {
  var printWarning = function printWarning(format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  warning = function warning(condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }

    if (format.indexOf('Failed Composite propType: ') === 0) {
      return; // Ignore CompositeComponent proptype check.
    }

    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

module.exports = warning;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 7 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var printWarning = function() {};

if (process.env.NODE_ENV !== 'production') {
  var ReactPropTypesSecret = __webpack_require__(19);
  var loggedTypeFailures = {};

  printWarning = function(text) {
    var message = 'Warning: ' + text;
    if (typeof console !== 'undefined') {
      console.error(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };
}

/**
 * Assert that the values match with the type specs.
 * Error messages are memorized and will only be shown once.
 *
 * @param {object} typeSpecs Map of name to a ReactPropType
 * @param {object} values Runtime values that need to be type-checked
 * @param {string} location e.g. "prop", "context", "child context"
 * @param {string} componentName Name of the component for error messages.
 * @param {?Function} getStack Returns the component stack.
 * @private
 */
function checkPropTypes(typeSpecs, values, location, componentName, getStack) {
  if (process.env.NODE_ENV !== 'production') {
    for (var typeSpecName in typeSpecs) {
      if (typeSpecs.hasOwnProperty(typeSpecName)) {
        var error;
        // Prop type validation may throw. In case they do, we don't want to
        // fail the render phase where it didn't fail before. So we log it.
        // After these have been cleaned up, we'll let them throw.
        try {
          // This is intentionally an invariant that gets caught. It's the same
          // behavior as without this statement except with a better message.
          if (typeof typeSpecs[typeSpecName] !== 'function') {
            var err = Error(
              (componentName || 'React class') + ': ' + location + ' type `' + typeSpecName + '` is invalid; ' +
              'it must be a function, usually from the `prop-types` package, but received `' + typeof typeSpecs[typeSpecName] + '`.'
            );
            err.name = 'Invariant Violation';
            throw err;
          }
          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);
        } catch (ex) {
          error = ex;
        }
        if (error && !(error instanceof Error)) {
          printWarning(
            (componentName || 'React class') + ': type specification of ' +
            location + ' `' + typeSpecName + '` is invalid; the type checker ' +
            'function must return `null` or an `Error` but returned a ' + typeof error + '. ' +
            'You may have forgotten to pass an argument to the type checker ' +
            'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' +
            'shape all require an argument).'
          )

        }
        if (error instanceof Error && !(error.message in loggedTypeFailures)) {
          // Only monitor this failure once because there tends to be a lot of the
          // same error.
          loggedTypeFailures[error.message] = true;

          var stack = getStack ? getStack() : '';

          printWarning(
            'Failed ' + location + ' type: ' + error.message + (stack != null ? stack : '')
          );
        }
      }
    }
  }
}

module.exports = checkPropTypes;

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



var canUseDOM = !!(typeof window !== 'undefined' && window.document && window.document.createElement);

/**
 * Simple, lightweight module assisting with the detection and context of
 * Worker. Helps avoid circular dependencies and allows code to reason about
 * whether or not they are in a Worker, even if they never include the main
 * `ReactWorker` dependency.
 */
var ExecutionEnvironment = {

  canUseDOM: canUseDOM,

  canUseWorkers: typeof Worker !== 'undefined',

  canUseEventListeners: canUseDOM && !!(window.addEventListener || window.attachEvent),

  canUseViewport: canUseDOM && !!window.screen,

  isInWorker: !canUseDOM // For now, this is true - might change in the future.

};

module.exports = ExecutionEnvironment;

/***/ }),
/* 9 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var emptyFunction = __webpack_require__(1);

/**
 * Upstream version of event listener. Does not take into account specific
 * nature of platform.
 */
var EventListener = {
  /**
   * Listen to DOM events during the bubble phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  listen: function listen(target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, false);
      return {
        remove: function remove() {
          target.removeEventListener(eventType, callback, false);
        }
      };
    } else if (target.attachEvent) {
      target.attachEvent('on' + eventType, callback);
      return {
        remove: function remove() {
          target.detachEvent('on' + eventType, callback);
        }
      };
    }
  },

  /**
   * Listen to DOM events during the capture phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  capture: function capture(target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, true);
      return {
        remove: function remove() {
          target.removeEventListener(eventType, callback, true);
        }
      };
    } else {
      if (process.env.NODE_ENV !== 'production') {
        console.error('Attempted to listen to events during the capture phase on a ' + 'browser that does not support the capture phase. Your application ' + 'will not receive some events.');
      }
      return {
        remove: emptyFunction
      };
    }
  },

  registerDefault: function registerDefault() {}
};

module.exports = EventListener;
/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

/* eslint-disable fb-www/typeof-undefined */

/**
 * Same as document.activeElement but wraps in a try-catch block. In IE it is
 * not safe to call document.activeElement if there is nothing focused.
 *
 * The activeElement will be null only if the document or document body is not
 * yet defined.
 *
 * @param {?DOMDocument} doc Defaults to current document.
 * @return {?DOMElement}
 */
function getActiveElement(doc) /*?DOMElement*/{
  doc = doc || (typeof document !== 'undefined' ? document : undefined);
  if (typeof doc === 'undefined') {
    return null;
  }
  try {
    return doc.activeElement || doc.body;
  } catch (e) {
    return doc.body;
  }
}

module.exports = getActiveElement;

/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 * 
 */

/*eslint-disable no-self-compare */



var hasOwnProperty = Object.prototype.hasOwnProperty;

/**
 * inlined Object.is polyfill to avoid requiring consumers ship their own
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is
 */
function is(x, y) {
  // SameValue algorithm
  if (x === y) {
    // Steps 1-5, 7-10
    // Steps 6.b-6.e: +0 != -0
    // Added the nonzero y check to make Flow happy, but it is redundant
    return x !== 0 || y !== 0 || 1 / x === 1 / y;
  } else {
    // Step 6.a: NaN == NaN
    return x !== x && y !== y;
  }
}

/**
 * Performs equality by iterating through keys on an object and returning false
 * when any key has values which are not strictly equal between the arguments.
 * Returns true when the values of all keys are strictly equal.
 */
function shallowEqual(objA, objB) {
  if (is(objA, objB)) {
    return true;
  }

  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
    return false;
  }

  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);

  if (keysA.length !== keysB.length) {
    return false;
  }

  // Test for A's keys different from B.
  for (var i = 0; i < keysA.length; i++) {
    if (!hasOwnProperty.call(objB, keysA[i]) || !is(objA[keysA[i]], objB[keysA[i]])) {
      return false;
    }
  }

  return true;
}

module.exports = shallowEqual;

/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * 
 */

var isTextNode = __webpack_require__(22);

/*eslint-disable no-bitwise */

/**
 * Checks if a given DOM node contains or is another DOM node.
 */
function containsNode(outerNode, innerNode) {
  if (!outerNode || !innerNode) {
    return false;
  } else if (outerNode === innerNode) {
    return true;
  } else if (isTextNode(outerNode)) {
    return false;
  } else if (isTextNode(innerNode)) {
    return containsNode(outerNode, innerNode.parentNode);
  } else if ('contains' in outerNode) {
    return outerNode.contains(innerNode);
  } else if (outerNode.compareDocumentPosition) {
    return !!(outerNode.compareDocumentPosition(innerNode) & 16);
  } else {
    return false;
  }
}

module.exports = containsNode;

/***/ }),
/* 13 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 */



/**
 * @param {DOMElement} node input/textarea to focus
 */

function focusNode(node) {
  // IE8 can throw "Can't move focus to the control because it is invisible,
  // not enabled, or of a type that does not accept the focus." for all kinds of
  // reasons that are too expensive and fragile to test.
  try {
    node.focus();
  } catch (e) {}
}

module.exports = focusNode;

/***/ }),
/* 14 */
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__(15);


/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(global) {
var d,aa="object"===typeof __ScalaJSEnv&&__ScalaJSEnv?__ScalaJSEnv:{},g="object"===typeof aa.global&&aa.global?aa.global:"object"===typeof global&&global&&global.Object===Object?global:this;aa.global=g;aa.exportsNamespace=exports;g.Object.freeze(aa);var ba={envInfo:aa,semantics:{asInstanceOfs:2,arrayIndexOutOfBounds:2,moduleInit:2,strictFloats:!1,productionMode:!0},assumingES6:!1,linkerVersion:"0.6.25",globalThis:this};g.Object.freeze(ba);g.Object.freeze(ba.semantics);
var ca=g.Math.imul||function(a,b){var c=a&65535,e=b&65535;return c*e+((a>>>16&65535)*e+c*(b>>>16&65535)<<16>>>0)|0},da=g.Math.clz32||function(a){if(0===a)return 32;var b=1;0===(a&4294901760)&&(a<<=16,b+=16);0===(a&4278190080)&&(a<<=8,b+=8);0===(a&4026531840)&&(a<<=4,b+=4);0===(a&3221225472)&&(a<<=2,b+=2);return b+(a>>31)},ea=0,fa=g.WeakMap?new g.WeakMap:null;function ga(a){return function(b,c){return!(!b||!b.$classData||b.$classData.Zg!==c||b.$classData.Yg!==a)}}
function ha(a){for(var b in a)return b}function ia(a,b){return new a.Bl(b)}function k(a,b){return ja(a,b,0)}function ja(a,b,c){var e=new a.Bl(b[c]);if(c<b.length-1){a=a.Xh;c+=1;for(var f=e.e,h=0;h<f.length;h++)f[h]=ja(a,b,c)}return e}function ka(a){return void 0===a?"undefined":a.toString()}
function la(a){switch(typeof a){case "string":return ma(na);case "number":var b=a|0;return b===a?oa(b)?ma(pa):qa(b)?ma(ra):ma(sa):"number"===typeof a?ma(ta):ma(ua);case "boolean":return ma(va);case "undefined":return ma(wa);default:return null===a?a.$C():xa(a)?ma(ya):a&&a.$classData?ma(a.$classData):null}}function za(a,b){return a&&a.$classData||null===a?a.o(b):"number"===typeof a?"number"===typeof b&&(a===b?0!==a||1/a===1/b:a!==a&&b!==b):a===b}
function Aa(a){switch(typeof a){case "string":return Ba(Ca(),a);case "number":return Da(Ea(),a);case "boolean":return a?1231:1237;case "undefined":return 0;default:return a&&a.$classData||null===a?a.s():null===fa?42:Fa(a)}}function Ga(a){return"string"===typeof a?a.length|0:a.aa()}function Ha(a,b,c){return"string"===typeof a?a.substring(b,c):a.Rm(b,c)}function Ia(a){return 2147483647<a?2147483647:-2147483648>a?-2147483648:a|0}
function Ja(a,b){var c=g.Object.getPrototypeOf,e=g.Object.getOwnPropertyDescriptor;for(a=c(a);null!==a;){var f=e(a,b);if(void 0!==f)return f;a=c(a)}}function Ka(a,b,c){a=Ja(a,c);if(void 0!==a)return c=a.get,void 0!==c?c.call(b):a.value}function La(a,b,c,e){a=Ja(a,c);if(void 0!==a&&(a=a.set,void 0!==a)){a.call(b,e);return}throw new g.TypeError("super has no setter '"+c+"'.");}function Ma(a){var b=[],c;for(c in a)b.push(c);return b}
function Na(a,b,c,e,f){a=a.e;c=c.e;if(a!==c||e<b||(b+f|0)<e)for(var h=0;h<f;h=h+1|0)c[e+h|0]=a[b+h|0];else for(h=f-1|0;0<=h;h=h-1|0)c[e+h|0]=a[b+h|0]}
var Fa=null!==fa?function(a){switch(typeof a){case "string":case "number":case "boolean":case "undefined":return Aa(a);default:if(null===a)return 0;var b=fa.get(a);void 0===b&&(ea=b=ea+1|0,fa.set(a,b));return b}}:function(a){if(a&&a.$classData){var b=a.$idHashCode$0;if(void 0!==b)return b;if(g.Object.isSealed(a))return 42;ea=b=ea+1|0;return a.$idHashCode$0=b}return null===a?0:Aa(a)};function oa(a){return"number"===typeof a&&a<<24>>24===a&&1/a!==1/-0}
function qa(a){return"number"===typeof a&&a<<16>>16===a&&1/a!==1/-0}function Oa(a){return"number"===typeof a&&(a|0)===a&&1/a!==1/-0}function Pa(a){return null===a?Qa().Dj:a}function Ra(){this.rk=this.Bl=void 0;this.Yg=this.Xh=this.p=null;this.Zg=0;this.Zm=null;this.Jj="";this.Gd=this.Ej=this.Fj=void 0;this.name="";this.isRawJSType=this.isArrayClass=this.isInterface=this.isPrimitive=!1;this.isInstance=void 0}
function Sa(a,b,c){var e=new Ra;e.p={};e.Xh=null;e.Zm=a;e.Jj=b;e.Gd=function(){return!1};e.name=c;e.isPrimitive=!0;e.isInstance=function(){return!1};return e}function l(a,b,c,e,f,h,m,q){var y=new Ra,K=ha(a);m=m||function(a){return!!(a&&a.$classData&&a.$classData.p[K])};q=q||function(a,b){return!!(a&&a.$classData&&a.$classData.Zg===b&&a.$classData.Yg.p[K])};y.rk=h;y.p=e;y.Jj="L"+c+";";y.Gd=q;y.name=c;y.isInterface=b;y.isRawJSType=!!f;y.isInstance=m;return y}
function Ta(a){function b(a){if("number"===typeof a){this.e=Array(a);for(var b=0;b<a;b++)this.e[b]=f}else this.e=a}var c=new Ra,e=a.Zm,f="longZero"==e?Qa().Dj:e;b.prototype=new n;b.prototype.constructor=b;b.prototype.$classData=c;var e="["+a.Jj,h=a.Yg||a,m=a.Zg+1;c.Bl=b;c.rk=p;c.p={c:1,Hd:1,h:1};c.Xh=a;c.Yg=h;c.Zg=m;c.Zm=null;c.Jj=e;c.Fj=void 0;c.Ej=void 0;c.Gd=void 0;c.name=e;c.isPrimitive=!1;c.isInterface=!1;c.isArrayClass=!0;c.isInstance=function(a){return h.Gd(a,m)};return c}
function ma(a){if(!a.Fj){var b=new Ua;b.Eb=a;a.Fj=b}return a.Fj}function r(a){a.Ej||(a.Ej=Ta(a));return a.Ej}Ra.prototype.getFakeInstance=function(){return this===na?"some string":this===va?!1:this===pa||this===ra||this===sa||this===ta||this===ua?0:this===ya?Qa().Dj:this===wa?void 0:{$classData:this}};Ra.prototype.getSuperclass=function(){return this.rk?ma(this.rk):null};Ra.prototype.getComponentType=function(){return this.Xh?ma(this.Xh):null};
Ra.prototype.newArrayOfThisClass=function(a){for(var b=this,c=0;c<a.length;c++)b=r(b);return k(b,a)};var Va=Sa(!1,"Z","boolean"),Wa=Sa(0,"C","char"),Xa=Sa(0,"B","byte"),Ya=Sa(0,"S","short"),Za=Sa(0,"I","int"),$a=Sa("longZero","J","long"),ab=Sa(0,"F","float"),bb=Sa(0,"D","double"),cb=ga(Va);Va.Gd=cb;var db=ga(Wa);Wa.Gd=db;var eb=ga(Xa);Xa.Gd=eb;var fb=ga(Ya);Ya.Gd=fb;var gb=ga(Za);Za.Gd=gb;var hb=ga($a);$a.Gd=hb;var ib=ga(ab);ab.Gd=ib;var jb=ga(bb);bb.Gd=jb;var kb=__webpack_require__(4),lb=__webpack_require__(20);function mb(a,b){return t(new u,function(a,b){return function(f){return a.f(b.f(f))}}(a,b))}function nb(a,b){return t(new u,function(a,b){return function(f){return b.f(a.f(f))}}(a,b))}function ob(a){return a.Hb().mountedImpure}function pb(a){var b=qb(),c=rb();return sb(b,tb(c,v(new w,function(a){return function(){return a.ui}}(a)),ub().de),vb(rb(),v(new w,function(a){return function(){a.ui=xb()}}(a))))}
function yb(a,b){return vb(rb(),v(new w,function(a,b){return function(){a.ui=zb(new Ab,(new Bb).oa(b),a.ui)}}(a,b)))}function Cb(a,b){Db();return Eb(function(a,b){return function(a,c){a.f(b.f(c))}}(a,b))}function Fb(a){Db();a.JB=Eb(function(){return function(a,c){var e=Pa(c);c=e.jb;e=e.qc;c=Gb(Qa(),c,e);a.f(c)}}(a));a.KB=Cb(a,t(new u,function(){return function(a){return a}}(a)))}function Hb(){}function n(){}n.prototype=Hb.prototype;Hb.prototype.b=function(){return this};
Hb.prototype.o=function(a){return this===a};Hb.prototype.m=function(){var a=Ib(la(this)),b=(+(this.s()>>>0)).toString(16);return a+"@"+b};Hb.prototype.s=function(){return Fa(this)};Hb.prototype.toString=function(){return this.m()};function Jb(a,b){if(a=a&&a.$classData){var c=a.Zg||0;return!(c<b)&&(c>b||!a.Yg.isPrimitive)}return!1}var p=l({c:0},!1,"java.lang.Object",{c:1},void 0,void 0,function(a){return null!==a},Jb);Hb.prototype.$classData=p;
function Kb(a,b){var c=a.Ib();return b.Ba().tf(c,Eb(function(){return function(a,b){return a.Za(b)}}(a)))}function Mb(a){var b=k(r(p),[a.e.length]);Na(a,0,b,0,a.e.length);return b}
function Nb(a,b,c){if(32>c)return a.Wa().e[31&b];if(1024>c)return a.x().e[31&(b>>>5|0)].e[31&b];if(32768>c)return a.K().e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];if(1048576>c)return a.W().e[31&(b>>>15|0)].e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];if(33554432>c)return a.Ia().e[31&(b>>>20|0)].e[31&(b>>>15|0)].e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];if(1073741824>c)return a.oc().e[31&(b>>>25|0)].e[31&(b>>>20|0)].e[31&(b>>>15|0)].e[31&(b>>>10|0)].e[31&(b>>>5|0)].e[31&b];throw(new Ob).b();}
function Pb(a,b,c,e){if(32<=e)if(1024>e)1===a.mb()&&(a.X(k(r(p),[32])),a.x().e[31&(b>>>5|0)]=a.Wa(),a.kd(1+a.mb()|0)),a.za(k(r(p),[32]));else if(32768>e)2===a.mb()&&(a.Ca(k(r(p),[32])),a.K().e[31&(b>>>10|0)]=a.x(),a.kd(1+a.mb()|0)),a.X(a.K().e[31&(c>>>10|0)]),null===a.x()&&a.X(k(r(p),[32])),a.za(k(r(p),[32]));else if(1048576>e)3===a.mb()&&(a.Xa(k(r(p),[32])),a.W().e[31&(b>>>15|0)]=a.K(),a.kd(1+a.mb()|0)),a.Ca(a.W().e[31&(c>>>15|0)]),null===a.K()&&a.Ca(k(r(p),[32])),a.X(a.K().e[31&(c>>>10|0)]),null===
a.x()&&a.X(k(r(p),[32])),a.za(k(r(p),[32]));else if(33554432>e)4===a.mb()&&(a.ic(k(r(p),[32])),a.Ia().e[31&(b>>>20|0)]=a.W(),a.kd(1+a.mb()|0)),a.Xa(a.Ia().e[31&(c>>>20|0)]),null===a.W()&&a.Xa(k(r(p),[32])),a.Ca(a.W().e[31&(c>>>15|0)]),null===a.K()&&a.Ca(k(r(p),[32])),a.X(a.K().e[31&(c>>>10|0)]),null===a.x()&&a.X(k(r(p),[32])),a.za(k(r(p),[32]));else if(1073741824>e)5===a.mb()&&(a.Cf(k(r(p),[32])),a.oc().e[31&(b>>>25|0)]=a.Ia(),a.kd(1+a.mb()|0)),a.ic(a.oc().e[31&(c>>>25|0)]),null===a.Ia()&&a.ic(k(r(p),
[32])),a.Xa(a.Ia().e[31&(c>>>20|0)]),null===a.W()&&a.Xa(k(r(p),[32])),a.Ca(a.W().e[31&(c>>>15|0)]),null===a.K()&&a.Ca(k(r(p),[32])),a.X(a.K().e[31&(c>>>10|0)]),null===a.x()&&a.X(k(r(p),[32])),a.za(k(r(p),[32]));else throw(new Ob).b();}function Qb(a,b,c){var e=k(r(p),[32]);Na(a,b,e,c,32-(c>b?c:b)|0);return e}
function Rb(a,b,c){if(32<=c)if(1024>c)a.za(a.x().e[31&(b>>>5|0)]);else if(32768>c)a.X(a.K().e[31&(b>>>10|0)]),a.za(a.x().e[31&(b>>>5|0)]);else if(1048576>c)a.Ca(a.W().e[31&(b>>>15|0)]),a.X(a.K().e[31&(b>>>10|0)]),a.za(a.x().e[31&(b>>>5|0)]);else if(33554432>c)a.Xa(a.Ia().e[31&(b>>>20|0)]),a.Ca(a.W().e[31&(b>>>15|0)]),a.X(a.K().e[31&(b>>>10|0)]),a.za(a.x().e[31&(b>>>5|0)]);else if(1073741824>c)a.ic(a.oc().e[31&(b>>>25|0)]),a.Xa(a.Ia().e[31&(b>>>20|0)]),a.Ca(a.W().e[31&(b>>>15|0)]),a.X(a.K().e[31&(b>>>
10|0)]),a.za(a.x().e[31&(b>>>5|0)]);else throw(new Ob).b();}
function Sb(a,b){var c=-1+a.mb()|0;switch(c){case 5:a.Cf(Mb(a.oc()));a.ic(Mb(a.Ia()));a.Xa(Mb(a.W()));a.Ca(Mb(a.K()));a.X(Mb(a.x()));a.oc().e[31&(b>>>25|0)]=a.Ia();a.Ia().e[31&(b>>>20|0)]=a.W();a.W().e[31&(b>>>15|0)]=a.K();a.K().e[31&(b>>>10|0)]=a.x();a.x().e[31&(b>>>5|0)]=a.Wa();break;case 4:a.ic(Mb(a.Ia()));a.Xa(Mb(a.W()));a.Ca(Mb(a.K()));a.X(Mb(a.x()));a.Ia().e[31&(b>>>20|0)]=a.W();a.W().e[31&(b>>>15|0)]=a.K();a.K().e[31&(b>>>10|0)]=a.x();a.x().e[31&(b>>>5|0)]=a.Wa();break;case 3:a.Xa(Mb(a.W()));
a.Ca(Mb(a.K()));a.X(Mb(a.x()));a.W().e[31&(b>>>15|0)]=a.K();a.K().e[31&(b>>>10|0)]=a.x();a.x().e[31&(b>>>5|0)]=a.Wa();break;case 2:a.Ca(Mb(a.K()));a.X(Mb(a.x()));a.K().e[31&(b>>>10|0)]=a.x();a.x().e[31&(b>>>5|0)]=a.Wa();break;case 1:a.X(Mb(a.x()));a.x().e[31&(b>>>5|0)]=a.Wa();break;case 0:break;default:throw(new x).n(c);}}function Tb(a,b){var c=a.e[b];a.e[b]=null;return Mb(c)}
function Ub(a,b,c){a.kd(c);c=-1+c|0;switch(c){case -1:break;case 0:a.za(b.Wa());break;case 1:a.X(b.x());a.za(b.Wa());break;case 2:a.Ca(b.K());a.X(b.x());a.za(b.Wa());break;case 3:a.Xa(b.W());a.Ca(b.K());a.X(b.x());a.za(b.Wa());break;case 4:a.ic(b.Ia());a.Xa(b.W());a.Ca(b.K());a.X(b.x());a.za(b.Wa());break;case 5:a.Cf(b.oc());a.ic(b.Ia());a.Xa(b.W());a.Ca(b.K());a.X(b.x());a.za(b.Wa());break;default:throw(new x).n(c);}}function Vb(a){return null===a?Wb():a}function Xb(a){return a===Wb()?null:a}
var Yb=l({Im:0},!0,"scala.collection.mutable.HashEntry",{Im:1});function Zb(){}Zb.prototype=new n;Zb.prototype.constructor=Zb;Zb.prototype.b=function(){return this};Zb.prototype.$classData=l({tr:0},!1,"AppCSS$",{tr:1,c:1});var $b=void 0;function ac(){this.Bm=null}ac.prototype=new n;ac.prototype.constructor=ac;
ac.prototype.b=function(){cc=this;(new dc).b();var a=(new ec).b(),b=fc().wg("PUBLIC_URL",v(new w,function(){return function(){return""}}(this))),c=gc(hc(),b),b=ic(),c=jc(a,kc(lc(mc(),c.J))),b=nc(b,c.f(v(new w,function(a,b){return function(){return oc(b,v(new w,function(){return function(){pc||(pc=(new qc).b());return pc.Qh()}}(a)),t(new u,function(){return function(a){rc();return a.Sk()}}(a)))}}(this,a)))),c=(new sc).i(""),c=tc(a,kc(lc(mc(),c.J))),b=uc(nc(b,c.f(v(new w,function(){return function(){var a=
vc(),b=wc();return xc(a,b)}}(this,a))))),c=vc(),e=wc();this.Bm=yc(b,zc(a,xc(c,e),t(new u,function(){return function(a){Ac();return(new Bc).n(a)}}(this,a))));return this};
ac.prototype.Qh=function(){var a=this.Bm,b=Cc(),c=new Dc,e=a.ub,f=a.Db,h=a.Od,m=a.Pf;c.Gb=a.Gb;c.ub=e;c.Db=f;c.Od=h;c.Pf=m;c.sd=b;Ec||(Ec=(new Fc).b());a=Gc(Hc()).location;b=a.protocol+"//"+a.hostname;c=a.port;if(null===c)throw(new Ic).b();e=Jc();e=Kc(e,"^(?:80)?$");Lc(Mc(new Pc,e,c,Ga(c)))||(b=b+":"+a.port);a=(new dd).i(b);ed||(ed=(new fd).b());return gd(hd(a,this.Bm)).og.V()};ac.prototype.$classData=l({ur:0},!1,"AppRouter$",{ur:1,c:1});var cc=void 0;function id(){}id.prototype=new n;
id.prototype.constructor=id;id.prototype.b=function(){return this};id.prototype.$classData=l({wr:0},!1,"ScalaJsApp$",{wr:1,c:1});var jd=void 0;function kd(){this.Wh=null}kd.prototype=new n;kd.prototype.constructor=kd;kd.prototype.b=function(){ld=this;md();rc();nd();rc();od||(od=(new pd).b());var a=qd().gj,a=[rd(a),(rc(),sd(),(new td).Xl("Hello!"))],a=ud((new A).g(a));this.Wh=gd(vd("Title",wd(a)));return this};kd.prototype.Qh=function(){return this.Wh.og.V()};
kd.prototype.$classData=l({xr:0},!1,"components.Title$",{xr:1,c:1});var ld=void 0;function xd(){this.Y=null}xd.prototype=new n;xd.prototype.constructor=xd;xd.prototype.b=function(){yd=this;this.Y=zd(qb(),void 0);return this};function vb(a,b){return v(new w,function(a,b){return function(){b.V()}}(a,b))}function tb(a,b,c){return vb(rb(),v(new w,function(a,b,c){return function(){b.V().M(t(new u,function(a,b){return function(a){b.f(a).pa.V()}}(a,c)))}}(a,b,c)))}
function Ad(a,b){var c=(new A).g([]);return vb(rb(),v(new w,function(a,b,c){return function(){var a=Bd(),e=a.log,y;Cd||(Cd=(new Dd).b());y=Cd;if(c&&c.$classData&&c.$classData.p.Zq)y=c.Jb;else if(c&&c.$classData&&c.$classData.p.ar)y=c.ya;else{var K=[];c.M(t(new u,function(a,b){return function(a){return b.push(a)|0}}(y,K)));y=K}y=[b].concat(y);e.apply(a,y)}}(a,b,c)))}xd.prototype.$classData=l({zr:0},!1,"japgolly.scalajs.react.Callback$",{zr:1,c:1});var yd=void 0;
function rb(){yd||(yd=(new xd).b());return yd}function Bb(){this.pa=null}Bb.prototype=new n;Bb.prototype.constructor=Bb;Bb.prototype.oa=function(a){this.pa=a;return this};Bb.prototype.o=function(a){qb();return a&&a.$classData&&a.$classData.p.jn?this.pa===(null===a?null:a.pa):!1};Bb.prototype.s=function(){return Fa(this.pa)};Bb.prototype.$classData=l({jn:0},!1,"japgolly.scalajs.react.CallbackTo",{jn:1,c:1});function Fd(){}Fd.prototype=new n;Fd.prototype.constructor=Fd;Fd.prototype.b=function(){return this};
function Gd(a,b){return b===rb().Y}function Hd(a,b){return function(a){return function(){return a.V()}}(b)}function sb(a,b,c){return Gd(qb(),b)?c:v(new w,function(a,b,c){return function(){c.V();return b.V()}}(a,c,b))}function Id(a,b,c){return v(new w,function(a,b,c){return function(){return c.f(b.V())}}(a,b,c))}function Jd(a,b,c){return v(new w,function(a,b,c){return function(){return c.f(b.V()).pa.V()}}(a,b,c))}function zd(a,b){return v(new w,function(a,b){return function(){return b}}(a,b))}
Fd.prototype.$classData=l({Ar:0},!1,"japgolly.scalajs.react.CallbackTo$",{Ar:1,c:1});var Kd=void 0;function qb(){Kd||(Kd=(new Fd).b());return Kd}function le(){}le.prototype=new n;le.prototype.constructor=le;function me(){}me.prototype=le.prototype;function ne(){}ne.prototype=new n;ne.prototype.constructor=ne;ne.prototype.b=function(){return this};
function oe(){var a;pe||(pe=(new ne).b());a=pe;qe||(qe=(new re).b());a=t(new u,function(a,c){return function(e){return se(new te,kb.createElement(e,c.t),t(new u,function(a,b,c){return function(a){a=a.Ld;return kb.createElement(c,ue(ve(),a,b.Tp.V()))}}(a,c,e)),void 0)}}(a,qe.en));we||(we=(new xe).b());return ye(a)}ne.prototype.$classData=l({Er:0},!1,"japgolly.scalajs.react.CtorType$Summoner$",{Er:1,c:1});var pe=void 0;function ze(){}ze.prototype=new n;ze.prototype.constructor=ze;ze.prototype.b=function(){return this};
ze.prototype.$classData=l({Gr:0},!1,"japgolly.scalajs.react.component.Generic$",{Gr:1,c:1});var Ae=void 0;function Be(){this.ve=null}Be.prototype=new n;Be.prototype.constructor=Be;Be.prototype.b=function(){Ce=this;De||(De=(new Ee).b());this.ve=De;Fe();return this};Be.prototype.$classData=l({bs:0},!1,"japgolly.scalajs.react.component.Scala$",{bs:1,c:1});var Ce=void 0;function Ge(){Ce||(Ce=(new Be).b())}function He(){}He.prototype=new n;He.prototype.constructor=He;He.prototype.b=function(){return this};
He.prototype.$classData=l({gs:0},!1,"japgolly.scalajs.react.component.ScalaFn$",{gs:1,c:1});var Ie=void 0;function Je(){this.xp=null}Je.prototype=new n;Je.prototype.constructor=Je;Je.prototype.b=function(){Ke=this;this.xp=t(new u,function(){return function(){return Le().fg}}(this));return this};
function Me(a,b,c){Ne();return Oe(Ne(),b,c).qo(t(new u,function(){return function(a){Le();return{a:a}}}(a))).Qp(t(new u,function(a){return function(b){return b.Pp(t(new u,function(){return function(a){return a.a}}(a))).Np(t(new u,function(){return function(a){Ge();return Pe(a)}}(a)))}}(a)))}Je.prototype.$classData=l({js:0},!1,"japgolly.scalajs.react.component.builder.Builder$",{js:1,c:1});var Ke=void 0;function Qe(){Ke||(Ke=(new Je).b());return Ke}function Re(){this.qa=null}Re.prototype=new n;
Re.prototype.constructor=Re;function Se(a,b){return Te(a,v(new w,function(a,b){return function(){return b.V()}}(a,b)))}Re.prototype.i=function(a){this.qa=a;return this};function Te(a,b){return(new Ue).Bg(a.qa,t(new u,function(a,b){return function(){Le();return{a:b.V()}}}(a,b)))}Re.prototype.$classData=l({ks:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step1",{ks:1,c:1});function Ue(){this.Ae=this.qa=null}Ue.prototype=new n;Ue.prototype.constructor=Ue;
function Ve(a,b){var c=a.Ae,e=new We;e.qa=a.qa;e.Ae=c;e.ig=b;return e}function Xe(a){return Ve(a,t(new u,function(){return function(){}}(a)))}Ue.prototype.Bg=function(a,b){this.qa=a;this.Ae=b;return this};Ue.prototype.$classData=l({ls:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step2",{ls:1,c:1});function We(){this.ig=this.Ae=this.qa=null}We.prototype=new n;We.prototype.constructor=We;
function Ye(a,b){var c=new Ze,e=a.Ae,f=a.ig,h=$e(new af,B(),B(),B(),B(),B(),B(),B(),B());c.qa=a.qa;c.Ae=e;c.ig=f;c.Od=b;c.fe=h;return c}function bf(a,b){return Ye(a,t(new u,function(a,b){return function(a){a=(new cf).Ya(a.L);return b.f(ob(a).Sc())}}(a,b)))}function df(a,b){return Ye(a,t(new u,function(a,b){return function(){return b}}(a,b)))}We.prototype.$classData=l({ms:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step3",{ms:1,c:1});
function Ze(){this.fe=this.Od=this.ig=this.Ae=this.qa=null}Ze.prototype=new n;Ze.prototype.constructor=Ze;function ef(a){var b=zd(qb(),!1),b=t(new u,function(a,b){return function(){return(new Bb).oa(b)}}(a,b));return ff(a,gf(),b,hf().Qo)}function ff(a,b,c,e){b=jf(a.fe,b,c,e);c=new Ze;e=a.Ae;var f=a.ig,h=a.Od;c.qa=a.qa;c.Ae=e;c.ig=f;c.Od=h;c.fe=b;return c}function gd(a){var b=oe();a=kf(lf(),a);return Me(Qe(),a,b)}
Ze.prototype.$classData=l({ns:0},!1,"japgolly.scalajs.react.component.builder.Builder$Step4",{ns:1,c:1});function Ee(){}Ee.prototype=new n;Ee.prototype.constructor=Ee;Ee.prototype.b=function(){return this};function vd(a,b){Qe();a=(new Re).i(a);b=df(Xe((Qe(),(new Ue).Bg(a.qa,Qe().xp))),b);return ef(b)}Ee.prototype.$classData=l({os:0},!1,"japgolly.scalajs.react.component.builder.EntryPoint$",{os:1,c:1});var De=void 0;function mf(){}mf.prototype=new n;mf.prototype.constructor=mf;d=mf.prototype;d.b=function(){return this};
d.Mg=function(a){Fe();var b=nf().Qf(a);a=(new of).Ya(a);return pf(0,"ComponentDidMount(props: "+b+", state: "+ob(a).Sc()+")")};d.Qf=function(a){a=(new of).Ya(a);return ob(a).Nd()};d.qg=function(a,b){return b&&b.$classData&&b.$classData.p.mn?(b=null===b?null:b.L,D(G(),a,b)):!1};d.$classData=l({rs:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidMount$",{rs:1,c:1});var qf=void 0;function nf(){qf||(qf=(new mf).b());return qf}function rf(){}rf.prototype=new n;
rf.prototype.constructor=rf;d=rf.prototype;d.b=function(){return this};d.Mg=function(a){Fe();var b=sf().Qf(a);a=(new tf).Ya(a);return pf(0,"ComponentWillMount(props: "+b+", state: "+ob(a).Sc()+")")};d.Qf=function(a){a=(new tf).Ya(a);return ob(a).Nd()};d.qg=function(a,b){return b&&b.$classData&&b.$classData.p.nn?(b=null===b?null:b.L,D(G(),a,b)):!1};d.$classData=l({ts:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillMount$",{ts:1,c:1});var uf=void 0;
function sf(){uf||(uf=(new rf).b());return uf}function vf(){}vf.prototype=new n;vf.prototype.constructor=vf;d=vf.prototype;d.b=function(){return this};d.Mg=function(a){Fe();var b="ComponentWillUnmount(props: "+wf().Qf(a)+", state: ";wf();a=(new xf).Ya(a);a=ob(a).Sc();return pf(0,b+a+")")};d.Qf=function(a){a=(new xf).Ya(a);return ob(a).Nd()};d.qg=function(a,b){return b&&b.$classData&&b.$classData.p.on?(b=null===b?null:b.L,D(G(),a,b)):!1};
d.$classData=l({vs:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillUnmount$",{vs:1,c:1});var Af=void 0;function wf(){Af||(Af=(new vf).b());return Af}function Bf(){}Bf.prototype=new n;Bf.prototype.constructor=Bf;d=Bf.prototype;d.b=function(){return this};d.Mg=function(a){Fe();var b=Cf().Qf(a);a=(new cf).Ya(a);return pf(0,"Render(props: "+b+", state: "+ob(a).Sc()+")")};d.Qf=function(a){a=(new cf).Ya(a);return ob(a).Nd()};
d.qg=function(a,b){return b&&b.$classData&&b.$classData.p.pn?(b=null===b?null:b.L,D(G(),a,b)):!1};d.$classData=l({xs:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$RenderScope$",{xs:1,c:1});var Df=void 0;function Cf(){Df||(Df=(new Bf).b());return Df}function Ef(){this.go=this.dn=this.bn=this.Ym=null}Ef.prototype=new n;Ef.prototype.constructor=Ef;function Ff(a,b,c){Gf(a,function(a){return function(b,c){return a.Hj(this,b,c)}}(b),c)}
Ef.prototype.b=function(){Hf=this;var a=g.Object.setPrototypeOf,b=void 0===a?B():(new H).n(a);if(If(b))a=t(new u,function(){return function(a){return g.Object.getPrototypeOf(a)}}(this)),b=b.J;else{if(B()!==b)throw(new x).n(b);a=t(new u,function(){return function(a){return a.__proto__}}(this));b=function(a,b){lf();a.__proto__=b}}this.Ym=(new I).d(a,b);this.bn=this.Ym.R;this.dn=this.Ym.U;this.go=kb.Component;return this};
function Jf(a,b){for(var c=0,e=b.length|0;c<e;){var f=b[c];f.configurable=!0;"value"in f&&(f.writable=!0);g.Object.defineProperty(a,f.key,f);c=1+c|0}}
function kf(a,b){var c=b.Ae,e=b.ig,f=b.Od,h=(new Kf).n(null);h.H=function(a,b,c){return function(e){lf();var f;lf();f=lf().bn.f(c.H).call(this,e);f=f instanceof g.Object||f instanceof g.Function?f:this;Ne();var h=(new kg).Ya(f);f.mountedImpure=(Ge(),Pe(h));var h=f.mountedImpure,C=lg().lp;f.mountedPure=h.qr(C);f.backend=b.f(f.mountedPure);f.state=a.f(e);return f}}(c,e,h);mg(a,h.H,a.go);c=[];ng("render",t(new u,function(a,b){return function(a){return b.f((new cf).Ya(a)).yk}}(a,f)),c);f=b.fe.Te;f.l()||
(f=f.P(),Ff("componentDidCatch",og(function(a,b){return function(a,c){b.f(pg(a,c)).pa.V()}}(a,f)),c));f=b.fe.Ue;f.l()||(f=f.P(),ng("componentDidMount",t(new u,function(a,b){return function(a){b.f((new of).Ya(a)).pa.V()}}(a,f)),c));f=b.fe.Ve;f.l()||(f=f.P(),Ff("componentDidUpdate",og(function(a,b){return function(a,c,e){b.f((new qg).oi(a,c.a,e.a)).pa.V()}}(a,f)),c));f=b.fe.We;f.l()||(f=f.P(),ng("componentWillMount",t(new u,function(a,b){return function(a){b.f((new tf).Ya(a)).pa.V()}}(a,f)),c));f=b.fe.Xe;
f.l()||(f=f.P(),rg(Eb(function(a,b){return function(a,c){b.f(sg(a,c.a)).pa.V()}}(a,f)),c));f=b.fe.Ye;f.l()||(f=f.P(),ng("componentWillUnmount",t(new u,function(a,b){return function(a){b.f((new xf).Ya(a)).pa.V()}}(a,f)),c));f=b.fe.Ze;f.l()||(f=f.P(),Ff("componentWillUpdate",og(function(a,b){return function(a,c,e){b.f((new tg).oi(a,c.a,e.a)).pa.V()}}(a,f)),c));f=b.fe.mf;f.l()||(f=f.P(),Ff("shouldComponentUpdate",og(function(a,b){return function(a,c,e){return!!b.f((new ug).oi(a,c.a,e.a)).pa.V()}}(a,
f)),c));Jf(h.H.prototype,c);a=vg().Rh(b.qa);a.l()||(a=a.P(),h.H.displayName=a);return h.H}function ng(a,b,c){Gf(a,function(a){return function(){return a.f(this)}}(b),c)}function Gf(a,b,c){c.push({key:a,value:b})}function rg(a,b){Gf("componentWillReceiveProps",function(a){return function(b){return wg(a,this,b)}}(a),b)}function mg(a,b,c){b.prototype=g.Object.create(c.prototype,{constructor:{value:b,enumerable:!1,writable:!0,configurable:!0}});(0,a.dn)(b,c)}
Ef.prototype.$classData=l({zs:0},!1,"japgolly.scalajs.react.component.builder.ViaReactComponent$",{zs:1,c:1});var Hf=void 0;function lf(){Hf||(Hf=(new Ef).b());return Hf}function xg(a,b){return v(new w,function(a,b){return function(){var f=(new I).d(b,void 0);a.kh=zb(new Ab,f,a.kh);return(new Bb).oa(vb(rb(),v(new w,function(a,b){return function(){var c=a.kh;yg();for(var e=(new zg).b();!c.l();){var f=c.I();f!==b!==!1&&Ag(e,f);c=c.w()}a.kh=Bg(e)}}(a,f))))}}(a,b))}
function Cg(a){return tb(rb(),v(new w,function(a){return function(){return a.kh}}(a)),t(new u,function(a,c){return function(a){return(new Bb).oa(a.R.f(c).pa)}}(a,void 0)))}function Dg(){}Dg.prototype=new n;Dg.prototype.constructor=Dg;Dg.prototype.b=function(){return this};function Eg(a,b,c,e){return Fg(Gg(),b,t(new u,function(a,b){return function(c){c=b.f(c).pa;return t(new u,function(a,b){return function(){return(new Bb).oa(b)}}(a,c))}}(a,c)),e)}
Dg.prototype.$classData=l({As:0},!1,"japgolly.scalajs.react.extra.EventListener$",{As:1,c:1});var Hg=void 0;function Ig(){Hg||(Hg=(new Dg).b());return Hg}function Jg(){}Jg.prototype=new n;Jg.prototype.constructor=Jg;Jg.prototype.b=function(){return this};
function Fg(a,b,c,e){return Kg().Dd(t(new u,function(a,b,c,e,y){return function(K){var z=t(new u,function(a,b,c,e,f){return function(h){h=h.L;var m=(new of).Ya(h),q=e.f(ob(m)),m=(new of).Ya(h),y=function(a){return function(b){Gg();a.f(b).pa.V()}}(c.f(m.Hb().mountedPure)),m=vb(rb(),v(new w,function(a,b,c,e,f){return function(){b.addEventListener(e,c,f)}}(a,q,y,b,f))),q=vb(rb(),v(new w,function(a,b,c,e,f){return function(){b.removeEventListener(e,c,f)}}(a,q,y,b,f))),y=qb();h=(new of).Ya(h);return(new Bb).oa(sb(y,
m,yb(h.Hb().backend,q)))}}(a,b,c,e,y));return ff(K,Lg(),z,hf().$g)}}(a,b,c,e,!1)))}Jg.prototype.$classData=l({Bs:0},!1,"japgolly.scalajs.react.extra.EventListener$OfEventType$",{Bs:1,c:1});var Mg=void 0;function Gg(){Mg||(Mg=(new Jg).b());return Mg}function Ng(){}Ng.prototype=new n;Ng.prototype.constructor=Ng;Ng.prototype.b=function(){return this};
function Og(a,b,c){return Pg(a,b,t(new u,function(a,b){return function(c){return t(new u,function(a,b,c){return function(){return(new Bb).oa(b.f((new of).Ya(c)).pa)}}(a,b,c.L))}}(a,c)))}
function Pg(a,b,c){return Kg().Dd(t(new u,function(a,b,c){return function(m){var q=t(new u,function(a,b,c){return function(e){e=e.L;var f=b.f(nf().Qf(e)),h=c.f((new of).Ya(e)),f=xg(f,h);e=(new of).Ya(e);e=t(new u,function(a,b){return function(a){return(new Bb).oa(yb(b,a.pa))}}(a,e.Hb().backend));return(new Bb).oa(Jd(qb(),f,e))}}(a,b,c));return ff(m,Lg(),q,hf().$g)}}(a,b,c)))}Ng.prototype.$classData=l({Cs:0},!1,"japgolly.scalajs.react.extra.Listenable$",{Cs:1,c:1});var Qg=void 0;function Rg(){}
Rg.prototype=new n;Rg.prototype.constructor=Rg;Rg.prototype.b=function(){return this};function Kg(){Sg||(Sg=(new Rg).b());return t(new u,function(a){return function(b){var c=t(new u,function(){return function(a){a=(new xf).Ya(a.L);return(new Bb).oa(pb(a.Hb().backend))}}(a));return ff(b,Tg(),c,hf().$g)}}(Sg))}Rg.prototype.$classData=l({Ds:0},!1,"japgolly.scalajs.react.extra.OnUnmount$",{Ds:1,c:1});var Sg=void 0;function Ug(){}Ug.prototype=new n;Ug.prototype.constructor=Ug;function Vg(){}
Vg.prototype=Ug.prototype;function hc(){var a=(new sc).i(""),b=a.Lk(a);return a.ik(Wg(Ca(),b,"/*$","/"))}function gc(a,b){return a.ik(""+a.Lk(a)+b)}function Xg(){}Xg.prototype=new n;Xg.prototype.constructor=Xg;function Yg(){}Yg.prototype=Xg.prototype;function Zg(a,b){a=$g(a)?ah(a.hh,a.mh):ah(Ac().Ec.dc,a);var c=new bh;c.hh=a;c.mh=b;return c}function fd(){}fd.prototype=new n;fd.prototype.constructor=fd;fd.prototype.b=function(){return this};function hd(a,b){return ch(ed,b,dh(a,b))}
function ch(a,b,c){var e=bf(Ve(Se((md(),(new Re).i("Router")),c.Tm),t(new u,function(){return function(){return(new eh).b()}}(a))),t(new u,function(a,b){return function(a){return wg(b.id.Od,b.$h,a)}}(a,c))),f=t(new u,function(a,b){return function(a){var c=a.L;a=b.Pf;var e=B(),c=(new of).Ya(c);return(new Bb).oa(wg(a,e,ob(c).Sc().$c).pa)}}(a,b)),e=ff(e,Lg(),f,hf().$g);b=t(new u,function(a,b){return function(a){return(new Bb).oa(wg(b.Pf,(new H).n(a.xm.$c),ob(a).Sc().$c).pa)}}(a,b));var h=ff(e,fh(),b,
hf().$g);b=Eg(Ig(),"popstate",t(new u,function(a,b){return function(){return(new Bb).oa(b.$h.zk())}}(a,c)),t(new u,function(){return function(){return Gc(Hc())}}(a)));Qg||(Qg=(new Ng).b());b=[b,Og(Qg,t(new u,function(a,b){return function(){return b}}(a,c)),t(new u,function(a,b){return function(c){c=c.L;return(new Bb).oa(Jd(qb(),b.Tm,t(new u,function(a,b){return function(a){var c=(new of).Ya(b);return(new Bb).oa(c.Bh(a,rb().Y).pa)}}(a,c))))}}(a,c)))];var e=b.length|0,f=0,m=h;a:for(;;){if(f!==e){h=
1+f|0;m=b[f].f(m);f=h;continue a}e=m;break}a=[-1!==(Gc(Hc()).navigator.userAgent.indexOf("Trident")|0)?Eg(Ig(),"hashchange",t(new u,function(a,b){return function(){return(new Bb).oa(b.$h.zk())}}(a,c)),t(new u,function(){return function(){return Gc(Hc())}}(a))):ub().de];c=a.length|0;b=0;f=e;a:for(;;){if(b!==c){e=1+b|0;f=a[b].f(f);b=e;continue a}return f}}fd.prototype.$classData=l({Ns:0},!1,"japgolly.scalajs.react.extra.router.Router$",{Ns:1,c:1});var ed=void 0;function ec(){}ec.prototype=new n;
ec.prototype.constructor=ec;function gh(a,b,c){return t(new u,function(a,b,c){return function(m){return(new hh).ih(t(new u,function(a,b){return function(a){a=ih(b,a);if(a.l())return B();a=a.P();Ac();return(new H).n((new jh).n(a))}}(a,b)),t(new u,function(a,b,c){return function(a){return kh(c,a,b)}}(a,c,t(new u,function(a,b){return function(a){return b.xl.f(a)}}(a,b)))),Eb(function(a,b,c){return function(a,e){return kh(c,e,b)}}(a,c,m)))}}(a,b,c))}ec.prototype.b=function(){return this};
function zc(a,b,c){return t(new u,function(a,b,c){return function(){return c.f(b)}}(a,b,c))}function kh(a,b,c){b=c.f(b);return b.l()?B():(new H).n(a.f(b.P()))}function lh(a,b){return t(new u,function(a,b){return function(){return b.V()}}(a,b))}function mh(a,b,c){return t(new u,function(a,b,c){return function(a){a=b.f(a);if(a.l())return B();a=a.P();return(new H).n(c.f(a))}}(a,b,c))}function oc(a,b,c){return(new nh).Mb(t(new u,function(a,b,c){return function(){return c.f(b.V())}}(a,b,c)))}
function oh(a,b){return ph(qh(),mh(a,b,t(new u,function(){return function(a){Ac();return(new Bc).n(a)}}(a))))}function tc(a,b){return t(new u,function(a,b){return function(f){return oh(a,t(new u,function(a,b,c){return function(a){a=ih(b,a);if(a.l())return B();a.P();return(new H).n(c.V())}}(a,b,f)))}}(a,b))}
function jc(a,b){var c=vc(),e=J().zq;b=rh(b,t(new u,function(a,b){return function(){return b}}(b,c)),t(new u,function(){return function(){}}(b,e)));c=gh(a,b,sh(new th,uh(c)));return t(new u,function(a,b){return function(c){c=lh(a,c);return b.f(c)}}(a,c))}ec.prototype.$classData=l({Ps:0},!1,"japgolly.scalajs.react.extra.router.RouterConfigDsl",{Ps:1,c:1});function dc(){}dc.prototype=new n;dc.prototype.constructor=dc;dc.prototype.b=function(){return this};
dc.prototype.$classData=l({Rs:0},!1,"japgolly.scalajs.react.extra.router.RouterConfigDsl$BuildInterface",{Rs:1,c:1});function vh(){}vh.prototype=new n;vh.prototype.constructor=vh;function wh(){}wh.prototype=vh.prototype;function xh(){this.nq=this.mq=null}xh.prototype=new n;xh.prototype.constructor=xh;xh.prototype.b=function(){yh=this;var a=(new zh).i("([-()\\[\\]{}+?*.$\\^|,:#\x3c!\\\\])");xb();this.mq=Ah(a.bb);a=(new zh).i("\\x08");xb();this.nq=Ah(a.bb);return this};
function Bh(a){var b=yh;a=Ch(Mc(new Pc,b.mq.sh,a,Ga(a)),"\\\\$1");return a=Ch(Mc(new Pc,b.nq.sh,a,Ga(a)),"\\\\x08")}xh.prototype.$classData=l({Vs:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$",{Vs:1,c:1});var yh=void 0;function Dh(){this.cr=null}Dh.prototype=new n;Dh.prototype.constructor=Dh;Dh.prototype.b=function(){Eh=this;this.cr=(new H).n(void 0);lc(this,"/");return this};
function lc(a,b){var c=new Fh;yh||(yh=(new xh).b());return Gh(c,Bh(b),0,t(new u,function(){return function(){return mc().cr}}(a)),t(new u,function(a,b){return function(){return b}}(a,b)))}Dh.prototype.$classData=l({Ys:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$RouteB$",{Ys:1,c:1});var Eh=void 0;function mc(){Eh||(Eh=(new Dh).b());return Eh}function Hh(){}Hh.prototype=new n;Hh.prototype.constructor=Hh;function Ih(){}Ih.prototype=Hh.prototype;
function rh(a,b,c){return a.dq(t(new u,function(a,b){return function(a){return(new H).n(b.f(a))}}(a,b)),c)}function Jh(){}Jh.prototype=new n;Jh.prototype.constructor=Jh;Jh.prototype.b=function(){return this};function Kh(a,b){return Eb(function(a,b,f){return function(a,c){var q=wg(b,a,c);return q.l()?wg(f,a,c):q.P()}}(Lh,a,b))}Jh.prototype.$classData=l({at:0},!1,"japgolly.scalajs.react.extra.router.package$OptionFn2Ext$",{at:1,c:1});var Lh=void 0;function Mh(){}Mh.prototype=new n;
Mh.prototype.constructor=Mh;Mh.prototype.b=function(){return this};function Nh(a,b,c){return t(new u,function(a,b,c){return function(a){var e=b.f(a);return e.l()?c.f(a):e}}(a,b,c))}function Oh(a,b,c){return t(new u,function(a,b,c){return function(a){var e=b.f(a);return e.l()?c.f(a):e.P()}}(a,b,c))}Mh.prototype.$classData=l({bt:0},!1,"japgolly.scalajs.react.extra.router.package$OptionFnExt$",{bt:1,c:1});var Ph=void 0;function Qh(){Ph||(Ph=(new Mh).b());return Ph}function Rh(){this.fg=null}
Rh.prototype=new n;Rh.prototype.constructor=Rh;Rh.prototype.b=function(){Sh=this;this.fg=(Le(),{a:void 0});return this};Rh.prototype.$classData=l({ct:0},!1,"japgolly.scalajs.react.internal.Box$",{ct:1,c:1});var Sh=void 0;function Le(){Sh||(Sh=(new Rh).b());return Sh}function Th(){}Th.prototype=new n;Th.prototype.constructor=Th;function Uh(){}Uh.prototype=Th.prototype;function Vh(){this.yl=this.zg=null}Vh.prototype=new n;Vh.prototype.constructor=Vh;
Vh.prototype.b=function(){Wh=this;this.zg=(new Xh).b();this.yl=(new Yh).b();return this};Vh.prototype.$classData=l({et:0},!1,"japgolly.scalajs.react.internal.Effect$",{et:1,c:1});var Wh=void 0;function Zh(){Wh||(Wh=(new Vh).b());return Wh}function $h(){this.Pk=this.ye=null}$h.prototype=new n;$h.prototype.constructor=$h;function ai(){}ai.prototype=$h.prototype;$h.prototype.Ph=function(a){a=this.ye.ap(a);return this.Pk.eq(a)};$h.prototype.ck=function(a,b){this.ye=a;this.Pk=b;return this};
$h.prototype.$classData=l({Bn:0},!1,"japgolly.scalajs.react.internal.Effect$Trans",{Bn:1,c:1});function bi(){this.lp=this.Nl=null}bi.prototype=new n;bi.prototype.constructor=bi;bi.prototype.b=function(){ci=this;lg();var a=Zh().zg;this.Nl=di(a);lg();Zh();this.lp=ei(lg(),Zh().zg,Zh().yl);ei(lg(),Zh().yl,Zh().zg);return this};function ei(a,b,c){return null===(lg(),null)?(new $h).ck(b,c):di(b)}bi.prototype.$classData=l({ht:0},!1,"japgolly.scalajs.react.internal.Effect$Trans$",{ht:1,c:1});var ci=void 0;
function lg(){ci||(ci=(new bi).b());return ci}function fi(){}fi.prototype=new n;fi.prototype.constructor=fi;fi.prototype.b=function(){return this};function gi(a){var b=hi,c=Ma(a),c=(new ii).g(c),c=ji(new ki,c,0,c.Jb.length|0);return(new li).pb(c,t(new u,function(a,b){return function(a){try{var c=b[a]}catch(e){if(c=mi(ni(),e),null!==c)c=c.m();else throw e;}return(new I).d(a,c)}}(b,a)))}fi.prototype.$classData=l({jt:0},!1,"japgolly.scalajs.react.internal.JsUtil$",{jt:1,c:1});var hi=void 0;
function oi(){this.Ld=this.Ah=this.eh=null}oi.prototype=new n;oi.prototype.constructor=oi;function pi(a,b,c){a.eh=b;a.Ah=c;a.Ld=t(new u,function(a){return function(b){return t(new u,function(a,b){return function(c){return a.Ah.f(b.f(a.eh.f(c))).f(c)}}(a,b))}}(a));return a}oi.prototype.$classData=l({kt:0},!1,"japgolly.scalajs.react.internal.Lens",{kt:1,c:1});function qi(){this.zg=null}qi.prototype=new n;qi.prototype.constructor=qi;qi.prototype.b=function(){ri=this;this.zg=si(this);return this};
function si(a){return pi(new oi,t(new u,function(){return function(a){return a}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(){return b}}(a,c))}}(a)))}qi.prototype.$classData=l({lt:0},!1,"japgolly.scalajs.react.internal.Lens$",{lt:1,c:1});var ri=void 0;function ti(){ri||(ri=(new qi).b());return ri}function ui(){this.qk=this.Gf=null}ui.prototype=new n;ui.prototype.constructor=ui;
ui.prototype.$classData=l({mt:0},!1,"japgolly.scalajs.react.internal.Profunctor$Ops",{mt:1,c:1});function vi(){this.Tp=this.t=null}vi.prototype=new n;vi.prototype.constructor=vi;function wi(a,b,c,e){a.t=b;a.Tp=e;return a}vi.prototype.$classData=l({ot:0},!1,"japgolly.scalajs.react.internal.Singleton",{ot:1,c:1});function re(){this.en=this.fg=null}re.prototype=new n;re.prototype.constructor=re;
re.prototype.b=function(){qe=this;this.fg=wi(new vi,void 0,v(new w,function(){return function(){}}(this)),v(new w,function(){return function(){return{}}}(this)));this.en=wi(new vi,Le().fg,v(new w,function(){return function(){Le();return{a:void 0}}}(this)),v(new w,function(){return function(){Le();return{a:void 0}}}(this)));return this};re.prototype.$classData=l({pt:0},!1,"japgolly.scalajs.react.internal.Singleton$",{pt:1,c:1});var qe=void 0;function xi(){this.de=null}xi.prototype=new n;
xi.prototype.constructor=xi;xi.prototype.b=function(){yi=this;this.de=t(new u,function(){return function(a){return a}}(this));return this};xi.prototype.$classData=l({qt:0},!1,"japgolly.scalajs.react.internal.package$",{qt:1,c:1});var yi=void 0;function ub(){yi||(yi=(new xi).b());return yi}function zi(){this.qa=null}zi.prototype=new n;zi.prototype.constructor=zi;function Ai(){}Ai.prototype=zi.prototype;zi.prototype.o=function(a){return a&&a.$classData&&a.$classData.p.Aj?this.qa===a.qa:!1};
zi.prototype.m=function(){return"VdomAttr{name\x3d"+this.qa+"}"};zi.prototype.i=function(a){this.qa=a;return this};zi.prototype.s=function(){return Bi(Ci(),this.qa)};function Di(){}Di.prototype=new n;Di.prototype.constructor=Di;Di.prototype.b=function(){return this};Di.prototype.$classData=l({st:0},!1,"japgolly.scalajs.react.vdom.Attr$",{st:1,c:1});var Ei=void 0;function Fi(){this.fr=this.Bo=null}Fi.prototype=new n;Fi.prototype.constructor=Fi;
Fi.prototype.b=function(){Gi=this;this.Bo=Eb(function(){return function(a,b){a.f(b)}}(this));this.fr=Eb(function(){return function(a,b){a.f(b)}}(this));return this};function Hi(a,b){return Eb(function(a,b){return function(a,c){a.f(b.f(c))}}(a,b))}Fi.prototype.$classData=l({wt:0},!1,"japgolly.scalajs.react.vdom.Attr$ValueType$",{wt:1,c:1});var Gi=void 0;function Db(){Gi||(Gi=(new Fi).b());return Gi}function Ii(){}Ii.prototype=new n;Ii.prototype.constructor=Ii;Ii.prototype.b=function(){return this};
Ii.prototype.$classData=l({xt:0},!1,"japgolly.scalajs.react.vdom.Builder$",{xt:1,c:1});var Ji=void 0;function Ki(){Ji||(Ji=(new Ii).b())}
function Li(a){a.Si={};a.Qm={};a.zl=[];a.xa=void 0;a.nk=void 0;a.hw=Eb(function(a){return function(c,e){Ki();a.Si[c]=e}}(a));a.jo=t(new u,function(a){return function(c){var e=a.nk;void 0!==e&&(Mi||(Mi=(new Ni).b()),c=""+(""+e+" ")+c);a.nk=c}}(a));a.ko=Eb(function(a){return function(c,e){Ki();a.Qm[c]=e}}(a));a.iw=t(new u,function(a){return function(c){hi||(hi=(new fi).b());c=gi(c);for(c=(new Oi).pb(c,t(new u,function(){return function(a){return null!==a}}(a)));c.u();){var e=c.r();if(null!==e)wg(a.ko,
e.R,e.U);else throw(new x).n(e);}}}(a));a.ql=t(new u,function(a){return function(c){a.zl.push(c)}}(a));a.iB=t(new u,function(a){return function(c){a.xa=c}}(a))}function Pi(){this.no=null}Pi.prototype=new n;Pi.prototype.constructor=Pi;
Pi.prototype.b=function(){Qi=this;var a;try{a=g.Symbol["for"]("react.element")}catch(b){if(null!==mi(ni(),b))a=60103;else throw b;}this.no=Ri(function(a,c){return function(a,b,h,m){if(void 0!==b.ref)return kb.createElement.apply(kb,[a,b].concat(m));var q=m.length|0;0!==q&&(m=1===q?m[0]:m,Ki(),b.children=m);return{$$typeof:c,type:a,key:void 0===h?null:""+h,ref:null,props:b,_owner:null}}}(this,a));return this};
Pi.prototype.$classData=l({yt:0},!1,"japgolly.scalajs.react.vdom.Builder$ToRawReactElement$",{yt:1,c:1});var Qi=void 0;function Si(){}Si.prototype=new n;Si.prototype.constructor=Si;function Ti(){}Ti.prototype=Si.prototype;Si.prototype.b=function(){sd();Ui||(Ui=(new Vi).b());Wi||(Wi=(new Xi).b());Yi||(Yi=(new Zi).b());nd();$i||($i=(new aj).b());bj||(bj=(new cj).b());sd();Ei||(Ei=(new Di).b());dj||(dj=(new ej).b());fj||(fj=(new gj).b());return this};
function hj(a){a.GB=Hi(Db(),t(new u,function(){return function(a){return!!a}}(a)));a.lr=Db().fr;a.HB=Hi(Db(),t(new u,function(){return function(a){return a|0}}(a)));a.IB=Db().Bo}function Xi(){}Xi.prototype=new n;Xi.prototype.constructor=Xi;Xi.prototype.b=function(){return this};Xi.prototype.$classData=l({Nt:0},!1,"japgolly.scalajs.react.vdom.ReactFragment$",{Nt:1,c:1});var Wi=void 0;function Zi(){}Zi.prototype=new n;Zi.prototype.constructor=Zi;Zi.prototype.b=function(){return this};
Zi.prototype.$classData=l({Ot:0},!1,"japgolly.scalajs.react.vdom.ReactPortal$",{Ot:1,c:1});var Yi=void 0;function ej(){}ej.prototype=new n;ej.prototype.constructor=ej;ej.prototype.b=function(){return this};ej.prototype.$classData=l({Pt:0},!1,"japgolly.scalajs.react.vdom.Style$",{Pt:1,c:1});var dj=void 0;function cj(){this.Y=null}cj.prototype=new n;cj.prototype.constructor=cj;cj.prototype.b=function(){bj=this;this.Y=(new ij).b();return this};
cj.prototype.$classData=l({Rt:0},!1,"japgolly.scalajs.react.vdom.TagMod$",{Rt:1,c:1});var bj=void 0;function gj(){}gj.prototype=new n;gj.prototype.constructor=gj;gj.prototype.b=function(){return this};gj.prototype.$classData=l({Vt:0},!1,"japgolly.scalajs.react.vdom.VdomArray$",{Vt:1,c:1});var fj=void 0;function Vi(){}Vi.prototype=new n;Vi.prototype.constructor=Vi;Vi.prototype.b=function(){return this};Vi.prototype.$classData=l({Xt:0},!1,"japgolly.scalajs.react.vdom.VdomElement$",{Xt:1,c:1});
var Ui=void 0;function jj(){this.Y=null}jj.prototype=new n;jj.prototype.constructor=jj;jj.prototype.b=function(){kj=this;this.Y=(new td).Xl(null);return this};jj.prototype.$classData=l({Yt:0},!1,"japgolly.scalajs.react.vdom.VdomNode$",{Yt:1,c:1});var kj=void 0;function sd(){kj||(kj=(new jj).b())}function lj(){}lj.prototype=new n;lj.prototype.constructor=lj;lj.prototype.b=function(){mj=this;(new nj).b();(new oj).b();(new pj).b();(new qj).b();(new rj).b();(new sj).b();(new tj).b();(new uj).b();return this};
lj.prototype.$classData=l({$t:0},!1,"japgolly.univeq.UnivEq$",{$t:1,c:1});var mj=void 0;function vj(){mj||(mj=(new lj).b())}function wj(){this.to=this.Mo=this.pr=null;this.Q=0}wj.prototype=new n;wj.prototype.constructor=wj;wj.prototype.b=function(){return this};function xj(){var a=Hc();0===(268435456&a.Q)&&0===(268435456&a.Q)&&(a.Mo=Gc(a).document,a.Q|=268435456);return a.Mo}function Gc(a){0===(134217728&a.Q)&&0===(134217728&a.Q)&&(a.pr=g.window,a.Q|=134217728);return a.pr}
function Bd(){var a=Hc();0===(536870912&a.Q)&&0===(536870912&a.Q)&&(a.to=Gc(a).console,a.Q|=536870912);return a.to}wj.prototype.$classData=l({ku:0},!1,"org.scalajs.dom.package$",{ku:1,c:1});var yj=void 0;function Hc(){yj||(yj=(new wj).b());return yj}function qc(){this.Wh=null}qc.prototype=new n;qc.prototype.constructor=qc;
qc.prototype.b=function(){pc=this;md();rc();nd();rc();od||(od=(new pd).b());var a=zj().gj,a=rd(a);rc();ld||(ld=(new kd).b());var b=ld.Qh(),a=ud((new A).g([a,b.Sk()]));this.Wh=gd(vd("HomePage",wd(a)));return this};qc.prototype.Qh=function(){return this.Wh.og.V()};qc.prototype.$classData=l({lu:0},!1,"pages.HomePage$",{lu:1,c:1});var pc=void 0;function rd(a){var b=Aj;rc();Bj||(Bj=(new Cj).b());return b(Bj,a.kp)}function Dj(){}Dj.prototype=new n;Dj.prototype.constructor=Dj;Dj.prototype.b=function(){return this};
Dj.prototype.$classData=l({pu:0},!1,"scalacss.defaults.PlatformExports$",{pu:1,c:1});var Ej=void 0;function Fj(){}Fj.prototype=new n;Fj.prototype.constructor=Fj;Fj.prototype.b=function(){return this};Fj.prototype.$classData=l({ru:0},!1,"scalacss.defaults.PlatformExports$StyleSheetInlineJsOps$",{ru:1,c:1});var Gj=void 0;function Hj(){this.Eb=this.Md=null}Hj.prototype=new n;Hj.prototype.constructor=Hj;function Ij(a,b,c){a.Md=b;a.Eb=c;return a}
function Jj(a){var b=a.C();return(new Kj).pb(b,t(new u,function(a){return function(b){var f=Lj(Mj(b.U));return(new li).pb(f,t(new u,function(a,b){return function(a){return Nj(b.R,a)}}(a,b)))}}(a)))}Hj.prototype.m=function(){var a=this.C();return Oj(a,"AVs(",", ",")")};Hj.prototype.C=function(){var a=Lj(Mj(this.Md));return(new li).pb(a,t(new u,function(a){return function(c){return(new I).d(c,a.Eb.f(c))}}(this)))};
function Pj(a,b,c){var e=Qj(a,b),f=Rj(e)?Sj(a.Md,b):a.Md;a=a.Eb.Cb(b,Tj(c,e));return Ij(new Hj,f,a)}function Uj(a,b){Vj();var c=Mj(a.Md),c=Wj(0,Xj(c,b));if(c.l())return B();c=c.P();return(new H).n(Ij(new Hj,c,Yj(a.Eb,b)))}function Qj(a,b){a=a.Eb.sb(b);return a.l()?Ac().Ec.dc:Mj(a.P())}Hj.prototype.$classData=l({su:0},!1,"scalacss.internal.AVs",{su:1,c:1});function rk(){}rk.prototype=new n;rk.prototype.constructor=rk;rk.prototype.b=function(){return this};
function sk(a,b,c){return Ij(new Hj,tk(Vj(),b),(new uk).d(b,c))}rk.prototype.$classData=l({tu:0},!1,"scalacss.internal.AVs$",{tu:1,c:1});var vk=void 0;function wk(){vk||(vk=(new rk).b());return vk}function xk(){this.hp=this.yg=null}xk.prototype=new n;xk.prototype.constructor=xk;function yk(){}yk.prototype=xk.prototype;xk.prototype.o=function(a){if(a&&a.$classData&&a.$classData.p.uu){a=a.yg;var b=this.yg;vj();return D(G(),a,b)}return!1};xk.prototype.m=function(){return this.yg};
xk.prototype.s=function(){return Bi(Ci(),this.yg)};xk.prototype.Bg=function(a,b){this.yg=a;this.hp=b;return this};function zk(){}zk.prototype=new n;zk.prototype.constructor=zk;zk.prototype.b=function(){return this};function Ak(a){var b=Bk();return(new Ck).Bg(a,Dk(b,a))}function Dk(a,b){return t(new u,function(a,b){return function(){return t(new u,function(a,b){return function(a){return Ek(Fk(),Gk(b,a))}}(a,b))}}(a,b))}function Hk(a,b,c){return(new Ck).Bg(b,Ik(c,b))}
zk.prototype.$classData=l({vu:0},!1,"scalacss.internal.Attr$",{vu:1,c:1});var Jk=void 0;function Bk(){Jk||(Jk=(new zk).b());return Jk}function Kk(){}Kk.prototype=new n;Kk.prototype.constructor=Kk;function Lk(){}Lk.prototype=Kk.prototype;function Mk(){this.zo=this.po=this.oo=null}Mk.prototype=new n;Mk.prototype.constructor=Mk;
Mk.prototype.b=function(){Nk=this;this.oo=Eb(function(){return function(a,b){(vj(),D(G(),a,b))?(Ok||(Ok=(new Pk).b()),a=Ok):a=Qk();return a}}(this));this.po=Eb(function(a){return function(b,c){b.zm.Ac(t(new u,function(a,b){return function(a){return b.O(a)}}(a,c.zm)))?(Rk||(Rk=(new Sk).b()),b=Rk):b=Qk();return b}}(this));this.zo=Tk(this,this.oo,this.po);return this};
function Tk(a,b,c){return Eb(function(a,b,c){return function(m,q){return wg(b,m,q).$m(v(new w,function(a,b,c,e){return function(){return wg(b,c,e)}}(a,c,m,q)))}}(a,b,c))}Mk.prototype.$classData=l({xu:0},!1,"scalacss.internal.AttrCmp$",{xu:1,c:1});var Nk=void 0;function Uk(){Nk||(Nk=(new Mk).b());return Nk}function Vk(){this.xg=this.Yj=null}Vk.prototype=new n;Vk.prototype.constructor=Vk;
Vk.prototype.b=function(){Wk=this;var a=J().hd,b=L().Ub,c=M(J().j,(new A).g([N(),Xk(),O()])),e=(new I).d(b,c),f=L().Vb,h=M(J().j,(new A).g([O()])),m=(new I).d(f,h),q=L().Wb,y=M(J().j,(new A).g([O()])),K=(new I).d(q,y),z=L().Xb,E=M(J().j,(new A).g([Xk()])),C=(new I).d(z,E),F=L().mc,W=M(J().j,(new A).g([Xk()])),bc=(new I).d(F,W),Nc=L().Yb,wb=M(J().j,(new A).g([Xk(),O()])),Lb=(new I).d(Nc,wb),yf=L().Zb,Oc=M(J().j,(new A).g([O()])),Ed=(new I).d(yf,Oc),ao=L().$b,bo=M(J().j,(new A).g([P(),Xk(),O()])),zf=
(new I).d(ao,bo),Ld=L().ac,Lf=M(J().j,(new A).g([P(),O()])),Qc=(new I).d(Ld,Lf),Md=L().bc,no=M(J().j,(new A).g([O()])),oo=(new I).d(Md,no),Mf=L().cc,Nd=M(J().j,(new A).g([Xk(),O()])),Nf=(new I).d(Mf,Nd),Rc=L().ec,Od=M(J().j,(new A).g([P(),Xk(),O()])),po=(new I).d(Rc,Od),qo=L().fc,Of=M(J().j,(new A).g([P()])),Pd=(new I).d(qo,Of),Pf=L().gc,Sc=M(J().j,(new A).g([P(),O()])),Qd=(new I).d(Pf,Sc),ro=L().hc,so=M(J().j,(new A).g([P(),Xk(),O()])),Qf=(new I).d(ro,so),Rd=L().nc,Rf=M(J().j,(new A).g([O()]));Yk(a,
(new A).g([e,m,K,C,bc,Lb,Ed,zf,Qc,oo,Nf,po,Pd,Qd,Qf,(new I).d(Rd,Rf)]));var Tc=J().hd,Sd=L().Ub,to=M(J().j,(new A).g([Zk()])),uo=(new I).d(Sd,to),Sf=L().Vb,Td=M(J().j,(new A).g([Zk()])),Tf=(new I).d(Sf,Td),Uc=L().Wb,Ud=M(J().j,(new A).g([Zk()])),vo=(new I).d(Uc,Ud),wo=L().Xb,Uf=M(J().j,(new A).g([Zk()])),Vd=(new I).d(wo,Uf),Vf=L().mc,Vc=M(J().j,(new A).g([Zk()])),Wd=(new I).d(Vf,Vc),xo=L().Yb,yo=M(J().j,(new A).g([N(),Zk()])),Wf=(new I).d(xo,yo),Xd=L().Zb,Xf=M(J().j,(new A).g([O()])),Wc=(new I).d(Xd,
Xf),Yd=L().$b,zo=M(J().j,(new A).g([P(),N(),Zk()])),Ao=(new I).d(Yd,zo),Yf=L().ac,Zd=M(J().j,(new A).g([P(),O()])),Zf=(new I).d(Yf,Zd),Xc=L().bc,$d=M(J().j,(new A).g([P(),O()])),Bo=(new I).d(Xc,$d),Co=L().cc,$f=M(J().j,(new A).g([N(),Zk(),O()])),ae=(new I).d(Co,$f),ag=L().ec,Yc=M(J().j,(new A).g([P(),Zk(),N()])),be=(new I).d(ag,Yc),Do=L().fc,Eo=M(J().j,(new A).g([N()])),bg=(new I).d(Do,Eo),ce=L().gc,cg=M(J().j,(new A).g([P(),N(),Zk()])),Zc=(new I).d(ce,cg),de=L().hc,Fo=M(J().j,(new A).g([N(),Zk(),
O()])),Go=(new I).d(de,Fo),dg=L().nc,ee=M(J().j,(new A).g([Zk()]));Yk(Tc,(new A).g([uo,Tf,vo,Vd,Wd,Wf,Wc,Ao,Zf,Bo,ae,be,bg,Zc,Go,(new I).d(dg,ee)]));var eg=J().hd,$c=L().Ub,fe=M(J().j,(new A).g([Xk(),O()])),Ho=(new I).d($c,fe),Io=L().Vb,fg=M(J().j,(new A).g([O()])),ge=(new I).d(Io,fg),gg=L().Wb,ad=M(J().j,(new A).g([O()])),he=(new I).d(gg,ad),Jo=L().Xb,Ko=M(J().j,(new A).g([O()])),hg=(new I).d(Jo,Ko),ie=L().mc,ig=M(J().j,(new A).g([O()])),bd=(new I).d(ie,ig),je=L().Yb,Zj=M(J().j,(new A).g([Xk(),O()])),
jg=(new I).d(je,Zj),cd=L().Zb,ke=M(J().j,(new A).g([O()])),Lo=(new I).d(cd,ke),Nw=L().$b,Ow=M(J().j,(new A).g([N(),Xk(),O()])),Pw=(new I).d(Nw,Ow),Qw=L().ac,Mo=M(J().j,(new A).g([P(),O()])),ts=(new I).d(Qw,Mo),us=L().bc,ak=M(J().j,(new A).g([O()])),Rw=(new I).d(us,ak),Sw=L().cc,Tw=M(J().j,(new A).g([Xk(),O()])),No=(new I).d(Sw,Tw),vs=L().ec,ws=M(J().j,(new A).g([P(),O()])),bk=(new I).d(vs,ws),Uw=L().fc,Vw=M(J().j,(new A).g([P()])),Ww=(new I).d(Uw,Vw),Oo=L().gc,xs=M(J().j,(new A).g([P(),O()])),ys=
(new I).d(Oo,xs),ck=L().hc,Xw=M(J().j,(new A).g([Xk(),O()])),Yw=(new I).d(ck,Xw),Zw=L().nc,Po=M(J().j,(new A).g([O()]));Yk(eg,(new A).g([Ho,ge,he,hg,bd,jg,Lo,Pw,ts,Rw,No,bk,Ww,ys,Yw,(new I).d(Zw,Po)]));var zs=J().hd,As=L().Ub,dk=M(J().j,(new A).g([O()])),$w=(new I).d(As,dk),ax=L().Vb,bx=M(J().j,(new A).g([O()])),Qo=(new I).d(ax,bx),Bs=L().Wb,Cs=M(J().j,(new A).g([O()])),ek=(new I).d(Bs,Cs),cx=L().Xb,dx=M(J().j,(new A).g([O()])),ex=(new I).d(cx,dx),Ro=L().mc,Ds=M(J().j,(new A).g([O()])),Es=(new I).d(Ro,
Ds),fk=L().Yb,fx=M(J().j,(new A).g([O()])),gx=(new I).d(fk,fx),hx=L().Zb,So=M(J().j,(new A).g([O()])),Fs=(new I).d(hx,So),Gs=L().$b,gk=M(J().j,(new A).g([O()])),ix=(new I).d(Gs,gk),jx=L().ac,kx=M(J().j,(new A).g([P(),O()])),To=(new I).d(jx,kx),Hs=L().bc,Is=M(J().j,(new A).g([O()])),hk=(new I).d(Hs,Is),lx=L().cc,mx=M(J().j,(new A).g([O()])),nx=(new I).d(lx,mx),Uo=L().ec,Js=M(J().j,(new A).g([O()])),Ks=(new I).d(Uo,Js),ik=L().fc,ox=M(J().j,(new A).g([O()])),px=(new I).d(ik,ox),qx=L().gc,Vo=M(J().j,
(new A).g([O()])),Ls=(new I).d(qx,Vo),Ms=L().hc,jk=M(J().j,(new A).g([O()])),rx=(new I).d(Ms,jk),sx=L().nc,tx=M(J().j,(new A).g([O()]));Yk(zs,(new A).g([$w,Qo,ek,ex,Es,gx,Fs,ix,To,hk,nx,Ks,px,Ls,rx,(new I).d(sx,tx)]));var Wo=J().hd,Ns=L().Ub,Os=M(J().j,(new A).g([N(),O()])),kk=(new I).d(Ns,Os),ux=L().Vb,vx=M(J().j,(new A).g([O()])),wx=(new I).d(ux,vx),Xo=L().Wb,Ps=M(J().j,(new A).g([O()])),Qs=(new I).d(Xo,Ps),lk=L().Xb,xx=M(J().j,(new A).g([N()])),yx=(new I).d(lk,xx),zx=L().mc,Yo=M(J().j,(new A).g([N(),
O()])),Rs=(new I).d(zx,Yo),Ss=L().Yb,mk=M(J().j,(new A).g([N(),Xk(),O()])),Ax=(new I).d(Ss,mk),Bx=L().Zb,Cx=M(J().j,(new A).g([O()])),Zo=(new I).d(Bx,Cx),Ts=L().$b,Us=M(J().j,(new A).g([N(),Zk(),O()])),nk=(new I).d(Ts,Us),Dx=L().ac,Ex=M(J().j,(new A).g([P(),N(),Zk()])),Fx=(new I).d(Dx,Ex),$o=L().bc,Vs=M(J().j,(new A).g([N(),O()])),Ws=(new I).d($o,Vs),ok=L().cc,Gx=M(J().j,(new A).g([N(),Xk(),O()])),Hx=(new I).d(ok,Gx),Ix=L().ec,ap=M(J().j,(new A).g([P(),O(),Xk()])),Xs=(new I).d(Ix,ap),Ys=L().fc,pk=
M(J().j,(new A).g([O()])),Jx=(new I).d(Ys,pk),Zs=L().gc,$s=M(J().j,(new A).g([P(),O()])),qk=(new I).d(Zs,$s),Kx=L().hc,SH=M(J().j,(new A).g([N(),Xk(),O()])),TH=(new I).d(Kx,SH),UH=L().nc,VH=M(J().j,(new A).g([O()]));this.Yj=Yk(Wo,(new A).g([kk,wx,Qs,yx,Rs,Ax,Zo,nk,Fx,Ws,Hx,Xs,Jx,qk,TH,(new I).d(UH,VH)]));var WH=J().hd,XH=L().Ub,YH=M(J().j,(new A).g([P()])),ZH=(new I).d(XH,YH),$H=L().Vb,aI=M(J().j,(new A).g([P()])),bI=(new I).d($H,aI),cI=L().Wb,dI=M(J().j,(new A).g([P()])),eI=(new I).d(cI,dI),fI=L().Xb,
gI=M(J().j,(new A).g([P()])),hI=(new I).d(fI,gI),iI=L().mc,jI=M(J().j,(new A).g([P()])),kI=(new I).d(iI,jI),lI=L().Yb,mI=M(J().j,(new A).g([P()])),nI=(new I).d(lI,mI),oI=L().Zb,pI=M(J().j,(new A).g([N()])),qI=(new I).d(oI,pI),rI=L().$b,sI=M(J().j,(new A).g([P()])),tI=(new I).d(rI,sI),uI=L().ac,vI=M(J().j,(new A).g([P(),N()])),wI=(new I).d(uI,vI),xI=L().bc,yI=M(J().j,(new A).g([N()])),zI=(new I).d(xI,yI),AI=L().cc,BI=M(J().j,(new A).g([P()])),CI=(new I).d(AI,BI),DI=L().ec,EI=M(J().j,(new A).g([P()])),
FI=(new I).d(DI,EI),GI=L().fc,HI=M(J().j,(new A).g([P()])),II=(new I).d(GI,HI),JI=L().gc,KI=M(J().j,(new A).g([P()])),LI=(new I).d(JI,KI),MI=L().hc,NI=M(J().j,(new A).g([P(),$k(),O()])),OI=(new I).d(MI,NI),PI=L().nc,QI=M(J().j,(new A).g([P()]));this.xg=Yk(WH,(new A).g([ZH,bI,eI,hI,kI,nI,qI,tI,wI,zI,CI,FI,II,LI,OI,(new I).d(PI,QI)]));var RI=J().hd,SI=L().Ub,TI=M(J().j,(new A).g([N()])),UI=(new I).d(SI,TI),VI=L().Vb,WI=M(J().j,(new A).g([N()])),XI=(new I).d(VI,WI),YI=L().Wb,ZI=M(J().j,(new A).g([Zk()])),
$I=(new I).d(YI,ZI),aJ=L().Xb,bJ=M(J().j,(new A).g([N()])),cJ=(new I).d(aJ,bJ),dJ=L().mc,eJ=M(J().j,(new A).g([N()])),fJ=(new I).d(dJ,eJ),gJ=L().Yb,hJ=M(J().j,(new A).g([N()])),iJ=(new I).d(gJ,hJ),jJ=L().Zb,kJ=M(J().j,(new A).g([P()])),lJ=(new I).d(jJ,kJ),mJ=L().$b,nJ=M(J().j,(new A).g([P(),Zk()])),oJ=(new I).d(mJ,nJ),pJ=L().ac,qJ=M(J().j,(new A).g([P()])),rJ=(new I).d(pJ,qJ),sJ=L().bc,tJ=M(J().j,(new A).g([P()])),uJ=(new I).d(sJ,tJ),vJ=L().cc,wJ=M(J().j,(new A).g([N()])),xJ=(new I).d(vJ,wJ),yJ=L().ec,
zJ=M(J().j,(new A).g([P(),N()])),AJ=(new I).d(yJ,zJ),BJ=L().fc,CJ=M(J().j,(new A).g([P()])),DJ=(new I).d(BJ,CJ),EJ=L().gc,FJ=M(J().j,(new A).g([P(),N()])),GJ=(new I).d(EJ,FJ),HJ=L().hc,IJ=M(J().j,(new A).g([P(),N()])),JJ=(new I).d(HJ,IJ),KJ=L().nc,LJ=M(J().j,(new A).g([N()]));Yk(RI,(new A).g([UI,XI,$I,cJ,fJ,iJ,lJ,oJ,rJ,uJ,xJ,AJ,DJ,GJ,JJ,(new I).d(KJ,LJ)]));var MJ=J().hd,NJ=L().Ub,OJ=M(J().j,(new A).g([N()])),PJ=(new I).d(NJ,OJ),QJ=L().Vb,RJ=M(J().j,(new A).g([Zk()])),SJ=(new I).d(QJ,RJ),TJ=L().Wb,
UJ=M(J().j,(new A).g([N()])),VJ=(new I).d(TJ,UJ),WJ=L().Xb,XJ=M(J().j,(new A).g([N()])),YJ=(new I).d(WJ,XJ),ZJ=L().mc,$J=M(J().j,(new A).g([N()])),aK=(new I).d(ZJ,$J),bK=L().Yb,cK=M(J().j,(new A).g([N(),Zk()])),dK=(new I).d(bK,cK),eK=L().Zb,fK=M(J().j,(new A).g([O()])),gK=(new I).d(eK,fK),hK=L().$b,iK=M(J().j,(new A).g([N()])),jK=(new I).d(hK,iK),kK=L().ac,lK=M(J().j,(new A).g([P(),O()])),mK=(new I).d(kK,lK),nK=L().bc,oK=M(J().j,(new A).g([O()])),pK=(new I).d(nK,oK),qK=L().cc,rK=M(J().j,(new A).g([N(),
Zk()])),sK=(new I).d(qK,rK),tK=L().ec,uK=M(J().j,(new A).g([P(),O(),N(),Zk()])),vK=(new I).d(tK,uK),wK=L().fc,xK=M(J().j,(new A).g([O()])),yK=(new I).d(wK,xK),zK=L().gc,AK=M(J().j,(new A).g([P(),O(),N()])),BK=(new I).d(zK,AK),CK=L().hc,DK=M(J().j,(new A).g([N(),Zk()])),EK=(new I).d(CK,DK),FK=L().nc,GK=M(J().j,(new A).g([N()]));Yk(MJ,(new A).g([PJ,SJ,VJ,YJ,aK,dK,gK,jK,mK,pK,sK,vK,yK,BK,EK,(new I).d(FK,GK)]));var HK=J().hd,IK=L().Ub,JK=M(J().j,(new A).g([O()])),KK=(new I).d(IK,JK),LK=L().Vb,MK=M(J().j,
(new A).g([O()])),NK=(new I).d(LK,MK),OK=L().Wb,PK=M(J().j,(new A).g([O()])),QK=(new I).d(OK,PK),RK=L().Xb,SK=M(J().j,(new A).g([O()])),TK=(new I).d(RK,SK),UK=L().mc,VK=M(J().j,(new A).g([O()])),WK=(new I).d(UK,VK),XK=L().Yb,YK=M(J().j,(new A).g([O()])),ZK=(new I).d(XK,YK),$K=L().Zb,aL=M(J().j,(new A).g([O()])),bL=(new I).d($K,aL),cL=L().$b,dL=M(J().j,(new A).g([O()])),eL=(new I).d(cL,dL),fL=L().ac,gL=M(J().j,(new A).g([P(),O()])),hL=(new I).d(fL,gL),iL=L().bc,jL=M(J().j,(new A).g([O()])),kL=(new I).d(iL,
jL),lL=L().cc,mL=M(J().j,(new A).g([O()])),nL=(new I).d(lL,mL),oL=L().ec,pL=M(J().j,(new A).g([O()])),qL=(new I).d(oL,pL),rL=L().fc,sL=M(J().j,(new A).g([P()])),tL=(new I).d(rL,sL),uL=L().gc,vL=M(J().j,(new A).g([O()])),wL=(new I).d(uL,vL),xL=L().hc,yL=M(J().j,(new A).g([O()])),zL=(new I).d(xL,yL),AL=L().nc,BL=M(J().j,(new A).g([O()]));Yk(HK,(new A).g([KK,NK,QK,TK,WK,ZK,bL,eL,hL,kL,nL,qL,tL,wL,zL,(new I).d(AL,BL)]));var CL=J().hd,DL=L().Ub,EL=M(J().j,(new A).g([P(),O()])),FL=(new I).d(DL,EL),GL=L().Vb,
HL=M(J().j,(new A).g([O()])),IL=(new I).d(GL,HL),JL=L().Wb,KL=M(J().j,(new A).g([P()])),LL=(new I).d(JL,KL),ML=L().Xb,NL=M(J().j,(new A).g([P()])),OL=(new I).d(ML,NL),PL=L().mc,QL=M(J().j,(new A).g([P()])),RL=(new I).d(PL,QL),SL=L().Yb,TL=M(J().j,(new A).g([P(),O()])),UL=(new I).d(SL,TL),VL=L().Zb,WL=M(J().j,(new A).g([P()])),XL=(new I).d(VL,WL),YL=L().$b,ZL=M(J().j,(new A).g([P()])),$L=(new I).d(YL,ZL),aM=L().ac,bM=M(J().j,(new A).g([P()])),cM=(new I).d(aM,bM),dM=L().bc,eM=M(J().j,(new A).g([P()])),
fM=(new I).d(dM,eM),gM=L().cc,hM=M(J().j,(new A).g([P(),Xk()])),iM=(new I).d(gM,hM),jM=L().ec,kM=M(J().j,(new A).g([P(),O()])),lM=(new I).d(jM,kM),mM=L().fc,nM=M(J().j,(new A).g([P()])),oM=(new I).d(mM,nM),pM=L().gc,qM=M(J().j,(new A).g([P(),O()])),rM=(new I).d(pM,qM),sM=L().hc,tM=M(J().j,(new A).g([P(),Xk()])),uM=(new I).d(sM,tM),vM=L().nc,wM=M(J().j,(new A).g([O()]));Yk(CL,(new A).g([FL,IL,LL,OL,RL,UL,XL,$L,cM,fM,iM,lM,oM,rM,uM,(new I).d(vM,wM)]));var xM=J().hd,yM=L().Ub,zM=M(J().j,(new A).g([P()])),
AM=(new I).d(yM,zM),BM=L().Vb,CM=M(J().j,(new A).g([P()])),DM=(new I).d(BM,CM),EM=L().Wb,FM=M(J().j,(new A).g([O()])),GM=(new I).d(EM,FM),HM=L().Xb,IM=M(J().j,(new A).g([P()])),JM=(new I).d(HM,IM),KM=L().mc,LM=M(J().j,(new A).g([P()])),MM=(new I).d(KM,LM),NM=L().Yb,OM=M(J().j,(new A).g([P()])),PM=(new I).d(NM,OM),QM=L().Zb,RM=M(J().j,(new A).g([P()])),SM=(new I).d(QM,RM),TM=L().$b,UM=M(J().j,(new A).g([P(),Xk(),O()])),VM=(new I).d(TM,UM),WM=L().ac,XM=M(J().j,(new A).g([P()])),YM=(new I).d(WM,XM),
ZM=L().bc,$M=M(J().j,(new A).g([P()])),aN=(new I).d(ZM,$M),bN=L().cc,cN=M(J().j,(new A).g([P(),N()])),dN=(new I).d(bN,cN),eN=L().ec,fN=M(J().j,(new A).g([P()])),gN=(new I).d(eN,fN),hN=L().fc,iN=M(J().j,(new A).g([P()])),jN=(new I).d(hN,iN),kN=L().gc,lN=M(J().j,(new A).g([P()])),mN=(new I).d(kN,lN),nN=L().hc,oN=M(J().j,(new A).g([P(),N()])),pN=(new I).d(nN,oN),qN=L().nc,rN=M(J().j,(new A).g([P()]));Yk(xM,(new A).g([AM,DM,GM,JM,MM,PM,SM,VM,YM,aN,dN,gN,jN,mN,pN,(new I).d(qN,rN)]));var sN=J().hd,tN=L().Ub,
uN=M(J().j,(new A).g([Xk(),O()])),vN=(new I).d(tN,uN),wN=L().Vb,xN=M(J().j,(new A).g([O()])),yN=(new I).d(wN,xN),zN=L().Wb,AN=M(J().j,(new A).g([O()])),BN=(new I).d(zN,AN),CN=L().Xb,DN=M(J().j,(new A).g([Xk()])),EN=(new I).d(CN,DN),FN=L().mc,GN=M(J().j,(new A).g([Xk(),O()])),HN=(new I).d(FN,GN),IN=L().Yb,JN=M(J().j,(new A).g([Xk(),O()])),KN=(new I).d(IN,JN),LN=L().Zb,MN=M(J().j,(new A).g([O()])),NN=(new I).d(LN,MN),ON=L().$b,PN=M(J().j,(new A).g([P(),Xk(),O()])),QN=(new I).d(ON,PN),RN=L().ac,SN=M(J().j,
(new A).g([P(),O()])),TN=(new I).d(RN,SN),UN=L().bc,VN=M(J().j,(new A).g([O()])),WN=(new I).d(UN,VN),XN=L().cc,YN=M(J().j,(new A).g([Xk(),O()])),ZN=(new I).d(XN,YN),$N=L().ec,aO=M(J().j,(new A).g([P(),Xk(),O()])),bO=(new I).d($N,aO),cO=L().fc,dO=M(J().j,(new A).g([P()])),eO=(new I).d(cO,dO),fO=L().gc,gO=M(J().j,(new A).g([Xk(),O()])),hO=(new I).d(fO,gO),iO=L().hc,jO=M(J().j,(new A).g([Xk(),O()])),kO=(new I).d(iO,jO),lO=L().nc,mO=M(J().j,(new A).g([O()]));Yk(sN,(new A).g([vN,yN,BN,EN,HN,KN,NN,QN,TN,
WN,ZN,bO,eO,hO,kO,(new I).d(lO,mO)]));return this};Vk.prototype.$classData=l({Mu:0},!1,"scalacss.internal.CanIUse$",{Mu:1,c:1});var Wk=void 0;function al(){Wk||(Wk=(new Vk).b());return Wk}function bl(){this.Cc=this.qa=null}bl.prototype=new n;bl.prototype.constructor=bl;function cl(){}cl.prototype=bl.prototype;bl.prototype.i=function(a){this.qa=a;this.Cc="-"+a+"-";return this};function dl(){this.Pg=null}dl.prototype=new n;dl.prototype.constructor=dl;
dl.prototype.b=function(){el=this;var a=fl(),b=(new A).g([gl(),hl(),il()]);jl();var c=kl().Qa,b=ll(b,c);this.Pg=ml(new nl,a,b);return this};dl.prototype.$classData=l({Ou:0},!1,"scalacss.internal.CanIUse$Prefix$",{Ou:1,c:1});var el=void 0;function ol(){this.hq=this.fq=this.rm=this.vp=null;this.Q=0}ol.prototype=new n;ol.prototype.constructor=ol;
function pl(a){if(-1!==(ql(rl(),a).toLowerCase().indexOf("android")|0)){a=a.qa;if(a.l())return L().Pg;a=a.P();if("Chrome"===a){a=L().Vb;var b=(new A).g([]);jl();var c=kl().Qa,b=ll(b,c);return ml(new nl,a,b)}if("Firefox"===a)return a=L().Wb,b=(new A).g([]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b);a=L().Ub;b=(new A).g([L().Xb]);jl();c=kl().Qa;b=ll(b,c);return ml(new nl,a,b)}a=a.qa;if(a.l())return L().Pg;a=a.P();if("Chrome"===a)return a=L().Yb,b=(new A).g([]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b);if("Firefox"===
a)return a=L().$b,b=(new A).g([]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b);if("IE"===a)return a=L().ac,b=(new A).g([L().bc]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b);if("Opera"===a)return a=L().ec,b=(new A).g([L().fc,L().gc]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b);if("Safari"===a)return a=L().cc,b=(new A).g([L().hc]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b);Ca();return"Edge"===a.substring((a.length|0)-4|0)?(a=L().Zb,b=(new A).g([]),jl(),c=kl().Qa,b=ll(b,c),ml(new nl,a,b)):L().Pg}
ol.prototype.b=function(){sl=this;this.rm=t(new u,function(){return function(a){if(P()===a||O()===a||Zk()===a)return!1;if(Xk()===a||N()===a)return!0;if($k()===a)return!1;throw(new x).n(a);}}(this));var a=Fk(),b=t(new u,function(a){return function(b){var f=tl(a,b);jl();var h=kl().Qa,h=ll(f,h);jl();f=kl().Qa;f=ul(h,f);for(h=Lj(h);h.Pb;){var m=h.r();f.kb((new H).n(m))}f=f.Ja();b=b.C();for(h=!1;!h&&b.u();)h=b.r().U.Ac(t(new u,function(a){return function(b){return!a.rm.f(b)}}(a)));return h?ah(f,B()):f}}(this));
vj();L();vj();vj();vj();this.fq=vl(a,b);el||(el=(new dl).b());a=Lj(Mj(el.Pg));a=(new li).pb(a,t(new u,function(){return function(a){return a.Cc}}(this)));a=Oj(a,".*(?:","|",").*");a=(new zh).i(a);xb();a=Ah(a.bb).sh;this.hq=t(new u,function(a,b){return function(a){return Lc(Mc(new Pc,b,a,a.length|0))}}(this,a));return this};
function wl(){var a=xl();if(0===(1&a.Q)&&0===(1&a.Q)){var b=yl,c=zl();al();var e=L().Ub,f=[P(),Xk(),O()];if(0===(f.length|0))var h=Q();else{for(var m=R(new S,Q()),q=0,y=f.length|0;q<y;)T(m,f[q]),q=1+q|0;h=m.y}var K=(new I).d(e,h),z=L().Vb,E=[O()];if(0===(E.length|0))var C=Q();else{for(var F=R(new S,Q()),W=0,bc=E.length|0;W<bc;)T(F,E[W]),W=1+W|0;C=F.y}var Nc=(new I).d(z,C),wb=L().Wb,Lb=[N()];if(0===(Lb.length|0))var yf=Q();else{for(var Oc=R(new S,Q()),Ed=0,ao=Lb.length|0;Ed<ao;)T(Oc,Lb[Ed]),Ed=1+Ed|
0;yf=Oc.y}var bo=(new I).d(wb,yf),zf=L().Xb,Ld=[P()];if(0===(Ld.length|0))var Lf=Q();else{for(var Qc=R(new S,Q()),Md=0,no=Ld.length|0;Md<no;)T(Qc,Ld[Md]),Md=1+Md|0;Lf=Qc.y}var oo=(new I).d(zf,Lf),Mf=L().mc,Nd=[P(),Xk()];if(0===(Nd.length|0))var Nf=Q();else{for(var Rc=R(new S,Q()),Od=0,po=Nd.length|0;Od<po;)T(Rc,Nd[Od]),Od=1+Od|0;Nf=Rc.y}var qo=(new I).d(Mf,Nf),Of=L().Yb,Pd=[P(),Xk(),O()];if(0===(Pd.length|0))var Pf=Q();else{for(var Sc=R(new S,Q()),Qd=0,ro=Pd.length|0;Qd<ro;)T(Sc,Pd[Qd]),Qd=1+Qd|0;
Pf=Sc.y}var so=(new I).d(Of,Pf),Qf=L().Zb,Rd=[P()];if(0===(Rd.length|0))var Rf=Q();else{for(var Tc=R(new S,Q()),Sd=0,to=Rd.length|0;Sd<to;)T(Tc,Rd[Sd]),Sd=1+Sd|0;Rf=Tc.y}var uo=(new I).d(Qf,Rf),Sf=L().$b,Td=[P(),N()];if(0===(Td.length|0))var Tf=Q();else{for(var Uc=R(new S,Q()),Ud=0,vo=Td.length|0;Ud<vo;)T(Uc,Td[Ud]),Ud=1+Ud|0;Tf=Uc.y}var wo=(new I).d(Sf,Tf),Uf=L().ac,Vd=[P()];if(0===(Vd.length|0))var Vf=Q();else{for(var Vc=R(new S,Q()),Wd=0,xo=Vd.length|0;Wd<xo;)T(Vc,Vd[Wd]),Wd=1+Wd|0;Vf=Vc.y}var yo=
(new I).d(Uf,Vf),Wf=L().bc,Xd=[P()];if(0===(Xd.length|0))var Xf=Q();else{for(var Wc=R(new S,Q()),Yd=0,zo=Xd.length|0;Yd<zo;)T(Wc,Xd[Yd]),Yd=1+Yd|0;Xf=Wc.y}var Ao=(new I).d(Wf,Xf),Yf=L().cc,Zd=[P(),N()];if(0===(Zd.length|0))var Zf=Q();else{for(var Xc=R(new S,Q()),$d=0,Bo=Zd.length|0;$d<Bo;)T(Xc,Zd[$d]),$d=1+$d|0;Zf=Xc.y}var Co=(new I).d(Yf,Zf),$f=L().ec,ae=[P(),Xk(),O()];if(0===(ae.length|0))var ag=Q();else{for(var Yc=R(new S,Q()),be=0,Do=ae.length|0;be<Do;)T(Yc,ae[be]),be=1+be|0;ag=Yc.y}var Eo=(new I).d($f,
ag),bg=L().fc,ce=[P()];if(0===(ce.length|0))var cg=Q();else{for(var Zc=R(new S,Q()),de=0,Fo=ce.length|0;de<Fo;)T(Zc,ce[de]),de=1+de|0;cg=Zc.y}var Go=(new I).d(bg,cg),dg=L().gc,ee=[P(),O()];if(0===(ee.length|0))var eg=Q();else{for(var $c=R(new S,Q()),fe=0,Ho=ee.length|0;fe<Ho;)T($c,ee[fe]),fe=1+fe|0;eg=$c.y}var Io=(new I).d(dg,eg),fg=L().hc,ge=[P(),N()];if(0===(ge.length|0))var gg=Q();else{for(var ad=R(new S,Q()),he=0,Jo=ge.length|0;he<Jo;)T(ad,ge[he]),he=1+he|0;gg=ad.y}var Ko=(new I).d(fg,gg),hg=
L().nc,ie=[Xk()];if(0===(ie.length|0))var ig=Q();else{for(var bd=R(new S,Q()),je=0,Zj=ie.length|0;je<Zj;)T(bd,ie[je]),je=1+je|0;ig=bd.y}for(var jg=[K,Nc,bo,oo,qo,so,uo,wo,yo,Ao,Co,Eo,Go,Io,Ko,(new I).d(hg,ig)],cd=Al(new Bl,Cl()),ke=0,Lo=jg.length|0;ke<Lo;)Dl(cd,jg[ke]),ke=1+ke|0;a.vp=b(c,cd.y,"fill-available",(new A).g(["max-content","min-content","fit-content","contain-floats"]));a.Q=(1|a.Q)<<24>>24}return a.vp}
function tl(a,b){b=b.C();b=(new Oi).pb(b,t(new u,function(a){return function(b){return b.U.Ac(a.rm)}}(a)));a=(new li).pb(b,t(new u,function(a){return function(b){return El(a).f(b.R)}}(a)));b=null;for(b=Q();a.u();){var c=a.r();b=b.Xd(c)}return b}function El(a){return t(new u,function(){return function(a){var c=[a.Cc];if(0===(c.length|0))c=Q();else{for(var e=R(new S,Q()),f=0,h=c.length|0;f<h;)T(e,c[f]),f=1+f|0;c=e.y}return c.Xd((new Fl).ef(a.Ri))}}(a))}
function Gl(a,b,c){jl();a=(new Hl).b();for(c=Lj(c);c.Pb;){var e=c.r(),f=e;f.l()?f=!0:(f=f.P(),f=b.O(f));!1!==f&&Il(a,e)}return Jl(a)}ol.prototype.$classData=l({Zu:0},!1,"scalacss.internal.CanIUse2$",{Zu:1,c:1});var sl=void 0;function xl(){sl||(sl=(new ol).b());return sl}function Kl(){this.vk=null}Kl.prototype=new n;Kl.prototype.constructor=Kl;
Kl.prototype.b=function(){Ll=this;this.vk=t(new u,function(a){return function(b){return(new H).n(t(new u,function(a,b){return function(a){return""+a.Cc+b}}(a,b)))}}(this));return this};function Ml(a,b){return t(new u,function(a,b){return function(f){return b.f(f)?a.vk.f(f):B()}}(a,b))}Kl.prototype.$classData=l({$u:0},!1,"scalacss.internal.CanIUse2$PrefixApply$",{$u:1,c:1});var Ll=void 0;function Nl(){Ll||(Ll=(new Kl).b());return Ll}function Ol(){}Ol.prototype=new n;Ol.prototype.constructor=Ol;
Ol.prototype.b=function(){return this};function Pl(a){if(Rj(a.cb))return a.nd;a=Mj(a);return Oj(a,"[","; ","]")}function Ql(){var a=Rl();return Eb(function(){return function(a,c){for(c=c.C();c.u();){var e=c.r();a=Pj(a,e.R,e.U)}return a}}(a))}
function Sl(){var a=Rl();return Eb(function(a){return function(c,e){e=e.C();c=(new li).pb(e,t(new u,function(a,b){return function(c){return Tl(Ul((new A).g(["{","} conflicts with ",""])),(new A).g([Vl(c.R,c.U),Wl(a,b)]))}}(a,c)));jl();e=kl().Qa;return Xl(c,e)}}(a))}function Wl(a,b){b=b.C();a=(new li).pb(b,t(new u,function(){return function(a){return Vl(a.R,a.U)}}(a)));return Oj(a,"{",", ","}")}function Vl(a,b){return Tl(Ul((new A).g(["",": ",""])),(new A).g([a.yg,Pl(b)]))}
Ol.prototype.$classData=l({bv:0},!1,"scalacss.internal.Compose$Rules$",{bv:1,c:1});var Yl=void 0;function Rl(){Yl||(Yl=(new Ol).b());return Yl}function Zl(){}Zl.prototype=new n;Zl.prototype.constructor=Zl;Zl.prototype.b=function(){return this};function $l(a,b,c){return am(0,"."+b.t,c)}function bm(a,b,c,e){a=cm(a,b,c,e).Yk(v(new w,function(a,b,c,e){return function(){return dm(a,b,c,e)}}(a,b,c,e)));jl();b=kl().Qa;return Xl(a,b)}
function em(a,b){b=b.rf();a=(new li).pb(b,t(new u,function(){return function(a){var b=a.Rc,f=a.tg,h=a.ug,m=a.vg,q=a.Ng,y=new fm;y.Lb=a.Lb;y.Rc=b;y.ii=f;y.ji=h;y.ki=m;y.nj=q;return y}}(a)));b=yg().Ra;return Xl(a,b)}function gm(a,b,c){b=b.rf();a=(new Kj).pb(b,t(new u,function(a,b){return function(c){return hm(a,c,b)}}(a,c)));jl();c=kl().Qa;return Xl(a,c)}function hm(a,b,c){return bm(a,"."+b.kg.t,b.ij,c)}
function im(a,b,c,e,f){Ac();var h=(new Hl).b();b=gm(a,b,f);jm(h,b);c=km(a,c,f);jm(h,c);a=em(a,e);jm(h,a);return Jl(h)}
function cm(a,b,c,e){c=c.Zd.C();return(new Kj).pb(c,t(new u,function(a,b,c){return function(e){if(null!==e){var y=e.R;e=Jj(e.U);e=(new li).pb(e,t(new u,function(a,b){return function(a){return a.gg.hp.f(b).f(a.t)}}(a,c)));for(var K=null,K=Ac().Ec.dc;e.u();)var z=e.r(),K=lm(K,z);if(Rj(K))return yg(),xb();e=K.I();K=mm(K);e=ml(new nl,e,K);K=nm(y);y=am(0,b,y);y=om(K,y,e);e=xb();return zb(new Ab,y,e)}throw(new x).n(e);}}(a,b,e)))}
function pm(a,b){Ac();a=(new Hl).b();yg();var c=(new zg).b();yg();var e=(new zg).b();for(b=Lj(b);b.Pb;){var f=b.r();if(qm(f))Il(a,f);else if(rm(f))Ag(c,f);else if(sm(f))Ag(e,f);else throw(new x).n(f);}return(new tm).Kf(Jl(a),Bg(c),Bg(e))}function nm(a){a=Wj(Vj(),a.nh);if(a.l())return B();a=a.P();return(new H).n(um(vm(),a))}function wm(a,b){a=Cl();for(b=Lj(b);b.Pb;){var c=b.r(),e=c,c=(new I).d(e.yh,e.mg),e=e.kf;a=a.Cb(e,xm(Vj(),a.sb(e),c))}return a}
function ym(a,b,c){var e=b.Dx();b=b.ZC().C();a=(new li).pb(b,t(new u,function(a,b){return function(c){return(new I).d(c.R,hm(a,c.U,b))}}(a,c)));for(c=Al(new Bl,Cl());a.u();)b=a.r(),Dl(c,b);return zm(e,c.y)}function dm(a,b,c,e){c=Lj(c.qe);return(new Kj).pb(c,t(new u,function(a,b,c){return function(e){var y=e.cE().f(""+b+e.XC());return bm(a,y,e.gE(),c)}}(a,b,e)))}function km(a,b,c){b=b.rf();a=(new li).pb(b,t(new u,function(a,b){return function(c){return ym(a,c,b)}}(a,c)));c=yg().Ra;return Xl(a,c)}
function am(a,b,c){a=c.Ti;return a.l()?b:a.P().mD(b)}Zl.prototype.$classData=l({ev:0},!1,"scalacss.internal.Css$",{ev:1,c:1});var Am=void 0;function Bm(){Am||(Am=(new Zl).b());return Am}function Cm(){this.Ah=this.eh=null}Cm.prototype=new n;Cm.prototype.constructor=Cm;function Dm(a,b,c){a.eh=b;a.Ah=c;return a}Cm.prototype.$classData=l({gv:0},!1,"scalacss.internal.CssKV$Lens",{gv:1,c:1});function Em(){this.Q=!1}Em.prototype=new n;Em.prototype.constructor=Em;
Em.prototype.b=function(){Fm=this;var a=[!0,!1];J();Gm();jl();for(var b=(new Hl).b(),c=0,e=a.length|0;c<e;)Il(b,a[c]),c=1+c|0;Jl(b).aa();return this};Em.prototype.$classData=l({hv:0},!1,"scalacss.internal.Domain$",{hv:1,c:1});var Fm=void 0;function Hm(){this.Aa=null}Hm.prototype=new n;Hm.prototype.constructor=Hm;Hm.prototype.o=function(a){Im||(Im=(new Jm).b());var b;b=this.Aa;a&&a.$classData&&a.$classData.p.Rn?(a=null===a?null:a.Aa,b=null===b?null===a:b.o(a)):b=!1;return b};
function Km(a,b){a.Aa=b;return a}Hm.prototype.s=function(){var a=this.Aa;return Lm(U(),a)};Hm.prototype.$classData=l({Rn:0},!1,"scalacss.internal.DslBase$ToStyle",{Rn:1,c:1});function Jm(){}Jm.prototype=new n;Jm.prototype.constructor=Jm;Jm.prototype.b=function(){return this};Jm.prototype.$classData=l({lv:0},!1,"scalacss.internal.DslBase$ToStyle$",{lv:1,c:1});var Im=void 0;function Mm(){this.Lb=null}Mm.prototype=new n;Mm.prototype.constructor=Mm;
Mm.prototype.o=function(a){var b;Nm||(Nm=(new Om).b());b=this.Lb;a&&a.$classData&&a.$classData.p.Wn?(a=null===a?null:a.Lb,b=null===b?null===a:b.o(a)):b=!1;return b};Mm.prototype.s=function(){return this.Lb.s()};function Pm(){var a=new Mm,b=(new H).n("myFont3");a.Lb=b;return a}Mm.prototype.$classData=l({Wn:0},!1,"scalacss.internal.FontFace$FontSrcSelector",{Wn:1,c:1});function Om(){}Om.prototype=new n;Om.prototype.constructor=Om;Om.prototype.b=function(){return this};
function Qm(a){var b=(new A).g([]).cg(),c=new Rm,b=ml(new nl,"local(Pacifico)",b),e=B(),f=B(),h=B(),m=B();c.Lb=a;c.Rc=b;c.tg=e;c.ug=f;c.vg=h;c.Ng=m;return c}Om.prototype.$classData=l({nv:0},!1,"scalacss.internal.FontFace$FontSrcSelector$",{nv:1,c:1});var Nm=void 0;function Sm(){this.t=null}Sm.prototype=new n;Sm.prototype.constructor=Sm;function Tm(){}Tm.prototype=Sm.prototype;Sm.prototype.i=function(a){this.t=a;return this};function Um(){this.t=null}Um.prototype=new n;Um.prototype.constructor=Um;
function Vm(){}Vm.prototype=Um.prototype;Um.prototype.m=function(){return Tl(Ul((new A).g(["Literal(",")"])),(new A).g([this.t]))};Um.prototype.i=function(a){this.t=a;return this};function Wm(){this.xo=this.Dl=null}Wm.prototype=new n;Wm.prototype.constructor=Wm;
Wm.prototype.b=function(){Xm=this;this.Dl=t(new u,function(a,b){return function(a){a=b.f(a);V();return""+X(V(),"(",a)+")"}}(this,t(new u,function(a){return function(b){if(b&&b.$classData&&b.$classData.p.tC){var c=b.Ex();b=function(){return function(a){return a.zc()}}(a);if(Ym(c)){c=c.bf();V();var e=X(V(),"aspect-ratio",":");return X(0,e,b(c))}if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","aspect-ratio"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-","aspect-ratio"),X(0,""+e+
":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.BC){c=b.im();b=function(){return function(a){return a.zc()}}(a);if(Ym(c))return c=c.bf(),V(),e=X(V(),"height",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","height"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-","height"),X(0,""+e+":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.LC){c=b.im();b=function(){return function(a){return a.zc()}}(a);if(Ym(c))return c=c.bf(),V(),e=X(V(),"width",
":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","width"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-","width"),X(0,""+e+":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.wC){c=b.Ex();b=function(){return function(a){return a.zc()}}(a);if(Ym(c))return c=c.bf(),V(),e=X(V(),"device-aspect-ratio",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","device-aspect-ratio"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-",
"device-aspect-ratio"),X(0,""+e+":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.xC){c=b.im();b=function(){return function(a){return a.zc()}}(a);if(Ym(c))return c=c.bf(),V(),e=X(V(),"device-height",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","device-height"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-","device-height"),X(0,""+e+":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.yC){c=b.im();b=function(){return function(a){return a.zc()}}(a);
if(Ym(c))return c=c.bf(),V(),e=X(V(),"device-width",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","device-width"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-","device-width"),X(0,""+e+":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.uC){c=b.WC();b=function(){return function(a){return""+(a|0)}}(a);if(c.l())return"color";c=c.P();if(Ym(c))return c=c.bf(),V(),e=X(V(),"color",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","color"),
X(0,""+e+":",b(c));if(!$m(c))throw(new x).n(c);c=c.hf();V();V();e=X(V(),"max-","color");return X(0,""+e+":",b(c))}if(b&&b.$classData&&b.$classData.p.vC){c=b.bD();b=function(){return function(a){return""+(a|0)}}(a);if(c.l())return"color-index";c=c.P();if(Ym(c))return c=c.bf(),V(),e=X(V(),"color-index",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","color-index"),X(0,""+e+":",b(c));if(!$m(c))throw(new x).n(c);c=c.hf();V();V();e=X(V(),"max-","color-index");return X(0,""+e+":",b(c))}if(b&&
b.$classData&&b.$classData.p.JC){c=b.oD();b=function(){return function(a){return a.zc()}}(a);if(Ym(c))return c=c.bf(),V(),e=X(V(),"resolution",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","resolution"),X(0,""+e+":",b(c));if($m(c))return c=c.hf(),V(),V(),e=X(V(),"max-","resolution"),X(0,""+e+":",b(c));throw(new x).n(c);}if(b&&b.$classData&&b.$classData.p.FC){c=b.VC();b=function(){return function(a){return""+(a|0)}}(a);if(c.l())return"monochrome";c=c.P();if(Ym(c))return c=c.bf(),
V(),e=X(V(),"monochrome",":"),X(0,e,b(c));if(Zm(c))return c=c.jf(),V(),V(),e=X(V(),"min-","monochrome"),X(0,""+e+":",b(c));if(!$m(c))throw(new x).n(c);c=c.hf();V();V();e=X(V(),"max-","monochrome");return X(0,""+e+":",b(c))}if(b&&b.$classData&&b.$classData.p.IC)return b=b.kE(),X(V(),"orientation:",b.zc());if(b&&b.$classData&&b.$classData.p.KC)return b=b.lE(),X(V(),"scan:",b.zc());if(b&&b.$classData&&b.$classData.p.AC){b=b.mE();if(b.l())return"grid";b=b.P();V();return""+X(V(),"grid",":")+(""+(b|0))}throw(new x).n(b);
}}(this))));this.xo=t(new u,function(){return function(a){if(a&&a.$classData&&a.$classData.p.CC)return a.hr().zc();if(a&&a.$classData&&a.$classData.p.GC)return a=a.hr(),V(),"not "+a.zc();if(a&&a.$classData&&a.$classData.p.HC)return a=a.hr(),V(),"only "+a.zc();throw(new x).n(a);}}(this));return this};
function an(a,b){a=function(a){return function(b){var c;c=b.aD();var e=a.xo;if(bn(c))c=a.Dl.f(c.J);else if(cn(c))c=e.f(c.J);else throw(new x).n(c);b=mm(b);for(b=Lj(b);b.Pb;)e=b.r(),V(),c=X(V(),c," and "),c=X(0,c,a.Dl.f(e));return c}}(a);var c=b.cb,e=null,e=a(b.nd);for(b=Lj(c);b.Pb;)c=b.r(),c=a(c),V(),e=""+X(V(),e,", ")+c;return e}function um(a,b){V();return"@media "+an(a,b)}Wm.prototype.$classData=l({uv:0},!1,"scalacss.internal.Media$",{uv:1,c:1});var Xm=void 0;
function vm(){Xm||(Xm=(new Wm).b());return Xm}function dn(){}dn.prototype=new n;dn.prototype.constructor=dn;dn.prototype.b=function(){return this};function X(a,b,c){return""+b+c}dn.prototype.$classData=l({vv:0},!1,"scalacss.internal.Media$StringExt$",{vv:1,c:1});var en=void 0;function V(){en||(en=(new dn).b());return en}function nl(){this.cb=this.nd=null}nl.prototype=new n;nl.prototype.constructor=nl;function Sj(a,b){var c=a.nd;a=ah(a.cb,b);return ml(new nl,c,a)}
function Tj(a,b){if(Rj(b))return a;var c=b.I();a=lm(mm(b),Mj(a));return ml(new nl,c,a)}nl.prototype.o=function(a){if(a&&a.$classData&&a.$classData.p.Yn&&D(G(),this.nd,a.nd)){var b=this.cb;a=a.cb;return null===b?null===a:fn(b,a)}return!1};nl.prototype.m=function(){var a=Mj(this);return"NonEmpty"+gn(a)};function Mj(a){return hn(a.cb,a.nd)}nl.prototype.s=function(){return ca(31,Bi(Ci(),this.nd))+Bi(Ci(),this.cb)|0};function ml(a,b,c){a.nd=b;a.cb=c;return a}
nl.prototype.$classData=l({Yn:0},!1,"scalacss.internal.NonEmptyVector",{Yn:1,c:1});function jn(){}jn.prototype=new n;jn.prototype.constructor=jn;jn.prototype.b=function(){return this};function kn(a,b,c){return Rj(b)?(a=Ac().Ec,ml(new nl,c,a.dc)):ml(new nl,b.I(),ah(mm(b),c))}function Wj(a,b){if(Rj(b))return B();a=b.I();b=mm(b);return(new H).n(ml(new nl,a,b))}function tk(a,b){a=Ac().Ec;return ml(new nl,b,a.dc)}function xm(a,b,c){return b.l()?tk(0,c):Sj(b.P(),c)}
jn.prototype.$classData=l({wv:0},!1,"scalacss.internal.NonEmptyVector$",{wv:1,c:1});var ln=void 0;function Vj(){ln||(ln=(new jn).b());return ln}function mn(){this.Md=null}mn.prototype=new n;mn.prototype.constructor=mn;mn.prototype.b=function(){nn=this;this.Md=(new on).b();return this};mn.prototype.$classData=l({xv:0},!1,"scalacss.internal.Pseudo$",{xv:1,c:1});var nn=void 0;function pn(){}pn.prototype=new n;pn.prototype.constructor=pn;pn.prototype.b=function(){qn=this;(new rn).b();return this};
pn.prototype.$classData=l({zv:0},!1,"scalacss.internal.PseudoType$",{zv:1,c:1});var qn=void 0;function sn(){this.Ao=null;this.Q=0}sn.prototype=new n;sn.prototype.constructor=sn;sn.prototype.b=function(){return this};function tn(){var a;un||(un=(new sn).b());a=un;if(0===(2&a.Q)&&0===(2&a.Q)){var b=vn(a);a.Ao=(new wn).Mb(b);a.Q=(2|a.Q)<<24>>24}return a.Ao}
function xn(a,b,c,e,f,h,m){!a.l()&&yn(m,f);!b.l()&&yn(m,f);yn(m,f);yn(m,c.xa);zn(m.ra,58);yn(m,h);a=c.t;e&&-1!==(a.indexOf(" ")|0)?(zn(m.ra,34),yn(m,a),zn(m.ra,34)):yn(m,a);yn(m,";\n")}
function vn(a){return t(new u,function(a,c,e){return function(f){return An(new Bn,t(new u,function(a,b){return function(a){yn(b,"@keyframes ");yn(b,a);yn(b," {\n")}}(a,f)),t(new u,function(a,b,c){return function(a){yn(c,b);yn(c,a);yn(c," {\n")}}(a,c,f)),t(new u,function(a,b){return function(a){yn(b,a);yn(b," {\n")}}(a,f)),Eb(function(a,b,c){return function(a,e){!a.l()&&yn(c,b);yn(c,e);yn(c," {\n")}}(a,c,f)),og(function(a,b,c,e){return function(a,f,h){xn(a,f,h,!1,b,c,e)}}(a,c,e,f)),og(function(a,b,
c,e){return function(a,f,h){xn(a,f,h,!1,b,c,e)}}(a,c,e,f)),Eb(function(a,b,c){return function(a){!a.l()&&yn(c,b);yn(c,"}\n");a.l()&&zn(c.ra,10)}}(a,c,f)),t(new u,function(a,b){return function(){yn(b,"}\n\n")}}(a,f)),t(new u,function(a,b,c){return function(){yn(c,b);yn(c,"}\n\n")}}(a,c,f)),t(new u,function(a,b){return function(){yn(b,"}\n\n")}}(a,f)),t(new u,function(a,b,c,e){return function(f){var z=function(a,b,c,e){return function(a,f,h){h=!!h;xn(B(),B(),Gk(a,f),h,b,c,e)}}(a,b,c,e);yn(e,"@font-face {\n");
var E=f.Lb;xn(B(),B(),Gk("font-family",E),!0,b,c,e);E=Mj(f.Rc);E=Oj(E,"",",","");z("src",E,!1);E=f.ii;E.l()||(E=E.P(),z("font-stretch",E,!1));E=f.ji;E.l()||(E=E.P(),z("font-style",E,!1));E=f.ki;E.l()||(E=E.P(),z("font-weight",E,!1));f=f.nj;f.l()||(f=f.P().m(),z("unicode-range",f,!1));yn(e,"}\n\n")}}(a,c,e,f)),v(new w,function(){return function(){}}(a)))}}(a,"  "," "))}sn.prototype.$classData=l({Dv:0},!1,"scalacss.internal.StringRenderer$",{Dv:1,c:1});var un=void 0;function Cn(){}Cn.prototype=new n;
Cn.prototype.constructor=Cn;Cn.prototype.b=function(){return this};Cn.prototype.$classData=l({Fv:0},!1,"scalacss.internal.Style$",{Fv:1,c:1});var Dn=void 0;function En(){}En.prototype=new n;En.prototype.constructor=En;En.prototype.b=function(){return this};En.prototype.$classData=l({Hv:0},!1,"scalacss.internal.StyleF$",{Hv:1,c:1});var Fn=void 0;function Gn(){this.Ek=null}Gn.prototype=new n;Gn.prototype.constructor=Gn;
function Hn(a,b){return(new Gn).Mb(t(new u,function(a,b){return function(f){var h=a.Ek.f(f);f=b.Ek.f(f);return t(new u,function(a,b,c){return function(a){return b.f(a).sg(c,(jl(),kl().Qa))}}(a,h,f))}}(a,b)))}Gn.prototype.Mb=function(a){this.Ek=a;return this};function Ik(a,b){return t(new u,function(a,b){return function(f){f=a.Ek.f(f);return t(new u,function(a,b,c){return function(a){return c.f(Gk(b,a))}}(a,b,f))}}(a,b))}Gn.prototype.$classData=l({Jv:0},!1,"scalacss.internal.Transform",{Jv:1,c:1});
function In(){}In.prototype=new n;In.prototype.constructor=In;In.prototype.b=function(){return this};function yl(a,b,c,e){c=e.kr().lc(c);c=Ml(Nl(),t(new u,function(a,b){return function(a){return b.O(a)}}(a,c)));return Jn(a,b,c,Kn().t)}
function Jn(a,b,c,e){b=xl().fq.f(b);return(new Gn).Mb(t(new u,function(a,b,c,e){return function(y){y=Gl(xl(),y.gq,e);return t(new u,function(a,b,c,e){return function(a){var f=xl(),h=c.eh.f(a);if(f.hq.f(h))a=Ek(Fk(),a);else if(f=b.f(h),f.l())a=Ek(Fk(),a);else{f=f.P();jl();for(var h=kl().Qa,h=ul(e,h),m=Lj(e);m.Pb;){var q=m.r();q.l()?q=a:(q=q.P(),q=wg(c.Ah,a,f.f(q)));h.kb(q)}a=h.Ja()}return a}}(a,b,c,y))}}(a,c,e,b)))}In.prototype.$classData=l({Kv:0},!1,"scalacss.internal.Transform$",{Kv:1,c:1});
var Ln=void 0;function zl(){Ln||(Ln=(new In).b());return Ln}function Mn(){}Mn.prototype=new n;Mn.prototype.constructor=Mn;function Nn(){}Nn.prototype=Mn.prototype;function On(a,b){return(new Pn).Mb(t(new u,function(a,b){return function(f){return Qn(b,Qn(a,f))}}(a,b)))}function Rn(){}Rn.prototype=new n;Rn.prototype.constructor=Rn;Rn.prototype.b=function(){return this};function Sn(a,b){return(new Pn).Mb(t(new u,function(a,b){return function(a){return(new Tn).i(b.f(a))}}(a,b)))}
Rn.prototype.$classData=l({Nv:0},!1,"scalacss.internal.ValueT$Rule$",{Nv:1,c:1});var Un=void 0;function Vn(){Un||(Un=(new Rn).b());return Un}function Wn(){}Wn.prototype=new n;Wn.prototype.constructor=Wn;function Xn(){}Xn.prototype=Wn.prototype;function Yn(){}Yn.prototype=new n;Yn.prototype.constructor=Yn;function Zn(){}Zn.prototype=Yn.prototype;Yn.prototype.m=function(){return this.ue().yg};function $n(){}$n.prototype=new n;$n.prototype.constructor=$n;$n.prototype.b=function(){return this};
$n.prototype.$classData=l({Tv:0},!1,"scalacss.internal.mutable.Mutex",{Tv:1,c:1});function co(){this.Gg=null}co.prototype=new n;co.prototype.constructor=co;co.prototype.b=function(){eo=this;this.Gg=(new $n).b();return this};co.prototype.$classData=l({Uv:0},!1,"scalacss.internal.mutable.Mutex$",{Uv:1,c:1});var eo=void 0;function fo(){eo||(eo=(new co).b());return eo}function go(){this.wj=this.Zk=this.Jh=this.al=this.Gg=this.Yo=this.Mp=null;this.Ih=!1}go.prototype=new n;go.prototype.constructor=go;
function ho(a){a.Ih=!0;return a.Jh}function io(a,b,c,e){if(c.Zd.l()&&Rj(c.qe))return c;var f=a.Mp;if(null===b)throw(new Ic).b();""===b?e=B():(b=Ch(Mc(new Pc,f.wo.sh,b,Ga(b)),"_"),e=(new H).n((new jo).i(Tl(Ul((new A).g(["","-",""])),(new A).g([e.t,b])))));e.l()?a=B():(e=e.P(),a=(new H).n(ko(a,e)));return lo(new mo,c.Zd,c.qe,a,c.Cd,c.sf)}
function bp(a,b){for(var c=Lj(a.Jh),e=!1;!e&&c.Pb;)e=c.r().kg,vj(),e=D(G(),e,b);if(e)return!0;a=Lj(a.Zk);for(c=!1;!c&&a.Pb;)c=a.r().Dx(),vj(),c=D(G(),c,b);return c}function cp(a,b,c,e){var f=fo().Gg;a.Mp=c;a.Yo=e;a.Gg=f;a.al=b;a.Jh=Ac().Ec.dc;a.Zk=Ac().Ec.dc;a.wj=Ac().Ec.dc;a.Ih=!1;return a}function ko(a,b){if(bp(a,b))a:{var c=2;for(;;){var e=(new jo).i(Tl(Ul((new A).g(["","-",""])),(new A).g([b.t,c])));if(bp(a,e))c=1+c|0;else{a=e;break a}}}else a=b;return a}function dp(a){a.Ih=!0;return a.wj}
function ep(a,b,c){var e=b.Lb;c=e.l()?fp(a,c).t:e.P();var e=new Rm,f=b.Rc,h=b.tg,m=b.ug,q=b.vg;b=b.Ng;e.Lb=c;e.Rc=f;e.tg=h;e.ug=m;e.vg=q;e.Ng=b;a.wj=ah(a.wj,e);return e}function gp(a){a.Ih=!0;return a.Zk}
function hp(a,b,c){if(!b.Cd.l()&&b.Zd.l()&&b.lg.l()&&Rj(b.qe)){var e=b.Cd.I(),f=mm(b.Cd),e=(new H).n(e);b=lo(new mo,b.Zd,b.qe,e,f,b.sf)}f=b.lg;c=f.l()?fp(a,c):f.P();f=ip(a.Yo).J;a.Ih&&wg(f,c,jp(new kp,lp().Y,"Style being registered after stylesheet has been rendered (via .render), or extracted (via .styles)."));bp(a,c)&&wg(f,c,jp(new kp,lp().Y,"Another style in the register has the same classname."));for(e=Lj(b.sf);e.Pb;){var h=e.r();wg(f,c,h)}c=mp(new np,c,b.Cd,b);a.Jh=ah(a.Jh,c);return c}
function fp(a,b){var c;c=a.al;Ca();b=b.t;var e=c.sm;b=(new jo).i(op(c.Ol,ia(r(p),[b,e])));c=(new pp).qi(c.Ol,1+c.sm|0);c=(new I).d(b,c);if(null===c)throw(new x).n(c);b=c.R;a.al=c.U;return b}function qp(a,b,c){return b.Ij(im(Bm(),ho(a),gp(a),dp(a),c))}go.prototype.$classData=l({Vv:0},!1,"scalacss.internal.mutable.Register",{Vv:1,c:1});function rp(){}rp.prototype=new n;rp.prototype.constructor=rp;rp.prototype.b=function(){return this};
function sp(){var a;tp||(tp=(new rp).b());a=tp;var b=up().Zo.Og;return(new vp).Mb(t(new u,function(a,b){return function(a){wp(b,null===a?"null":a);wp(b,"\n")}}(a,b)))}rp.prototype.$classData=l({Wv:0},!1,"scalacss.internal.mutable.Register$ErrorHandler$",{Wv:1,c:1});var tp=void 0;function xp(){this.ah=this.Uj=null}xp.prototype=new n;xp.prototype.constructor=xp;function yp(){}yp.prototype=xp.prototype;
xp.prototype.b=function(){Ca();Ca();Ca();var a=Ib(la(this));this.ah=(new zp).i(Ap(0,Wg(0,Wg(0,Wg(Ca(),a,"^\\$line.+[.$]\\$iw[.$]",""),"\\$+$",""),"^(?:.+\\.)(.+?)$","$1"),"\\$+","_"));return this};function Bp(a){if(null===a.Uj&&null===a.Uj){var b=new Cp;if(null===a)throw Dp(ni(),null);b.Tb=a;Ep.prototype.b.call(b);a.Uj=b}return a.Uj}function Fp(){this.xj=this.Hh=this.Gg=null}Fp.prototype=new n;Fp.prototype.constructor=Fp;function Gp(){}Gp.prototype=Fp.prototype;
function Hp(a,b){Ip(a,t(new u,function(a,b){return function(a){a=Lj(a);Jp(a,b)}}(a,b)))}Fp.prototype.mw=function(a){this.Gg=a;this.Hh=Ac().Ec.dc;this.xj=t(new u,function(){return function(){}}(this));return this};function Ip(a,b){!a.Hh.l()&&b.f(a.Hh);a.xj=t(new u,function(a,b,f){return function(a){f.f(a);b.f(a)}}(a,b,a.xj))}function Kp(a,b){b=b.cg();a.Hh=lm(a.Hh,b);a.xj.f(b)}function Lp(){}Lp.prototype=new n;Lp.prototype.constructor=Lp;Lp.prototype.b=function(){return this};
function Ek(a,b){return ah(Ac().Ec.dc,b)}function vl(a,b){var c=(new Mp).b();return t(new u,function(a,b,c){return function(a){var e=c.sb(a);return e.l()?(e=b.f(a),c.ym(a,e),e):e.P()}}(a,b,c))}Lp.prototype.$classData=l({ew:0},!1,"scalacss.internal.package$",{ew:1,c:1});var Np=void 0;function Fk(){Np||(Np=(new Lp).b());return Np}function Ua(){this.Eb=null}Ua.prototype=new n;Ua.prototype.constructor=Ua;function Ib(a){return a.Eb.name}
Ua.prototype.m=function(){return(this.Eb.isInterface?"interface ":this.Eb.isPrimitive?"":"class ")+Ib(this)};Ua.prototype.$classData=l({yw:0},!1,"java.lang.Class",{yw:1,c:1});function Op(){this.Xo=this.Yp=null}Op.prototype=new n;Op.prototype.constructor=Op;Op.prototype.b=function(){Pp=this;this.Yp=Qp(!1);this.Xo=Qp(!0);return this};Op.prototype.$classData=l({Pw:0},!1,"java.lang.System$",{Pw:1,c:1});var Pp=void 0;function Rp(){Pp||(Pp=(new Op).b());return Pp}function Sp(){}Sp.prototype=new n;
Sp.prototype.constructor=Sp;Sp.prototype.b=function(){return this};function Tp(a,b){a=null===b.xa?0:Aa(b.xa);b=null===b.t?0:Aa(b.t);return a^b}function Up(a,b,c){if(c&&c.$classData&&c.$classData.p.ux){a=b.xa;var e=c.xa;if(null===a?null===e:za(a,e))return b=b.t,c=c.t,null===b?null===c:za(b,c)}return!1}Sp.prototype.$classData=l({Sw:0},!1,"java.util.AbstractMap$",{Sw:1,c:1});var Vp=void 0;function Wp(){Vp||(Vp=(new Sp).b());return Vp}function Xp(){}Xp.prototype=new n;Xp.prototype.constructor=Xp;
Xp.prototype.b=function(){return this};function Yp(a,b){a=b.e.length;for(var c=0;c!==a;)b.e[c]=0,c=1+c|0}Xp.prototype.$classData=l({Tw:0},!1,"java.util.Arrays$",{Tw:1,c:1});var Zp=void 0;function $p(){Zp||(Zp=(new Xp).b());return Zp}function aq(){this.fn=this.gn=this.hn=null;this.Q=0}aq.prototype=new n;aq.prototype.constructor=aq;aq.prototype.b=function(){return this};aq.prototype.$classData=l({Uw:0},!1,"java.util.Collections$",{Uw:1,c:1});var bq=void 0;
function cq(){bq||(bq=(new aq).b());return bq}function dq(){this.yp=null}dq.prototype=new n;dq.prototype.constructor=dq;dq.prototype.b=function(){eq=this;this.yp=new g.RegExp("(?:(\\d+)\\$)?([-#+ 0,\\(\x3c]*)(\\d+)?(?:\\.(\\d+))?[%A-Za-z]","g");return this};dq.prototype.$classData=l({hx:0},!1,"java.util.Formatter$",{hx:1,c:1});var eq=void 0;function fq(){}fq.prototype=new n;fq.prototype.constructor=fq;function gq(){}gq.prototype=fq.prototype;function hq(){}hq.prototype=new n;
hq.prototype.constructor=hq;function iq(){}iq.prototype=hq.prototype;function jq(){}jq.prototype=new n;jq.prototype.constructor=jq;function kq(){}kq.prototype=jq.prototype;function lq(a,b,c){return a.Zc(b)?a.f(b):c.f(b)}function mq(){this.Uo=this.th=null}mq.prototype=new n;mq.prototype.constructor=mq;mq.prototype.b=function(){nq=this;this.th=(new oq).b();this.Uo=(new pq).b();return this};mq.prototype.$classData=l({Lx:0},!1,"scala.PartialFunction$",{Lx:1,c:1});var nq=void 0;
function qq(){nq||(nq=(new mq).b());return nq}function Ni(){}Ni.prototype=new n;Ni.prototype.constructor=Ni;Ni.prototype.b=function(){return this};Ni.prototype.$classData=l({Ux:0},!1,"scala.Predef$any2stringadd$",{Ux:1,c:1});var Mi=void 0;function rq(){}rq.prototype=new n;rq.prototype.constructor=rq;rq.prototype.b=function(){return this};rq.prototype.$classData=l({by:0},!1,"scala.math.Ordered$",{by:1,c:1});var sq=void 0;function tq(){this.Ec=null;this.Q=0}tq.prototype=new n;
tq.prototype.constructor=tq;tq.prototype.b=function(){uq=this;(new vq).b();wq||(wq=(new xq).b());yq();zq();kl();Aq();yg();xb();Bq||(Bq=(new Cq).b());Dq||(Dq=(new Eq).b());Fq||(Fq=(new Gq).b());Hq();Iq||(Iq=(new Jq).b());this.Ec=jl();Kq||(Kq=(new Lq).b());Mq||(Mq=(new Nq).b());Oq||(Oq=(new Pq).b());Qq||(Qq=(new Rq).b());Sq||(Sq=(new Tq).b());Uq||(Uq=(new Vq).b());sq||(sq=(new rq).b());Wq||(Wq=(new Xq).b());Yq||(Yq=(new Zq).b());$q||($q=(new ar).b());br||(br=(new cr).b());return this};
tq.prototype.$classData=l({gy:0},!1,"scala.package$",{gy:1,c:1});var uq=void 0;function Ac(){uq||(uq=(new tq).b());return uq}function dr(){this.fg=null}dr.prototype=new n;dr.prototype.constructor=dr;
dr.prototype.b=function(){er=this;fr||(fr=(new gr).b());hr||(hr=(new ir).b());jr||(jr=(new kr).b());lr||(lr=(new mr).b());nr||(nr=(new or).b());pr||(pr=(new qr).b());rr||(rr=(new sr).b());tr||(tr=(new ur).b());vr||(vr=(new wr).b());this.fg=vr;xr||(xr=(new yr).b());zr||(zr=(new Ar).b());Br||(Br=(new Cr).b());Dr||(Dr=(new Er).b());Fr||(Fr=(new Gr).b());return this};dr.prototype.$classData=l({iy:0},!1,"scala.reflect.ClassManifestFactory$",{iy:1,c:1});var er=void 0;function Hr(){}Hr.prototype=new n;
Hr.prototype.constructor=Hr;Hr.prototype.b=function(){return this};Hr.prototype.$classData=l({jy:0},!1,"scala.reflect.ManifestFactory$",{jy:1,c:1});var Ir=void 0;function Jr(){}Jr.prototype=new n;Jr.prototype.constructor=Jr;Jr.prototype.b=function(){Kr=this;er||(er=(new dr).b());Ir||(Ir=(new Hr).b());return this};Jr.prototype.$classData=l({zy:0},!1,"scala.reflect.package$",{zy:1,c:1});var Kr=void 0;function Lr(){}Lr.prototype=new n;Lr.prototype.constructor=Lr;Lr.prototype.b=function(){return this};
function fc(){Mr();var a=Nr(),b=Or();Rp();var c=cq();0===(4&c.Q)<<24>>24&&0===(4&c.Q)<<24>>24&&(c.gn=(new Pr).Wl((new Qr).b()),c.Q=(4|c.Q)<<24>>24);b=Rr(Sr(b,c.gn));return Yk(a,Tr(b))}Lr.prototype.$classData=l({Ay:0},!1,"scala.sys.package$",{Ay:1,c:1});var Ur=void 0;function Mr(){Ur||(Ur=(new Lr).b())}function Vr(){this.Og=null}Vr.prototype=new n;Vr.prototype.constructor=Vr;Vr.prototype.m=function(){return"DynamicVariable("+this.Og+")"};Vr.prototype.n=function(a){this.Og=a;return this};
Vr.prototype.$classData=l({By:0},!1,"scala.util.DynamicVariable",{By:1,c:1});function Wr(){}Wr.prototype=new n;Wr.prototype.constructor=Wr;Wr.prototype.b=function(){(new Xr).b();return this};Wr.prototype.$classData=l({Hy:0},!1,"scala.util.control.Breaks",{Hy:1,c:1});function Yr(){}Yr.prototype=new n;Yr.prototype.constructor=Yr;function Zr(){}Zr.prototype=Yr.prototype;function $r(a,b){b=ca(-862048943,b);b=ca(461845907,b<<15|b>>>17|0);return a^b}
function as(a,b){a=$r(a,b);return-430675100+ca(5,a<<13|a>>>19|0)|0}function bs(a){a=ca(-2048144789,a^(a>>>16|0));a=ca(-1028477387,a^(a>>>13|0));return a^(a>>>16|0)}function Lm(a,b){a=b.z();if(0===a)return b=b.B(),Ba(Ca(),b);for(var c=-889275714,e=0;e<a;)c=as(c,Bi(Ci(),b.A(e))),e=1+e|0;return bs(c^a)}
function cs(a,b,c){var e=(new ds).ob(0),f=(new ds).ob(0),h=(new ds).ob(0),m=(new ds).ob(1);b.M(t(new u,function(a,b,c,e,f){return function(a){a=Bi(Ci(),a);b.H=b.H+a|0;c.H^=a;0!==a&&(f.H=ca(f.H,a));e.H=1+e.H|0}}(a,e,f,h,m)));a=as(c,e.H);a=as(a,f.H);a=$r(a,m.H);return bs(a^h.H)}function es(a,b,c){var e=(new ds).ob(0);c=(new ds).ob(c);b.M(t(new u,function(a,b,c){return function(a){c.H=as(c.H,Bi(Ci(),a));b.H=1+b.H|0}}(a,e,c)));return bs(c.H^e.H)}function fs(){}fs.prototype=new n;
fs.prototype.constructor=fs;fs.prototype.b=function(){return this};function gs(a,b){a=ca(-1640532531,b);hs();return ca(-1640532531,a<<24|16711680&a<<8|65280&(a>>>8|0)|a>>>24|0)}fs.prototype.$classData=l({Ky:0},!1,"scala.util.hashing.package$",{Ky:1,c:1});var is=void 0;function js(){is||(is=(new fs).b());return is}function Gq(){}Gq.prototype=new n;Gq.prototype.constructor=Gq;Gq.prototype.b=function(){return this};Gq.prototype.$classData=l({My:0},!1,"scala.collection.$colon$plus$",{My:1,c:1});
var Fq=void 0;function Eq(){}Eq.prototype=new n;Eq.prototype.constructor=Eq;Eq.prototype.b=function(){return this};Eq.prototype.$classData=l({Ny:0},!1,"scala.collection.$plus$colon$",{Ny:1,c:1});var Dq=void 0;function ks(){this.Y=null}ks.prototype=new n;ks.prototype.constructor=ks;ks.prototype.b=function(){ls=this;this.Y=(new ms).b();return this};ks.prototype.$classData=l({Sy:0},!1,"scala.collection.Iterator$",{Sy:1,c:1});var ls=void 0;function Aq(){ls||(ls=(new ks).b());return ls}
function ns(){this.cb=this.nd=null}ns.prototype=new n;ns.prototype.constructor=ns;ns.prototype.$classData=l({Yy:0},!1,"scala.collection.Iterator$ConcatIteratorCell",{Yy:1,c:1});function Xl(a,b){b=b.Xg();b.db(a.Ba());return b.Ja()}function Oj(a,b,c,e){return a.vf((new os).b(),b,c,e).ra.Da}function ps(a,b,c){b=(new Kf).n(b);a.M(t(new u,function(a,b,c){return function(a){c.H=wg(b,c.H,a)}}(a,c,b)));return b.H}
function qs(a){var b=(new ds).ob(0);a.M(t(new u,function(a,b){return function(){b.H=1+b.H|0}}(a,b)));return b.H}function rs(a,b,c,e,f){var h=ss(!0);yn(b,c);a.M(t(new u,function(a,b,c,e){return function(a){if(e.H)at(b,a),e.H=!1;else return yn(b,c),at(b,a)}}(a,b,e,h)));yn(b,f);return b}function bt(a,b){return(new ct).oa(v(new w,function(a,b){return function(){return null===b?null:b&&b.$classData&&b.$classData.p.RD&&b.TD()===dt()?b.jE():et(new ft,dt(),b)}}(a,b)))}
function gt(a,b){return(new ct).oa(v(new w,function(a,b){return function(){return null===b?null:b&&b.$classData&&b.$classData.p.kz&&b.oz()===dt()?b.FB():ht(new it,dt(),b)}}(a,b)))}function Sr(a,b){return(new ct).oa(v(new w,function(a,b){return function(){return null===b?null:b&&b.$classData&&b.$classData.p.QD&&b.SD()===dt()?b.iE():jt(new kt,dt(),b)}}(a,b)))}function ct(){this.Xp=null}ct.prototype=new n;ct.prototype.constructor=ct;ct.prototype.oa=function(a){this.Xp=a;return this};
function Rr(a){return a.Xp.V()}ct.prototype.$classData=l({hz:0},!1,"scala.collection.convert.Decorators$AsScala",{hz:1,c:1});function lt(){}lt.prototype=new n;lt.prototype.constructor=lt;function mt(){}mt.prototype=lt.prototype;function Yk(a,b){return a.ba().db(b).Ja()}lt.prototype.ba=function(){return Al(new Bl,this.Vo())};function nt(){}nt.prototype=new n;nt.prototype.constructor=nt;function ot(){}ot.prototype=nt.prototype;function M(a,b){if(b.l())return a.ae();a=a.ba();a.db(b);return a.Ja()}
nt.prototype.ae=function(){return this.ba().Ja()};function pt(a,b){a:for(;;){if(!b.l()){a.Ob(b.I());b=b.w();continue a}break}}function jm(a,b){b&&b.$classData&&b.$classData.p.$i?pt(a,b):b.M(t(new u,function(a){return function(b){return a.Ob(b)}}(a)));return a}function qt(){}qt.prototype=new n;qt.prototype.constructor=qt;function rt(){}rt.prototype=qt.prototype;function Jq(){}Jq.prototype=new n;Jq.prototype.constructor=Jq;Jq.prototype.b=function(){return this};
Jq.prototype.$classData=l({mA:0},!1,"scala.collection.immutable.Stream$$hash$colon$colon$",{mA:1,c:1});var Iq=void 0;function st(){this.Pm=this.Og=null;this.Q=!1;this.wa=null}st.prototype=new n;st.prototype.constructor=st;function tt(a,b,c){a.Pm=c;if(null===b)throw Dp(ni(),null);a.wa=b;return a}function ut(a){a.Q||(a.Q||(a.Og=a.Pm.V(),a.Q=!0),a.Pm=null);return a.Og}st.prototype.$classData=l({qA:0},!1,"scala.collection.immutable.StreamIterator$LazyCell",{qA:1,c:1});function vt(){}vt.prototype=new n;
vt.prototype.constructor=vt;vt.prototype.b=function(){return this};function wt(a,b,c,e){a=0>c?0:c;return e<=a||a>=(b.length|0)?"":b.substring(a,e>(b.length|0)?b.length|0:e)}vt.prototype.$classData=l({rA:0},!1,"scala.collection.immutable.StringOps$",{rA:1,c:1});var xt=void 0;function yt(){xt||(xt=(new vt).b());return xt}function zt(){}zt.prototype=new n;zt.prototype.constructor=zt;zt.prototype.b=function(){return this};zt.prototype.ba=function(){var a=(new os).b();return At(new Bt,a,t(new u,function(){return function(a){return(new Ct).i(a)}}(this)))};
zt.prototype.$classData=l({zA:0},!1,"scala.collection.immutable.WrappedString$",{zA:1,c:1});var Dt=void 0;function Et(a){return Ft(hs(),-1+a.S.e.length|0)}function Gt(a,b){b=Vb(b);return Ht(a,b)}
function Ht(a,b){for(var c=Aa(b),c=It(a,c),e=a.S.e[c];null!==e;){if(D(G(),e,b))return!1;c=(1+c|0)%a.S.e.length|0;e=a.S.e[c]}a.S.e[c]=b;a.Kb=1+a.Kb|0;null!==a.wb&&(b=c>>5,c=a.wb,c.e[b]=1+c.e[b]|0);if(a.Kb>=a.pe)for(b=a.S,a.S=k(r(p),[a.S.e.length<<1]),a.Kb=0,null!==a.wb&&(c=1+(a.S.e.length>>5)|0,a.wb.e.length!==c?a.wb=k(r(Za),[c]):Yp($p(),a.wb)),a.le=Et(a),a.pe=Jt().Ni(a.re,a.S.e.length),c=0;c<b.e.length;)e=b.e[c],null!==e&&Ht(a,e),c=1+c|0;return!0}
function It(a,b){var c=a.le;b=gs(js(),b);a=-1+a.S.e.length|0;return((b>>>c|0|b<<(-c|0))>>>(32-Ft(hs(),a)|0)|0)&a}
function Kt(a,b){b=Vb(b);for(var c=Aa(b),c=It(a,c),e=a.S.e[c];null!==e;){if(D(G(),e,b)){b=c;for(c=(1+b|0)%a.S.e.length|0;null!==a.S.e[c];){var e=Aa(a.S.e[c]),e=It(a,e),f;if(f=e!==c)f=a.S.e.length>>1,f=e<=b?(b-e|0)<f:(e-b|0)>f;f&&(a.S.e[b]=a.S.e[c],b=c);c=(1+c|0)%a.S.e.length|0}a.S.e[b]=null;a.Kb=-1+a.Kb|0;null!==a.wb&&(a=a.wb,b>>=5,a.e[b]=-1+a.e[b]|0);return!0}c=(1+c|0)%a.S.e.length|0;e=a.S.e[c]}return!1}
function Lt(a,b){b=Vb(b);for(var c=Aa(b),c=It(a,c),e=a.S.e[c];null!==e&&!D(G(),e,b);)c=(1+c|0)%a.S.e.length|0,e=a.S.e[c];return e}function Mt(){}Mt.prototype=new n;Mt.prototype.constructor=Mt;Mt.prototype.b=function(){return this};
Mt.prototype.Ni=function(a,b){if(!(500>a))throw(new Nt).n("assertion failed: loadFactor too large; must be \x3c 0.5");var c=b>>31,e=a>>31,f=65535&b,h=b>>>16|0,m=65535&a,q=a>>>16|0,y=ca(f,m),m=ca(h,m),K=ca(f,q),f=y+((m+K|0)<<16)|0,y=(y>>>16|0)+K|0;a=(((ca(b,e)+ca(c,a)|0)+ca(h,q)|0)+(y>>>16|0)|0)+(((65535&y)+m|0)>>>16|0)|0;return Ot(Qa(),f,a)};Mt.prototype.$classData=l({GA:0},!1,"scala.collection.mutable.FlatHashTable$",{GA:1,c:1});var Pt=void 0;function Jt(){Pt||(Pt=(new Mt).b());return Pt}
function Qt(){}Qt.prototype=new n;Qt.prototype.constructor=Qt;Qt.prototype.b=function(){return this};Qt.prototype.m=function(){return"NullSentinel"};Qt.prototype.s=function(){return 0};Qt.prototype.$classData=l({IA:0},!1,"scala.collection.mutable.FlatHashTable$NullSentinel$",{IA:1,c:1});var Rt=void 0;function Wb(){Rt||(Rt=(new Qt).b());return Rt}function St(a){return Ft(hs(),-1+a.S.e.length|0)}
function Tt(a,b,c){for(a=a.S.e[c];;)if(null!==a?(c=a.pd(),c=!D(G(),c,b)):c=!1,c)a=a.r();else break;return a}function Ut(a,b){var c=-1+a.S.e.length|0,e=da(c);a=a.le;b=gs(js(),b);return((b>>>a|0|b<<(-a|0))>>>e|0)&c}
function Vt(a,b){var c=Bi(Ci(),b),c=Ut(a,c),e=a.S.e[c];if(null!==e){var f=e.pd();if(D(G(),f,b))return a.S.e[c]=e.r(),a.Dh(-1+a.Kb|0),Wt(a,c),e.qh(null),e;for(f=e.r();;){if(null!==f)var h=f.pd(),h=!D(G(),h,b);else h=!1;if(h)e=f,f=f.r();else break}if(null!==f)return e.qh(f.r()),a.Dh(-1+a.Kb|0),Wt(a,c),f.qh(null),f}return null}function Xt(a){a.$k(750);Yt();a.Mk(k(r(Yb),[Zt(0,16)]));a.Dh(0);var b=a.re,c=Yt();Yt();a.Nk(c.Ni(b,Zt(0,16)));a.Kk(null);a.Mm(St(a))}
function $t(a){for(var b=-1+a.S.e.length|0;null===a.S.e[b]&&0<b;)b=-1+b|0;return b}
function au(a,b,c){var e=Bi(Ci(),b),e=Ut(a,e),f=Tt(a,b,e);if(null!==f)return f;b=a.vo(b,c);b.qh(a.S.e[e]);a.S.e[e]=b;a.Dh(1+a.Kb|0);bu(a,e);if(a.Kb>a.pe){b=a.S.e.length<<1;c=a.S;a.Mk(k(r(Yb),[b]));null!==a.wb&&(e=1+(a.S.e.length>>5)|0,a.wb.e.length!==e?a.Kk(k(r(Za),[e])):Yp($p(),a.wb));for(e=-1+c.e.length|0;0<=e;){for(f=c.e[e];null!==f;){var h=f.pd(),h=Bi(Ci(),h),h=Ut(a,h),m=f.r();f.qh(a.S.e[h]);a.S.e[h]=f;f=m;bu(a,h)}e=-1+e|0}a.Nk(Yt().Ni(a.re,b))}return null}
function cu(a,b){var c=Bi(Ci(),b),c=Ut(a,c);return Tt(a,b,c)}function Wt(a,b){null!==a.wb&&(a=a.wb,b>>=5,a.e[b]=-1+a.e[b]|0)}function bu(a,b){null!==a.wb&&(a=a.wb,b>>=5,a.e[b]=1+a.e[b]|0)}function du(){}du.prototype=new n;du.prototype.constructor=du;du.prototype.b=function(){return this};function Zt(a,b){return 1<<(-da(-1+b|0)|0)}
du.prototype.Ni=function(a,b){var c=b>>31,e=a>>31,f=65535&b,h=b>>>16|0,m=65535&a,q=a>>>16|0,y=ca(f,m),m=ca(h,m),K=ca(f,q),f=y+((m+K|0)<<16)|0,y=(y>>>16|0)+K|0;a=(((ca(b,e)+ca(c,a)|0)+ca(h,q)|0)+(y>>>16|0)|0)+(((65535&y)+m|0)>>>16|0)|0;return Ot(Qa(),f,a)};du.prototype.$classData=l({RA:0},!1,"scala.collection.mutable.HashTable$",{RA:1,c:1});var eu=void 0;function Yt(){eu||(eu=(new du).b());return eu}function fu(){this.Kg=!1;this.cp=this.ek=this.Sh=null;this.ul=!1;this.Kp=this.jp=0}fu.prototype=new n;
fu.prototype.constructor=fu;fu.prototype.b=function(){gu=this;this.Sh=(this.Kg=!!(g.ArrayBuffer&&g.Int32Array&&g.Float32Array&&g.Float64Array))?new g.ArrayBuffer(8):null;this.ek=this.Kg?new g.Int32Array(this.Sh,0,2):null;this.Kg&&new g.Float32Array(this.Sh,0,2);this.cp=this.Kg?new g.Float64Array(this.Sh,0,1):null;if(this.Kg)this.ek[0]=16909060,a=1===((new g.Int8Array(this.Sh,0,8))[0]|0);else var a=!0;this.jp=(this.ul=a)?0:1;this.Kp=this.ul?1:0;return this};
function Da(a,b){var c=b|0;if(c===b&&-Infinity!==1/b)return c;if(a.Kg)a.cp[0]=b,a=(new hu).df(a.ek[a.Kp]|0,a.ek[a.jp]|0);else{if(b!==b)a=!1,b=2047,c=+g.Math.pow(2,51);else if(Infinity===b||-Infinity===b)a=0>b,b=2047,c=0;else if(0===b)a=-Infinity===1/b,c=b=0;else{var e=(a=0>b)?-b:b;if(e>=+g.Math.pow(2,-1022)){b=+g.Math.pow(2,52);var c=+g.Math.log(e)/.6931471805599453,c=+g.Math.floor(c)|0,c=1023>c?c:1023,f=+g.Math.pow(2,c);f>e&&(c=-1+c|0,f/=2);f=e/f*b;e=+g.Math.floor(f);f-=e;e=.5>f?e:.5<f?1+e:0!==e%
2?1+e:e;2<=e/b&&(c=1+c|0,e=1);1023<c?(c=2047,e=0):(c=1023+c|0,e-=b);b=c;c=e}else b=e/+g.Math.pow(2,-1074),c=+g.Math.floor(b),e=b-c,b=0,c=.5>e?c:.5<e?1+c:0!==c%2?1+c:c}c=+c;a=(new hu).df(c|0,(a?-2147483648:0)|(b|0)<<20|c/4294967296|0)}return a.jb^a.qc}fu.prototype.$classData=l({pB:0},!1,"scala.scalajs.runtime.Bits$",{pB:1,c:1});var gu=void 0;function Ea(){gu||(gu=(new fu).b());return gu}function Dd(){}Dd.prototype=new n;Dd.prototype.constructor=Dd;Dd.prototype.b=function(){return this};
Dd.prototype.$classData=l({qB:0},!1,"scala.scalajs.runtime.Compat$",{qB:1,c:1});var Cd=void 0;function iu(){this.Q=!1}iu.prototype=new n;iu.prototype.constructor=iu;iu.prototype.b=function(){return this};function Wg(a,b,c,e){if(null===b)throw(new Ic).b();a=Kc(Jc(),c);b=Mc(new Pc,a,b,b.length|0);ju(b);ku(b)?(a=(new lu).b(),mu(b,a,e),nu(b,a),e=a.m()):e=b.Cg;return e}function ou(){return g.String.fromCharCode(92)}
function Ba(a,b){a=0;for(var c=1,e=-1+(b.length|0)|0;0<=e;)a=a+ca(65535&(b.charCodeAt(e)|0),c)|0,c=ca(31,c),e=-1+e|0;return a}
function op(a,b){var c=(new pu).b();if(c.vi)throw(new qu).b();for(var e=0,f=0,h=a.length|0,m=0;m!==h;){var q=a.indexOf("%",m)|0;if(0>q){ru(c,a.substring(m));break}ru(c,a.substring(m,q));m=1+q|0;eq||(eq=(new dq).b());var y=eq.yp;y.lastIndex=m;q=y.exec(a);if(null===q||(q.index|0)!==m)throw c=m===h?"%":a.substring(m,1+m|0),(new su).i(c);for(var m=y.lastIndex|0,y=65535&(a.charCodeAt(-1+m|0)|0),K,z=q[2],E=90>=y?256:0,C=z.length|0,F=0;F!==C;){K=65535&(z.charCodeAt(F)|0);switch(K){case 45:var W=1;break;
case 35:W=2;break;case 43:W=4;break;case 32:W=8;break;case 48:W=16;break;case 44:W=32;break;case 40:W=64;break;case 60:W=128;break;default:throw(new x).n(tu(K));}if(0!==(E&W))throw(new uu).i(g.String.fromCharCode(K));E|=W;F=1+F|0}K=E;C=vu(q[3],-1);E=vu(q[4],-1);if(37===y||110===y)q=null;else{if(0!==(1&K)&&0>C)throw(new wu).i("%"+q[0]);0!==(128&K)?z=f:(z=vu(q[1],0),z=0===z?e=1+e|0:0>z?f:z);if(0>=z||z>b.e.length){c=g.String.fromCharCode(y);if(0>("bBhHsHcCdoxXeEgGfn%".indexOf(c)|0))throw(new su).i(c);
throw(new xu).i("%"+q[0]);}f=z;q=b.e[-1+z|0]}z=c;F=q;W=y;q=K;y=C;C=E;switch(W){case 98:case 66:0!==(126&q)&&yu(q,W,126);zu(z,q,y,C,!1===F||null===F?"false":"true");break;case 104:case 72:0!==(126&q)&&yu(q,W,126);E=null===F?"null":(+(Aa(F)>>>0)).toString(16);zu(z,q,y,C,E);break;case 115:case 83:F&&F.$classData&&F.$classData.p.hD?(0!==(124&q)&&yu(q,W,124),F.YC(z,(0!==(1&q)?1:0)|(0!==(2&q)?4:0)|(0!==(256&q)?2:0),y,C)):(0!==(126&q)&&yu(q,W,126),zu(z,q,y,C,""+F));break;case 99:case 67:0!==(126&q)&&yu(q,
W,126);if(0<=C)throw(new Au).ob(C);if(Bu(F))zu(z,q,y,-1,g.String.fromCharCode(null===F?0:F.t));else if(Oa(F)){E=F|0;if(!(0<=E&&1114111>=E))throw(new Cu).ob(E);E=65536>E?g.String.fromCharCode(E):g.String.fromCharCode(-64+(E>>10)|55296,56320|1023&E);zu(z,q,y,-1,E)}else Du(z,F,W,q,y,C);break;case 100:0!==(2&q)&&yu(q,W,2);17!==(17&q)&&12!==(12&q)||Eu(q);if(0<=C)throw(new Au).ob(C);Oa(F)?Fu(z,q,y,""+(F|0)):xa(F)?(C=Pa(F),E=C.jb,C=C.qc,Fu(z,q,y,Gb(Qa(),E,C))):Du(z,F,W,q,y,C);break;case 111:0!==(108&q)&&
yu(q,W,108);17===(17&q)&&Eu(q);if(0<=C)throw(new Au).ob(C);E=0!==(2&q)?"0":"";Oa(F)?(C=(+((F|0)>>>0)).toString(8),Gu(z,q,y,E,C)):xa(F)?(C=Pa(F),F=C.jb,K=C.qc,Hu||(Hu=(new Iu).b()),C=1073741823&F,W=1073741823&((F>>>30|0)+(K<<2)|0),F=K>>>28|0,0!==F?(F=(+(F>>>0)).toString(8),K=(+(W>>>0)).toString(8),W="0000000000".substring(K.length|0),C=(+(C>>>0)).toString(8),C=F+(""+W+K)+(""+"0000000000".substring(C.length|0)+C)):0!==W?(F=(+(W>>>0)).toString(8),C=(+(C>>>0)).toString(8),C=F+(""+"0000000000".substring(C.length|
0)+C)):C=(+(C>>>0)).toString(8),Gu(z,q,y,E,C)):Du(z,F,W,q,y,C);break;case 120:case 88:0!==(108&q)&&yu(q,W,108);17===(17&q)&&Eu(q);if(0<=C)throw(new Au).ob(C);E=0===(2&q)?"":0!==(256&q)?"0X":"0x";Oa(F)?(C=(+((F|0)>>>0)).toString(16),Gu(z,q,y,E,Ju(q,C))):xa(F)?(C=Pa(F),F=C.jb,K=C.qc,Hu||(Hu=(new Iu).b()),C=q,0!==K?(K=(+(K>>>0)).toString(16),F=(+(F>>>0)).toString(16),F=K+(""+"00000000".substring(F.length|0)+F)):F=(+(F>>>0)).toString(16),Gu(z,C,y,E,Ju(q,F))):Du(z,F,W,q,y,C);break;case 101:case 69:0!==
(32&q)&&yu(q,W,32);17!==(17&q)&&12!==(12&q)||Eu(q);"number"===typeof F?(E=+F,E!==E||Infinity===E||-Infinity===E?Ku(z,q,y,E):Fu(z,q,y,Lu(E,0<=C?C:6,0!==(2&q)))):Du(z,F,W,q,y,C);break;case 103:case 71:0!==(2&q)&&yu(q,W,2);17!==(17&q)&&12!==(12&q)||Eu(q);"number"===typeof F?(F=+F,F!==F||Infinity===F||-Infinity===F?Ku(z,q,y,F):(E=q,K=0<=C?C:6,q=0!==(2&q),C=+g.Math.abs(F),K=0===K?1:K,1E-4<=C&&C<+g.Math.pow(10,K)?(W=void 0!==g.Math.log10?+g.Math.log10(C):+g.Math.log(C)/2.302585092994046,W=Ia(+g.Math.ceil(W)),
C=+g.Math.pow(10,W)<=C?1+W|0:W,C=K-C|0,q=Mu(F,0<C?C:0,q)):q=Lu(F,-1+K|0,q),Fu(z,E,y,q))):Du(z,F,W,q,y,C);break;case 102:17!==(17&q)&&12!==(12&q)||Eu(q);"number"===typeof F?(E=+F,E!==E||Infinity===E||-Infinity===E?Ku(z,q,y,E):Fu(z,q,y,Mu(E,0<=C?C:6,0!==(2&q)))):Du(z,F,W,q,y,C);break;case 37:if(0!==(254&q))throw(new Nu).i(Ou(q));if(0<=C)throw(new Au).ob(C);if(0!==(1&q)&&0>y)throw(new wu).i("%-%");Pu(z,q,y,"%");break;case 110:if(0!==(255&q))throw(new Nu).i(Ou(q));if(0<=C)throw(new Au).ob(C);if(0<=y)throw(new Qu).ob(y);
ru(z,"\n");break;default:throw(new su).i(g.String.fromCharCode(W));}}a=c.m();c.Mj();return a}function Ap(a,b,c,e){if(null===b)throw(new Ic).b();a=Kc(Jc(),c);return Ch(Mc(new Pc,a,b,b.length|0),e)}iu.prototype.$classData=l({sB:0},!1,"scala.scalajs.runtime.RuntimeString$",{sB:1,c:1});var Ru=void 0;function Ca(){Ru||(Ru=(new iu).b());return Ru}function Su(){}Su.prototype=new n;Su.prototype.constructor=Su;Su.prototype.b=function(){return this};function Dp(a,b){return Tu(b)?b.Ff:b}
function mi(a,b){return b&&b.$classData&&b.$classData.p.ab?b:(new Uu).n(b)}Su.prototype.$classData=l({tB:0},!1,"scala.scalajs.runtime.package$",{tB:1,c:1});var Vu=void 0;function ni(){Vu||(Vu=(new Su).b());return Vu}function Wu(){}Wu.prototype=new n;Wu.prototype.constructor=Wu;Wu.prototype.b=function(){return this};
function Xu(a,b){if(Bu(b))return a.t===b.t;if(Yu(b)){if("number"===typeof b)return+b===a.t;if(xa(b)){b=Pa(b);var c=b.qc;a=a.t;return b.jb===a&&c===a>>31}return null===b?null===a:za(b,a)}return null===a&&null===b}function D(a,b,c){if(b===c)c=!0;else if(Yu(b))a:if(Yu(c))c=Zu(b,c);else{if(Bu(c)){if("number"===typeof b){c=+b===c.t;break a}if(xa(b)){a=Pa(b);b=a.qc;c=c.t;c=a.jb===c&&b===c>>31;break a}}c=null===b?null===c:za(b,c)}else c=Bu(b)?Xu(b,c):null===b?null===c:za(b,c);return c}
function Zu(a,b){if("number"===typeof a){a=+a;if("number"===typeof b)return a===+b;if(xa(b)){var c=Pa(b);b=c.jb;c=c.qc;return a===$u(Qa(),b,c)}return b&&b.$classData&&b.$classData.p.fy?b.o(a):!1}if(xa(a)){c=Pa(a);a=c.jb;c=c.qc;if(xa(b)){b=Pa(b);var e=b.qc;return a===b.jb&&c===e}return"number"===typeof b?(b=+b,$u(Qa(),a,c)===b):b&&b.$classData&&b.$classData.p.fy?b.o((new hu).df(a,c)):!1}return null===a?null===b:za(a,b)}Wu.prototype.$classData=l({xB:0},!1,"scala.runtime.BoxesRunTime$",{xB:1,c:1});
var av=void 0;function G(){av||(av=(new Wu).b());return av}var bv=l({AB:0},!1,"scala.runtime.Null$",{AB:1,c:1});function cv(){}cv.prototype=new n;cv.prototype.constructor=cv;cv.prototype.b=function(){return this};function dv(a,b){if(Jb(b,1)||gb(b,1)||jb(b,1)||hb(b,1)||ib(b,1)||db(b,1)||eb(b,1)||fb(b,1)||cb(b,1)||ev(b))return b.e.length;if(null===b)throw(new Ic).b();throw(new x).n(b);}
function fv(a,b,c,e){if(Jb(b,1))b.e[c]=e;else if(gb(b,1))b.e[c]=e|0;else if(jb(b,1))b.e[c]=+e;else if(hb(b,1))b.e[c]=Pa(e);else if(ib(b,1))b.e[c]=+e;else if(db(b,1))b.e[c]=null===e?0:e.t;else if(eb(b,1))b.e[c]=e|0;else if(fb(b,1))b.e[c]=e|0;else if(cb(b,1))b.e[c]=!!e;else if(ev(b))b.e[c]=void 0;else{if(null===b)throw(new Ic).b();throw(new x).n(b);}}function ql(a,b){a=b.D();return Oj(a,b.B()+"(",",",")")}cv.prototype.$classData=l({CB:0},!1,"scala.runtime.ScalaRunTime$",{CB:1,c:1});var gv=void 0;
function rl(){gv||(gv=(new cv).b());return gv}function hv(){}hv.prototype=new n;hv.prototype.constructor=hv;hv.prototype.b=function(){return this};
function Bi(a,b){if(null===b)return 0;if("number"===typeof b){a=+b;b=Ia(a);if(b===a)a=b;else{b=Qa();var c;if(-9223372036854775808>a)b.Fe=-2147483648,c=0;else if(0x7fffffffffffffff<=a)b.Fe=2147483647,c=-1;else{c=a|0;var e=a/4294967296|0;b.Fe=0>a&&0!==c?-1+e|0:e}b=b.Fe;a=$u(Qa(),c,b)===a?c^b:Da(Ea(),a)}return a}return xa(b)?(a=Pa(b),b=(new hu).df(a.jb,a.qc),a=b.jb,b=b.qc,b===a>>31?a:a^b):Aa(b)}hv.prototype.$classData=l({EB:0},!1,"scala.runtime.Statics$",{EB:1,c:1});var iv=void 0;
function Ci(){iv||(iv=(new hv).b());return iv}function te(){this.lk=this.Pj=this.Qk=null}te.prototype=new me;te.prototype.constructor=te;function se(a,b,c,e){a.Qk=b;a.Pj=c;a.lk=e;return a}te.prototype.V=function(){var a=this.lk,b=this.Pj;return void 0===a?this.Qk:b.f(a)};te.prototype.$classData=l({Cr:0},!1,"japgolly.scalajs.react.CtorType$Nullary",{Cr:1,PB:1,c:1});function xe(){}xe.prototype=new n;xe.prototype.constructor=xe;xe.prototype.b=function(){return this};
function jv(a,b){var c=b.f(a.Qk);return se(new te,c,mb(b,a.Pj),a.lk)}xe.prototype.$classData=l({Dr:0},!1,"japgolly.scalajs.react.CtorType$ProfunctorN$",{Dr:1,c:1,WB:1});var we=void 0;function kv(){this.Ig=this.gr=null}kv.prototype=new n;kv.prototype.constructor=kv;function ye(a){var b=we,c=new kv;c.gr=a;c.Ig=b;return c}kv.prototype.$classData=l({Fr:0},!1,"japgolly.scalajs.react.CtorType$Summoner$$anon$1",{Fr:1,c:1,QB:1});function lv(){}lv.prototype=new n;lv.prototype.constructor=lv;
lv.prototype.b=function(){mv=this;return this};function Oe(a,b,c){var e=c.gr.f(b),e=jv(e,t(new u,function(){return function(a){return nv(Ne(),a)}}(a)));return ov(a,b,e,c.Ig)}function nv(a,b){return pv(new qv,b,t(new u,function(){return function(a){Ne();return(new kg).Ya(a)}}(a)))}lv.prototype.$classData=l({Nr:0},!1,"japgolly.scalajs.react.component.Js$",{Nr:1,c:1,Wr:1});var mv=void 0;function Ne(){mv||(mv=(new lv).b());return mv}function rv(){}rv.prototype=new n;rv.prototype.constructor=rv;
rv.prototype.b=function(){sv=this;return this};rv.prototype.$classData=l({as:0},!1,"japgolly.scalajs.react.component.JsFn$",{as:1,c:1,Wr:1});var sv=void 0;function xf(){this.L=null}xf.prototype=new n;xf.prototype.constructor=xf;d=xf.prototype;d.o=function(a){return wf().qg(this.L,a)};d.m=function(){return wf().Mg(this.L)};d.Ya=function(a){this.L=a;return this};d.s=function(){return Aa(this.L)};d.Hb=function(){return this.L};
d.$classData=l({on:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillUnmount",{on:1,c:1,uf:1});function tg(){this.Oi=this.Nf=this.L=null}tg.prototype=new n;tg.prototype.constructor=tg;tg.prototype.m=function(){return pf(Fe(),"ComponentWillUpdate(props: "+ob(this).Nd()+" \u2192 "+this.Nf+", state: "+ob(this).Sc()+" \u2192 "+this.Oi+")")};tg.prototype.oi=function(a,b,c){this.L=a;this.Nf=b;this.Oi=c;return this};tg.prototype.Hb=function(){return this.L};
tg.prototype.$classData=l({ws:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillUpdate",{ws:1,c:1,uf:1});function ug(){this.Oi=this.Nf=this.L=null}ug.prototype=new n;ug.prototype.constructor=ug;ug.prototype.m=function(){return pf(Fe(),"ShouldComponentUpdate(props: "+ob(this).Nd()+" \u2192 "+this.Nf+", state: "+ob(this).Sc()+" \u2192 "+this.Oi+")")};ug.prototype.oi=function(a,b,c){this.L=a;this.Nf=b;this.Oi=c;return this};ug.prototype.Hb=function(){return this.L};
ug.prototype.$classData=l({ys:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ShouldComponentUpdate",{ys:1,c:1,uf:1});function eh(){this.ui=null}eh.prototype=new n;eh.prototype.constructor=eh;eh.prototype.b=function(){this.ui=xb();return this};eh.prototype.$classData=l({Es:0},!1,"japgolly.scalajs.react.extra.OnUnmount$Backend",{Es:1,c:1,UB:1});function tv(){this.sa=this.lq=null}tv.prototype=new wh;tv.prototype.constructor=tv;
function uv(a){var b=new tv;if(null===a)throw Dp(ni(),null);b.sa=a;b.lq=vv(a,wv());return b}tv.prototype.zk=function(){return this.lq};tv.prototype.$classData=l({Us:0},!1,"japgolly.scalajs.react.extra.router.RouterLogic$$anon$1",{Us:1,Ss:1,c:1});function xv(){this.xl=this.vm=this.sk=null}xv.prototype=new Ih;xv.prototype.constructor=xv;xv.prototype.m=function(){return"Route("+this.sk+")"};function yv(a,b,c,e){a.sk=b;a.vm=c;a.xl=e;return a}
function zv(a,b,c){return yv(new xv,a.sk,t(new u,function(a,b){return function(c){c=a.vm.f(c);return c.l()?B():b.f(c.P())}}(a,b)),mb(a.xl,c))}xv.prototype.dq=function(a,b){return zv(this,a,b)};function ih(a,b){b=b.J;b=Mc(new Pc,a.sk,b,b.length|0);return Lc(b)?a.vm.f(b):B()}xv.prototype.$classData=l({Ws:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Route",{Ws:1,Zs:1,c:1});function Fh(){this.Ak=null;this.Rp=0;this.wl=this.um=null}Fh.prototype=new Ih;Fh.prototype.constructor=Fh;
Fh.prototype.m=function(){return"RouteB("+this.Ak+")"};function Av(a,b,c){return Gh(new Fh,a.Ak,a.Rp,t(new u,function(a,b){return function(c){c=a.um.f(c);return c.l()?B():b.f(c.P())}}(a,b)),mb(a.wl,c))}Fh.prototype.dq=function(a,b){return Av(this,a,b)};
function kc(a){var b=Kc(Jc(),"^"+a.Ak+"$");return yv(new xv,b,t(new u,function(a){return function(b){return a.um.f(t(new u,function(a,b){return function(a){return Bv(b,1+(a|0)|0)}}(a,b)))}}(a)),t(new u,function(a){return function(b){return(new sc).i(a.wl.f(b))}}(a)))}function Gh(a,b,c,e,f){a.Ak=b;a.Rp=c;a.um=e;a.wl=f;return a}Fh.prototype.$classData=l({Xs:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$RouteB",{Xs:1,Zs:1,c:1});function Xh(){}Xh.prototype=new Uh;Xh.prototype.constructor=Xh;
Xh.prototype.b=function(){return this};Xh.prototype.ap=function(a){return a};Xh.prototype.eq=function(a){return a.V()};Xh.prototype.$classData=l({ft:0},!1,"japgolly.scalajs.react.internal.Effect$$anon$1",{ft:1,dt:1,c:1});function Yh(){}Yh.prototype=new Uh;Yh.prototype.constructor=Yh;Yh.prototype.b=function(){return this};Yh.prototype.ap=function(a){return a.V().pa};Yh.prototype.eq=function(a){return(new Bb).oa(a)};
Yh.prototype.$classData=l({gt:0},!1,"japgolly.scalajs.react.internal.Effect$$anon$2",{gt:1,dt:1,c:1});function Cv(){$h.call(this)}Cv.prototype=new ai;Cv.prototype.constructor=Cv;Cv.prototype.Ph=function(a){return a.V()};function di(a){var b=new Cv;$h.prototype.ck.call(b,a,a);return b}Cv.prototype.$classData=l({it:0},!1,"japgolly.scalajs.react.internal.Effect$Trans$Id",{it:1,Bn:1,c:1});function Dv(){}Dv.prototype=new n;Dv.prototype.constructor=Dv;
Dv.prototype.b=function(){Ev=this;Ae||(Ae=(new ze).b());Ne();sv||(sv=(new rv).b());Ge();Ie||(Ie=(new He).b());return this};Dv.prototype.$classData=l({rt:0},!1,"japgolly.scalajs.react.package$",{rt:1,c:1,RB:1});var Ev=void 0;function md(){Ev||(Ev=(new Dv).b())}function Cj(){this.qa=null}Cj.prototype=new Ai;Cj.prototype.constructor=Cj;Cj.prototype.b=function(){zi.prototype.i.call(this,"class");return this};
function Aj(a,b){var c=rc().lr;bj||(bj=(new cj).b());return(new Fv).Mb(t(new u,function(a,b,c){return function(a){wg(c,a.jo,b)}}(a,b,c)))}Cj.prototype.$classData=l({tt:0},!1,"japgolly.scalajs.react.vdom.Attr$ClassName$",{tt:1,Aj:1,c:1});var Bj=void 0;function Gv(){this.qa=null}Gv.prototype=new Ai;Gv.prototype.constructor=Gv;function Hv(){}Hv.prototype=Gv.prototype;Gv.prototype.i=function(a){zi.prototype.i.call(this,a);return this};
Gv.prototype.$classData=l({Cn:0},!1,"japgolly.scalajs.react.vdom.Attr$Generic",{Cn:1,Aj:1,c:1});function Iv(){this.qa=null}Iv.prototype=new Ai;Iv.prototype.constructor=Iv;Iv.prototype.b=function(){zi.prototype.i.call(this,"key");return this};Iv.prototype.$classData=l({vt:0},!1,"japgolly.scalajs.react.vdom.Attr$Key$",{vt:1,Aj:1,c:1});var Jv=void 0;function Kv(){}Kv.prototype=new n;Kv.prototype.constructor=Kv;Kv.prototype.b=function(){return this};
Kv.prototype.$classData=l({Dt:0},!1,"japgolly.scalajs.react.vdom.HtmlTags$",{Dt:1,c:1,bC:1});var Lv=void 0;function Fv(){this.fi=null}Fv.prototype=new n;Fv.prototype.constructor=Fv;Fv.prototype.Mb=function(a){this.fi=a;return this};Fv.prototype.Gj=function(a){this.fi.f(a)};Fv.prototype.$classData=l({St:0},!1,"japgolly.scalajs.react.vdom.TagMod$$anon$2",{St:1,c:1,Bj:1});function ij(){}ij.prototype=new n;ij.prototype.constructor=ij;ij.prototype.b=function(){return this};ij.prototype.Gj=function(){};
ij.prototype.$classData=l({Tt:0},!1,"japgolly.scalajs.react.vdom.TagMod$$anon$3",{Tt:1,c:1,Bj:1});function Mv(){this.nm=this.ir=this.Rf=null;this.Q=!1}Mv.prototype=new n;Mv.prototype.constructor=Mv;Mv.prototype.m=function(){return wd(this).m()};Mv.prototype.Gj=function(a){a=a.ql;var b=wd(this).kq;a.f(b)};
function wd(a){if(!a.Q&&!a.Q){for(var b=(new Nv).b(),c=a.nm,e=a.nm,e=k(r(Ov),[Pv(e)]),f=0;;){var h=c,m=xb();if(null!==h&&h.o(m))break;else e.e[f]=c.I(),c=c.w(),f=1+f|0}for(c=e.e.length;0<c;)for(c=-1+c|0,f=e.e[c],h=0;h<f.aa();)f.eb(h).Gj(b),h=1+h|0;e=a.ir;c=b.nk;void 0!==c&&(Ki(),b.Si.className=c);Ki();c=b.Qm;c=0===(g.Object.keys(c).length|0)?void 0:c;void 0!==c&&(Ki(),b.Si.style=c);Qi||(Qi=(new Pi).b());c=Qi.no;f=b.Si;h=b.xa;b=b.zl;b=(0,c.Rb)(e,f,h,b);b=Qv(b);a.Rf=b;a.Q=!0}return a.Rf}
Mv.prototype.$classData=l({Ut:0},!1,"japgolly.scalajs.react.vdom.TagOf",{Ut:1,c:1,Bj:1});function td(){this.yk=null}td.prototype=new n;td.prototype.constructor=td;function Rv(){}Rv.prototype=td.prototype;td.prototype.Gj=function(a){a.ql.f(this.yk)};td.prototype.Xl=function(a){this.yk=a;return this};td.prototype.$classData=l({Dn:0},!1,"japgolly.scalajs.react.vdom.VdomNode",{Dn:1,c:1,Bj:1});function nj(){}nj.prototype=new n;nj.prototype.constructor=nj;nj.prototype.b=function(){return this};
nj.prototype.$classData=l({hu:0},!1,"japgolly.univeq.UnivEq$$anon$8",{hu:1,c:1,eg:1});function Sv(){this.Aa=null}Sv.prototype=new n;Sv.prototype.constructor=Sv;Sv.prototype.Ij=function(a){Ej||(Ej=(new Dj).b());a=this.Aa.Ij(a);var b=xj().createElement("style");b.type="text/css";b.appendChild(xj().createTextNode(a));return b};function Tv(a){var b=new Sv;b.Aa=a;return b}Sv.prototype.$classData=l({qu:0},!1,"scalacss.defaults.PlatformExports$StyleElementRenderer",{qu:1,c:1,Cv:1});function Uv(){}
Uv.prototype=new Xn;Uv.prototype.constructor=Uv;function Vv(){}Vv.prototype=Uv.prototype;function Wv(){}Wv.prototype=new Lk;Wv.prototype.constructor=Wv;function Xv(){}Xv.prototype=Wv.prototype;Wv.prototype.so=function(){return!0};Wv.prototype.$m=function(){return this};function Yv(){this.Qb=null}Yv.prototype=new Zn;Yv.prototype.constructor=Yv;
Yv.prototype.b=function(){Zv=this;this.Qb=Hk(Bk(),"display",Hn(yl(zl(),al().Yj,"flex",(new A).g(["inline-flex"])),yl(zl(),al().xg,"grid",(new A).g(["inline-grid"]))));return this};Yv.prototype.ue=function(){return this.Qb};Yv.prototype.$classData=l({Eu:0},!1,"scalacss.internal.Attrs$display$",{Eu:1,Se:1,c:1});var Zv=void 0;function $v(){this.Qb=null}$v.prototype=new Zn;$v.prototype.constructor=$v;
$v.prototype.b=function(){aw=this;Bk();var a=zl(),b=al().Yj;this.Qb=Hk(0,"flex-direction",Jn(a,b,Nl().vk,Kn().xa));return this};$v.prototype.ue=function(){return this.Qb};$v.prototype.$classData=l({Fu:0},!1,"scalacss.internal.Attrs$flexDirection$",{Fu:1,Se:1,c:1});var aw=void 0;function bw(){this.Qb=null}bw.prototype=new Zn;bw.prototype.constructor=bw;bw.prototype.b=function(){cw=this;this.Qb=Ak("font-family");return this};bw.prototype.ue=function(){return this.Qb};
bw.prototype.$classData=l({Gu:0},!1,"scalacss.internal.Attrs$fontFamily$",{Gu:1,Se:1,c:1});var cw=void 0;function dw(){this.Qb=null}dw.prototype=new Zn;dw.prototype.constructor=dw;dw.prototype.b=function(){ew=this;Bk();var a=zl(),b=al().Yj;this.Qb=Hk(0,"justify-content",Jn(a,b,Nl().vk,Kn().xa));return this};dw.prototype.ue=function(){return this.Qb};dw.prototype.$classData=l({Ju:0},!1,"scalacss.internal.Attrs$justifyContent$",{Ju:1,Se:1,c:1});var ew=void 0;function fw(){this.Qb=null}
fw.prototype=new Zn;fw.prototype.constructor=fw;
fw.prototype.b=function(){gw=this;Bk();var a=yl,b=zl();al();var c=L().Ub,e=[N()];if(0===(e.length|0))var f=Q();else{for(var h=R(new S,Q()),m=0,q=e.length|0;m<q;)T(h,e[m]),m=1+m|0;f=h.y}var y=(new I).d(c,f),K=L().Vb,z=[N()];if(0===(z.length|0))var E=Q();else{for(var C=R(new S,Q()),F=0,W=z.length|0;F<W;)T(C,z[F]),F=1+F|0;E=C.y}var bc=(new I).d(K,E),Nc=L().Wb,wb=[N()];if(0===(wb.length|0))var Lb=Q();else{for(var yf=R(new S,Q()),Oc=0,Ed=wb.length|0;Oc<Ed;)T(yf,wb[Oc]),Oc=1+Oc|0;Lb=yf.y}var ao=(new I).d(Nc,
Lb),bo=L().Xb,zf=[N()];if(0===(zf.length|0))var Ld=Q();else{for(var Lf=R(new S,Q()),Qc=0,Md=zf.length|0;Qc<Md;)T(Lf,zf[Qc]),Qc=1+Qc|0;Ld=Lf.y}var no=(new I).d(bo,Ld),oo=L().mc,Mf=[N()];if(0===(Mf.length|0))var Nd=Q();else{for(var Nf=R(new S,Q()),Rc=0,Od=Mf.length|0;Rc<Od;)T(Nf,Mf[Rc]),Rc=1+Rc|0;Nd=Nf.y}var po=(new I).d(oo,Nd),qo=L().Yb,Of=[N()];if(0===(Of.length|0))var Pd=Q();else{for(var Pf=R(new S,Q()),Sc=0,Qd=Of.length|0;Sc<Qd;)T(Pf,Of[Sc]),Sc=1+Sc|0;Pd=Pf.y}var ro=(new I).d(qo,Pd),so=L().Zb,Qf=
[P()];if(0===(Qf.length|0))var Rd=Q();else{for(var Rf=R(new S,Q()),Tc=0,Sd=Qf.length|0;Tc<Sd;)T(Rf,Qf[Tc]),Tc=1+Tc|0;Rd=Rf.y}var to=(new I).d(so,Rd),uo=L().$b,Sf=[P(),N()];if(0===(Sf.length|0))var Td=Q();else{for(var Tf=R(new S,Q()),Uc=0,Ud=Sf.length|0;Uc<Ud;)T(Tf,Sf[Uc]),Uc=1+Uc|0;Td=Tf.y}var vo=(new I).d(uo,Td),wo=L().ac,Uf=[P()];if(0===(Uf.length|0))var Vd=Q();else{for(var Vf=R(new S,Q()),Vc=0,Wd=Uf.length|0;Vc<Wd;)T(Vf,Uf[Vc]),Vc=1+Vc|0;Vd=Vf.y}var xo=(new I).d(wo,Vd),yo=L().bc,Wf=[P()];if(0===
(Wf.length|0))var Xd=Q();else{for(var Xf=R(new S,Q()),Wc=0,Yd=Wf.length|0;Wc<Yd;)T(Xf,Wf[Wc]),Wc=1+Wc|0;Xd=Xf.y}var zo=(new I).d(yo,Xd),Ao=L().cc,Yf=[N()];if(0===(Yf.length|0))var Zd=Q();else{for(var Zf=R(new S,Q()),Xc=0,$d=Yf.length|0;Xc<$d;)T(Zf,Yf[Xc]),Xc=1+Xc|0;Zd=Zf.y}var Bo=(new I).d(Ao,Zd),Co=L().ec,$f=[P(),N()];if(0===($f.length|0))var ae=Q();else{for(var ag=R(new S,Q()),Yc=0,be=$f.length|0;Yc<be;)T(ag,$f[Yc]),Yc=1+Yc|0;ae=ag.y}var Do=(new I).d(Co,ae),Eo=L().fc,bg=[P()];if(0===(bg.length|
0))var ce=Q();else{for(var cg=R(new S,Q()),Zc=0,de=bg.length|0;Zc<de;)T(cg,bg[Zc]),Zc=1+Zc|0;ce=cg.y}var Fo=(new I).d(Eo,ce),Go=L().gc,dg=[P(),N()];if(0===(dg.length|0))var ee=Q();else{for(var eg=R(new S,Q()),$c=0,fe=dg.length|0;$c<fe;)T(eg,dg[$c]),$c=1+$c|0;ee=eg.y}var Ho=(new I).d(Go,ee),Io=L().hc,fg=[N()];if(0===(fg.length|0))var ge=Q();else{for(var gg=R(new S,Q()),ad=0,he=fg.length|0;ad<he;)T(gg,fg[ad]),ad=1+ad|0;ge=gg.y}var Jo=(new I).d(Io,ge),Ko=L().nc,hg=[N()];if(0===(hg.length|0))var ie=Q();
else{for(var ig=R(new S,Q()),bd=0,je=hg.length|0;bd<je;)T(ig,hg[bd]),bd=1+bd|0;ie=ig.y}for(var Zj=[y,bc,ao,no,po,ro,to,vo,xo,zo,Bo,Do,Fo,Ho,Jo,(new I).d(Ko,ie)],jg=Al(new Bl,Cl()),cd=0,ke=Zj.length|0;cd<ke;)Dl(jg,Zj[cd]),cd=1+cd|0;var Lo=jg.y;hw||(hw=(new iw).b());var Nw=a(b,Lo,hw.t,(new A).g([jw().t])),Ow=yl,Pw=zl();al();var Qw=L().Ub,Mo=[P()];if(0===(Mo.length|0))var ts=Q();else{for(var us=R(new S,Q()),ak=0,Rw=Mo.length|0;ak<Rw;)T(us,Mo[ak]),ak=1+ak|0;ts=us.y}var Sw=(new I).d(Qw,ts),Tw=L().Vb,No=
[P()];if(0===(No.length|0))var vs=Q();else{for(var ws=R(new S,Q()),bk=0,Uw=No.length|0;bk<Uw;)T(ws,No[bk]),bk=1+bk|0;vs=ws.y}var Vw=(new I).d(Tw,vs),Ww=L().Wb,Oo=[P()];if(0===(Oo.length|0))var xs=Q();else{for(var ys=R(new S,Q()),ck=0,Xw=Oo.length|0;ck<Xw;)T(ys,Oo[ck]),ck=1+ck|0;xs=ys.y}var Yw=(new I).d(Ww,xs),Zw=L().Xb,Po=[P()];if(0===(Po.length|0))var zs=Q();else{for(var As=R(new S,Q()),dk=0,$w=Po.length|0;dk<$w;)T(As,Po[dk]),dk=1+dk|0;zs=As.y}var ax=(new I).d(Zw,zs),bx=L().mc,Qo=[P()];if(0===(Qo.length|
0))var Bs=Q();else{for(var Cs=R(new S,Q()),ek=0,cx=Qo.length|0;ek<cx;)T(Cs,Qo[ek]),ek=1+ek|0;Bs=Cs.y}var dx=(new I).d(bx,Bs),ex=L().Yb,Ro=[P()];if(0===(Ro.length|0))var Ds=Q();else{for(var Es=R(new S,Q()),fk=0,fx=Ro.length|0;fk<fx;)T(Es,Ro[fk]),fk=1+fk|0;Ds=Es.y}var gx=(new I).d(ex,Ds),hx=L().Zb,So=[Zk()];if(0===(So.length|0))var Fs=Q();else{for(var Gs=R(new S,Q()),gk=0,ix=So.length|0;gk<ix;)T(Gs,So[gk]),gk=1+gk|0;Fs=Gs.y}var jx=(new I).d(hx,Fs),kx=L().$b,To=[P(),$k()];if(0===(To.length|0))var Hs=
Q();else{for(var Is=R(new S,Q()),hk=0,lx=To.length|0;hk<lx;)T(Is,To[hk]),hk=1+hk|0;Hs=Is.y}var mx=(new I).d(kx,Hs),nx=L().ac,Uo=[$k(),Zk()];if(0===(Uo.length|0))var Js=Q();else{for(var Ks=R(new S,Q()),ik=0,ox=Uo.length|0;ik<ox;)T(Ks,Uo[ik]),ik=1+ik|0;Js=Ks.y}var px=(new I).d(nx,Js),qx=L().bc,Vo=[Zk()];if(0===(Vo.length|0))var Ls=Q();else{for(var Ms=R(new S,Q()),jk=0,rx=Vo.length|0;jk<rx;)T(Ms,Vo[jk]),jk=1+jk|0;Ls=Ms.y}var sx=(new I).d(qx,Ls),tx=L().cc,Wo=[P()];if(0===(Wo.length|0))var Ns=Q();else{for(var Os=
R(new S,Q()),kk=0,ux=Wo.length|0;kk<ux;)T(Os,Wo[kk]),kk=1+kk|0;Ns=Os.y}var vx=(new I).d(tx,Ns),wx=L().ec,Xo=[P()];if(0===(Xo.length|0))var Ps=Q();else{for(var Qs=R(new S,Q()),lk=0,xx=Xo.length|0;lk<xx;)T(Qs,Xo[lk]),lk=1+lk|0;Ps=Qs.y}var yx=(new I).d(wx,Ps),zx=L().fc,Yo=[P()];if(0===(Yo.length|0))var Rs=Q();else{for(var Ss=R(new S,Q()),mk=0,Ax=Yo.length|0;mk<Ax;)T(Ss,Yo[mk]),mk=1+mk|0;Rs=Ss.y}var Bx=(new I).d(zx,Rs),Cx=L().gc,Zo=[P()];if(0===(Zo.length|0))var Ts=Q();else{for(var Us=R(new S,Q()),nk=
0,Dx=Zo.length|0;nk<Dx;)T(Us,Zo[nk]),nk=1+nk|0;Ts=Us.y}var Ex=(new I).d(Cx,Ts),Fx=L().hc,$o=[P()];if(0===($o.length|0))var Vs=Q();else{for(var Ws=R(new S,Q()),ok=0,Gx=$o.length|0;ok<Gx;)T(Ws,$o[ok]),ok=1+ok|0;Vs=Ws.y}var Hx=(new I).d(Fx,Vs),Ix=L().nc,ap=[P()];if(0===(ap.length|0))var Xs=Q();else{for(var Ys=R(new S,Q()),pk=0,Jx=ap.length|0;pk<Jx;)T(Ys,ap[pk]),pk=1+pk|0;Xs=Ys.y}for(var Zs=[Sw,Vw,Yw,ax,dx,gx,jx,mx,px,sx,vx,yx,Bx,Ex,Hx,(new I).d(Ix,Xs)],$s=Al(new Bl,Cl()),qk=0,Kx=Zs.length|0;qk<Kx;)Dl($s,
Zs[qk]),qk=1+qk|0;this.Qb=Hk(0,"text-align",Hn(Nw,Ow(Pw,$s.y,"justify",(new A).g([]))));return this};fw.prototype.ue=function(){return this.Qb};fw.prototype.$classData=l({Ku:0},!1,"scalacss.internal.Attrs$textAlign$",{Ku:1,Se:1,c:1});var gw=void 0;function kw(){this.nr=this.Lp=null}kw.prototype=new n;kw.prototype.constructor=kw;function lw(a,b,c){return(new I).d(wg(a.Lp,b,c),wg(a.nr,b,c))}kw.prototype.$classData=l({cv:0},!1,"scalacss.internal.Compose$Rules$$anon$1",{cv:1,c:1,oC:1});
l({iv:0},!1,"scalacss.internal.Domain$OverSeq",{iv:1,c:1,pC:1});function Ck(){xk.call(this);this.zm=null}Ck.prototype=new yk;Ck.prototype.constructor=Ck;Ck.prototype.Bg=function(a,b){xk.prototype.Bg.call(this,a,b);a=[this];if(0===(a.length|0))a=Q();else{b=R(new S,Q());for(var c=0,e=a.length|0;c<e;)T(b,a[c]),c=1+c|0;a=b.y}this.zm=a;return this};Ck.prototype.$classData=l({Bv:0},!1,"scalacss.internal.RealAttr",{Bv:1,uu:1,c:1});function wn(){this.ep=null}wn.prototype=new n;wn.prototype.constructor=wn;
wn.prototype.Ij=function(a){return mw(this,a)};wn.prototype.Mb=function(a){this.ep=a;return this};function mw(a,b){var c=(new os).b(),e=a.ep.f(c),f=pm(Bm(),b);if(null===f)throw(new x).n(f);var h=f.Sg;b=f.Tg;f=f.Ug;for(h=wm(Bm(),h);!f.l();){var m=f.I();nw(e,m);f=f.w()}for(;!b.l();)f=b.I(),nw(e,f),b=b.w();h.M(t(new u,function(a,b){return function(a){a.R.l()&&ow(b,B(),a.U)}}(a,e)));h.M(t(new u,function(a,b){return function(a){!a.R.l()&&ow(b,a.R,a.U)}}(a,e)));e.di.V();return c.ra.Da}
wn.prototype.$classData=l({Ev:0},!1,"scalacss.internal.StringRenderer$Default",{Ev:1,c:1,Cv:1});function pw(){}pw.prototype=new n;pw.prototype.constructor=pw;function qw(){}qw.prototype=pw.prototype;function Pn(){this.gi=null}Pn.prototype=new Nn;Pn.prototype.constructor=Pn;function Qn(a,b){return a.gi.f(b)}Pn.prototype.Mb=function(a){this.gi=a;return this};Pn.prototype.$classData=l({Ov:0},!1,"scalacss.internal.ValueT$Rule$$anon$1",{Ov:1,PC:1,c:1});function rw(){}rw.prototype=new Zn;
rw.prototype.constructor=rw;function sw(){}sw.prototype=rw.prototype;function tw(){Fp.call(this)}tw.prototype=new Gp;tw.prototype.constructor=tw;tw.prototype.b=function(){Fp.prototype.mw.call(this,fo().Gg);return this};tw.prototype.$classData=l({Sv:0},!1,"scalacss.internal.mutable.GlobalRegistry$",{Sv:1,UC:1,c:1});var uw=void 0;function vw(){uw||(uw=(new tw).b());return uw}function vp(){this.jq=null}vp.prototype=new n;vp.prototype.constructor=vp;
function ip(a){return(new H).n(Eb(function(a){return function(c,e){a.jq.f(Tl(Ul((new A).g(["[CSS WARNING] "," -- ",""])),(new A).g([$l(Bm(),c,e.Yh),e.Li])))}}(a)))}vp.prototype.Mb=function(a){this.jq=a;return this};vp.prototype.$classData=l({Xv:0},!1,"scalacss.internal.mutable.Register$ErrorHandler$Noisy",{Xv:1,c:1,QC:1});function ww(){this.wo=null}ww.prototype=new n;ww.prototype.constructor=ww;
ww.prototype.b=function(){xw=this;var a=(new zh).i("[^_a-zA-Z0-9\u00a0-\u00ff-]");xb();this.wo=Ah(a.bb);return this};ww.prototype.$classData=l({Yv:0},!1,"scalacss.internal.mutable.Register$MacroName$Use$",{Yv:1,c:1,RC:1});var xw=void 0;function yw(){xw||(xw=(new ww).b());return xw}function pp(){this.Ol=null;this.sm=0}pp.prototype=new n;pp.prototype.constructor=pp;pp.prototype.qi=function(a,b){this.Ol=a;this.sm=b;return this};
pp.prototype.$classData=l({Zv:0},!1,"scalacss.internal.mutable.Register$NameGen$IncFmt",{Zv:1,c:1,SC:1});function zw(){this.wa=null}zw.prototype=new n;zw.prototype.constructor=zw;function Aw(a){var b=new zw;if(null===a)throw Dp(ni(),null);b.wa=a;return b}zw.prototype.$classData=l({cw:0},!1,"scalacss.internal.mutable.StyleSheet$Inline$MFontFace",{cw:1,c:1,qC:1});function Bw(){this.wa=this.qa=null}Bw.prototype=new n;Bw.prototype.constructor=Bw;
function Cw(a){var b=new Bw;b.qa="self";if(null===a)throw Dp(ni(),null);b.wa=a;return b}Bw.prototype.$classData=l({dw:0},!1,"scalacss.internal.mutable.StyleSheet$Inline$MStyle",{dw:1,c:1,rC:1});function Dw(){}Dw.prototype=new n;Dw.prototype.constructor=Dw;function Ew(){}Ew.prototype=Dw.prototype;function Yu(a){return!!(a&&a.$classData&&a.$classData.p.Dg||"number"===typeof a)}function Fw(){this.Aa=null}Fw.prototype=new n;Fw.prototype.constructor=Fw;function Gw(){}Gw.prototype=Fw.prototype;
Fw.prototype.Xj=function(){if(void 0===g.Error.captureStackTrace){try{var a={}.undef()}catch(b){if(a=mi(ni(),b),null!==a)if(Tu(a))a=a.Ff;else throw Dp(ni(),a);else throw b;}this.stackdata=a}else g.Error.captureStackTrace(this),this.stackdata=this;return this};Fw.prototype.md=function(){return this.Aa};Fw.prototype.m=function(){var a=Ib(la(this)),b=this.md();return null===b?a:a+": "+b};Fw.prototype.Sa=function(a,b,c,e){this.Aa=a;e&&this.Xj();return this};function Hw(){}Hw.prototype=new n;
Hw.prototype.constructor=Hw;function Iw(){}d=Iw.prototype=Hw.prototype;d.xk=function(){throw(new Jw).b();};d.o=function(a){if(a===this)return!0;if(a&&a.$classData&&a.$classData.p.fm&&this.v()===a.v()){var b=Or(),c=this.be();return Rr(bt(b,c)).Hc(t(new u,function(a,b){return function(a){var c=b.mi(a.xa);a=a.t;return null===c?null===a:za(c,a)}}(this,a)))}return!1};
d.m=function(){var a=Or(),b=this.be().rc(),a=Rr(gt(a,b)),a=(new li).pb(a,t(new u,function(){return function(a){return a.xa+"\x3d"+a.t}}(this)));return Oj(a,"{",", ","}")};d.Zh=function(a){for(var b=Or(),c=this.be().rc(),b=Rr(gt(b,c)),c=!1;!c&&b.u();)c=b.r(),c=null===c?null===a:Up(Wp(),c,a);return c};d.mi=function(a){var b=Or(),c=this.be().rc(),b=Rr(gt(b,c));a:{for(;b.u();){var c=b.r(),e=c.xa;if(null===e?null===a:za(e,a)){a=(new H).n(c);break a}}a=B()}return a.l()?null:a.P().t};d.v=function(){return this.be().v()};
d.Ck=function(a){a:{var b=this.be().rc();for(;;)if(b.u()){var c=b.r(),e=c.xa;if(null===a?null===e:za(a,e)){b.Ui();a=c.t;break a}}else{a=null;break a}}return a};d.s=function(){var a=Or(),b=this.be();return Rr(bt(a,b)).Yc(0,Eb(function(){return function(a,b){a|=0;return Tp(Wp(),b)+a|0}}(this)))|0};function Kw(){}Kw.prototype=new n;Kw.prototype.constructor=Kw;d=Kw.prototype;d.b=function(){return this};d.Ui=function(){throw(new Lw).b();};d.r=function(){throw(new Mw).b();};d.u=function(){return!1};
d.$classData=l({Xw:0},!1,"java.util.Collections$EmptyIterator",{Xw:1,c:1,em:1});function Lx(){this.wa=this.Fi=this.Zl=null}Lx.prototype=new n;Lx.prototype.constructor=Lx;function Mx(){}Mx.prototype=Lx.prototype;Lx.prototype.Ui=function(){var a=this.Fi;if(If(a))this.wa.ee.qq(a.J),this.Fi=B();else{if(B()===a)throw(new Lw).b();throw(new x).n(a);}};
Lx.prototype.r=function(){this.Fi=(new H).n(this.Zl.r());var a=this.Fi.P(),b=new Nx;if(null===this)throw Dp(ni(),null);b.sa=this;Ox.prototype.d.call(b,a.Pa,this.sa.Ke.ee.f(a));return b};Lx.prototype.u=function(){return this.Zl.u()};Lx.prototype.Vl=function(a){if(null===a)throw Dp(ni(),null);this.wa=a;this.Zl=a.ee.Jp().C();this.Fi=B();return this};function Px(){this.wa=this.Jd=this.$l=null}Px.prototype=new n;Px.prototype.constructor=Px;
Px.prototype.Ui=function(){if(this.Jd.l())throw(new Lw).b();var a=this.Jd;a.l()||(a=a.P(),this.wa.Ee(a));this.Jd=B()};Px.prototype.r=function(){this.Jd=(new H).n(this.$l.r().Pa);return this.Jd.P()};Px.prototype.u=function(){return this.$l.u()};Px.prototype.$classData=l({nx:0},!1,"java.util.HashSet$$anon$1",{nx:1,c:1,em:1});function Pc(){this.up=this.bq=null;this.oq=this.pq=0;this.Be=this.Cg=this.Bk=null;this.Lj=!1;this.Wg=0}Pc.prototype=new n;Pc.prototype.constructor=Pc;
function ku(a){if(a.Lj){a.Be=a.Bk.exec(a.Cg);if(null!==a.Be){var b=a.Be[0];if(void 0===b)throw(new Mw).i("undefined.get");if(null===b)throw(new Ic).b();""===b&&(b=a.Bk,b.lastIndex=1+(b.lastIndex|0)|0)}else a.Lj=!1;B();return null!==a.Be}return!1}function Qx(a){if(null===a.Be)throw(new Lw).i("No match available");return a.Be}function Bv(a,b){a=Qx(a)[b];return void 0===a?null:a}function Lc(a){ju(a);ku(a);null===a.Be||0===(Qx(a).index|0)&&Rx(a)===(a.Cg.length|0)||ju(a);return null!==a.Be}
function nu(a,b){Sx(b,a.Cg.substring(a.Wg));a.Wg=a.Cg.length|0}function Rx(a){var b=Qx(a).index|0;a=Qx(a)[0];if(void 0===a)throw(new Mw).i("undefined.get");return b+(a.length|0)|0}function Mc(a,b,c,e){a.bq=b;a.up=c;a.pq=0;a.oq=e;b=a.bq;c=new g.RegExp(b.Eg);b=c!==b.Eg?c:new g.RegExp(b.Eg.source,(b.Eg.global?"g":"")+(b.Eg.ignoreCase?"i":"")+(b.Eg.multiline?"m":""));a.Bk=b;a.Cg=ka(Ha(a.up,a.pq,a.oq));a.Be=null;a.Lj=!0;a.Wg=0;B();return a}
function mu(a,b,c){var e=a.Cg,f=a.Wg,h=Qx(a).index|0;Sx(b,e.substring(f,h));e=c.length|0;for(f=0;f<e;)switch(h=65535&(c.charCodeAt(f)|0),h){case 36:for(h=f=1+f|0;;){if(f<e)var m=65535&(c.charCodeAt(f)|0),m=48<=m&&57>=m;else m=!1;if(m)f=1+f|0;else break}hs();h=c.substring(h,f);m=null===h?0:h.length|0;0===m&&Tx(h);var q=65535&(h.charCodeAt(0)|0),y=45===q,K=y?2147483648:2147483647,q=y||43===q?1:0;q>=(h.length|0)&&Tx(h);for(var z=0;q!==m;){Ux||(Ux=(new Vx).b());var E;E=Ux;var C=65535&(h.charCodeAt(q)|
0);if(256>C)E=48<=C&&57>=C?-48+C|0:65<=C&&90>=C?-55+C|0:97<=C&&122>=C?-87+C|0:-1;else if(65313<=C&&65338>=C)E=-65303+C|0;else if(65345<=C&&65370>=C)E=-65335+C|0;else{var F;a:{$p();F=Wx(E);var W=C,bc=0,Nc=F.e.length;for(;;){if(bc===Nc){F=-1-bc|0;break a}var wb=(bc+Nc|0)>>>1|0,Lb=F.e[wb];if(W<Lb)Nc=wb;else{if(D(G(),W,Lb)){F=wb;break a}bc=1+wb|0}}}F=0>F?-2-F|0:F;0>F?E=-1:(E=C-Wx(E).e[F]|0,E=9<E?-1:E)}E=10>E?E:-1;z=10*z+E;(-1===E||z>K)&&Tx(h);q=1+q|0}Sx(b,Bv(a,y?-z|0:z|0));break;case 92:f=1+f|0;f<e&&
(h=65535&(c.charCodeAt(f)|0),zn(b.ve,h));f=1+f|0;break;default:zn(b.ve,h),f=1+f|0}a.Wg=Rx(a)}function Ch(a,b){ju(a);for(var c=(new lu).b();ku(a);)mu(a,c,b);nu(a,c);return c.m()}function ju(a){a.Bk.lastIndex=0;a.Be=null;a.Lj=!0;a.Wg=0;B()}Pc.prototype.$classData=l({zx:0},!1,"java.util.regex.Matcher",{zx:1,c:1,kD:1});function Xx(){}Xx.prototype=new n;Xx.prototype.constructor=Xx;Xx.prototype.b=function(){return this};Xx.prototype.Xg=function(){return(new os).b()};Xx.prototype.xf=function(){return(new os).b()};
Xx.prototype.$classData=l({Tx:0},!1,"scala.Predef$$anon$3",{Tx:1,c:1,Jk:1});function vq(){}vq.prototype=new n;vq.prototype.constructor=vq;vq.prototype.b=function(){return this};vq.prototype.m=function(){return"object AnyRef"};vq.prototype.$classData=l({hy:0},!1,"scala.package$$anon$1",{hy:1,c:1,yD:1});function Yx(){this.Om=this.Op=this.Nm=0}Yx.prototype=new Zr;Yx.prototype.constructor=Yx;Yx.prototype.b=function(){Zx=this;this.Nm=Ba(Ca(),"Seq");this.Op=Ba(Ca(),"Map");this.Om=Ba(Ca(),"Set");return this};
function $x(a,b){var c;if(b&&b.$classData&&b.$classData.p.Iq){c=0;a=a.Nm;for(var e=b;!e.l();)b=e.I(),e=e.w(),a=as(a,Bi(Ci(),b)),c=1+c|0;c=bs(a^c)}else c=es(a,b,a.Nm);return c}Yx.prototype.$classData=l({Jy:0},!1,"scala.util.hashing.MurmurHash3$",{Jy:1,ED:1,c:1});var Zx=void 0;function U(){Zx||(Zx=(new Yx).b());return Zx}function ay(a){return(a.u()?"non-empty":"empty")+" iterator"}function by(a,b){for(var c=!0;c&&a.u();)c=!!b.f(a.r());return c}function Jp(a,b){for(;a.u();)b.f(a.r())}
function cy(a,b){var c=new dy;c.jd=a;c.cb=null;c.Jd=null;c.Bf=!1;return c.Yk(b)}function ey(a){if(a.u()){var b=a.r();return(new fy).pi(b,v(new w,function(a){return function(){return a.xc()}}(a)))}Hq();return gy()}function hy(a,b){for(var c=0;c<b&&a.u();)a.r(),c=1+c|0;return a}function iy(){this.wa=this.qk=null}iy.prototype=new n;iy.prototype.constructor=iy;iy.prototype.M=function(a){this.wa.M(t(new u,function(a,c){return function(e){return a.qk.f(e)?c.f(e):void 0}}(this,a)))};
function jy(a,b,c){a.qk=c;if(null===b)throw Dp(ni(),null);a.wa=b;return a}iy.prototype.$classData=l({gz:0},!1,"scala.collection.TraversableLike$WithFilter",{gz:1,c:1,Z:1});function ky(){}ky.prototype=new ot;ky.prototype.constructor=ky;function ly(){}ly.prototype=ky.prototype;function my(){this.wa=null}my.prototype=new n;my.prototype.constructor=my;my.prototype.Xg=function(){return this.wa.ba()};my.prototype.xf=function(a){return a&&a.$classData&&a.$classData.p.kc?a.$a().ba():this.wa.ba()};
function ny(a){var b=new my;if(null===a)throw Dp(ni(),null);b.wa=a;return b}my.prototype.$classData=l({qz:0},!1,"scala.collection.generic.GenSetFactory$$anon$1",{qz:1,c:1,Jk:1});function oy(){this.Ra=null}oy.prototype=new ot;oy.prototype.constructor=oy;function py(){}py.prototype=oy.prototype;oy.prototype.b=function(){this.Ra=(new qy).ri(this);return this};function ry(){this.wa=null}ry.prototype=new n;ry.prototype.constructor=ry;function sy(){}sy.prototype=ry.prototype;ry.prototype.Xg=function(){return this.wa.ba()};
ry.prototype.xf=function(a){return a.$a().ba()};ry.prototype.ri=function(a){if(null===a)throw Dp(ni(),null);this.wa=a;return this};function ty(){}ty.prototype=new mt;ty.prototype.constructor=ty;function uy(){}uy.prototype=ty.prototype;function vy(){this.km=null}vy.prototype=new rt;vy.prototype.constructor=vy;function wy(a,b){a.km=b;b=new xy;if(null===a)throw Dp(ni(),null);b.sa=a}vy.prototype.tl=function(a,b){return wg(this.km,a,b)};
vy.prototype.$classData=l({xz:0},!1,"scala.collection.immutable.HashMap$$anon$2",{xz:1,Cz:1,c:1});function xy(){this.sa=null}xy.prototype=new rt;xy.prototype.constructor=xy;xy.prototype.tl=function(a,b){return wg(this.sa.km,b,a)};xy.prototype.$classData=l({yz:0},!1,"scala.collection.immutable.HashMap$$anon$2$$anon$3",{yz:1,Cz:1,c:1});function yy(){}yy.prototype=new n;yy.prototype.constructor=yy;d=yy.prototype;d.b=function(){return this};d.f=function(){return this};d.m=function(){return"\x3cfunction1\x3e"};
d.Dd=function(a){return nb(this,a)};d.$classData=l({Mz:0},!1,"scala.collection.immutable.List$$anon$1",{Mz:1,c:1,T:1});function zy(a,b){b=b.Ie();switch(b){case -1:break;default:a.wc(b)}}function Ay(a,b,c){b=b.Ie();switch(b){case -1:break;default:a.wc(b+c|0)}}function By(a,b,c){c=c.Ie();switch(c){case -1:break;default:a.wc(b<c?b:c)}}function Cy(){}Cy.prototype=new n;Cy.prototype.constructor=Cy;function Dy(){}Dy.prototype=Cy.prototype;Cy.prototype.m=function(){return"\x3cfunction0\x3e"};
function Ey(){}Ey.prototype=new n;Ey.prototype.constructor=Ey;function Fy(){}Fy.prototype=Ey.prototype;Ey.prototype.m=function(){return"\x3cfunction1\x3e"};Ey.prototype.Dd=function(a){return nb(this,a)};function Gy(){}Gy.prototype=new n;Gy.prototype.constructor=Gy;function Hy(){}Hy.prototype=Gy.prototype;Gy.prototype.m=function(){return"\x3cfunction2\x3e"};function Iy(){}Iy.prototype=new n;Iy.prototype.constructor=Iy;function Jy(){}Jy.prototype=Iy.prototype;Iy.prototype.m=function(){return"\x3cfunction3\x3e"};
function Ky(){}Ky.prototype=new n;Ky.prototype.constructor=Ky;function Ly(){}Ly.prototype=Ky.prototype;Ky.prototype.m=function(){return"\x3cfunction4\x3e"};function My(){this.H=!1}My.prototype=new n;My.prototype.constructor=My;My.prototype.m=function(){return""+this.H};function ss(a){var b=new My;b.H=a;return b}My.prototype.$classData=l({wB:0},!1,"scala.runtime.BooleanRef",{wB:1,c:1,h:1});function ev(a){return!!(a&&a.$classData&&1===a.$classData.Zg&&a.$classData.Yg.p.er)}
var wa=l({er:0},!1,"scala.runtime.BoxedUnit",{er:1,c:1,h:1},void 0,void 0,function(a){return void 0===a});function ds(){this.H=0}ds.prototype=new n;ds.prototype.constructor=ds;ds.prototype.m=function(){return""+this.H};ds.prototype.ob=function(a){this.H=a;return this};ds.prototype.$classData=l({yB:0},!1,"scala.runtime.IntRef",{yB:1,c:1,h:1});function Kf(){this.H=null}Kf.prototype=new n;Kf.prototype.constructor=Kf;Kf.prototype.m=function(){return""+this.H};Kf.prototype.n=function(a){this.H=a;return this};
Kf.prototype.$classData=l({BB:0},!1,"scala.runtime.ObjectRef",{BB:1,c:1,h:1});function Ny(){}Ny.prototype=new n;Ny.prototype.constructor=Ny;Ny.prototype.b=function(){return this};function gf(){var a=Fe();ti();return pi(new oi,t(new u,function(){return function(a){return a.mf}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return $e(new af,a.Ue,a.Ve,a.We,a.Xe,a.Ye,a.Ze,b,a.Te)}}(a,c))}}(a)))}
function fh(){var a=Fe();ti();return pi(new oi,t(new u,function(){return function(a){return a.Ve}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return $e(new af,a.Ue,b,a.We,a.Xe,a.Ye,a.Ze,a.mf,a.Te)}}(a,c))}}(a)))}
function Tg(){var a=Fe();ti();return pi(new oi,t(new u,function(){return function(a){return a.Ye}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return $e(new af,a.Ue,a.Ve,a.We,a.Xe,b,a.Ze,a.mf,a.Te)}}(a,c))}}(a)))}function pf(a,b){return Ap(Ca(),b,"undefined \u2192 undefined","undefined").split("props: undefined, ").join("").split("state: undefined)").join(")").split(", )").join(")")}
function Lg(){var a=Fe();ti();return pi(new oi,t(new u,function(){return function(a){return a.Ue}}(a)),t(new u,function(a){return function(c){return t(new u,function(a,b){return function(a){return $e(new af,b,a.Ve,a.We,a.Xe,a.Ye,a.Ze,a.mf,a.Te)}}(a,c))}}(a)))}Ny.prototype.$classData=l({ps:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$",{ps:1,c:1,k:1,h:1});var Oy=void 0;function Fe(){Oy||(Oy=(new Ny).b());return Oy}function Py(){}Py.prototype=new n;Py.prototype.constructor=Py;
Py.prototype.b=function(){return this};Py.prototype.$classData=l({Fs:0},!1,"japgolly.scalajs.react.extra.router.AbsUrl$",{Fs:1,c:1,k:1,h:1});var Qy=void 0;function Fc(){}Fc.prototype=new n;Fc.prototype.constructor=Fc;Fc.prototype.b=function(){return this};Fc.prototype.$classData=l({Hs:0},!1,"japgolly.scalajs.react.extra.router.BaseUrl$",{Hs:1,c:1,k:1,h:1});var Ec=void 0;function Ry(){this.Vp=null}Ry.prototype=new n;Ry.prototype.constructor=Ry;
Ry.prototype.b=function(){Sy=this;this.Vp=t(new u,function(){return function(){var a=(new Bb).oa(rb().Y);return(new Bb).oa(a.pa)}}(this));return this};function Ty(a){var b=vb(rb(),v(new w,function(){return function(){Gc(Hc()).scrollTo(0,0)}}(a)));return Eb(function(a,b){return function(){return(new Bb).oa(b)}}(a,b))}function Uy(a){return Eb(function(){return function(a,c){return c.Rf.V()}}(a))}
function Cc(){var a=Vy();return t(new u,function(){return function(a){var c=rb();a="[Router] "+a.V();return(new Bb).oa(Ad(c,a))}}(a))}Ry.prototype.$classData=l({Os:0},!1,"japgolly.scalajs.react.extra.router.RouterConfig$",{Os:1,c:1,k:1,h:1});var Sy=void 0;function Vy(){Sy||(Sy=(new Ry).b());return Sy}function Wy(){this.kh=this.$h=this.yo=this.Tm=this.id=this.jg=null}Wy.prototype=new n;Wy.prototype.constructor=Wy;
function dh(a,b){var c=new Wy;c.jg=a;c.id=b;c.kh=(yg(),xb());c.Tm=Jd(qb(),v(new w,function(){return function(){Qy||(Qy=(new Py).b());return(new Xy).i(Gc(Hc()).location.href)}}(c)),t(new u,function(a){return function(b){return(new Bb).oa(Jd(qb(),a.id.sd.f(v(new w,function(a,b){return function(){return"Syncing to "+b+"."}}(a,b))).pa,t(new u,function(a,b){return function(){return(new Bb).oa(Jd(qb(),vv(a,Yy(a,b)),t(new u,function(a){return function(b){return(new Bb).oa(Jd(qb(),a.id.sd.f(v(new w,function(a,
b){return function(){return"Resolved to page "+b.$c+"."}}(a,b))).pa,t(new u,function(a,b){return function(){return(new Bb).oa(Id(qb(),a.id.sd.f(v(new w,function(){return function(){return""}}(a))).pa,t(new u,function(a,b){return function(){return b}}(a,b))))}}(a,b))))}}(a))))}}(a,b))))}}(c)));c.yo=uv(c);c.$h=Zy(c.yo,b.ub);return c}
function $y(a,b,c){return Zg(Zg((new az).oa(v(new w,function(a,b,c){return function(){return"Redirecting to "+bz(a.jg,b)+" via "+c+"."}}(a,b,c))),cz(a,b,c)),Yy(a,bz(a.jg,b)))}function Yy(a,b){var c;c=b.J;var e=a.jg.J;c=0<=(c.length|0)&&c.substring(0,e.length|0)===e?(new H).n((new sc).i(b.J.substring(a.jg.J.length|0))):B();if(If(c))return dz(a,c.J);if(B()===c)return ez(a,b);throw(new x).n(c);}
function vv(a,b){if(b&&b.$classData&&b.$classData.p.jl){var c=b.yc;b=v(new w,function(a,b){return function(){Gc(Hc()).history.pushState({},"",b.J)}}(a,c));a=a.id.sd.f(v(new w,function(a,b){return function(){return"PushState: ["+b.J+"]"}}(a,c))).pa;return(new Bb).oa(sb(qb(),a,b)).pa}if(b&&b.$classData&&b.$classData.p.kl)return c=b.yc,b=v(new w,function(a,b){return function(){Gc(Hc()).history.replaceState({},"",b.J)}}(a,c)),a=a.id.sd.f(v(new w,function(a,b){return function(){return"ReplaceState: ["+
b.J+"]"}}(a,c))).pa,(new Bb).oa(sb(qb(),a,b)).pa;if(b&&b.$classData&&b.$classData.p.ml)return c=b.yc,b=v(new w,function(a,b){return function(){Gc(Hc()).location.href=b.J}}(a,c)),a=a.id.sd.f(v(new w,function(a,b){return function(){return"SetWindowLocation: ["+b.J+"]"}}(a,c))).pa,(new Bb).oa(sb(qb(),a,b)).pa;if(wv()===b)return b=Cg(a),a=a.id.sd.f(v(new w,function(){return function(){return"Broadcasting sync request."}}(a))).pa,(new Bb).oa(sb(qb(),a,b)).pa;if(b&&b.$classData&&b.$classData.p.ll)return a=
b.Nh,zd(qb(),a);if(b&&b.$classData&&b.$classData.p.il)return a=a.id.sd.f(b.Mi),null===a?null:a.pa;if($g(b)){var e=b.hh;b=b.mh;for(var c=qb(),f=null,f=(new Bb).oa(rb().Y),e=Lj(e);e.Pb;)var h=e.r(),f=f.pa,f=(new Bb).oa(sb(qb(),f,vv(a,h)));return sb(c,f.pa,vv(a,b))}throw(new x).n(b);}function ez(a,b){var c=(new sc).i("");return Zg((new az).oa(v(new w,function(a,b,c){return function(){return"Wrong base: "+b+" is outside of "+bz(a.jg,c)+"."}}(a,b,c))),$y(a,c,fz()))}
function gz(a){var b=ub().de;if(bn(a))return(new hz).n(a.J);if(!cn(a))throw(new x).n(a);return b.f(a.J)}function cz(a,b,c){a=bz(a.jg,b);if(fz()===c)return(new iz).ni(a);if(wc()===c)return(new jz).ni(a);kz||(kz=(new lz).b());if(kz===c)return(new mz).ni(a);throw(new x).n(c);}
function dz(a,b){var c=a.id.Gb.f(b);if(bn(c))var e=c.J,f=wg(a.id.Db,b,e),e=Zg((new az).oa(v(new w,function(a,b,c,e){return function(){return"Action for page "+c+" at "+b+" is "+e+"."}}(a,b,e,f))),nz(a,e,f));else{if(!cn(c))throw(new x).n(c);e=oz(a,c.J)}return Zg((new az).oa(v(new w,function(a,b,c){return function(){return"Parsed "+b+" to "+c+"."}}(a,b,c))),e)}
function oz(a,b){if(b&&b.$classData&&b.$classData.p.gl){var c=b.Ii;return $y(a,a.id.ub.f(b.$c),c)}if(b&&b.$classData&&b.$classData.p.VB)return c=b.nD(),b=b.lD(),$y(a,c,b);throw(new x).n(b);}function pz(a,b){if(b&&b.$classData&&b.$classData.p.hl)return Ac(),(new jh).n(b);if(b&&b.$classData&&b.$classData.p.Is)return Ac(),a=oz(a,b),(new Bc).n(a);throw(new x).n(b);}
function nz(a,b,c){c=pz(a,c);a=bn(c)?(new jh).n((new qz).pi(b,v(new w,function(a,b){return function(){return b.Gf.f(a.$h)}}(a,c.J)))):c;return gz(a)}Wy.prototype.$classData=l({Ts:0},!1,"japgolly.scalajs.react.extra.router.RouterLogic",{Ts:1,c:1,SB:1,TB:1});function rz(){}rz.prototype=new n;rz.prototype.constructor=rz;rz.prototype.b=function(){return this};
function ic(){var a=qh();return(new hh).ih(t(new u,function(){return function(){return B()}}(a)),t(new u,function(){return function(){return B()}}(a)),Eb(function(){return function(){return B()}}(a)))}function ph(a,b){return(new hh).ih(b,t(new u,function(){return function(){return B()}}(a)),Eb(function(){return function(){return B()}}(a)))}rz.prototype.$classData=l({$s:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Rule$",{$s:1,c:1,k:1,h:1});var sz=void 0;
function qh(){sz||(sz=(new rz).b());return sz}function tz(){this.Qo=this.$g=null}tz.prototype=new n;tz.prototype.constructor=tz;
tz.prototype.b=function(){uz=this;this.$g=Eb(function(){return function(a,b){a=a.pa;return(new Bb).oa(sb(qb(),a,b.V().pa))}}(this));this.Qo=Eb(function(){return function(a,b){a=a.pa;var c=qb();b=b.V().pa;var e=qb();return(new Bb).oa(v(new w,function(a,b,c,e){return function(){return!!wg(e,b,c)}}(e,(new Bb).oa(a).pa,b,Eb(function(){return function(a,b){return!!a.V()||!!b.V()}}(c)))))}}(this));return this};
tz.prototype.$classData=l({nt:0},!1,"japgolly.scalajs.react.internal.Semigroup$",{nt:1,c:1,k:1,h:1});var uz=void 0;function hf(){uz||(uz=(new tz).b());return uz}function vz(){this.qa=null}vz.prototype=new Hv;vz.prototype.constructor=vz;vz.prototype.i=function(a){zi.prototype.i.call(this,a);return this};vz.prototype.$classData=l({ut:0},!1,"japgolly.scalajs.react.vdom.Attr$Event",{ut:1,Cn:1,Aj:1,c:1});
function Nv(){this.iB=this.ql=this.iw=this.ko=this.jo=this.hw=this.nk=this.xa=this.zl=this.Qm=this.Si=null}Nv.prototype=new n;Nv.prototype.constructor=Nv;Nv.prototype.b=function(){Li(this);return this};Nv.prototype.$classData=l({zt:0},!1,"japgolly.scalajs.react.vdom.Builder$ToVdomElement",{zt:1,c:1,YB:1,XB:1});function wz(){}wz.prototype=new n;wz.prototype.constructor=wz;wz.prototype.b=function(){return this};
function ud(a){var b=xb(),c=new Mv;a=zb(new Ab,a,b);xz||(xz=(new yz).b());c.ir="div";c.nm=a;return c}wz.prototype.$classData=l({Ct:0},!1,"japgolly.scalajs.react.vdom.HtmlTagOf$",{Ct:1,c:1,k:1,h:1});var zz=void 0;function nd(){zz||(zz=(new wz).b())}function yz(){}yz.prototype=new n;yz.prototype.constructor=yz;yz.prototype.b=function(){return this};yz.prototype.$classData=l({Kt:0},!1,"japgolly.scalajs.react.vdom.Namespace$",{Kt:1,c:1,k:1,h:1});var xz=void 0;function aj(){}aj.prototype=new n;
aj.prototype.constructor=aj;aj.prototype.b=function(){return this};aj.prototype.$classData=l({Qt:0},!1,"japgolly.scalajs.react.vdom.SvgTagOf$",{Qt:1,c:1,k:1,h:1});var $i=void 0;function Az(){this.kq=this.yk=null}Az.prototype=new Rv;Az.prototype.constructor=Az;function Qv(a){var b=new Az;b.kq=a;td.prototype.Xl.call(b,a);return b}Az.prototype.$classData=l({Wt:0},!1,"japgolly.scalajs.react.vdom.VdomElement",{Wt:1,Dn:1,c:1,Bj:1});function oj(){}oj.prototype=new n;oj.prototype.constructor=oj;
oj.prototype.b=function(){return this};oj.prototype.$classData=l({au:0},!1,"japgolly.univeq.UnivEq$$anon$1",{au:1,c:1,hC:1,eg:1});function pj(){}pj.prototype=new n;pj.prototype.constructor=pj;pj.prototype.b=function(){return this};pj.prototype.$classData=l({bu:0},!1,"japgolly.univeq.UnivEq$$anon$2",{bu:1,c:1,dC:1,eg:1});function qj(){}qj.prototype=new n;qj.prototype.constructor=qj;qj.prototype.b=function(){return this};
qj.prototype.$classData=l({cu:0},!1,"japgolly.univeq.UnivEq$$anon$3",{cu:1,c:1,fC:1,eg:1});function rj(){}rj.prototype=new n;rj.prototype.constructor=rj;rj.prototype.b=function(){return this};rj.prototype.$classData=l({du:0},!1,"japgolly.univeq.UnivEq$$anon$4",{du:1,c:1,eC:1,eg:1});function sj(){}sj.prototype=new n;sj.prototype.constructor=sj;sj.prototype.b=function(){return this};sj.prototype.$classData=l({eu:0},!1,"japgolly.univeq.UnivEq$$anon$5",{eu:1,c:1,gC:1,eg:1});function tj(){}
tj.prototype=new n;tj.prototype.constructor=tj;tj.prototype.b=function(){return this};tj.prototype.$classData=l({fu:0},!1,"japgolly.univeq.UnivEq$$anon$6",{fu:1,c:1,iC:1,eg:1});function uj(){}uj.prototype=new n;uj.prototype.constructor=uj;uj.prototype.b=function(){return this};uj.prototype.$classData=l({gu:0},!1,"japgolly.univeq.UnivEq$$anon$7",{gu:1,c:1,cC:1,eg:1});function pd(){}pd.prototype=new n;pd.prototype.constructor=pd;pd.prototype.b=function(){return this};
pd.prototype.$classData=l({nu:0},!1,"scalacss.ScalaCssReact$",{nu:1,c:1,lC:1,ou:1});var od=void 0;function Bz(){this.Pg=this.nc=this.hc=this.gc=this.fc=this.ec=this.cc=this.bc=this.ac=this.$b=this.Zb=this.Yb=this.mc=this.Xb=this.Wb=this.Vb=this.Ub=null}Bz.prototype=new n;Bz.prototype.constructor=Bz;
Bz.prototype.b=function(){Cz=this;this.Ub=Dz(new Ez,il(),Cl());this.Vb=Dz(new Ez,il(),Cl());this.Wb=Dz(new Ez,fl(),Cl());for(var a=il(),b=il(),b=[(new I).d("9.9",b)],c=Al(new Bl,Cl()),e=0,f=b.length|0;e<f;)Dl(c,b[e]),e=1+e|0;this.Xb=Dz(new Ez,a,c.y);this.mc=Dz(new Ez,il(),Cl());this.Yb=Dz(new Ez,il(),Cl());this.Zb=Dz(new Ez,gl(),Cl());this.$b=Dz(new Ez,fl(),Cl());this.ac=Dz(new Ez,gl(),Cl());this.bc=Dz(new Ez,gl(),Cl());this.cc=Dz(new Ez,il(),Cl());a=il();b=hl();b=[(new I).d("9,9.5-9.6,10.0-10.1,10.5,10.6,11,11.1,11.5,11.6,12,12.1",
b)];c=Al(new Bl,Cl());e=0;for(f=b.length|0;e<f;)Dl(c,b[e]),e=1+e|0;this.ec=Dz(new Ez,a,c.y);this.fc=Dz(new Ez,hl(),Cl());a=hl();b=il();b=[(new I).d("37",b)];c=Al(new Bl,Cl());e=0;for(f=b.length|0;e<f;)Dl(c,b[e]),e=1+e|0;this.gc=Dz(new Ez,a,c.y);this.hc=Dz(new Ez,il(),Cl());this.nc=Dz(new Ez,il(),Cl());a=this.Ub;b=(new A).g([this.Vb,this.Wb,this.Xb,this.mc,this.Yb,this.Zb,this.$b,this.ac,this.bc,this.cc,this.ec,this.fc,this.gc,this.hc,this.nc]);jl();c=kl().Qa;b=ll(b,c);this.Pg=ml(new nl,a,b);return this};
Bz.prototype.$classData=l({Nu:0},!1,"scalacss.internal.CanIUse$Agent$",{Nu:1,c:1,k:1,h:1});var Cz=void 0;function L(){Cz||(Cz=(new Bz).b());return Cz}function Fz(){this.wq=null;this.Q=0}Fz.prototype=new n;Fz.prototype.constructor=Fz;Fz.prototype.b=function(){return this};function Gz(){var a=Hz();if(0===(1&a.Q)&&0===(1&a.Q)){var b=Ql(),c=Sl(),e=new Iz,f=new kw;f.Lp=b;f.nr=c;e.Xi=f;a.wq=e;a.Q=(1|a.Q)<<24>>24}return a.wq}
Fz.prototype.$classData=l({av:0},!1,"scalacss.internal.Compose$",{av:1,c:1,k:1,h:1});var Jz=void 0;function Hz(){Jz||(Jz=(new Fz).b());return Jz}function Kz(){this.Y=null}Kz.prototype=new n;Kz.prototype.constructor=Kz;Kz.prototype.b=function(){Lz=this;var a=B(),b=Ac().Ec.dc,c=new Mz;c.Ti=a;c.nh=b;this.Y=c;return this};Kz.prototype.$classData=l({dv:0},!1,"scalacss.internal.Cond$",{dv:1,c:1,k:1,h:1});var Lz=void 0;function lp(){Lz||(Lz=(new Kz).b());return Lz}function Nz(){this.t=this.xa=null}
Nz.prototype=new n;Nz.prototype.constructor=Nz;Nz.prototype.b=function(){Oz=this;this.xa=Dm(new Cm,t(new u,function(){return function(a){return a.xa}}(this)),Eb(function(){return function(a,b){return Gk(b,a.t)}}(this)));this.t=Dm(new Cm,t(new u,function(){return function(a){return a.t}}(this)),Eb(function(){return function(a,b){return Gk(a.xa,b)}}(this)));return this};Nz.prototype.$classData=l({fv:0},!1,"scalacss.internal.CssKV$",{fv:1,c:1,k:1,h:1});var Oz=void 0;
function Kn(){Oz||(Oz=(new Nz).b());return Oz}function Pz(){this.Y=null}Pz.prototype=new n;Pz.prototype.constructor=Pz;Pz.prototype.b=function(){Qz=this;var a=B(),b=(new Rz).Ag(a,a,a,a,a,a,a,a),c=B(),a=new Sz,e=new Tz;e.Uk=c;e.bk=c;e.Kj=c;e.Tj=c;e.Sj=c;e.Rj=c;e.mk=c;e.Nj=c;e.Oj=c;e.ok=c;e.Dk=c;e.Ik=c;e.xg=c;a.uk=b;a.kk=e;for(var e=xl(),c=pl(b),b=El(e),f=c.cb,e=b.f(c.nd),c=Lj(f);c.Pb;)f=c.r(),f=b.f(f),e=e.Xd(f);a.gq=e;this.Y=a;return this};
Pz.prototype.$classData=l({mv:0},!1,"scalacss.internal.Env$",{mv:1,c:1,k:1,h:1});var Qz=void 0;function Uz(){Qz||(Qz=(new Pz).b());return Qz}function Vz(){this.t=null}Vz.prototype=new Vm;Vz.prototype.constructor=Vz;Vz.prototype.b=function(){Um.prototype.i.call(this,"end");return this};Vz.prototype.$classData=l({rv:0},!1,"scalacss.internal.Literal$Typed$end$",{rv:1,pv:1,c:1,qv:1});var Wz=void 0;function jw(){Wz||(Wz=(new Vz).b());return Wz}function iw(){this.t=null}iw.prototype=new Vm;
iw.prototype.constructor=iw;iw.prototype.b=function(){Um.prototype.i.call(this,"start");return this};iw.prototype.$classData=l({sv:0},!1,"scalacss.internal.Literal$Typed$start$",{sv:1,pv:1,c:1,qv:1});var hw=void 0;function Xz(){this.Y=null}Xz.prototype=new n;Xz.prototype.constructor=Xz;Xz.prototype.b=function(){Yz=this;this.Y=Zz(Cl());return this};function Zz(a){var b=Ac().Ec.dc,c=B(),e=Ac().Ec.dc,f=Ac().Ec;return lo(new mo,a,b,c,e,f.dc)}
Xz.prototype.$classData=l({Iv:0},!1,"scalacss.internal.StyleS$",{Iv:1,c:1,k:1,h:1});var Yz=void 0;function $z(){xp.call(this);this.Af=null}$z.prototype=new yp;$z.prototype.constructor=$z;function aA(){}aA.prototype=$z.prototype;$z.prototype.rp=function(a){this.Af=a;xp.prototype.b.call(this);return this};var va=l({vw:0},!1,"java.lang.Boolean",{vw:1,c:1,h:1,Id:1},void 0,void 0,function(a){return"boolean"===typeof a});function bA(){this.t=0}bA.prototype=new n;bA.prototype.constructor=bA;
bA.prototype.o=function(a){return Bu(a)?this.t===a.t:!1};bA.prototype.m=function(){return g.String.fromCharCode(this.t)};function tu(a){var b=new bA;b.t=a;return b}bA.prototype.s=function(){return this.t};function Bu(a){return!!(a&&a.$classData&&a.$classData.p.Dp)}bA.prototype.$classData=l({Dp:0},!1,"java.lang.Character",{Dp:1,c:1,h:1,Id:1});function Vx(){this.Up=null;this.Q=0}Vx.prototype=new n;Vx.prototype.constructor=Vx;Vx.prototype.b=function(){return this};
function Wx(a){if(0===(16&a.Q)<<24>>24&&0===(16&a.Q)<<24>>24){var b=(new A).g([1632,1776,1984,2406,2534,2662,2790,2918,3046,3174,3302,3430,3664,3792,3872,4160,4240,6112,6160,6470,6608,6784,6800,6992,7088,7232,7248,42528,43216,43264,43472,43600,44016,65296,66720,69734,69872,69942,70096,71360,120782,120792,120802,120812,120822]),c=b.ya.length|0,c=k(r(Za),[c]),e;e=0;for(b=ji(new ki,b,0,b.ya.length|0);b.u();){var f=b.r();c.e[e]=f|0;e=1+e|0}a.Up=c;a.Q=(16|a.Q)<<24>>24}return a.Up}
Vx.prototype.$classData=l({xw:0},!1,"java.lang.Character$",{xw:1,c:1,k:1,h:1});var Ux=void 0;function cA(){this.Aa=null}cA.prototype=new Gw;cA.prototype.constructor=cA;function dA(){}dA.prototype=cA.prototype;function eA(){this.Aa=null}eA.prototype=new Gw;eA.prototype.constructor=eA;function fA(){}fA.prototype=eA.prototype;function gA(){}gA.prototype=new n;gA.prototype.constructor=gA;gA.prototype.b=function(){return this};function Tx(a){throw(new hA).i('For input string: "'+a+'"');}
function Ft(a,b){a=b-(1431655765&b>>1)|0;a=(858993459&a)+(858993459&a>>2)|0;return ca(16843009,252645135&(a+(a>>4)|0))>>24}gA.prototype.$classData=l({Ew:0},!1,"java.lang.Integer$",{Ew:1,c:1,k:1,h:1});var iA=void 0;function hs(){iA||(iA=(new gA).b());return iA}function Iu(){this.Q=!1}Iu.prototype=new n;Iu.prototype.constructor=Iu;Iu.prototype.b=function(){return this};Iu.prototype.$classData=l({Iw:0},!1,"java.lang.Long$",{Iw:1,c:1,k:1,h:1});var Hu=void 0;function jA(){}jA.prototype=new n;
jA.prototype.constructor=jA;function kA(){}d=kA.prototype=jA.prototype;d.uo=function(a){var b=Or();a=a.rc();b=Rr(gt(b,a));for(a=!0;a&&b.u();)a=b.r(),a=this.O(a);return a};d.m=function(){var a=Or(),b=this.rc();return Rr(gt(a,b)).Mf("[",",","]")};d.O=function(a){for(var b=Or(),c=this.rc(),b=Rr(gt(b,c)),c=!1;!c&&b.u();)c=b.r(),c=null===a?null===c:za(a,c);return c};d.Ee=function(a){a:{var b=this.rc();for(;;)if(b.u()){var c=b.r();if(null===c?null===a:za(c,a)){b.Ui();a=!0;break a}}else{a=!1;break a}}return a};
d.se=function(){throw(new Jw).b();};function lA(){this.Pa=null}lA.prototype=new n;lA.prototype.constructor=lA;lA.prototype.Ui=function(){throw(new Jw).b();};lA.prototype.r=function(){return this.Pa.r()};lA.prototype.u=function(){return this.Pa.u()};lA.prototype.$classData=l({ax:0},!1,"java.util.Collections$UnmodifiableIterator",{ax:1,c:1,fD:1,em:1});function mA(){Lx.call(this);this.sa=null}mA.prototype=new Mx;mA.prototype.constructor=mA;
mA.prototype.$classData=l({lx:0},!1,"java.util.HashMap$EntrySet$$anon$2",{lx:1,jD:1,c:1,em:1});function nA(){this.bl=this.Eg=null}nA.prototype=new n;nA.prototype.constructor=nA;nA.prototype.m=function(){return this.bl};nA.prototype.$classData=l({Ax:0},!1,"java.util.regex.Pattern",{Ax:1,c:1,k:1,h:1});function oA(){this.zp=this.Ap=null}oA.prototype=new n;oA.prototype.constructor=oA;
oA.prototype.b=function(){pA=this;this.Ap=new g.RegExp("^\\\\Q(.|\\n|\\r)\\\\E$");this.zp=new g.RegExp("^\\(\\?([idmsuxU]*)(?:-([idmsuxU]*))?\\)");return this};
function Kc(a,b){a=a.Ap.exec(b);if(null!==a){a=a[1];if(void 0===a)throw(new Mw).i("undefined.get");a=(new H).n((new I).d(qA(a),0))}else a=B();if(a.l()){var c=Jc().zp.exec(b);if(null!==c){a=c[0];if(void 0===a)throw(new Mw).i("undefined.get");a=b.substring(a.length|0);var e=c[1];if(void 0===e)var f=0;else{var e=(new zh).i(e),f=e.bb.length|0,h=0,m=0;a:for(;;){if(h!==f){var q=1+h|0,h=e.eb(h),h=null===h?0:h.t,m=m|0|rA(Jc(),h),h=q;continue a}break}f=m|0}c=c[2];if(void 0===c)c=f;else{c=(new zh).i(c);e=c.bb.length|
0;q=0;h=f;a:for(;;){if(q!==e){f=1+q|0;q=c.eb(q);q=null===q?0:q.t;h=(h|0)&~rA(Jc(),q);q=f;continue a}break}c=h|0}a=(new H).n((new I).d(a,c))}else a=B()}a=a.l()?(new I).d(b,0):a.P();if(null===a)throw(new x).n(a);c=a.U|0;a=new g.RegExp(a.R,"g"+(0!==(2&c)?"i":"")+(0!==(8&c)?"m":""));c=new nA;c.Eg=a;c.bl=b;return c}
function qA(a){for(var b="",c=0;c<(a.length|0);){var e=65535&(a.charCodeAt(c)|0);switch(e){case 92:case 46:case 40:case 41:case 91:case 93:case 123:case 125:case 124:case 63:case 42:case 43:case 94:case 36:e="\\"+tu(e);break;default:e=tu(e)}b=""+b+e;c=1+c|0}return b}function rA(a,b){switch(b){case 105:return 2;case 100:return 1;case 109:return 8;case 115:return 32;case 117:return 64;case 120:return 4;case 85:return 256;default:throw(new Ob).i("bad in-pattern flag");}}
oA.prototype.$classData=l({Bx:0},!1,"java.util.regex.Pattern$",{Bx:1,c:1,k:1,h:1});var pA=void 0;function Jc(){pA||(pA=(new oA).b());return pA}function sA(){this.Zo=this.Zp=null}sA.prototype=new gq;sA.prototype.constructor=sA;sA.prototype.b=function(){tA=this;this.Zp=(new Vr).n(Rp().Yp);this.Zo=(new Vr).n(Rp().Xo);(new Vr).n(null);return this};sA.prototype.$classData=l({Gx:0},!1,"scala.Console$",{Gx:1,pD:1,c:1,zD:1});var tA=void 0;function up(){tA||(tA=(new sA).b());return tA}function uA(){}
uA.prototype=new n;uA.prototype.constructor=uA;uA.prototype.b=function(){return this};uA.prototype.Rh=function(a){return null===a?B():(new H).n(a)};uA.prototype.$classData=l({Kx:0},!1,"scala.Option$",{Kx:1,c:1,k:1,h:1});var vA=void 0;function vg(){vA||(vA=(new uA).b());return vA}function wA(){this.zq=this.j=this.hd=null}wA.prototype=new kq;wA.prototype.constructor=wA;
wA.prototype.b=function(){xA=this;Ac();yg();this.hd=Nr();this.j=yA();Kr||(Kr=(new Jr).b());Kr||(Kr=(new Jr).b());zA||(zA=(new AA).b());(new Xx).b();(new BA).b();this.zq=(new CA).b();return this};function DA(a,b){if(!b)throw(new Ob).i("requirement failed");}wA.prototype.$classData=l({Qx:0},!1,"scala.Predef$",{Qx:1,sD:1,c:1,qD:1});var xA=void 0;function J(){xA||(xA=(new wA).b());return xA}function EA(){}EA.prototype=new n;EA.prototype.constructor=EA;EA.prototype.b=function(){return this};
EA.prototype.$classData=l({Vx:0},!1,"scala.StringContext$",{Vx:1,c:1,k:1,h:1});var FA=void 0;function Rq(){}Rq.prototype=new n;Rq.prototype.constructor=Rq;Rq.prototype.b=function(){return this};Rq.prototype.$classData=l({Zx:0},!1,"scala.math.Fractional$",{Zx:1,c:1,k:1,h:1});var Qq=void 0;function Tq(){}Tq.prototype=new n;Tq.prototype.constructor=Tq;Tq.prototype.b=function(){return this};Tq.prototype.$classData=l({$x:0},!1,"scala.math.Integral$",{$x:1,c:1,k:1,h:1});var Sq=void 0;function Vq(){}
Vq.prototype=new n;Vq.prototype.constructor=Vq;Vq.prototype.b=function(){return this};Vq.prototype.$classData=l({ay:0},!1,"scala.math.Numeric$",{ay:1,c:1,k:1,h:1});var Uq=void 0;function Zq(){}Zq.prototype=new n;Zq.prototype.constructor=Zq;Zq.prototype.b=function(){return this};Zq.prototype.$classData=l({Dy:0},!1,"scala.util.Either$",{Dy:1,c:1,k:1,h:1});var Yq=void 0;function ar(){}ar.prototype=new n;ar.prototype.constructor=ar;ar.prototype.b=function(){return this};ar.prototype.m=function(){return"Left"};
ar.prototype.$classData=l({Ey:0},!1,"scala.util.Left$",{Ey:1,c:1,k:1,h:1});var $q=void 0;function cr(){}cr.prototype=new n;cr.prototype.constructor=cr;cr.prototype.b=function(){return this};cr.prototype.m=function(){return"Right"};cr.prototype.$classData=l({Fy:0},!1,"scala.util.Right$",{Fy:1,c:1,k:1,h:1});var br=void 0;function GA(){this.cn=!1}GA.prototype=new n;GA.prototype.constructor=GA;GA.prototype.b=function(){this.cn=!1;return this};
GA.prototype.$classData=l({Iy:0},!1,"scala.util.control.NoStackTrace$",{Iy:1,c:1,k:1,h:1});var HA=void 0;function IA(){this.sh=null}IA.prototype=new n;IA.prototype.constructor=IA;function Ah(a){var b=new IA,c=Jc();IA.prototype.ow.call(b,Kc(c,a));return b}IA.prototype.ow=function(a){this.sh=a;return this};IA.prototype.m=function(){return this.sh.bl};IA.prototype.$classData=l({Ly:0},!1,"scala.util.matching.Regex",{Ly:1,c:1,k:1,h:1});function JA(){this.wa=null}JA.prototype=new sy;
JA.prototype.constructor=JA;JA.prototype.b=function(){ry.prototype.ri.call(this,kl());return this};JA.prototype.Xg=function(){kl();Gm();jl();return(new Hl).b()};JA.prototype.$classData=l({Py:0},!1,"scala.collection.IndexedSeq$$anon$1",{Py:1,Aq:1,c:1,Jk:1});function KA(){}KA.prototype=new n;KA.prototype.constructor=KA;KA.prototype.b=function(){return this};KA.prototype.$classData=l({iz:0},!1,"scala.collection.convert.WrapAsScala$",{iz:1,c:1,ND:1,MD:1});var LA=void 0;function MA(){this.Ra=null}
MA.prototype=new py;MA.prototype.constructor=MA;function NA(){}NA.prototype=MA.prototype;function qy(){this.sa=this.wa=null}qy.prototype=new sy;qy.prototype.constructor=qy;qy.prototype.Xg=function(){return this.sa.ba()};qy.prototype.ri=function(a){if(null===a)throw Dp(ni(),null);this.sa=a;ry.prototype.ri.call(this,a);return this};qy.prototype.$classData=l({rz:0},!1,"scala.collection.generic.GenTraversableFactory$$anon$1",{rz:1,Aq:1,c:1,Jk:1});function OA(){}OA.prototype=new uy;
OA.prototype.constructor=OA;function PA(){}PA.prototype=OA.prototype;function Cq(){}Cq.prototype=new n;Cq.prototype.constructor=Cq;Cq.prototype.b=function(){return this};Cq.prototype.m=function(){return"::"};Cq.prototype.$classData=l({vz:0},!1,"scala.collection.immutable.$colon$colon$",{vz:1,c:1,k:1,h:1});var Bq=void 0;function Nq(){}Nq.prototype=new n;Nq.prototype.constructor=Nq;Nq.prototype.b=function(){return this};
Nq.prototype.$classData=l({cA:0},!1,"scala.collection.immutable.Range$",{cA:1,c:1,k:1,h:1});var Mq=void 0;function QA(){this.wa=null}QA.prototype=new sy;QA.prototype.constructor=QA;QA.prototype.b=function(){ry.prototype.ri.call(this,Hq());return this};QA.prototype.$classData=l({oA:0},!1,"scala.collection.immutable.Stream$StreamCanBuildFrom",{oA:1,Aq:1,c:1,Jk:1});function Lq(){}Lq.prototype=new n;Lq.prototype.constructor=Lq;Lq.prototype.b=function(){return this};
Lq.prototype.$classData=l({hB:0},!1,"scala.collection.mutable.StringBuilder$",{hB:1,c:1,k:1,h:1});var Kq=void 0;function w(){this.Rb=null}w.prototype=new Dy;w.prototype.constructor=w;w.prototype.V=function(){return(0,this.Rb)()};function v(a,b){a.Rb=b;return a}w.prototype.$classData=l({kB:0},!1,"scala.scalajs.runtime.AnonFunction0",{kB:1,dE:1,c:1,LB:1});function u(){this.Rb=null}u.prototype=new Fy;u.prototype.constructor=u;u.prototype.f=function(a){return(0,this.Rb)(a)};
function t(a,b){a.Rb=b;return a}u.prototype.$classData=l({lB:0},!1,"scala.scalajs.runtime.AnonFunction1",{lB:1,dr:1,c:1,T:1});function RA(){this.Rb=null}RA.prototype=new Hy;RA.prototype.constructor=RA;function Eb(a){var b=new RA;b.Rb=a;return b}function wg(a,b,c){return(0,a.Rb)(b,c)}RA.prototype.$classData=l({mB:0},!1,"scala.scalajs.runtime.AnonFunction2",{mB:1,eE:1,c:1,MB:1});function SA(){this.Rb=null}SA.prototype=new Jy;SA.prototype.constructor=SA;function og(a){var b=new SA;b.Rb=a;return b}
SA.prototype.Hj=function(a,b,c){return(0,this.Rb)(a,b,c)};SA.prototype.$classData=l({nB:0},!1,"scala.scalajs.runtime.AnonFunction3",{nB:1,uB:1,c:1,sr:1});function TA(){this.Rb=null}TA.prototype=new Ly;TA.prototype.constructor=TA;function Ri(a){var b=new TA;b.Rb=a;return b}TA.prototype.$classData=l({oB:0},!1,"scala.scalajs.runtime.AnonFunction4",{oB:1,fE:1,c:1,NB:1});function UA(){this.Fe=0;this.Dj=null}UA.prototype=new n;UA.prototype.constructor=UA;
UA.prototype.b=function(){VA=this;this.Dj=(new hu).df(0,0);return this};function WA(a,b,c){return 0===(-2097152&c)?""+(4294967296*c+ +(b>>>0)):XA(a,b,c,1E9,2)}function Ot(a,b,c){if(c===b>>31){var e=b/1E3|0;a.Fe=e>>31;return e}if(e=0>c){var f=-b|0;b=0!==b?~c:-c|0}else f=b,b=c;0===(-2097152&b)?(f=(4294967296*b+ +(f>>>0))/1E3,a.Fe=f/4294967296|0,f|=0):f=XA(a,f,b,1E3,0)|0;if(!1===e)return f;e=a.Fe;a.Fe=0!==f?~e:-e|0;return-f|0}
function $u(a,b,c){return 0>c?-(4294967296*+((0!==b?~c:-c|0)>>>0)+ +((-b|0)>>>0)):4294967296*c+ +(b>>>0)}function Gb(a,b,c){return c===b>>31?""+b:0>c?"-"+WA(a,-b|0,0!==b?~c:-c|0):WA(a,b,c)}
function XA(a,b,c,e,f){var h=(32+da(e)|0)-(0!==c?da(c):32+da(b)|0)|0,m=h,q=0===(32&m)?e<<m:0,y=0===(32&m)?(e>>>1|0)>>>(31-m|0)|0|0<<m:e<<m,m=b,K=c;for(b=c=0;0<=h&&0!==(-2097152&K);){var z=m,E=K,C=q,F=y;if(E===F?(-2147483648^z)>=(-2147483648^C):(-2147483648^E)>=(-2147483648^F))z=K,E=y,K=m-q|0,z=(-2147483648^K)>(-2147483648^m)?-1+(z-E|0)|0:z-E|0,m=K,K=z,32>h?c|=1<<h:b|=1<<h;h=-1+h|0;z=y>>>1|0;q=q>>>1|0|y<<31;y=z}h=K;if(0===h?(-2147483648^m)>=(-2147483648^e):-2147483648<=(-2147483648^h))h=4294967296*
K+ +(m>>>0),e=0+ +(e>>>0),1!==f&&(z=h/e,q=z/4294967296|0,y=c,c=z=y+(z|0)|0,b=(-2147483648^z)<(-2147483648^y)?1+(b+q|0)|0:b+q|0),0!==f&&(e=h%e,m=e|0,K=e/4294967296|0);if(0===f)return a.Fe=b,c;if(1===f)return a.Fe=K,m;a=""+m;return""+(4294967296*b+ +(c>>>0))+"000000000".substring(a.length|0)+a}UA.prototype.$classData=l({rB:0},!1,"scala.scalajs.runtime.RuntimeLong$",{rB:1,c:1,k:1,h:1});var VA=void 0;function Qa(){VA||(VA=(new UA).b());return VA}function YA(){}YA.prototype=new n;
YA.prototype.constructor=YA;function ZA(){}ZA.prototype=YA.prototype;YA.prototype.f=function(a){return this.wf(a,qq().Uo)};YA.prototype.m=function(){return"\x3cfunction1\x3e"};YA.prototype.Dd=function(a){return $A(this,a)};var aB=l({zB:0},!1,"scala.runtime.Nothing$",{zB:1,ab:1,c:1,h:1});function bB(){$z.call(this);this.gj=this.dp=null}bB.prototype=new aA;bB.prototype.constructor=bB;
bB.prototype.b=function(){var a=cB();$z.prototype.rp.call(this,cp(new go,a.Fl,yw(),a.El));dB=this;var a=Aw(this),b=a.wa.Af,c=Pm().Lb;Nm||(Nm=(new Om).b());this.dp=ep(b,Qm(c),a.wa.ah);a=Cw(this);Bp(this);Bp(this);eB||(eB=(new fB).b());b=eB;Bp(this);Bp(this);gB||(gB=(new hB).b());c=iB();Bp(this);var e=Bp(this),e=Sn(Vn(),t(new u,function(){return function(a){return a.zc()}}(e)));Bp(this);var f=Vn(),c=Qn(On(e,(new Pn).Mb(t(new u,function(){return function(a){return a}}(f)))),c).t,b=Km(new Hm,jB(Nj(b.te,
c)));Bp(this);Bp(this);cw||(cw=(new bw).b());c=Km(new Hm,jB(Nj(cw.Qb,this.dp.Lb)));Bp(this);Bp(this);kB||(kB=(new lB).b());e=Km(new Hm,jB(Nj(kB.hg,"white")));Bp(this);Bp(this);gw||(gw=(new fw).b());c=[b,c,e,Km(new Hm,jB(Nj(gw.Qb,"center")))];cB();e=Gz();f=mB();if(0===(c.length|0))b=f.Mh.Y;else{zq();nB();for(var b=[],h=0,m=c.length|0;h<m;)b.push(c[h].Aa),h=1+h|0;c=function(a,b){return function(a,c){return oB(b,a,c)}}(f,e);if(0<(b.length|0))a:for(e=b.length|0,f=1,m=b[0];;){if(f!==e){h=1+f|0;m=c(m,b[f]);
f=h;continue a}b=m;break}else{if(0===(b.length|0))throw(new Jw).i("empty.reduceLeft");e=!1;e=!0;f=null;h=f=0;for(m=b.length|0;h<m;){var q=b[h];e?(f=q,e=!1):f=c(f,q);h=1+h|0}b=f}}b=io(a.wa.Af,a.qa,b,a.wa.ah);this.gj=hp(a.wa.Af,b,a.wa.ah);return this};bB.prototype.$classData=l({yr:0},!1,"components.Title$Style$",{yr:1,bw:1,$v:1,c:1,tv:1});var dB=void 0;function qd(){dB||(dB=(new bB).b());return dB}function pB(){this.t=this.Rc=this.xa=null}pB.prototype=new n;pB.prototype.constructor=pB;
pB.prototype.b=function(){qB=this;Jv||(Jv=(new Iv).b());this.xa=Jv;(new vz).i("onChange");(new vz).i("onClick");(new vz).i("onClickCapture");this.Rc=(rB(),(new Gv).i("src"));rB();(new Gv).i("title");rB();(new Gv).i("type");this.t=(rB(),(new Gv).i("value"));return this};pB.prototype.$classData=l({Bt:0},!1,"japgolly.scalajs.react.vdom.HtmlAttrAndStyles$",{Bt:1,c:1,ZB:1,$B:1,aC:1});var qB=void 0;function sB(){}sB.prototype=new n;sB.prototype.constructor=sB;function tB(){}tB.prototype=sB.prototype;
sB.prototype.Mj=function(){};function uB(){$z.call(this);this.gj=null}uB.prototype=new aA;uB.prototype.constructor=uB;
uB.prototype.b=function(){var a=cB();$z.prototype.rp.call(this,cp(new go,a.Fl,yw(),a.El));vB=this;a=Cw(this);Bp(this);Bp(this);var b;wB||(wB=(new xB).b());b=wB;Bp(this);Bp(this);var c=(new yB).n(100);Bp(this);var e=Bp(this),e=Sn(Vn(),t(new u,function(){return function(a){return a.zc()}}(e)));Bp(this);var f=Vn(),c=Qn(On(e,(new Pn).Mb(t(new u,function(){return function(a){return a}}(f)))),c).t;b=Km(new Hm,jB(Nj(b.te,c)));Bp(this);Bp(this);zB||(zB=(new AB).b());c=zB;Bp(this);Bp(this);e=(new yB).n(100);
Bp(this);f=Bp(this);f=Sn(Vn(),t(new u,function(){return function(a){return a.zc()}}(f)));Bp(this);var h=Vn(),e=Qn(On(f,(new Pn).Mb(t(new u,function(){return function(a){return a}}(h)))),e).t,c=Km(new Hm,jB(Nj(c.te,e)));Bp(this);Bp(this);BB||(BB=(new CB).b());e=BB;f=Bp(this).Sp(Tl(Ul((new A).g(["rgb(",",",",",")"])),(new A).g([79,112,165]))).t;e=Km(new Hm,jB(Nj(e.hg,f)));Bp(this);Bp(this);Zv||(Zv=(new Yv).b());f=Km(new Hm,jB(Nj(Zv.Qb,"flex")));Bp(this);Bp(this);aw||(aw=(new $v).b());h=Km(new Hm,jB(Nj(aw.Qb,
"column")));Bp(this);Bp(this);ew||(ew=(new dw).b());c=[b,c,e,f,h,Km(new Hm,jB(Nj(ew.Qb,"center")))];cB();e=Gz();f=mB();if(0===(c.length|0))b=f.Mh.Y;else{zq();nB();b=[];for(var h=0,m=c.length|0;h<m;)b.push(c[h].Aa),h=1+h|0;c=function(a,b){return function(a,c){return oB(b,a,c)}}(f,e);if(0<(b.length|0))a:for(e=b.length|0,f=1,m=b[0];;){if(f!==e){h=1+f|0;m=c(m,b[f]);f=h;continue a}b=m;break}else{if(0===(b.length|0))throw(new Jw).i("empty.reduceLeft");e=!1;e=!0;f=null;h=f=0;for(m=b.length|0;h<m;){var q=
b[h];e?(f=q,e=!1):f=c(f,q);h=1+h|0}b=f}}b=io(a.wa.Af,a.qa,b,a.wa.ah);this.gj=hp(a.wa.Af,b,a.wa.ah);return this};uB.prototype.$classData=l({mu:0},!1,"pages.HomePage$Style$",{mu:1,bw:1,$v:1,c:1,tv:1});var vB=void 0;function zj(){vB||(vB=(new uB).b());return vB}function fB(){this.te=null}fB.prototype=new sw;fB.prototype.constructor=fB;fB.prototype.b=function(){eB=this;this.te=Ak("font-size");return this};fB.prototype.ue=function(){return this.te};
fB.prototype.$classData=l({Hu:0},!1,"scalacss.internal.Attrs$fontSize$",{Hu:1,Cj:1,Se:1,c:1,eo:1});var eB=void 0;function DB(){this.te=null}DB.prototype=new sw;DB.prototype.constructor=DB;function EB(){}EB.prototype=DB.prototype;DB.prototype.b=function(){this.te=this.mo().f(wl());return this};DB.prototype.ue=function(){return this.te};var na=l({gw:0},!1,"java.lang.String",{gw:1,c:1,h:1,bm:1,Id:1},void 0,void 0,function(a){return"string"===typeof a});function Nt(){this.Aa=null}Nt.prototype=new dA;
Nt.prototype.constructor=Nt;Nt.prototype.n=function(a){Fw.prototype.Sa.call(this,""+a,0,0,!0);return this};Nt.prototype.$classData=l({uw:0},!1,"java.lang.AssertionError",{uw:1,cD:1,ab:1,c:1,h:1});
var pa=l({ww:0},!1,"java.lang.Byte",{ww:1,Dg:1,c:1,h:1,Id:1},void 0,void 0,function(a){return oa(a)}),ua=l({Aw:0},!1,"java.lang.Double",{Aw:1,Dg:1,c:1,h:1,Id:1},void 0,void 0,function(a){return"number"===typeof a}),ta=l({Bw:0},!1,"java.lang.Float",{Bw:1,Dg:1,c:1,h:1,Id:1},void 0,void 0,function(a){return"number"===typeof a}),sa=l({Dw:0},!1,"java.lang.Integer",{Dw:1,Dg:1,c:1,h:1,Id:1},void 0,void 0,function(a){return Oa(a)}),ya=l({Hw:0},!1,"java.lang.Long",{Hw:1,Dg:1,c:1,h:1,Id:1},void 0,void 0,function(a){return xa(a)});
function FB(){this.Aa=null}FB.prototype=new fA;FB.prototype.constructor=FB;function GB(){}GB.prototype=FB.prototype;FB.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};FB.prototype.$classData=l({qb:0},!1,"java.lang.RuntimeException",{qb:1,tb:1,ab:1,c:1,h:1});var ra=l({Mw:0},!1,"java.lang.Short",{Mw:1,Dg:1,c:1,h:1,Id:1},void 0,void 0,function(a){return qa(a)});function lu(){this.ve=null}lu.prototype=new n;lu.prototype.constructor=lu;d=lu.prototype;
d.b=function(){lu.prototype.Tl.call(this,(new HB).b());return this};d.Rm=function(a,b){return this.ve.Da.substring(a,b)};d.m=function(){return this.ve.Da};d.rl=function(a){var b=this.ve;b.Da=""+b.Da+a;return this};function Sx(a,b){a=a.ve;a.Da=""+a.Da+b}d.aa=function(){return this.ve.aa()};d.Tl=function(a){this.ve=a;return this};d.$classData=l({Nw:0},!1,"java.lang.StringBuffer",{Nw:1,c:1,bm:1,Bp:1,h:1});function HB(){this.Da=null}HB.prototype=new n;HB.prototype.constructor=HB;d=HB.prototype;
d.b=function(){this.Da="";return this};d.Rm=function(a,b){return this.Da.substring(a,b)};d.m=function(){return this.Da};d.rl=function(a){this.Da=""+this.Da+a;return this};d.ob=function(a){HB.prototype.b.call(this);if(0>a)throw(new IB).b();return this};d.aa=function(){return this.Da.length|0};function zn(a,b){b=g.String.fromCharCode(b);a.Da=""+a.Da+b}d.i=function(a){HB.prototype.b.call(this);if(null===a)throw(new Ic).b();this.Da=a;return this};
d.$classData=l({Ow:0},!1,"java.lang.StringBuilder",{Ow:1,c:1,bm:1,Bp:1,h:1});function Ox(){this.t=this.xa=null}Ox.prototype=new n;Ox.prototype.constructor=Ox;function JB(){}JB.prototype=Ox.prototype;Ox.prototype.o=function(a){return Up(Wp(),this,a)};Ox.prototype.d=function(a,b){this.xa=a;this.t=b;return this};Ox.prototype.m=function(){return this.xa+"\x3d"+this.t};Ox.prototype.s=function(){return Tp(Wp(),this)};function Qr(){}Qr.prototype=new Iw;Qr.prototype.constructor=Qr;Qr.prototype.b=function(){return this};
Qr.prototype.be=function(){var a=cq();0===(1&a.Q)<<24>>24&&0===(1&a.Q)<<24>>24&&(a.hn=(new KB).dk((new LB).b()),a.Q=(1|a.Q)<<24>>24);return a.hn};Qr.prototype.$classData=l({Ww:0},!1,"java.util.Collections$$anon$17",{Ww:1,Rw:1,c:1,fm:1,h:1});function MB(){this.Pa=null;this.Vj=!1}MB.prototype=new n;MB.prototype.constructor=MB;function NB(){}d=NB.prototype=MB.prototype;d.xk=function(){throw(new Jw).b();};d.o=function(a){return this.Pa.o(a)};d.m=function(){return this.Pa.m()};d.mi=function(a){return this.Pa.mi(a)};
d.Zh=function(a){return this.Pa.Zh(a)};d.be=function(){var a=this.Pa.be();return(new OB).dk(a)};d.v=function(){return this.Pa.v()};d.Ck=function(a){if(this.No||this.Pa.Zh(a))throw(new Jw).b();return null};d.Wl=function(a){this.Pa=a;this.Vj=!0;return this};d.s=function(){return this.Pa.s()};function pu(){this.bg=this.af=null;this.vi=!1}pu.prototype=new n;pu.prototype.constructor=pu;d=pu.prototype;d.b=function(){pu.prototype.nw.call(this,null);return this};
function PB(a,b,c){null===a.af?a.bg=""+a.bg+b+c:QB(a,[b,c])}function Lu(a,b,c){b=a.toExponential(b);a=0===a&&0>1/a?"-"+b:b;b=a.length|0;a=101!==(65535&(a.charCodeAt(-3+b|0)|0))?a:a.substring(0,-1+b|0)+"0"+a.substring(-1+b|0);if(!c||0<=(a.indexOf(".")|0))return a;c=a.indexOf("e")|0;return a.substring(0,c)+"."+a.substring(c)}function RB(a,b){for(var c="",e=0;e!==b;)c=""+c+a,e=1+e|0;return c}function Pu(a,b,c,e){var f=e.length|0;f>=c?ru(a,e):0!==(1&b)?PB(a,e,RB(" ",c-f|0)):PB(a,RB(" ",c-f|0),e)}
function Ju(a,b){return 0!==(256&a)?b.toUpperCase():b}d.m=function(){if(this.vi)throw(new qu).b();return null===this.af?this.bg:this.af.m()};function Ou(a){return(0!==(1&a)?"-":"")+(0!==(2&a)?"#":"")+(0!==(4&a)?"+":"")+(0!==(8&a)?" ":"")+(0!==(16&a)?"0":"")+(0!==(32&a)?",":"")+(0!==(64&a)?"(":"")+(0!==(128&a)?"\x3c":"")}d.nw=function(a){this.af=a;this.bg="";this.vi=!1;return this};function vu(a,b){if(void 0===a)return b;a=+g.parseInt(a,10);return 2147483647>=a?Ia(a):-1}
function SB(a,b,c,e){null===a.af?a.bg=a.bg+(""+b+c)+e:QB(a,[b,c,e])}function Gu(a,b,c,e,f){var h=(e.length|0)+(f.length|0)|0;h>=c?PB(a,e,f):0!==(16&b)?SB(a,e,RB("0",c-h|0),f):0!==(1&b)?SB(a,e,f,RB(" ",c-h|0)):SB(a,RB(" ",c-h|0),e,f)}function Ku(a,b,c,e){Pu(a,b,c,Ju(b,e!==e?"NaN":0<e?0!==(4&b)?"+Infinity":0!==(8&b)?" Infinity":"Infinity":0!==(64&b)?"(Infinity)":"-Infinity"))}
function Du(a,b,c,e,f,h){if(null===b)zu(a,e,f,h,"null");else{a=new TB;b=la(b);a.zf=c;a.lo=b;Fw.prototype.Sa.call(a,null,0,0,!0);if(null===b)throw(new Ic).b();throw a;}}function QB(a,b){try{for(var c=0,e=b.length|0;c<e;)a.af.rl(b[c]),c=1+c|0}catch(f){if(!(f&&f.$classData&&f.$classData.p.iu))throw f;}}function Mu(a,b,c){b=a.toFixed(b);a=0===a&&0>1/a?"-"+b:b;return c&&0>(a.indexOf(".")|0)?a+".":a}function zu(a,b,c,e,f){e=0>e?f:f.substring(0,e);Pu(a,b,c,Ju(b,e))}
function Eu(a){throw(new Nu).i(Ou(a));}function ru(a,b){null===a.af?a.bg=""+a.bg+b:QB(a,[b])}function yu(a,b,c){a=Ou(a&c);c=new UB;c.Hf=a;c.zf=b;Fw.prototype.Sa.call(c,null,0,0,!0);if(null===a)throw(new Ic).b();throw c;}
function Fu(a,b,c,e){if((e.length|0)>=c&&0===(108&b))ru(a,Ju(b,e));else if(0===(124&b))zu(a,b,c,-1,e);else{if(45!==(65535&(e.charCodeAt(0)|0)))var f=0!==(4&b)?"+":0!==(8&b)?" ":"";else 0!==(64&b)?(e=e.substring(1)+")",f="("):(e=e.substring(1),f="-");if(0!==(32&b)){for(var h=e.length|0,m=0;;){if(m!==h)var q=65535&(e.charCodeAt(m)|0),q=48<=q&&57>=q;else q=!1;if(q)m=1+m|0;else break}m=-3+m|0;if(!(0>=m)){for(h=e.substring(m);3<m;)q=-3+m|0,h=e.substring(q,m)+","+h,m=q;e=e.substring(0,m)+","+h}}Gu(a,b,
c,f,Ju(b,e))}}d.Mj=function(){if(!this.vi&&null!==this.af){var a=this.af;if(a&&a.$classData&&a.$classData.p.nl)try{a.Mj()}catch(b){if(!(b&&b.$classData&&b.$classData.p.iu))throw b;}}this.vi=!0};d.$classData=l({gx:0},!1,"java.util.Formatter",{gx:1,c:1,nl:1,Cp:1,En:1});function VB(){}VB.prototype=new iq;VB.prototype.constructor=VB;VB.prototype.b=function(){return this};
function WB(a,b,c,e,f,h){a=la(b);var m;if(m=!!a.Eb.isArrayClass)m=la(e),m.Eb.isPrimitive||a.Eb.isPrimitive?a=m===a||(m===ma(Ya)?a===ma(Xa):m===ma(Za)?a===ma(Xa)||a===ma(Ya):m===ma(ab)?a===ma(Xa)||a===ma(Ya)||a===ma(Za):m===ma(bb)&&(a===ma(Xa)||a===ma(Ya)||a===ma(Za)||a===ma(ab))):(a=a.Eb.getFakeInstance(),a=!!m.Eb.isInstance(a)),m=a;if(m)Na(b,c,e,f,h);else for(a=c,c=c+h|0;a<c;){rl();h=f;rl();m=b;var q=a;if(Jb(m,1)||gb(m,1)||jb(m,1)||hb(m,1)||ib(m,1))m=m.e[q];else if(db(m,1))m=tu(m.e[q]);else if(eb(m,
1)||fb(m,1)||cb(m,1)||ev(m))m=m.e[q];else{if(null===m)throw(new Ic).b();throw(new x).n(m);}fv(0,e,h,m);a=1+a|0;f=1+f|0}}VB.prototype.$classData=l({Fx:0},!1,"scala.Array$",{Fx:1,rD:1,c:1,k:1,h:1});var XB=void 0;function YB(){XB||(XB=(new VB).b());return XB}function ZB(){}ZB.prototype=new n;ZB.prototype.constructor=ZB;function $B(){}$B.prototype=ZB.prototype;ZB.prototype.m=function(){return"\x3cfunction1\x3e"};
ZB.prototype.Dd=function(a){return t(new u,function(a,c){return function(e){return c.f(a.f(e))}}(this,a))};function aC(){}aC.prototype=new n;aC.prototype.constructor=aC;function bC(){}bC.prototype=aC.prototype;aC.prototype.m=function(){return"\x3cfunction1\x3e"};aC.prototype.Dd=function(a){return t(new u,function(a,c){return function(e){return c.f(a.f(e))}}(this,a))};function Pq(){}Pq.prototype=new n;Pq.prototype.constructor=Pq;Pq.prototype.b=function(){return this};
Pq.prototype.$classData=l({Yx:0},!1,"scala.math.Equiv$",{Yx:1,c:1,AD:1,k:1,h:1});var Oq=void 0;function Xq(){}Xq.prototype=new n;Xq.prototype.constructor=Xq;Xq.prototype.b=function(){return this};Xq.prototype.$classData=l({dy:0},!1,"scala.math.Ordering$",{dy:1,c:1,BD:1,k:1,h:1});var Wq=void 0;function AA(){}AA.prototype=new n;AA.prototype.constructor=AA;AA.prototype.b=function(){return this};AA.prototype.m=function(){return"\x3c?\x3e"};
AA.prototype.$classData=l({yy:0},!1,"scala.reflect.NoManifest$",{yy:1,c:1,ad:1,k:1,h:1});var zA=void 0;function cC(){}cC.prototype=new n;cC.prototype.constructor=cC;function dC(){}d=dC.prototype=cC.prototype;d.Ba=function(){return this};d.rf=function(){return this};d.l=function(){return!this.u()};d.Mf=function(a,b,c){return Oj(this,a,b,c)};d.m=function(){return ay(this)};d.M=function(a){Jp(this,a)};d.Yc=function(a,b){return ps(this,a,b)};d.Yk=function(a){return cy(this,a)};
d.cg=function(){jl();var a=kl().Qa;return Xl(this,a)};d.v=function(){return qs(this)};d.xc=function(){return ey(this)};d.vf=function(a,b,c,e){return rs(this,a,b,c,e)};d.tf=function(a,b){return ps(this,a,b)};d.ti=function(){return!1};d.Il=function(a){return hy(this,a)};function dy(){this.Jd=this.cb=this.jd=null;this.Bf=!1}dy.prototype=new n;dy.prototype.constructor=dy;d=dy.prototype;d.Ba=function(){return this};d.r=function(){return this.u()?(this.Bf=!1,this.jd.r()):Aq().Y.r()};d.rf=function(){return this};
d.l=function(){return!this.u()};d.Mf=function(a,b,c){return Oj(this,a,b,c)};d.m=function(){return ay(this)};d.M=function(a){Jp(this,a)};d.Yc=function(a,b){return ps(this,a,b)};d.Yk=function(a){var b=new ns;b.nd=a;b.cb=null;null===this.cb?this.cb=b:this.Jd.cb=b;this.Jd=b;null===this.jd&&(this.jd=Aq().Y);return this};d.cg=function(){jl();var a=kl().Qa;return Xl(this,a)};d.v=function(){return qs(this)};
d.u=function(){if(this.Bf)return!0;if(null!==this.jd){if(this.jd.u())return this.Bf=!0;var a;a:for(;;){if(null===this.cb){this.Jd=this.jd=null;a=!1;break a}this.jd=this.cb.nd.V().rf();this.cb=this.cb.cb;c:for(;;){if((a=this.jd)&&a.$classData&&a.$classData.p.xq){a=this.jd;this.jd=a.jd;this.Bf=a.Bf;null!==a.cb&&(a.Jd.cb=this.cb,this.cb=a.cb);continue c}break}if(this.Bf){a=!0;break a}if(this.jd.u()){a=this.Bf=!0;break a}}return a}return!1};d.xc=function(){return ey(this)};
d.vf=function(a,b,c,e){return rs(this,a,b,c,e)};d.tf=function(a,b){return ps(this,a,b)};d.ti=function(){return!1};d.Il=function(a){return hy(this,a)};d.$classData=l({xq:0},!1,"scala.collection.Iterator$ConcatIterator",{xq:1,c:1,vb:1,G:1,F:1});function eC(){}eC.prototype=new n;eC.prototype.constructor=eC;eC.prototype.b=function(){return this};eC.prototype.$classData=l({jz:0},!1,"scala.collection.convert.Wrappers$",{jz:1,c:1,OD:1,k:1,h:1});var fC=void 0;
function dt(){fC||(fC=(new eC).b());return fC}function gC(){}gC.prototype=new ly;gC.prototype.constructor=gC;function hC(){}hC.prototype=gC.prototype;function iC(){}iC.prototype=new PA;iC.prototype.constructor=iC;iC.prototype.b=function(){return this};iC.prototype.Vo=function(){return Cl()};iC.prototype.$classData=l({Uz:0},!1,"scala.collection.immutable.Map$",{Uz:1,sz:1,tz:1,pz:1,c:1});var jC=void 0;function Nr(){jC||(jC=(new iC).b());return jC}function kC(){this.Hg=this.t=this.xa=null}
kC.prototype=new n;kC.prototype.constructor=kC;d=kC.prototype;d.r=function(){return this.Hg};function lC(a){return"(kv: "+a.xa+", "+a.t+")"+(null!==a.Hg?" -\x3e "+lC(a.Hg):"")}d.d=function(a,b){this.xa=a;this.t=b;return this};d.m=function(){return lC(this)};d.pd=function(){return this.xa};d.qh=function(a){this.Hg=a};d.$classData=l({FA:0},!1,"scala.collection.mutable.DefaultEntry",{FA:1,c:1,Im:1,k:1,h:1});function mC(){this.y=this.Y=null}mC.prototype=new n;mC.prototype.constructor=mC;
function nC(a,b){a.Y=b;a.y=b;return a}d=mC.prototype;d.Ob=function(a){this.y.Ob(a);return this};d.Ja=function(){return this.y};d.gd=function(a,b){By(this,a,b)};d.kb=function(a){this.y.Ob(a);return this};d.wc=function(){};d.db=function(a){return jm(this,a)};d.$classData=l({JA:0},!1,"scala.collection.mutable.GrowingBuilder",{JA:1,c:1,Qc:1,Oc:1,Mc:1});function oC(){this.Hg=this.Ce=this.Df=this.xa=null}oC.prototype=new n;oC.prototype.constructor=oC;d=oC.prototype;d.r=function(){return this.Hg};
d.n=function(a){this.xa=a;this.Ce=this.Df=null;return this};d.pd=function(){return this.xa};d.qh=function(a){this.Hg=a};d.$classData=l({ZA:0},!1,"scala.collection.mutable.LinkedHashSet$Entry",{ZA:1,c:1,Im:1,k:1,h:1});function hu(){this.qc=this.jb=0}hu.prototype=new Ew;hu.prototype.constructor=hu;d=hu.prototype;d.o=function(a){return xa(a)?this.jb===a.jb&&this.qc===a.qc:!1};d.Ic=function(a,b,c){hu.prototype.df.call(this,a|b<<22,b>>10|c<<12);return this};d.m=function(){return Gb(Qa(),this.jb,this.qc)};
d.df=function(a,b){this.jb=a;this.qc=b;return this};d.ob=function(a){hu.prototype.df.call(this,a,a>>31);return this};d.s=function(){return this.jb^this.qc};function xa(a){return!!(a&&a.$classData&&a.$classData.p.br)}d.$classData=l({br:0},!1,"scala.scalajs.runtime.RuntimeLong",{br:1,Dg:1,c:1,h:1,Id:1});function pC(){this.Ld=null}pC.prototype=new n;pC.prototype.constructor=pC;d=pC.prototype;d.B=function(){return"Mod"};d.z=function(){return 1};
d.o=function(a){var b;ve();b=this.Ld;a&&a.$classData&&a.$classData.p.kn?(a=null===a?null:a.Ld,b=null===b?null===a:b.o(a)):b=!1;return b};d.A=function(a){a:switch(ve(),a){case 0:a=this.Ld;break a;default:throw(new Y).i(""+a);}return a};d.m=function(){ve();var a=this.Ld;return ql(rl(),(new pC).Mb(a))};d.Mb=function(a){this.Ld=a;return this};d.s=function(){return this.Ld.s()};d.D=function(){ve();return Z((new pC).Mb(this.Ld))};
d.$classData=l({kn:0},!1,"japgolly.scalajs.react.CtorType$Mod",{kn:1,c:1,E:1,q:1,k:1,h:1});function qC(){}qC.prototype=new Fy;qC.prototype.constructor=qC;qC.prototype.b=function(){return this};qC.prototype.f=function(a){return(new pC).Mb(a)};qC.prototype.m=function(){return"Mod"};function ue(a,b,c){b.f(c);return c}qC.prototype.$classData=l({Br:0},!1,"japgolly.scalajs.react.CtorType$Mod$",{Br:1,dr:1,c:1,T:1,k:1,h:1});var rC=void 0;function ve(){rC||(rC=(new qC).b());return rC}
function af(){this.Te=this.mf=this.Ze=this.Ye=this.Xe=this.We=this.Ve=this.Ue=null}af.prototype=new n;af.prototype.constructor=af;d=af.prototype;d.B=function(){return"Lifecycle"};d.z=function(){return 8};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.ln){var b=this.Ue,c=a.Ue;(null===b?null===c:b.o(c))?(b=this.Ve,c=a.Ve,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.We,c=a.We,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.Xe,c=a.Xe,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.Ye,c=a.Ye,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.Ze,c=a.Ze,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.mf,c=a.mf,b=null===b?null===c:b.o(c)):b=!1;if(b)return b=this.Te,a=a.Te,null===b?null===a:b.o(a)}return!1};
d.A=function(a){switch(a){case 0:return this.Ue;case 1:return this.Ve;case 2:return this.We;case 3:return this.Xe;case 4:return this.Ye;case 5:return this.Ze;case 6:return this.mf;case 7:return this.Te;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};function $e(a,b,c,e,f,h,m,q,y){a.Ue=b;a.Ve=c;a.We=e;a.Xe=f;a.Ye=h;a.Ze=m;a.mf=q;a.Te=y;return a}
function jf(a,b,c,e){return b.Ld.f(t(new u,function(a,b,c){return function(e){e.l()?e=b:(e=e.P(),e=t(new u,function(a,b,c,e){return function(f){return wg(c,e.f(f),v(new w,function(a,b,c){return function(){return b.f(c)}}(a,b,f)))}}(a,b,c,e)));return(new H).n(e)}}(a,c,e))).f(a)}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({ln:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle",{ln:1,c:1,E:1,q:1,k:1,h:1});function qz(){this.Rf=this.$c=null}qz.prototype=new n;
qz.prototype.constructor=qz;d=qz.prototype;d.B=function(){return"Resolution"};d.z=function(){return 2};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.vn?D(G(),this.$c,a.$c)?this.Rf===a.Rf:!1:!1};d.A=function(a){switch(a){case 0:return this.$c;case 1:return this.Rf;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.pi=function(a,b){this.$c=a;this.Rf=b;return this};
d.$classData=l({vn:0},!1,"japgolly.scalajs.react.extra.router.Resolution",{vn:1,c:1,E:1,q:1,k:1,h:1});function Dc(){this.sd=this.Pf=this.Od=this.Db=this.ub=this.Gb=null}Dc.prototype=new n;Dc.prototype.constructor=Dc;d=Dc.prototype;d.B=function(){return"RouterConfig"};d.z=function(){return 6};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.xn){var b=this.Gb,c=a.Gb;(null===b?null===c:b.o(c))?(b=this.ub,c=a.ub,b=null===b?null===c:b.o(c)):b=!1;if(b&&this.Db===a.Db&&this.Od===a.Od&&this.Pf===a.Pf)return b=this.sd,a=a.sd,null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.Gb;case 1:return this.ub;case 2:return this.Db;case 3:return this.Od;case 4:return this.Pf;case 5:return this.sd;default:throw(new Y).i(""+a);}};
d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({xn:0},!1,"japgolly.scalajs.react.extra.router.RouterConfig",{xn:1,c:1,E:1,q:1,k:1,h:1});function hh(){this.Db=this.ub=this.Gb=null}hh.prototype=new n;hh.prototype.constructor=hh;d=hh.prototype;d.ih=function(a,b,c){this.Gb=a;this.ub=b;this.Db=c;return this};d.B=function(){return"Rule"};d.z=function(){return 3};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.zn){var b=this.Gb,c=a.Gb;(null===b?null===c:b.o(c))?(b=this.ub,c=a.ub,b=null===b?null===c:b.o(c)):b=!1;return b?this.Db===a.Db:!1}return!1};d.A=function(a){switch(a){case 0:return this.Gb;case 1:return this.ub;case 2:return this.Db;default:throw(new Y).i(""+a);}};
function uc(a){return sC(a,t(new u,function(){return function(a){Mr();a="Unspecified path for page "+a+".";throw Dp(ni(),(new FB).i(a));}}(a)),Eb(function(){return function(a,c){Mr();a="Unspecified action for page "+c+" at "+a+".";throw Dp(ni(),(new FB).i(a));}}(a)))}d.m=function(){return ql(rl(),this)};function sC(a,b,c){var e=a.Gb;b=Oh(Qh(),a.ub,b);Lh||(Lh=(new Jh).b());return(new tC).ih(e,b,Kh(a.Db,c))}
function nc(a,b){var c=Nh(Qh(),a.Gb,b.Gb),e=Qh();return(new hh).ih(c,Nh(e,a.ub,b.ub),Eb(function(a,b){return function(c,e){return wg(a.ub.f(e).l()?b.Db:a.Db,c,e)}}(a,b)))}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({zn:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Rule",{zn:1,c:1,E:1,q:1,k:1,h:1});function tC(){this.Db=this.ub=this.Gb=null}tC.prototype=new n;tC.prototype.constructor=tC;d=tC.prototype;d.ih=function(a,b,c){this.Gb=a;this.ub=b;this.Db=c;return this};
d.B=function(){return"Rules"};d.z=function(){return 3};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.An){var b=this.Gb,c=a.Gb;(null===b?null===c:b.o(c))?(b=this.ub,c=a.ub,b=null===b?null===c:b.o(c)):b=!1;return b?this.Db===a.Db:!1}return!1};d.A=function(a){switch(a){case 0:return this.Gb;case 1:return this.ub;case 2:return this.Db;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};
function yc(a,b){var c=Vy();b=Oh(Qh(),a.Gb,b);var e=new Dc,f=a.ub;a=a.Db;var h=Uy(c),m=Ty(c),c=c.Vp;e.Gb=b;e.ub=f;e.Db=a;e.Od=h;e.Pf=m;e.sd=c;return e}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({An:0},!1,"japgolly.scalajs.react.extra.router.StaticDsl$Rules",{An:1,c:1,E:1,q:1,k:1,h:1});function uC(){}uC.prototype=new tB;uC.prototype.constructor=uC;function vC(){}vC.prototype=uC.prototype;uC.prototype.kw=function(){return this};
function wC(){this.t=this.gg=null}wC.prototype=new n;wC.prototype.constructor=wC;d=wC.prototype;d.B=function(){return"AV"};d.z=function(){return 2};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Fn){var b=this.gg,c=a.gg;return(null===b?null===c:b.o(c))?this.t===a.t:!1}return!1};d.A=function(a){switch(a){case 0:return this.gg;case 1:return this.t;default:throw(new Y).i(""+a);}};function Nj(a,b){var c=new wC;c.gg=a;c.t=b;return c}d.m=function(){return ql(rl(),this)};
d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Fn:0},!1,"scalacss.internal.AV",{Fn:1,c:1,E:1,q:1,k:1,h:1});function AB(){this.te=null}AB.prototype=new EB;AB.prototype.constructor=AB;AB.prototype.b=function(){DB.prototype.b.call(this);return this};AB.prototype.mo=function(){return t(new u,function(){return function(a){return Hk(Bk(),"height",a)}}(this))};AB.prototype.$classData=l({Iu:0},!1,"scalacss.internal.Attrs$height$",{Iu:1,Rv:1,Cj:1,Se:1,c:1,eo:1});
var zB=void 0;function xB(){this.te=null}xB.prototype=new EB;xB.prototype.constructor=xB;xB.prototype.b=function(){DB.prototype.b.call(this);return this};xB.prototype.mo=function(){return t(new u,function(){return function(a){return Hk(Bk(),"width",a)}}(this))};xB.prototype.$classData=l({Lu:0},!1,"scalacss.internal.Attrs$width$",{Lu:1,Rv:1,Cj:1,Se:1,c:1,eo:1});var wB=void 0;function Ez(){this.Ri=this.Cc=null}Ez.prototype=new n;Ez.prototype.constructor=Ez;d=Ez.prototype;d.B=function(){return"Agent"};
d.z=function(){return 2};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Hn&&this.Cc===a.Cc){var b=this.Ri;a=a.Ri;return null===b?null===a:xC(b,a)}return!1};d.A=function(a){switch(a){case 0:return this.Cc;case 1:return this.Ri;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};function Dz(a,b,c){a.Cc=b;a.Ri=c;return a}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({Hn:0},!1,"scalacss.internal.CanIUse$Agent",{Hn:1,c:1,E:1,q:1,k:1,h:1});function jo(){this.t=null}jo.prototype=new n;jo.prototype.constructor=jo;d=jo.prototype;d.B=function(){return"ClassName"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.In?this.t===a.t:!1};d.A=function(a){switch(a){case 0:return this.t;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.i=function(a){this.t=a;return this};
d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({In:0},!1,"scalacss.internal.ClassName",{In:1,c:1,E:1,q:1,k:1,h:1});function zp(){this.t=null}zp.prototype=new n;zp.prototype.constructor=zp;d=zp.prototype;d.B=function(){return"ClassNameHint"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Jn?this.t===a.t:!1};d.A=function(a){switch(a){case 0:return this.t;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};
d.i=function(a){this.t=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Jn:0},!1,"scalacss.internal.ClassNameHint",{Jn:1,c:1,E:1,q:1,k:1,h:1});function Iz(){this.Xi=null}Iz.prototype=new n;Iz.prototype.constructor=Iz;d=Iz.prototype;d.B=function(){return"Compose"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Kn?this.Xi===a.Xi:!1};
d.A=function(a){switch(a){case 0:return this.Xi;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};
function yC(a,b,c,e,f){c=(new Kf).n(c.Eb);jy(new iy,c.H,t(new u,function(){return function(a){return null!==a}}(a))).M(t(new u,function(a,b,c){return function(a){if(null!==a){var e=a.R;a=a.U;var f=b.Eb.sb(e);if(!f.l()){var f=Mj(f.P()),h=yA(),h=ny(h),f=ll(f,h),h=Mj(a);jl();a=(new Hl).b();for(h=Lj(h);h.Pb;){var C=h.r();!0!==f.O(C)&&Il(a,C)}f=Jl(a);Rj(f)?c.H=c.H.Je(e):(a=f.I(),f=mm(f),c.H=c.H.Cb(e,ml(new nl,a,f)))}}else throw(new x).n(a);}}(a,e,c)));return ps(c.H,e,Eb(function(a,b,c){return function(e,
f){f=(new I).d(e,f);e=f.R;var z=f.U;if(null!==z){f=z.R;var z=z.U,E=Uj(e,t(new u,function(a,b){return function(a){return wg(Uk().zo,b,a).so()}}(a,f)));if(B()===E)return Pj(e,f,z);if(If(E)){E=E.J;f=zC(a,c,lw(a.Xi,E,sk(wk(),f,z)),b);z=AC(E.Eb);E=Mj(f.Md);z=Kb(z,E);e=Uj(e,t(new u,function(a,b){return function(a){return!b.O(a)}}(a,z)));if(B()===e)return f;if(If(e))return ps(f.Eb,e.J,Eb(function(){return function(a,b){var c=b.R,e=Qj(a,c),e=Rj(e)?Sj(a.Md,c):a.Md;a=a.Eb.Cb(c,b.U);return Ij(new Hj,e,a)}}(a)));
throw(new x).n(e);}throw(new x).n(E);}throw(new x).n(f);}}(a,f,b)))}function zC(a,b,c,e){c.U.M(t(new u,function(a,b,c){return function(a){b.H=ah(b.H,jp(new kp,c,a))}}(a,e,b)));return c.R}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
function oB(a,b,c){var e=lm(b.sf,c.sf),e=(new Kf).n(e);a=ps(c.Zd,b.Zd,Eb(function(a,b){return function(c,e){e=(new I).d(c,e);c=e.R;var f=e.U;if(null!==f){e=f.R;var f=f.U,z=c.sb(e);z.l()||(z=z.P(),f=yC(a,e,f,z,b));return c.Cb(e,f)}throw(new x).n(e);}}(a,e)));var f=lm(b.qe,c.qe);b=lm(b.Cd,c.Cd);return lo(new mo,a,f,B(),b,e.H)}d.$classData=l({Kn:0},!1,"scalacss.internal.Compose",{Kn:1,c:1,E:1,q:1,k:1,h:1});function BC(){this.t=this.xa=null}BC.prototype=new n;BC.prototype.constructor=BC;
function Gk(a,b){var c=new BC;c.xa=a;c.t=b;return c}d=BC.prototype;d.B=function(){return"CssKV"};d.z=function(){return 2};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Qn?this.xa===a.xa&&this.t===a.t:!1};d.A=function(a){switch(a){case 0:return this.xa;case 1:return this.t;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({Qn:0},!1,"scalacss.internal.CssKV",{Qn:1,c:1,E:1,q:1,k:1,h:1});function Ep(){this.Mh=null}Ep.prototype=new Vv;Ep.prototype.constructor=Ep;function CC(){}CC.prototype=Ep.prototype;Ep.prototype.b=function(){qn||(qn=(new pn).b());nn||(nn=(new mn).b());Fm||(Fm=(new Em).b());Dn||(Dn=(new Cn).b());Yz||(Yz=(new Xz).b());this.Mh=Yz;Fn||(Fn=(new En).b());return this};
function jB(a){var b=[],c;wk();c=a.t;c=sk(0,a.gg,tk(Vj(),c));a=b.length|0;var e=0,f=c;a:for(;;){if(e!==a){c=1+e|0;var h=b[e],e=f,f=h.gg,m=h.t,q=Qj(e,f),h=Rj(q)?Sj(e.Md,f):e.Md,e=e.Eb.Cb(f,kn(Vj(),q,m)),f=Ij(new Hj,h,e),e=c;continue a}b=f;break}a=lp().Y;return Zz((new uk).d(a,b))}Ep.prototype.Sp=function(a){return(new Tn).i(a)};function Sz(){this.gq=this.kk=this.uk=null}Sz.prototype=new n;Sz.prototype.constructor=Sz;d=Sz.prototype;d.B=function(){return"Env"};d.z=function(){return 2};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Sn){var b=this.uk,c=a.uk;if(null===b?null===c:b.o(c))return b=this.kk,a=a.kk,null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.uk;case 1:return this.kk;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Sn:0},!1,"scalacss.internal.Env",{Sn:1,c:1,E:1,q:1,k:1,h:1});
function Tz(){this.xg=this.Ik=this.Dk=this.ok=this.Oj=this.Nj=this.mk=this.Rj=this.Sj=this.Tj=this.Kj=this.bk=this.Uk=null}Tz.prototype=new n;Tz.prototype.constructor=Tz;d=Tz.prototype;d.B=function(){return"Media"};d.z=function(){return 13};
d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Tn?D(G(),this.Uk,a.Uk)&&D(G(),this.bk,a.bk)&&D(G(),this.Kj,a.Kj)&&D(G(),this.Tj,a.Tj)&&D(G(),this.Sj,a.Sj)&&D(G(),this.Rj,a.Rj)&&D(G(),this.mk,a.mk)&&D(G(),this.Nj,a.Nj)&&D(G(),this.Oj,a.Oj)&&D(G(),this.ok,a.ok)&&D(G(),this.Dk,a.Dk)&&D(G(),this.Ik,a.Ik)&&D(G(),this.xg,a.xg):!1};
d.A=function(a){

}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({Tn:0},!1,"scalacss.internal.Env$Media",{Tn:1,c:1,E:1,q:1,k:1,h:1});function Rz(){this.pk=this.Rk=this.wk=this.jk=this.Qj=this.Tk=this.hk=this.qa=null}Rz.prototype=new n;Rz.prototype.constructor=Rz;d=Rz.prototype;d.B=function(){return"Platform"};d.z=function(){return 8};
d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Un?D(G(),this.qa,a.qa)&&D(G(),this.hk,a.hk)&&D(G(),this.Tk,a.Tk)&&D(G(),this.Qj,a.Qj)&&D(G(),this.jk,a.jk)&&D(G(),this.wk,a.wk)&&D(G(),this.Rk,a.Rk)&&D(G(),this.pk,a.pk):!1};d.A=function(a){switch(a){case 0:return this.qa;case 1:return this.hk;case 2:return this.Tk;case 3:return this.Qj;case 4:return this.jk;case 5:return this.wk;case 6:return this.Rk;case 7:return this.pk;default:throw(new Y).i(""+a);}};
d.m=function(){return ql(rl(),this)};d.Ag=function(a,b,c,e,f,h,m,q){this.qa=a;this.hk=b;this.Tk=c;this.Qj=e;this.jk=f;this.wk=h;this.Rk=m;this.pk=q;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Un:0},!1,"scalacss.internal.Env$Platform",{Un:1,c:1,E:1,q:1,k:1,h:1});function Rm(){this.Ng=this.vg=this.ug=this.tg=this.Rc=this.Lb=null}Rm.prototype=new n;Rm.prototype.constructor=Rm;d=Rm.prototype;d.B=function(){return"FontFace"};d.z=function(){return 6};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Vn){if(D(G(),this.Lb,a.Lb))var b=this.Rc,c=a.Rc,b=null===b?null===c:b.o(c);else b=!1;b?(b=this.tg,c=a.tg,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.ug,c=a.ug,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.vg,c=a.vg,b=null===b?null===c:b.o(c)):b=!1;if(b)return b=this.Ng,a=a.Ng,null===b?null===a:b.o(a)}return!1};
d.A=function(a){switch(a){case 0:return this.Lb;case 1:return this.Rc;case 2:return this.tg;case 3:return this.ug;case 4:return this.vg;case 5:return this.Ng;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Vn:0},!1,"scalacss.internal.FontFace",{Vn:1,c:1,E:1,q:1,k:1,h:1});function DC(){this.lj=this.ge=null}DC.prototype=new n;DC.prototype.constructor=DC;d=DC.prototype;d.B=function(){return"Length"};
d.z=function(){return 2};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Xn?D(G(),this.ge,a.ge)?this.lj===a.lj:!1:!1};function iB(){var a=gB,b=new DC;b.ge=20;b.lj=a;return b}d.A=function(a){switch(a){case 0:return this.ge;case 1:return this.lj;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.zc=function(){var a=ka(this.ge);return"0"===a?"0":""+a+this.lj.t};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({Xn:0},!1,"scalacss.internal.Length",{Xn:1,c:1,E:1,q:1,k:1,h:1});function yB(){this.ge=null}yB.prototype=new n;yB.prototype.constructor=yB;d=yB.prototype;d.B=function(){return"Percentage"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.Zn?D(G(),this.ge,a.ge):!1};d.A=function(a){switch(a){case 0:return this.ge;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.zc=function(){return ka(this.ge)+"%"};
d.n=function(a){this.ge=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Zn:0},!1,"scalacss.internal.Percentage",{Zn:1,c:1,E:1,q:1,k:1,h:1});function Bn(){this.di=this.hi=this.yi=this.Ai=this.oh=this.ej=this.Di=this.Ci=this.fj=this.ph=this.Bi=this.zi=null}Bn.prototype=new n;Bn.prototype.constructor=Bn;
function ow(a,b,c){var e=a.ph;b.l()||e.f(b.P());c=Mj(c);jy(new iy,c,t(new u,function(){return function(a){return null!==a}}(a))).M(t(new u,function(a,b){return function(c){if(null!==c){var e=c.R;c=c.U;EC(a,B(),b,e,c)}else throw(new x).n(c);}}(a,b)));a=a.oh;b.l()||a.f(b.P())}d=Bn.prototype;d.B=function(){return"FormatSB"};d.z=function(){return 12};
function nw(a,b){if(qm(b)){var c=b.kf,e=a.ph;c.l()||e.f(c.P());EC(a,B(),b.kf,b.yh,b.mg);b=b.kf;a=a.oh;b.l()||a.f(b.P())}else if(rm(b))a.zi.f(b.qa.t),jy(new iy,b.li,t(new u,function(){return function(a){return null!==a}}(a))).M(t(new u,function(a){return function(b){if(null!==b){var c=b.R,e=b.U;a.Bi.f(c.zc());b=(new H).n(c);for(e=Lj(e);e.Pb;){var y=e.r();FC(a,b,y.kf,y.mg)}a.Ai.f(c)}else throw(new x).n(b);}}(a))),a.yi.f(b.qa);else if(sm(b))a.hi.f(b);else throw(new x).n(b);}
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.$n){var b=this.zi,c=a.zi;(null===b?null===c:b.o(c))?(b=this.Bi,c=a.Bi,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.ph,c=a.ph,b=null===b?null===c:b.o(c)):b=!1;b&&this.fj===a.fj&&this.Ci===a.Ci&&this.Di===a.Di&&this.ej===a.ej?(b=this.oh,c=a.oh,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.Ai,c=a.Ai,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.yi,c=a.yi,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.hi,c=a.hi,b=null===b?null===c:b.o(c)):b=
!1;return b?this.di===a.di:!1}return!1};d.A=function(a){

function An(a,b,c,e,f,h,m,q,y,K,z,E,C){a.zi=b;a.Bi=c;a.ph=e;a.fj=f;a.Ci=h;a.Di=m;a.ej=q;a.oh=y;a.Ai=K;a.yi=z;a.hi=E;a.di=C;return a}function EC(a,b,c,e,f){wg(a.fj,c,e);FC(a,b,c,f);wg(a.ej,c,e)}d.s=function(){return Lm(U(),this)};function FC(a,b,c,e){a.Ci.Hj(b,c,e.nd);for(e=Lj(e.cb);e.Pb;){var f=e.r();a.Di.Hj(b,c,f)}}d.D=function(){return Z(this)};d.$classData=l({$n:0},!1,"scalacss.internal.StringRenderer$FormatSB",{$n:1,c:1,E:1,q:1,k:1,h:1});function np(){this.kp=this.ij=this.Oh=this.kg=null}
np.prototype=new n;np.prototype.constructor=np;d=np.prototype;d.B=function(){return"StyleA"};d.z=function(){return 3};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.ao){var b=this.kg,c=a.kg;(null===b?null===c:b.o(c))?(b=this.Oh,c=a.Oh,b=null===b?null===c:fn(b,c)):b=!1;if(b)return b=this.ij,a=a.ij,null===b?null===a:b.o(a)}return!1};
function mp(a,b,c,e){a.kg=b;a.Oh=c;a.ij=e;b=GC(a);b=(new li).pb(b,t(new u,function(){return function(a){return a.t}}(a)));a.kp=Oj(b,""," ","");return a}d.A=function(a){switch(a){case 0:return this.kg;case 1:return this.Oh;case 2:return this.ij;default:throw(new Y).i(""+a);}};function GC(a){Ac();var b=(new HC).n(a.kg);return cy(b,v(new w,function(a){return function(){return a.Oh}}(a)))}d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({ao:0},!1,"scalacss.internal.StyleA",{ao:1,c:1,E:1,q:1,k:1,h:1});function IC(){}IC.prototype=new Jy;IC.prototype.constructor=IC;IC.prototype.b=function(){return this};IC.prototype.m=function(){return"StyleA"};IC.prototype.Hj=function(a,b,c){return mp(new np,a,b,c)};IC.prototype.$classData=l({Gv:0},!1,"scalacss.internal.StyleA$",{Gv:1,uB:1,c:1,sr:1,k:1,h:1});var JC=void 0;function Tn(){this.t=null}Tn.prototype=new n;Tn.prototype.constructor=Tn;d=Tn.prototype;d.B=function(){return"ValueT"};
d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.co?this.t===a.t:!1};d.A=function(a){switch(a){case 0:return this.t;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.i=function(a){this.t=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({co:0},!1,"scalacss.internal.ValueT",{co:1,c:1,E:1,q:1,k:1,h:1});function KC(){}KC.prototype=new sw;KC.prototype.constructor=KC;function LC(){}
LC.prototype=KC.prototype;KC.prototype.Sp=function(a){return Nj(this.ue(),a)};function kp(){this.Li=this.Yh=null}kp.prototype=new n;kp.prototype.constructor=kp;d=kp.prototype;d.B=function(){return"Warning"};d.z=function(){return 2};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.fo){var b=this.Yh,c=a.Yh;return(null===b?null===c:b.o(c))?this.Li===a.Li:!1}return!1};d.A=function(a){switch(a){case 0:return this.Yh;case 1:return this.Li;default:throw(new Y).i(""+a);}};
function jp(a,b,c){a.Yh=b;a.Li=c;return a}d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({fo:0},!1,"scalacss.internal.package$Warning",{fo:1,c:1,E:1,q:1,k:1,h:1});function MC(){this.El=this.Fl=null}MC.prototype=new n;MC.prototype.constructor=MC;MC.prototype.b=function(){NC=this;qn||(qn=(new pn).b());nn||(nn=(new mn).b());Fm||(Fm=(new Em).b());JC||(JC=(new IC).b());Hz();Uz();this.Fl=(new pp).qi("%s-%04d",1);this.El=sp();return this};
MC.prototype.$classData=l({fw:0},!1,"scalacss.package$DevDefaults$",{fw:1,c:1,nC:1,ou:1,mC:1,TC:1});var NC=void 0;function cB(){NC||(NC=(new MC).b());return NC}l({tw:0},!1,"java.lang.ArithmeticException",{tw:1,qb:1,tb:1,ab:1,c:1,h:1});function Ob(){this.Aa=null}Ob.prototype=new GB;Ob.prototype.constructor=Ob;function OC(){}OC.prototype=Ob.prototype;Ob.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Ob.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};
Ob.prototype.$classData=l({od:0},!1,"java.lang.IllegalArgumentException",{od:1,qb:1,tb:1,ab:1,c:1,h:1});function Lw(){this.Aa=null}Lw.prototype=new GB;Lw.prototype.constructor=Lw;function PC(){}PC.prototype=Lw.prototype;Lw.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Lw.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};Lw.prototype.$classData=l({Ep:0},!1,"java.lang.IllegalStateException",{Ep:1,qb:1,tb:1,ab:1,c:1,h:1});
function Y(){this.Aa=null}Y.prototype=new GB;Y.prototype.constructor=Y;Y.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Y.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};Y.prototype.$classData=l({Cw:0},!1,"java.lang.IndexOutOfBoundsException",{Cw:1,qb:1,tb:1,ab:1,c:1,h:1});function QC(){}QC.prototype=new tB;QC.prototype.constructor=QC;QC.prototype.b=function(){return this};
QC.prototype.$classData=l({Gw:0},!1,"java.lang.JSConsoleBasedPrintStream$DummyOutputStream",{Gw:1,ju:1,c:1,nl:1,Cp:1,En:1});function IB(){this.Aa=null}IB.prototype=new GB;IB.prototype.constructor=IB;IB.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};IB.prototype.$classData=l({Jw:0},!1,"java.lang.NegativeArraySizeException",{Jw:1,qb:1,tb:1,ab:1,c:1,h:1});function Ic(){this.Aa=null}Ic.prototype=new GB;Ic.prototype.constructor=Ic;
Ic.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Ic.prototype.$classData=l({Kw:0},!1,"java.lang.NullPointerException",{Kw:1,qb:1,tb:1,ab:1,c:1,h:1});function Jw(){this.Aa=null}Jw.prototype=new GB;Jw.prototype.constructor=Jw;Jw.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Jw.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};
Jw.prototype.$classData=l({Qw:0},!1,"java.lang.UnsupportedOperationException",{Qw:1,qb:1,tb:1,ab:1,c:1,h:1});function RC(){}RC.prototype=new kA;RC.prototype.constructor=RC;function SC(){}SC.prototype=RC.prototype;RC.prototype.o=function(a){return a===this?!0:a&&a.$classData&&a.$classData.p.lh?a.v()===this.v()&&this.uo(a):!1};RC.prototype.s=function(){var a=Or(),b=this.rc();return Rr(gt(a,b)).Yc(0,Eb(function(){return function(a,b){a|=0;return Aa(b)+a|0}}(this)))|0};
function Pr(){MB.call(this);this.No=!1}Pr.prototype=new NB;Pr.prototype.constructor=Pr;Pr.prototype.Wl=function(a){MB.prototype.Wl.call(this,a);this.No=!1;return this};Pr.prototype.$classData=l({Yw:0},!1,"java.util.Collections$ImmutableMap",{Yw:1,eD:1,c:1,gD:1,Gp:1,fm:1});function TC(){this.Pa=null;this.Vj=!1}TC.prototype=new n;TC.prototype.constructor=TC;function UC(){}d=UC.prototype=TC.prototype;d.Po=function(){return this.Vj};d.m=function(){return this.Pa.m()};d.v=function(){return this.Pa.v()};
d.O=function(a){return this.Pa.O(a)};d.Ul=function(a){this.Pa=a;this.Vj=!0;return this};d.Ee=function(a){if(this.Po()||this.Pa.O(a))throw(new Jw).b();return!1};d.se=function(){throw(new Jw).b();};d.rc=function(){var a=new lA,b=this.Pa.rc();a.Pa=b;return a};function Nx(){Ox.call(this);this.sa=null}Nx.prototype=new JB;Nx.prototype.constructor=Nx;Nx.prototype.$classData=l({mx:0},!1,"java.util.HashMap$EntrySet$$anon$2$$anon$1",{mx:1,dD:1,c:1,ux:1,k:1,h:1});function Mw(){this.Aa=null}Mw.prototype=new GB;
Mw.prototype.constructor=Mw;Mw.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Mw.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};Mw.prototype.$classData=l({xx:0},!1,"java.util.NoSuchElementException",{xx:1,qb:1,tb:1,ab:1,c:1,h:1});function VC(){this.Pa=null}VC.prototype=new n;VC.prototype.constructor=VC;d=VC.prototype;d.B=function(){return"Box"};d.z=function(){return 1};
d.o=function(a){if(a&&a.$classData&&a.$classData.p.Ip){var b=this.Pa;a=a.Pa;return null===b?null===a:za(b,a)}return!1};d.A=function(a){switch(a){case 0:return this.Pa;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.n=function(a){this.Pa=a;return this};d.s=function(){return null===this.Pa?0:Aa(this.Pa)};d.D=function(){return Z(this)};d.$classData=l({Ip:0},!1,"java.util.package$Box",{Ip:1,c:1,E:1,q:1,k:1,h:1});function x(){this.Pi=this.Wp=this.Aa=null;this.vl=!1}x.prototype=new GB;
x.prototype.constructor=x;x.prototype.md=function(){if(!this.vl&&!this.vl){var a;if(null===this.Pi)a="null";else try{a=ka(this.Pi)+" ("+("of class "+Ib(la(this.Pi)))+")"}catch(b){if(null!==mi(ni(),b))a="an instance of class "+Ib(la(this.Pi));else throw b;}this.Wp=a;this.vl=!0}return this.Wp};x.prototype.n=function(a){this.Pi=a;Fw.prototype.Sa.call(this,null,0,0,!0);return this};x.prototype.$classData=l({Hx:0},!1,"scala.MatchError",{Hx:1,qb:1,tb:1,ab:1,c:1,h:1});function WC(){}WC.prototype=new n;
WC.prototype.constructor=WC;function XC(){}XC.prototype=WC.prototype;function pq(){}pq.prototype=new n;pq.prototype.constructor=pq;d=pq.prototype;d.b=function(){return this};d.f=function(a){this.sl(a)};d.m=function(){return"\x3cfunction1\x3e"};d.Zc=function(){return!1};d.wf=function(a,b){return lq(this,a,b)};d.sl=function(a){throw(new x).n(a);};d.Dd=function(){return this};d.$classData=l({Mx:0},!1,"scala.PartialFunction$$anon$1",{Mx:1,c:1,Va:1,T:1,k:1,h:1});function YC(){this.gm=this.Ig=null}
YC.prototype=new n;YC.prototype.constructor=YC;function $A(a,b){var c=new YC;c.Ig=a;c.gm=b;return c}d=YC.prototype;d.f=function(a){return this.gm.f(this.Ig.f(a))};d.m=function(){return"\x3cfunction1\x3e"};d.Zc=function(a){return this.Ig.Zc(a)};d.wf=function(a,b){var c=this.Ig.wf(a,qq().th);return qq().th===c?b.f(a):this.gm.f(c)};d.Dd=function(a){return $A(this,a)};d.$classData=l({Ox:0},!1,"scala.PartialFunction$AndThen",{Ox:1,c:1,Va:1,T:1,k:1,h:1});function th(){this.cq=null}th.prototype=new Fy;
th.prototype.constructor=th;th.prototype.f=function(a){return this.Rh(a)};function sh(a,b){a.cq=b;return a}th.prototype.Rh=function(a){a=this.cq.wf(a,qq().th);return qq().th===a?B():(new H).n(a)};th.prototype.$classData=l({Px:0},!1,"scala.PartialFunction$Lifted",{Px:1,dr:1,c:1,T:1,k:1,h:1});function BA(){}BA.prototype=new bC;BA.prototype.constructor=BA;BA.prototype.b=function(){return this};BA.prototype.f=function(a){return a};
BA.prototype.$classData=l({Rx:0},!1,"scala.Predef$$anon$1",{Rx:1,uD:1,c:1,T:1,k:1,h:1});function CA(){}CA.prototype=new $B;CA.prototype.constructor=CA;CA.prototype.b=function(){return this};CA.prototype.f=function(a){return a};CA.prototype.$classData=l({Sx:0},!1,"scala.Predef$$anon$2",{Sx:1,tD:1,c:1,T:1,k:1,h:1});function ZC(){this.td=null}ZC.prototype=new n;ZC.prototype.constructor=ZC;d=ZC.prototype;d.B=function(){return"StringContext"};d.z=function(){return 1};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.sq){var b=this.td;a=a.td;return null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.td;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};function $C(a,b){if(a.td.aa()!==(1+b.aa()|0))throw(new Ob).i("wrong number of arguments ("+b.aa()+") for interpolated string with "+a.td.aa()+" parts");}
function Tl(a,b){var c=function(){return function(a){FA||(FA=(new EA).b());a:{var b=a.length|0,c;Ca();var e=ou();c=a.indexOf(e)|0;switch(c){case -1:break a;default:e=(new HB).b();b:{var f=c;c=0;for(;;)if(0<=f){if(f>c){var z=e;c=Ha(null===a?"null":a,c,f);z.Da=""+z.Da+c}c=1+f|0;if(c>=b)throw(new aD).qi(a,f);z=65535&(a.charCodeAt(c)|0);switch(z){case 98:f=8;break;case 116:f=9;break;case 110:f=10;break;case 102:f=12;break;case 114:f=13;break;case 34:f=34;break;case 39:f=39;break;case 92:f=92;break;default:if(48<=
z&&55>=z)f=65535&(a.charCodeAt(c)|0),z=-48+f|0,c=1+c|0,c<b&&48<=(65535&(a.charCodeAt(c)|0))&&55>=(65535&(a.charCodeAt(c)|0))&&(z=-48+((z<<3)+(65535&(a.charCodeAt(c)|0))|0)|0,c=1+c|0,c<b&&51>=f&&48<=(65535&(a.charCodeAt(c)|0))&&55>=(65535&(a.charCodeAt(c)|0))&&(z=-48+((z<<3)+(65535&(a.charCodeAt(c)|0))|0)|0,c=1+c|0)),c=-1+c|0,f=65535&z;else throw(new aD).qi(a,f);}c=1+c|0;zn(e,f);f=c;Ca();var z=a,E=ou(),z=z.indexOf(E,c)|0;c=f;f=z}else{c<b&&(f=e,a=Ha(null===a?"null":a,c,b),f.Da=""+f.Da+a);a=e.Da;break b}}}}return a}}(a);
$C(a,b);a=a.td.C();b=b.C();for(var e=a.r(),e=(new HB).i(c(e));b.u();){var f=b.r();e.Da=""+e.Da+f;f=a.r();f=c(f);e.Da=""+e.Da+f}return e.Da}function Ul(a){var b=new ZC;b.td=a;return b}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({sq:0},!1,"scala.StringContext",{sq:1,c:1,E:1,q:1,k:1,h:1});function bD(){}bD.prototype=new n;bD.prototype.constructor=bD;function cD(){}cD.prototype=bD.prototype;function Xr(){this.Aa=null}Xr.prototype=new Gw;Xr.prototype.constructor=Xr;
Xr.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};Xr.prototype.Xj=function(){HA||(HA=(new GA).b());return HA.cn?Fw.prototype.Xj.call(this):this};Xr.prototype.$classData=l({Gy:0},!1,"scala.util.control.BreakControl",{Gy:1,ab:1,c:1,h:1,CD:1,DD:1});
function xC(a,b){if(b&&b.$classData&&b.$classData.p.Lc){var c;if(!(c=a===b)&&(c=a.v()===b.v()))try{var e=a.C();for(a=!0;a&&e.u();){var f=e.r();if(null===f)throw(new x).n(f);var h=f.U,m=b.sb(f.R);b:{if(If(m)){var q=m.J;if(D(G(),h,q)){a=!0;break b}}a=!1}}c=a}catch(y){if(y&&y.$classData&&y.$classData.p.zw)c=!1;else throw y;}b=c}else b=!1;return b}function dD(a,b,c){return a.ze(t(new u,function(a,b){return function(a){return D(G(),b,a)}}(a,b)),c)}
function fn(a,b){return b&&b.$classData&&b.$classData.p.Pd?a.ie(b):!1}function eD(a,b){return 0<=b&&b<a.aa()}function fD(){this.Ra=null}fD.prototype=new py;fD.prototype.constructor=fD;fD.prototype.b=function(){oy.prototype.b.call(this);return this};fD.prototype.ba=function(){gD();return(new zg).b()};fD.prototype.$classData=l({Ry:0},!1,"scala.collection.Iterable$",{Ry:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var hD=void 0;function yq(){hD||(hD=(new fD).b());return hD}function li(){this.bp=this.sa=null}
li.prototype=new dC;li.prototype.constructor=li;li.prototype.r=function(){return this.bp.f(this.sa.r())};li.prototype.pb=function(a,b){if(null===a)throw Dp(ni(),null);this.sa=a;this.bp=b;return this};li.prototype.u=function(){return this.sa.u()};li.prototype.$classData=l({Ty:0},!1,"scala.collection.Iterator$$anon$10",{Ty:1,yb:1,c:1,vb:1,G:1,F:1});function Kj(){this.gi=this.sa=this.we=null}Kj.prototype=new dC;Kj.prototype.constructor=Kj;Kj.prototype.r=function(){return(this.u()?this.we:Aq().Y).r()};
Kj.prototype.pb=function(a,b){if(null===a)throw Dp(ni(),null);this.sa=a;this.gi=b;this.we=Aq().Y;return this};Kj.prototype.u=function(){for(;!this.we.u();){if(!this.sa.u())return!1;this.we=this.gi.f(this.sa.r()).rf()}return!0};Kj.prototype.$classData=l({Uy:0},!1,"scala.collection.Iterator$$anon$11",{Uy:1,yb:1,c:1,vb:1,G:1,F:1});function Oi(){this.Pl=null;this.ak=!1;this.$p=this.sa=null}Oi.prototype=new dC;Oi.prototype.constructor=Oi;Oi.prototype.r=function(){return this.u()?(this.ak=!1,this.Pl):Aq().Y.r()};
Oi.prototype.pb=function(a,b){if(null===a)throw Dp(ni(),null);this.sa=a;this.$p=b;this.ak=!1;return this};Oi.prototype.u=function(){if(!this.ak){do{if(!this.sa.u())return!1;this.Pl=this.sa.r()}while(!this.$p.f(this.Pl));this.ak=!0}return!0};Oi.prototype.$classData=l({Vy:0},!1,"scala.collection.Iterator$$anon$12",{Vy:1,yb:1,c:1,vb:1,G:1,F:1});function ms(){}ms.prototype=new dC;ms.prototype.constructor=ms;ms.prototype.b=function(){return this};
ms.prototype.r=function(){throw(new Mw).i("next on empty iterator");};ms.prototype.u=function(){return!1};ms.prototype.$classData=l({Wy:0},!1,"scala.collection.Iterator$$anon$2",{Wy:1,yb:1,c:1,vb:1,G:1,F:1});function HC(){this.Zj=!1;this.Ro=null}HC.prototype=new dC;HC.prototype.constructor=HC;HC.prototype.r=function(){return this.Zj?(this.Zj=!1,this.Ro):Aq().Y.r()};HC.prototype.n=function(a){this.Ro=a;this.Zj=!0;return this};HC.prototype.u=function(){return this.Zj};
HC.prototype.$classData=l({Xy:0},!1,"scala.collection.Iterator$$anon$3",{Xy:1,yb:1,c:1,vb:1,G:1,F:1});function iD(){}iD.prototype=new n;iD.prototype.constructor=iD;iD.prototype.b=function(){return this};iD.prototype.$classData=l({Zy:0},!1,"scala.collection.JavaConverters$",{Zy:1,c:1,KD:1,ID:1,LD:1,JD:1});var jD=void 0;function Or(){jD||(jD=(new iD).b());return jD}function kD(){this.Vd=null}kD.prototype=new dC;kD.prototype.constructor=kD;function lD(a){var b=new kD;b.Vd=a;return b}
kD.prototype.r=function(){if(this.u()){var a=this.Vd.I();this.Vd=this.Vd.w();return a}return Aq().Y.r()};kD.prototype.u=function(){return!this.Vd.l()};kD.prototype.$classData=l({$y:0},!1,"scala.collection.LinearSeqLike$$anon$1",{$y:1,yb:1,c:1,vb:1,G:1,F:1});function mD(){this.Jc=null}mD.prototype=new dC;mD.prototype.constructor=mD;mD.prototype.r=function(){return this.Jc.r().R};mD.prototype.u=function(){return this.Jc.u()};mD.prototype.ef=function(a){this.Jc=a.C();return this};
mD.prototype.$classData=l({az:0},!1,"scala.collection.MapLike$$anon$1",{az:1,yb:1,c:1,vb:1,G:1,F:1});function nD(){this.Jc=null}nD.prototype=new dC;nD.prototype.constructor=nD;nD.prototype.r=function(){return this.Jc.r().U};nD.prototype.u=function(){return this.Jc.u()};nD.prototype.ef=function(a){this.Jc=a.C();return this};nD.prototype.$classData=l({bz:0},!1,"scala.collection.MapLike$$anon$2",{bz:1,yb:1,c:1,vb:1,G:1,F:1});function oD(){}oD.prototype=new hC;oD.prototype.constructor=oD;
oD.prototype.b=function(){return this};oD.prototype.ae=function(){return Q()};oD.prototype.ba=function(){return R(new S,Q())};oD.prototype.$classData=l({ez:0},!1,"scala.collection.Set$",{ez:1,wh:1,vh:1,zb:1,c:1,Ab:1});var pD=void 0;function qD(){pD||(pD=(new oD).b());return pD}function xq(){this.Ra=null}xq.prototype=new py;xq.prototype.constructor=xq;xq.prototype.b=function(){oy.prototype.b.call(this);wq=this;(new Wr).b();return this};xq.prototype.ba=function(){rD||(rD=(new sD).b());return(new zg).b()};
xq.prototype.$classData=l({fz:0},!1,"scala.collection.Traversable$",{fz:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var wq=void 0;function tD(){this.Wm=null}tD.prototype=new dC;tD.prototype.constructor=tD;tD.prototype.r=function(){var a=this.Wm.r();return(new I).d(a.xa,a.t)};tD.prototype.u=function(){return this.Wm.u()};tD.prototype.$classData=l({mz:0},!1,"scala.collection.convert.Wrappers$JMapWrapperLike$$anon$2",{mz:1,yb:1,c:1,vb:1,G:1,F:1});function uD(){}uD.prototype=new hC;uD.prototype.constructor=uD;
function vD(){}vD.prototype=uD.prototype;uD.prototype.ae=function(){return this.Wj()};uD.prototype.ba=function(){return R(new S,this.Wj())};function wD(){}wD.prototype=new hC;wD.prototype.constructor=wD;function xD(){}xD.prototype=wD.prototype;wD.prototype.ba=function(){return nC(new mC,this.ae())};function yD(){this.Ra=null}yD.prototype=new py;yD.prototype.constructor=yD;yD.prototype.b=function(){oy.prototype.b.call(this);return this};yD.prototype.ba=function(){return(new zg).b()};
yD.prototype.$classData=l({Kz:0},!1,"scala.collection.immutable.Iterable$",{Kz:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var zD=void 0;function gD(){zD||(zD=(new yD).b());return zD}function AD(){this.Vd=null}AD.prototype=new dC;AD.prototype.constructor=AD;AD.prototype.r=function(){if(!this.u())return Aq().Y.r();var a=ut(this.Vd),b=a.I();this.Vd=tt(new st,this,v(new w,function(a,b){return function(){return b.w()}}(this,a)));return b};
function BD(a){var b=new AD;b.Vd=tt(new st,b,v(new w,function(a,b){return function(){return b}}(b,a)));return b}AD.prototype.u=function(){return!ut(this.Vd).l()};AD.prototype.xc=function(){var a=ut(this.Vd);this.Vd=tt(new st,this,v(new w,function(){return function(){Hq();return gy()}}(this)));return a};AD.prototype.$classData=l({pA:0},!1,"scala.collection.immutable.StreamIterator",{pA:1,yb:1,c:1,vb:1,G:1,F:1});function sD(){this.Ra=null}sD.prototype=new py;sD.prototype.constructor=sD;
sD.prototype.b=function(){oy.prototype.b.call(this);return this};sD.prototype.ba=function(){return(new zg).b()};sD.prototype.$classData=l({sA:0},!1,"scala.collection.immutable.Traversable$",{sA:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var rD=void 0;function CD(){this.To=null;this.yd=0;this.uh=this.Fm=this.Hk=null;this.Xf=0;this.Jg=null}CD.prototype=new dC;CD.prototype.constructor=CD;function DD(){}DD.prototype=CD.prototype;
CD.prototype.r=function(){if(null!==this.Jg){var a=this.Jg.r();this.Jg.u()||(this.Jg=null);return a}a:{var a=this.uh,b=this.Xf;for(;;){b===(-1+a.e.length|0)?(this.yd=-1+this.yd|0,0<=this.yd?(this.uh=this.Hk.e[this.yd],this.Xf=this.Fm.e[this.yd],this.Hk.e[this.yd]=null):(this.uh=null,this.Xf=0)):this.Xf=1+this.Xf|0;if((a=a.e[b])&&a.$classData&&a.$classData.p.Eq||a&&a.$classData&&a.$classData.p.Gq){a=this.ip(a);break a}if(ED(a)||FD(a))0<=this.yd&&(this.Hk.e[this.yd]=this.uh,this.Fm.e[this.yd]=this.Xf),
this.yd=1+this.yd|0,this.uh=GD(a),this.Xf=0,a=GD(a),b=0;else{this.Jg=a.C();a=this.r();break a}}}return a};CD.prototype.u=function(){return null!==this.Jg||0<=this.yd};function GD(a){if(ED(a))return a.ib;if(!FD(a))throw(new x).n(a);return a.hb}CD.prototype.op=function(a){this.To=a;this.yd=0;this.Hk=k(r(r(HD)),[6]);this.Fm=k(r(Za),[6]);this.uh=this.To;this.Xf=0;this.Jg=null;return this};function ID(){this.Bc=0;this.sa=null}ID.prototype=new dC;ID.prototype.constructor=ID;
ID.prototype.r=function(){return 0<this.Bc?(this.Bc=-1+this.Bc|0,this.sa.eb(this.Bc)):Aq().Y.r()};ID.prototype.u=function(){return 0<this.Bc};function JD(a){var b=new ID;if(null===a)throw Dp(ni(),null);b.sa=a;b.Bc=a.aa();return b}ID.prototype.$classData=l({vA:0},!1,"scala.collection.immutable.Vector$$anon$1",{vA:1,yb:1,c:1,vb:1,G:1,F:1});function Bt(){this.fi=this.me=null}Bt.prototype=new n;Bt.prototype.constructor=Bt;function At(a,b,c){a.fi=c;a.me=b;return a}d=Bt.prototype;
d.o=function(a){return null!==a&&(a===this||a===this.me||za(a,this.me))};d.Ob=function(a){this.me.kb(a);return this};d.m=function(){return""+this.me};d.Ja=function(){return this.fi.f(this.me.Ja())};d.gd=function(a,b){this.me.gd(a,b)};d.kb=function(a){this.me.kb(a);return this};d.s=function(){return this.me.s()};d.wc=function(a){this.me.wc(a)};d.db=function(a){this.me.db(a);return this};d.$classData=l({EA:0},!1,"scala.collection.mutable.Builder$$anon$1",{EA:1,c:1,Qc:1,Oc:1,Mc:1,xD:1});
function KD(){this.Bc=0;this.sa=null}KD.prototype=new dC;KD.prototype.constructor=KD;KD.prototype.r=function(){return this.u()?(this.Bc=1+this.Bc|0,Xb(this.sa.S.e[-1+this.Bc|0])):Aq().Y.r()};KD.prototype.u=function(){for(;this.Bc<this.sa.S.e.length&&null===this.sa.S.e[this.Bc];)this.Bc=1+this.Bc|0;return this.Bc<this.sa.S.e.length};KD.prototype.$classData=l({HA:0},!1,"scala.collection.mutable.FlatHashTable$$anon$1",{HA:1,yb:1,c:1,vb:1,G:1,F:1});function LD(){this.Jc=null}LD.prototype=new dC;
LD.prototype.constructor=LD;LD.prototype.r=function(){return this.Jc.r().xa};LD.prototype.si=function(a){this.Jc=MD(a);return this};LD.prototype.u=function(){return this.Jc.u()};LD.prototype.$classData=l({MA:0},!1,"scala.collection.mutable.HashMap$$anon$3",{MA:1,yb:1,c:1,vb:1,G:1,F:1});function ND(){this.Jc=null}ND.prototype=new dC;ND.prototype.constructor=ND;ND.prototype.r=function(){return this.Jc.r().t};ND.prototype.si=function(a){this.Jc=MD(a);return this};ND.prototype.u=function(){return this.Jc.u()};
ND.prototype.$classData=l({NA:0},!1,"scala.collection.mutable.HashMap$$anon$4",{NA:1,yb:1,c:1,vb:1,G:1,F:1});function OD(){this.am=null;this.gh=0;this.rg=null}OD.prototype=new dC;OD.prototype.constructor=OD;OD.prototype.r=function(){var a=this.rg;for(this.rg=this.rg.r();null===this.rg&&0<this.gh;)this.gh=-1+this.gh|0,this.rg=this.am.e[this.gh];return a};function MD(a){var b=new OD;b.am=a.S;b.gh=$t(a);b.rg=b.am.e[b.gh];return b}OD.prototype.u=function(){return null!==this.rg};
OD.prototype.$classData=l({SA:0},!1,"scala.collection.mutable.HashTable$$anon$1",{SA:1,yb:1,c:1,vb:1,G:1,F:1});function PD(){this.Ra=null}PD.prototype=new py;PD.prototype.constructor=PD;PD.prototype.b=function(){oy.prototype.b.call(this);return this};PD.prototype.ba=function(){return(new QD).b()};PD.prototype.$classData=l({VA:0},!1,"scala.collection.mutable.Iterable$",{VA:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var RD=void 0;function SD(){this.td=null}SD.prototype=new n;SD.prototype.constructor=SD;
function TD(){}d=TD.prototype=SD.prototype;d.b=function(){this.td=(new zg).b();return this};d.Ob=function(a){return UD(this,a)};function UD(a,b){var c=a.td;yg();b=(new A).g([b]);var e=yg().Ra;Ag(c,ll(b,e));return a}d.gd=function(a,b){By(this,a,b)};d.kb=function(a){return UD(this,a)};d.wc=function(){};d.db=function(a){Ag(this.td,a);return this};function VD(){this.we=null}VD.prototype=new dC;VD.prototype.constructor=VD;VD.prototype.r=function(){if(this.u()){var a=this.we.xa;this.we=this.we.Ce;return a}return Aq().Y.r()};
VD.prototype.u=function(){return null!==this.we};VD.prototype.$classData=l({YA:0},!1,"scala.collection.mutable.LinkedHashSet$$anon$1",{YA:1,yb:1,c:1,vb:1,G:1,F:1});function WD(){this.ai=null}WD.prototype=new dC;WD.prototype.constructor=WD;WD.prototype.r=function(){if(this.u()){var a=this.ai.I();this.ai=this.ai.w();return a}throw(new Mw).i("next on empty Iterator");};WD.prototype.u=function(){return this.ai!==xb()};
WD.prototype.$classData=l({aB:0},!1,"scala.collection.mutable.ListBuffer$$anon$1",{aB:1,yb:1,c:1,vb:1,G:1,F:1});function Bl(){this.y=this.Y=null}Bl.prototype=new n;Bl.prototype.constructor=Bl;function Dl(a,b){a.y=a.y.Yd(b);return a}d=Bl.prototype;d.Ob=function(a){return Dl(this,a)};d.Ja=function(){return this.y};d.gd=function(a,b){By(this,a,b)};function Al(a,b){a.Y=b;a.y=b;return a}d.kb=function(a){return Dl(this,a)};d.wc=function(){};d.db=function(a){return jm(this,a)};
d.$classData=l({cB:0},!1,"scala.collection.mutable.MapBuilder",{cB:1,c:1,dj:1,Qc:1,Oc:1,Mc:1});function S(){this.y=this.Y=null}S.prototype=new n;S.prototype.constructor=S;d=S.prototype;d.Ob=function(a){return T(this,a)};d.Ja=function(){return this.y};d.gd=function(a,b){By(this,a,b)};function T(a,b){a.y=a.y.lc(b);return a}function R(a,b){a.Y=b;a.y=b;return a}d.kb=function(a){return T(this,a)};d.wc=function(){};d.db=function(a){return jm(this,a)};
d.$classData=l({fB:0},!1,"scala.collection.mutable.SetBuilder",{fB:1,c:1,dj:1,Qc:1,Oc:1,Mc:1});function XD(){this.ro=this.Vh=0;this.rr=null}XD.prototype=new dC;XD.prototype.constructor=XD;XD.prototype.r=function(){var a=this.rr.A(this.Vh);this.Vh=1+this.Vh|0;return a};function Z(a){var b=new XD;b.rr=a;b.Vh=0;b.ro=a.z();return b}XD.prototype.u=function(){return this.Vh<this.ro};XD.prototype.$classData=l({DB:0},!1,"scala.runtime.ScalaRunTime$$anon$1",{DB:1,yb:1,c:1,vb:1,G:1,F:1});function YD(){}
YD.prototype=new n;YD.prototype.constructor=YD;d=YD.prototype;d.b=function(){return this};d.B=function(){return"Home"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Home"};d.s=function(){return 2255103};d.D=function(){return Z(this)};d.$classData=l({vr:0},!1,"AppRouter$Home$",{vr:1,c:1,OB:1,E:1,q:1,k:1,h:1});var ZD=void 0;function vc(){ZD||(ZD=(new YD).b());return ZD}function qv(){this.mr=this.Ki=this.L=null}qv.prototype=new n;qv.prototype.constructor=qv;
qv.prototype.Sk=function(){return this.mr};qv.prototype.Np=function(a){Ne();var b=ub().de;return $D(this,b,a)};qv.prototype.Pp=function(a){Ne();var b=ub().de;return $D(this,a,b)};function pv(a,b,c){a.L=b;a.Ki=c;a.mr=Qv(a.L);return a}qv.prototype.$classData=l({Qr:0},!1,"japgolly.scalajs.react.component.Js$$anon$3",{Qr:1,c:1,Vr:1,Ur:1,Lr:1,Kr:1,Mr:1});function aE(){this.lm=this.pm=this.If=this.Ki=this.L=null}aE.prototype=new n;aE.prototype.constructor=aE;aE.prototype.Sk=function(){return this.If.Sk()};
aE.prototype.Np=function(a){Ne();var b=this.If,c=this.pm;a=mb(a,this.lm);return $D(b,c,a)};function $D(a,b,c){var e=new aE;e.If=a;e.pm=b;e.lm=c;e.L=a.L;e.Ki=mb(c,a.Ki);return e}aE.prototype.Pp=function(a){Ne();var b=this.If;a=mb(a,this.pm);return $D(b,a,this.lm)};aE.prototype.$classData=l({Rr:0},!1,"japgolly.scalajs.react.component.Js$$anon$4",{Rr:1,c:1,Vr:1,Ur:1,Lr:1,Kr:1,Mr:1});function bE(){this.wm=this.wa=this.og=this.L=null}bE.prototype=new n;bE.prototype.constructor=bE;
function ov(a,b,c,e){var f=new bE;if(null===a)throw Dp(ni(),null);f.wa=a;f.wm=e;f.L=b;f.og=c;return f}bE.prototype.qo=function(a){var b=this.wa,c=ub().de,e=ub().de;return cE(b,this,a,c,e,this.wm)};bE.prototype.Qp=function(a){var b=this.wa,c=ub().de,e=ub().de;return cE(b,this,c,e,a,this.wm)};bE.prototype.$classData=l({Xr:0},!1,"japgolly.scalajs.react.component.JsBaseComponentTemplate$$anon$1",{Xr:1,c:1,$r:1,Zr:1,Ir:1,Hr:1,Jr:1});
function dE(){this.tk=this.qm=this.jm=this.Cl=this.If=this.wa=this.og=this.L=null}dE.prototype=new n;dE.prototype.constructor=dE;dE.prototype.qo=function(a){var b=this.wa,c=this.If;a=mb(this.Cl,a);return cE(b,c,a,this.jm,this.qm,this.tk)};function cE(a,b,c,e,f,h){var m=new dE;if(null===a)throw Dp(ni(),null);m.wa=a;m.If=b;m.Cl=c;m.jm=e;m.qm=f;m.tk=h;m.L=b.L;ub();a=e.f(b.og);b=m.tk;c=new ui;c.Gf=a;c.qk=b;a=c.Gf;b=f.f(a.Qk);f=se(new te,b,mb(f,a.Pj),a.lk);m.og=f;return m}
dE.prototype.Qp=function(a){var b=this.wa,c=this.If,e=this.Cl,f=this.jm;a=mb(a,this.qm);return cE(b,c,e,f,a,this.tk)};dE.prototype.$classData=l({Yr:0},!1,"japgolly.scalajs.react.component.JsBaseComponentTemplate$$anon$2",{Yr:1,c:1,$r:1,Zr:1,Ir:1,Hr:1,Jr:1});function Xy(){this.J=null}Xy.prototype=new Vg;Xy.prototype.constructor=Xy;d=Xy.prototype;d.B=function(){return"AbsUrl"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.qn?this.J===a.J:!1};
d.A=function(a){switch(a){case 0:return this.J;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.Lk=function(a){return a.J};d.i=function(a){this.J=a;-1===(this.J.indexOf("://")|0)&&(a=this+" doesn't seem to be a valid URL. It's missing '://'. Consider using AbsUrl.fromWindow.",Bd().warn(a));return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.ik=function(a){return(new Xy).i(a)};
d.$classData=l({qn:0},!1,"japgolly.scalajs.react.extra.router.AbsUrl",{qn:1,tn:1,c:1,E:1,q:1,k:1,h:1});function dd(){this.J=null}dd.prototype=new Vg;dd.prototype.constructor=dd;d=dd.prototype;d.B=function(){return"BaseUrl"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.rn?this.J===a.J:!1};d.A=function(a){switch(a){case 0:return this.J;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.Lk=function(a){return a.J};
d.i=function(a){this.J=a;-1===(this.J.indexOf("://")|0)&&(a=this+" doesn't seem to be a valid URL. It's missing '://'. Consider using BaseUrl.fromWindowOrigin.",Bd().warn(a));return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};function bz(a,b){return(new Xy).i(""+a.J+b.J)}d.ik=function(a){return(new dd).i(a)};d.$classData=l({rn:0},!1,"japgolly.scalajs.react.extra.router.BaseUrl",{rn:1,tn:1,c:1,E:1,q:1,k:1,h:1});function sc(){this.J=null}sc.prototype=new Vg;
sc.prototype.constructor=sc;d=sc.prototype;d.B=function(){return"Path"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.sn?this.J===a.J:!1};d.A=function(a){switch(a){case 0:return this.J;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.Lk=function(a){return a.J};d.i=function(a){this.J=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.ik=function(a){return(new sc).i(a)};
d.$classData=l({sn:0},!1,"japgolly.scalajs.react.extra.router.Path",{sn:1,tn:1,c:1,E:1,q:1,k:1,h:1});function lz(){}lz.prototype=new n;lz.prototype.constructor=lz;d=lz.prototype;d.b=function(){return this};d.B=function(){return"Force"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Force"};d.s=function(){return 68065995};d.D=function(){return Z(this)};d.$classData=l({Js:0},!1,"japgolly.scalajs.react.extra.router.Redirect$Force$",{Js:1,c:1,un:1,E:1,q:1,k:1,h:1});
var kz=void 0;function eE(){}eE.prototype=new n;eE.prototype.constructor=eE;d=eE.prototype;d.b=function(){return this};d.B=function(){return"Push"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Push"};d.s=function(){return 2499386};d.D=function(){return Z(this)};d.$classData=l({Ks:0},!1,"japgolly.scalajs.react.extra.router.Redirect$Push$",{Ks:1,c:1,un:1,E:1,q:1,k:1,h:1});var fE=void 0;function fz(){fE||(fE=(new eE).b());return fE}function gE(){}
gE.prototype=new n;gE.prototype.constructor=gE;d=gE.prototype;d.b=function(){return this};d.B=function(){return"Replace"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Replace"};d.s=function(){return-1535817068};d.D=function(){return Z(this)};d.$classData=l({Ls:0},!1,"japgolly.scalajs.react.extra.router.Redirect$Replace$",{Ls:1,c:1,un:1,E:1,q:1,k:1,h:1});var hE=void 0;function wc(){hE||(hE=(new gE).b());return hE}function nh(){this.Gf=null}nh.prototype=new n;
nh.prototype.constructor=nh;d=nh.prototype;d.B=function(){return"Renderer"};d.z=function(){return 1};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.hl){var b=this.Gf;a=a.Gf;return null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.Gf;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.Mb=function(a){this.Gf=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({hl:0},!1,"japgolly.scalajs.react.extra.router.Renderer",{hl:1,c:1,Gs:1,E:1,q:1,k:1,h:1});function iE(){}iE.prototype=new Yg;iE.prototype.constructor=iE;d=iE.prototype;d.b=function(){return this};d.B=function(){return"BroadcastSync"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"BroadcastSync"};d.s=function(){return-155951396};d.D=function(){return Z(this)};
d.$classData=l({Ms:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$BroadcastSync$",{Ms:1,Vg:1,c:1,E:1,q:1,k:1,h:1});var jE=void 0;function wv(){jE||(jE=(new iE).b());return jE}function az(){this.Mi=null}az.prototype=new Yg;az.prototype.constructor=az;d=az.prototype;d.oa=function(a){this.Mi=a;return this};d.B=function(){return"Log"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.il?this.Mi===a.Mi:!1};
d.A=function(a){switch(a){case 0:return this.Mi;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({il:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$Log",{il:1,Vg:1,c:1,E:1,q:1,k:1,h:1});function iz(){this.yc=null}iz.prototype=new Yg;iz.prototype.constructor=iz;d=iz.prototype;d.B=function(){return"PushState"};d.z=function(){return 1};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.jl){var b=this.yc;a=a.yc;return null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.yc;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.ni=function(a){this.yc=a;return this};d.D=function(){return Z(this)};d.$classData=l({jl:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$PushState",{jl:1,Vg:1,c:1,E:1,q:1,k:1,h:1});
function jz(){this.yc=null}jz.prototype=new Yg;jz.prototype.constructor=jz;d=jz.prototype;d.B=function(){return"ReplaceState"};d.z=function(){return 1};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.kl){var b=this.yc;a=a.yc;return null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.yc;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.ni=function(a){this.yc=a;return this};d.D=function(){return Z(this)};
d.$classData=l({kl:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$ReplaceState",{kl:1,Vg:1,c:1,E:1,q:1,k:1,h:1});function hz(){this.Nh=null}hz.prototype=new Yg;hz.prototype.constructor=hz;d=hz.prototype;d.B=function(){return"Return"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.ll?D(G(),this.Nh,a.Nh):!1};d.A=function(a){switch(a){case 0:return this.Nh;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};
d.n=function(a){this.Nh=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({ll:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$Return",{ll:1,Vg:1,c:1,E:1,q:1,k:1,h:1});function bh(){this.mh=this.hh=null}bh.prototype=new Yg;bh.prototype.constructor=bh;d=bh.prototype;d.B=function(){return"Sequence"};d.z=function(){return 2};
d.o=function(a){if(this===a)return!0;if($g(a)){var b=this.hh,c=a.hh;if(null===b?null===c:fn(b,c))return b=this.mh,a=a.mh,null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.hh;case 1:return this.mh;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};function $g(a){return!!(a&&a.$classData&&a.$classData.p.wn)}
d.$classData=l({wn:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$Sequence",{wn:1,Vg:1,c:1,E:1,q:1,k:1,h:1});function mz(){this.yc=null}mz.prototype=new Yg;mz.prototype.constructor=mz;d=mz.prototype;d.B=function(){return"SetWindowLocation"};d.z=function(){return 1};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.ml){var b=this.yc;a=a.yc;return null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.yc;default:throw(new Y).i(""+a);}};
d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.ni=function(a){this.yc=a;return this};d.D=function(){return Z(this)};d.$classData=l({ml:0},!1,"japgolly.scalajs.react.extra.router.RouteCmd$SetWindowLocation",{ml:1,Vg:1,c:1,E:1,q:1,k:1,h:1});function kE(){this.tm=null}kE.prototype=new ZA;kE.prototype.constructor=kE;function uh(a){var b=new kE;b.tm=a;return b}kE.prototype.Zc=function(a){return D(G(),this.tm,a)};kE.prototype.wf=function(a,b){return D(G(),this.tm,a)?a:b.f(a)};
kE.prototype.$classData=l({Qs:0},!1,"japgolly.scalajs.react.extra.router.RouterConfigDsl$$anonfun$1",{Qs:1,vB:1,c:1,T:1,Va:1,k:1,h:1});function lE(){this.Rb=this.mj=null}lE.prototype=new wh;lE.prototype.constructor=lE;d=lE.prototype;d.B=function(){return"Contramap"};function Zy(a,b){var c=new lE;c.mj=a;c.Rb=b;return c}d.z=function(){return 2};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.yn){var b=this.mj,c=a.mj;if(null===b?null===c:b.o(c))return b=this.Rb,a=a.Rb,null===b?null===a:b.o(a)}return!1};d.A=function(a){switch(a){case 0:return this.mj;case 1:return this.Rb;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.zk=function(){return this.mj.zk()};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({yn:0},!1,"japgolly.scalajs.react.extra.router.RouterCtl$Contramap",{yn:1,Ss:1,c:1,E:1,q:1,k:1,h:1});function mE(){}mE.prototype=new Lk;mE.prototype.constructor=mE;d=mE.prototype;d.b=function(){return this};d.B=function(){return"Unrelated"};d.z=function(){return 0};d.so=function(){return!1};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Unrelated"};d.$m=function(a){return a.V()};d.s=function(){return 511295314};d.D=function(){return Z(this)};
d.$classData=l({Bu:0},!1,"scalacss.internal.AttrCmp$Unrelated$",{Bu:1,Gn:1,c:1,E:1,q:1,k:1,h:1});var nE=void 0;function Qk(){nE||(nE=(new mE).b());return nE}function CB(){this.hg=null}CB.prototype=new LC;CB.prototype.constructor=CB;CB.prototype.b=function(){BB=this;this.hg=Ak("background-color");return this};CB.prototype.ue=function(){return this.hg};CB.prototype.$classData=l({Cu:0},!1,"scalacss.internal.Attrs$backgroundColor$",{Cu:1,Qv:1,Cj:1,Se:1,c:1,Mv:1,pl:1});var BB=void 0;
function lB(){this.hg=null}lB.prototype=new LC;lB.prototype.constructor=lB;lB.prototype.b=function(){kB=this;this.hg=Ak("color");return this};lB.prototype.ue=function(){return this.hg};lB.prototype.$classData=l({Du:0},!1,"scalacss.internal.Attrs$color$",{Du:1,Qv:1,Cj:1,Se:1,c:1,Mv:1,pl:1});var kB=void 0;function oE(){bl.call(this)}oE.prototype=new cl;oE.prototype.constructor=oE;d=oE.prototype;d.b=function(){bl.prototype.i.call(this,"moz");return this};d.B=function(){return"moz"};d.z=function(){return 0};
d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"moz"};d.s=function(){return 108312};d.D=function(){return Z(this)};d.$classData=l({Pu:0},!1,"scalacss.internal.CanIUse$Prefix$moz$",{Pu:1,ol:1,c:1,E:1,q:1,k:1,h:1});var pE=void 0;function fl(){pE||(pE=(new oE).b());return pE}function qE(){bl.call(this)}qE.prototype=new cl;qE.prototype.constructor=qE;d=qE.prototype;d.b=function(){bl.prototype.i.call(this,"ms");return this};d.B=function(){return"ms"};d.z=function(){return 0};
d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"ms"};d.s=function(){return 3494};d.D=function(){return Z(this)};d.$classData=l({Qu:0},!1,"scalacss.internal.CanIUse$Prefix$ms$",{Qu:1,ol:1,c:1,E:1,q:1,k:1,h:1});var rE=void 0;function gl(){rE||(rE=(new qE).b());return rE}function sE(){bl.call(this)}sE.prototype=new cl;sE.prototype.constructor=sE;d=sE.prototype;d.b=function(){bl.prototype.i.call(this,"o");return this};d.B=function(){return"o"};d.z=function(){return 0};
d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"o"};d.s=function(){return 111};d.D=function(){return Z(this)};d.$classData=l({Ru:0},!1,"scalacss.internal.CanIUse$Prefix$o$",{Ru:1,ol:1,c:1,E:1,q:1,k:1,h:1});var tE=void 0;function hl(){tE||(tE=(new sE).b());return tE}function uE(){bl.call(this)}uE.prototype=new cl;uE.prototype.constructor=uE;d=uE.prototype;d.b=function(){bl.prototype.i.call(this,"webkit");return this};d.B=function(){return"webkit"};d.z=function(){return 0};
d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"webkit"};d.s=function(){return-791796990};d.D=function(){return Z(this)};d.$classData=l({Su:0},!1,"scalacss.internal.CanIUse$Prefix$webkit$",{Su:1,ol:1,c:1,E:1,q:1,k:1,h:1});var vE=void 0;function il(){vE||(vE=(new uE).b());return vE}function wE(){}wE.prototype=new n;wE.prototype.constructor=wE;d=wE.prototype;d.b=function(){return this};d.B=function(){return"Full"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Full"};
d.s=function(){return 2201263};d.D=function(){return Z(this)};d.$classData=l({Tu:0},!1,"scalacss.internal.CanIUse$Support$Full$",{Tu:1,c:1,Lh:1,E:1,q:1,k:1,h:1});var xE=void 0;function O(){xE||(xE=(new wE).b());return xE}function yE(){}yE.prototype=new n;yE.prototype.constructor=yE;d=yE.prototype;d.b=function(){return this};d.B=function(){return"FullX"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"FullX"};d.s=function(){return 68239241};d.D=function(){return Z(this)};
d.$classData=l({Uu:0},!1,"scalacss.internal.CanIUse$Support$FullX$",{Uu:1,c:1,Lh:1,E:1,q:1,k:1,h:1});var zE=void 0;function Xk(){zE||(zE=(new yE).b());return zE}function AE(){}AE.prototype=new n;AE.prototype.constructor=AE;d=AE.prototype;d.b=function(){return this};d.B=function(){return"Partial"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Partial"};d.s=function(){return 871719265};d.D=function(){return Z(this)};
d.$classData=l({Vu:0},!1,"scalacss.internal.CanIUse$Support$Partial$",{Vu:1,c:1,Lh:1,E:1,q:1,k:1,h:1});var BE=void 0;function Zk(){BE||(BE=(new AE).b());return BE}function CE(){}CE.prototype=new n;CE.prototype.constructor=CE;d=CE.prototype;d.b=function(){return this};d.B=function(){return"PartialX"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"PartialX"};d.s=function(){return 1253493527};d.D=function(){return Z(this)};
d.$classData=l({Wu:0},!1,"scalacss.internal.CanIUse$Support$PartialX$",{Wu:1,c:1,Lh:1,E:1,q:1,k:1,h:1});var DE=void 0;function N(){DE||(DE=(new CE).b());return DE}function EE(){}EE.prototype=new n;EE.prototype.constructor=EE;d=EE.prototype;d.b=function(){return this};d.B=function(){return"Unknown"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Unknown"};d.s=function(){return 1379812394};d.D=function(){return Z(this)};
d.$classData=l({Xu:0},!1,"scalacss.internal.CanIUse$Support$Unknown$",{Xu:1,c:1,Lh:1,E:1,q:1,k:1,h:1});var FE=void 0;function $k(){FE||(FE=(new EE).b());return FE}function GE(){}GE.prototype=new n;GE.prototype.constructor=GE;d=GE.prototype;d.b=function(){return this};d.B=function(){return"Unsupported"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Unsupported"};d.s=function(){return 1716505109};d.D=function(){return Z(this)};
d.$classData=l({Yu:0},!1,"scalacss.internal.CanIUse$Support$Unsupported$",{Yu:1,c:1,Lh:1,E:1,q:1,k:1,h:1});var HE=void 0;function P(){HE||(HE=(new GE).b());return HE}function Mz(){this.nh=this.Ti=null}Mz.prototype=new n;Mz.prototype.constructor=Mz;d=Mz.prototype;d.B=function(){return"Cond"};d.z=function(){return 2};d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.Ln){var b=this.Ti,c=a.Ti;if(null===b?null===c:b.o(c))return b=this.nh,a=a.nh,null===b?null===a:fn(b,a)}return!1};
d.A=function(a){switch(a){case 0:return this.Ti;case 1:return this.nh;default:throw(new Y).i(""+a);}};d.m=function(){var a=Wj(Vj(),this.nh);a.l()?a=B():(a=a.P(),a=(new H).n(um(vm(),a)));return(a.l()?"":a.P()+" ")+am(Bm(),"",this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({Ln:0},!1,"scalacss.internal.Cond",{Ln:1,c:1,MC:1,E:1,q:1,k:1,h:1});function fm(){this.nj=this.ki=this.ji=this.ii=this.Rc=this.Lb=null}fm.prototype=new n;fm.prototype.constructor=fm;d=fm.prototype;
d.B=function(){return"FontFace"};d.z=function(){return 6};d.o=function(a){if(this===a)return!0;if(sm(a)){if(this.Lb===a.Lb)var b=this.Rc,c=a.Rc,b=null===b?null===c:b.o(c);else b=!1;b?(b=this.ii,c=a.ii,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.ji,c=a.ji,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.ki,c=a.ki,b=null===b?null===c:b.o(c)):b=!1;if(b)return b=this.nj,a=a.nj,null===b?null===a:b.o(a)}return!1};
d.A=function(a){switch(a){case 0:return this.Lb;case 1:return this.Rc;case 2:return this.ii;case 3:return this.ji;case 4:return this.ki;case 5:return this.nj;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};function sm(a){return!!(a&&a.$classData&&a.$classData.p.Nn)}d.$classData=l({Nn:0},!1,"scalacss.internal.CssEntry$FontFace",{Nn:1,c:1,Mn:1,E:1,q:1,k:1,h:1});function IE(){this.li=this.qa=null}IE.prototype=new n;
IE.prototype.constructor=IE;function zm(a,b){var c=new IE;c.qa=a;c.li=b;return c}d=IE.prototype;d.B=function(){return"Keyframes"};d.z=function(){return 2};d.o=function(a){if(this===a)return!0;if(rm(a)){var b=this.qa,c=a.qa;if(null===b?null===c:b.o(c))return b=this.li,a=a.li,null===b?null===a:xC(b,a)}return!1};d.A=function(a){switch(a){case 0:return this.qa;case 1:return this.li;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
function rm(a){return!!(a&&a.$classData&&a.$classData.p.On)}d.$classData=l({On:0},!1,"scalacss.internal.CssEntry$Keyframes",{On:1,c:1,Mn:1,E:1,q:1,k:1,h:1});function JE(){this.mg=this.yh=this.kf=null}JE.prototype=new n;JE.prototype.constructor=JE;d=JE.prototype;d.B=function(){return"Style"};d.z=function(){return 3};d.o=function(a){if(this===a)return!0;if(qm(a)){var b=this.kf,c=a.kf;if((null===b?null===c:b.o(c))&&this.yh===a.yh)return b=this.mg,a=a.mg,null===b?null===a:b.o(a)}return!1};
d.A=function(a){switch(a){case 0:return this.kf;case 1:return this.yh;case 2:return this.mg;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};function om(a,b,c){var e=new JE;e.kf=a;e.yh=b;e.mg=c;return e}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};function qm(a){return!!(a&&a.$classData&&a.$classData.p.Pn)}d.$classData=l({Pn:0},!1,"scalacss.internal.CssEntry$Style",{Pn:1,c:1,Mn:1,E:1,q:1,k:1,h:1});function KE(){this.Mh=null}KE.prototype=new CC;
KE.prototype.constructor=KE;KE.prototype.b=function(){Ep.prototype.b.call(this);return this};KE.prototype.$classData=l({jv:0},!1,"scalacss.internal.Dsl$",{jv:1,kv:1,wu:1,Pv:1,c:1,Lv:1,pl:1});var LE=void 0;function mB(){LE||(LE=(new KE).b());return LE}function hB(){this.t=null}hB.prototype=new Tm;hB.prototype.constructor=hB;d=hB.prototype;d.b=function(){Sm.prototype.i.call(this,"vw");return this};d.B=function(){return"vw"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"vw"};
d.s=function(){return 3777};d.D=function(){return Z(this)};d.$classData=l({ov:0},!1,"scalacss.internal.LengthUnit$vw$",{ov:1,sC:1,c:1,E:1,q:1,k:1,h:1});var gB=void 0;function Ym(a){return!!(a&&a.$classData&&a.$classData.p.zC)}function $m(a){return!!(a&&a.$classData&&a.$classData.p.DC)}function Zm(a){return!!(a&&a.$classData&&a.$classData.p.EC)}function Cp(){this.Tb=this.Mh=null}Cp.prototype=new CC;Cp.prototype.constructor=Cp;
Cp.prototype.$classData=l({aw:0},!1,"scalacss.internal.mutable.StyleSheet$Base$dsl$",{aw:1,kv:1,wu:1,Pv:1,c:1,Lv:1,pl:1});function I(){this.U=this.R=null}I.prototype=new n;I.prototype.constructor=I;d=I.prototype;d.B=function(){return"Tuple2"};d.z=function(){return 2};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.ho?D(G(),this.R,a.R)&&D(G(),this.U,a.U):!1};d.d=function(a,b){this.R=a;this.U=b;return this};
d.A=function(a){a:switch(a){case 0:a=this.R;break a;case 1:a=this.U;break a;default:throw(new Y).i(""+a);}return a};d.m=function(){return"("+this.R+","+this.U+")"};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({ho:0},!1,"scala.Tuple2",{ho:1,c:1,vD:1,E:1,q:1,k:1,h:1});function tm(){this.Ug=this.Tg=this.Sg=null}tm.prototype=new n;tm.prototype.constructor=tm;d=tm.prototype;d.B=function(){return"Tuple3"};d.z=function(){return 3};
d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.io?D(G(),this.Sg,a.Sg)&&D(G(),this.Tg,a.Tg)&&D(G(),this.Ug,a.Ug):!1};d.A=function(a){a:switch(a){case 0:a=this.Sg;break a;case 1:a=this.Tg;break a;case 2:a=this.Ug;break a;default:throw(new Y).i(""+a);}return a};d.m=function(){return"("+this.Sg+","+this.Tg+","+this.Ug+")"};d.Kf=function(a,b,c){this.Sg=a;this.Tg=b;this.Ug=c;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({io:0},!1,"scala.Tuple3",{io:1,c:1,wD:1,E:1,q:1,k:1,h:1});function hA(){this.Aa=null}hA.prototype=new OC;hA.prototype.constructor=hA;hA.prototype.i=function(a){Fw.prototype.Sa.call(this,a,0,0,!0);return this};hA.prototype.$classData=l({Lw:0},!1,"java.lang.NumberFormatException",{Lw:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function qu(){this.Aa=null}qu.prototype=new PC;qu.prototype.constructor=qu;qu.prototype.b=function(){Fw.prototype.Sa.call(this,null,0,0,!0);return this};
qu.prototype.$classData=l({ix:0},!1,"java.util.FormatterClosedException",{ix:1,Ep:1,qb:1,tb:1,ab:1,c:1,h:1});function ME(){this.Aa=null}ME.prototype=new OC;ME.prototype.constructor=ME;function NE(){}NE.prototype=ME.prototype;function OE(){}OE.prototype=new XC;OE.prototype.constructor=OE;d=OE.prototype;d.b=function(){return this};d.B=function(){return"None"};d.z=function(){return 0};d.l=function(){return!0};d.P=function(){throw(new Mw).i("None.get");};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"None"};
d.s=function(){return 2433880};d.D=function(){return Z(this)};d.$classData=l({Ix:0},!1,"scala.None$",{Ix:1,Jx:1,c:1,E:1,q:1,k:1,h:1});var PE=void 0;function B(){PE||(PE=(new OE).b());return PE}function oq(){}oq.prototype=new ZA;oq.prototype.constructor=oq;oq.prototype.b=function(){return this};oq.prototype.Zc=function(){return!0};oq.prototype.wf=function(){return qq().th};oq.prototype.$classData=l({Nx:0},!1,"scala.PartialFunction$$anonfun$1",{Nx:1,vB:1,c:1,T:1,Va:1,k:1,h:1});
function H(){this.J=null}H.prototype=new XC;H.prototype.constructor=H;d=H.prototype;d.B=function(){return"Some"};d.z=function(){return 1};d.o=function(a){return this===a?!0:If(a)?D(G(),this.J,a.J):!1};d.l=function(){return!1};d.A=function(a){switch(a){case 0:return this.J;default:throw(new Y).i(""+a);}};d.P=function(){return this.J};d.m=function(){return ql(rl(),this)};d.n=function(a){this.J=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
function If(a){return!!(a&&a.$classData&&a.$classData.p.rq)}d.$classData=l({rq:0},!1,"scala.Some",{rq:1,Jx:1,c:1,E:1,q:1,k:1,h:1});function aD(){this.Aa=null}aD.prototype=new OC;aD.prototype.constructor=aD;
aD.prototype.qi=function(a,b){DA(J(),0<=b&&b<(a.length|0));if(b===(-1+(a.length|0)|0))var c="at terminal";else c=65535&(a.charCodeAt(1+b|0)|0),c="'\\"+tu(c)+"' not one of [\\b, \\t, \\n, \\f, \\r, \\\\, \\\", \\'] at";Fw.prototype.Sa.call(this,"invalid escape "+c+" index "+b+' in "'+a+'". Use \\\\ for literal \\.',0,0,!0);return this};aD.prototype.$classData=l({Wx:0},!1,"scala.StringContext$InvalidEscapeException",{Wx:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function Bc(){this.J=null}Bc.prototype=new cD;
Bc.prototype.constructor=Bc;d=Bc.prototype;d.B=function(){return"Left"};d.z=function(){return 1};d.o=function(a){return this===a?!0:cn(a)?D(G(),this.J,a.J):!1};d.A=function(a){switch(a){case 0:return this.J;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.n=function(a){this.J=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};function cn(a){return!!(a&&a.$classData&&a.$classData.p.uq)}
d.$classData=l({uq:0},!1,"scala.util.Left",{uq:1,Cy:1,c:1,E:1,q:1,k:1,h:1});function jh(){this.J=null}jh.prototype=new cD;jh.prototype.constructor=jh;d=jh.prototype;d.B=function(){return"Right"};d.z=function(){return 1};d.o=function(a){return this===a?!0:bn(a)?D(G(),this.J,a.J):!1};d.A=function(a){switch(a){case 0:return this.J;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.n=function(a){this.J=a;return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
function bn(a){return!!(a&&a.$classData&&a.$classData.p.vq)}d.$classData=l({vq:0},!1,"scala.util.Right",{vq:1,Cy:1,c:1,E:1,q:1,k:1,h:1});function QE(a,b){if(b&&b.$classData&&b.$classData.p.jc){var c;if(!(c=a===b)&&(c=a.v()===b.v()))try{c=a.Sm(b)}catch(e){if(e&&e.$classData&&e.$classData.p.zw)c=!1;else throw e;}a=c}else a=!1;return a}function RE(a,b,c){c=c.xf(a.Sf());a.M(t(new u,function(a,b,c){return function(a){return c.db(b.f(a).Ba())}}(a,b,c)));return c.Ja()}
function ll(a,b){b=b.Xg();zy(b,a);b.db(a.Ta());return b.Ja()}function gn(a){return a.Mf(a.Ad()+"(",", ",")")}function Xj(a,b){var c=a.ba();a.M(t(new u,function(a,b,c,m){return function(a){return!!b.f(a)!==c?m.kb(a):void 0}}(a,b,!1,c)));return c.Ja()}function SE(a,b,c){c=c.xf(a.Sf());if(b&&b.$classData&&b.$classData.p.Wf){var e=b.Ba().v();Ay(c,a,e)}c.db(a.Ta());c.db(b.Ba());return c.Ja()}function TE(a){if(a.l())throw(new Jw).i("empty.tail");return a.xe(1)}
function ul(a,b){b=b.xf(a.Sf());zy(b,a);return b}
function UE(a){a=Ib(la(a.Sf()));for(var b=-1+(a.length|0)|0;;)if(-1!==b&&36===(65535&(a.charCodeAt(b)|0)))b=-1+b|0;else break;if(-1===b||46===(65535&(a.charCodeAt(b)|0)))return"";for(var c="";;){for(var e=1+b|0;;)if(-1!==b&&57>=(65535&(a.charCodeAt(b)|0))&&48<=(65535&(a.charCodeAt(b)|0)))b=-1+b|0;else break;for(var f=b;;)if(-1!==b&&36!==(65535&(a.charCodeAt(b)|0))&&46!==(65535&(a.charCodeAt(b)|0)))b=-1+b|0;else break;var h=1+b|0;if(b===f&&e!==(a.length|0))return c;for(;;)if(-1!==b&&36===(65535&(a.charCodeAt(b)|
0)))b=-1+b|0;else break;var f=-1===b?!0:46===(65535&(a.charCodeAt(b)|0)),m;(m=f)||(m=65535&(a.charCodeAt(h)|0),m=!(90<m&&127>m||65>m));if(m){e=a.substring(h,e);h=c;if(null===h)throw(new Ic).b();c=""===h?e:""+e+tu(46)+c;if(f)return c}}}function VE(){this.Ra=null}VE.prototype=new NA;VE.prototype.constructor=VE;function WE(){}WE.prototype=VE.prototype;function XE(){CD.call(this)}XE.prototype=new DD;XE.prototype.constructor=XE;XE.prototype.ip=function(a){return YE(a)};
XE.prototype.$classData=l({Bz:0},!1,"scala.collection.immutable.HashMap$HashTrieMap$$anon$1",{Bz:1,tA:1,yb:1,c:1,vb:1,G:1,F:1});function ZE(){CD.call(this)}ZE.prototype=new DD;ZE.prototype.constructor=ZE;ZE.prototype.ip=function(a){return a.Nb};ZE.prototype.$classData=l({Gz:0},!1,"scala.collection.immutable.HashSet$HashTrieSet$$anon$1",{Gz:1,tA:1,yb:1,c:1,vb:1,G:1,F:1});function $E(){}$E.prototype=new vD;$E.prototype.constructor=$E;$E.prototype.b=function(){return this};$E.prototype.Wj=function(){return Q()};
$E.prototype.$classData=l({eA:0},!1,"scala.collection.immutable.Set$",{eA:1,Bq:1,wh:1,vh:1,zb:1,c:1,Ab:1});var aF=void 0;function yA(){aF||(aF=(new $E).b());return aF}function bF(){this.td=null}bF.prototype=new TD;bF.prototype.constructor=bF;bF.prototype.b=function(){SD.prototype.b.call(this);return this};bF.prototype.Ja=function(){return cF(this)};function cF(a){return a.td.Ka.xc().sg(t(new u,function(){return function(a){return a.xc()}}(a)),(Hq(),(new QA).b()))}
function dF(a){return!!(a&&a.$classData&&a.$classData.p.Kq)}bF.prototype.$classData=l({Kq:0},!1,"scala.collection.immutable.Stream$StreamBuilder",{Kq:1,aE:1,c:1,dj:1,Qc:1,Oc:1,Mc:1});function Hl(){this.bi=this.Fg=this.Th=0;this.Ko=this.Io=this.Go=this.Eo=this.Co=this.ci=null}Hl.prototype=new n;Hl.prototype.constructor=Hl;d=Hl.prototype;d.W=function(){return this.Go};d.b=function(){this.ci=k(r(p),[32]);this.bi=1;this.Fg=this.Th=0;return this};d.mb=function(){return this.bi};
d.Ob=function(a){return Il(this,a)};d.Cf=function(a){this.Ko=a};d.Wa=function(){return this.ci};d.Ca=function(a){this.Eo=a};d.Ia=function(){return this.Io};
function Il(a,b){if(a.Fg>=a.ci.e.length){var c=32+a.Th|0,e=a.Th^c;if(1024>e)1===a.mb()&&(a.X(k(r(p),[32])),a.x().e[0]=a.Wa(),a.kd(1+a.mb()|0)),a.za(k(r(p),[32])),a.x().e[31&(c>>>5|0)]=a.Wa();else if(32768>e)2===a.mb()&&(a.Ca(k(r(p),[32])),a.K().e[0]=a.x(),a.kd(1+a.mb()|0)),a.za(k(r(p),[32])),a.X(k(r(p),[32])),a.x().e[31&(c>>>5|0)]=a.Wa(),a.K().e[31&(c>>>10|0)]=a.x();else if(1048576>e)3===a.mb()&&(a.Xa(k(r(p),[32])),a.W().e[0]=a.K(),a.kd(1+a.mb()|0)),a.za(k(r(p),[32])),a.X(k(r(p),[32])),a.Ca(k(r(p),
[32])),a.x().e[31&(c>>>5|0)]=a.Wa(),a.K().e[31&(c>>>10|0)]=a.x(),a.W().e[31&(c>>>15|0)]=a.K();else if(33554432>e)4===a.mb()&&(a.ic(k(r(p),[32])),a.Ia().e[0]=a.W(),a.kd(1+a.mb()|0)),a.za(k(r(p),[32])),a.X(k(r(p),[32])),a.Ca(k(r(p),[32])),a.Xa(k(r(p),[32])),a.x().e[31&(c>>>5|0)]=a.Wa(),a.K().e[31&(c>>>10|0)]=a.x(),a.W().e[31&(c>>>15|0)]=a.K(),a.Ia().e[31&(c>>>20|0)]=a.W();else if(1073741824>e)5===a.mb()&&(a.Cf(k(r(p),[32])),a.oc().e[0]=a.Ia(),a.kd(1+a.mb()|0)),a.za(k(r(p),[32])),a.X(k(r(p),[32])),a.Ca(k(r(p),
[32])),a.Xa(k(r(p),[32])),a.ic(k(r(p),[32])),a.x().e[31&(c>>>5|0)]=a.Wa(),a.K().e[31&(c>>>10|0)]=a.x(),a.W().e[31&(c>>>15|0)]=a.K(),a.Ia().e[31&(c>>>20|0)]=a.W(),a.oc().e[31&(c>>>25|0)]=a.Ia();else throw(new Ob).b();a.Th=c;a.Fg=0}a.ci.e[a.Fg]=b;a.Fg=1+a.Fg|0;return a}d.Ja=function(){return Jl(this)};d.gd=function(a,b){By(this,a,b)};d.X=function(a){this.Co=a};d.ic=function(a){this.Io=a};d.x=function(){return this.Co};d.oc=function(){return this.Ko};
function Jl(a){var b=a.Th+a.Fg|0;if(0===b)return jl().dc;var c=(new eF).Ic(0,b,0);Ub(c,a,a.bi);1<a.bi&&Rb(c,0,-1+b|0);return c}d.kb=function(a){return Il(this,a)};d.wc=function(){};d.kd=function(a){this.bi=a};d.K=function(){return this.Eo};d.za=function(a){this.ci=a};d.db=function(a){return jm(this,a)};d.Xa=function(a){this.Go=a};d.$classData=l({wA:0},!1,"scala.collection.immutable.VectorBuilder",{wA:1,c:1,dj:1,Qc:1,Oc:1,Mc:1,Oq:1});
function fF(){this.Ml=this.jb=this.yf=this.Ll=0;this.Pb=!1;this.Gl=0;this.Lo=this.Jo=this.Ho=this.Fo=this.Do=this.Hl=null}fF.prototype=new dC;fF.prototype.constructor=fF;d=fF.prototype;
d.r=function(){if(!this.Pb)throw(new Mw).i("reached iterator end");var a=this.Hl.e[this.jb];this.jb=1+this.jb|0;if(this.jb===this.Ml)if((this.yf+this.jb|0)<this.Ll){var b=32+this.yf|0,c=this.yf^b;if(1024>c)this.za(this.x().e[31&(b>>>5|0)]);else if(32768>c)this.X(this.K().e[31&(b>>>10|0)]),this.za(this.x().e[0]);else if(1048576>c)this.Ca(this.W().e[31&(b>>>15|0)]),this.X(this.K().e[0]),this.za(this.x().e[0]);else if(33554432>c)this.Xa(this.Ia().e[31&(b>>>20|0)]),this.Ca(this.W().e[0]),this.X(this.K().e[0]),
this.za(this.x().e[0]);else if(1073741824>c)this.ic(this.oc().e[31&(b>>>25|0)]),this.Xa(this.Ia().e[0]),this.Ca(this.W().e[0]),this.X(this.K().e[0]),this.za(this.x().e[0]);else throw(new Ob).b();this.yf=b;b=this.Ll-this.yf|0;this.Ml=32>b?b:32;this.jb=0}else this.Pb=!1;return a};d.W=function(){return this.Ho};d.mb=function(){return this.Gl};d.Cf=function(a){this.Lo=a};d.df=function(a,b){this.Ll=b;this.yf=-32&a;this.jb=31&a;a=b-this.yf|0;this.Ml=32>a?a:32;this.Pb=(this.yf+this.jb|0)<b;return this};


;d.$classData=l({xA:0},!1,"scala.collection.immutable.VectorIterator",{xA:1,yb:1,c:1,vb:1,G:1,F:1,Oq:1});function gF(){}gF.prototype=new xD;
gF.prototype.constructor=gF;gF.prototype.b=function(){return this};gF.prototype.ae=function(){return(new hF).b()};gF.prototype.$classData=l({eB:0},!1,"scala.collection.mutable.Set$",{eB:1,Dq:1,wh:1,vh:1,zb:1,c:1,Ab:1});var iF=void 0;function jF(){this.Ii=this.$c=null}jF.prototype=new n;jF.prototype.constructor=jF;d=jF.prototype;d.B=function(){return"RedirectToPage"};d.z=function(){return 2};
d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.gl?D(G(),this.$c,a.$c)?this.Ii===a.Ii:!1:!1};d.A=function(a){switch(a){case 0:return this.$c;case 1:return this.Ii;default:throw(new Y).i(""+a);}};d.m=function(){return ql(rl(),this)};d.s=function(){return Lm(U(),this)};function xc(a,b){var c=new jF;c.$c=a;c.Ii=b;return c}d.D=function(){return Z(this)};d.$classData=l({gl:0},!1,"japgolly.scalajs.react.extra.router.RedirectToPage",{gl:1,c:1,Is:1,Gs:1,E:1,q:1,k:1,h:1});
function kF(){}kF.prototype=new vC;kF.prototype.constructor=kF;function lF(){}lF.prototype=kF.prototype;kF.prototype.rl=function(a){a=null===a?"null":ka(a);wp(this,null===a?"null":a);return this};kF.prototype.lw=function(){uC.prototype.kw.call(this);return this};function Sk(){}Sk.prototype=new Xv;Sk.prototype.constructor=Sk;d=Sk.prototype;d.b=function(){return this};d.B=function(){return"Overlap"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Overlap"};
d.s=function(){return 573365287};d.D=function(){return Z(this)};d.$classData=l({zu:0},!1,"scalacss.internal.AttrCmp$Overlap$",{zu:1,yu:1,Gn:1,c:1,E:1,q:1,k:1,h:1});var Rk=void 0;function Pk(){}Pk.prototype=new Xv;Pk.prototype.constructor=Pk;d=Pk.prototype;d.b=function(){return this};d.B=function(){return"Same"};d.z=function(){return 0};d.A=function(a){throw(new Y).i(""+a);};d.m=function(){return"Same"};d.s=function(){return 2569350};d.D=function(){return Z(this)};
d.$classData=l({Au:0},!1,"scalacss.internal.AttrCmp$Same$",{Au:1,yu:1,Gn:1,c:1,E:1,q:1,k:1,h:1});var Ok=void 0;function on(){}on.prototype=new n;on.prototype.constructor=on;on.prototype.b=function(){return this};on.prototype.$classData=l({yv:0},!1,"scalacss.internal.Pseudo$$anon$2",{yv:1,c:1,cy:1,dx:1,ey:1,Xx:1,k:1,h:1});function rn(){}rn.prototype=new n;rn.prototype.constructor=rn;rn.prototype.b=function(){return this};
rn.prototype.$classData=l({Av:0},!1,"scalacss.internal.PseudoType$$anon$1",{Av:1,c:1,cy:1,dx:1,ey:1,Xx:1,k:1,h:1});function mo(){this.sf=this.Cd=this.lg=this.qe=this.Zd=null}mo.prototype=new qw;mo.prototype.constructor=mo;d=mo.prototype;d.B=function(){return"StyleS"};d.z=function(){return 5};
d.o=function(a){if(this===a)return!0;if(a&&a.$classData&&a.$classData.p.bo){var b=this.Zd,c=a.Zd;(null===b?null===c:xC(b,c))?(b=this.qe,c=a.qe,b=null===b?null===c:fn(b,c)):b=!1;b?(b=this.lg,c=a.lg,b=null===b?null===c:b.o(c)):b=!1;b?(b=this.Cd,c=a.Cd,b=null===b?null===c:fn(b,c)):b=!1;if(b)return b=this.sf,a=a.sf,null===b?null===a:fn(b,a)}return!1};
d.A=function(a){switch(a){case 0:return this.Zd;case 1:return this.qe;case 2:return this.lg;case 3:return this.Cd;case 4:return this.sf;default:throw(new Y).i(""+a);}};function lo(a,b,c,e,f,h){a.Zd=b;a.qe=c;a.lg=e;a.Cd=f;a.sf=h;return a}d.m=function(){return tn().Ij(mF(this))};function mF(a){var b=Bm(),c=a.lg;return hm(b,mp(new np,c.l()?(new jo).i("???"):c.P(),a.Cd,a),Uz().Y)}d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};
d.$classData=l({bo:0},!1,"scalacss.internal.StyleS",{bo:1,OC:1,c:1,NC:1,E:1,q:1,k:1,h:1});function LB(){}LB.prototype=new SC;LB.prototype.constructor=LB;LB.prototype.b=function(){return this};LB.prototype.v=function(){return 0};LB.prototype.rc=function(){var a=cq();0===(8&a.Q)<<24>>24&&0===(8&a.Q)<<24>>24&&(a.fn=(new Kw).b(),a.Q=(8|a.Q)<<24>>24);return a.fn};LB.prototype.$classData=l({Vw:0},!1,"java.util.Collections$$anon$11",{Vw:1,dm:1,cm:1,c:1,lh:1,wi:1,xi:1,h:1});
function uu(){this.Hf=this.Aa=null}uu.prototype=new NE;uu.prototype.constructor=uu;uu.prototype.md=function(){return"Flags \x3d '"+this.Hf+"'"};uu.prototype.i=function(a){this.Hf=a;Fw.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ic).b();return this};uu.prototype.$classData=l({ex:0},!1,"java.util.DuplicateFormatFlagsException",{ex:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function UB(){this.Hf=this.Aa=null;this.zf=0}UB.prototype=new NE;UB.prototype.constructor=UB;
UB.prototype.md=function(){return"Conversion \x3d "+tu(this.zf)+", Flags \x3d "+this.Hf};UB.prototype.$classData=l({fx:0},!1,"java.util.FormatFlagsConversionMismatchException",{fx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function nF(){this.ee=null}nF.prototype=new Iw;nF.prototype.constructor=nF;d=nF.prototype;d.b=function(){nF.prototype.sw.call(this,(new Mp).b());return this};d.xk=function(a,b){a=this.ee.ym((new VC).n(a),b);return a.l()?null:a.P()};d.sw=function(a){this.ee=a;return this};
d.mi=function(a){a=this.ee.sb((new VC).n(a));return a.l()?null:a.P()};d.Zh=function(a){return this.ee.O((new VC).n(a))};d.v=function(){return this.ee.v()};d.be=function(){return(new oF).Vl(this)};d.Ck=function(a){a=(new VC).n(a);var b=this.ee.sb(a);if(b.l())return null;b=b.P();this.ee.sj(a);return b};d.$classData=l({jx:0},!1,"java.util.HashMap",{jx:1,Rw:1,c:1,fm:1,k:1,h:1,he:1,Hd:1});function oF(){this.Ke=null}oF.prototype=new SC;oF.prototype.constructor=oF;oF.prototype.v=function(){return this.Ke.ee.v()};
oF.prototype.Vl=function(a){if(null===a)throw Dp(ni(),null);this.Ke=a;return this};oF.prototype.rc=function(){var a=new mA;if(null===this)throw Dp(ni(),null);a.sa=this;Lx.prototype.Vl.call(a,this.Ke);return a};oF.prototype.$classData=l({kx:0},!1,"java.util.HashMap$EntrySet",{kx:1,dm:1,cm:1,c:1,lh:1,wi:1,xi:1,iD:1});function Cu(){this.Aa=null;this.zf=0}Cu.prototype=new NE;Cu.prototype.constructor=Cu;Cu.prototype.md=function(){return"Code point \x3d 0x"+(+(this.zf>>>0)).toString(16)};
Cu.prototype.ob=function(a){this.zf=a;Fw.prototype.Sa.call(this,null,0,0,!0);return this};Cu.prototype.$classData=l({ox:0},!1,"java.util.IllegalFormatCodePointException",{ox:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function TB(){this.Aa=null;this.zf=0;this.lo=null}TB.prototype=new NE;TB.prototype.constructor=TB;TB.prototype.md=function(){return g.String.fromCharCode(this.zf)+" !\x3d "+Ib(this.lo)};
TB.prototype.$classData=l({px:0},!1,"java.util.IllegalFormatConversionException",{px:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function Nu(){this.Hf=this.Aa=null}Nu.prototype=new NE;Nu.prototype.constructor=Nu;Nu.prototype.md=function(){return"Flags \x3d '"+this.Hf+"'"};Nu.prototype.i=function(a){this.Hf=a;Fw.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ic).b();return this};Nu.prototype.$classData=l({qx:0},!1,"java.util.IllegalFormatFlagsException",{qx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});
function Au(){this.Aa=null;this.aq=0}Au.prototype=new NE;Au.prototype.constructor=Au;Au.prototype.md=function(){return""+this.aq};Au.prototype.ob=function(a){this.aq=a;Fw.prototype.Sa.call(this,null,0,0,!0);return this};Au.prototype.$classData=l({rx:0},!1,"java.util.IllegalFormatPrecisionException",{rx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function Qu(){this.Aa=null;this.or=0}Qu.prototype=new NE;Qu.prototype.constructor=Qu;Qu.prototype.md=function(){return""+this.or};
Qu.prototype.ob=function(a){this.or=a;Fw.prototype.Sa.call(this,null,0,0,!0);return this};Qu.prototype.$classData=l({sx:0},!1,"java.util.IllegalFormatWidthException",{sx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function xu(){this.Tf=this.Aa=null}xu.prototype=new NE;xu.prototype.constructor=xu;xu.prototype.md=function(){return"Format specifier '"+this.Tf+"'"};xu.prototype.i=function(a){this.Tf=a;Fw.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ic).b();return this};
xu.prototype.$classData=l({vx:0},!1,"java.util.MissingFormatArgumentException",{vx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function wu(){this.Tf=this.Aa=null}wu.prototype=new NE;wu.prototype.constructor=wu;wu.prototype.md=function(){return this.Tf};wu.prototype.i=function(a){this.Tf=a;Fw.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ic).b();return this};wu.prototype.$classData=l({wx:0},!1,"java.util.MissingFormatWidthException",{wx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});
function su(){this.Tf=this.Aa=null}su.prototype=new NE;su.prototype.constructor=su;su.prototype.md=function(){return"Conversion \x3d '"+this.Tf+"'"};su.prototype.i=function(a){this.Tf=a;Fw.prototype.Sa.call(this,null,0,0,!0);if(null===a)throw(new Ic).b();return this};su.prototype.$classData=l({yx:0},!1,"java.util.UnknownFormatConversionException",{yx:1,ff:1,od:1,qb:1,tb:1,ab:1,c:1,h:1});function pF(){this.Ra=null}pF.prototype=new WE;pF.prototype.constructor=pF;
pF.prototype.b=function(){oy.prototype.b.call(this);return this};pF.prototype.ba=function(){qF();return(new zg).b()};pF.prototype.$classData=l({dz:0},!1,"scala.collection.Seq$",{dz:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var rF=void 0;function zq(){rF||(rF=(new pF).b());return rF}function sF(){this.Ra=null}sF.prototype=new WE;sF.prototype.constructor=sF;function tF(){}tF.prototype=sF.prototype;function uF(){}uF.prototype=new PA;uF.prototype.constructor=uF;
uF.prototype.b=function(){vF=this;wy(new vy,Eb(function(){return function(a){return a}}(this)));return this};function wF(a,b,c,e,f,h,m){var q=31&(b>>>h|0),y=31&(e>>>h|0);if(q!==y)return a=1<<q|1<<y,b=k(r(xF),[2]),q<y?(b.e[0]=c,b.e[1]=f):(b.e[0]=f,b.e[1]=c),yF(new zF,a,b,m);y=k(r(xF),[1]);q=1<<q;y.e[0]=wF(a,b,c,e,f,5+h|0,m);return yF(new zF,q,y,m)}uF.prototype.Vo=function(){return AF()};uF.prototype.$classData=l({wz:0},!1,"scala.collection.immutable.HashMap$",{wz:1,sz:1,tz:1,pz:1,c:1,UD:1,k:1,h:1});
var vF=void 0;function BF(){vF||(vF=(new uF).b());return vF}function CF(){this.Ra=null}CF.prototype=new WE;CF.prototype.constructor=CF;CF.prototype.b=function(){oy.prototype.b.call(this);return this};CF.prototype.ba=function(){return(new zg).b()};CF.prototype.$classData=l({dA:0},!1,"scala.collection.immutable.Seq$",{dA:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var DF=void 0;function qF(){DF||(DF=(new CF).b());return DF}function EF(){this.Ra=null}EF.prototype=new WE;EF.prototype.constructor=EF;
EF.prototype.b=function(){oy.prototype.b.call(this);return this};EF.prototype.ba=function(){return(new QD).b()};EF.prototype.$classData=l({UA:0},!1,"scala.collection.mutable.IndexedSeq$",{UA:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var FF=void 0;function GF(){this.Ra=null}GF.prototype=new WE;GF.prototype.constructor=GF;GF.prototype.b=function(){oy.prototype.b.call(this);return this};GF.prototype.ba=function(){return(new A).b()};
GF.prototype.$classData=l({jB:0},!1,"scala.scalajs.js.WrappedArray$",{jB:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var HF=void 0;function nB(){HF||(HF=(new GF).b());return HF}function IF(){this.KB=this.JB=this.IB=this.HB=this.lr=this.GB=null}IF.prototype=new Ti;IF.prototype.constructor=IF;function JF(){}JF.prototype=IF.prototype;function KF(){this.Uh=this.wp=null}KF.prototype=new lF;KF.prototype.constructor=KF;function Qp(a){var b=new KF;b.wp=a;(new QC).b();kF.prototype.lw.call(b);b.Uh="";return b}
function wp(a,b){for(;""!==b;){var c=b.indexOf("\n")|0;if(0>c)a.Uh=""+a.Uh+b,b="";else{var e=""+a.Uh+b.substring(0,c);g.console&&(a.wp&&g.console.error?g.console.error(e):g.console.log(e));a.Uh="";b=b.substring(1+c|0)}}}KF.prototype.Mj=function(){};KF.prototype.$classData=l({Fw:0},!1,"java.lang.JSConsoleBasedPrintStream",{Fw:1,kC:1,jC:1,ju:1,c:1,nl:1,Cp:1,En:1,Bp:1});function LF(){this.Sb=null}LF.prototype=new n;LF.prototype.constructor=LF;function MF(){}MF.prototype=LF.prototype;
LF.prototype.o=function(a){return this===a};LF.prototype.m=function(){return this.Sb};LF.prototype.s=function(){return Fa(this)};function NF(){this.Cc=null}NF.prototype=new n;NF.prototype.constructor=NF;function OF(){}OF.prototype=NF.prototype;function PF(){this.Qa=this.Ra=null}PF.prototype=new tF;PF.prototype.constructor=PF;PF.prototype.b=function(){oy.prototype.b.call(this);QF=this;this.Qa=(new JA).b();return this};PF.prototype.ba=function(){Gm();jl();return(new Hl).b()};
PF.prototype.$classData=l({Oy:0},!1,"scala.collection.IndexedSeq$",{Oy:1,Cq:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var QF=void 0;function kl(){QF||(QF=(new PF).b());return QF}function ki(){this.cf=this.Ef=0;this.sa=null}ki.prototype=new dC;ki.prototype.constructor=ki;ki.prototype.r=function(){this.cf>=this.Ef&&Aq().Y.r();var a=this.sa.eb(this.cf);this.cf=1+this.cf|0;return a};function ji(a,b,c,e){a.Ef=e;if(null===b)throw Dp(ni(),null);a.sa=b;a.cf=c;return a}
ki.prototype.u=function(){return this.cf<this.Ef};ki.prototype.Il=function(a){return 0>=a?ji(new ki,this.sa,this.cf,this.Ef):(this.cf+a|0)>=this.Ef?ji(new ki,this.sa,this.Ef,this.Ef):ji(new ki,this.sa,this.cf+a|0,this.Ef)};ki.prototype.$classData=l({Qy:0},!1,"scala.collection.IndexedSeqLike$Elements",{Qy:1,yb:1,c:1,vb:1,G:1,F:1,FD:1,k:1,h:1});function RF(){}RF.prototype=new vD;RF.prototype.constructor=RF;RF.prototype.b=function(){return this};
function SF(a,b,c,e,f,h){var m=31&(b>>>h|0),q=31&(e>>>h|0);if(m!==q)return a=1<<m|1<<q,b=k(r(TF),[2]),m<q?(b.e[0]=c,b.e[1]=f):(b.e[0]=f,b.e[1]=c),UF(new VF,a,b,c.v()+f.v()|0);q=k(r(TF),[1]);m=1<<m;c=SF(a,b,c,e,f,5+h|0);q.e[0]=c;return UF(new VF,m,q,c.nf)}RF.prototype.Wj=function(){return WF()};RF.prototype.$classData=l({Dz:0},!1,"scala.collection.immutable.HashSet$",{Dz:1,Bq:1,wh:1,vh:1,zb:1,c:1,Ab:1,k:1,h:1});var XF=void 0;function YF(){XF||(XF=(new RF).b());return XF}
function ZF(){this.Ra=null}ZF.prototype=new tF;ZF.prototype.constructor=ZF;ZF.prototype.b=function(){oy.prototype.b.call(this);return this};ZF.prototype.ba=function(){jl();return(new Hl).b()};ZF.prototype.$classData=l({Jz:0},!1,"scala.collection.immutable.IndexedSeq$",{Jz:1,Cq:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1});var $F=void 0;function Gm(){$F||($F=(new ZF).b());return $F}function aG(){}aG.prototype=new vD;aG.prototype.constructor=aG;aG.prototype.b=function(){return this};aG.prototype.Wj=function(){return bG()};
aG.prototype.$classData=l({Rz:0},!1,"scala.collection.immutable.ListSet$",{Rz:1,Bq:1,wh:1,vh:1,zb:1,c:1,Ab:1,k:1,h:1});var cG=void 0;function dG(){}dG.prototype=new xD;dG.prototype.constructor=dG;dG.prototype.b=function(){return this};dG.prototype.ae=function(){return(new hF).b()};dG.prototype.$classData=l({PA:0},!1,"scala.collection.mutable.HashSet$",{PA:1,Dq:1,wh:1,vh:1,zb:1,c:1,Ab:1,k:1,h:1});var eG=void 0;function fG(){}fG.prototype=new xD;fG.prototype.constructor=fG;fG.prototype.b=function(){return this};
fG.prototype.ae=function(){return(new gG).b()};fG.prototype.$classData=l({XA:0},!1,"scala.collection.mutable.LinkedHashSet$",{XA:1,Dq:1,wh:1,vh:1,zb:1,c:1,Ab:1,k:1,h:1});var hG=void 0;function Uu(){this.Ff=this.Aa=null}Uu.prototype=new GB;Uu.prototype.constructor=Uu;d=Uu.prototype;d.B=function(){return"JavaScriptException"};d.z=function(){return 1};d.Xj=function(){this.stackdata=this.Ff;return this};d.o=function(a){return this===a?!0:Tu(a)?D(G(),this.Ff,a.Ff):!1};
d.A=function(a){switch(a){case 0:return this.Ff;default:throw(new Y).i(""+a);}};d.md=function(){return ka(this.Ff)};d.n=function(a){this.Ff=a;Fw.prototype.Sa.call(this,null,0,0,!0);return this};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};function Tu(a){return!!(a&&a.$classData&&a.$classData.p.$q)}d.$classData=l({$q:0},!1,"scala.scalajs.js.JavaScriptException",{$q:1,qb:1,tb:1,ab:1,c:1,h:1,E:1,q:1,k:1});function qg(){this.xm=this.iq=this.L=null}qg.prototype=new n;
qg.prototype.constructor=qg;qg.prototype.m=function(){return pf(Fe(),"ComponentDidUpdate(props: "+this.iq+" \u2192 "+ob(this).Nd()+", state: "+this.xm+" \u2192 "+ob(this).Sc()+")")};qg.prototype.oi=function(a,b,c){this.L=a;this.iq=b;this.xm=c;return this};qg.prototype.Hb=function(){return this.L};qg.prototype.$classData=l({ss:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidUpdate",{ss:1,c:1,Kh:1,uf:1,Re:1,Qe:1,Pe:1,Ne:1,Oe:1,yj:1});function tf(){this.L=null}tf.prototype=new n;
tf.prototype.constructor=tf;d=tf.prototype;d.o=function(a){return sf().qg(this.L,a)};d.m=function(){return sf().Mg(this.L)};d.Ya=function(a){this.L=a;return this};d.s=function(){return Aa(this.L)};d.Hb=function(){return this.L};d.$classData=l({nn:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillMount",{nn:1,c:1,zj:1,Kh:1,uf:1,Re:1,Qe:1,Pe:1,Ne:1,Oe:1});function iG(){IF.call(this)}iG.prototype=new JF;iG.prototype.constructor=iG;
iG.prototype.b=function(){Si.prototype.b.call(this);Fb(this);hj(this);return this};iG.prototype.$classData=l({Mt:0},!1,"japgolly.scalajs.react.vdom.PackageBase$",{Mt:1,Lt:1,At:1,c:1,Et:1,Ft:1,Gt:1,Ht:1,Jt:1,It:1});var jG=void 0;function rB(){jG||(jG=(new iG).b())}function kG(){IF.call(this)}kG.prototype=new JF;kG.prototype.constructor=kG;kG.prototype.b=function(){Si.prototype.b.call(this);Fb(this);hj(this);lG=this;Lv||(Lv=(new Kv).b());qB||(qB=(new pB).b());return this};
kG.prototype.$classData=l({Zt:0},!1,"japgolly.scalajs.react.vdom.html_$less$up$",{Zt:1,Lt:1,At:1,c:1,Et:1,Ft:1,Gt:1,Ht:1,Jt:1,It:1});var lG=void 0;function rc(){lG||(lG=(new kG).b());return lG}function OB(){TC.call(this)}OB.prototype=new UC;OB.prototype.constructor=OB;function mG(){}mG.prototype=OB.prototype;OB.prototype.o=function(a){return this.Pa.o(a)};OB.prototype.s=function(){return this.Pa.s()};OB.prototype.dk=function(a){TC.prototype.Ul.call(this,a);return this};
OB.prototype.$classData=l({Fp:0},!1,"java.util.Collections$UnmodifiableSet",{Fp:1,$w:1,c:1,bx:1,lh:1,wi:1,h:1,cx:1,Gp:1,xi:1});function ur(){this.Sb=null}ur.prototype=new MF;ur.prototype.constructor=ur;ur.prototype.b=function(){this.Sb="Boolean";return this};ur.prototype.$classData=l({my:0},!1,"scala.reflect.ManifestFactory$BooleanManifest$",{my:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var tr=void 0;function gr(){this.Sb=null}gr.prototype=new MF;gr.prototype.constructor=gr;
gr.prototype.b=function(){this.Sb="Byte";return this};gr.prototype.$classData=l({ny:0},!1,"scala.reflect.ManifestFactory$ByteManifest$",{ny:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var fr=void 0;function kr(){this.Sb=null}kr.prototype=new MF;kr.prototype.constructor=kr;kr.prototype.b=function(){this.Sb="Char";return this};kr.prototype.$classData=l({oy:0},!1,"scala.reflect.ManifestFactory$CharManifest$",{oy:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var jr=void 0;function sr(){this.Sb=null}
sr.prototype=new MF;sr.prototype.constructor=sr;sr.prototype.b=function(){this.Sb="Double";return this};sr.prototype.$classData=l({py:0},!1,"scala.reflect.ManifestFactory$DoubleManifest$",{py:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var rr=void 0;function qr(){this.Sb=null}qr.prototype=new MF;qr.prototype.constructor=qr;qr.prototype.b=function(){this.Sb="Float";return this};
qr.prototype.$classData=l({qy:0},!1,"scala.reflect.ManifestFactory$FloatManifest$",{qy:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var pr=void 0;function mr(){this.Sb=null}mr.prototype=new MF;mr.prototype.constructor=mr;mr.prototype.b=function(){this.Sb="Int";return this};mr.prototype.$classData=l({ry:0},!1,"scala.reflect.ManifestFactory$IntManifest$",{ry:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var lr=void 0;function or(){this.Sb=null}or.prototype=new MF;or.prototype.constructor=or;
or.prototype.b=function(){this.Sb="Long";return this};or.prototype.$classData=l({sy:0},!1,"scala.reflect.ManifestFactory$LongManifest$",{sy:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var nr=void 0;function nG(){this.Eh=this.Cc=null}nG.prototype=new OF;nG.prototype.constructor=nG;function oG(){}oG.prototype=nG.prototype;nG.prototype.o=function(a){return this===a};nG.prototype.m=function(){return this.Eh};nG.prototype.s=function(){return Fa(this)};function ir(){this.Sb=null}ir.prototype=new MF;
ir.prototype.constructor=ir;ir.prototype.b=function(){this.Sb="Short";return this};ir.prototype.$classData=l({wy:0},!1,"scala.reflect.ManifestFactory$ShortManifest$",{wy:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var hr=void 0;function wr(){this.Sb=null}wr.prototype=new MF;wr.prototype.constructor=wr;wr.prototype.b=function(){this.Sb="Unit";return this};wr.prototype.$classData=l({xy:0},!1,"scala.reflect.ManifestFactory$UnitManifest$",{xy:1,Vf:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var vr=void 0;
function pG(a,b){a=a.C();for(b=b.C();a.u()&&b.u();)if(!D(G(),a.r(),b.r()))return!1;return!a.u()&&!b.u()}function it(){this.sa=this.Fh=null}it.prototype=new dC;it.prototype.constructor=it;d=it.prototype;d.r=function(){return this.Fh.r()};d.B=function(){return"JIteratorWrapper"};d.z=function(){return 1};d.o=function(a){return this===a?!0:a&&a.$classData&&a.$classData.p.yq&&a.sa===this.sa?this.Fh===a.Fh:!1};d.A=function(a){switch(a){case 0:return this.Fh;default:throw(new Y).i(""+a);}};
function ht(a,b,c){a.Fh=c;if(null===b)throw Dp(ni(),null);a.sa=b;return a}d.u=function(){return this.Fh.u()};d.s=function(){return Lm(U(),this)};d.D=function(){return Z(this)};d.$classData=l({yq:0},!1,"scala.collection.convert.Wrappers$JIteratorWrapper",{yq:1,yb:1,c:1,vb:1,G:1,F:1,E:1,q:1,k:1,h:1});function qG(){this.Ra=null}qG.prototype=new WE;qG.prototype.constructor=qG;qG.prototype.b=function(){oy.prototype.b.call(this);rG=this;(new yy).b();return this};qG.prototype.ae=function(){return xb()};
qG.prototype.ba=function(){return(new zg).b()};qG.prototype.$classData=l({Lz:0},!1,"scala.collection.immutable.List$",{Lz:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1,k:1,h:1});var rG=void 0;function yg(){rG||(rG=(new qG).b());return rG}function sG(){this.Ra=null}sG.prototype=new WE;sG.prototype.constructor=sG;sG.prototype.b=function(){oy.prototype.b.call(this);return this};sG.prototype.ae=function(){return gy()};sG.prototype.ba=function(){return(new bF).b()};
sG.prototype.$classData=l({lA:0},!1,"scala.collection.immutable.Stream$",{lA:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1,k:1,h:1});var tG=void 0;function Hq(){tG||(tG=(new sG).b());return tG}function uG(){this.Ra=null}uG.prototype=new WE;uG.prototype.constructor=uG;uG.prototype.b=function(){oy.prototype.b.call(this);return this};uG.prototype.ba=function(){return(new QD).b()};uG.prototype.$classData=l({CA:0},!1,"scala.collection.mutable.ArrayBuffer$",{CA:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1,k:1,h:1});
var vG=void 0;function wG(){this.Ra=null}wG.prototype=new WE;wG.prototype.constructor=wG;wG.prototype.b=function(){oy.prototype.b.call(this);return this};wG.prototype.ba=function(){return nC(new mC,(new zg).b())};wG.prototype.$classData=l({$A:0},!1,"scala.collection.mutable.ListBuffer$",{$A:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1,k:1,h:1});var xG=void 0;function yG(){this.ce=this.Gi=this.om=this.ye=null}yG.prototype=new n;yG.prototype.constructor=yG;function zG(){}d=zG.prototype=yG.prototype;
d.qr=function(a){return this.Hi(this.om,this.Gi,(new $h).ck(this.ce.ye,a.Pk))};d.Nd=function(){return this.ce.Ph(v(new w,function(a){return function(){return a.om.f(a.ye.Nd())}}(this)))};d.Ji=function(a,b){return this.ce.Ph(v(new w,function(a,b,f){return function(){a.ye.Ji(a.Gi.Ld.f(b),f)}}(this,a,b)))};d.Bh=function(a,b){return this.ce.Ph(v(new w,function(a,b,f){return function(){a.ye.Ji(a.Gi.Ah.f(b),f)}}(this,a,b)))};d.pp=function(a,b,c,e){this.ye=a;this.om=b;this.Gi=c;this.ce=e;return this};
d.Sc=function(){return this.ce.Ph(v(new w,function(a){return function(){return a.Gi.eh.f(a.ye.Sc())}}(this)))};function AG(){this.ce=null}AG.prototype=new n;AG.prototype.constructor=AG;function BG(){}BG.prototype=AG.prototype;AG.prototype.qr=function(a){var b=ub().de,c=ti();return this.Hi(b,c.zg,(new $h).ck(this.ce.ye,a.Pk))};AG.prototype.qp=function(a){this.ce=a;return this};function CG(){this.$o=this.L=null}CG.prototype=new n;CG.prototype.constructor=CG;
CG.prototype.m=function(){return pf(Fe(),"ComponentDidCatch("+this.$o+")")};function pg(a,b){var c=new CG;c.L=a;c.$o=b;return c}CG.prototype.Hb=function(){return this.L};CG.prototype.$classData=l({qs:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidCatch",{qs:1,c:1,zj:1,Kh:1,uf:1,Re:1,Qe:1,Pe:1,Ne:1,Oe:1,yj:1});function of(){this.L=null}of.prototype=new n;of.prototype.constructor=of;d=of.prototype;d.o=function(a){return nf().qg(this.L,a)};d.m=function(){return nf().Mg(this.L)};
d.Ya=function(a){this.L=a;return this};d.Bh=function(a,b){return(new Bb).oa(this.Hb().mountedPure.Bh(a,b).pa)};d.s=function(){return Aa(this.L)};d.Hb=function(){return this.L};d.$classData=l({mn:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentDidMount",{mn:1,c:1,zj:1,Kh:1,uf:1,Re:1,Qe:1,Pe:1,Ne:1,Oe:1,yj:1});function DG(){this.Nf=this.L=null}DG.prototype=new n;DG.prototype.constructor=DG;function sg(a,b){var c=new DG;c.L=a;c.Nf=b;return c}
DG.prototype.m=function(){return pf(Fe(),"ComponentWillReceiveProps(props: "+ob(this).Nd()+" \u2192 "+this.Nf+", state: "+ob(this).Sc()+")")};DG.prototype.Hb=function(){return this.L};DG.prototype.$classData=l({us:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$ComponentWillReceiveProps",{us:1,c:1,zj:1,Kh:1,uf:1,Re:1,Qe:1,Pe:1,Ne:1,Oe:1,yj:1});function cf(){this.L=null}cf.prototype=new n;cf.prototype.constructor=cf;d=cf.prototype;d.o=function(a){return Cf().qg(this.L,a)};d.m=function(){return Cf().Mg(this.L)};
d.Ya=function(a){this.L=a;return this};d.s=function(){return Aa(this.L)};d.Hb=function(){return this.L};d.$classData=l({pn:0},!1,"japgolly.scalajs.react.component.builder.Lifecycle$RenderScope",{pn:1,c:1,zj:1,Kh:1,uf:1,Re:1,Qe:1,Pe:1,Ne:1,Oe:1,yj:1});function KB(){TC.call(this);this.Oo=!1}KB.prototype=new mG;KB.prototype.constructor=KB;KB.prototype.Po=function(){return this.Oo};KB.prototype.dk=function(a){OB.prototype.dk.call(this,a);this.Oo=!1;return this};
KB.prototype.$classData=l({Zw:0},!1,"java.util.Collections$ImmutableSet",{Zw:1,Fp:1,$w:1,c:1,bx:1,lh:1,wi:1,h:1,cx:1,Gp:1,xi:1});function EG(){this.Yl=null}EG.prototype=new SC;EG.prototype.constructor=EG;function FG(){}d=FG.prototype=EG.prototype;d.b=function(){this.Yl=(new hF).b();return this};d.uo=function(a){var b=Or();a=a.rc();b=Rr(gt(b,a));for(a=!0;a&&b.u();)a=b.r(),a=this.O(a);return a};d.v=function(){return this.jh().v()};d.O=function(a){return this.jh().O((new VC).n(a))};d.Ee=function(a){return this.jh().Ee((new VC).n(a))};
d.jh=function(){return this.Yl};d.se=function(a){return this.jh().se((new VC).n(a))};d.rc=function(){var a=new Px;if(null===this)throw Dp(ni(),null);a.wa=this;a.$l=this.jh().Al().C();a.Jd=B();return a};d.$classData=l({Hp:0},!1,"java.util.HashSet",{Hp:1,dm:1,cm:1,c:1,lh:1,wi:1,xi:1,he:1,Hd:1,k:1,h:1});function yr(){nG.call(this)}yr.prototype=new oG;yr.prototype.constructor=yr;yr.prototype.b=function(){this.Eh="Any";var a=B();xb();this.Cc=a;ma(p);return this};
yr.prototype.$classData=l({ky:0},!1,"scala.reflect.ManifestFactory$AnyManifest$",{ky:1,Gk:1,Fk:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var xr=void 0;function Cr(){nG.call(this)}Cr.prototype=new oG;Cr.prototype.constructor=Cr;Cr.prototype.b=function(){this.Eh="AnyVal";var a=B();xb();this.Cc=a;ma(p);return this};Cr.prototype.$classData=l({ly:0},!1,"scala.reflect.ManifestFactory$AnyValManifest$",{ly:1,Gk:1,Fk:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var Br=void 0;function Er(){nG.call(this)}
Er.prototype=new oG;Er.prototype.constructor=Er;Er.prototype.b=function(){this.Eh="Nothing";var a=B();xb();this.Cc=a;ma(aB);return this};Er.prototype.$classData=l({ty:0},!1,"scala.reflect.ManifestFactory$NothingManifest$",{ty:1,Gk:1,Fk:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var Dr=void 0;function Gr(){nG.call(this)}Gr.prototype=new oG;Gr.prototype.constructor=Gr;Gr.prototype.b=function(){this.Eh="Null";var a=B();xb();this.Cc=a;ma(bv);return this};
Gr.prototype.$classData=l({uy:0},!1,"scala.reflect.ManifestFactory$NullManifest$",{uy:1,Gk:1,Fk:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var Fr=void 0;function Ar(){nG.call(this)}Ar.prototype=new oG;Ar.prototype.constructor=Ar;Ar.prototype.b=function(){this.Eh="Object";var a=B();xb();this.Cc=a;ma(p);return this};Ar.prototype.$classData=l({vy:0},!1,"scala.reflect.ManifestFactory$ObjectManifest$",{vy:1,Gk:1,Fk:1,c:1,wd:1,vd:1,ud:1,ad:1,k:1,h:1,q:1});var zr=void 0;function GG(){this.dc=this.Ra=null}
GG.prototype=new tF;GG.prototype.constructor=GG;GG.prototype.b=function(){oy.prototype.b.call(this);HG=this;this.dc=(new eF).Ic(0,0,0);return this};GG.prototype.ae=function(){return this.dc};GG.prototype.ba=function(){return(new Hl).b()};GG.prototype.$classData=l({uA:0},!1,"scala.collection.immutable.Vector$",{uA:1,Cq:1,He:1,Ge:1,Nc:1,zb:1,c:1,Pc:1,Ab:1,k:1,h:1});var HG=void 0;function jl(){HG||(HG=(new GG).b());return HG}function IG(){this.tp=this.Yl=null}IG.prototype=new FG;
IG.prototype.constructor=IG;IG.prototype.b=function(){EG.prototype.b.call(this);this.tp=(new gG).b();return this};IG.prototype.Ul=function(a){IG.prototype.b.call(this);a=a.rc();for(var b=!1;a.u();)b=this.se(a.r())||b;return this};IG.prototype.jh=function(){return this.tp};IG.prototype.$classData=l({tx:0},!1,"java.util.LinkedHashSet",{tx:1,Hp:1,dm:1,cm:1,c:1,lh:1,wi:1,xi:1,he:1,Hd:1,k:1,h:1});function JG(){}JG.prototype=new n;JG.prototype.constructor=JG;function KG(){}d=KG.prototype=JG.prototype;
d.sg=function(a,b){return RE(this,a,b)};d.Mf=function(a,b,c){return Oj(this,a,b,c)};d.m=function(){return gn(this)};d.Yc=function(a,b){return ps(this,a,b)};d.cg=function(){jl();var a=kl().Qa;return ll(this,a)};d.v=function(){return qs(this)};d.Ie=function(){return-1};d.w=function(){return TE(this)};d.vf=function(a,b,c,e){return rs(this,a,b,c,e)};d.Sf=function(){return this};d.tf=function(a,b){return this.Yc(a,b)};d.kr=function(){var a=yA(),a=ny(a);return ll(this,a)};d.ti=function(){return!0};
d.ba=function(){return this.$a().ba()};d.Ad=function(){return UE(this)};function Rj(a){return 0===a.De(0)}function kg(){this.Kc=this.ce=null}kg.prototype=new BG;kg.prototype.constructor=kg;d=kg.prototype;d.mm=function(a,b){this.Kc.setState(function(a){return function(b){return a.f(b)}}(a),Hd(qb(),b))};d.Hi=function(a,b,c){Ne();return LG(this,a,b,c)};d.Nd=function(){return this.Kc.props};d.Ji=function(a,b){this.mm(a,b)};d.Bh=function(a,b){this.Kc.setState(a,Hd(qb(),b))};
d.Ya=function(a){AG.prototype.qp.call(this,lg().Nl);this.Kc=a;return this};d.Sc=function(){return this.Kc.state};d.Hb=function(){return this.Kc};d.$classData=l({Or:0},!1,"japgolly.scalajs.react.component.Js$$anon$1",{Or:1,is:1,c:1,fl:1,el:1,dl:1,cl:1,Qe:1,Pe:1,Ne:1,Re:1,Oe:1,Tr:1,Sr:1});function MG(){yG.call(this);this.gp=this.Kc=null}MG.prototype=new zG;MG.prototype.constructor=MG;MG.prototype.Hi=function(a,b,c){Ne();return LG(this.gp,a,b,c)};
function LG(a,b,c,e){var f=new MG;f.gp=a;yG.prototype.pp.call(f,a,b,c,e);f.Kc=a.Hb();return f}MG.prototype.Hb=function(){return this.Kc};MG.prototype.$classData=l({Pr:0},!1,"japgolly.scalajs.react.component.Js$$anon$2",{Pr:1,hs:1,c:1,fl:1,el:1,dl:1,cl:1,Qe:1,Pe:1,Ne:1,Re:1,Oe:1,Tr:1,Sr:1});function NG(){this.rj=this.Kc=this.fk=this.ce=null}NG.prototype=new BG;NG.prototype.constructor=NG;d=NG.prototype;
d.mm=function(a,b){this.rj.Ji(t(new u,function(a,b){return function(a){Le();return{a:b.f(a.a)}}}(this,a)),b)};d.Hi=function(a,b,c){Ge();return OG(this,a,b,c)};d.Nd=function(){return this.rj.Nd().a};d.Ji=function(a,b){this.mm(a,b)};d.Bh=function(a,b){this.rj.Bh((Le(),{a:a}),b)};d.Sc=function(){return this.rj.Sc().a};function Pe(a){var b=new NG;b.rj=a;AG.prototype.qp.call(b,lg().Nl);b.fk=a;b.Kc=a.Hb();return b}d.Hb=function(){return this.Kc};
d.$classData=l({cs:0},!1,"japgolly.scalajs.react.component.Scala$$anon$1",{cs:1,is:1,c:1,fl:1,el:1,dl:1,cl:1,Qe:1,Pe:1,Ne:1,Re:1,Oe:1,fs:1,es:1});function PG(){yG.call(this);this.fp=this.Kc=this.fk=null}PG.prototype=new zG;PG.prototype.constructor=PG;function OG(a,b,c,e){var f=new PG;f.fp=a;yG.prototype.pp.call(f,a,b,c,e);f.fk=a.fk;f.Kc=a.Hb();return f}PG.prototype.Hi=function(a,b,c){Ge();return OG(this.fp,a,b,c)};PG.prototype.Hb=function(){return this.Kc};
PG.prototype.$classData=l({ds:0},!1,"japgolly.scalajs.react.component.Scala$$anon$2",{ds:1,hs:1,c:1,fl:1,el:1,dl:1,cl:1,Qe:1,Pe:1,Ne:1,Re:1,Oe:1,fs:1,es:1});function QG(a){return RG(a)?ji(new ki,a,0,a.aa()).r():a.eb(0)}function SG(a,b){return a.aa()-b|0}function TG(a,b){return UG(a,b,!1)!==a.aa()}function VG(a,b){if(b&&b.$classData&&b.$classData.p.Yi){var c=a.aa();if(c===b.aa()){for(var e=0;e<c&&D(G(),a.eb(e),b.eb(e));)e=1+e|0;return e===c}return!1}return pG(a,b)}
function RG(a){return 0===a.aa()}function UG(a,b,c){for(var e=0;e<a.aa()&&!!b.f(a.eb(e))===c;)e=1+e|0;return e}function WG(a,b){return UG(a,b,!0)===a.aa()}function XG(a,b){for(var c=0,e=a.aa();c<e;)b.f(a.eb(c)),c=1+c|0}function YG(a,b,c){c=0<c?c:0;for(var e=a.aa(),f=c;;){if(f<e)var h=a.eb(f),h=!b.f(h);else h=!1;if(h)f=1+f|0;else break}b=c+(f-c|0)|0;return b>=a.aa()?-1:b}
function ZG(a,b,c){b=0<b?b:0;c=0<c?c:0;var e=a.aa();c=c<e?c:e;var e=c-b|0,f=0<e?e:0,e=a.ba();for(e.wc(f);b<c;)e.kb(a.eb(b)),b=1+b|0;return e.Ja()}function $G(a,b,c,e){var f=0;for(;;){if(f===b)return c;var h=1+f|0;c=wg(e,c,a.eb(f));f=h}}function aH(a){return RG(a)?TE(a):a.Ch(1,a.aa())}function bH(a,b,c,e){var f=0,h=c,m=a.aa();e=m<e?m:e;c=dv(rl(),b)-c|0;for(c=e<c?e:c;f<c;)fv(rl(),b,h,a.eb(f)),f=1+f|0,h=1+h|0}function cH(a,b){a=a.Jl(b);if(0>b||a.l())throw(new Y).i(""+b);return a.I()}
function dH(a,b){for(;!a.l();){if(b.f(a.I()))return!0;a=a.w()}return!1}function eH(a,b){if(b&&b.$classData&&b.$classData.p.$i){if(a===b)return!0;for(;!a.l()&&!b.l()&&D(G(),a.I(),b.I());)a=a.w(),b=b.w();return a.l()&&b.l()}return pG(a,b)}function fH(a,b){for(;!a.l();){if(!b.f(a.I()))return!1;a=a.w()}return!0}function gH(a,b,c){for(;!a.l();)b=wg(c,b,a.I()),a=a.w();return b}function hH(a,b,c){var e=0<c?c:0;for(a=a.Jl(c);!a.l();){if(b.f(a.I()))return e;e=1+e|0;a=a.w()}return-1}
function Pv(a){for(var b=0;!a.l();)b=1+b|0,a=a.w();return b}function iH(a){if(a.l())throw(new Mw).b();for(var b=a.w();!b.l();)a=b,b=b.w();return a.I()}function jH(a,b){return 0<=b&&0<(0>b?1:kH(a,b))}function kH(a,b){var c=0;for(;;){if(c===b)return a.l()?0:1;if(a.l())return-1;c=1+c|0;a=a.w()}}function lH(a,b){return b.Ba().tf(a,Eb(function(){return function(a,b){return a.lc(b)}}(a)))}function mH(a){throw(new Mw).i("key not found: "+a);}
function nH(a,b,c,e,f){var h=a.C();a=(new li).pb(h,t(new u,function(){return function(a){if(null!==a){var b=a.R;a=a.U;Mi||(Mi=(new Ni).b());return""+(""+b+" -\x3e ")+a}throw(new x).n(a);}}(a)));return rs(a,b,c,e,f)}function oH(a,b,c){return a.wg(b,v(new w,function(a,b,c){return function(){return c.f(b)}}(a,b,c)))}function pH(a,b,c){b=0<b?b:0;var e=a.aa(),e=c<e?c:e;if(b>=e)return a.ba().Ja();c=a.ba();a=a.m().substring(b,e);return c.db((new zh).i(a)).Ja()}function qH(){}qH.prototype=new KG;
qH.prototype.constructor=qH;function rH(){}d=rH.prototype=qH.prototype;d.Ba=function(){return this.Bb()};d.I=function(){return this.C().r()};d.pf=function(){return this};d.rf=function(){return this.C()};d.Ac=function(a){for(var b=this.C(),c=!1;!c&&b.u();)c=!!a.f(b.r());return c};d.ie=function(a){return pG(this,a)};d.Ta=function(){return this.pf()};d.Bb=function(){return this};d.l=function(){return!this.C().u()};d.$a=function(){return yq()};d.Hc=function(a){var b=this.C();return by(b,a)};
d.M=function(a){var b=this.C();Jp(b,a)};d.xc=function(){return this.C().xc()};d.xe=function(a){var b=this.ba();Ay(b,this,-(0>a?0:a)|0);for(var c=0,e=this.C();c<a&&e.u();)e.r(),c=1+c|0;return b.db(e).Ja()};d.ng=function(a,b,c){var e=b;b=b+c|0;c=dv(rl(),a);b=b<c?b:c;for(c=this.C();e<b&&c.u();)fv(rl(),a,e,c.r()),e=1+e|0};var HD=l({Ma:0},!0,"scala.collection.immutable.Iterable",{Ma:1,Oa:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,Na:1,la:1,ja:1,ca:1,ea:1,q:1});function zh(){this.bb=null}
zh.prototype=new n;zh.prototype.constructor=zh;d=zh.prototype;d.Ba=function(){return(new Ct).i(this.bb)};d.I=function(){return QG(this)};d.eb=function(a){a=65535&(this.bb.charCodeAt(a)|0);return tu(a)};d.De=function(a){return SG(this,a)};d.rf=function(){return ji(new ki,this,0,this.bb.length|0)};d.ie=function(a){return VG(this,a)};d.Ac=function(a){return TG(this,a)};d.l=function(){return RG(this)};d.Ta=function(){return(new Ct).i(this.bb)};d.sg=function(a,b){return RE(this,a,b)};
d.o=function(a){yt();return a&&a.$classData&&a.$classData.p.Mq?this.bb===(null===a?null:a.bb):!1};d.Mf=function(a,b,c){return Oj(this,a,b,c)};d.Hc=function(a){return WG(this,a)};d.m=function(){return this.bb};d.M=function(a){XG(this,a)};d.Yc=function(a,b){return $G(this,this.bb.length|0,a,b)};d.ze=function(a,b){return YG(this,a,b)};d.Ch=function(a,b){return wt(yt(),this.bb,a,b)};d.cg=function(){jl();var a=kl().Qa;return ll(this,a)};d.v=function(){return this.bb.length|0};
d.C=function(){return ji(new ki,this,0,this.bb.length|0)};d.aa=function(){return this.bb.length|0};d.Ie=function(){return this.bb.length|0};d.xc=function(){var a=ji(new ki,this,0,this.bb.length|0);return ey(a)};d.xe=function(a){var b=this.bb.length|0;return wt(yt(),this.bb,a,b)};d.oe=function(){return(new Ct).i(this.bb)};d.w=function(){return aH(this)};d.vf=function(a,b,c,e){return rs(this,a,b,c,e)};d.Sf=function(){return this.bb};d.tf=function(a,b){return $G(this,this.bb.length|0,a,b)};
d.ng=function(a,b,c){bH(this,a,b,c)};d.ti=function(){return!0};d.s=function(){var a=this.bb;return Ba(Ca(),a)};d.i=function(a){this.bb=a;return this};d.ba=function(){return(new os).b()};d.Ad=function(){return UE(this)};d.$classData=l({Mq:0},!1,"scala.collection.immutable.StringOps",{Mq:1,c:1,Lq:1,Zi:1,Wf:1,Rd:1,ea:1,q:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ca:1,Qd:1,tq:1,Id:1});function Fl(){this.Ke=null}Fl.prototype=new rH;Fl.prototype.constructor=Fl;d=Fl.prototype;
d.M=function(a){var b=this.Ke.Xm();Jp(b,a)};d.v=function(){return this.Ke.v()};d.C=function(){return this.Ke.Xm()};d.ef=function(a){if(null===a)throw Dp(ni(),null);this.Ke=a;return this};d.$classData=l({cz:0},!1,"scala.collection.MapLike$DefaultValuesIterable",{cz:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,k:1,h:1});var Ov=l({je:0},!0,"scala.collection.Seq",{je:1,Va:1,T:1,la:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,ja:1,ca:1,ea:1,q:1,Pd:1,Qd:1,Rd:1});
function sH(){}sH.prototype=new rH;sH.prototype.constructor=sH;function tH(){}tH.prototype=sH.prototype;function uH(a){var b=a.ba();b.db(a);return b.Ja()}function ii(){this.Jb=null}ii.prototype=new n;ii.prototype.constructor=ii;d=ii.prototype;d.Ba=function(){return(new A).g(this.Jb)};d.b=function(){ii.prototype.g.call(this,[]);return this};d.I=function(){return QG(this)};d.eb=function(a){return this.Jb[a]};d.De=function(a){return SG(this,a)};
d.rf=function(){return ji(new ki,this,0,this.Jb.length|0)};d.ie=function(a){return VG(this,a)};d.Ac=function(a){return TG(this,a)};d.l=function(){return RG(this)};d.Ta=function(){return(new A).g(this.Jb)};d.sg=function(a,b){return RE(this,a,b)};d.o=function(a){return fn(this,a)};d.Mf=function(a,b,c){return Oj(this,a,b,c)};d.Ob=function(a){this.Jb.push(a);return this};d.Hc=function(a){return WG(this,a)};d.m=function(){return gn(this)};d.M=function(a){XG(this,a)};
d.Yc=function(a,b){return $G(this,this.Jb.length|0,a,b)};d.ze=function(a,b){return YG(this,a,b)};d.Ch=function(a,b){return ZG(this,a,b)};d.cg=function(){jl();var a=kl().Qa;return ll(this,a)};d.v=function(){return this.Jb.length|0};d.Ja=function(){return this.Jb};d.C=function(){return ji(new ki,this,0,this.Jb.length|0)};d.gd=function(a,b){By(this,a,b)};d.aa=function(){return this.Jb.length|0};d.Ie=function(){return this.Jb.length|0};d.xc=function(){var a=ji(new ki,this,0,this.Jb.length|0);return ey(a)};
d.xe=function(a){return ZG(this,a,this.Jb.length|0)};d.oe=function(){return(new A).g(this.Jb)};d.w=function(){return aH(this)};d.vf=function(a,b,c,e){return rs(this,a,b,c,e)};d.Sf=function(){return this.Jb};d.tf=function(a,b){return $G(this,this.Jb.length|0,a,b)};d.kb=function(a){this.Jb.push(a);return this};d.ng=function(a,b,c){bH(this,a,b,c)};d.wc=function(){};d.s=function(){return $x(U(),(new A).g(this.Jb))};d.ti=function(){return!0};d.g=function(a){this.Jb=a;return this};d.ba=function(){return(new ii).b()};
d.db=function(a){return jm(this,a)};d.Ad=function(){return UE(this)};d.$classData=l({Zq:0},!1,"scala.scalajs.js.ArrayOps",{Zq:1,c:1,DA:1,Vq:1,Jm:1,Wf:1,Rd:1,ea:1,q:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ca:1,Qd:1,Zi:1,Qc:1,Oc:1,Mc:1});function Tr(a){var b=(new QD).ob(a.v());a.M(t(new u,function(a,b){return function(a){return vH(b,a)}}(a,b)));return b}function wH(a){return a.Xc().db(a)}function xH(){}xH.prototype=new rH;xH.prototype.constructor=xH;function yH(){}d=yH.prototype=xH.prototype;
d.np=function(a,b){return dD(this,a,b)};d.l=function(){return Rj(this)};d.o=function(a){return fn(this,a)};d.m=function(){return gn(this)};d.ze=function(a,b){a:{var c=0<b?b:0;for(b=this.C().Il(b);b.u();){if(a.f(b.r())){a=c;break a}c=1+c|0}a=-1}return a};d.v=function(){return this.aa()};d.oe=function(){return this};d.mp=function(a){return this.np(a,0)};d.wf=function(a,b){return lq(this,a,b)};d.s=function(){return $x(U(),this.ag())};d.Dd=function(a){return $A(this,a)};function zH(){}zH.prototype=new rH;
zH.prototype.constructor=zH;function AH(){}d=AH.prototype=zH.prototype;d.f=function(a){var b=this.sb(a);if(B()===b)a=mH(a);else if(If(b))a=b.J;else throw(new x).n(b);return a};d.l=function(){return 0===this.v()};d.o=function(a){return xC(this,a)};d.wg=function(a,b){a=this.sb(a);if(If(a))b=a.J;else if(B()===a)b=b.V();else throw(new x).n(a);return b};d.m=function(){return gn(this)};d.hm=function(){return(new mD).ef(this)};d.Xm=function(){return(new nD).ef(this)};d.O=function(a){return!this.sb(a).l()};
d.vf=function(a,b,c,e){return nH(this,a,b,c,e)};d.Zc=function(a){return this.O(a)};d.Jp=function(){return(new BH).ef(this)};d.s=function(){var a=U();return cs(a,this.zh(),a.Op)};d.wf=function(a,b){return oH(this,a,b)};d.Dd=function(a){return $A(this,a)};d.Ad=function(){return"Map"};d.ba=function(){return Al(new Bl,this.pg())};function CH(){}CH.prototype=new rH;CH.prototype.constructor=CH;function DH(){}d=DH.prototype=CH.prototype;d.Ba=function(){return this.ne()};d.Ib=function(){return this};
d.f=function(a){return this.O(a)};d.Bb=function(){return this.ne()};d.Ta=function(){return this};d.l=function(){return 0===this.v()};d.o=function(a){return QE(this,a)};d.$a=function(){return qD()};d.m=function(){return gn(this)};d.Sm=function(a){return this.Hc(a)};d.Xc=function(){return this.Wo()};d.ne=function(){return this};d.Wo=function(){return this.$a().ae()};d.s=function(){var a=U();return cs(a,this.ne(),a.Om)};d.Dd=function(a){return nb(this,a)};d.Xd=function(a){return lH(this,a)};
d.ba=function(){return R(new S,this.Xc())};d.Ad=function(){return"Set"};function EH(){this.Tb=this.Qi=null}EH.prototype=new AH;EH.prototype.constructor=EH;function FH(){}d=FH.prototype=EH.prototype;d.M=function(a){this.Tb.M(t(new u,function(a,c){return function(e){return a.Qi.f(e.R)?c.f(e):void 0}}(this,a)))};d.pw=function(a,b){this.Qi=b;if(null===a)throw Dp(ni(),null);this.Tb=a;return this};d.C=function(){var a=this.Tb.C();return(new Oi).pb(a,t(new u,function(a){return function(c){return!!a.Qi.f(c.R)}}(this)))};
d.sb=function(a){return this.Qi.f(a)?this.Tb.sb(a):B()};d.O=function(a){return!!this.Qi.f(a)&&this.Tb.O(a)};function GH(a,b){var c=Al(new Bl,Cl());jm(c,a);Dl(c,(new I).d(b.R,b.U));return c.y}function HH(a,b){var c=Al(new Bl,Cl());jy(new iy,a,t(new u,function(a,b){return function(a){return!D(G(),a.R,b)}}(a,b))).M(t(new u,function(a,b){return function(a){return b.kb(a)}}(a,c)));return c.y}function BH(){this.Tb=null}BH.prototype=new DH;BH.prototype.constructor=BH;function IH(){}d=IH.prototype=BH.prototype;
d.Za=function(a){return this.Dc(a)};d.M=function(a){var b=this.Tb.hm();Jp(b,a)};d.v=function(){return this.Tb.v()};d.C=function(){return this.Tb.hm()};d.Dc=function(a){return M(qD(),xb()).Xd(this).Dc(a)};d.ef=function(a){if(null===a)throw Dp(ni(),null);this.Tb=a;return this};d.O=function(a){return this.Tb.O(a)};d.lc=function(a){return M(qD(),xb()).Xd(this).lc(a)};
d.$classData=l({Em:0},!1,"scala.collection.MapLike$DefaultKeySet",{Em:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,k:1,h:1});function JH(){this.Uc=this.Tb=null}JH.prototype=new IH;JH.prototype.constructor=JH;JH.prototype.M=function(a){for(var b=this.Uc,c=b.S,b=$t(b),e=c.e[b];null!==e;){var f=e.r();a.f(e.xa);for(e=f;null===e&&0<b;)b=-1+b|0,e=c.e[b]}};
JH.prototype.si=function(a){if(null===a)throw Dp(ni(),null);this.Uc=a;BH.prototype.ef.call(this,a);return this};JH.prototype.$classData=l({LA:0},!1,"scala.collection.mutable.HashMap$$anon$1",{LA:1,Em:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,k:1,h:1});function KH(){}KH.prototype=new AH;KH.prototype.constructor=KH;function LH(){}d=LH.prototype=KH.prototype;d.Ba=function(){return this};d.Ib=function(){return this};
d.Bb=function(){return this};d.Ta=function(){return this};d.$a=function(){return gD()};d.pg=function(){return this.Kl()};d.Kl=function(){return Cl()};d.zh=function(){return this};function MH(){}MH.prototype=new DH;MH.prototype.constructor=MH;function NH(){}d=NH.prototype=MH.prototype;d.Ba=function(){return this};d.Ib=function(){return this};d.rh=function(){throw(new Mw).i("next of empty set");};d.f=function(a){return this.O(a)};d.Za=function(a){return this.vj(a)};d.l=function(){return!0};d.Bb=function(){return this};
d.Ta=function(){return this};d.Qg=function(a){return OH(new PH,this,a)};d.$a=function(){cG||(cG=(new aG).b());return cG};d.v=function(){return 0};d.C=function(){var a=QH(this);return lD(a)};d.Dc=function(a){return this.vj(a)};d.Xc=function(){return bG()};function QH(a){for(var b=xb();!a.l();){var c=a.ei(),b=zb(new Ab,c,b);a=a.rh()}return b}d.ne=function(){return this};d.ei=function(){throw(new Mw).i("elem of empty set");};d.O=function(){return!1};
function RH(a,b){return b.l()?a:b.tf(a,Eb(function(){return function(a,b){return a.Qg(b)}}(a)))}d.vj=function(){return this};d.lc=function(a){return this.Qg(a)};d.Xd=function(a){return RH(this,a)};d.Ad=function(){return"ListSet"};function nO(){}nO.prototype=new DH;nO.prototype.constructor=nO;d=nO.prototype;d.Ba=function(){return this};d.Ib=function(){return this};d.b=function(){return this};d.f=function(){return!1};d.Za=function(){return this};d.Bb=function(){return this};d.Ta=function(){return this};
d.$a=function(){return yA()};d.M=function(){};d.v=function(){return 0};d.C=function(){return Aq().Y};d.Dc=function(){return this};d.Xc=function(){return Q()};d.ne=function(){return this};d.O=function(){return!1};d.lc=function(a){return(new oO).n(a)};d.$classData=l({fA:0},!1,"scala.collection.immutable.Set$EmptySet$",{fA:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});var pO=void 0;
function Q(){pO||(pO=(new nO).b());return pO}function oO(){this.ia=null}oO.prototype=new DH;oO.prototype.constructor=oO;d=oO.prototype;d.Ba=function(){return this};d.Ib=function(){return this};d.I=function(){return this.ia};d.f=function(a){return this.O(a)};d.Ac=function(a){return!!a.f(this.ia)};d.Za=function(a){return this.Tc(a)};d.Bb=function(){return this};d.Ta=function(){return this};d.Hc=function(a){return!!a.f(this.ia)};d.$a=function(){return yA()};d.M=function(a){a.f(this.ia)};d.v=function(){return 1};
d.C=function(){Aq();var a=(new A).g([this.ia]);return ji(new ki,a,0,a.ya.length|0)};d.n=function(a){this.ia=a;return this};d.Dc=function(a){return this.Tc(a)};d.Xc=function(){return Q()};d.Le=function(a){return this.O(a)?this:(new qO).d(this.ia,a)};d.ne=function(){return this};d.O=function(a){return D(G(),a,this.ia)};d.w=function(){return Q()};d.lc=function(a){return this.Le(a)};d.Tc=function(a){return D(G(),a,this.ia)?Q():this};
d.$classData=l({gA:0},!1,"scala.collection.immutable.Set$Set1",{gA:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});function qO(){this.Ea=this.ia=null}qO.prototype=new DH;qO.prototype.constructor=qO;d=qO.prototype;d.Ba=function(){return this};d.Ib=function(){return this};d.I=function(){return this.ia};d.f=function(a){return this.O(a)};d.Ac=function(a){return!!a.f(this.ia)||!!a.f(this.Ea)};
d.Za=function(a){return this.Tc(a)};d.kj=function(){return(new oO).n(this.Ea)};d.Bb=function(){return this};d.Ta=function(){return this};d.d=function(a,b){this.ia=a;this.Ea=b;return this};d.Hc=function(a){return!!a.f(this.ia)&&!!a.f(this.Ea)};d.$a=function(){return yA()};d.M=function(a){a.f(this.ia);a.f(this.Ea)};d.v=function(){return 2};d.C=function(){Aq();var a=(new A).g([this.ia,this.Ea]);return ji(new ki,a,0,a.ya.length|0)};d.Dc=function(a){return this.Tc(a)};d.Xc=function(){return Q()};
d.Le=function(a){return this.O(a)?this:(new rO).Kf(this.ia,this.Ea,a)};d.ne=function(){return this};d.O=function(a){return D(G(),a,this.ia)||D(G(),a,this.Ea)};d.w=function(){return this.kj()};d.lc=function(a){return this.Le(a)};d.Tc=function(a){return D(G(),a,this.ia)?(new oO).n(this.Ea):D(G(),a,this.Ea)?(new oO).n(this.ia):this};
d.$classData=l({hA:0},!1,"scala.collection.immutable.Set$Set2",{hA:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});function rO(){this.nb=this.Ea=this.ia=null}rO.prototype=new DH;rO.prototype.constructor=rO;d=rO.prototype;d.Ba=function(){return this};d.Ib=function(){return this};d.I=function(){return this.ia};d.f=function(a){return this.O(a)};
d.Ac=function(a){return!!a.f(this.ia)||!!a.f(this.Ea)||!!a.f(this.nb)};d.Za=function(a){return this.Tc(a)};d.kj=function(){return(new qO).d(this.Ea,this.nb)};d.Bb=function(){return this};d.Ta=function(){return this};d.Hc=function(a){return!!a.f(this.ia)&&!!a.f(this.Ea)&&!!a.f(this.nb)};d.$a=function(){return yA()};d.M=function(a){a.f(this.ia);a.f(this.Ea);a.f(this.nb)};d.v=function(){return 3};d.Kf=function(a,b,c){this.ia=a;this.Ea=b;this.nb=c;return this};
d.C=function(){Aq();var a=(new A).g([this.ia,this.Ea,this.nb]);return ji(new ki,a,0,a.ya.length|0)};d.Dc=function(a){return this.Tc(a)};d.Xc=function(){return Q()};d.Le=function(a){return this.O(a)?this:(new sO).Lf(this.ia,this.Ea,this.nb,a)};d.ne=function(){return this};d.O=function(a){return D(G(),a,this.ia)||D(G(),a,this.Ea)||D(G(),a,this.nb)};d.w=function(){return this.kj()};d.lc=function(a){return this.Le(a)};
d.Tc=function(a){return D(G(),a,this.ia)?(new qO).d(this.Ea,this.nb):D(G(),a,this.Ea)?(new qO).d(this.ia,this.nb):D(G(),a,this.nb)?(new qO).d(this.ia,this.Ea):this};d.$classData=l({iA:0},!1,"scala.collection.immutable.Set$Set3",{iA:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});function sO(){this.Fd=this.nb=this.Ea=this.ia=null}sO.prototype=new DH;sO.prototype.constructor=sO;d=sO.prototype;
d.Ba=function(){return this};d.Ib=function(){return this};d.I=function(){return this.ia};d.f=function(a){return this.O(a)};d.Ac=function(a){return!!a.f(this.ia)||!!a.f(this.Ea)||!!a.f(this.nb)||!!a.f(this.Fd)};d.Za=function(a){return this.Tc(a)};d.kj=function(){return(new rO).Kf(this.Ea,this.nb,this.Fd)};d.Bb=function(){return this};d.Ta=function(){return this};d.Hc=function(a){return!!a.f(this.ia)&&!!a.f(this.Ea)&&!!a.f(this.nb)&&!!a.f(this.Fd)};d.$a=function(){return yA()};
d.M=function(a){a.f(this.ia);a.f(this.Ea);a.f(this.nb);a.f(this.Fd)};d.v=function(){return 4};d.C=function(){Aq();var a=(new A).g([this.ia,this.Ea,this.nb,this.Fd]);return ji(new ki,a,0,a.ya.length|0)};d.Dc=function(a){return this.Tc(a)};d.Xc=function(){return Q()};d.Le=function(a){return this.O(a)?this:tO(tO(tO(tO(tO((new uO).b(),this.ia),this.Ea),this.nb),this.Fd),a)};d.ne=function(){return this};d.O=function(a){return D(G(),a,this.ia)||D(G(),a,this.Ea)||D(G(),a,this.nb)||D(G(),a,this.Fd)};
d.w=function(){return this.kj()};d.Lf=function(a,b,c,e){this.ia=a;this.Ea=b;this.nb=c;this.Fd=e;return this};d.lc=function(a){return this.Le(a)};d.Tc=function(a){return D(G(),a,this.ia)?(new rO).Kf(this.Ea,this.nb,this.Fd):D(G(),a,this.Ea)?(new rO).Kf(this.ia,this.nb,this.Fd):D(G(),a,this.nb)?(new rO).Kf(this.ia,this.Ea,this.Fd):D(G(),a,this.Fd)?(new rO).Kf(this.ia,this.Ea,this.nb):this};
d.$classData=l({jA:0},!1,"scala.collection.immutable.Set$Set4",{jA:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});function uO(){}uO.prototype=new DH;uO.prototype.constructor=uO;function vO(){}d=vO.prototype=uO.prototype;d.oj=function(a,b){return wO(new xO,a,b)};d.$e=function(a){return this.Sl(Bi(Ci(),a))};d.Ba=function(){return this};d.Ib=function(){return this};d.b=function(){return this};
d.f=function(a){return this.O(a)};d.Za=function(a){return yO(this,a)};function tO(a,b){return a.oj(b,a.$e(b),0)}d.Bb=function(){return this};d.Ta=function(){return this};d.$a=function(){return YF()};d.M=function(){};d.Sm=function(a){if(a&&a.$classData&&a.$classData.p.xh)return this.jj(a,0);var b=this.C();return by(b,a)};d.v=function(){return 0};d.C=function(){return Aq().Y};d.Dc=function(a){return yO(this,a)};d.Xc=function(){return WF()};d.Wi=function(){return this};
function yO(a,b){a=a.Wi(b,a.$e(b),0);return null===a?WF():a}d.Sl=function(a){a=a+~(a<<9)|0;a^=a>>>14|0;a=a+(a<<4)|0;return a^(a>>>10|0)};d.ne=function(){return this};d.O=function(a){return this.Jf(a,this.$e(a),0)};d.w=function(){return this.Vm()};d.Vm=function(){return yO(this,this.I())};d.Wo=function(){return WF()};d.lc=function(a){return tO(this,a)};d.Jf=function(){return!1};d.jj=function(){return!0};
var TF=l({xh:0},!1,"scala.collection.immutable.HashSet",{xh:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,xd:1,k:1,h:1});uO.prototype.$classData=TF;function zO(){}zO.prototype=new NH;zO.prototype.constructor=zO;zO.prototype.b=function(){return this};
zO.prototype.$classData=l({Sz:0},!1,"scala.collection.immutable.ListSet$EmptyListSet$",{Sz:1,Qz:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});var AO=void 0;function bG(){AO||(AO=(new zO).b());return AO}function PH(){this.Uc=this.So=null}PH.prototype=new NH;PH.prototype.constructor=PH;d=PH.prototype;d.rh=function(){return this.Uc};d.Za=function(a){return BO(a,this)};d.l=function(){return!1};
d.Qg=function(a){return CO(this,a)?this:OH(new PH,this,a)};d.v=function(){a:{var a=this,b=0;for(;;){if(a.l())break a;a=a.rh();b=1+b|0}}return b};function BO(a,b){var c=xb();for(;;){if(b.l())return iH(c);if(D(G(),a,b.ei())){b=b.rh();for(a=c;!a.l();)c=a.I(),b=OH(new PH,b,c.ei()),a=a.w();return b}var e=b.rh(),c=zb(new Ab,b,c);b=e}}d.Dc=function(a){return BO(a,this)};function OH(a,b,c){a.So=c;if(null===b)throw Dp(ni(),null);a.Uc=b;return a}d.O=function(a){return CO(this,a)};d.ei=function(){return this.So};
d.vj=function(a){return BO(a,this)};function CO(a,b){for(;;){if(a.l())return!1;if(D(G(),a.ei(),b))return!0;a=a.rh()}}d.lc=function(a){return this.Qg(a)};d.$classData=l({Tz:0},!1,"scala.collection.immutable.ListSet$Node",{Tz:1,Qz:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,k:1,h:1});function DO(){this.Tb=null}DO.prototype=new IH;DO.prototype.constructor=DO;d=DO.prototype;d.Ba=function(){return this};
d.Ib=function(){return this};d.f=function(a){return this.Tb.O(a)};d.Za=function(a){return this.Tc(a)};d.Bb=function(){return this};d.Ta=function(){return this};d.$a=function(){return yA()};function AC(a){var b=new DO;BH.prototype.ef.call(b,a);return b}d.Dc=function(a){return this.Tc(a)};d.Xc=function(){return Q()};d.Le=function(a){return this.Tb.O(a)?this:M(yA(),xb()).Xd(this).lc(a)};d.ne=function(){return this};d.lc=function(a){return this.Le(a)};
d.Tc=function(a){return this.Tb.O(a)?M(yA(),xb()).Xd(this).Dc(a):this};d.$classData=l({aA:0},!1,"scala.collection.immutable.MapLike$ImmutableDefaultKeySet",{aA:1,Em:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,k:1,h:1,Ud:1,Ma:1,Oa:1,Na:1});function EO(){}EO.prototype=new yH;EO.prototype.constructor=EO;function FO(){}FO.prototype=EO.prototype;EO.prototype.Ba=function(){return this.hj()};EO.prototype.Bb=function(){return this.hj()};
EO.prototype.hj=function(){return this};function GO(a,b){a.ra.xk(b.R,b.U);return a}function HO(){}HO.prototype=new vO;HO.prototype.constructor=HO;d=HO.prototype;d.b=function(){return this};d.I=function(){throw(new Mw).i("Empty Set");};d.w=function(){return this.Vm()};d.Vm=function(){throw(new Mw).i("Empty Set");};
d.$classData=l({Ez:0},!1,"scala.collection.immutable.HashSet$EmptyHashSet$",{Ez:1,xh:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,xd:1,k:1,h:1});var IO=void 0;function WF(){IO||(IO=(new HO).b());return IO}function VF(){this.Vc=0;this.hb=null;this.nf=0}VF.prototype=new vO;VF.prototype.constructor=VF;d=VF.prototype;
d.oj=function(a,b,c){var e=1<<(31&(b>>>c|0)),f=Ft(hs(),this.Vc&(-1+e|0));if(0!==(this.Vc&e)){e=this.hb.e[f];a=e.oj(a,b,5+c|0);if(e===a)return this;b=k(r(TF),[this.hb.e.length]);WB(YB(),this.hb,0,b,0,this.hb.e.length);b.e[f]=a;return UF(new VF,this.Vc,b,this.nf+(a.v()-e.v()|0)|0)}c=k(r(TF),[1+this.hb.e.length|0]);WB(YB(),this.hb,0,c,0,f);c.e[f]=wO(new xO,a,b);WB(YB(),this.hb,f,c,1+f|0,this.hb.e.length-f|0);return UF(new VF,this.Vc|e,c,1+this.nf|0)};
d.M=function(a){for(var b=0;b<this.hb.e.length;)this.hb.e[b].M(a),b=1+b|0};d.v=function(){return this.nf};d.C=function(){var a=new ZE;CD.prototype.op.call(a,this.hb);return a};
d.Wi=function(a,b,c){var e=1<<(31&(b>>>c|0)),f=Ft(hs(),this.Vc&(-1+e|0));if(0!==(this.Vc&e)){var h=this.hb.e[f];a=h.Wi(a,b,5+c|0);return h===a?this:null===a?(e^=this.Vc,0!==e?(a=k(r(TF),[-1+this.hb.e.length|0]),WB(YB(),this.hb,0,a,0,f),WB(YB(),this.hb,1+f|0,a,f,-1+(this.hb.e.length-f|0)|0),f=this.nf-h.v()|0,1!==a.e.length||FD(a.e[0])?UF(new VF,e,a,f):a.e[0]):null):1!==this.hb.e.length||FD(a)?(e=k(r(TF),[this.hb.e.length]),WB(YB(),this.hb,0,e,0,this.hb.e.length),e.e[f]=a,f=this.nf+(a.v()-h.v()|0)|
0,UF(new VF,this.Vc,e,f)):a}return this};function UF(a,b,c,e){a.Vc=b;a.hb=c;a.nf=e;J();if(Ft(hs(),b)!==c.e.length)throw(new Nt).n("assertion failed");return a}d.Jf=function(a,b,c){var e=31&(b>>>c|0),f=1<<e;return-1===this.Vc?this.hb.e[31&e].Jf(a,b,5+c|0):0!==(this.Vc&f)?(e=Ft(hs(),this.Vc&(-1+f|0)),this.hb.e[e].Jf(a,b,5+c|0)):!1};
d.jj=function(a,b){if(a===this)return!0;if(FD(a)&&this.nf<=a.nf){var c=this.Vc,e=this.hb,f=0,h=a.hb;a=a.Vc;var m=0;if((c&a)===c){for(;0!==c;){var q=c^c&(-1+c|0),y=a^a&(-1+a|0);if(q===y){if(!e.e[f].jj(h.e[m],5+b|0))return!1;c&=~q;f=1+f|0}a&=~y;m=1+m|0}return!0}}return!1};function FD(a){return!!(a&&a.$classData&&a.$classData.p.Hq)}
d.$classData=l({Hq:0},!1,"scala.collection.immutable.HashSet$HashTrieSet",{Hq:1,xh:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,xd:1,k:1,h:1});function JO(){}JO.prototype=new vO;JO.prototype.constructor=JO;function KO(){}KO.prototype=JO.prototype;function LO(){}LO.prototype=new LH;LO.prototype.constructor=LO;function MO(){}d=MO.prototype=LO.prototype;d.Ib=function(){return this};d.Za=function(a){return this.uj(a)};
d.qj=function(){throw(new Mw).i("value of empty map");};d.Ta=function(){return this};d.l=function(){return!0};d.Me=function(a){return this.Rg(a)};d.pg=function(){return NO()};d.Je=function(a){return this.uj(a)};d.Kl=function(){return NO()};d.zh=function(){return this};d.v=function(){return 0};d.Rg=function(a){return OO(new PO,this,a.R,a.U)};d.C=function(){var a=QO(this);return lD(a)};d.pd=function(){throw(new Mw).i("key of empty map");};d.Cb=function(a,b){return this.pj(a,b)};
d.pj=function(a,b){return OO(new PO,this,a,b)};d.uj=function(){return this};d.sb=function(){return B()};function QO(a){for(var b=xb();!a.l();){var c=(new I).d(a.pd(),a.qj()),b=zb(new Ab,c,b);a=a.Of()}return b}d.Of=function(){throw(new Mw).i("next of empty map");};d.Yd=function(a){return this.Rg(a)};d.Ad=function(){return"ListMap"};function RO(){}RO.prototype=new LH;RO.prototype.constructor=RO;d=RO.prototype;d.b=function(){return this};d.f=function(a){this.sl(a)};d.Za=function(){return this};
d.wg=function(a,b){return b.V()};d.Me=function(a){return(new uk).d(a.R,a.U)};d.Je=function(){return this};d.v=function(){return 0};d.C=function(){return Aq().Y};d.Cb=function(a,b){return(new uk).d(a,b)};d.sb=function(){return B()};d.O=function(){return!1};d.sl=function(a){throw(new Mw).i("key not found: "+a);};d.Yd=function(a){return(new uk).d(a.R,a.U)};
d.$classData=l({Vz:0},!1,"scala.collection.immutable.Map$EmptyMap$",{Vz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});var SO=void 0;function Cl(){SO||(SO=(new RO).b());return SO}function uk(){this.va=this.N=null}uk.prototype=new LH;uk.prototype.constructor=uk;d=uk.prototype;d.f=function(a){if(D(G(),a,this.N))return this.va;throw(new Mw).i("key not found: "+a);};d.Za=function(a){return this.Wd(a)};
d.wg=function(a,b){return D(G(),a,this.N)?this.va:b.V()};d.d=function(a,b){this.N=a;this.va=b;return this};d.Me=function(a){return this.Cb(a.R,a.U)};d.M=function(a){a.f((new I).d(this.N,this.va))};d.Je=function(a){return this.Wd(a)};d.v=function(){return 1};d.C=function(){Aq();var a=(new A).g([(new I).d(this.N,this.va)]);return ji(new ki,a,0,a.ya.length|0)};d.Cb=function(a,b){return D(G(),a,this.N)?(new uk).d(this.N,b):(new TO).Lf(this.N,this.va,a,b)};
d.sb=function(a){return D(G(),a,this.N)?(new H).n(this.va):B()};d.O=function(a){return D(G(),a,this.N)};d.Wd=function(a){return D(G(),a,this.N)?Cl():this};d.Yd=function(a){return this.Cb(a.R,a.U)};d.$classData=l({Wz:0},!1,"scala.collection.immutable.Map$Map1",{Wz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});function TO(){this.Ha=this.$=this.va=this.N=null}TO.prototype=new LH;
TO.prototype.constructor=TO;d=TO.prototype;d.f=function(a){if(D(G(),a,this.N))return this.va;if(D(G(),a,this.$))return this.Ha;throw(new Mw).i("key not found: "+a);};d.Za=function(a){return this.Wd(a)};d.wg=function(a,b){return D(G(),a,this.N)?this.va:D(G(),a,this.$)?this.Ha:b.V()};d.Me=function(a){return this.Cb(a.R,a.U)};d.M=function(a){a.f((new I).d(this.N,this.va));a.f((new I).d(this.$,this.Ha))};d.Je=function(a){return this.Wd(a)};d.v=function(){return 2};
d.C=function(){Aq();var a=(new A).g([(new I).d(this.N,this.va),(new I).d(this.$,this.Ha)]);return ji(new ki,a,0,a.ya.length|0)};d.Cb=function(a,b){return D(G(),a,this.N)?(new TO).Lf(this.N,b,this.$,this.Ha):D(G(),a,this.$)?(new TO).Lf(this.N,this.va,this.$,b):UO(this.N,this.va,this.$,this.Ha,a,b)};d.sb=function(a){return D(G(),a,this.N)?(new H).n(this.va):D(G(),a,this.$)?(new H).n(this.Ha):B()};d.O=function(a){return D(G(),a,this.N)||D(G(),a,this.$)};
d.Lf=function(a,b,c,e){this.N=a;this.va=b;this.$=c;this.Ha=e;return this};d.Wd=function(a){return D(G(),a,this.N)?(new uk).d(this.$,this.Ha):D(G(),a,this.$)?(new uk).d(this.N,this.va):this};d.Yd=function(a){return this.Cb(a.R,a.U)};d.$classData=l({Xz:0},!1,"scala.collection.immutable.Map$Map2",{Xz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});
function VO(){this.lb=this.La=this.Ha=this.$=this.va=this.N=null}VO.prototype=new LH;VO.prototype.constructor=VO;d=VO.prototype;d.f=function(a){if(D(G(),a,this.N))return this.va;if(D(G(),a,this.$))return this.Ha;if(D(G(),a,this.La))return this.lb;throw(new Mw).i("key not found: "+a);};d.Za=function(a){return this.Wd(a)};d.wg=function(a,b){return D(G(),a,this.N)?this.va:D(G(),a,this.$)?this.Ha:D(G(),a,this.La)?this.lb:b.V()};d.Me=function(a){return this.Cb(a.R,a.U)};
d.M=function(a){a.f((new I).d(this.N,this.va));a.f((new I).d(this.$,this.Ha));a.f((new I).d(this.La,this.lb))};d.Je=function(a){return this.Wd(a)};function UO(a,b,c,e,f,h){var m=new VO;m.N=a;m.va=b;m.$=c;m.Ha=e;m.La=f;m.lb=h;return m}d.v=function(){return 3};d.C=function(){Aq();var a=(new A).g([(new I).d(this.N,this.va),(new I).d(this.$,this.Ha),(new I).d(this.La,this.lb)]);return ji(new ki,a,0,a.ya.length|0)};
d.Cb=function(a,b){return D(G(),a,this.N)?UO(this.N,b,this.$,this.Ha,this.La,this.lb):D(G(),a,this.$)?UO(this.N,this.va,this.$,b,this.La,this.lb):D(G(),a,this.La)?UO(this.N,this.va,this.$,this.Ha,this.La,b):(new WO).Ag(this.N,this.va,this.$,this.Ha,this.La,this.lb,a,b)};d.sb=function(a){return D(G(),a,this.N)?(new H).n(this.va):D(G(),a,this.$)?(new H).n(this.Ha):D(G(),a,this.La)?(new H).n(this.lb):B()};d.O=function(a){return D(G(),a,this.N)||D(G(),a,this.$)||D(G(),a,this.La)};
d.Wd=function(a){return D(G(),a,this.N)?(new TO).Lf(this.$,this.Ha,this.La,this.lb):D(G(),a,this.$)?(new TO).Lf(this.N,this.va,this.La,this.lb):D(G(),a,this.La)?(new TO).Lf(this.N,this.va,this.$,this.Ha):this};d.Yd=function(a){return this.Cb(a.R,a.U)};d.$classData=l({Yz:0},!1,"scala.collection.immutable.Map$Map3",{Yz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});
function WO(){this.Bd=this.sc=this.lb=this.La=this.Ha=this.$=this.va=this.N=null}WO.prototype=new LH;WO.prototype.constructor=WO;d=WO.prototype;d.f=function(a){if(D(G(),a,this.N))return this.va;if(D(G(),a,this.$))return this.Ha;if(D(G(),a,this.La))return this.lb;if(D(G(),a,this.sc))return this.Bd;throw(new Mw).i("key not found: "+a);};d.Za=function(a){return this.Wd(a)};d.wg=function(a,b){return D(G(),a,this.N)?this.va:D(G(),a,this.$)?this.Ha:D(G(),a,this.La)?this.lb:D(G(),a,this.sc)?this.Bd:b.V()};
d.Me=function(a){return this.Cb(a.R,a.U)};d.M=function(a){a.f((new I).d(this.N,this.va));a.f((new I).d(this.$,this.Ha));a.f((new I).d(this.La,this.lb));a.f((new I).d(this.sc,this.Bd))};d.Je=function(a){return this.Wd(a)};d.v=function(){return 4};d.C=function(){Aq();var a=(new A).g([(new I).d(this.N,this.va),(new I).d(this.$,this.Ha),(new I).d(this.La,this.lb),(new I).d(this.sc,this.Bd)]);return ji(new ki,a,0,a.ya.length|0)};
d.Cb=function(a,b){return D(G(),a,this.N)?(new WO).Ag(this.N,b,this.$,this.Ha,this.La,this.lb,this.sc,this.Bd):D(G(),a,this.$)?(new WO).Ag(this.N,this.va,this.$,b,this.La,this.lb,this.sc,this.Bd):D(G(),a,this.La)?(new WO).Ag(this.N,this.va,this.$,this.Ha,this.La,b,this.sc,this.Bd):D(G(),a,this.sc)?(new WO).Ag(this.N,this.va,this.$,this.Ha,this.La,this.lb,this.sc,b):XO(XO(XO(XO(XO((new YO).b(),this.N,this.va),this.$,this.Ha),this.La,this.lb),this.sc,this.Bd),a,b)};
d.Ag=function(a,b,c,e,f,h,m,q){this.N=a;this.va=b;this.$=c;this.Ha=e;this.La=f;this.lb=h;this.sc=m;this.Bd=q;return this};d.sb=function(a){return D(G(),a,this.N)?(new H).n(this.va):D(G(),a,this.$)?(new H).n(this.Ha):D(G(),a,this.La)?(new H).n(this.lb):D(G(),a,this.sc)?(new H).n(this.Bd):B()};d.O=function(a){return D(G(),a,this.N)||D(G(),a,this.$)||D(G(),a,this.La)||D(G(),a,this.sc)};
d.Wd=function(a){return D(G(),a,this.N)?UO(this.$,this.Ha,this.La,this.lb,this.sc,this.Bd):D(G(),a,this.$)?UO(this.N,this.va,this.La,this.lb,this.sc,this.Bd):D(G(),a,this.La)?UO(this.N,this.va,this.$,this.Ha,this.sc,this.Bd):D(G(),a,this.sc)?UO(this.N,this.va,this.$,this.Ha,this.La,this.lb):this};d.Yd=function(a){return this.Cb(a.R,a.U)};
d.$classData=l({Zz:0},!1,"scala.collection.immutable.Map$Map4",{Zz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});function ZO(){EH.call(this)}ZO.prototype=new FH;ZO.prototype.constructor=ZO;d=ZO.prototype;d.Ba=function(){return this};function Yj(a,b){var c=new ZO;EH.prototype.pw.call(c,a,b);return c}d.Ib=function(){return this};d.Za=function(a){return HH(this,a)};d.Bb=function(){return this};
d.Ta=function(){return this};d.Me=function(a){return GH(this,a)};d.$a=function(){return gD()};d.pg=function(){return Cl()};d.Je=function(a){return HH(this,a)};d.zh=function(){return this};d.Cb=function(a,b){return this.Me((new I).d(a,b))};d.Yd=function(a){return GH(this,a)};
d.$classData=l({$z:0},!1,"scala.collection.immutable.MapLike$$anon$1",{$z:1,HD:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,GD:1,YD:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1});function YO(){}YO.prototype=new LH;YO.prototype.constructor=YO;function $O(){}d=$O.prototype=YO.prototype;d.$e=function(a){return this.Sl(Bi(Ci(),a))};d.Ba=function(){return this};d.Ib=function(){return this};d.b=function(){return this};
d.Za=function(a){return aP(this,a)};d.Ta=function(){return this};d.Gh=function(a,b,c,e,f){return bP(a,b,e,f)};d.fh=function(){return B()};d.Me=function(a){return cP(this,a)};function cP(a,b){return a.Gh(b.R,a.$e(b.R),0,b.U,b,null)}d.M=function(){};d.pg=function(){BF();return AF()};function XO(a,b,c){return a.Gh(b,a.$e(b),0,c,null,null)}d.Je=function(a){return aP(this,a)};d.Vi=function(){return this};function aP(a,b){return a.Vi(b,a.$e(b),0)}d.Kl=function(){BF();return AF()};d.v=function(){return 0};
d.zh=function(){return this};d.C=function(){return Aq().Y};d.Cb=function(a,b){return XO(this,a,b)};d.Um=function(){return aP(this,this.I().R)};d.Sl=function(a){a=a+~(a<<9)|0;a^=a>>>14|0;a=a+(a<<4)|0;return a^(a>>>10|0)};d.sb=function(a){return this.fh(a,this.$e(a),0)};d.bh=function(){return!1};d.O=function(a){return this.bh(a,this.$e(a),0)};d.w=function(){return this.Um()};d.Yd=function(a){return cP(this,a)};
var xF=l({bj:0},!1,"scala.collection.immutable.HashMap",{bj:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1,xd:1});YO.prototype.$classData=xF;function xO(){this.Nb=null;this.Ua=0}xO.prototype=new KO;xO.prototype.constructor=xO;d=xO.prototype;
d.oj=function(a,b,c){if(b===this.Ua&&D(G(),a,this.Nb))return this;if(b!==this.Ua)return SF(YF(),this.Ua,this,b,wO(new xO,a,b),c);c=bG();return dP(new eP,b,OH(new PH,c,this.Nb).Qg(a))};d.M=function(a){a.f(this.Nb)};function wO(a,b,c){a.Nb=b;a.Ua=c;return a}d.v=function(){return 1};d.C=function(){Aq();var a=(new A).g([this.Nb]);return ji(new ki,a,0,a.ya.length|0)};d.Wi=function(a,b){return b===this.Ua&&D(G(),a,this.Nb)?null:this};d.Jf=function(a,b){return b===this.Ua&&D(G(),a,this.Nb)};
d.jj=function(a,b){return a.Jf(this.Nb,this.Ua,b)};d.$classData=l({Gq:0},!1,"scala.collection.immutable.HashSet$HashSet1",{Gq:1,Hz:1,xh:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,xd:1,k:1,h:1});function eP(){this.Ua=0;this.gf=null}eP.prototype=new KO;eP.prototype.constructor=eP;d=eP.prototype;
d.oj=function(a,b,c){return b===this.Ua?dP(new eP,b,this.gf.Qg(a)):SF(YF(),this.Ua,this,b,wO(new xO,a,b),c)};d.M=function(a){var b=QH(this.gf);Jp(lD(b),a)};d.v=function(){return this.gf.v()};d.C=function(){var a=QH(this.gf);return lD(a)};d.Wi=function(a,b){if(b===this.Ua){a=this.gf.vj(a);var c=a.v();switch(c){case 0:return null;case 1:return a=QH(a),wO(new xO,lD(a).r(),b);default:return c===this.gf.v()?this:dP(new eP,b,a)}}else return this};function dP(a,b,c){a.Ua=b;a.gf=c;return a}
d.Jf=function(a,b){return b===this.Ua&&this.gf.O(a)};d.jj=function(a,b){for(var c=QH(this.gf),c=lD(c),e=!0;e&&c.u();)e=c.r(),e=a.Jf(e,this.Ua,b);return e};d.$classData=l({Fz:0},!1,"scala.collection.immutable.HashSet$HashSetCollision1",{Fz:1,Hz:1,xh:1,cd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Ud:1,Ma:1,Oa:1,Na:1,xd:1,k:1,h:1});function fP(){}fP.prototype=new MO;fP.prototype.constructor=fP;fP.prototype.b=function(){return this};
fP.prototype.$classData=l({Oz:0},!1,"scala.collection.immutable.ListMap$EmptyListMap$",{Oz:1,Nz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});var gP=void 0;function NO(){gP||(gP=(new fP).b());return gP}function PO(){this.an=this.dg=this.Nb=null}PO.prototype=new MO;PO.prototype.constructor=PO;
function hP(a,b){var c=xb();for(;;){if(b.l())return iH(c);if(D(G(),a,b.pd())){b=b.Of();for(a=c;!a.l();)c=a.I(),b=OO(new PO,b,c.pd(),c.qj()),a=a.w();return b}var e=b.Of(),c=zb(new Ab,b,c);b=e}}d=PO.prototype;d.f=function(a){a:{var b=this;for(;;){if(b.l())throw(new Mw).i("key not found: "+a);if(D(G(),a,b.pd())){a=b.qj();break a}b=b.Of()}}return a};d.qj=function(){return this.dg};d.Za=function(a){return hP(a,this)};d.l=function(){return!1};d.Me=function(a){return this.Rg(a)};
d.Je=function(a){return hP(a,this)};d.v=function(){a:{var a=this,b=0;for(;;){if(a.l())break a;a=a.Of();b=1+b|0}}return b};d.pd=function(){return this.Nb};d.Rg=function(a){var b=hP(a.R,this);return OO(new PO,b,a.R,a.U)};d.Cb=function(a,b){return this.pj(a,b)};d.pj=function(a,b){var c=hP(a,this);return OO(new PO,c,a,b)};d.uj=function(a){return hP(a,this)};d.sb=function(a){a:{var b=this;for(;;){if(b.l()){a=B();break a}if(D(G(),a,b.pd())){a=(new H).n(b.qj());break a}b=b.Of()}}return a};
d.O=function(a){a:{var b=this;for(;;){if(b.l()){a=!1;break a}if(D(G(),a,b.pd())){a=!0;break a}b=b.Of()}}return a};function OO(a,b,c,e){a.Nb=c;a.dg=e;if(null===b)throw Dp(ni(),null);a.an=b;return a}d.Of=function(){return this.an};d.Yd=function(a){return this.Rg(a)};
d.$classData=l({Pz:0},!1,"scala.collection.immutable.ListMap$Node",{Pz:1,Nz:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1});function iP(){}iP.prototype=new yH;iP.prototype.constructor=iP;function jP(){}d=jP.prototype=iP.prototype;d.Ba=function(){return this};d.eb=function(a){return cH(this,a)};d.pf=function(){return this};d.De=function(a){return 0>a?1:kH(this,a)};
d.ie=function(a){return eH(this,a)};d.f=function(a){return cH(this,a|0)};d.Ac=function(a){return dH(this,a)};d.Bb=function(){return this};d.Ta=function(){return this};d.sg=function(a,b){if(dF(b.xf(this))){if(this.l())a=gy();else{b=(new Kf).n(this);for(var c=a.f(b.H.I()).xc();!b.H.l()&&c.l();)b.H=b.H.w(),b.H.l()||(c=a.f(b.H.I()).xc());a=b.H.l()?(Hq(),gy()):kP(c,v(new w,function(a,b,c){return function(){return c.H.w().sg(b,(Hq(),(new QA).b()))}}(this,a,b)))}return a}return RE(this,a,b)};
d.o=function(a){return this===a||fn(this,a)};d.Jl=function(a){return lP(this,a)};d.Mf=function(a,b,c){var e=this,f=this;for(e.l()||(e=e.w());f!==e&&!e.l();){e=e.w();if(e.l())break;e=e.w();if(e===f)break;f=f.w()}return Oj(this,a,b,c)};d.Hc=function(a){return fH(this,a)};d.$a=function(){return Hq()};d.m=function(){return Oj(this,"Stream(",", ",")")};d.M=function(a){var b=this;a:for(;;){if(!b.l()){a.f(b.I());b=b.w();continue a}break}};
d.Yc=function(a,b){var c=this;for(;;){if(c.l())return a;var e=c.w();a=wg(b,a,c.I());c=e}};d.ze=function(a,b){return hH(this,a,b)};d.C=function(){return BD(this)};d.aa=function(){for(var a=0,b=this;!b.l();)a=1+a|0,b=b.w();return a};d.ag=function(){return this};d.xc=function(){return this};d.xe=function(a){return lP(this,a)};function lP(a,b){for(;;){if(0>=b||a.l())return a;a=a.w();b=-1+b|0}}d.oe=function(){return this};
d.vf=function(a,b,c,e){yn(a,b);if(!this.l()){at(a,this.I());b=this;if(b.of()){var f=this.w();if(f.l())return yn(a,e),a;if(b!==f&&(b=f,f.of()))for(f=f.w();b!==f&&f.of();)at(yn(a,c),b.I()),b=b.w(),f=f.w(),f.of()&&(f=f.w());if(f.of()){for(var h=this,m=0;h!==f;)h=h.w(),f=f.w(),m=1+m|0;b===f&&0<m&&(at(yn(a,c),b.I()),b=b.w());for(;b!==f;)at(yn(a,c),b.I()),b=b.w()}else{for(;b!==f;)at(yn(a,c),b.I()),b=b.w();!b.l()&&at(yn(a,c),b.I())}}b.l()||(b.of()?yn(yn(a,c),"..."):yn(yn(a,c),"?"))}yn(a,e);return a};
d.Zc=function(a){return jH(this,a|0)};d.s=function(){return $x(U(),this)};function kP(a,b){if(a.l())return b.V().xc();var c=a.I();return(new fy).pi(c,v(new w,function(a,b){return function(){return kP(a.w(),b)}}(a,b)))}d.Ad=function(){return"Stream"};function mP(a,b){if(b>=a.Ga)throw(new Y).i(""+b);return a.ya.e[b]}
function nP(a,b){var c=a.ya.e.length,e=c>>31,f=b>>31;if(f===e?(-2147483648^b)>(-2147483648^c):f>e){f=c<<1;for(c=c>>>31|0|e<<1;;){var e=b>>31,h=f,m=c;if(e===m?(-2147483648^b)>(-2147483648^h):e>m)c=f>>>31|0|c<<1,f<<=1;else break}b=c;if(0===b?-1<(-2147483648^f):0<b)f=2147483647;b=f;b=k(r(p),[b]);Na(a.ya,0,b,0,a.Ga);a.ya=b}}function oP(){}oP.prototype=new $O;oP.prototype.constructor=oP;d=oP.prototype;d.b=function(){return this};d.I=function(){throw(new Mw).i("Empty Map");};
d.Um=function(){throw(new Mw).i("Empty Map");};d.w=function(){return this.Um()};d.$classData=l({zz:0},!1,"scala.collection.immutable.HashMap$EmptyHashMap$",{zz:1,bj:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1,xd:1});var pP=void 0;function AF(){pP||(pP=(new oP).b());return pP}function qP(){this.Nb=null;this.Ua=0;this.gk=this.dg=null}qP.prototype=new $O;qP.prototype.constructor=qP;
function YE(a){null===a.gk&&(a.gk=(new I).d(a.Nb,a.dg));return a.gk}function bP(a,b,c,e){var f=new qP;f.Nb=a;f.Ua=b;f.dg=c;f.gk=e;return f}d=qP.prototype;d.Gh=function(a,b,c,e,f,h){if(b===this.Ua&&D(G(),a,this.Nb)){if(null===h)return this.dg===e?this:bP(a,b,e,f);a=h.tl(YE(this),null!==f?f:(new I).d(a,e));return bP(a.R,b,a.U,a)}if(b!==this.Ua)return a=bP(a,b,e,f),wF(BF(),this.Ua,this,b,a,c,2);c=NO();return rP(new sP,b,OO(new PO,c,this.Nb,this.dg).pj(a,e))};
d.fh=function(a,b){return b===this.Ua&&D(G(),a,this.Nb)?(new H).n(this.dg):B()};d.M=function(a){a.f(YE(this))};d.Vi=function(a,b){return b===this.Ua&&D(G(),a,this.Nb)?(BF(),AF()):this};d.v=function(){return 1};d.C=function(){Aq();var a=[YE(this)],a=(new A).g(a);return ji(new ki,a,0,a.ya.length|0)};d.bh=function(a,b){return b===this.Ua&&D(G(),a,this.Nb)};
d.$classData=l({Eq:0},!1,"scala.collection.immutable.HashMap$HashMap1",{Eq:1,bj:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1,xd:1});function sP(){this.Ua=0;this.qd=null}sP.prototype=new $O;sP.prototype.constructor=sP;d=sP.prototype;
d.Gh=function(a,b,c,e,f,h){if(b===this.Ua)return null!==h&&this.qd.O(a)?rP(new sP,b,this.qd.Rg(h.tl((new I).d(a,this.qd.f(a)),f))):rP(new sP,b,this.qd.pj(a,e));a=bP(a,b,e,f);return wF(BF(),this.Ua,this,b,a,c,1+this.qd.v()|0)};d.fh=function(a,b){return b===this.Ua?this.qd.sb(a):B()};d.M=function(a){var b=QO(this.qd);Jp(lD(b),a)};
d.Vi=function(a,b){if(b===this.Ua){a=this.qd.uj(a);var c=a.v();switch(c){case 0:return BF(),AF();case 1:return a=QO(a),a=lD(a).r(),bP(a.R,b,a.U,a);default:return c===this.qd.v()?this:rP(new sP,b,a)}}else return this};d.C=function(){var a=QO(this.qd);return lD(a)};d.v=function(){return this.qd.v()};function rP(a,b,c){a.Ua=b;a.qd=c;return a}d.bh=function(a,b){return b===this.Ua&&this.qd.O(a)};
d.$classData=l({Az:0},!1,"scala.collection.immutable.HashMap$HashMapCollision1",{Az:1,bj:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1,xd:1});function zF(){this.Fc=0;this.ib=null;this.Ga=0}zF.prototype=new $O;zF.prototype.constructor=zF;d=zF.prototype;
d.Gh=function(a,b,c,e,f,h){var m=1<<(31&(b>>>c|0)),q=Ft(hs(),this.Fc&(-1+m|0));if(0!==(this.Fc&m)){m=this.ib.e[q];a=m.Gh(a,b,5+c|0,e,f,h);if(a===m)return this;b=k(r(xF),[this.ib.e.length]);WB(YB(),this.ib,0,b,0,this.ib.e.length);b.e[q]=a;return yF(new zF,this.Fc,b,this.Ga+(a.v()-m.v()|0)|0)}c=k(r(xF),[1+this.ib.e.length|0]);WB(YB(),this.ib,0,c,0,q);c.e[q]=bP(a,b,e,f);WB(YB(),this.ib,q,c,1+q|0,this.ib.e.length-q|0);return yF(new zF,this.Fc|m,c,1+this.Ga|0)};
d.fh=function(a,b,c){var e=31&(b>>>c|0);if(-1===this.Fc)return this.ib.e[e].fh(a,b,5+c|0);e=1<<e;return 0!==(this.Fc&e)?(e=Ft(hs(),this.Fc&(-1+e|0)),this.ib.e[e].fh(a,b,5+c|0)):B()};d.M=function(a){for(var b=0;b<this.ib.e.length;)this.ib.e[b].M(a),b=1+b|0};
d.Vi=function(a,b,c){var e=1<<(31&(b>>>c|0)),f=Ft(hs(),this.Fc&(-1+e|0));if(0!==(this.Fc&e)){var h=this.ib.e[f];a=h.Vi(a,b,5+c|0);if(a===h)return this;if(0===a.v()){e^=this.Fc;if(0!==e)return a=k(r(xF),[-1+this.ib.e.length|0]),WB(YB(),this.ib,0,a,0,f),WB(YB(),this.ib,1+f|0,a,f,-1+(this.ib.e.length-f|0)|0),f=this.Ga-h.v()|0,1!==a.e.length||ED(a.e[0])?yF(new zF,e,a,f):a.e[0];BF();return AF()}return 1!==this.ib.e.length||ED(a)?(e=k(r(xF),[this.ib.e.length]),WB(YB(),this.ib,0,e,0,this.ib.e.length),e.e[f]=
a,f=this.Ga+(a.v()-h.v()|0)|0,yF(new zF,this.Fc,e,f)):a}return this};d.C=function(){var a=new XE;CD.prototype.op.call(a,this.ib);return a};d.v=function(){return this.Ga};function yF(a,b,c,e){a.Fc=b;a.ib=c;a.Ga=e;return a}d.bh=function(a,b,c){var e=31&(b>>>c|0);if(-1===this.Fc)return this.ib.e[e].bh(a,b,5+c|0);e=1<<e;return 0!==(this.Fc&e)?(e=Ft(hs(),this.Fc&(-1+e|0)),this.ib.e[e].bh(a,b,5+c|0)):!1};function ED(a){return!!(a&&a.$classData&&a.$classData.p.Fq)}
d.$classData=l({Fq:0},!1,"scala.collection.immutable.HashMap$HashTrieMap",{Fq:1,bj:1,ke:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,Sd:1,Ma:1,Oa:1,Na:1,Td:1,k:1,h:1,xd:1});function tP(){}tP.prototype=new yH;tP.prototype.constructor=tP;function uP(){}d=uP.prototype=tP.prototype;d.Ba=function(){return this};d.pf=function(){return this};d.eb=function(a){return cH(this,a)};d.De=function(a){return 0>a?1:kH(this,a)};
d.f=function(a){return cH(this,a|0)};d.ie=function(a){return eH(this,a)};d.Ac=function(a){return dH(this,a)};d.Bb=function(){return this};d.Ta=function(){return this};
d.sg=function(a,b){if(b===yg().Ra){if(this===xb())return xb();b=this;for(var c=ss(!1),e=(new Kf).n(null),f=(new Kf).n(null);b!==xb();)a.f(b.I()).Ba().M(t(new u,function(a,b,c,e){return function(a){b.H?(a=zb(new Ab,a,xb()),e.H.qf=a,e.H=a):(c.H=zb(new Ab,a,xb()),e.H=c.H,b.H=!0)}}(this,c,e,f))),b=b.w();return c.H?e.H:xb()}return RE(this,a,b)};d.Jl=function(a){return vP(this,a)};d.Hc=function(a){return fH(this,a)};d.$a=function(){return yg()};d.M=function(a){for(var b=this;!b.l();)a.f(b.I()),b=b.w()};
d.Yc=function(a,b){return gH(this,a,b)};d.ze=function(a,b){return hH(this,a,b)};d.C=function(){return lD(this)};function vP(a,b){for(;!a.l()&&0<b;)a=a.w(),b=-1+b|0;return a}d.ag=function(){return this};d.aa=function(){return Pv(this)};d.xc=function(){return this.l()?gy():(new fy).pi(this.I(),v(new w,function(a){return function(){return a.w().xc()}}(this)))};d.xe=function(a){return vP(this,a)};d.oe=function(){return this};d.Zc=function(a){return jH(this,a|0)};d.s=function(){return $x(U(),this)};
d.Ad=function(){return"List"};function fy(){this.Ok=this.jr=this.$j=null}fy.prototype=new jP;fy.prototype.constructor=fy;d=fy.prototype;d.I=function(){return this.$j};function wP(a){a.of()||a.of()||(a.jr=a.Ok.V(),a.Ok=null);return a.jr}d.ie=function(a){return xP(a)?yP(this,a):eH(this,a)};d.l=function(){return!1};d.of=function(){return null===this.Ok};function yP(a,b){for(;;)if(D(G(),a.$j,b.$j))if(a=wP(a),xP(a))if(b=wP(b),xP(b)){if(a===b)return!0}else return!1;else return wP(b).l();else return!1}
d.w=function(){return wP(this)};d.pi=function(a,b){this.$j=a;this.Ok=b;return this};function xP(a){return!!(a&&a.$classData&&a.$classData.p.Jq)}d.$classData=l({Jq:0},!1,"scala.collection.immutable.Stream$Cons",{Jq:1,kA:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Gm:1,cj:1,Ma:1,Oa:1,Na:1,$i:1,Cm:1,Dm:1,k:1,h:1});function zP(){}zP.prototype=new jP;zP.prototype.constructor=zP;d=zP.prototype;d.I=function(){this.Rl()};d.b=function(){return this};
d.l=function(){return!0};d.of=function(){return!1};d.Rl=function(){throw(new Mw).i("head of empty stream");};d.w=function(){throw(new Jw).i("tail of empty stream");};d.$classData=l({nA:0},!1,"scala.collection.immutable.Stream$Empty$",{nA:1,kA:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Gm:1,cj:1,Ma:1,Oa:1,Na:1,$i:1,Cm:1,Dm:1,k:1,h:1});var AP=void 0;function gy(){AP||(AP=(new zP).b());return AP}
function eF(){this.pc=this.xb=this.rb=0;this.gb=!1;this.fb=0;this.$d=this.Ed=this.ld=this.Wc=this.Gc=this.Fb=null}eF.prototype=new yH;eF.prototype.constructor=eF;d=eF.prototype;d.Ba=function(){return this};d.W=function(){return this.ld};
function BP(a,b,c,e){if(a.gb)if(32>e)a.za(Mb(a.Wa()));else if(1024>e)a.X(Mb(a.x())),a.x().e[31&(b>>>5|0)]=a.Wa(),a.za(Tb(a.x(),31&(c>>>5|0)));else if(32768>e)a.X(Mb(a.x())),a.Ca(Mb(a.K())),a.x().e[31&(b>>>5|0)]=a.Wa(),a.K().e[31&(b>>>10|0)]=a.x(),a.X(Tb(a.K(),31&(c>>>10|0))),a.za(Tb(a.x(),31&(c>>>5|0)));else if(1048576>e)a.X(Mb(a.x())),a.Ca(Mb(a.K())),a.Xa(Mb(a.W())),a.x().e[31&(b>>>5|0)]=a.Wa(),a.K().e[31&(b>>>10|0)]=a.x(),a.W().e[31&(b>>>15|0)]=a.K(),a.Ca(Tb(a.W(),31&(c>>>15|0))),a.X(Tb(a.K(),31&
(c>>>10|0))),a.za(Tb(a.x(),31&(c>>>5|0)));else if(33554432>e)a.X(Mb(a.x())),a.Ca(Mb(a.K())),a.Xa(Mb(a.W())),a.ic(Mb(a.Ia())),a.x().e[31&(b>>>5|0)]=a.Wa(),a.K().e[31&(b>>>10|0)]=a.x(),a.W().e[31&(b>>>15|0)]=a.K(),a.Ia().e[31&(b>>>20|0)]=a.W(),a.Xa(Tb(a.Ia(),31&(c>>>20|0))),a.Ca(Tb(a.W(),31&(c>>>15|0))),a.X(Tb(a.K(),31&(c>>>10|0))),a.za(Tb(a.x(),31&(c>>>5|0)));else if(1073741824>e)a.X(Mb(a.x())),a.Ca(Mb(a.K())),a.Xa(Mb(a.W())),a.ic(Mb(a.Ia())),a.Cf(Mb(a.oc())),a.x().e[31&(b>>>5|0)]=a.Wa(),a.K().e[31&
(b>>>10|0)]=a.x(),a.W().e[31&(b>>>15|0)]=a.K(),a.Ia().e[31&(b>>>20|0)]=a.W(),a.oc().e[31&(b>>>25|0)]=a.Ia(),a.ic(Tb(a.oc(),31&(c>>>25|0))),a.Xa(Tb(a.Ia(),31&(c>>>20|0))),a.Ca(Tb(a.W(),31&(c>>>15|0))),a.X(Tb(a.K(),31&(c>>>10|0))),a.za(Tb(a.x(),31&(c>>>5|0)));else throw(new Ob).b();else{b=-1+a.mb()|0;switch(b){case 5:a.Cf(Mb(a.oc()));a.ic(Tb(a.oc(),31&(c>>>25|0)));a.Xa(Tb(a.Ia(),31&(c>>>20|0)));a.Ca(Tb(a.W(),31&(c>>>15|0)));a.X(Tb(a.K(),31&(c>>>10|0)));a.za(Tb(a.x(),31&(c>>>5|0)));break;case 4:a.ic(Mb(a.Ia()));
a.Xa(Tb(a.Ia(),31&(c>>>20|0)));a.Ca(Tb(a.W(),31&(c>>>15|0)));a.X(Tb(a.K(),31&(c>>>10|0)));a.za(Tb(a.x(),31&(c>>>5|0)));break;case 3:a.Xa(Mb(a.W()));a.Ca(Tb(a.W(),31&(c>>>15|0)));a.X(Tb(a.K(),31&(c>>>10|0)));a.za(Tb(a.x(),31&(c>>>5|0)));break;case 2:a.Ca(Mb(a.K()));a.X(Tb(a.K(),31&(c>>>10|0)));a.za(Tb(a.x(),31&(c>>>5|0)));break;case 1:a.X(Mb(a.x()));a.za(Tb(a.x(),31&(c>>>5|0)));break;case 0:a.za(Mb(a.Wa()));break;default:throw(new x).n(b);}a.gb=!0}}
d.I=function(){if(Rj(this))throw(new Jw).i("empty.head");return this.eb(0)};d.eb=function(a){var b=a+this.rb|0;if(0<=a&&b<this.xb)a=b;else throw(new Y).i(""+a);return Nb(this,a,a^this.pc)};d.pf=function(){return this};d.mb=function(){return this.fb};d.De=function(a){return this.aa()-a|0};d.f=function(a){return this.eb(a|0)};d.Bb=function(){return this};d.Ta=function(){return this};d.Ic=function(a,b,c){this.rb=a;this.xb=b;this.pc=c;this.gb=!1;return this};d.Cf=function(a){this.$d=a};
function ah(a,b){var c=(jl(),kl().Qa);c===(Gm(),kl().Qa)||c===qF().Ra||c===zq().Ra?a=CP(a,b):(c=c.xf(a.Sf()),c.db(a.oe()),c.kb(b),a=c.Ja());return a}d.$a=function(){return jl()};d.Wa=function(){return this.Fb};d.Ca=function(a){this.Wc=a};d.Ia=function(){return this.Ed};
function DP(a,b,c){var e=-1+a.fb|0;switch(e){case 0:a.Fb=Qb(a.Fb,b,c);break;case 1:a.Gc=Qb(a.Gc,b,c);break;case 2:a.Wc=Qb(a.Wc,b,c);break;case 3:a.ld=Qb(a.ld,b,c);break;case 4:a.Ed=Qb(a.Ed,b,c);break;case 5:a.$d=Qb(a.$d,b,c);break;default:throw(new x).n(e);}}function mm(a){if(Rj(a))throw(new Jw).i("empty.tail");return EP(a,1)}d.cg=function(){return this};
function CP(a,b){if(a.xb!==a.rb){var c=-32&a.xb,e=31&a.xb;if(a.xb!==c){var f=(new eF).Ic(a.rb,1+a.xb|0,c);Ub(f,a,a.fb);f.gb=a.gb;BP(f,a.pc,c,a.pc^c);f.Fb.e[e]=b;return f}var h=a.rb&~(-1+(1<<ca(5,-1+a.fb|0))|0),f=a.rb>>>ca(5,-1+a.fb|0)|0;if(0!==h){if(1<a.fb){var c=c-h|0,m=a.pc-h|0,h=(new eF).Ic(a.rb-h|0,(1+a.xb|0)-h|0,c);Ub(h,a,a.fb);h.gb=a.gb;DP(h,f,0);FP(h,m,c,m^c);h.Fb.e[e]=b;return h}e=-32+c|0;c=a.pc;m=(new eF).Ic(a.rb-h|0,(1+a.xb|0)-h|0,e);Ub(m,a,a.fb);m.gb=a.gb;DP(m,f,0);BP(m,c,e,c^e);m.Fb.e[32-
h|0]=b;return m}f=a.pc;h=(new eF).Ic(a.rb,1+a.xb|0,c);Ub(h,a,a.fb);h.gb=a.gb;FP(h,f,c,f^c);h.Fb.e[e]=b;return h}a=k(r(p),[32]);a.e[0]=b;b=(new eF).Ic(0,1,0);b.fb=1;b.Fb=a;return b}function hn(a,b){var c=(jl(),kl().Qa);c===(Gm(),kl().Qa)||c===qF().Ra||c===zq().Ra?a=GP(a,b):(c=c.xf(a.Sf()),c.kb(b),c.db(a.oe()),a=c.Ja());return a}d.C=function(){return Lj(this)};d.X=function(a){this.Gc=a};
function lm(a,b){var c=(jl(),kl().Qa);if(c===(Gm(),kl().Qa)||c===qF().Ra||c===zq().Ra){if(b.l())return a;b=b.ti()?b.Ba():b.cg();var e=b.v();if(2>=e||e<(a.aa()>>>5|0))return c=(new Kf).n(a),b.M(t(new u,function(a,b){return function(a){b.H=ah(b.H,a)}}(a,c))),c.H;if(a.aa()<(e>>>5|0)&&b&&b.$classData&&b.$classData.p.Nq){c=b;for(a=JD(a);a.u();)b=a.r(),c=hn(c,b);return c}return SE(a,b,c)}return SE(a,b.Ba(),c)}d.aa=function(){return this.xb-this.rb|0};d.ag=function(){return this};
d.ic=function(a){this.Ed=a};function FP(a,b,c,e){a.gb?(Sb(a,b),Pb(a,b,c,e)):(Pb(a,b,c,e),a.gb=!0)}d.Ie=function(){return this.aa()};d.x=function(){return this.Gc};d.xe=function(a){return EP(this,a)};d.oc=function(){return this.$d};d.w=function(){return mm(this)};d.oe=function(){return this};function HP(a){if(32>a)return 1;if(1024>a)return 2;if(32768>a)return 3;if(1048576>a)return 4;if(33554432>a)return 5;if(1073741824>a)return 6;throw(new Ob).b();}
function Lj(a){var b=(new fF).df(a.rb,a.xb);Ub(b,a,a.fb);a.gb&&Sb(b,a.pc);1<b.Gl&&Rb(b,a.rb,a.rb^a.pc);return b}d.Zc=function(a){return eD(this,a|0)};function IP(a,b){for(var c=0;c<b;)a.e[c]=null,c=1+c|0}d.s=function(){return $x(U(),this)};d.kd=function(a){this.fb=a};d.K=function(){return this.Wc};d.za=function(a){this.Fb=a};
function GP(a,b){if(a.xb!==a.rb){var c=-32&(-1+a.rb|0),e=31&(-1+a.rb|0);if(a.rb!==(32+c|0)){var f=(new eF).Ic(-1+a.rb|0,a.xb,c);Ub(f,a,a.fb);f.gb=a.gb;BP(f,a.pc,c,a.pc^c);f.Fb.e[e]=b;return f}var h=(1<<ca(5,a.fb))-a.xb|0,f=h&~(-1+(1<<ca(5,-1+a.fb|0))|0),h=h>>>ca(5,-1+a.fb|0)|0;if(0!==f){if(1<a.fb){var c=c+f|0,m=a.pc+f|0,f=(new eF).Ic((-1+a.rb|0)+f|0,a.xb+f|0,c);Ub(f,a,a.fb);f.gb=a.gb;DP(f,0,h);FP(f,m,c,m^c);f.Fb.e[e]=b;return f}e=32+c|0;c=a.pc;m=(new eF).Ic((-1+a.rb|0)+f|0,a.xb+f|0,e);Ub(m,a,a.fb);
m.gb=a.gb;DP(m,0,h);BP(m,c,e,c^e);m.Fb.e[-1+f|0]=b;return m}if(0>c)return f=(1<<ca(5,1+a.fb|0))-(1<<ca(5,a.fb))|0,h=c+f|0,c=a.pc+f|0,f=(new eF).Ic((-1+a.rb|0)+f|0,a.xb+f|0,h),Ub(f,a,a.fb),f.gb=a.gb,FP(f,c,h,c^h),f.Fb.e[e]=b,f;f=a.pc;h=(new eF).Ic(-1+a.rb|0,a.xb,c);Ub(h,a,a.fb);h.gb=a.gb;FP(h,f,c,f^c);h.Fb.e[e]=b;return h}a=k(r(p),[32]);a.e[31]=b;b=(new eF).Ic(31,32,0);b.fb=1;b.Fb=a;return b}
function EP(a,b){if(0>=b)b=a;else if(a.rb<(a.xb-b|0)){var c=a.rb+b|0,e=-32&c,f=HP(c^(-1+a.xb|0)),h=c&~(-1+(1<<ca(5,f))|0);b=(new eF).Ic(c-h|0,a.xb-h|0,e-h|0);Ub(b,a,a.fb);b.gb=a.gb;BP(b,a.pc,e,a.pc^e);b.fb=f;a=-1+f|0;switch(a){case 0:b.Gc=null;b.Wc=null;b.ld=null;b.Ed=null;b.$d=null;break;case 1:b.Wc=null;b.ld=null;b.Ed=null;b.$d=null;break;case 2:b.ld=null;b.Ed=null;b.$d=null;break;case 3:b.Ed=null;b.$d=null;break;case 4:b.$d=null;break;case 5:break;default:throw(new x).n(a);}a=c-h|0;if(32>a)IP(b.Fb,
a);else if(1024>a)IP(b.Fb,31&a),b.Gc=JP(b.Gc,a>>>5|0);else if(32768>a)IP(b.Fb,31&a),b.Gc=JP(b.Gc,31&(a>>>5|0)),b.Wc=JP(b.Wc,a>>>10|0);else if(1048576>a)IP(b.Fb,31&a),b.Gc=JP(b.Gc,31&(a>>>5|0)),b.Wc=JP(b.Wc,31&(a>>>10|0)),b.ld=JP(b.ld,a>>>15|0);else if(33554432>a)IP(b.Fb,31&a),b.Gc=JP(b.Gc,31&(a>>>5|0)),b.Wc=JP(b.Wc,31&(a>>>10|0)),b.ld=JP(b.ld,31&(a>>>15|0)),b.Ed=JP(b.Ed,a>>>20|0);else if(1073741824>a)IP(b.Fb,31&a),b.Gc=JP(b.Gc,31&(a>>>5|0)),b.Wc=JP(b.Wc,31&(a>>>10|0)),b.ld=JP(b.ld,31&(a>>>15|0)),
b.Ed=JP(b.Ed,31&(a>>>20|0)),b.$d=JP(b.$d,a>>>25|0);else throw(new Ob).b();}else b=jl().dc;return b}function JP(a,b){var c=k(r(p),[a.e.length]);Na(a,b,c,b,c.e.length-b|0);return c}d.Xa=function(a){this.ld=a};d.$classData=l({Nq:0},!1,"scala.collection.immutable.Vector",{Nq:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Iz:1,cj:1,Ma:1,Oa:1,Na:1,Yi:1,Wf:1,Oq:1,k:1,h:1,xd:1});function Ct(){this.zd=null}Ct.prototype=new yH;
Ct.prototype.constructor=Ct;d=Ct.prototype;d.Ba=function(){return this};d.I=function(){return QG(this)};d.eb=function(a){a=65535&(this.zd.charCodeAt(a)|0);return tu(a)};d.pf=function(){return this};d.De=function(a){return SG(this,a)};d.ie=function(a){return VG(this,a)};d.f=function(a){a=65535&(this.zd.charCodeAt(a|0)|0);return tu(a)};d.Ac=function(a){return TG(this,a)};d.l=function(){return RG(this)};d.Bb=function(){return this};d.Ta=function(){return this};d.Hc=function(a){return WG(this,a)};
d.$a=function(){return Gm()};d.m=function(){return this.zd};d.M=function(a){XG(this,a)};d.Yc=function(a,b){return $G(this,this.zd.length|0,a,b)};d.ze=function(a,b){return YG(this,a,b)};d.Ch=function(a,b){return KP(this,a,b)};d.C=function(){return ji(new ki,this,0,this.zd.length|0)};d.aa=function(){return this.zd.length|0};d.ag=function(){return this};d.Ie=function(){return this.zd.length|0};d.xe=function(a){return KP(this,a,this.zd.length|0)};d.w=function(){return aH(this)};d.oe=function(){return this};
d.Zc=function(a){return eD(this,a|0)};d.ng=function(a,b,c){bH(this,a,b,c)};d.s=function(){return $x(U(),this)};d.i=function(a){this.zd=a;return this};function KP(a,b,c){b=0>b?0:b;if(c<=b||b>=(a.zd.length|0))return(new Ct).i("");c=c>(a.zd.length|0)?a.zd.length|0:c;J();return(new Ct).i((null!==a?a.zd:null).substring(b,c))}d.ba=function(){Dt||(Dt=(new zt).b());return Dt.ba()};
d.$classData=l({yA:0},!1,"scala.collection.immutable.WrappedString",{yA:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Iz:1,cj:1,Ma:1,Oa:1,Na:1,Yi:1,Wf:1,Lq:1,Zi:1,tq:1,Id:1});function Ab(){this.qf=this.Ql=null}Ab.prototype=new uP;Ab.prototype.constructor=Ab;d=Ab.prototype;d.B=function(){return"::"};d.I=function(){return this.Ql};d.z=function(){return 2};d.l=function(){return!1};
d.A=function(a){switch(a){case 0:return this.Ql;case 1:return this.qf;default:throw(new Y).i(""+a);}};d.w=function(){return this.qf};function zb(a,b,c){a.Ql=b;a.qf=c;return a}d.D=function(){return Z(this)};d.$classData=l({uz:0},!1,"scala.collection.immutable.$colon$colon",{uz:1,Iq:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Gm:1,cj:1,Ma:1,Oa:1,Na:1,$i:1,Cm:1,E:1,Dm:1,k:1,h:1});function LP(){}LP.prototype=new uP;
LP.prototype.constructor=LP;d=LP.prototype;d.b=function(){return this};d.I=function(){this.Rl()};d.B=function(){return"Nil"};d.z=function(){return 0};d.l=function(){return!0};d.o=function(a){return a&&a.$classData&&a.$classData.p.Pd?a.l():!1};d.A=function(a){throw(new Y).i(""+a);};d.Rl=function(){throw(new Mw).i("head of empty list");};d.w=function(){throw(new Jw).i("tail of empty list");};d.D=function(){return Z(this)};
d.$classData=l({bA:0},!1,"scala.collection.immutable.Nil$",{bA:1,Iq:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Gm:1,cj:1,Ma:1,Oa:1,Na:1,$i:1,Cm:1,E:1,Dm:1,k:1,h:1});var MP=void 0;function xb(){MP||(MP=(new LP).b());return MP}function NP(){}NP.prototype=new AH;NP.prototype.constructor=NP;function OP(){}d=OP.prototype=NP.prototype;d.Ba=function(){return this};d.Bb=function(){return this};
d.$a=function(){RD||(RD=(new PD).b());return RD};d.gd=function(a,b){By(this,a,b)};d.wc=function(){};d.db=function(a){return jm(this,a)};d.ba=function(){return this.pg()};function PP(){}PP.prototype=new tH;PP.prototype.constructor=PP;function QP(){}d=QP.prototype=PP.prototype;d.Ba=function(){return this};d.Bb=function(){return this};d.l=function(){return 0===this.v()};d.o=function(a){return QE(this,a)};d.$a=function(){iF||(iF=(new gF).b());return iF};d.m=function(){return gn(this)};
d.Sm=function(a){var b=this.C();return by(b,a)};d.gd=function(a,b){By(this,a,b)};d.Al=function(){return wH(this)};d.wc=function(){};d.s=function(){var a=U();return cs(a,this,a.Om)};d.Dd=function(a){return nb(this,a)};d.db=function(a){return jm(this,a)};d.ba=function(){return this.Xc()};d.Ad=function(){return"Set"};function RP(){}RP.prototype=new FO;RP.prototype.constructor=RP;function SP(){}SP.prototype=RP.prototype;RP.prototype.Vk=function(a){a=this.mp(a);-1!==a&&this.Am(a);return this};
RP.prototype.db=function(a){return jm(this,a)};function kt(){this.Uc=this.ra=null}kt.prototype=new OP;kt.prototype.constructor=kt;d=kt.prototype;d.Ib=function(){return this};d.B=function(){return"JMapWrapper"};d.ym=function(a,b){return vg().Rh(this.ra.xk(a,b))};d.sj=function(a){this.ra.Ck(a);return this};d.z=function(){return 1};d.Za=function(a){var b=jt(new kt,this.Uc,(new nF).b());return jm(b,this).sj(a)};d.Ta=function(){return this};d.qq=function(a){return vg().Rh(this.ra.Ck(a))};
d.A=function(a){switch(a){case 0:return this.ra;default:throw(new Y).i(""+a);}};d.Ob=function(a){return GO(this,a)};d.pg=function(){return jt(new kt,this.Uc,(new nF).b())};function jt(a,b,c){a.ra=c;if(null===b)throw Dp(ni(),null);a.Uc=b;return a}d.v=function(){return this.ra.v()};d.Ja=function(){return this};d.zh=function(){return this};d.C=function(){var a=new tD;a.Wm=this.ra.be().rc();return a};d.sb=function(a){var b=this.ra.mi(a);return null!==b?(new H).n(b):this.ra.Zh(a)?(new H).n(null):B()};
d.Xk=function(a){return GO(this,a)};d.kb=function(a){return GO(this,a)};d.D=function(){return Z(this)};d.Yd=function(a){var b=jt(new kt,this.Uc,(new nF).b());return jm(b,this).Xk(a)};d.$classData=l({lz:0},!1,"scala.collection.convert.Wrappers$JMapWrapper",{lz:1,AA:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,bB:1,Zf:1,$f:1,Uf:1,dB:1,Qc:1,Oc:1,Mc:1,Lg:1,Yf:1,he:1,Hd:1,PD:1,E:1,k:1,h:1});
function ft(){this.Uc=this.ra=null}ft.prototype=new QP;ft.prototype.constructor=ft;d=ft.prototype;d.Ib=function(){return this};d.tj=function(a){return TP(this,a)};d.B=function(){return"JSetWrapper"};d.f=function(a){return this.ra.O(a)};d.z=function(){return 1};d.Za=function(a){return TP(UP(this),a)};d.Ta=function(){return this};function VP(a,b){a.ra.se(b);return a}d.A=function(a){switch(a){case 0:return this.ra;default:throw(new Y).i(""+a);}};d.Ob=function(a){return VP(this,a)};
d.Wk=function(a){return VP(this,a)};d.v=function(){return this.ra.v()};d.Ja=function(){return this};d.C=function(){LA||(LA=(new KA).b());var a=this.ra.rc();return null===a?null:a&&a.$classData&&a.$classData.p.kz&&a.oz()===dt()?a.FB():ht(new it,dt(),a)};d.Dc=function(a){return TP(UP(this),a)};d.Xc=function(){return et(new ft,this.Uc,(new EG).b())};d.Al=function(){return UP(this)};function et(a,b,c){a.ra=c;if(null===b)throw Dp(ni(),null);a.Uc=b;return a}d.O=function(a){return this.ra.O(a)};
d.kb=function(a){return VP(this,a)};d.Ee=function(a){return this.ra.Ee(a)};d.D=function(){return Z(this)};d.se=function(a){return this.ra.se(a)};d.lc=function(a){return VP(UP(this),a)};d.Xd=function(a){var b=UP(this);a=a.Ba();return jm(b,a)};function UP(a){return et(new ft,a.Uc,(new IG).Ul(a.ra))}function TP(a,b){a.ra.Ee(b);return a}
d.$classData=l({nz:0},!1,"scala.collection.convert.Wrappers$JSetWrapper",{nz:1,Rq:1,Qq:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,Zf:1,$f:1,Uf:1,Xq:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Yq:1,aj:1,Qc:1,Oc:1,Mc:1,Lg:1,Yf:1,he:1,Hd:1,E:1,k:1,h:1});function Mp(){this.re=0;this.S=null;this.pe=this.Kb=0;this.wb=null;this.le=0}Mp.prototype=new OP;Mp.prototype.constructor=Mp;d=Mp.prototype;d.Ba=function(){return this};d.Ib=function(){return this};
d.ym=function(a,b){a=au(this,a,b);if(null===a)return B();var c=a.t;a.t=b;return(new H).n(c)};d.b=function(){Mp.prototype.rw.call(this,null);return this};d.sj=function(a){Vt(this,a);return this};d.Mm=function(a){this.le=a};d.f=function(a){var b=cu(this,a);return null===b?mH(a):b.t};d.Za=function(a){var b=(new Mp).b();return jm(b,this).sj(a)};d.Ta=function(){return this};d.qq=function(a){a=Vt(this,a);return null!==a?(new H).n(a.t):B()};
function WP(a,b){var c=au(a,b.R,b.U);null!==c&&(c.t=b.U);return a}d.Ob=function(a){return WP(this,a)};d.M=function(a){for(var b=this.S,c=$t(this),e=b.e[c];null!==e;){var f=e.r();a.f((new I).d(e.xa,e.t));for(e=f;null===e&&0<c;)c=-1+c|0,e=b.e[c]}};d.pg=function(){return(new Mp).b()};d.hm=function(){return(new LD).si(this)};d.v=function(){return this.Kb};d.zh=function(){return this};d.Ja=function(){return this};
d.C=function(){return(new li).pb(MD(this),t(new u,function(){return function(a){return(new I).d(a.xa,a.t)}}(this)))};d.Xm=function(){return(new ND).si(this)};d.rw=function(a){Xt(this);null!==a&&(this.$k(a.Cx()),this.Mk(a.S),this.Dh(a.Kb),this.Nk(a.pe),this.Mm(a.le),this.Kk(a.wb));return this};d.vo=function(a,b){return(new kC).d(a,b)};d.sb=function(a){a=cu(this,a);return null===a?B():(new H).n(a.t)};d.Xk=function(a){return WP(this,a)};d.Kk=function(a){this.wb=a};
d.O=function(a){return null!==cu(this,a)};d.Mk=function(a){this.S=a};d.kb=function(a){return WP(this,a)};d.Jp=function(){return(new JH).si(this)};d.$k=function(a){this.re=a};d.Yd=function(a){var b=(new Mp).b();return jm(b,this).Xk(a)};d.Nk=function(a){this.pe=a};d.Dh=function(a){this.Kb=a};
d.$classData=l({KA:0},!1,"scala.collection.mutable.HashMap",{KA:1,AA:1,bd:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,ed:1,Lc:1,dd:1,fd:1,Va:1,T:1,Fa:1,bB:1,Zf:1,$f:1,Uf:1,dB:1,Qc:1,Oc:1,Mc:1,Lg:1,Yf:1,he:1,Hd:1,QA:1,TA:1,xd:1,k:1,h:1});function gG(){this.Ei=this.dh=null;this.re=0;this.S=null;this.pe=this.Kb=0;this.wb=null;this.le=0}gG.prototype=new QP;gG.prototype.constructor=gG;d=gG.prototype;d.tj=function(a){this.Ee(a);return this};d.Ib=function(){return this};
d.b=function(){Xt(this);this.Ei=this.dh=null;return this};d.Mm=function(a){this.le=a};d.f=function(a){return this.O(a)};d.Za=function(a){return wH(this).tj(a)};d.Ta=function(){return this};d.Ob=function(a){return XP(this,a)};d.$a=function(){hG||(hG=(new fG).b());return hG};d.M=function(a){for(var b=this.dh;null!==b;)a.f(b.xa),b=b.Ce};function XP(a,b){a.se(b);return a}d.Wk=function(a){return XP(this,a)};d.v=function(){return this.Kb};d.Ja=function(){return this};
d.C=function(){var a=new VD;a.we=this.dh;return a};d.Dc=function(a){return wH(this).tj(a)};d.Xc=function(){return(new gG).b()};d.vo=function(a){a=(new oC).n(a);null===this.dh?this.dh=a:(this.Ei.Ce=a,a.Df=this.Ei);return this.Ei=a};d.Kk=function(a){this.wb=a};d.O=function(a){return null!==cu(this,a)};d.Mk=function(a){this.S=a};d.kb=function(a){return XP(this,a)};
d.Ee=function(a){a=Vt(this,a);return null!==a?(null===a.Df?this.dh=a.Ce:a.Df.Ce=a.Ce,null===a.Ce?this.Ei=a.Df:a.Ce.Df=a.Df,a.Df=null,a.Ce=null,!0):!1};d.se=function(a){return null===au(this,a,null)};d.lc=function(a){return wH(this).Wk(a)};d.Xd=function(a){var b=wH(this);a=a.Ba();return jm(b,a)};d.$k=function(a){this.re=a};d.Nk=function(a){this.pe=a};d.Dh=function(a){this.Kb=a};
d.$classData=l({WA:0},!1,"scala.collection.mutable.LinkedHashSet",{WA:1,Rq:1,Qq:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,Zf:1,$f:1,Uf:1,Xq:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Yq:1,aj:1,Qc:1,Oc:1,Mc:1,Lg:1,Yf:1,he:1,Hd:1,QA:1,TA:1,k:1,h:1});function hF(){this.re=0;this.S=null;this.pe=this.Kb=0;this.wb=null;this.le=0}hF.prototype=new QP;hF.prototype.constructor=hF;d=hF.prototype;d.Ba=function(){return this};d.tj=function(a){return YP(this,a)};d.Ib=function(){return this};
d.b=function(){hF.prototype.qw.call(this,null);return this};d.f=function(a){return null!==Lt(this,a)};d.Za=function(a){var b=(new hF).b();return YP(jm(b,this),a)};d.Ta=function(){return this};d.Ob=function(a){return ZP(this,a)};d.$a=function(){eG||(eG=(new dG).b());return eG};d.M=function(a){for(var b=0,c=this.S.e.length;b<c;){var e=this.S.e[b];null!==e&&a.f(Xb(e));b=1+b|0}};d.Wk=function(a){return ZP(this,a)};d.v=function(){return this.Kb};d.Ja=function(){return this};
d.C=function(){var a=new KD;if(null===this)throw Dp(ni(),null);a.sa=this;a.Bc=0;return a};d.Dc=function(a){var b=(new hF).b();return YP(jm(b,this),a)};d.Xc=function(){return(new hF).b()};d.Al=function(){var a=(new hF).b();return jm(a,this)};d.qw=function(a){this.re=450;this.S=k(r(p),[Zt(Yt(),32)]);this.Kb=0;this.pe=Jt().Ni(this.re,Zt(Yt(),32));this.wb=null;this.le=Et(this);null!==a&&(this.re=a.Cx(),this.S=a.hE(),this.Kb=a.Kb,this.pe=a.pe,this.le=a.le,this.wb=a.wb);return this};
d.O=function(a){return null!==Lt(this,a)};d.kb=function(a){return ZP(this,a)};function YP(a,b){Kt(a,b);return a}d.Ee=function(a){return Kt(this,a)};d.se=function(a){return Gt(this,a)};d.lc=function(a){var b=(new hF).b();return ZP(jm(b,this),a)};d.Xd=function(a){var b=(new hF).b(),b=jm(b,this);a=a.Ba();return jm(b,a)};function ZP(a,b){Gt(a,b);return a}
d.$classData=l({OA:0},!1,"scala.collection.mutable.HashSet",{OA:1,Rq:1,Qq:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,Zf:1,$f:1,Uf:1,Xq:1,kc:1,T:1,jc:1,tc:1,vc:1,uc:1,Fa:1,Yq:1,aj:1,Qc:1,Oc:1,Mc:1,Lg:1,Yf:1,he:1,Hd:1,ZD:1,$D:1,xd:1,k:1,h:1});function zg(){this.Kd=this.Ka=null;this.ch=!1;this.rd=0}zg.prototype=new SP;zg.prototype.constructor=zg;d=zg.prototype;d.np=function(a,b){return dD(this.Ka,a,b)};
function $P(a){if(!a.l()){var b=a.Ka,c=a.Kd.qf;a.Ka=xb();a.Kd=null;a.ch=!1;for(a.rd=0;b!==c;)Ag(a,b.I()),b=b.w()}}d.Ib=function(){return this};d.b=function(){this.Ka=xb();this.ch=!1;this.rd=0;return this};d.I=function(){return this.Ka.I()};d.eb=function(a){if(0>a||a>=this.rd)throw(new Y).i(""+a);return cH(this.Ka,a)};d.pf=function(){return this};d.De=function(a){return 0>a?1:kH(this.Ka,a)};d.f=function(a){return this.eb(a|0)};d.ie=function(a){return eH(this.Ka,a)};
d.Za=function(a){return aQ(bQ((new zg).b(),this),a)};d.Ac=function(a){return dH(this.Ka,a)};d.l=function(){return 0===this.rd};function Bg(a){a.ch=!a.l();return a.Ka}d.Ta=function(){return this};d.o=function(a){return a&&a.$classData&&a.$classData.p.Wq?this.Ka.o(a.Ka):fn(this,a)};d.Mf=function(a,b,c){return Oj(this.Ka,a,b,c)};d.Ob=function(a){return Ag(this,a)};d.Hc=function(a){return fH(this.Ka,a)};d.$a=function(){xG||(xG=(new wG).b());return xG};
d.M=function(a){for(var b=this.Ka;!b.l();)a.f(b.I()),b=b.w()};d.Yc=function(a,b){return gH(this.Ka,a,b)};d.ze=function(a,b){return hH(this.Ka,a,b)};d.v=function(){return this.rd};d.Ja=function(){return Bg(this)};d.C=function(){var a=new WD;a.ai=this.l()?xb():this.Ka;return a};d.gd=function(a,b){By(this,a,b)};d.aa=function(){return this.rd};d.ag=function(){return this};
d.Am=function(a){if(0>a||a>=this.rd)throw(new Y).i(""+a);this.ch&&$P(this);var b=this.Ka.I();if(0===a)this.Ka=this.Ka.w();else{for(var c=this.Ka,b=1;b<a;)c=c.w(),b=1+b|0;b=c.w().I();this.Kd===c.w()&&(this.Kd=c);c.qf=c.w().w()}cQ(this);return b};function aQ(a,b){a.ch&&$P(a);if(!a.l())if(D(G(),a.Ka.I(),b))a.Ka=a.Ka.w(),cQ(a);else{for(var c=a.Ka;!c.w().l()&&!D(G(),c.w().I(),b);)c=c.w();if(!c.w().l()){b=c;var e=b.qf,f=a.Kd;if(null===e?null===f:e.o(f))a.Kd=b;b.qf=c.w().w();cQ(a)}}return a}d.xc=function(){return this.Ka.xc()};
function cQ(a){a.rd=a.rd-1|0;0>=a.rd&&(a.Kd=null)}d.vf=function(a,b,c,e){return rs(this.Ka,a,b,c,e)};function Ag(a,b){a.ch&&$P(a);if(a.l())a.Kd=zb(new Ab,b,xb()),a.Ka=a.Kd;else{var c=a.Kd;a.Kd=zb(new Ab,b,xb());c.qf=a.Kd}a.rd=1+a.rd|0;return a}d.Vk=function(a){return aQ(this,a)};d.Zc=function(a){return jH(this.Ka,a|0)};d.kr=function(){var a=this.Ka,b=yA(),b=ny(b);return ll(a,b)};d.tf=function(a,b){return gH(this.Ka,a,b)};d.kb=function(a){return Ag(this,a)};d.mp=function(a){return dD(this.Ka,a,0)};
d.wc=function(){};function bQ(a,b){a:for(;;){var c=b;if(null!==c&&c===a){var e=a;b=a.rd;c=e.ba();if(!(0>=b)){c.gd(b,e);for(var f=0,e=e.C();f<b&&e.u();)c.kb(e.r()),f=1+f|0}b=c.Ja();continue a}return jm(a,b)}}d.db=function(a){return bQ(this,a)};d.Ad=function(){return"ListBuffer"};
d.$classData=l({Wq:0},!1,"scala.collection.mutable.ListBuffer",{Wq:1,Pq:1,Hm:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Km:1,Zf:1,$f:1,Uf:1,Lm:1,Yf:1,he:1,Hd:1,Sq:1,Tq:1,Oc:1,Mc:1,Lg:1,aj:1,Fa:1,dj:1,Qc:1,WD:1,VD:1,XD:1,k:1,h:1});function os(){this.ra=null}os.prototype=new FO;os.prototype.constructor=os;d=os.prototype;d.Ba=function(){return this};d.b=function(){os.prototype.jw.call(this,16,"");return this};d.I=function(){return QG(this)};
d.eb=function(a){a=65535&(this.ra.Da.charCodeAt(a)|0);return tu(a)};d.pf=function(){return this};d.De=function(a){return SG(this,a)};d.f=function(a){a=65535&(this.ra.Da.charCodeAt(a|0)|0);return tu(a)};d.ie=function(a){return VG(this,a)};d.Ac=function(a){return TG(this,a)};d.l=function(){return RG(this)};d.Bb=function(){return this};d.Ta=function(){return this};d.Rm=function(a,b){return this.ra.Da.substring(a,b)};d.Ob=function(a){zn(this.ra,null===a?0:a.t);return this};
d.Hc=function(a){return WG(this,a)};d.$a=function(){FF||(FF=(new EF).b());return FF};d.m=function(){return this.ra.Da};d.M=function(a){XG(this,a)};d.Yc=function(a,b){var c=this.ra.aa();return $G(this,c,a,b)};d.ze=function(a,b){return YG(this,a,b)};d.Ch=function(a,b){return pH(this,a,b)};d.Ja=function(){return this.ra.Da};function yn(a,b){var c=a.ra;c.Da=""+c.Da+b;return a}d.C=function(){return ji(new ki,this,0,this.ra.aa())};d.hj=function(){return this};d.gd=function(a,b){By(this,a,b)};
d.jw=function(a,b){a=(new HB).ob((b.length|0)+a|0);a.Da=""+a.Da+b;os.prototype.Tl.call(this,a);return this};d.aa=function(){return this.ra.aa()};d.ag=function(){return this};d.Ie=function(){return this.ra.aa()};d.xe=function(a){var b=this.ra.aa();return pH(this,a,b)};d.oe=function(){return this};d.w=function(){return aH(this)};d.Tl=function(a){this.ra=a;return this};function at(a,b){var c=a.ra;c.Da+=""+b;return a}d.Zc=function(a){return eD(this,a|0)};d.kb=function(a){zn(this.ra,null===a?0:a.t);return this};
d.ng=function(a,b,c){bH(this,a,b,c)};d.wc=function(){};d.s=function(){return $x(U(),this)};d.ba=function(){return nC(new mC,(new os).b())};d.db=function(a){return jm(this,a)};d.$classData=l({gB:0},!1,"scala.collection.mutable.StringBuilder",{gB:1,Hm:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Km:1,Zf:1,$f:1,Uf:1,Lm:1,Yf:1,he:1,Hd:1,bm:1,Uq:1,Yi:1,Wf:1,Jm:1,Lq:1,Zi:1,tq:1,Id:1,dj:1,Qc:1,Oc:1,Mc:1,k:1,h:1});
function A(){this.ya=null}A.prototype=new SP;A.prototype.constructor=A;d=A.prototype;d.Ba=function(){return this};d.Ib=function(){return this};d.b=function(){A.prototype.g.call(this,[]);return this};d.I=function(){return QG(this)};d.eb=function(a){return this.ya[a]};d.pf=function(){return this};d.De=function(a){return SG(this,a)};d.f=function(a){return this.ya[a|0]};d.ie=function(a){return VG(this,a)};d.Ac=function(a){return TG(this,a)};d.Za=function(a){return uH(this).Vk(a)};d.l=function(){return RG(this)};
d.Bb=function(){return this};d.Ta=function(){return this};d.Ob=function(a){this.ya.push(a);return this};d.Hc=function(a){return WG(this,a)};d.$a=function(){return nB()};d.M=function(a){XG(this,a)};d.Yc=function(a,b){return $G(this,this.ya.length|0,a,b)};d.ze=function(a,b){return YG(this,a,b)};d.Ch=function(a,b){return ZG(this,a,b)};d.Ja=function(){return this};d.C=function(){return ji(new ki,this,0,this.ya.length|0)};d.hj=function(){return this};d.gd=function(a,b){By(this,a,b)};
d.aa=function(){return this.ya.length|0};d.ag=function(){return this};d.Ie=function(){return this.ya.length|0};d.Am=function(a){if(0>a||a>=(this.ya.length|0))throw(new Y).b();return this.ya.splice(a,1)[0]};d.xe=function(a){return ZG(this,a,this.ya.length|0)};d.w=function(){return aH(this)};d.oe=function(){return this};d.Zc=function(a){return eD(this,a|0)};d.kb=function(a){this.ya.push(a);return this};d.ng=function(a,b,c){bH(this,a,b,c)};d.wc=function(){};d.s=function(){return $x(U(),this)};
d.g=function(a){this.ya=a;return this};d.Ad=function(){return"WrappedArray"};d.$classData=l({ar:0},!1,"scala.scalajs.js.WrappedArray",{ar:1,Pq:1,Hm:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Km:1,Zf:1,$f:1,Uf:1,Lm:1,Yf:1,he:1,Hd:1,Sq:1,Tq:1,Oc:1,Mc:1,Lg:1,aj:1,Fa:1,Uq:1,Yi:1,Wf:1,Jm:1,DA:1,Vq:1,Zi:1,Qc:1});function QD(){this.sp=0;this.ya=null;this.Ga=0}QD.prototype=new SP;QD.prototype.constructor=QD;d=QD.prototype;
d.Ba=function(){return this};d.Ib=function(){return this};function vH(a,b){nP(a,1+a.Ga|0);a.ya.e[a.Ga]=b;a.Ga=1+a.Ga|0;return a}d.b=function(){QD.prototype.ob.call(this,16);return this};d.I=function(){return QG(this)};d.eb=function(a){return mP(this,a)};d.pf=function(){return this};d.De=function(a){return SG(this,a)};d.f=function(a){return mP(this,a|0)};d.ie=function(a){return VG(this,a)};d.Ac=function(a){return TG(this,a)};d.Za=function(a){return uH(this).Vk(a)};d.l=function(){return RG(this)};
d.Bb=function(){return this};d.Ta=function(){return this};d.Ob=function(a){return vH(this,a)};d.Hc=function(a){return WG(this,a)};d.$a=function(){vG||(vG=(new uG).b());return vG};d.M=function(a){for(var b=0,c=this.Ga;b<c;)a.f(this.ya.e[b]),b=1+b|0};d.Yc=function(a,b){return $G(this,this.Ga,a,b)};d.ze=function(a,b){return YG(this,a,b)};d.Ch=function(a,b){return ZG(this,a,b)};d.Ja=function(){return this};d.hj=function(){return this};d.C=function(){return ji(new ki,this,0,this.Ga)};
d.gd=function(a,b){By(this,a,b)};d.ob=function(a){a=this.sp=a;this.ya=k(r(p),[1<a?a:1]);this.Ga=0;return this};d.aa=function(){return this.Ga};d.ag=function(){return this};d.Ie=function(){return this.Ga};d.Am=function(a){var b=mP(this,a);if(0>a||a>(this.Ga-1|0))throw(new Y).i("at "+a+" deleting 1");Na(this.ya,a+1|0,this.ya,a,this.Ga-(a+1|0)|0);a=this.Ga-1|0;for(DA(J(),a<=this.Ga);this.Ga>a;)this.Ga=-1+this.Ga|0,this.ya.e[this.Ga]=null;return b};d.xe=function(a){return ZG(this,a,this.Ga)};d.oe=function(){return this};
d.w=function(){return aH(this)};d.Zc=function(a){return eD(this,a|0)};d.kb=function(a){return vH(this,a)};d.ng=function(a,b,c){var e=dv(rl(),a)-b|0;c=c<e?c:e;e=this.Ga;c=c<e?c:e;0<c&&WB(YB(),this.ya,0,a,b,c)};d.wc=function(a){a>this.Ga&&1<=a&&(a=k(r(p),[a]),Na(this.ya,0,a,0,this.Ga),this.ya=a)};d.s=function(){return $x(U(),this)};d.db=function(a){if(a&&a.$classData&&a.$classData.p.Wf){var b=a.aa();nP(this,this.Ga+b|0);a.ng(this.ya,this.Ga,b);this.Ga=this.Ga+b|0;a=this}else a=jm(this,a);return a};
d.Ad=function(){return"ArrayBuffer"};d.$classData=l({BA:0},!1,"scala.collection.mutable.ArrayBuffer",{BA:1,Pq:1,Hm:1,lf:1,ta:1,ua:1,c:1,ma:1,ga:1,ha:1,Z:1,G:1,F:1,da:1,fa:1,ka:1,na:1,la:1,ja:1,ca:1,ea:1,q:1,je:1,Va:1,T:1,Pd:1,Qd:1,Rd:1,Km:1,Zf:1,$f:1,Uf:1,Lm:1,Yf:1,he:1,Hd:1,Sq:1,Tq:1,Oc:1,Mc:1,Lg:1,aj:1,Fa:1,Vq:1,Jm:1,Wf:1,Zi:1,Qc:1,bE:1,Uq:1,Yi:1,xd:1,k:1,h:1});jd||(jd=(new id).b());ia(r(na),[]);
(function(){var a;$b||($b=(new Zb).b());a=$b;var b=qd();cB();cB();var c=tn(),c=Tv(c);cB();var e=Uz().Y,b=qp(b.Af,c,e).outerHTML;wp(up().Zp.Og,b+"\n");b=vw();c=[zj(),qd()];Kp(b,(new A).g(c));Hp(vw(),t(new u,function(){return function(a){Gj||(Gj=(new Fj).b());cB();cB();cB();var b=tn(),b=Tv(b),c=(cB(),Uz().Y);Ej||(Ej=(new Dj).b());a=qp(a.Af,b,c);xj().head.appendChild(a)}}(a)))})();cc||(cc=(new ac).b());var dQ=cc.Qh(),eQ=xj().getElementById("container"),fQ=rb().Y;dQ.Ki.f(lb.render(dQ.L,eQ,Hd(qb(),fQ)));

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(16)))

/***/ }),
/* 16 */
/***/ (function(module, exports) {

var g;

// This works in non-strict mode
g = (function() {
	return this;
})();

try {
	// This works if eval is allowed (see CSP)
	g = g || Function("return this")() || (1,eval)("this");
} catch(e) {
	// This works if the window reference is available
	if(typeof window === "object")
		g = window;
}

// g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}

module.exports = g;


/***/ }),
/* 17 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.2.0
 * react.production.min.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

var m=__webpack_require__(2),n=__webpack_require__(3),p=__webpack_require__(1),q="function"===typeof Symbol&&Symbol["for"],r=q?Symbol["for"]("react.element"):60103,t=q?Symbol["for"]("react.call"):60104,u=q?Symbol["for"]("react.return

c<b;c++)e+="\x26args[]\x3d"+encodeURIComponent(arguments[c+1]);b=Error(e+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings.");b.name="Invariant Violation";b.framesToPop=1;throw b;}
var z={

var D=B.prototype=new C;D.constructor=B;m(D,A.prototype);D.isPureReactComponent=!0;function E(a,b,e){this.props=a;this.context=b;this.refs=n;this.updater=e||z}var F=E.prototype=new C;F.constructor=E;m(F,A.prototype);F.unstable_isAsyncReactComponent=!0;F.render=function(){

$$typeof:r,type:a.type,key:b,ref:a.ref,props:a.props,_owner:a._owner}),c.push(a))}function T(a,b,e,c,d){var g="";null!=e&&(g=(""+e).replace(L,"$\x26/")+"/");b=N(b,g,c,d);null==a||P(a,"",S,b);O(b)}
var U={Children:{map:function(a,b,e){if(null==a)return a;var c=[];T(a,c,null,b,e);return c},forEach:function(a,b,e){if(null==a)return a;b=N(null,null,b,e);null==a||P(a,"",R,b);O(b)},count:function(a){return null==a?0:P(a,"",p.thatReturnsNull,null)},toArray:function(a){var b=[];T(a,b,null,p.thatReturnsArgument);return b},only:function(a){K(a)?void 0:y("143");return a}



/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/** @license React v16.2.0
 * react.development.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (process.env.NODE_ENV !== "production") {
  (function() {
'use strict';

var _assign = __webpack_require__(2);
var emptyObject = __webpack_require__(3);
var invariant = __webpack_require__(5);
var warning = __webpack_require__(6);
var emptyFunction = __webpack_require__(1);
var checkPropTypes = __webpack_require__(7);

// TODO: this is special because it gets imported during build.

var ReactVersion = '16.2.0';

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol['for'];

var REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;
var REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;
var REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;

var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';

function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable === 'undefined') {
    return null;
  }
  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }
  return null;
}

/**
 * WARNING: DO NOT manually require this module.
 * This is a replacement for `invariant(...)` used by the error code system
 * and will _only_ be required by the corresponding babel pass.
 * It always throws.
 */

/**
 * Forked from fbjs/warning:
 * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
 *
 * Only change is we use console.warn instead of console.error,
 * and do nothing when 'console' is not supported.
 * This really simplifies the code.
 * ---
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var lowPriorityWarning = function () {};

{
  var printWarning = function (format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.warn(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  lowPriorityWarning = function (condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }
    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

var lowPriorityWarning$1 = lowPriorityWarning;

var didWarnStateUpdateForUnmountedComponent = {};

function warnNoop(publicInstance, callerName) {
  {
    var constructor = publicInstance.constructor;
    var componentName = constructor && (constructor.displayName || constructor.name) || 'ReactClass';
    var warningKey = componentName + '.' + callerName;
    if (didWarnStateUpdateForUnmountedComponent[warningKey]) {
      return;
    }
    warning(false, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op.\n\nPlease check the code for the %s component.', callerName, callerName, componentName);
    didWarnStateUpdateForUnmountedComponent[warningKey] = true;
  }
}

/**
 * This is the abstract API for an update queue.
 */
var ReactNoopUpdateQueue = {
  /**
   * Checks whether or not this composite component is mounted.
   * @param {ReactClass} publicInstance The instance we want to test.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function (publicInstance) {
    return false;
  },

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldComponentUpdate`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueForceUpdate: function (publicInstance, callback, callerName) {
    warnNoop(publicInstance, 'forceUpdate');
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} completeState Next state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} callerName name of the calling function in the public API.
   * @internal
   */
  enqueueReplaceState: function (publicInstance, completeState, callback, callerName) {
    warnNoop(publicInstance, 'replaceState');
  },

  /**
   * Sets a subset of the state. This only exists because _pendingState is
   * internal. This provides a merging strategy that is not available to deep
   * properties which is confusing. TODO: Expose pendingState or don't use it
   * during the merge.
   *
   * @param {ReactClass} publicInstance The instance that should rerender.
   * @param {object} partialState Next partial state to be merged with state.
   * @param {?function} callback Called after component is updated.
   * @param {?string} Name of the calling function in the public API.
   * @internal
   */
  enqueueSetState: function (publicInstance, partialState, callback, callerName) {
    warnNoop(publicInstance, 'setState');
  }
};

/**
 * Base class helpers for the updating state of a component.
 */
function Component(props, context, updater) {
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

Component.prototype.isReactComponent = {};

/**
 * Sets a subset of the state. Always use this to mutate
 * state. You should treat `this.state` as immutable.
 *
 * There is no guarantee that `this.state` will be immediately updated, so
 * accessing `this.state` after calling this method may return the old value.
 *
 * There is no guarantee that calls to `setState` will run synchronously,
 * as they may eventually be batched together.  You can provide an optional
 * callback that will be executed when the call to setState is actually
 * completed.
 *
 * When a function is provided to setState, it will be called at some point in
 * the future (not synchronously). It will be called with the up to date
 * component arguments (state, props, context). These values can be different
 * from this.* because your function may be called after receiveProps but before
 * shouldComponentUpdate, and this new state, props, and context will not yet be
 * assigned to this.
 *
 * @param {object|function} partialState Next partial state or function to
 *        produce next partial state to be merged with current state.
 * @param {?function} callback Called after state is updated.
 * @final
 * @protected
 */
Component.prototype.setState = function (partialState, callback) {
  !(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null) ? invariant(false, 'setState(...): takes an object of state variables to update or a function which returns an object of state variables.') : void 0;
  this.updater.enqueueSetState(this, partialState, callback, 'setState');
};

/**
 * Forces an update. This should only be invoked when it is known with
 * certainty that we are **not** in a DOM transaction.
 *
 * You may want to call this when you know that some deeper aspect of the
 * component's state has changed but `setState` was not called.
 *
 * This will not invoke `shouldComponentUpdate`, but it will invoke
 * `componentWillUpdate` and `componentDidUpdate`.
 *
 * @param {?function} callback Called after update is complete.
 * @final
 * @protected
 */
Component.prototype.forceUpdate = function (callback) {
  this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');
};

/**
 * Deprecated APIs. These APIs used to exist on classic React classes but since
 * we would like to deprecate them, we're not going to move them over to this
 * modern base class. Instead, we define a getter that warns if it's accessed.
 */
{
  var deprecatedAPIs = {
    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],
    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']
  };
  var defineDeprecationWarning = function (methodName, info) {
    Object.defineProperty(Component.prototype, methodName, {
      get: function () {
        lowPriorityWarning$1(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);
        return undefined;
      }
    });
  };
  for (var fnName in deprecatedAPIs) {
    if (deprecatedAPIs.hasOwnProperty(fnName)) {
      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
    }
  }
}

/**
 * Base class helpers for the updating state of a component.
 */
function PureComponent(props, context, updater) {
  // Duplicated from Component.
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

function ComponentDummy() {}
ComponentDummy.prototype = Component.prototype;
var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
pureComponentPrototype.constructor = PureComponent;
// Avoid an extra prototype jump for these methods.
_assign(pureComponentPrototype, Component.prototype);
pureComponentPrototype.isPureReactComponent = true;

function AsyncComponent(props, context, updater) {
  // Duplicated from Component.
  this.props = props;
  this.context = context;
  this.refs = emptyObject;
  // We initialize the default updater but the real one gets injected by the
  // renderer.
  this.updater = updater || ReactNoopUpdateQueue;
}

var asyncComponentPrototype = AsyncComponent.prototype = new ComponentDummy();
asyncComponentPrototype.constructor = AsyncComponent;
// Avoid an extra prototype jump for these methods.
_assign(asyncComponentPrototype, Component.prototype);
asyncComponentPrototype.unstable_isAsyncReactComponent = true;
asyncComponentPrototype.render = function () {
  return this.props.children;
};

/**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 */
var ReactCurrentOwner = {
  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null
};

var hasOwnProperty = Object.prototype.hasOwnProperty;

var RESERVED_PROPS = {
  key: true,
  ref: true,
  __self: true,
  __source: true
};

var specialPropKeyWarningShown;
var specialPropRefWarningShown;

function hasValidRef(config) {
  {
    if (hasOwnProperty.call(config, 'ref')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;
      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }
  return config.ref !== undefined;
}

function hasValidKey(config) {
  {
    if (hasOwnProperty.call(config, 'key')) {
      var getter = Object.getOwnPropertyDescriptor(config, 'key').get;
      if (getter && getter.isReactWarning) {
        return false;
      }
    }
  }
  return config.key !== undefined;
}

function defineKeyPropWarningGetter(props, displayName) {
  var warnAboutAccessingKey = function () {
    if (!specialPropKeyWarningShown) {
      specialPropKeyWarningShown = true;
      warning(false, '%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
    }
  };
  warnAboutAccessingKey.isReactWarning = true;
  Object.defineProperty(props, 'key', {
    get: warnAboutAccessingKey,
    configurable: true
  });
}

function defineRefPropWarningGetter(props, displayName) {
  var warnAboutAccessingRef = function () {
    if (!specialPropRefWarningShown) {
      specialPropRefWarningShown = true;
      warning(false, '%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);
    }
  };
  warnAboutAccessingRef.isReactWarning = true;
  Object.defineProperty(props, 'ref', {
    get: warnAboutAccessingRef,
    configurable: true
  });
}

/**
 * Factory method to create a new React element. This no longer adheres to
 * the class pattern, so do not use new to call it. Also, no instanceof check
 * will work. Instead test $$typeof field against Symbol.for('react.element') to check
 * if something is a React Element.
 *
 * @param {*} type
 * @param {*} key
 * @param {string|object} ref
 * @param {*} self A *temporary* helper to detect places where `this` is
 * different from the `owner` when React.createElement is called, so that we
 * can warn. We want to get rid of owner and replace string `ref`s with arrow
 * functions, and as long as `this` and owner are the same, there will be no
 * change in behavior.
 * @param {*} source An annotation object (added by a transpiler or otherwise)
 * indicating filename, line number, and/or other information.
 * @param {*} owner
 * @param {*} props
 * @internal
 */
var ReactElement = function (type, key, ref, self, source, owner, props) {
  var element = {
    // This tag allow us to uniquely identify this as a React Element
    $$typeof: REACT_ELEMENT_TYPE,

    // Built-in properties that belong on the element
    type: type,
    key: key,
    ref: ref,
    props: props,

    // Record the component responsible for creating this element.
    _owner: owner
  };

  {
    // The validation flag is currently mutative. We put it on
    // an external backing store so that we can freeze the whole object.
    // This can be replaced with a WeakMap once they are implemented in
    // commonly used development environments.
    element._store = {};

    // To make comparing ReactElements easier for testing purposes, we make
    // the validation flag non-enumerable (where possible, which should
    // include every environment we run tests in), so the test framework
    // ignores it.
    Object.defineProperty(element._store, 'validated', {
      configurable: false,
      enumerable: false,
      writable: true,
      value: false
    });
    // self and source are DEV only properties.
    Object.defineProperty(element, '_self', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: self
    });
    // Two elements created in two different places should be considered
    // equal for testing purposes and therefore we hide it from enumeration.
    Object.defineProperty(element, '_source', {
      configurable: false,
      enumerable: false,
      writable: false,
      value: source
    });
    if (Object.freeze) {
      Object.freeze(element.props);
      Object.freeze(element);
    }
  }

  return element;
};

/**
 * Create and return a new ReactElement of the given type.
 * See https://reactjs.org/docs/react-api.html#createelement
 */
function createElement(type, config, children) {
  var propName;

  // Reserved names are extracted
  var props = {};

  var key = null;
  var ref = null;
  var self = null;
  var source = null;

  if (config != null) {
    if (hasValidRef(config)) {
      ref = config.ref;
    }
    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    self = config.__self === undefined ? null : config.__self;
    source = config.__source === undefined ? null : config.__source;
    // Remaining properties are added to a new props object
    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        props[propName] = config[propName];
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    {
      if (Object.freeze) {
        Object.freeze(childArray);
      }
    }
    props.children = childArray;
  }

  // Resolve default props
  if (type && type.defaultProps) {
    var defaultProps = type.defaultProps;
    for (propName in defaultProps) {
      if (props[propName] === undefined) {
        props[propName] = defaultProps[propName];
      }
    }
  }
  {
    if (key || ref) {
      if (typeof props.$$typeof === 'undefined' || props.$$typeof !== REACT_ELEMENT_TYPE) {
        var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;
        if (key) {
          defineKeyPropWarningGetter(props, displayName);
        }
        if (ref) {
          defineRefPropWarningGetter(props, displayName);
        }
      }
    }
  }
  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);
}

/**
 * Return a function that produces ReactElements of a given type.
 * See https://reactjs.org/docs/react-api.html#createfactory
 */


function cloneAndReplaceKey(oldElement, newKey) {
  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);

  return newElement;
}

/**
 * Clone and return a new ReactElement using element as the starting point.
 * See https://reactjs.org/docs/react-api.html#cloneelement
 */
function cloneElement(element, config, children) {
  var propName;

  // Original props are copied
  var props = _assign({}, element.props);

  // Reserved names are extracted
  var key = element.key;
  var ref = element.ref;
  // Self is preserved since the owner is preserved.
  var self = element._self;
  // Source is preserved since cloneElement is unlikely to be targeted by a
  // transpiler, and the original source is probably a better indicator of the
  // true owner.
  var source = element._source;

  // Owner will be preserved, unless ref is overridden
  var owner = element._owner;

  if (config != null) {
    if (hasValidRef(config)) {
      // Silently steal the ref from the parent.
      ref = config.ref;
      owner = ReactCurrentOwner.current;
    }
    if (hasValidKey(config)) {
      key = '' + config.key;
    }

    // Remaining properties override existing props
    var defaultProps;
    if (element.type && element.type.defaultProps) {
      defaultProps = element.type.defaultProps;
    }
    for (propName in config) {
      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {
        if (config[propName] === undefined && defaultProps !== undefined) {
          // Resolve default props
          props[propName] = defaultProps[propName];
        } else {
          props[propName] = config[propName];
        }
      }
    }
  }

  // Children can be more than one argument, and those are transferred onto
  // the newly allocated props object.
  var childrenLength = arguments.length - 2;
  if (childrenLength === 1) {
    props.children = children;
  } else if (childrenLength > 1) {
    var childArray = Array(childrenLength);
    for (var i = 0; i < childrenLength; i++) {
      childArray[i] = arguments[i + 2];
    }
    props.children = childArray;
  }

  return ReactElement(element.type, key, ref, self, source, owner, props);
}

/**
 * Verifies the object is a ReactElement.
 * See https://reactjs.org/docs/react-api.html#isvalidelement
 * @param {?object} object
 * @return {boolean} True if `object` is a valid component.
 * @final
 */
function isValidElement(object) {
  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;
}

var ReactDebugCurrentFrame = {};

{
  // Component that is being worked on
  ReactDebugCurrentFrame.getCurrentStack = null;

  ReactDebugCurrentFrame.getStackAddendum = function () {
    var impl = ReactDebugCurrentFrame.getCurrentStack;
    if (impl) {
      return impl();
    }
    return null;
  };
}

var SEPARATOR = '.';
var SUBSEPARATOR = ':';

/**
 * Escape and wrap key so it is safe to use as a reactid
 *
 * @param {string} key to be escaped.
 * @return {string} the escaped key.
 */
function escape(key) {
  var escapeRegex = /[=:]/g;
  var escaperLookup = {
    '=': '=0',
    ':': '=2'
  };
  var escapedString = ('' + key).replace(escapeRegex, function (match) {
    return escaperLookup[match];
  });

  return '$' + escapedString;
}

/**
 * TODO: Test that a single child and an array with one item have the same key
 * pattern.
 */

var didWarnAboutMaps = false;

var userProvidedKeyEscapeRegex = /\/+/g;
function escapeUserProvidedKey(text) {
  return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');
}

var POOL_SIZE = 10;
var traverseContextPool = [];
function getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {
  if (traverseContextPool.length) {
    var traverseContext = traverseContextPool.pop();
    traverseContext.result = mapResult;
    traverseContext.keyPrefix = keyPrefix;
    traverseContext.func = mapFunction;
    traverseContext.context = mapContext;
    traverseContext.count = 0;
    return traverseContext;
  } else {
    return {
      result: mapResult,
      keyPrefix: keyPrefix,
      func: mapFunction,
      context: mapContext,
      count: 0
    };
  }
}

function releaseTraverseContext(traverseContext) {
  traverseContext.result = null;
  traverseContext.keyPrefix = null;
  traverseContext.func = null;
  traverseContext.context = null;
  traverseContext.count = 0;
  if (traverseContextPool.length < POOL_SIZE) {
    traverseContextPool.push(traverseContext);
  }
}

/**
 * @param {?*} children Children tree container.
 * @param {!string} nameSoFar Name of the key path so far.
 * @param {!function} callback Callback to invoke with each child found.
 * @param {?*} traverseContext Used to pass information throughout the traversal
 * process.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {
  var type = typeof children;

  if (type === 'undefined' || type === 'boolean') {
    // All of the above are perceived as null.
    children = null;
  }

  var invokeCallback = false;

  if (children === null) {
    invokeCallback = true;
  } else {
    switch (type) {
      case 'string':
      case 'number':
        invokeCallback = true;
        break;
      case 'object':
        switch (children.$$typeof) {
          case REACT_ELEMENT_TYPE:
          case REACT_CALL_TYPE:
          case REACT_RETURN_TYPE:
          case REACT_PORTAL_TYPE:
            invokeCallback = true;
        }
    }
  }

  if (invokeCallback) {
    callback(traverseContext, children,
    // If it's the only child, treat the name as if it was wrapped in an array
    // so that it's consistent if the number of children grows.
    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);
    return 1;
  }

  var child;
  var nextName;
  var subtreeCount = 0; // Count of children found in the current subtree.
  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;

  if (Array.isArray(children)) {
    for (var i = 0; i < children.length; i++) {
      child = children[i];
      nextName = nextNamePrefix + getComponentKey(child, i);
      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
    }
  } else {
    var iteratorFn = getIteratorFn(children);
    if (typeof iteratorFn === 'function') {
      {
        // Warn about using Maps as children
        if (iteratorFn === children.entries) {
          warning(didWarnAboutMaps, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', ReactDebugCurrentFrame.getStackAddendum());
          didWarnAboutMaps = true;
        }
      }

      var iterator = iteratorFn.call(children);
      var step;
      var ii = 0;
      while (!(step = iterator.next()).done) {
        child = step.value;
        nextName = nextNamePrefix + getComponentKey(child, ii++);
        subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);
      }
    } else if (type === 'object') {
      var addendum = '';
      {
        addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();
      }
      var childrenString = '' + children;
      invariant(false, 'Objects are not valid as a React child (found: %s).%s', childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString, addendum);
    }
  }

  return subtreeCount;
}

/**
 * Traverses children that are typically specified as `props.children`, but
 * might also be specified through attributes:
 *
 * - `traverseAllChildren(this.props.children, ...)`
 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
 *
 * The `traverseContext` is an optional argument that is passed through the
 * entire traversal. It can be used to store accumulations or anything else that
 * the callback might find relevant.
 *
 * @param {?*} children Children tree object.
 * @param {!function} callback To invoke upon traversing each child.
 * @param {?*} traverseContext Context for traversal.
 * @return {!number} The number of children in this subtree.
 */
function traverseAllChildren(children, callback, traverseContext) {
  if (children == null) {
    return 0;
  }

  return traverseAllChildrenImpl(children, '', callback, traverseContext);
}

/**
 * Generate a key string that identifies a component within a set.
 *
 * @param {*} component A component that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */
function getComponentKey(component, index) {
  // Do some typechecking here since we call this blindly. We want to ensure
  // that we don't block potential future ES APIs.
  if (typeof component === 'object' && component !== null && component.key != null) {
    // Explicit key
    return escape(component.key);
  }
  // Implicit key determined by the index in the set
  return index.toString(36);
}

function forEachSingleChild(bookKeeping, child, name) {
  var func = bookKeeping.func,
      context = bookKeeping.context;

  func.call(context, child, bookKeeping.count++);
}

/**
 * Iterates through children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.foreach
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc
 * @param {*} forEachContext Context for forEachContext.
 */
function forEachChildren(children, forEachFunc, forEachContext) {
  if (children == null) {
    return children;
  }
  var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);
  traverseAllChildren(children, forEachSingleChild, traverseContext);
  releaseTraverseContext(traverseContext);
}

function mapSingleChildIntoContext(bookKeeping, child, childKey) {
  var result = bookKeeping.result,
      keyPrefix = bookKeeping.keyPrefix,
      func = bookKeeping.func,
      context = bookKeeping.context;


  var mappedChild = func.call(context, child, bookKeeping.count++);
  if (Array.isArray(mappedChild)) {
    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, emptyFunction.thatReturnsArgument);
  } else if (mappedChild != null) {
    if (isValidElement(mappedChild)) {
      mappedChild = cloneAndReplaceKey(mappedChild,
      // Keep both the (mapped) and old keys if they differ, just as
      // traverseAllChildren used to do for objects as children
      keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);
    }
    result.push(mappedChild);
  }
}

function mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {
  var escapedPrefix = '';
  if (prefix != null) {
    escapedPrefix = escapeUserProvidedKey(prefix) + '/';
  }
  var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);
  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
  releaseTraverseContext(traverseContext);
}

/**
 * Maps children that are typically specified as `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.map
 *
 * The provided mapFunction(child, key, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} func The map function.
 * @param {*} context Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */
function mapChildren(children, func, context) {
  if (children == null) {
    return children;
  }
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, func, context);
  return result;
}

/**
 * Count the number of children that are typically specified as
 * `props.children`.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.count
 *
 * @param {?*} children Children tree container.
 * @return {number} The number of children.
 */
function countChildren(children, context) {
  return traverseAllChildren(children, emptyFunction.thatReturnsNull, null);
}

/**
 * Flatten a children object (typically specified as `props.children`) and
 * return an array with appropriately re-keyed children.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.toarray
 */
function toArray(children) {
  var result = [];
  mapIntoWithKeyPrefixInternal(children, result, null, emptyFunction.thatReturnsArgument);
  return result;
}

/**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection.
 *
 * See https://reactjs.org/docs/react-api.html#react.children.only
 *
 * The current implementation of this function assumes that a single child gets
 * passed without a wrapper, but the purpose of this helper function is to
 * abstract away the particular structure of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactElement} The first and only `ReactElement` contained in the
 * structure.
 */
function onlyChild(children) {
  !isValidElement(children) ? invariant(false, 'React.Children.only expected to receive a single React element child.') : void 0;
  return children;
}

var describeComponentFrame = function (name, source, ownerName) {
  return '\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');
};

function getComponentName(fiber) {
  var type = fiber.type;

  if (typeof type === 'string') {
    return type;
  }
  if (typeof type === 'function') {
    return type.displayName || type.name;
  }
  return null;
}

/**
 * ReactElementValidator provides a wrapper around a element factory
 * which validates the props passed to the element. This is intended to be
 * used only in DEV and could be replaced by a static type checker for languages
 * that support it.
 */

{
  var currentlyValidatingElement = null;

  var propTypesMisspellWarningShown = false;

  var getDisplayName = function (element) {
    if (element == null) {
      return '#empty';
    } else if (typeof element === 'string' || typeof element === 'number') {
      return '#text';
    } else if (typeof element.type === 'string') {
      return element.type;
    } else if (element.type === REACT_FRAGMENT_TYPE) {
      return 'React.Fragment';
    } else {
      return element.type.displayName || element.type.name || 'Unknown';
    }
  };

  var getStackAddendum = function () {
    var stack = '';
    if (currentlyValidatingElement) {
      var name = getDisplayName(currentlyValidatingElement);
      var owner = currentlyValidatingElement._owner;
      stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner));
    }
    stack += ReactDebugCurrentFrame.getStackAddendum() || '';
    return stack;
  };

  var VALID_FRAGMENT_PROPS = new Map([['children', true], ['key', true]]);
}

function getDeclarationErrorAddendum() {
  if (ReactCurrentOwner.current) {
    var name = getComponentName(ReactCurrentOwner.current);
    if (name) {
      return '\n\nCheck the render method of `' + name + '`.';
    }
  }
  return '';
}

function getSourceInfoErrorAddendum(elementProps) {
  if (elementProps !== null && elementProps !== undefined && elementProps.__source !== undefined) {
    var source = elementProps.__source;
    var fileName = source.fileName.replace(/^.*[\\\/]/, '');
    var lineNumber = source.lineNumber;
    return '\n\nCheck your code at ' + fileName + ':' + lineNumber + '.';
  }
  return '';
}

/**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */
var ownerHasKeyUseWarning = {};

function getCurrentComponentErrorInfo(parentType) {
  var info = getDeclarationErrorAddendum();

  if (!info) {
    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;
    if (parentName) {
      info = '\n\nCheck the top-level render call using <' + parentName + '>.';
    }
  }
  return info;
}

/**
 * Warn if the element doesn't have an explicit key assigned to it.
 * This element is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it. Error statuses are cached so a warning
 * will only be shown once.
 *
 * @internal
 * @param {ReactElement} element Element that requires a key.
 * @param {*} parentType element's parent's type.
 */
function validateExplicitKey(element, parentType) {
  if (!element._store || element._store.validated || element.key != null) {
    return;
  }
  element._store.validated = true;

  var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);
  if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
    return;
  }
  ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

  // Usually the current owner is the offender, but if it accepts children as a
  // property, it may be the creator of the child that's responsible for
  // assigning it a key.
  var childOwner = '';
  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {
    // Give the component that originally created this child.
    childOwner = ' It was passed a child from ' + getComponentName(element._owner) + '.';
  }

  currentlyValidatingElement = element;
  {
    warning(false, 'Each child in an array or iterator should have a unique "key" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.%s', currentComponentErrorInfo, childOwner, getStackAddendum());
  }
  currentlyValidatingElement = null;
}

/**
 * Ensure that every element either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {ReactNode} node Statically passed child of any type.
 * @param {*} parentType node's parent's type.
 */
function validateChildKeys(node, parentType) {
  if (typeof node !== 'object') {
    return;
  }
  if (Array.isArray(node)) {
    for (var i = 0; i < node.length; i++) {
      var child = node[i];
      if (isValidElement(child)) {
        validateExplicitKey(child, parentType);
      }
    }
  } else if (isValidElement(node)) {
    // This element was passed in a valid location.
    if (node._store) {
      node._store.validated = true;
    }
  } else if (node) {
    var iteratorFn = getIteratorFn(node);
    if (typeof iteratorFn === 'function') {
      // Entry iterators used to provide implicit keys,
      // but now we print a separate warning for them later.
      if (iteratorFn !== node.entries) {
        var iterator = iteratorFn.call(node);
        var step;
        while (!(step = iterator.next()).done) {
          if (isValidElement(step.value)) {
            validateExplicitKey(step.value, parentType);
          }
        }
      }
    }
  }
}

/**
 * Given an element, validate that its props follow the propTypes definition,
 * provided by the type.
 *
 * @param {ReactElement} element
 */
function validatePropTypes(element) {
  var componentClass = element.type;
  if (typeof componentClass !== 'function') {
    return;
  }
  var name = componentClass.displayName || componentClass.name;
  var propTypes = componentClass.propTypes;
  if (propTypes) {
    currentlyValidatingElement = element;
    checkPropTypes(propTypes, element.props, 'prop', name, getStackAddendum);
    currentlyValidatingElement = null;
  } else if (componentClass.PropTypes !== undefined && !propTypesMisspellWarningShown) {
    propTypesMisspellWarningShown = true;
    warning(false, 'Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');
  }
  if (typeof componentClass.getDefaultProps === 'function') {
    warning(componentClass.getDefaultProps.isReactClassApproved, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.');
  }
}

/**
 * Given a fragment, validate that it can only be provided with fragment props
 * @param {ReactElement} fragment
 */
function validateFragmentProps(fragment) {
  currentlyValidatingElement = fragment;

  var _iteratorNormalCompletion = true;
  var _didIteratorError = false;
  var _iteratorError = undefined;

  try {
    for (var _iterator = Object.keys(fragment.props)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
      var key = _step.value;

      if (!VALID_FRAGMENT_PROPS.has(key)) {
        warning(false, 'Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.%s', key, getStackAddendum());
        break;
      }
    }
  } catch (err) {
    _didIteratorError = true;
    _iteratorError = err;
  } finally {
    try {
      if (!_iteratorNormalCompletion && _iterator['return']) {
        _iterator['return']();
      }
    } finally {
      if (_didIteratorError) {
        throw _iteratorError;
      }
    }
  }

  if (fragment.ref !== null) {
    warning(false, 'Invalid attribute `ref` supplied to `React.Fragment`.%s', getStackAddendum());
  }

  currentlyValidatingElement = null;
}

function createElementWithValidation(type, props, children) {
  var validType = typeof type === 'string' || typeof type === 'function' || typeof type === 'symbol' || typeof type === 'number';
  // We warn in this case but don't throw. We expect the element creation to
  // succeed and there will likely be errors in render.
  if (!validType) {
    var info = '';
    if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
      info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
    }

    var sourceInfo = getSourceInfoErrorAddendum(props);
    if (sourceInfo) {
      info += sourceInfo;
    } else {
      info += getDeclarationErrorAddendum();
    }

    info += getStackAddendum() || '';

    warning(false, 'React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', type == null ? type : typeof type, info);
  }

  var element = createElement.apply(this, arguments);

  // The result can be nullish if a mock or a custom function is used.
  // TODO: Drop this when these are no longer allowed as the type argument.
  if (element == null) {
    return element;
  }

  // Skip key warning if the type isn't valid since our key validation logic
  // doesn't expect a non-string/function type and can throw confusing errors.
  // We don't want exception behavior to differ between dev and prod.
  // (Rendering will throw with a helpful message and as soon as the type is
  // fixed, the key warnings will appear.)
  if (validType) {
    for (var i = 2; i < arguments.length; i++) {
      validateChildKeys(arguments[i], type);
    }
  }

  if (typeof type === 'symbol' && type === REACT_FRAGMENT_TYPE) {
    validateFragmentProps(element);
  } else {
    validatePropTypes(element);
  }

  return element;
}

function createFactoryWithValidation(type) {
  var validatedFactory = createElementWithValidation.bind(null, type);
  // Legacy hook TODO: Warn if this is accessed
  validatedFactory.type = type;

  {
    Object.defineProperty(validatedFactory, 'type', {
      enumerable: false,
      get: function () {
        lowPriorityWarning$1(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');
        Object.defineProperty(this, 'type', {
          value: type
        });
        return type;
      }
    });
  }

  return validatedFactory;
}

function cloneElementWithValidation(element, props, children) {
  var newElement = cloneElement.apply(this, arguments);
  for (var i = 2; i < arguments.length; i++) {
    validateChildKeys(arguments[i], newElement.type);
  }
  validatePropTypes(newElement);
  return newElement;
}

var React = {
  Children: {
    map: mapChildren,
    forEach: forEachChildren,
    count: countChildren,
    toArray: toArray,
    only: onlyChild
  },

  Component: Component,
  PureComponent: PureComponent,
  unstable_AsyncComponent: AsyncComponent,

  Fragment: REACT_FRAGMENT_TYPE,

  createElement: createElementWithValidation,
  cloneElement: cloneElementWithValidation,
  createFactory: createFactoryWithValidation,
  isValidElement: isValidElement,

  version: ReactVersion,

  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
    ReactCurrentOwner: ReactCurrentOwner,
    // Used by renderers to avoid bundling object-assign twice in UMD bundles:
    assign: _assign
  }
};

{
  _assign(React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, {
    // These should not be included in production.
    ReactDebugCurrentFrame: ReactDebugCurrentFrame,
    // Shim for React DOM 16.0.0 which still destructured (but not used) this.
    // TODO: remove in React 17.0.
    ReactComponentTreeHook: {}
  });
}



var React$2 = Object.freeze({
	default: React
});

var React$3 = ( React$2 && React ) || React$2;

// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var react = React$3['default'] ? React$3['default'] : React$3;

module.exports = react;
  })();
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 19 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */



var ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';

module.exports = ReactPropTypesSecret;


/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {

function checkDCE() {
  /* global __REACT_DEVTOOLS_GLOBAL_HOOK__ */
  if (
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined' ||
    typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== 'function'
  ) {
    return;
  }
  if (process.env.NODE_ENV !== 'production') {
    // This branch is unreachable because this function is only called
    // in production, but the condition is true only in development.
    // Therefore if the branch is still here, dead code elimination wasn't
    // properly applied.
    // Don't change the message. React DevTools relies on it. Also make sure
    // this message doesn't occur elsewhere in this function, or it will cause
    // a false positive.
    throw new Error('^_^');
  }
  try {
    // Verify that the code above has been dead code eliminated (DCE'd).
    __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
  } catch (err) {
    // DevTools shouldn't crash React, no matter what.
    // We should still report in case we break this code.
    console.error(err);
  }
}

if (process.env.NODE_ENV === 'production') {
  // DCE check should happen before ReactDOM bundle executes so that
  // DevTools can report bad minification during injection.
  checkDCE();
  module.exports = __webpack_require__(21);
} else {
  module.exports = __webpack_require__(24);
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 21 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/** @license React v16.2.0
 * react-dom.production.min.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

/*
 Modernizr 3.0.0pre (Custom Build) | MIT
*/


d<b;d++)c+="\x26args[]\x3d"+encodeURIComponent(arguments[d+1]);b=Error(c+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings.");b.name="Invariant Violation";b.framesToPop=1;throw b;}aa?void 0:E("227");


;1>=g.hasBooleanValue+g.hasNumericValue+g.hasOverloadedBooleanValue?void 0:E("50",f);e.hasOwnProperty(f)&&(g.attributeName=e[f]);d.hasOwnProperty(f)&&(g.attributeNamespace=d[f]);a.hasOwnProperty(f)&&(g.mutationMethod=a[f]);ua[f]=g}}},ua={};


var P={_caughtError:null,_hasCaughtError:!1,_rethrowError:null,_hasRethrowError:!1,injection:{injectErrorUtils:function(a){"function"!==typeof a.invokeGuardedCallback?E("197"):void 0;Ja=a.invokeGuardedCallback}},invokeGuardedCallback:function(a,b,c,d,e,f,g,h,k){Ja.apply(P,arguments)},invokeGuardedCallbackAndCatchFirstError:function(a,b,c,d,e,f,g,h,k){P.invokeGuardedCallback.apply(this,arguments);if(P.hasCaughtError()){var q=P.clearCaughtError();P._hasRethrowError||(P._hasRethrowError=!0,P._rethrowError=


P._caughtError=null;P._hasCaughtError=!1;return a}E("198")}};function Ja(a,b,c,d,e,f,g,h,k){P._hasCaughtError=!1;P._caughtError=null;var q=Array.prototype.slice.call(arguments,3);try{b.apply(c,q)}catch(v){P._caughtError=v,P._hasCaughtError=!0}}
function Ka(){if(P._hasRethrowError){var a=P._rethrowError;P._rethrowError=null;P._hasRethrowError=!1;throw a;}}var La=null,Ma={};
function Na(){if(La)for(var a in Ma){var b=Ma[a],c=La.indexOf(a);-1<c?void 0:E("96",a);if(!Oa[c]){b.extractEvents?void 0:E("97",a);Oa[c]=b;c=b.eventTypes;for(var d in c){var e=void 0;var f=c[d],g=b,h=d;Pa.hasOwnProperty(h)?E("99",h):void 0;Pa[h]=f;var k=f.phasedRegistrationNames;if(k){for(e in k)k.hasOwnProperty(e)&&Qa(k[e],g,h);e=!0}else f.registrationName?(Qa(f.registrationName,g,h),e=!0):e=!1;e?void 0:E("98",d,a)}}}}
function Qa(a,b,c){Ra[a]?E("100",a):void 0;Ra[a]=b;Sa[a]=b.eventTypes[c].dependencies}var Oa=[],Pa={},Ra={},Sa={};function Ta(a){La?E("101"):void 0;La=Array.prototype.slice.call(a);Na()}function Ua(a){var b=!1,c;for(c in a)if(a.hasOwnProperty(c)){var d=a[c];Ma.hasOwnProperty(c)&&Ma[c]===d||(Ma[c]?E("102",c):void 0,Ma[c]=d,b=!0)}b&&Na()}


function $a(a,b){

return c}function jb(a,b,c,d){for(var e,f=0;f<Oa.length;f++){var g=Oa[f];g&&(g=g.extractEvents(a,b,c,d))&&(e=$a(e,g))}return e}function kb(a){a&&(bb=$a(bb,a))}function lb(a){var b=bb;bb=null;b&&(a?ab(b,db):ab(b,gb),bb?E("95"):void 0,P.rethrowCaughtError())}

)if(b.push(a),a.parentNode)a=a.parentNode;else return null;var c=void 0,d=a[Q];if(5===d.tag||6===d.tag)return d;for(;a&&(d=a[Q]);a=b.pop())c=d;return c}function qb(a){if(5===a.tag||6===a.tag)return a.stateNode;E("33")}function rb(a){return a[ob]||null}


return Db}var S={_root:null,_startText:null,_fallbackText:null};
function Fb(){if(S._fallbackText)return S._fallbackText;var a,b=S._startText,c=b.length,d,e=Gb(),f=e.length;for(a=0;a<c&&b[a]===e[a];a++);var g=c-a;for(d=1;d<=g&&b[c-d]===e[f-d];d++);S._fallbackText=e.slice(a,1<d?1-d:void 0);return S._fallbackText}function Gb(){return"value"in S._root?S._root.value:S._root[Eb()]}


});T.Interface=Ib;T.augmentClass=function(a,b){function c(){}c.prototype=this.prototype;var d=new c;B(d,a.prototype);a.prototype=d;a.prototype.constructor=a;a.Interface=B({},this.Interface,b);a.augmentClass=this.augmentClass;Jb(a)};Jb(T);function Kb(a,b,c,d){

g.target=f;g.relatedTarget=e;c=fd.getPooled(gd.mouseEnter,b,c,d);c.type="mouseenter";c.target=e;c.relatedTarget=f;Bb(g,c,a,b);return[g,c]}},id=aa.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner;function jd(a){a=a.type;return"string"===typeof a?a:"function"===typeof a?a.displayName||a.name:null}
function kd(a){var b=a;if(a.alternate)for(;b["return"];)b=b["return"];else{if(0!==(b.effectTag&2))return 1;for(;b["return"];)if(b=b["return"],0!==(b.effectTag&2))return 1}return 3===b.tag?2:3}function ld(a){return(a=a._reactInternalFiber)?2===kd(a):!1}function md(a){2!==kd(a)?E("188"):void 0}
function nd(a){var b=a.alternate;if(!b)return b=kd(a),3===b?E("188"):void 0,1===b?null:a;for(var c=a,d=b;;){var e=c["return"],f=e?e.alternate:null;if(!e||!f)break;if(e.child===f.child){for(var g=e.child;g;){if(g===c)return md(e),a;if(g===d)return md(e),b;g=g.sibling}E("188")}if(c["return"]!==d["return"])c=e,d=f;else{g=!1;for(var h=e.child;h;){if(h===c){g=!0;c=e;d=f;break}if(h===d){g=!0;

if(b.child)b.child["return"]=b,b=b.child;else{if(b===a)break;for(;!b.sibling;){if(!b["return"]||b["return"]===a)return null;b=b["return"]}b.sibling["return"]=b["return"];b=b.sibling}}return null}
function pd(a){a=nd(a);if(!a)return null;for(var b=a;;){if(5===b.tag||6===b.tag)return b;if(b.child&&4!==b.tag)b.child["return"]=b,b=b.child;

e=Hd(e);f=Sa.onSelect;for(var g=0;g<f.length;g++){var h=f[g];if(!e.hasOwnProperty(h)||!e[h]){e=!1;break a}}

clipboardData:function(a){return"clipboardData"in a?a.clipboardData:window.clipboardData}});function Vd(a,b,c,d){return T.call(this,a,b,c,d)}bd.augmentClass(Vd,{relatedTarget:null});
function Wd(a){var b=a.keyCode;"charCode"in a?(a=a.charCode,0===a&&13===b&&(a=13)):a=b;return 32<=a||13===a?a:0}


b=a.getPooled(e,b,c,d);Ab(b);return b}};sd=function(a,b,c,d){a=jb(a,b,c,d);kb(a);lb(!1)};hb.injectEventPluginOrder("ResponderEventPlugin SimpleEventPlugin TapEventPlugin EnterLeaveEventPlugin ChangeEventPlugin SelectEventPlugin BeforeInputEventPlugin".split(" "));
Wa=sb.getFiberCurrentPropsFromNode;Xa=sb.getInstanceFromNode;Ya=sb.getNodeFromInstance;hb.injectEventPluginsByName({

var c=a.stateNode;c?void 0:E("169");if(b){var d=pe(a,je);c.__reactInternalMemoizedMergedChildContext=d;V(X,a);V(ie,a);W(ie,d,a)}else V(X,a);W(X,b,a)}
function Y(a,b,c){this.tag=a;this.key=b;this.stateNode=this.type=null;this.sibling=this.child=this["return"]=null;this.index=0;this.memoizedState=this.updateQueue=this.memoizedProps=this.pendingProps=this.ref=null;this.internalContextTag=c;this.effectTag=0;this.lastEffect=this.firstEffect=this.nextEffect=null;this.expirationTime=0;this.alternate=null}
function se(a,b,c){var d=a.alternate;null===d?(d=new Y(a.tag,a.key,a.internalContextTag),d.type=a.type,d.stateNode=a.stateNode,d.alternate=a,a.alternate=d):(d.effectTag=0,d.nextEffect=null,d.firstEffect=null,d.lastEffect=null);d.expirationTime=c;d.pendingProps=b;d.child=a.child;d.memoizedProps=a.memoizedProps;d.memoizedState=a.memoizedState;d.updateQueue=a.updateQueue;d.sibling=a.sibling;d.index=a.index;d.ref=a.ref;return d}
function te(a,b,c){var d=void 0,e=a.type,f=a.key;"function

function ve(a,b,c){b=new Y(6,null,b);b.pendingProps=a;b.expirationTime=c;return b}function we(a,b,c){b=new Y(7,a.key,b);b.type=a.handler;b.pendingProps=a;b.expirationTime=c;return b}function xe(a,b,c){a=new Y(9,null,b);a.expirationTime=c;return a}function ye(a,b,c){b=new Y(4,a.key,b);b.pendingProps=a.children||[];b.expirationTime=c;b.stateNode={containerInfo:a.containerInfo,pendingChildren:null,implementation:a.implementation};return b}var ze=null,Ae=null;


function He(a,b){var c=a.alternate,d=a.updateQueue;null===d&&(d=a.updateQueue=Fe(null));null!==c?(a=c.updateQueue,null===a&&(a=c.updateQueue=Fe(null))):a=null;a=a!==d?a:null;null===a?Ge(d,b):null===d.last||null===a.last?(Ge(d,b),Ge(a,b)):(Ge(d,b),a.last=b)}function Ie(a,b,c,d){a=a.partialState;return"function"===typeof a?a.call(b,c,d):a}


c.first&&(c.last=null));if(h.isReplace)a=Ie(h,d,a,e),g=!0;else if(q=Ie(h,d,a,e))a=g?B({},a,q):B(a,q),g=!1;h.isForced&&(c.hasForceUpdate=!0);null!==h.callback&&(q=c.callbackList,null===q&&(q=c.callbackList=[]),q.push(h))}

function Le(a,b,c,d){function e(a,b){b.updater=f;a.stateNode=b;b._reactInternalFiber=a}var f={isMounted:ld,enqueueSetState:function(c,d,e){c=c._reactInternalFiber;e=void 0===e?null:e;var g=b(c);He(c,{expirationTime:g,partialState:d,callback:e,isReplace:!1,isForced:!1,nextCallback:null,next:null});a(c,g)},enqueueReplaceState:function(c,d,e){c=c._reactInternalFiber;e=void 0===e?null:e;var g=b(c);He(c,{expirationTime:g,partialState:d,callback:e,isReplace:!0,isForced:!1,nextCallback:null,next:null});
a(c,g)},enqueueForceUpdate:function(c,d){c=c._reactInternalFiber;

b=new c(b,g);e(a,b);f&&(a=a.stateNode,a.__reactInternalMemoizedUnmaskedChildContext=d,a.__reactInternalMemoizedMaskedChildContext=g);return b},mountClassInstance:function(a,
b){var c=a.alternate,d=a.stateNode,e=d.state||null,g=a.pendingProps;g?void 0:E("158");var h=ke(a);d.props=g;d.state=a.memoizedState=e;d.refs=D;d.context=me(a,h);null!=a.type&&null!=a.type.prototype&&!0===a.type.prototype.unstable_isAsyncReactComponent&&(a.internalContextTag|=1);"function"===typeof d.componentWillMount&&(e=d.state,d.componentWillMount(),e!==d.state&&f.enqueueReplaceState(d,d.state,null),e=a.updateQueue,null!==e&&(d.state=Je(c,a,e,d,g,b)));"function"===typeof d.componentDidMount&&(a.effectTag|=
4)},updateClassInstance:function(a,b,e){var g=b.stateNode;g.props=b.memoizedProps;g.state=b.memoizedState;var h=b.memoizedProps,k=b.pendingProps;k||(k=h,null==k?E("159"):void 0);var u=g.context,z=ke(b);z=me(b,z);"function"!==typeof g.componentWillReceiveProps||h===k&&u===z||(u=g.state,g.componentWillReceiveProps(k,z),g.state!==u&&f.enqueueReplaceState(g,g.state,null));u=b.memoizedState;e=null!==b.updateQueue?Je(a,b,b.updateQueue,g,k,e):u;if(!(h!==k||u!==e||X.current||null!==b.updateQueue&&b.updateQueue.hasForceUpdate))return"function"!==


g.props=k;g.state=e;g.context=z;return G}}}var Qe="function"===typeof Symbol&&Symbol["for"],Re=Qe?Symbol["for"]("react.element"):60103,Se=Qe?Symbol["for"]("react.call"):60104,Te=Qe?Symbol["for"]("react.return"):60105,Ue=Qe?Symbol["for"]("react.portal"):60106,Ve=Qe?Symbol["for"]("react.fragment"):60107,We="function"===typeof Symbol&&Symbol.iterator;
function Xe(a){if(null===a||"undefined"===typeof a)return null;a=We&&a[We]||a["@@iterator"];return"function"===typeof a?a:null}var Ye=Array.isArray;
function Ze(a,b){

b=e(b,c,d);b["return"]=a;return b}function k(a,b,c,d){if(null!==b&&b.type===c.type)return d=e(b,c.props,d),d.ref=Ze(b,c),d["return"]=a,d;d=te(c,a.internalContextTag,d);d.ref=Ze(b,c);d["return"]=a;return d}function q(a,b,c,d){if(null===b||7!==b.tag)return b=we(c,a.internalContextTag,d),b["return"]=a,b;b=e(b,c,d);
b["return"]=a;return b}function v(a,b,c,d){if(null===b||9!==b.tag)return b=xe(c,a.internalContextTag,d),b.type=c.value,b["return"]=a,b;b=e(b,null,d);b.type=c.value;b["return"]=a;return b}function y(a,b,c,d){if(null===b||4!==b.tag||b.stateNode.containerInfo!==c.containerInfo||b.stateNode.implementation!==c.implementation)return b=ye(c,a.internalContextTag,d),b["return"]=a,b;b=e(b,c.children||[],d);b["return"]=a;return b}function u(a,b,c,d,f){if(null===b||10!==b.tag)return b=ue(c,a.internalContextTag,
d,f),b["return"]=a,b;b=e(b,c,d);b["return"]=a;return b}function z(a,b,c){if("string"===typeof b||"number"===typeof b)return b=ve(""+b,a.internalContextTag,c),b["return"]=a,b;if("object"===typeof b&&null!==b){switch(b.$$typeof){case Re:if(b.type===Ve)return b=ue(b.props.children,a.internalContextTag,c,b.key),b["return"]=a,b;c=te(b,a.internalContextTag,c);c.ref=Ze(null,b);c["return"]=a;return c;case Se:return b=we(b,a.internalContextTag,c),b["return"]=a,b;case Te:return c=xe(b,a.internalContextTag,
c),c.type=b.value,c["return"]=a,c;case Ue:return b=ye(b,a.internalContextTag,c),b["return"]=a,b}

d(e,n);w<m.length;w++)if(k=I(n,e,w,m[w],A)){if(a&&null!==k.alternate)n["delete"](null===k.key?w:k.key);g=f(k,g,w);null===r?h=k:r.sibling=k;r=k}a&&n.forEach(function(a){

if(a&&null!==x.alternate)n["delete"](null===x.key?w:x.key);g=f(x,g,w);null===r?h=x:r.sibling=x;r=x}a&&n.forEach(function(a){

return g(a);case Se:a:{for(m=f.key;null!==d;){if(d.key===
m)if(7===d.tag){c(a,d.sibling);d=e(d,f,h);d["return"]=a;a=d;break a}else{c(a,d);break}else b(a,d);d=d.sibling}d=we(f,a.internalContextTag,h);d["return"]=a;a=d}return g(a);case Te:a:{if(null!==d)if(9===d.tag){c(a,d.sibling);d=e(d,null,h);d.type=f.value;d["return"]=a;a=d;break a}else c(a,d);d=xe(f,a.internalContextTag,h);d.type=f.value;d["return"]=a;a=d}

d["return"]=a;a=d}return g(a)}if("string"===typeof f||"number"===typeof f)return f=""+f,null!==d&&6===d.tag?(c(a,d.sibling),d=e(d,f,h)):(c(a,d),d=ve(f,a.internalContextTag,h)),d["return"]=a,a=d,g(a);if(Ye(f))return L(a,d,f,h);if(Xe(f))return N(a,d,f,h);m&&$e(a,f);if("undefined"===typeof f)switch(a.tag){case 2:case 1:h=a.type,E("152",h.displayName||


g(a,b);if(!c)return d&&re(b,!1),q(a,b);c=b.stateNode;id.current=b;var e=c.render();b.effectTag|=1;f(a,b,e);b.memoizedState=c.state;b.memoizedProps=c.props;d&&re(b,!0);

1;"object"===typeof d&&null!==d&&"function"===typeof d.render?(b.tag=2,e=qe(b),w(b,d),A(b,c),b=h(a,b,!0,e)):(b.tag=1,f(a,b,d),b.memoizedProps=e,b=b.child);return b;case 1:a:{e=b.type;c=b.pendingProps;d=b.memoizedProps;if(X.current)null===c&&(c=d);else if(null===c||d===c){b=q(a,b);break a}d=ke(b);d=me(b,d);e=e(c,d);b.effectTag|=1;f(a,b,e);b.memoizedProps=c;b=b.child}

null===a&&J(b);e=b.type;var n=b.memoizedProps;d=b.pendingProps;null===d&&(d=n,null===d?E("154"):void 0);r=null!==a?a.memoizedProps:null;X.current||null!==d&&n!==d?(n=d.children,y(e,d)?n=null:r&&y(e,r)&&(b.effectTag|=16),g(a,b),


if(X.current)null===e&&(e=a&&a.memoizedProps,null===e?E("154"):void 0);else if(null===e||b.memoizedProps===e)e=b.memoizedProps;d=e.children;b.stateNode=null===a?cf(b,b.stateNode,d,c):bf(b,b.stateNode,d,c);b.memoizedProps=e;return b.stateNode;
case 9:return null;case 4:a:{I(b,b.stateNode.containerInfo);e=b.pendingProps;if(X.current)null===e&&(e=a&&a.memoizedProps,null==e?E("154"):void 0);else if(null===e||b.memoizedProps===e){b=q(a,b);break a}null===a?b.child=bf(b,null,e,c):f(a,b,e);b.memoizedProps=e;b=b.child}return b;case 10:a:{c=b.pendingProps;if(X.current)null===c&&(c=b.memoizedProps);else if(null===c||b.memoizedProps===c){b=q(a,b);break a}f(a,b,c);b.memoizedProps=c;b=b.child}return b;default:E("156")}},beginFailedWork:function(a,b,
c){switch(b.tag){case 2:qe(b);break;case 3:k(b);break;default:E("157")}b.effectTag|=64;null===a?b.child=null:b.child!==a.child&&(b.child=a.child);if(0===b.expirationTime||b.expirationTime>c)return v(a,b);b.firstEffect=null;b.lastEffect=null;b.child=null===a?cf(b,null,null,c):bf(b,a.child,null,c);

return{completeWork:function(a,b,c){var m=b.pendingProps;if(null===m)m=b.memoizedProps;else if(2147483647!==b.expirationTime||2147483647===c)b.pendingProps=null;

J(a,b,q,A,p,m,c);a.ref!==b.ref&&(b.effectTag|=128)}else{if(!m)return null===b.stateNode?E("166"):void 0,null;a=u();if(L(b))G(b,c,a)&&d(b);

(b.effectTag|=128)}return null;case 6:if(a&&null!=b.stateNode)w(a,b,a.memoizedProps,m);else{if("string"!==typeof m)return null===b.stateNode?E("166"):void 0,null;a=v();c=u();L(b)?I(b)&&d(b):b.stateNode=f(m,a,c,b)}return null;case 7:(m=b.memoizedProps)?void 0:E("165");b.tag=8;A=[];a:for((p=b.stateNode)&&(p["return"]=b);null!==p;){if(5===p.tag||6===p.tag||4===p.tag)E("247");else if(9===p.tag)A.push(p.type);else if(null!==p.child){p.child["return"]=p;p=p.child;continue}for(;null===p.sibling;){if(null===
p["return"]||p["return"]===b)break a;p=p["return"]}p.sibling["return"]=p["return"];p=p.sibling}p=m.handler;m=p(m.props,A);b.child=bf(b,null!==a?a.child:null,m,c);return b.child;case 8:return b.tag=7,null;case 9:return null;case 10:return null;case 4:return z(b),N(b),null;case 0:E("167");default:E("156")}}}}
function ff(a,b){function c(a){var c=a.ref;if(null!==c)try{c(null)}catch(A){b(a,A)}}function d(a){"function"===typeof Ee&&Ee(a);switch(a.tag){

b=b["return"];4===b.tag&&(c=!1)}b.sibling["return"]=b["return"];b=b.sibling}}var h=a.getPublicInstance,k=a.mutation;a=a.persistence;k||(a?E("235"):E("236"));var q=k.

},commitDeletion:function(a){g(a);a["return"]=null;a.child=null;a.alternate&&(a.alternate.child=null,a.alternate["return"]=null)},commitWork:function(a,b){switch(b.tag){case 2:break;case 5:var c=b.stateNode;if(null!=c){var d=b.memoizedProps;a=null!==a?a.memoizedProps:d;var e=b.type,f=b.updateQueue;b.updateQueue=null;null!==f&&v(c,f,e,a,d,b)}break;case 6:null===b.stateNode?E("162"):void 0;c=b.memoizedProps;u(b.stateNode,null!==a?a.memoizedProps:


c.props=b.memoizedProps;c.state=b.memoizedState;c.componentDidUpdate(d,a)}b=b.updateQueue;null!==b&&Ke(b,c);break;case 3:c=b.updateQueue;null!==c&&Ke(c,null!==b.child?b.child.stateNode:null);break;case 5:c=b.stateNode;null===a&&b.effectTag&4&&q(c,
b.type,b.memoizedProps,b);break;case 6:break;case 4:break;default:E("163")}},commitAttachRef:function(a){var b=a.ref;if(null!==b){var c=a.stateNode;switch(a.tag){case 5:b(h(c));break;default:b(c)}}},commitDetachRef:function(a){a=a.ref;null!==a&&a(null)}}}var gf={};
function hf(a){function b(a){a===gf?E("174"):void 0;return a}var c=a.getChildHostContext,d=a.getRootHostContext,e={

d=c(h,a.type,d);h!==d&&(W(f,a,a),W(e,d,a))},resetHostContainer:function(){e.current=gf;g.current=gf}}}


case 6:return b=g(b,a.pendingProps),null!==b?(a.stateNode=b,!0):!1;default:return!1}}function d(a){for(a=a["return"];null!==a&&5!==a.tag&&3!==a.tag;)a=a["return"];y=a}var e=a.shouldSetTextContent;
a=a.hydration;

k(a.stateNode.containerInfo);y=a;return z=!0},resetHydrationState:function(){u=y=null;z=!1},tryToClaimNextHydratableInstance:function(a){if(z){var d=u;if(d){if(!c(a,d)){d=h(d);if(!d||!c(a,d)){a.effectTag|=2;z=!1;y=a;return}b(y,u)}y=a;u=k(d)}else a.effectTag|=2,z=!1,y=a}},prepareToHydrateHostInstance:function(a,b,c){b=q(a.stateNode,a.type,a.memoizedProps,b,c,a);a.updateQueue=b;return null!==b?!0:!1}

function kf(a){function b(a){Qb=ja=!0;var b=a.stateNode;b.current===a?E("177"):void 0;b.isReadyForCommit=!1;id.current=null;if(1<a.effectTag)if(null!==a.lastEffect){a.lastEffect.nextEffect=a;var c=a.firstEffect}else c=a;else c=a.firstEffect;yg();for(t=c;null!==t;){var d=!1,e=void 0;try{for(;null!==t;){var f=t.effectTag;f&16&&zg(t);if(f&128){var g=t.alternate;null!==g&&Ag(g)}switch(f&-242){case 2:Ne(t);t.effectTag&=-3;break;case 6:Ne(t);t.effectTag&=-3;Oe(t.alternate,t);break;case 4:Oe(t.alternate,


k&36&&Dg(t.alternate,t);k&128&&Eg(t);if(k&64)switch(e=t,f=void 0,null!==R&&(f=R.get(e),R["delete"](e),null==f&&null!==e.alternate&&(e=e.alternate,f=R.get(e),R["delete"](e))),null==f?E("184"):void 0,e.tag){case 2:e.stateNode.componentDidCatch(f.error,{componentStack:f.componentStack});
break;case 3:null===ca&&(ca=f.error);break;default:E("157")}var Qc=t.nextEffect;t.nextEffect=null;t=Qc}}catch(Tc){c=!0,d=Tc}c&&(null===t?E("178"):void 0,h(t,d),null!==t&&(t=t.nextEffect))}ja=Qb=!1;"function"===typeof De&&De(a.stateNode);ha&&(ha.forEach(G),ha=null);null!==ca&&(a=ca,ca=null,Ob(a));b=b.current.expirationTime;0===b&&(qa=R=null);return b}function c(a){for(;;){var b=Fg(a.alternate,a,H),c=a["return"],d=a.sibling;var e=a;if(2147483647===H||2147483647!==e.expirationTime){if(2!==e.tag&&3!==


!1;if(a!==ra||b!==H||null===F){for(;-1<he;)ge[he]=null,he--;je=D;ie.current=D;X.current=!1;x();ra=a;H=b;F=se(ra.current,null,b)}var c=!1,d=null;try{f(b)}catch(Rc){c=!0,d=Rc}for(;c;){if(eb){ca=d;break}var g=F;if(null===g)eb=!0;else{var k=h(g,d);null===k?E("183"):void 0;if(!eb){try{c=k;d=b;for(k=c;null!==g;){switch(g.tag){case 2:ne(g);break;case 5:qg(g);break;case 3:p(g);break;case 4:p(g)}if(g===k||g.alternate===k)break;g=g["return"]}F=e(c);f(d)}catch(Rc){c=!0;d=Rc;continue}break}}}b=ca;eb=ja=!1;ca=
null;null!==b&&Ob(b);return a.isReadyForCommit?a.current.alternate:null}function h(a,b){var c=id.current=null,d=!1,e=!1,f=null;if(3===a.tag)c=a,q(a)&&(eb=!0);else for(var g=a["return"];null!==g&&null===c;){2===g.tag?"function"===typeof g.stateNode.componentDidCatch&&(d=!0,f=jd(g),c=g,e=!0):3===g.tag&&(c=g);if(q(g)){if(Sc||null!==ha&&(ha.has(g)||null!==g.alternate&&ha.has(g.alternate)))return null;c=null;e=!1}g=g["return"]}if(null!==c){null===qa&&(qa=new Set);qa.add(c);var h="";g=a;do{a:switch(g.tag){case 0:case 1:case 2:case 5:var k=
g._debugOwner,Qc=g._debugSource;var m=jd(g);var n=null;k&&(n=jd(k));k=Qc;m="\n    in "+(m||"Unknown")+(k?" (at "+k.fileName.replace(/^.*[\\\/]/,"")+":"+k.lineNumber+")":n?" (created by "+n+")":"");break a;default:m=""}h+=m;g=g["return"]}while(g);g=h;a=jd(a);null===R&&(R=new Map);b={componentName:a,componentStack:g,error:b,errorBoundary:d?c.stateNode:null,errorBoundaryFound:d,errorBoundaryName:f,willRetry:e}

Qb?(null===ha&&(ha=new Set),ha.add(c)):G(c);return c}null===ca&&(ca=b);return null}function k(a){return null!==R&&(R.has(a)||null!==a.alternate&&R.has(a.alternate))}function q(a){return null!==qa&&(qa.has(a)||null!==a.alternate&&qa.has(a.alternate))}function v(){return 20*(((I()+100)/20|0)+1)}function y(a){return 0!==ka?ka:ja?Qb?1:H:!Hg||a.internalContextTag&1?v():1}function u(a,b){return z(a,b,!1)}function z(a,b){for(;null!==a;){if(0===a.expirationTime||
a.expirationTime>b)a.expirationTime=b;null!==a.alternate&&(0===a.alternate.expirationTime||a.alternate.expirationTime>b)&&(a.alternate.expirationTime=b);if(null===a["return"])if(3===a.tag){var c=a.stateNode;!ja&&c===ra&&b<H&&(F=ra=null,H=0);var d=c,e=b;Rb>Ig&&E("185");if(null===d.nextScheduledRoot)d.remainingExpirationTime=e,null===O?(sa=O=d,d.nextScheduledRoot=d):(O=O.nextScheduledRoot=d,O.nextScheduledRoot=sa);else{var f=d.remainingExpirationTime;if(0===f||e<f)d.remainingExpirationTime=e}Fa||(la?
Sb&&(ma=d,na=1,m(ma,na)):1===e?w(1,null):L(e));!ja&&c===ra&&b<H&&(F=ra=null,H=0)}else break;a=a["return"]}}function G(a){z(a,1,!0)}function I(){return Uc=((Wc()-Pe)/10|0)+2}function L(a){if(0!==Tb){if(a>Tb)return;Jg(Xc)}var b=Wc()-Pe;Tb=a;Xc=Kg(J,{

if(0===a||e<a)a=e,b=d;if(d===O)break;c=d;d=d.nextScheduledRoot}}c=ma;null!==c&&c===b?Rb++:Rb=0;ma=b;na=a}function J(a){w(0,a)}function w(a,b){fb=b;for(N();null!==ma&&0!==na&&(0===a||na<=a)&&!Yc;)m(ma,na),N();null!==fb&&(Tb=0,Xc=-1);0!==na&&L(na);fb=null;Yc=!1;Rb=0;if(Ub)throw a=Zc,Zc=
null,Ub=!1,a;}function m(a,c){Fa?E("245"):void 0;Fa=!0;if(c<=I()){

Fa=!1}function A(){return null===fb||fb.timeRemaining()>Lg?!1:Yc=!0}function Ob(a){null===ma?E("246"):
void 0;ma.remainingExpirationTime=0;Ub||(Ub=!0,Zc=a)}var

var h;b:{2===kd(c)&&2===c.tag?void 0:E("170");for(h=c;3!==h.tag;){if(le(h)){h=h.stateNode.__reactInternalMemoizedMergedChildContext;break b}(h=h["return"])?void 0:E("171")}h=h.stateNode.context}c=le(c)?pe(c,h):h}else c=D;null===b.context?b.context=c:b.pendingContext=c;b=q;b=void 0===b?null:b;q=null!=a&&null!=a.type&&null!=a.type.prototype&&!0===a.type.prototype.unstable_isAsyncReactComponent?d():e(g);

a)Bf.didTimeout=!0;else{xf||(xf=!0,requestAnimationFrame(Df));return}else Bf.didTimeout=!1;wf=-1;a=uf;uf=null;null!==a&&a(Bf)}},!1);var Df=function(a){xf=!1;var b=a-yf+Af;b<Af&&zf<Af?(8>b&&(b=8),Af=b<zf?zf:b):zf=b;yf=a+Af;vf||(vf=!0,window.postMessage(Cf,"*"))};sf=function(a,b){uf=a;null!=b&&"number"===typeof b.timeout&&(wf=rf()+b.timeout);xf||(xf=!0,requestAnimationFrame(Df));return 0};tf=function(){uf=null;vf=!1;wf=-1}}else sf=window.requestIdleCallback,tf=window.cancelIdleCallback;else sf=function(a){return setTimeout(function(){a({timeRemaining:function(){return Infinity}})})},
tf=function(a){clearTimeout(a)};var Ef=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\

function Of(a,b){Nf(a,b);var c=b.value;if(null!=c)if(0===c&&""===a.value)a.value="0";else if("number"===b.type){if(b=parseFloat(a.value)||0,c!=b||c==b&&a.value!=c)a.value=""+c}

a.value=a.defaultValue;break;default:a.value=a.value}b=a.name;""!==b&&(a.name="");a.defaultChecked=!a.defaultChecked;a.defaultChecked=!a.defaultChecked;""!==b&&(a.name=b)}function Qf(a){var b="";aa.Children.forEach(a,function(a){null==a||"string"!==typeof a&&"number"!==typeof a||(b+=a)});return b}


c=9===c.nodeType?c:c.ownerDocument;d===jg&&(d=Zf(a));d===jg?"script"===a?(a=c.createElement("div"),a.innerHTML="\x3cscript\x3e\x3c/script\x3e",a=a.removeChild(a.firstChild)):a="string"===typeof b.is?c.createElement(a,{

f=c;break;case "img":case "image":U("topError","error",a);U("topLoad","load",a);f=c;break;case "form":U("topReset","reset",a);U("topSubmit","submit",a);f=c;break;case "details":U("topToggle","toggle",a);f=c;break;case "input":Mf(a,c);f=Lf(a,c);U("topInvalid","invalid",a);
lg(d,"onChange");break;case "option":f=Rf(a,c);break;case "select":Tf(a,c);f=B({},c,{value:void 0});U("topInvalid","invalid",a);lg(d,"onChange");break;case "textarea":Vf(a,c);f=Uf(a,c);U("topInvalid","invalid",a);lg(d,"onChange");break;default:f=c}hg(b,f,kg);var g=f,h;for(h in g)if(g.hasOwnProperty(h)){var k=g[h];"style"===h?fg

break;case "form":U("topReset","reset",a);U("topSubmit","submit",a);break;case "details":U("topToggle","toggle",a);break;case "input":Mf(a,c);U("topInvalid","invalid",a);lg(e,"onChange");break;case "select":Tf(a,c);
U("topInvalid","invalid",a);lg(e,"onChange");

a=a?9===a.nodeType?a.documentElement:a.firstChild:null;return!(!a||1!==a.nodeType||!a.hasAttribute("data-reactroot"))}


b.removeAllRanges();a>d?(b.addRange(g),b.extend(f.node,f.offset)):(g.setEnd(f.node,f.offset),b.addRange(g))}}b=[];for(a=c;a=a.parentNode;)1===a.nodeType&&b.push({element:a,left:a.scrollLeft,top:a.scrollTop});ia(c);for(c=0;c<b.length;c++)a=b[c],a.element.scrollLeft=a.left,a.element.scrollTop=
a.top}Mg=null;ud(xg);xg=null},createInstance:function(a,b,c,d,e){a=ng(a,b,c,d);a[Q]=e;a[ob]=b;return a}

function Pg(a,b,c,d,e){Ng(c)?void 0:E("200");var f=c._reactRootContainer;if(f)Z.updateContainer(b,f,a,e);else{d=d||Og(c);if(!d)for(f=void 0;

function Rg(a,b){this._reactRootContainer=Z.createContainer(a,b)}Rg.prototype.render=function(a,b){Z.updateContainer(a,this._reactRootContainer,null,b)};Rg.prototype.unmount=function(a){Z.updateContainer(null,this._reactRootContainer,null,a)};


null==a||void 0===a._reactInternalFiber?E("38"):void 0;return Pg(a,b,c,!1,d)},unmountComponentAtNode:function(a){Ng(a)?void 0:
E("40");return a._reactRootContainer?(Z.unbatchedUpdates(function(){



/***/ }),
/* 22 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var isNode = __webpack_require__(23);

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM text node.
 */
function isTextNode(object) {
  return isNode(object) && object.nodeType == 3;
}

module.exports = isTextNode;

/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM node.
 */
function isNode(object) {
  var doc = object ? object.ownerDocument || object : document;
  var defaultView = doc.defaultView || window;
  return !!(object && (typeof defaultView.Node === 'function' ? object instanceof defaultView.Node : typeof object === 'object' && typeof object.nodeType === 'number' && typeof object.nodeName === 'string'));
}

module.exports = isNode;

/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/* WEBPACK VAR INJECTION */(function(process) {/** @license React v16.2.0
 * react-dom.development.js
 *
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */





if (process.env.NODE_ENV !== "production") {
  (function() {
'use strict';

var React = __webpack_require__(4);
var invariant = __webpack_require__(5);
var warning = __webpack_require__(6);
var ExecutionEnvironment = __webpack_require__(8);
var _assign = __webpack_require__(2);
var emptyFunction = __webpack_require__(1);
var EventListener = __webpack_require__(9);
var getActiveElement = __webpack_require__(10);
var shallowEqual = __webpack_require__(11);
var containsNode = __webpack_require__(12);
var focusNode = __webpack_require__(13);
var emptyObject = __webpack_require__(3);
var checkPropTypes = __webpack_require__(7);
var hyphenateStyleName = __webpack_require__(25);
var camelizeStyleName = __webpack_require__(27);

/**
 * WARNING: DO NOT manually require this module.
 * This is a replacement for `invariant(...)` used by the error code system
 * and will _only_ be required by the corresponding babel pass.
 * It always throws.
 */

!React ? invariant(false, 'ReactDOM was loaded before React. Make sure you load the React package before loading ReactDOM.') : void 0;

// These attributes should be all lowercase to allow for
// case insensitive checks
var RESERVED_PROPS = {
  children: true,
  dangerouslySetInnerHTML: true,
  defaultValue: true,
  defaultChecked: true,
  innerHTML: true,
  suppressContentEditableWarning: true,
  suppressHydrationWarning: true,
  style: true
};

function checkMask(value, bitmask) {
  return (value & bitmask) === bitmask;
}

var DOMPropertyInjection = {
  /**
   * Mapping from normalized, camelcased property names to a configuration that
   * specifies how the associated DOM property should be accessed or rendered.
   */
  MUST_USE_PROPERTY: 0x1,
  HAS_BOOLEAN_VALUE: 0x4,
  HAS_NUMERIC_VALUE: 0x8,
  HAS_POSITIVE_NUMERIC_VALUE: 0x10 | 0x8,
  HAS_OVERLOADED_BOOLEAN_VALUE: 0x20,
  HAS_STRING_BOOLEAN_VALUE: 0x40,

  /**
   * Inject some specialized knowledge about the DOM. This takes a config object
   * with the following properties:
   *
   * Properties: object mapping DOM property name to one of the
   * DOMPropertyInjection constants or null. If your attribute isn't in here,
   * it won't get written to the DOM.
   *
   * DOMAttributeNames: object mapping React attribute name to the DOM
   * attribute name. Attribute names not specified use the **lowercase**
   * normalized name.
   *
   * DOMAttributeNamespaces: object mapping React attribute name to the DOM
   * attribute namespace URL. (Attribute names not specified use no namespace.)
   *
   * DOMPropertyNames: similar to DOMAttributeNames but for DOM properties.
   * Property names not specified use the normalized name.
   *
   * DOMMutationMethods: Properties that require special mutation methods. If
   * `value` is undefined, the mutation method should unset the property.
   *
   * @param {object} domPropertyConfig the config as described above.
   */
  injectDOMPropertyConfig: function (domPropertyConfig) {
    var Injection = DOMPropertyInjection;
    var Properties = domPropertyConfig.Properties || {};
    var DOMAttributeNamespaces = domPropertyConfig.DOMAttributeNamespaces || {};
    var DOMAttributeNames = domPropertyConfig.DOMAttributeNames || {};
    var DOMMutationMethods = domPropertyConfig.DOMMutationMethods || {};

    for (var propName in Properties) {
      !!properties.hasOwnProperty(propName) ? invariant(false, "injectDOMPropertyConfig(...): You're trying to inject DOM property '%s' which has already been injected. You may be accidentally injecting the same DOM property config twice, or you may be injecting two configs that have conflicting property names.", propName) : void 0;

      var lowerCased = propName.toLowerCase();
      var propConfig = Properties[propName];

      var propertyInfo = {
        attributeName: lowerCased,
        attributeNamespace: null,
        propertyName: propName,
        mutationMethod: null,

        mustUseProperty: checkMask(propConfig, Injection.MUST_USE_PROPERTY),
        hasBooleanValue: checkMask(propConfig, Injection.HAS_BOOLEAN_VALUE),
        hasNumericValue: checkMask(propConfig, Injection.HAS_NUMERIC_VALUE),
        hasPositiveNumericValue: checkMask(propConfig, Injection.HAS_POSITIVE_NUMERIC_VALUE),
        hasOverloadedBooleanValue: checkMask(propConfig, Injection.HAS_OVERLOADED_BOOLEAN_VALUE),
        hasStringBooleanValue: checkMask(propConfig, Injection.HAS_STRING_BOOLEAN_VALUE)
      };
      !(propertyInfo.hasBooleanValue + propertyInfo.hasNumericValue + propertyInfo.hasOverloadedBooleanValue <= 1) ? invariant(false, "DOMProperty: Value can be one of boolean, overloaded boolean, or numeric value, but not a combination: %s", propName) : void 0;

      if (DOMAttributeNames.hasOwnProperty(propName)) {
        var attributeName = DOMAttributeNames[propName];

        propertyInfo.attributeName = attributeName;
      }

      if (DOMAttributeNamespaces.hasOwnProperty(propName)) {
        propertyInfo.attributeNamespace = DOMAttributeNamespaces[propName];
      }

      if (DOMMutationMethods.hasOwnProperty(propName)) {
        propertyInfo.mutationMethod = DOMMutationMethods[propName];
      }

      // Downcase references to whitelist properties to check for membership
      // without case-sensitivity. This allows the whitelist to pick up
      // `allowfullscreen`, which should be written using the property configuration
      // for `allowFullscreen`
      properties[propName] = propertyInfo;
    }
  }
};

/* eslint-disable max-len */
var ATTRIBUTE_NAME_START_CHAR = ":A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
/* eslint-enable max-len */
var ATTRIBUTE_NAME_CHAR = ATTRIBUTE_NAME_START_CHAR + "\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040";


var ROOT_ATTRIBUTE_NAME = 'data-reactroot';

/**
 * Map from property "standard name" to an object with info about how to set
 * the property in the DOM. Each object contains:
 *
 * attributeName:
 *   Used when rendering markup or with `*Attribute()`.
 * attributeNamespace
 * propertyName:
 *   Used on DOM node instances. (This includes properties that mutate due to
 *   external factors.)
 * mutationMethod:
 *   If non-null, used instead of the property or `setAttribute()` after
 *   initial render.
 * mustUseProperty:
 *   Whether the property must be accessed and mutated as an object property.
 * hasBooleanValue:
 *   Whether the property should be removed when set to a falsey value.
 * hasNumericValue:
 *   Whether the property must be numeric or parse as a numeric and should be
 *   removed when set to a falsey value.
 * hasPositiveNumericValue:
 *   Whether the property must be positive numeric or parse as a positive
 *   numeric and should be removed when set to a falsey value.
 * hasOverloadedBooleanValue:
 *   Whether the property can be used as a flag as well as with a value.
 *   Removed when strictly equal to false; present without a value when
 *   strictly equal to true; present with a value otherwise.
 */
var properties = {};

/**
 * Checks whether a property name is a writeable attribute.
 * @method
 */
function shouldSetAttribute(name, value) {
  if (isReservedProp(name)) {
    return false;
  }
  if (name.length > 2 && (name[0] === 'o' || name[0] === 'O') && (name[1] === 'n' || name[1] === 'N')) {
    return false;
  }
  if (value === null) {
    return true;
  }
  switch (typeof value) {
    case 'boolean':
      return shouldAttributeAcceptBooleanValue(name);
    case 'undefined':
    case 'number':
    case 'string':
    case 'object':
      return true;
    default:
      // function, symbol
      return false;
  }
}

function getPropertyInfo(name) {
  return properties.hasOwnProperty(name) ? properties[name] : null;
}

function shouldAttributeAcceptBooleanValue(name) {
  if (isReservedProp(name)) {
    return true;
  }
  var propertyInfo = getPropertyInfo(name);
  if (propertyInfo) {
    return propertyInfo.hasBooleanValue || propertyInfo.hasStringBooleanValue || propertyInfo.hasOverloadedBooleanValue;
  }
  var prefix = name.toLowerCase().slice(0, 5);
  return prefix === 'data-' || prefix === 'aria-';
}

/**
 * Checks to see if a property name is within the list of properties
 * reserved for internal React operations. These properties should
 * not be set on an HTML element.
 *
 * @private
 * @param {string} name
 * @return {boolean} If the name is within reserved props
 */
function isReservedProp(name) {
  return RESERVED_PROPS.hasOwnProperty(name);
}

var injection = DOMPropertyInjection;

var MUST_USE_PROPERTY = injection.MUST_USE_PROPERTY;
var HAS_BOOLEAN_VALUE = injection.HAS_BOOLEAN_VALUE;
var HAS_NUMERIC_VALUE = injection.HAS_NUMERIC_VALUE;
var HAS_POSITIVE_NUMERIC_VALUE = injection.HAS_POSITIVE_NUMERIC_VALUE;
var HAS_OVERLOADED_BOOLEAN_VALUE = injection.HAS_OVERLOADED_BOOLEAN_VALUE;
var HAS_STRING_BOOLEAN_VALUE = injection.HAS_STRING_BOOLEAN_VALUE;

var HTMLDOMPropertyConfig = {
  // When adding attributes to this list, be sure to also add them to
  // the `possibleStandardNames` module to ensure casing and incorrect
  // name warnings.
  Properties: {
    allowFullScreen: HAS_BOOLEAN_VALUE,
    // specifies target context for links with `preload` type
    async: HAS_BOOLEAN_VALUE,
    // Note: there is a special case that prevents it from being written to the DOM
    // on the client side because the browsers are inconsistent. Instead we call focus().
    autoFocus: HAS_BOOLEAN_VALUE,
    autoPlay: HAS_BOOLEAN_VALUE,
    capture: HAS_OVERLOADED_BOOLEAN_VALUE,
    checked: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    cols: HAS_POSITIVE_NUMERIC_VALUE,
    contentEditable: HAS_STRING_BOOLEAN_VALUE,
    controls: HAS_BOOLEAN_VALUE,
    'default': HAS_BOOLEAN_VALUE,
    defer: HAS_BOOLEAN_VALUE,
    disabled: HAS_BOOLEAN_VALUE,
    download: HAS_OVERLOADED_BOOLEAN_VALUE,
    draggable: HAS_STRING_BOOLEAN_VALUE,
    formNoValidate: HAS_BOOLEAN_VALUE,
    hidden: HAS_BOOLEAN_VALUE,
    loop: HAS_BOOLEAN_VALUE,
    // Caution; `option.selected` is not updated if `select.multiple` is
    // disabled with `removeAttribute`.
    multiple: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    muted: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    noValidate: HAS_BOOLEAN_VALUE,
    open: HAS_BOOLEAN_VALUE,
    playsInline: HAS_BOOLEAN_VALUE,
    readOnly: HAS_BOOLEAN_VALUE,
    required: HAS_BOOLEAN_VALUE,
    reversed: HAS_BOOLEAN_VALUE,
    rows: HAS_POSITIVE_NUMERIC_VALUE,
    rowSpan: HAS_NUMERIC_VALUE,
    scoped: HAS_BOOLEAN_VALUE,
    seamless: HAS_BOOLEAN_VALUE,
    selected: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    size: HAS_POSITIVE_NUMERIC_VALUE,
    start: HAS_NUMERIC_VALUE,
    // support for projecting regular DOM Elements via V1 named slots ( shadow dom )
    span: HAS_POSITIVE_NUMERIC_VALUE,
    spellCheck: HAS_STRING_BOOLEAN_VALUE,
    // Style must be explicitly set in the attribute list. React components
    // expect a style object
    style: 0,
    // Keep it in the whitelist because it is case-sensitive for SVG.
    tabIndex: 0,
    // itemScope is for for Microdata support.
    // See http://schema.org/docs/gs.html
    itemScope: HAS_BOOLEAN_VALUE,
    // These attributes must stay in the white-list because they have
    // different attribute names (see DOMAttributeNames below)
    acceptCharset: 0,
    className: 0,
    htmlFor: 0,
    httpEquiv: 0,
    // Attributes with mutation methods must be specified in the whitelist
    // Set the string boolean flag to allow the behavior
    value: HAS_STRING_BOOLEAN_VALUE
  },
  DOMAttributeNames: {
    acceptCharset: 'accept-charset',
    className: 'class',
    htmlFor: 'for',
    httpEquiv: 'http-equiv'
  },
  DOMMutationMethods: {
    value: function (node, value) {
      if (value == null) {
        return node.removeAttribute('value');
      }

      // Number inputs get special treatment due to some edge cases in
      // Chrome. Let everything else assign the value attribute as normal.
      // https://github.com/facebook/react/issues/7253#issuecomment-236074326
      if (node.type !== 'number' || node.hasAttribute('value') === false) {
        node.setAttribute('value', '' + value);
      } else if (node.validity && !node.validity.badInput && node.ownerDocument.activeElement !== node) {
        // Don't assign an attribute if validation reports bad
        // input. Chrome will clear the value. Additionally, don't
        // operate on inputs that have focus, otherwise Chrome might
        // strip off trailing decimal places and cause the user's
        // cursor position to jump to the beginning of the input.
        //
        // In ReactDOMInput, we have an onBlur event that will trigger
        // this function again when focus is lost.
        node.setAttribute('value', '' + value);
      }
    }
  }
};

var HAS_STRING_BOOLEAN_VALUE$1 = injection.HAS_STRING_BOOLEAN_VALUE;


var NS = {
  xlink: 'http://www.w3.org/1999/xlink',
  xml: 'http://www.w3.org/XML/1998/namespace'
};

/**
 * This is a list of all SVG attributes that need special casing,
 * namespacing, or boolean value assignment.
 *
 * When adding attributes to this list, be sure to also add them to
 * the `possibleStandardNames` module to ensure casing and incorrect
 * name warnings.
 *
 * SVG Attributes List:
 * https://www.w3.org/TR/SVG/attindex.html
 * SMIL Spec:
 * https://www.w3.org/TR/smil
 */
var ATTRS = ['accent-height', 'alignment-baseline', 'arabic-form', 'baseline-shift', 'cap-height', 'clip-path', 'clip-rule', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'dominant-baseline', 'enable-background', 'fill-opacity', 'fill-rule', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'glyph-name', 'glyph-orientation-horizontal', 'glyph-orientation-vertical', 'horiz-adv-x', 'horiz-origin-x', 'image-rendering', 'letter-spacing', 'lighting-color', 'marker-end', 'marker-mid', 'marker-start', 'overline-position', 'overline-thickness', 'paint-order', 'panose-1', 'pointer-events', 'rendering-intent', 'shape-rendering', 'stop-color', 'stop-opacity', 'strikethrough-position', 'strikethrough-thickness', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke-width', 'text-anchor', 'text-decoration', 'text-rendering', 'underline-position', 'underline-thickness', 'unicode-bidi', 'unicode-range', 'units-per-em', 'v-alphabetic', 'v-hanging', 'v-ideographic', 'v-mathematical', 'vector-effect', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'word-spacing', 'writing-mode', 'x-height', 'xlink:actuate', 'xlink:arcrole', 'xlink:href', 'xlink:role', 'xlink:show', 'xlink:title', 'xlink:type', 'xml:base', 'xmlns:xlink', 'xml:lang', 'xml:space'];

var SVGDOMPropertyConfig = {
  Properties: {
    autoReverse: HAS_STRING_BOOLEAN_VALUE$1,
    externalResourcesRequired: HAS_STRING_BOOLEAN_VALUE$1,
    preserveAlpha: HAS_STRING_BOOLEAN_VALUE$1
  },
  DOMAttributeNames: {
    autoReverse: 'autoReverse',
    externalResourcesRequired: 'externalResourcesRequired',
    preserveAlpha: 'preserveAlpha'
  },
  DOMAttributeNamespaces: {


    xmlSpace: NS.xml
  }
};

var CAMELIZE = /[\-\:]([a-z])/g;
var capitalize = function (token) {
  return token[1].toUpperCase();
};

ATTRS.forEach(function (original) {
  var reactName = original.replace(CAMELIZE, capitalize);

  SVGDOMPropertyConfig.Properties[reactName] = 0;
  SVGDOMPropertyConfig.DOMAttributeNames[reactName] = original;
});

injection.injectDOMPropertyConfig(HTMLDOMPropertyConfig);
injection.injectDOMPropertyConfig(SVGDOMPropertyConfig);

var ReactErrorUtils = {
  // Used by Fiber to simulate a try-catch.
  _caughtError: null,
  _hasCaughtError: false,

  // Used by event system to capture/rethrow the first error.
  _rethrowError: null,
  _hasRethrowError: false,

  injection: {
    injectErrorUtils: function (injectedErrorUtils) {
      !(typeof injectedErrorUtils.invokeGuardedCallback === 'function') ? invariant(false, 'Injected invokeGuardedCallback() must be a function.') : void 0;
      invokeGuardedCallback = injectedErrorUtils.invokeGuardedCallback;
    }
  },

  /**
   * Call a function while guarding against errors that happens within it.
   * Returns an error if it throws, otherwise null.
   *
   * In production, this is implemented using a try-catch. The reason we don't
   * use a try-catch directly is so that we can swap out a different
   * implementation in DEV mode.
   *
   * @param {String} name of the guard to use for logging or debugging
   * @param {Function} func The function to invoke
   * @param {*} context The context to use when calling the function
   * @param {...*} args Arguments for function
   */
  invokeGuardedCallback: function (name, func, context, a, b, c, d, e, f) {
    invokeGuardedCallback.apply(ReactErrorUtils, arguments);
  },

  /**
   * Same as invokeGuardedCallback, but instead of returning an error, it stores
   * it in a global so it can be rethrown by `rethrowCaughtError` later.
   * TODO: See if _caughtError and _rethrowError can be unified.
   *
   * @param {String} name of the guard to use for logging or debugging
   * @param {Function} func The function to invoke
   * @param {*} context The context to use when calling the function
   * @param {...*} args Arguments for function
   */
  invokeGuardedCallbackAndCatchFirstError: function (name, func, context, a, b, c, d, e, f) {
    ReactErrorUtils.invokeGuardedCallback.apply(this, arguments);
    if (ReactErrorUtils.hasCaughtError()) {
      var error = ReactErrorUtils.clearCaughtError();
      if (!ReactErrorUtils._hasRethrowError) {
        ReactErrorUtils._hasRethrowError = true;
        ReactErrorUtils._rethrowError = error;
      }
    }
  },

  /**
   * During execution of guarded functions we will capture the first error which
   * we will rethrow to be handled by the top level error handler.
   */
  rethrowCaughtError: function () {
    return rethrowCaughtError.apply(ReactErrorUtils, arguments);
  },

  hasCaughtError: function () {
    return ReactErrorUtils._hasCaughtError;
  },

  clearCaughtError: function () {
    if (ReactErrorUtils._hasCaughtError) {
      var error = ReactErrorUtils._caughtError;
      ReactErrorUtils._caughtError = null;
      ReactErrorUtils._hasCaughtError = false;
      return error;
    } else {
      invariant(false, 'clearCaughtError was called but no error was captured. This error is likely caused by a bug in React. Please file an issue.');
    }
  }
};

var invokeGuardedCallback = function (name, func, context, a, b, c, d, e, f) {
  ReactErrorUtils._hasCaughtError = false;
  ReactErrorUtils._caughtError = null;
  var funcArgs = Array.prototype.slice.call(arguments, 3);
  try {
    func.apply(context, funcArgs);
  } catch (error) {
    ReactErrorUtils._caughtError = error;
    ReactErrorUtils._hasCaughtError = true;
  }
};

{
  // In DEV mode, we swap out invokeGuardedCallback for a special version
  // that plays more nicely with the browser's DevTools. The idea is to preserve
  // "Pause on exceptions" behavior. Because React wraps all user-provided
  // functions in invokeGuardedCallback, and the production version of
  // invokeGuardedCallback uses a try-catch, all user exceptions are treated
  // like caught exceptions, and the DevTools won't pause unless the developer
  // takes the extra step of enabling pause on caught exceptions. This is
  // untintuitive, though, because even though React has caught the error, from
  // the developer's perspective, the error is uncaught.
  //
  // To preserve the expected "Pause on exceptions" behavior, we don't use a
  // try-catch in DEV. Instead, we synchronously dispatch a fake event to a fake
  // DOM node, and call the user-provided callback from inside an event handler
  // for that fake event. If the callback throws, the error is "captured" using
  // a global event handler. But because the error happens in a different
  // event loop context, it does not interrupt the normal program flow.
  // Effectively, this gives us try-catch behavior without actually using
  // try-catch. Neat!

  // Check that the browser supports the APIs we need to implement our special
  // DEV version of invokeGuardedCallback
  if (typeof window !== 'undefined' && typeof window.dispatchEvent === 'function' && typeof document !== 'undefined' && typeof document.createEvent === 'function') {
    var fakeNode = document.createElement('react');

    var invokeGuardedCallbackDev = function (name, func, context, a, b, c, d, e, f) {
      // Keeps track of whether the user-provided callback threw an error. We
      // set this to true at the beginning, then set it to false right after
      // calling the function. If the function errors, `didError` will never be
      // set to false. This strategy works even if the browser is flaky and
      // fails to call our global error handler, because it doesn't rely on
      // the error event at all.
      var didError = true;

      // Create an event handler for our fake event. We will synchronously
      // dispatch our fake event using `dispatchEvent`. Inside the handler, we
      // call the user-provided callback.
      var funcArgs = Array.prototype.slice.call(arguments, 3);
      function callCallback() {
        // We immediately remove the callback from event listeners so that
        // nested `invokeGuardedCallback` calls do not clash. Otherwise, a
        // nested call would trigger the fake event handlers of any call higher
        // in the stack.
        fakeNode.removeEventListener(evtType, callCallback, false);
        func.apply(context, funcArgs);
        didError = false;
      }

      // Create a global error event handler. We use this to capture the value
      // that was thrown. It's possible that this error handler will fire more
      // than once; for example, if non-React code also calls `dispatchEvent`
      // and a handler for that event throws. We should be resilient to most of
      // those cases. Even if our error event handler fires more than once, the
      // last error event is always used. If the callback actually does error,
      // we know that the last error event is the correct one, because it's not
      // possible for anything else to have happened in between our callback
      // erroring and the code that follows the `dispatchEvent` call below. If
      // the callback doesn't error, but the error event was fired, we know to
      // ignore it because `didError` will be false, as described above.
      var error = void 0;
      // Use this to track whether the error event is ever called.
      var didSetError = false;
      var isCrossOriginError = false;

      function onError(event) {
        error = event.error;
        didSetError = true;
        if (error === null && event.colno === 0 && event.lineno === 0) {
          isCrossOriginError = true;
        }
      }

      // Create a fake event type.
      var evtType = 'react-' + (name ? name : 'invokeguardedcallback');

      // Attach our event handlers
      window.addEventListener('error', onError);
      fakeNode.addEventListener(evtType, callCallback, false);

      // Synchronously dispatch our fake event. If the user-provided function
      // errors, it will trigger our global error handler.
      var evt = document.createEvent('Event');
      evt.initEvent(evtType, false, false);
      fakeNode.dispatchEvent(evt);

      if (didError) {
        if (!didSetError) {
          // The callback errored, but the error event never fired.
          error = new Error('An error was thrown inside one of your components, but React ' + "doesn't know what it was. This is likely due to browser " + 'flakiness. React does its best to preserve the "Pause on ' + 'exceptions" behavior of the DevTools, which requires some ' + "DEV-mode only tricks. It's possible that these don't work in " + 'your browser. Try triggering the error in production mode, ' + 'or switching to a modern browser. If you suspect that this is ' + 'actually an issue with React, please file an issue.');
        } else if (isCrossOriginError) {
          error = new Error("A cross-origin error was thrown. React doesn't have access to " + 'the actual error object in development. ' + 'See https://fb.me/react-crossorigin-error for more information.');
        }
        ReactErrorUtils._hasCaughtError = true;
        ReactErrorUtils._caughtError = error;
      } else {
        ReactErrorUtils._hasCaughtError = false;
        ReactErrorUtils._caughtError = null;
      }

      // Remove our event listeners
      window.removeEventListener('error', onError);
    };

    invokeGuardedCallback = invokeGuardedCallbackDev;
  }
}

var rethrowCaughtError = function () {
  if (ReactErrorUtils._hasRethrowError) {
    var error = ReactErrorUtils._rethrowError;
    ReactErrorUtils._rethrowError = null;
    ReactErrorUtils._hasRethrowError = false;
    throw error;
  }
};

/**
 * Injectable ordering of event plugins.
 */
var eventPluginOrder = null;

/**
 * Injectable mapping from names to event plugin modules.
 */
var namesToPlugins = {};

/**
 * Recomputes the plugin list using the injected plugins and plugin ordering.
 *
 * @private
 */
function recomputePluginOrdering() {
  if (!eventPluginOrder) {
    // Wait until an `eventPluginOrder` is injected.
    return;
  }
  for (var pluginName in namesToPlugins) {
    var pluginModule = namesToPlugins[pluginName];
    var pluginIndex = eventPluginOrder.indexOf(pluginName);
    !(pluginIndex > -1) ? invariant(false, 'EventPluginRegistry: Cannot inject event plugins that do not exist in the plugin ordering, `%s`.', pluginName) : void 0;
    if (plugins[pluginIndex]) {
      continue;
    }
    !pluginModule.extractEvents ? invariant(false, 'EventPluginRegistry: Event plugins must implement an `extractEvents` method, but `%s` does not.', pluginName) : void 0;
    plugins[pluginIndex] = pluginModule;
    var publishedEvents = pluginModule.eventTypes;
    for (var eventName in publishedEvents) {
      !publishEventForPlugin(publishedEvents[eventName], pluginModule, eventName) ? invariant(false, 'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.', eventName, pluginName) : void 0;
    }
  }
}

/**
 * Publishes an event so that it can be dispatched by the supplied plugin.
 *
 * @param {object} dispatchConfig Dispatch configuration for the event.
 * @param {object} PluginModule Plugin publishing the event.
 * @return {boolean} True if the event was successfully published.
 * @private
 */
function publishEventForPlugin(dispatchConfig, pluginModule, eventName) {
  !!eventNameDispatchConfigs.hasOwnProperty(eventName) ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same event name, `%s`.', eventName) : void 0;
  eventNameDispatchConfigs[eventName] = dispatchConfig;

  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
  if (phasedRegistrationNames) {
    for (var phaseName in phasedRegistrationNames) {
      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
        var phasedRegistrationName = phasedRegistrationNames[phaseName];
        publishRegistrationName(phasedRegistrationName, pluginModule, eventName);
      }
    }
    return true;
  } else if (dispatchConfig.registrationName) {
    publishRegistrationName(dispatchConfig.registrationName, pluginModule, eventName);
    return true;
  }
  return false;
}

/**
 * Publishes a registration name that is used to identify dispatched events.
 *
 * @param {string} registrationName Registration name to add.
 * @param {object} PluginModule Plugin publishing the event.
 * @private
 */
function publishRegistrationName(registrationName, pluginModule, eventName) {
  !!registrationNameModules[registrationName] ? invariant(false, 'EventPluginHub: More than one plugin attempted to publish the same registration name, `%s`.', registrationName) : void 0;
  registrationNameModules[registrationName] = pluginModule;
  registrationNameDependencies[registrationName] = pluginModule.eventTypes[eventName].dependencies;

  {
    var lowerCasedName = registrationName.toLowerCase();
    possibleRegistrationNames[lowerCasedName] = registrationName;

    if (registrationName === 'onDoubleClick') {
      possibleRegistrationNames.ondblclick = registrationName;
    }
  }
}

/**
 * Registers plugins so that they can extract and dispatch events.
 *
 * @see {EventPluginHub}
 */

/**
 * Ordered list of injected plugins.
 */
var plugins = [];

/**
 * Mapping from event name to dispatch config
 */
var eventNameDispatchConfigs = {};

/**
 * Mapping from registration name to plugin module
 */
var registrationNameModules = {};

/**
 * Mapping from registration name to event name
 */
var registrationNameDependencies = {};

/**
 * Mapping from lowercase registration names to the properly cased version,
 * used to warn in the case of missing event handlers. Available
 * only in true.
 * @type {Object}
 */
var possibleRegistrationNames = {};
// Trust the developer to only use possibleRegistrationNames in true

/**
 * Injects an ordering of plugins (by plugin name). This allows the ordering
 * to be decoupled from injection of the actual plugins so that ordering is
 * always deterministic regardless of packaging, on-the-fly injection, etc.
 *
 * @param {array} InjectedEventPluginOrder
 * @internal
 * @see {EventPluginHub.injection.injectEventPluginOrder}
 */
function injectEventPluginOrder(injectedEventPluginOrder) {
  !!eventPluginOrder ? invariant(false, 'EventPluginRegistry: Cannot inject event plugin ordering more than once. You are likely trying to load more than one copy of React.') : void 0;
  // Clone the ordering so it cannot be dynamically mutated.
  eventPluginOrder = Array.prototype.slice.call(injectedEventPluginOrder);
  recomputePluginOrdering();
}

/**
 * Injects plugins to be used by `EventPluginHub`. The plugin names must be
 * in the ordering injected by `injectEventPluginOrder`.
 *
 * Plugins can be injected as part of page initialization or on-the-fly.
 *
 * @param {object} injectedNamesToPlugins Map from names to plugin modules.
 * @internal
 * @see {EventPluginHub.injection.injectEventPluginsByName}
 */
function injectEventPluginsByName(injectedNamesToPlugins) {
  var isOrderingDirty = false;
  for (var pluginName in injectedNamesToPlugins) {
    if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
      continue;
    }
    var pluginModule = injectedNamesToPlugins[pluginName];
    if (!namesToPlugins.hasOwnProperty(pluginName) || namesToPlugins[pluginName] !== pluginModule) {
      !!namesToPlugins[pluginName] ? invariant(false, 'EventPluginRegistry: Cannot inject two different event plugins using the same name, `%s`.', pluginName) : void 0;
      namesToPlugins[pluginName] = pluginModule;
      isOrderingDirty = true;
    }
  }
  if (isOrderingDirty) {
    recomputePluginOrdering();
  }
}

var EventPluginRegistry = Object.freeze({
	plugins: plugins,
	eventNameDispatchConfigs: eventNameDispatchConfigs,
	registrationNameModules: registrationNameModules,
	registrationNameDependencies: registrationNameDependencies,
	possibleRegistrationNames: possibleRegistrationNames,
	injectEventPluginOrder: injectEventPluginOrder,
	injectEventPluginsByName: injectEventPluginsByName
});

var getFiberCurrentPropsFromNode = null;
var getInstanceFromNode = null;
var getNodeFromInstance = null;

var injection$2 = {
  injectComponentTree: function (Injected) {
    getFiberCurrentPropsFromNode = Injected.getFiberCurrentPropsFromNode;
    getInstanceFromNode = Injected.getInstanceFromNode;
    getNodeFromInstance = Injected.getNodeFromInstance;

    {
      warning(getNodeFromInstance && getInstanceFromNode, 'EventPluginUtils.injection.injectComponentTree(...): Injected ' + 'module is missing getNodeFromInstance or getInstanceFromNode.');
    }
  }
};






var validateEventDispatches;
{
  validateEventDispatches = function (event) {
    var dispatchListeners = event._dispatchListeners;
    var dispatchInstances = event._dispatchInstances;

    var listenersIsArr = Array.isArray(dispatchListeners);
    var listenersLen = listenersIsArr ? dispatchListeners.length : dispatchListeners ? 1 : 0;

    var instancesIsArr = Array.isArray(dispatchInstances);
    var instancesLen = instancesIsArr ? dispatchInstances.length : dispatchInstances ? 1 : 0;

    warning(instancesIsArr === listenersIsArr && instancesLen === listenersLen, 'EventPluginUtils: Invalid `event`.');
  };
}

/**
 * Dispatch the event to the listener.
 * @param {SyntheticEvent} event SyntheticEvent to handle
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @param {function} listener Application-level callback
 * @param {*} inst Internal component instance
 */
function executeDispatch(event, simulated, listener, inst) {
  var type = event.type || 'unknown-event';
  event.currentTarget = getNodeFromInstance(inst);
  ReactErrorUtils.invokeGuardedCallbackAndCatchFirstError(type, listener, undefined, event);
  event.currentTarget = null;
}

/**
 * Standard/simple iteration through an event's collected dispatches.
 */
function executeDispatchesInOrder(event, simulated) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchInstances = event._dispatchInstances;
  {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and Instances are two parallel arrays that are always in sync.
      executeDispatch(event, simulated, dispatchListeners[i], dispatchInstances[i]);
    }
  } else if (dispatchListeners) {
    executeDispatch(event, simulated, dispatchListeners, dispatchInstances);
  }
  event._dispatchListeners = null;
  event._dispatchInstances = null;
}

/**
 * @see executeDispatchesInOrderStopAtTrueImpl
 */


/**
 * Execution of a "direct" dispatch - there must be at most one dispatch
 * accumulated on the event or it is considered an error. It doesn't really make
 * sense for an event with multiple dispatches (bubbled) to keep track of the
 * return values at each dispatch execution, but it does tend to make sense when
 * dealing with "direct" dispatches.
 *
 * @return {*} The return value of executing the single dispatch.
 */


/**
 * @param {SyntheticEvent} event
 * @return {boolean} True iff number of dispatches accumulated is greater than 0.
 */

/**
 * Accumulates items that must not be null or undefined into the first one. This
 * is used to conserve memory by avoiding array allocations, and thus sacrifices
 * API cleanness. Since `current` can be null before being passed in and not
 * null after this function, make sure to assign it back to `current`:
 *
 * `a = accumulateInto(a, b);`
 *
 * This API should be sparingly used. Try `accumulate` for something cleaner.
 *
 * @return {*|array<*>} An accumulation of items.
 */

function accumulateInto(current, next) {
  !(next != null) ? invariant(false, 'accumulateInto(...): Accumulated items must not be null or undefined.') : void 0;

  if (current == null) {
    return next;
  }

  // Both are not empty. Warning: Never call x.concat(y) when you are not
  // certain that x is an Array (x could be a string with concat method).
  if (Array.isArray(current)) {
    if (Array.isArray(next)) {
      current.push.apply(current, next);
      return current;
    }
    current.push(next);
    return current;
  }

  if (Array.isArray(next)) {
    // A bit too dangerous to mutate `next`.
    return [current].concat(next);
  }

  return [current, next];
}

/**
 * @param {array} arr an "accumulation" of items which is either an Array or
 * a single item. Useful when paired with the `accumulate` module. This is a
 * simple utility that allows us to reason about a collection of items, but
 * handling the case when there is exactly one item (and we do not need to
 * allocate an array).
 * @param {function} cb Callback invoked with each element or a collection.
 * @param {?} [scope] Scope used as `this` in a callback.
 */
function forEachAccumulated(arr, cb, scope) {
  if (Array.isArray(arr)) {
    arr.forEach(cb, scope);
  } else if (arr) {
    cb.call(scope, arr);
  }
}

/**
 * Internal queue of events that have accumulated their dispatches and are
 * waiting to have their dispatches executed.
 */
var eventQueue = null;

/**
 * Dispatches an event and releases it back into the pool, unless persistent.
 *
 * @param {?object} event Synthetic event to be dispatched.
 * @param {boolean} simulated If the event is simulated (changes exn behavior)
 * @private
 */
var executeDispatchesAndRelease = function (event, simulated) {
  if (event) {
    executeDispatchesInOrder(event, simulated);

    if (!event.isPersistent()) {
      event.constructor.release(event);
    }
  }
};
var executeDispatchesAndReleaseSimulated = function (e) {
  return executeDispatchesAndRelease(e, true);
};
var executeDispatchesAndReleaseTopLevel = function (e) {
  return executeDispatchesAndRelease(e, false);
};

function isInteractive(tag) {
  return tag === 'button' || tag === 'input' || tag === 'select' || tag === 'textarea';
}

function shouldPreventMouseEvent(name, type, props) {
  switch (name) {
    case 'onClick':
    case 'onClickCapture':
    case 'onDoubleClick':
    case 'onDoubleClickCapture':
    case 'onMouseDown':
    case 'onMouseDownCapture':
    case 'onMouseMove':
    case 'onMouseMoveCapture':
    case 'onMouseUp':
    case 'onMouseUpCapture':
      return !!(props.disabled && isInteractive(type));
    default:
      return false;
  }
}

/**
 * This is a unified interface for event plugins to be installed and configured.
 *
 * Event plugins can implement the following properties:
 *
 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
 *     Required. When a top-level event is fired, this method is expected to
 *     extract synthetic events that will in turn be queued and dispatched.
 *
 *   `eventTypes` {object}
 *     Optional, plugins that fire events must publish a mapping of registration
 *     names that are used to register listeners. Values of this mapping must
 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
 *
 *   `executeDispatch` {function(object, function, string)}
 *     Optional, allows plugins to override how an event gets dispatched. By
 *     default, the listener is simply invoked.
 *
 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
 *
 * @public
 */

/**
 * Methods for injecting dependencies.
 */
var injection$1 = {
  /**
   * @param {array} InjectedEventPluginOrder
   * @public
   */
  injectEventPluginOrder: injectEventPluginOrder,

  /**
   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
   */
  injectEventPluginsByName: injectEventPluginsByName
};

/**
 * @param {object} inst The instance, which is the source of events.
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @return {?function} The stored callback.
 */
function getListener(inst, registrationName) {
  var listener;

  // TODO: shouldPreventMouseEvent is DOM-specific and definitely should not
  // live here; needs to be moved to a better place soon
  var stateNode = inst.stateNode;
  if (!stateNode) {
    // Work in progress (ex: onload events in incremental mode).
    return null;
  }
  var props = getFiberCurrentPropsFromNode(stateNode);
  if (!props) {
    // Work in progress.
    return null;
  }
  listener = props[registrationName];
  if (shouldPreventMouseEvent(registrationName, inst.type, props)) {
    return null;
  }
  !(!listener || typeof listener === 'function') ? invariant(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.', registrationName, typeof listener) : void 0;
  return listener;
}

/**
 * Allows registered plugins an opportunity to extract events from top-level
 * native browser events.
 *
 * @return {*} An accumulation of synthetic events.
 * @internal
 */
function extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var events;
  for (var i = 0; i < plugins.length; i++) {
    // Not every plugin in the ordering may be loaded at runtime.
    var possiblePlugin = plugins[i];
    if (possiblePlugin) {
      var extractedEvents = possiblePlugin.extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget);
      if (extractedEvents) {
        events = accumulateInto(events, extractedEvents);
      }
    }
  }
  return events;
}

/**
 * Enqueues a synthetic event that should be dispatched when
 * `processEventQueue` is invoked.
 *
 * @param {*} events An accumulation of synthetic events.
 * @internal
 */
function enqueueEvents(events) {
  if (events) {
    eventQueue = accumulateInto(eventQueue, events);
  }
}

/**
 * Dispatches all synthetic events on the event queue.
 *
 * @internal
 */
function processEventQueue(simulated) {
  // Set `eventQueue` to null before processing it so that we can tell if more
  // events get enqueued while processing.
  var processingEventQueue = eventQueue;
  eventQueue = null;

  if (!processingEventQueue) {
    return;
  }

  if (simulated) {
    forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseSimulated);
  } else {
    forEachAccumulated(processingEventQueue, executeDispatchesAndReleaseTopLevel);
  }
  !!eventQueue ? invariant(false, 'processEventQueue(): Additional events were enqueued while processing an event queue. Support for this has not yet been implemented.') : void 0;
  // This would be a good time to rethrow if any of the event handlers threw.
  ReactErrorUtils.rethrowCaughtError();
}

var EventPluginHub = Object.freeze({
	injection: injection$1,
	getListener: getListener,
	extractEvents: extractEvents,
	enqueueEvents: enqueueEvents,
	processEventQueue: processEventQueue
});

var IndeterminateComponent = 0; // Before we know whether it is functional or class
var FunctionalComponent = 1;
var ClassComponent = 2;
var HostRoot = 3; // Root of a host tree. Could be nested inside another node.
var HostPortal = 4; // A subtree. Could be an entry point to a different renderer.
var HostComponent = 5;
var HostText = 6;
var CallComponent = 7;
var CallHandlerPhase = 8;
var ReturnComponent = 9;
var Fragment = 10;

var randomKey = Math.random().toString(36).slice(2);
var internalInstanceKey = '__reactInternalInstance$' + randomKey;
var internalEventHandlersKey = '__reactEventHandlers$' + randomKey;

function precacheFiberNode$1(hostInst, node) {
  node[internalInstanceKey] = hostInst;
}

/**
 * Given a DOM node, return the closest ReactDOMComponent or
 * ReactDOMTextComponent instance ancestor.
 */
function getClosestInstanceFromNode(node) {
  if (node[internalInstanceKey]) {
    return node[internalInstanceKey];
  }

  // Walk up the tree until we find an ancestor whose instance we have cached.
  var parents = [];
  while (!node[internalInstanceKey]) {
    parents.push(node);
    if (node.parentNode) {
      node = node.parentNode;
    } else {
      // Top of the tree. This node must not be part of a React tree (or is
      // unmounted, potentially).
      return null;
    }
  }

  var closest = void 0;
  var inst = node[internalInstanceKey];
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber, this will always be the deepest root.
    return inst;
  }
  for (; node && (inst = node[internalInstanceKey]); node = parents.pop()) {
    closest = inst;
  }

  return closest;
}

/**
 * Given a DOM node, return the ReactDOMComponent or ReactDOMTextComponent
 * instance, or null if the node was not rendered by this React.
 */
function getInstanceFromNode$1(node) {
  var inst = node[internalInstanceKey];
  if (inst) {
    if (inst.tag === HostComponent || inst.tag === HostText) {
      return inst;
    } else {
      return null;
    }
  }
  return null;
}

/**
 * Given a ReactDOMComponent or ReactDOMTextComponent, return the corresponding
 * DOM node.
 */
function getNodeFromInstance$1(inst) {
  if (inst.tag === HostComponent || inst.tag === HostText) {
    // In Fiber this, is just the state node right now. We assume it will be
    // a host component or host text.
    return inst.stateNode;
  }

  // Without this first invariant, passing a non-DOM-component triggers the next
  // invariant for a missing parent, which is super confusing.
  invariant(false, 'getNodeFromInstance: Invalid argument.');
}

function getFiberCurrentPropsFromNode$1(node) {
  return node[internalEventHandlersKey] || null;
}

function updateFiberProps$1(node, props) {
  node[internalEventHandlersKey] = props;
}

var ReactDOMComponentTree = Object.freeze({
	precacheFiberNode: precacheFiberNode$1,
	getClosestInstanceFromNode: getClosestInstanceFromNode,
	getInstanceFromNode: getInstanceFromNode$1,
	getNodeFromInstance: getNodeFromInstance$1,
	getFiberCurrentPropsFromNode: getFiberCurrentPropsFromNode$1,
	updateFiberProps: updateFiberProps$1
});

function getParent(inst) {
  do {
    inst = inst['return'];
    // TODO: If this is a HostRoot we might want to bail out.
    // That is depending on if we want nested subtrees (layers) to bubble
    // events to their parent. We could also go through parentNode on the
    // host node but that wouldn't work for React Native and doesn't let us
    // do the portal feature.
  } while (inst && inst.tag !== HostComponent);
  if (inst) {
    return inst;
  }
  return null;
}

/**
 * Return the lowest common ancestor of A and B, or null if they are in
 * different trees.
 */
function getLowestCommonAncestor(instA, instB) {
  var depthA = 0;
  for (var tempA = instA; tempA; tempA = getParent(tempA)) {
    depthA++;
  }
  var depthB = 0;
  for (var tempB = instB; tempB; tempB = getParent(tempB)) {
    depthB++;
  }

  // If A is deeper, crawl up.
  while (depthA - depthB > 0) {
    instA = getParent(instA);
    depthA--;
  }

  // If B is deeper, crawl up.
  while (depthB - depthA > 0) {
    instB = getParent(instB);
    depthB--;
  }

  // Walk in lockstep until we find a match.
  var depth = depthA;
  while (depth--) {
    if (instA === instB || instA === instB.alternate) {
      return instA;
    }
    instA = getParent(instA);
    instB = getParent(instB);
  }
  return null;
}

/**
 * Return if A is an ancestor of B.
 */


/**
 * Return the parent instance of the passed-in instance.
 */
function getParentInstance(inst) {
  return getParent(inst);
}

/**
 * Simulates the traversal of a two-phase, capture/bubble event dispatch.
 */
function traverseTwoPhase(inst, fn, arg) {
  var path = [];
  while (inst) {
    path.push(inst);
    inst = getParent(inst);
  }
  var i;
  for (i = path.length; i-- > 0;) {
    fn(path[i], 'captured', arg);
  }
  for (i = 0; i < path.length; i++) {
    fn(path[i], 'bubbled', arg);
  }
}

/**
 * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
 * should would receive a `mouseEnter` or `mouseLeave` event.
 *
 * Does not invoke the callback on the nearest common ancestor because nothing
 * "entered" or "left" that element.
 */
function traverseEnterLeave(from, to, fn, argFrom, argTo) {
  var common = from && to ? getLowestCommonAncestor(from, to) : null;
  var pathFrom = [];
  while (true) {
    if (!from) {
      break;
    }
    if (from === common) {
      break;
    }
    var alternate = from.alternate;
    if (alternate !== null && alternate === common) {
      break;
    }
    pathFrom.push(from);
    from = getParent(from);
  }
  var pathTo = [];
  while (true) {
    if (!to) {
      break;
    }
    if (to === common) {
      break;
    }
    var _alternate = to.alternate;
    if (_alternate !== null && _alternate === common) {
      break;
    }
    pathTo.push(to);
    to = getParent(to);
  }
  for (var i = 0; i < pathFrom.length; i++) {
    fn(pathFrom[i], 'bubbled', argFrom);
  }
  for (var _i = pathTo.length; _i-- > 0;) {
    fn(pathTo[_i], 'captured', argTo);
  }
}

/**
 * Some event types have a notion of different registration names for different
 * "phases" of propagation. This finds listeners by a given phase.
 */
function listenerAtPhase(inst, event, propagationPhase) {
  var registrationName = event.dispatchConfig.phasedRegistrationNames[propagationPhase];
  return getListener(inst, registrationName);
}

/**
 * A small set of propagation patterns, each of which will accept a small amount
 * of information, and generate a set of "dispatch ready event objects" - which
 * are sets of events that have already been annotated with a set of dispatched
 * listener functions/ids. The API is designed this way to discourage these
 * propagation strategies from actually executing the dispatches, since we
 * always want to collect the entire set of dispatches before executing even a
 * single one.
 */

/**
 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
 * here, allows us to not have to bind or create functions for each event.
 * Mutating the event's members allows us to not have to create a wrapping
 * "dispatch" object that pairs the event with the listener.
 */
function accumulateDirectionalDispatches(inst, phase, event) {
  {
    warning(inst, 'Dispatching inst must not be null');
  }
  var listener = listenerAtPhase(inst, event, phase);
  if (listener) {
    event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
    event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
  }
}

/**
 * Collect dispatches (must be entirely collected before dispatching - see unit
 * tests). Lazily allocate the array to conserve memory.  We must loop through
 * each event and perform the traversal for each one. We cannot perform a
 * single traversal for the entire collection of events because each event may
 * have a different target.
 */
function accumulateTwoPhaseDispatchesSingle(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    traverseTwoPhase(event._targetInst, accumulateDirectionalDispatches, event);
  }
}

/**
 * Same as `accumulateTwoPhaseDispatchesSingle`, but skips over the targetID.
 */
function accumulateTwoPhaseDispatchesSingleSkipTarget(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    var targetInst = event._targetInst;
    var parentInst = targetInst ? getParentInstance(targetInst) : null;
    traverseTwoPhase(parentInst, accumulateDirectionalDispatches, event);
  }
}

/**
 * Accumulates without regard to direction, does not look for phased
 * registration names. Same as `accumulateDirectDispatchesSingle` but without
 * requiring that the `dispatchMarker` be the same as the dispatched ID.
 */
function accumulateDispatches(inst, ignoredDirection, event) {
  if (inst && event && event.dispatchConfig.registrationName) {
    var registrationName = event.dispatchConfig.registrationName;
    var listener = getListener(inst, registrationName);
    if (listener) {
      event._dispatchListeners = accumulateInto(event._dispatchListeners, listener);
      event._dispatchInstances = accumulateInto(event._dispatchInstances, inst);
    }
  }
}

/**
 * Accumulates dispatches on an `SyntheticEvent`, but only for the
 * `dispatchMarker`.
 * @param {SyntheticEvent} event
 */
function accumulateDirectDispatchesSingle(event) {
  if (event && event.dispatchConfig.registrationName) {
    accumulateDispatches(event._targetInst, null, event);
  }
}

function accumulateTwoPhaseDispatches(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
}

function accumulateTwoPhaseDispatchesSkipTarget(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingleSkipTarget);
}

function accumulateEnterLeaveDispatches(leave, enter, from, to) {
  traverseEnterLeave(from, to, accumulateDispatches, leave, enter);
}

function accumulateDirectDispatches(events) {
  forEachAccumulated(events, accumulateDirectDispatchesSingle);
}

var EventPropagators = Object.freeze({
	accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
	accumulateTwoPhaseDispatchesSkipTarget: accumulateTwoPhaseDispatchesSkipTarget,
	accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches,
	accumulateDirectDispatches: accumulateDirectDispatches
});

var contentKey = null;

/**
 * Gets the key used to access text content on a DOM node.
 *
 * @return {?string} Key used to access text content.
 * @internal
 */
function getTextContentAccessor() {
  if (!contentKey && ExecutionEnvironment.canUseDOM) {
    // Prefer textContent to innerText because many browsers support both but
    // SVG <text> elements don't support innerText even when <div> does.
    contentKey = 'textContent' in document.documentElement ? 'textContent' : 'innerText';
  }
  return contentKey;
}

/**
 * This helper object stores information about text content of a target node,
 * allowing comparison of content before and after a given event.
 *
 * Identify the node where selection currently begins, then observe
 * both its text content and its current position in the DOM. Since the
 * browser may natively replace the target node during composition, we can
 * use its position to find its replacement.
 *
 *
 */
var compositionState = {
  _root: null,
  _startText: null,
  _fallbackText: null
};

function initialize(nativeEventTarget) {
  compositionState._root = nativeEventTarget;
  compositionState._startText = getText();
  return true;
}

function reset() {
  compositionState._root = null;
  compositionState._startText = null;
  compositionState._fallbackText = null;
}

function getData() {
  if (compositionState._fallbackText) {
    return compositionState._fallbackText;
  }

  var start;
  var startValue = compositionState._startText;
  var startLength = startValue.length;
  var end;
  var endValue = getText();
  var endLength = endValue.length;

  for (start = 0; start < startLength; start++) {
    if (startValue[start] !== endValue[start]) {
      break;
    }
  }

  var minEnd = startLength - start;
  for (end = 1; end <= minEnd; end++) {
    if (startValue[startLength - end] !== endValue[endLength - end]) {
      break;
    }
  }

  var sliceTail = end > 1 ? 1 - end : undefined;
  compositionState._fallbackText = endValue.slice(start, sliceTail);
  return compositionState._fallbackText;
}

function getText() {
  if ('value' in compositionState._root) {
    return compositionState._root.value;
  }
  return compositionState._root[getTextContentAccessor()];
}

/* eslint valid-typeof: 0 */

var didWarnForAddedNewProperty = false;
var isProxySupported = typeof Proxy === 'function';
var EVENT_POOL_SIZE = 10;

var shouldBeReleasedProperties = ['dispatchConfig', '_targetInst', 'nativeEvent', 'isDefaultPrevented', 'isPropagationStopped', '_dispatchListeners', '_dispatchInstances'];

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var EventInterface = {
  type: null,
  target: null,
  // currentTarget is set when dispatching; no use in copying it here
  currentTarget: emptyFunction.thatReturnsNull,
  eventPhase: null,
  bubbles: null,
  cancelable: null,
  timeStamp: function (event) {
    return event.timeStamp || Date.now();
  },
  defaultPrevented: null,
  isTrusted: null
};

/**
 * Synthetic events are dispatched by event plugins, typically in response to a
 * top-level event delegation handler.
 *
 * These systems should generally use pooling to reduce the frequency of garbage
 * collection. The system should check `isPersistent` to determine whether the
 * event should be released into the pool after being dispatched. Users that
 * need a persisted event should invoke `persist`.
 *
 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
 * normalizing browser quirks. Subclasses do not necessarily have to implement a
 * DOM interface; custom application-specific events can also subclass this.
 *
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {*} targetInst Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @param {DOMEventTarget} nativeEventTarget Target node.
 */
function SyntheticEvent(dispatchConfig, targetInst, nativeEvent, nativeEventTarget) {
  {
    // these have a getter/setter for warnings
    delete this.nativeEvent;
    delete this.preventDefault;
    delete this.stopPropagation;
  }

  this.dispatchConfig = dispatchConfig;
  this._targetInst = targetInst;
  this.nativeEvent = nativeEvent;

  var Interface = this.constructor.Interface;
  for (var propName in Interface) {
    if (!Interface.hasOwnProperty(propName)) {
      continue;
    }
    {
      delete this[propName]; // this has a getter/setter for warnings
    }
    var normalize = Interface[propName];
    if (normalize) {
      this[propName] = normalize(nativeEvent);
    } else {
      if (propName === 'target') {
        this.target = nativeEventTarget;
      } else {
        this[propName] = nativeEvent[propName];
      }
    }
  }

  var defaultPrevented = nativeEvent.defaultPrevented != null ? nativeEvent.defaultPrevented : nativeEvent.returnValue === false;
  if (defaultPrevented) {
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  } else {
    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
  }
  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
  return this;
}

_assign(SyntheticEvent.prototype, {
  preventDefault: function () {
    this.defaultPrevented = true;
    var event = this.nativeEvent;
    if (!event) {
      return;
    }

    if (event.preventDefault) {
      event.preventDefault();
    } else if (typeof event.returnValue !== 'unknown') {
      event.returnValue = false;
    }
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  },

  stopPropagation: function () {
    var event = this.nativeEvent;
    if (!event) {
      return;
    }

    if (event.stopPropagation) {
      event.stopPropagation();
    } else if (typeof event.cancelBubble !== 'unknown') {
      // The ChangeEventPlugin registers a "propertychange" event for
      // IE. This event does not support bubbling or cancelling, and
      // any references to cancelBubble throw "Member not found".  A
      // typeof check of "unknown" circumvents this issue (and is also
      // IE specific).
      event.cancelBubble = true;
    }

    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
  },

  /**
   * We release all dispatched `SyntheticEvent`s after each event loop, adding
   * them back into the pool. This allows a way to hold onto a reference that
   * won't be added back into the pool.
   */
  persist: function () {
    this.isPersistent = emptyFunction.thatReturnsTrue;
  },

  /**
   * Checks if this event should be released back into the pool.
   *
   * @return {boolean} True if this should not be released, false otherwise.
   */
  isPersistent: emptyFunction.thatReturnsFalse,

  /**
   * `PooledClass` looks for `destructor` on each instance it releases.
   */
  destructor: function () {
    var Interface = this.constructor.Interface;
    for (var propName in Interface) {
      {
        Object.defineProperty(this, propName, getPooledWarningPropertyDefinition(propName, Interface[propName]));
      }
    }
    for (var i = 0; i < shouldBeReleasedProperties.length; i++) {
      this[shouldBeReleasedProperties[i]] = null;
    }
    {
      Object.defineProperty(this, 'nativeEvent', getPooledWarningPropertyDefinition('nativeEvent', null));
      Object.defineProperty(this, 'preventDefault', getPooledWarningPropertyDefinition('preventDefault', emptyFunction));
      Object.defineProperty(this, 'stopPropagation', getPooledWarningPropertyDefinition('stopPropagation', emptyFunction));
    }
  }
});

SyntheticEvent.Interface = EventInterface;

/**
 * Helper to reduce boilerplate when creating subclasses.
 *
 * @param {function} Class
 * @param {?object} Interface
 */
SyntheticEvent.augmentClass = function (Class, Interface) {
  var Super = this;

  var E = function () {};
  E.prototype = Super.prototype;
  var prototype = new E();

  _assign(prototype, Class.prototype);
  Class.prototype = prototype;
  Class.prototype.constructor = Class;

  Class.Interface = _assign({}, Super.Interface, Interface);
  Class.augmentClass = Super.augmentClass;
  addEventPoolingTo(Class);
};

/** Proxying after everything set on SyntheticEvent
 * to resolve Proxy issue on some WebKit browsers
 * in which some Event properties are set to undefined (GH#10010)
 */
{
  if (isProxySupported) {
    /*eslint-disable no-func-assign */
    SyntheticEvent = new Proxy(SyntheticEvent, {
      construct: function (target, args) {
        return this.apply(target, Object.create(target.prototype), args);
      },
      apply: function (constructor, that, args) {
        return new Proxy(constructor.apply(that, args), {
          set: function (target, prop, value) {
            if (prop !== 'isPersistent' && !target.constructor.Interface.hasOwnProperty(prop) && shouldBeReleasedProperties.indexOf(prop) === -1) {
              warning(didWarnForAddedNewProperty || target.isPersistent(), "This synthetic event is reused for performance reasons. If you're " + "seeing this, you're adding a new property in the synthetic event object. " + 'The property is never released. See ' + 'https://fb.me/react-event-pooling for more information.');
              didWarnForAddedNewProperty = true;
            }
            target[prop] = value;
            return true;
          }
        });
      }
    });
    /*eslint-enable no-func-assign */
  }
}

addEventPoolingTo(SyntheticEvent);

/**
 * Helper to nullify syntheticEvent instance properties when destructing
 *
 * @param {String} propName
 * @param {?object} getVal
 * @return {object} defineProperty object
 */
function getPooledWarningPropertyDefinition(propName, getVal) {
  var isFunction = typeof getVal === 'function';
  return {
    configurable: true,
    set: set,
    get: get
  };

  function set(val) {
    var action = isFunction ? 'setting the method' : 'setting the property';
    warn(action, 'This is effectively a no-op');
    return val;
  }

  function get() {
    var action = isFunction ? 'accessing the method' : 'accessing the property';
    var result = isFunction ? 'This is a no-op function' : 'This is set to null';
    warn(action, result);
    return getVal;
  }

  function warn(action, result) {
    var warningCondition = false;
    warning(warningCondition, "This synthetic event is reused for performance reasons. If you're seeing this, " + "you're %s `%s` on a released/nullified synthetic event. %s. " + 'If you must keep the original synthetic event around, use event.persist(). ' + 'See https://fb.me/react-event-pooling for more information.', action, propName, result);
  }
}

function getPooledEvent(dispatchConfig, targetInst, nativeEvent, nativeInst) {
  var EventConstructor = this;
  if (EventConstructor.eventPool.length) {
    var instance = EventConstructor.eventPool.pop();
    EventConstructor.call(instance, dispatchConfig, targetInst, nativeEvent, nativeInst);
    return instance;
  }
  return new EventConstructor(dispatchConfig, targetInst, nativeEvent, nativeInst);
}

function releasePooledEvent(event) {
  var EventConstructor = this;
  !(event instanceof EventConstructor) ? invariant(false, 'Trying to release an event instance  into a pool of a different type.') : void 0;
  event.destructor();
  if (EventConstructor.eventPool.length < EVENT_POOL_SIZE) {
    EventConstructor.eventPool.push(event);
  }
}

function addEventPoolingTo(EventConstructor) {
  EventConstructor.eventPool = [];
  EventConstructor.getPooled = getPooledEvent;
  EventConstructor.release = releasePooledEvent;
}

var SyntheticEvent$1 = SyntheticEvent;

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */
var CompositionEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticCompositionEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticCompositionEvent, CompositionEventInterface);

/**
 * @interface Event
 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
 *      /#events-inputevents
 */
var InputEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticInputEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticInputEvent, InputEventInterface);

var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
var START_KEYCODE = 229;

var canUseCompositionEvent = ExecutionEnvironment.canUseDOM && 'CompositionEvent' in window;

var documentMode = null;
if (ExecutionEnvironment.canUseDOM && 'documentMode' in document) {
  documentMode = document.documentMode;
}

// Webkit offers a very useful `textInput` event that can be used to
// directly represent `beforeInput`. The IE `textinput` event is not as
// useful, so we don't use it.
var canUseTextInputEvent = ExecutionEnvironment.canUseDOM && 'TextEvent' in window && !documentMode && !isPresto();

// In IE9+, we have access to composition events, but the data supplied
// by the native compositionend event may be incorrect. Japanese ideographic
// spaces, for instance (\u3000) are not recorded correctly.
var useFallbackCompositionData = ExecutionEnvironment.canUseDOM && (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11);

/**
 * Opera <= 12 includes TextEvent in window, but does not fire
 * text input events. Rely on keypress instead.
 */
function isPresto() {
  var opera = window.opera;
  return typeof opera === 'object' && typeof opera.version === 'function' && parseInt(opera.version(), 10) <= 12;
}

var SPACEBAR_CODE = 32;
var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);

// Events and their corresponding property names.
var eventTypes = {
  beforeInput: {
    phasedRegistrationNames: {
      bubbled: 'onBeforeInput',
      captured: 'onBeforeInputCapture'
    },
    dependencies: ['topCompositionEnd', 'topKeyPress', 'topTextInput', 'topPaste']
  },
  compositionEnd: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionEnd',
      captured: 'onCompositionEndCapture'
    },
    dependencies: ['topBlur', 'topCompositionEnd', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  },
  compositionStart: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionStart',
      captured: 'onCompositionStartCapture'
    },
    dependencies: ['topBlur', 'topCompositionStart', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  },
  compositionUpdate: {
    phasedRegistrationNames: {
      bubbled: 'onCompositionUpdate',
      captured: 'onCompositionUpdateCapture'
    },
    dependencies: ['topBlur', 'topCompositionUpdate', 'topKeyDown', 'topKeyPress', 'topKeyUp', 'topMouseDown']
  }
};

// Track whether we've ever handled a keypress on the space key.
var hasSpaceKeypress = false;

/**
 * Return whether a native keypress event is assumed to be a command.
 * This is required because Firefox fires `keypress` events for key commands
 * (cut, copy, select-all, etc.) even though no character is inserted.
 */
function isKeypressCommand(nativeEvent) {
  return (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) &&
  // ctrlKey && altKey is equivalent to AltGr, and is not a command.
  !(nativeEvent.ctrlKey && nativeEvent.altKey);
}

/**
 * Translate native top level events into event types.
 *
 * @param {string} topLevelType
 * @return {object}
 */
function getCompositionEventType(topLevelType) {
  switch (topLevelType) {
    case 'topCompositionStart':
      return eventTypes.compositionStart;
    case 'topCompositionEnd':
      return eventTypes.compositionEnd;
    case 'topCompositionUpdate':
      return eventTypes.compositionUpdate;
  }
}

/**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionStart(topLevelType, nativeEvent) {
  return topLevelType === 'topKeyDown' && nativeEvent.keyCode === START_KEYCODE;
}

/**
 * Does our fallback mode think that this event is the end of composition?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackCompositionEnd(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case 'topKeyUp':
      // Command keys insert or clear IME input.
      return END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1;
    case 'topKeyDown':
      // Expect IME keyCode on each keydown. If we get any other
      // code we must have exited earlier.
      return nativeEvent.keyCode !== START_KEYCODE;
    case 'topKeyPress':
    case 'topMouseDown':
    case 'topBlur':
      // Events are not possible without cancelling IME.
      return true;
    default:
      return false;
  }
}

/**
 * Google Input Tools provides composition data via a CustomEvent,
 * with the `data` property populated in the `detail` object. If this
 * is available on the event object, use it. If not, this is a plain
 * composition event and we have nothing special to extract.
 *
 * @param {object} nativeEvent
 * @return {?string}
 */
function getDataFromCustomEvent(nativeEvent) {
  var detail = nativeEvent.detail;
  if (typeof detail === 'object' && 'data' in detail) {
    return detail.data;
  }
  return null;
}

// Track the current IME composition status, if any.
var isComposing = false;

/**
 * @return {?object} A SyntheticCompositionEvent.
 */
function extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var eventType;
  var fallbackData;

  if (canUseCompositionEvent) {
    eventType = getCompositionEventType(topLevelType);
  } else if (!isComposing) {
    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
      eventType = eventTypes.compositionStart;
    }
  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
    eventType = eventTypes.compositionEnd;
  }

  if (!eventType) {
    return null;
  }

  if (useFallbackCompositionData) {
    // The current composition is stored statically and must not be
    // overwritten while composition continues.
    if (!isComposing && eventType === eventTypes.compositionStart) {
      isComposing = initialize(nativeEventTarget);
    } else if (eventType === eventTypes.compositionEnd) {
      if (isComposing) {
        fallbackData = getData();
      }
    }
  }

  var event = SyntheticCompositionEvent.getPooled(eventType, targetInst, nativeEvent, nativeEventTarget);

  if (fallbackData) {
    // Inject data generated from fallback path into the synthetic event.
    // This matches the property of native CompositionEventInterface.
    event.data = fallbackData;
  } else {
    var customData = getDataFromCustomEvent(nativeEvent);
    if (customData !== null) {
      event.data = customData;
    }
  }

  accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * @param {TopLevelTypes} topLevelType Record from `BrowserEventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The string corresponding to this `beforeInput` event.
 */
function getNativeBeforeInputChars(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case 'topCompositionEnd':
      return getDataFromCustomEvent(nativeEvent);
    case 'topKeyPress':
      /**
       * If native `textInput` events are available, our goal is to make
       * use of them. However, there is a special case: the spacebar key.
       * In Webkit, preventing default on a spacebar `textInput` event
       * cancels character insertion, but it *also* causes the browser
       * to fall back to its default spacebar behavior of scrolling the
       * page.
       *
       * Tracking at:
       * https://code.google.com/p/chromium/issues/detail?id=355103
       *
       * To avoid this issue, use the keypress event as if no `textInput`
       * event is available.
       */
      var which = nativeEvent.which;
      if (which !== SPACEBAR_CODE) {
        return null;
      }

      hasSpaceKeypress = true;
      return SPACEBAR_CHAR;

    case 'topTextInput':
      // Record the characters to be added to the DOM.
      var chars = nativeEvent.data;

      // If it's a spacebar character, assume that we have already handled
      // it at the keypress level and bail immediately. Android Chrome
      // doesn't give us keycodes, so we need to blacklist it.
      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
        return null;
      }

      return chars;

    default:
      // For other native event types, do nothing.
      return null;
  }
}

/**
 * For browsers that do not provide the `textInput` event, extract the
 * appropriate string to use for SyntheticInputEvent.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {object} nativeEvent Native browser event.
 * @return {?string} The fallback string for this `beforeInput` event.
 */
function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
  // If we are currently composing (IME) and using a fallback to do so,
  // try to extract the composed characters from the fallback object.
  // If composition event is available, we extract a string only at
  // compositionevent, otherwise extract it at fallback events.
  if (isComposing) {
    if (topLevelType === 'topCompositionEnd' || !canUseCompositionEvent && isFallbackCompositionEnd(topLevelType, nativeEvent)) {
      var chars = getData();
      reset();
      isComposing = false;
      return chars;
    }
    return null;
  }

  switch (topLevelType) {
    case 'topPaste':
      // If a paste event occurs after a keypress, throw out the input
      // chars. Paste events should not lead to BeforeInput events.
      return null;
    case 'topKeyPress':
      /**
       * As of v27, Firefox may fire keypress events even when no character
       * will be inserted. A few possibilities:
       *
       * - `which` is `0`. Arrow keys, Esc key, etc.
       *
       * - `which` is the pressed key code, but no char is available.
       *   Ex: 'AltGr + d` in Polish. There is no modified character for
       *   this key combination and no character is inserted into the
       *   document, but FF fires the keypress for char code `100` anyway.
       *   No `input` event will occur.
       *
       * - `which` is the pressed key code, but a command combination is
       *   being used. Ex: `Cmd+C`. No character is inserted, and no
       *   `input` event will occur.
       */
      if (!isKeypressCommand(nativeEvent)) {
        // IE fires the `keypress` event when a user types an emoji via
        // Touch keyboard of Windows.  In such a case, the `char` property
        // holds an emoji character like `\uD83D\uDE0A`.  Because its length
        // is 2, the property `which` does not represent an emoji correctly.
        // In such a case, we directly return the `char` property instead of
        // using `which`.
        if (nativeEvent.char && nativeEvent.char.length > 1) {
          return nativeEvent.char;
        } else if (nativeEvent.which) {
          return String.fromCharCode(nativeEvent.which);
        }
      }
      return null;
    case 'topCompositionEnd':
      return useFallbackCompositionData ? null : nativeEvent.data;
    default:
      return null;
  }
}

/**
 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
 * `textInput` or fallback behavior.
 *
 * @return {?object} A SyntheticInputEvent.
 */
function extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var chars;

  if (canUseTextInputEvent) {
    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
  } else {
    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
  }

  // If no characters are being inserted, no BeforeInput event should
  // be fired.
  if (!chars) {
    return null;
  }

  var event = SyntheticInputEvent.getPooled(eventTypes.beforeInput, targetInst, nativeEvent, nativeEventTarget);

  event.data = chars;
  accumulateTwoPhaseDispatches(event);
  return event;
}

/**
 * Create an `onBeforeInput` event to match
 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
 *
 * This event plugin is based on the native `textInput` event
 * available in Chrome, Safari, Opera, and IE. This event fires after
 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
 *
 * `beforeInput` is spec'd but not implemented in any browsers, and
 * the `input` event does not provide any useful information about what has
 * actually been added, contrary to the spec. Thus, `textInput` is the best
 * available event to identify the characters that have actually been inserted
 * into the target node.
 *
 * This plugin is also responsible for emitting `composition` events, thus
 * allowing us to share composition fallback code for both `beforeInput` and
 * `composition` event types.
 */
var BeforeInputEventPlugin = {
  eventTypes: eventTypes,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    return [extractCompositionEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget), extractBeforeInputEvent(topLevelType, targetInst, nativeEvent, nativeEventTarget)];
  }
};

// Use to restore controlled state after a change event has fired.

var fiberHostComponent = null;

var ReactControlledComponentInjection = {
  injectFiberControlledHostComponent: function (hostComponentImpl) {
    // The fiber implementation doesn't use dynamic dispatch so we need to
    // inject the implementation.
    fiberHostComponent = hostComponentImpl;
  }
};

var restoreTarget = null;
var restoreQueue = null;

function restoreStateOfTarget(target) {
  // We perform this translation at the end of the event loop so that we
  // always receive the correct fiber here
  var internalInstance = getInstanceFromNode(target);
  if (!internalInstance) {
    // Unmounted
    return;
  }
  !(fiberHostComponent && typeof fiberHostComponent.restoreControlledState === 'function') ? invariant(false, 'Fiber needs to be injected to handle a fiber target for controlled events. This error is likely caused by a bug in React. Please file an issue.') : void 0;
  var props = getFiberCurrentPropsFromNode(internalInstance.stateNode);
  fiberHostComponent.restoreControlledState(internalInstance.stateNode, internalInstance.type, props);
}

var injection$3 = ReactControlledComponentInjection;

function enqueueStateRestore(target) {
  if (restoreTarget) {
    if (restoreQueue) {
      restoreQueue.push(target);
    } else {
      restoreQueue = [target];
    }
  } else {
    restoreTarget = target;
  }
}

function restoreStateIfNeeded() {
  if (!restoreTarget) {
    return;
  }
  var target = restoreTarget;
  var queuedTargets = restoreQueue;
  restoreTarget = null;
  restoreQueue = null;

  restoreStateOfTarget(target);
  if (queuedTargets) {
    for (var i = 0; i < queuedTargets.length; i++) {
      restoreStateOfTarget(queuedTargets[i]);
    }
  }
}

var ReactControlledComponent = Object.freeze({
	injection: injection$3,
	enqueueStateRestore: enqueueStateRestore,
	restoreStateIfNeeded: restoreStateIfNeeded
});

// Used as a way to call batchedUpdates when we don't have a reference to
// the renderer. Such as when we're dispatching events or if third party
// libraries need to call batchedUpdates. Eventually, this API will go away when
// everything is batched by default. We'll then have a similar API to opt-out of
// scheduled work and instead do synchronous work.

// Defaults
var fiberBatchedUpdates = function (fn, bookkeeping) {
  return fn(bookkeeping);
};

var isNestingBatched = false;
function batchedUpdates(fn, bookkeeping) {
  if (isNestingBatched) {
    // If we are currently inside another batch, we need to wait until it
    // fully completes before restoring state. Therefore, we add the target to
    // a queue of work.
    return fiberBatchedUpdates(fn, bookkeeping);
  }
  isNestingBatched = true;
  try {
    return fiberBatchedUpdates(fn, bookkeeping);
  } finally {
    // Here we wait until all updates have propagated, which is important
    // when using controlled components within layers:
    // https://github.com/facebook/react/issues/1698
    // Then we restore state of any controlled component.
    isNestingBatched = false;
    restoreStateIfNeeded();
  }
}

var ReactGenericBatchingInjection = {
  injectFiberBatchedUpdates: function (_batchedUpdates) {
    fiberBatchedUpdates = _batchedUpdates;
  }
};

var injection$4 = ReactGenericBatchingInjection;

/**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */
var supportedInputTypes = {
  color: true,
  date: true,
  datetime: true,
  'datetime-local': true,
  email: true,
  month: true,
  number: true,
  password: true,
  range: true,
  search: true,
  tel: true,
  text: true,
  time: true,
  url: true,
  week: true
};

function isTextInputElement(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();

  if (nodeName === 'input') {
    return !!supportedInputTypes[elem.type];
  }

  if (nodeName === 'textarea') {
    return true;
  }

  return false;
}

/**
 * HTML nodeType values that represent the type of the node
 */

var ELEMENT_NODE = 1;
var TEXT_NODE = 3;
var COMMENT_NODE = 8;
var DOCUMENT_NODE = 9;
var DOCUMENT_FRAGMENT_NODE = 11;

/**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */
function getEventTarget(nativeEvent) {
  var target = nativeEvent.target || nativeEvent.srcElement || window;

  // Normalize SVG <use> element events #4963
  if (target.correspondingUseElement) {
    target = target.correspondingUseElement;
  }

  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
  // @see http://www.quirksmode.org/js/events_properties.html
  return target.nodeType === TEXT_NODE ? target.parentNode : target;
}

var useHasFeature;
if (ExecutionEnvironment.canUseDOM) {
  useHasFeature = document.implementation && document.implementation.hasFeature &&
  // always returns true in newer browsers as per the standard.
  // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature
  document.implementation.hasFeature('', '') !== true;
}

/**
 * Checks if an event is supported in the current execution environment.


 */
function isEventSupported(eventNameSuffix, capture) {
  if (!ExecutionEnvironment.canUseDOM || capture && !('addEventListener' in document)) {
    return false;
  }

  var eventName = 'on' + eventNameSuffix;
  var isSupported = eventName in document;

  if (!isSupported) {
    var element = document.createElement('div');
    element.setAttribute(eventName, 'return;');
    isSupported = typeof element[eventName] === 'function';
  }

  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {
    // This is the only way to test support for the `wheel` event in IE9+.
    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');
  }

  return isSupported;
}

function isCheckable(elem) {
  var type = elem.type;
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (type === 'checkbox' || type === 'radio');
}

function getTracker(node) {
  return node._valueTracker;
}

function detachTracker(node) {
  node._valueTracker = null;
}

function getValueFromNode(node) {
  var value = '';
  if (!node) {
    return value;
  }

  if (isCheckable(node)) {
    value = node.checked ? 'true' : 'false';
  } else {
    value = node.value;
  }

  return value;
}

function trackValueOnNode(node) {
  var valueField = isCheckable(node) ? 'checked' : 'value';
  var descriptor = Object.getOwnPropertyDescriptor(node.constructor.prototype, valueField);

  var currentValue = '' + node[valueField];

  // if someone has already defined a value or Safari, then bail
  // and don't track value will cause over reporting of changes,
  // but it's better then a hard failure
  // (needed for certain tests that spyOn input values and Safari)
  if (node.hasOwnProperty(valueField) || typeof descriptor.get !== 'function' || typeof descriptor.set !== 'function') {
    return;
  }

  Object.defineProperty(node, valueField, {
    enumerable: descriptor.enumerable,
    configurable: true,
    get: function () {
      return descriptor.get.call(this);
    },
    set: function (value) {
      currentValue = '' + value;
      descriptor.set.call(this, value);
    }
  });

  var tracker = {
    getValue: function () {
      return currentValue;
    },
    setValue: function (value) {
      currentValue = '' + value;
    },
    stopTracking: function () {
      detachTracker(node);
      delete node[valueField];
    }
  };
  return tracker;
}

function track(node) {
  if (getTracker(node)) {
    return;
  }

  // TODO: Once it's just Fiber we can move this to node._wrapperState
  node._valueTracker = trackValueOnNode(node);
}

function updateValueIfChanged(node) {
  if (!node) {
    return false;
  }

  var tracker = getTracker(node);
  // if there is no tracker at this point it's unlikely
  // that trying again will succeed
  if (!tracker) {
    return true;
  }

  var lastValue = tracker.getValue();
  var nextValue = getValueFromNode(node);
  if (nextValue !== lastValue) {
    tracker.setValue(nextValue);
    return true;
  }
  return false;
}

var eventTypes$1 = {
  change: {
    phasedRegistrationNames: {
      bubbled: 'onChange',
      captured: 'onChangeCapture'
    },
    dependencies: ['topBlur', 'topChange', 'topClick', 'topFocus', 'topInput', 'topKeyDown', 'topKeyUp', 'topSelectionChange']
  }
};

function createAndAccumulateChangeEvent(inst, nativeEvent, target) {
  var event = SyntheticEvent$1.getPooled(eventTypes$1.change, inst, nativeEvent, target);
  event.type = 'change';
  // Flag this event loop as needing state restore.
  enqueueStateRestore(target);
  accumulateTwoPhaseDispatches(event);
  return event;
}
/**
 * For IE shims
 */
var activeElement = null;
var activeElementInst = null;

/**
 * SECTION: handle `change` event
 */
function shouldUseChangeEvent(elem) {
  var nodeName = elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName === 'select' || nodeName === 'input' && elem.type === 'file';
}

function manualDispatchChangeEvent(nativeEvent) {
  var event = createAndAccumulateChangeEvent(activeElementInst, nativeEvent, getEventTarget(nativeEvent));

  // If change and propertychange bubbled, we'd just bind to it like all the
  // other events and have it go through ReactBrowserEventEmitter. Since it
  // doesn't, we manually listen for the events and so we have to enqueue and
  // process the abstract event manually.
  //
  // Batching is necessary here in order to ensure that all event handlers run
  // before the next rerender (including event handlers attached to ancestor
  // elements instead of directly on the input). Without this, controlled
  // components don't work properly in conjunction with event bubbling because
  // the component is rerendered and the value reverted before all the event
  // handlers can run. See https://github.com/facebook/react/issues/708.
  batchedUpdates(runEventInBatch, event);
}

function runEventInBatch(event) {
  enqueueEvents(event);
  processEventQueue(false);
}

function getInstIfValueChanged(targetInst) {
  var targetNode = getNodeFromInstance$1(targetInst);
  if (updateValueIfChanged(targetNode)) {
    return targetInst;
  }
}

function getTargetInstForChangeEvent(topLevelType, targetInst) {
  if (topLevelType === 'topChange') {
    return targetInst;
  }
}

/**
 * SECTION: handle `input` event
 */
var isInputEventSupported = false;
if (ExecutionEnvironment.canUseDOM) {
  // IE9 claims to support the input event but fails to trigger it when
  // deleting text, so we ignore its input events.
  isInputEventSupported = isEventSupported('input') && (!document.documentMode || document.documentMode > 9);
}

/**
 * (For IE <=9) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */
function startWatchingForValueChange(target, targetInst) {
  activeElement = target;
  activeElementInst = targetInst;
  activeElement.attachEvent('onpropertychange', handlePropertyChange);
}

/**
 * (For IE <=9) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */
function stopWatchingForValueChange() {
  if (!activeElement) {
    return;
  }
  activeElement.detachEvent('onpropertychange', handlePropertyChange);
  activeElement = null;
  activeElementInst = null;
}

/**
 * (For IE <=9) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */
function handlePropertyChange(nativeEvent) {
  if (nativeEvent.propertyName !== 'value') {
    return;
  }
  if (getInstIfValueChanged(activeElementInst)) {
    manualDispatchChangeEvent(nativeEvent);
  }
}

function handleEventsForInputEventPolyfill(topLevelType, target, targetInst) {
  if (topLevelType === 'topFocus') {
    // In IE9, propertychange fires for most input events but is buggy and
    // doesn't fire when text is deleted, but conveniently, selectionchange
    // appears to fire in all of the remaining cases so we catch those and
    // forward the event if the value has changed
    // In either case, we don't want to call the event handler if the value
    // is changed from JS so we redefine a setter for `.value` that updates
    // our activeElementValue variable, allowing us to ignore those changes
    //
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForValueChange();
    startWatchingForValueChange(target, targetInst);
  } else if (topLevelType === 'topBlur') {
    stopWatchingForValueChange();
  }
}

// For IE8 and IE9.
function getTargetInstForInputEventPolyfill(topLevelType, targetInst) {
  if (topLevelType === 'topSelectionChange' || topLevelType === 'topKeyUp' || topLevelType === 'topKeyDown') {
    // On the selectionchange event, the target is just document which isn't
    // helpful for us so just check activeElement instead.
    //
    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
    // propertychange on the first input event after setting `value` from a
    // script and fires only keydown, keypress, keyup. Catching keyup usually
    // gets it and catching keydown lets us fire an event for the first
    // keystroke if user does a key repeat (it'll be a little delayed: right
    // before the second keystroke). Other input methods (e.g., paste) seem to
    // fire selectionchange normally.
    return getInstIfValueChanged(activeElementInst);
  }
}

/**
 * SECTION: handle `click` event
 */
function shouldUseClickEvent(elem) {
  // Use the `click` event to detect changes to checkbox and radio inputs.
  // This approach works across all browsers, whereas `change` does not fire
  // until `blur` in IE8.
  var nodeName = elem.nodeName;
  return nodeName && nodeName.toLowerCase() === 'input' && (elem.type === 'checkbox' || elem.type === 'radio');
}

function getTargetInstForClickEvent(topLevelType, targetInst) {
  if (topLevelType === 'topClick') {
    return getInstIfValueChanged(targetInst);
  }
}

function getTargetInstForInputOrChangeEvent(topLevelType, targetInst) {
  if (topLevelType === 'topInput' || topLevelType === 'topChange') {
    return getInstIfValueChanged(targetInst);
  }
}

function handleControlledInputBlur(inst, node) {
  // TODO: In IE, inst is occasionally null. Why?
  if (inst == null) {
    return;
  }

  // Fiber and ReactDOM keep wrapper state in separate places
  var state = inst._wrapperState || node._wrapperState;

  if (!state || !state.controlled || node.type !== 'number') {
    return;
  }

  // If controlled, assign the value attribute to the current value on blur
  var value = '' + node.value;
  if (node.getAttribute('value') !== value) {
    node.setAttribute('value', value);
  }
}

/**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */
var ChangeEventPlugin = {
  eventTypes: eventTypes$1,

  _isInputEventSupported: isInputEventSupported,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    var getTargetInstFunc, handleEventFunc;
    if (shouldUseChangeEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForChangeEvent;
    } else if (isTextInputElement(targetNode)) {
      if (isInputEventSupported) {
        getTargetInstFunc = getTargetInstForInputOrChangeEvent;
      } else {
        getTargetInstFunc = getTargetInstForInputEventPolyfill;
        handleEventFunc = handleEventsForInputEventPolyfill;
      }
    } else if (shouldUseClickEvent(targetNode)) {
      getTargetInstFunc = getTargetInstForClickEvent;
    }

    if (getTargetInstFunc) {
      var inst = getTargetInstFunc(topLevelType, targetInst);
      if (inst) {
        var event = createAndAccumulateChangeEvent(inst, nativeEvent, nativeEventTarget);
        return event;
      }
    }

    if (handleEventFunc) {
      handleEventFunc(topLevelType, targetNode, targetInst);
    }

    // When blurring, set the value attribute for number inputs
    if (topLevelType === 'topBlur') {
      handleControlledInputBlur(targetInst, targetNode);
    }
  }
};

/**
 * Module that is injectable into `EventPluginHub`, that specifies a
 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
 * plugins, without having to package every one of them. This is better than
 * having plugins be ordered in the same order that they are injected because
 * that ordering would be influenced by the packaging order.
 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
 */
var DOMEventPluginOrder = ['ResponderEventPlugin', 'SimpleEventPlugin', 'TapEventPlugin', 'EnterLeaveEventPlugin', 'ChangeEventPlugin', 'SelectEventPlugin', 'BeforeInputEventPlugin'];

/**
 * @interface UIEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var UIEventInterface = {
  view: null,
  detail: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticUIEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticUIEvent, UIEventInterface);

/**
 * Translation from modifier key to the associated property in the event.
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
 */

var modifierKeyToProp = {
  Alt: 'altKey',
  Control: 'ctrlKey',
  Meta: 'metaKey',
  Shift: 'shiftKey'
};

// IE8 does not implement getModifierState so we simply map it to the only
// modifier keys exposed by the event itself, does not support Lock-keys.
// Currently, all major browsers except Chrome seems to support Lock-keys.
function modifierStateGetter(keyArg) {
  var syntheticEvent = this;
  var nativeEvent = syntheticEvent.nativeEvent;
  if (nativeEvent.getModifierState) {
    return nativeEvent.getModifierState(keyArg);
  }
  var keyProp = modifierKeyToProp[keyArg];
  return keyProp ? !!nativeEvent[keyProp] : false;
}

function getEventModifierState(nativeEvent) {
  return modifierStateGetter;
}

/**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var MouseEventInterface = {
  screenX: null,
  screenY: null,
  clientX: null,
  clientY: null,
  pageX: null,
  pageY: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  getModifierState: getEventModifierState,
  button: null,
  buttons: null,
  relatedTarget: function (event) {
    return event.relatedTarget || (event.fromElement === event.srcElement ? event.toElement : event.fromElement);
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticMouseEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticMouseEvent, MouseEventInterface);

var eventTypes$2 = {
  mouseEnter: {
    registrationName: 'onMouseEnter',
    dependencies: ['topMouseOut', 'topMouseOver']
  },
  mouseLeave: {
    registrationName: 'onMouseLeave',
    dependencies: ['topMouseOut', 'topMouseOver']
  }
};

var EnterLeaveEventPlugin = {
  eventTypes: eventTypes$2,

  /**
   * For almost every interaction we care about, there will be both a top-level
   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
   * we do not extract duplicate events. However, moving the mouse into the
   * browser from outside will not fire a `mouseout` event. In this case, we use
   * the `mouseover` top-level event.
   */
  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    if (topLevelType === 'topMouseOver' && (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
      return null;
    }
    if (topLevelType !== 'topMouseOut' && topLevelType !== 'topMouseOver') {
      // Must not be a mouse in or mouse out - ignoring.
      return null;
    }

    var win;
    if (nativeEventTarget.window === nativeEventTarget) {
      // `nativeEventTarget` is probably a window object.
      win = nativeEventTarget;
    } else {
      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
      var doc = nativeEventTarget.ownerDocument;
      if (doc) {
        win = doc.defaultView || doc.parentWindow;
      } else {
        win = window;
      }
    }

    var from;
    var to;
    if (topLevelType === 'topMouseOut') {
      from = targetInst;
      var related = nativeEvent.relatedTarget || nativeEvent.toElement;
      to = related ? getClosestInstanceFromNode(related) : null;
    } else {
      // Moving to a node from outside the window.
      from = null;
      to = targetInst;
    }

    if (from === to) {
      // Nothing pertains to our managed components.
      return null;
    }

    var fromNode = from == null ? win : getNodeFromInstance$1(from);
    var toNode = to == null ? win : getNodeFromInstance$1(to);

    var leave = SyntheticMouseEvent.getPooled(eventTypes$2.mouseLeave, from, nativeEvent, nativeEventTarget);
    leave.type = 'mouseleave';
    leave.target = fromNode;
    leave.relatedTarget = toNode;

    var enter = SyntheticMouseEvent.getPooled(eventTypes$2.mouseEnter, to, nativeEvent, nativeEventTarget);
    enter.type = 'mouseenter';
    enter.target = toNode;
    enter.relatedTarget = fromNode;

    accumulateEnterLeaveDispatches(leave, enter, from, to);

    return [leave, enter];
  }
};

/**
 * `ReactInstanceMap` maintains a mapping from a public facing stateful
 * instance (key) and the internal representation (value). This allows public
 * methods to accept the user facing instance as an argument and map them back
 * to internal methods.
 *
 * Note that this module is currently shared and assumed to be stateless.
 * If this becomes an actual Map, that will break.
 */

/**
 * This API should be called `delete` but we'd have to make sure to always
 * transform these to strings for IE support. When this transform is fully
 * supported we can rename it.
 */


function get(key) {
  return key._reactInternalFiber;
}

function has(key) {
  return key._reactInternalFiber !== undefined;
}

function set(key, value) {
  key._reactInternalFiber = value;
}

var ReactInternals = React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;

var ReactCurrentOwner = ReactInternals.ReactCurrentOwner;
var ReactDebugCurrentFrame = ReactInternals.ReactDebugCurrentFrame;

function getComponentName(fiber) {
  var type = fiber.type;

  if (typeof type === 'string') {
    return type;
  }
  if (typeof type === 'function') {
    return type.displayName || type.name;
  }
  return null;
}

// Don't change these two values:
var NoEffect = 0; //           0b00000000
var PerformedWork = 1; //      0b00000001

// You can change the rest (and add more).
var Placement = 2; //          0b00000010
var Update = 4; //             0b00000100
var PlacementAndUpdate = 6; // 0b00000110
var Deletion = 8; //           0b00001000
var ContentReset = 16; //      0b00010000
var Callback = 32; //          0b00100000
var Err = 64; //               0b01000000
var Ref = 128; //              0b10000000

var MOUNTING = 1;
var MOUNTED = 2;
var UNMOUNTED = 3;

function isFiberMountedImpl(fiber) {
  var node = fiber;
  if (!fiber.alternate) {
    // If there is no alternate, this might be a new tree that isn't inserted
    // yet. If it is, then it will have a pending insertion effect on it.
    if ((node.effectTag & Placement) !== NoEffect) {
      return MOUNTING;
    }
    while (node['return']) {
      node = node['return'];
      if ((node.effectTag & Placement) !== NoEffect) {
        return MOUNTING;
      }
    }
  } else {
    while (node['return']) {
      node = node['return'];
    }
  }
  if (node.tag === HostRoot) {
    // TODO: Check if this was a nested HostRoot when used with
    // renderContainerIntoSubtree.
    return MOUNTED;
  }
  // If we didn't hit the root, that means that we're in an disconnected tree
  // that has been unmounted.
  return UNMOUNTED;
}

function isFiberMounted(fiber) {
  return isFiberMountedImpl(fiber) === MOUNTED;
}

function isMounted(component) {
  {
    var owner = ReactCurrentOwner.current;
    if (owner !== null && owner.tag === ClassComponent) {
      var ownerFiber = owner;
      var instance = ownerFiber.stateNode;
      warning(instance._warnedAboutRefsInRender, '%s is accessing isMounted inside its render() function. ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(ownerFiber) || 'A component');
      instance._warnedAboutRefsInRender = true;
    }
  }

  var fiber = get(component);
  if (!fiber) {
    return false;
  }
  return isFiberMountedImpl(fiber) === MOUNTED;
}

function assertIsMounted(fiber) {
  !(isFiberMountedImpl(fiber) === MOUNTED) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
}

function findCurrentFiberUsingSlowPath(fiber) {
  var alternate = fiber.alternate;
  if (!alternate) {
    // If there is no alternate, then we only need to check if it is mounted.
    var state = isFiberMountedImpl(fiber);
    !(state !== UNMOUNTED) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
    if (state === MOUNTING) {
      return null;
    }
    return fiber;
  }
  // If we have two possible branches, we'll walk backwards up to the root
  // to see what path the root points to. On the way we may hit one of the
  // special cases and we'll deal with them.
  var a = fiber;
  var b = alternate;
  while (true) {
    var parentA = a['return'];
    var parentB = parentA ? parentA.alternate : null;
    if (!parentA || !parentB) {
      // We're at the root.
      break;
    }

    // If both copies of the parent fiber point to the same child, we can
    // assume that the child is current. This happens when we bailout on low
    // priority: the bailed out fiber's child reuses the current child.
    if (parentA.child === parentB.child) {
      var child = parentA.child;
      while (child) {
        if (child === a) {
          // We've determined that A is the current branch.
          assertIsMounted(parentA);
          return fiber;
        }
        if (child === b) {
          // We've determined that B is the current branch.
          assertIsMounted(parentA);
          return alternate;
        }
        child = child.sibling;
      }
      // We should never have an alternate for any mounting node. So the only
      // way this could possibly happen is if this was unmounted, if at all.
      invariant(false, 'Unable to find node on an unmounted component.');
    }

    if (a['return'] !== b['return']) {
      // The return pointer of A and the return pointer of B point to different
      // fibers. We assume that return pointers never criss-cross, so A must
      // belong to the child set of A.return, and B must belong to the child
      // set of B.return.
      a = parentA;
      b = parentB;
    } else {
      // The return pointers point to the same fiber. We'll have to use the
      // default, slow path: scan the child sets of each parent alternate to see
      // which child belongs to which set.
      //
      // Search parent A's child set
      var didFindChild = false;
      var _child = parentA.child;
      while (_child) {
        if (_child === a) {
          didFindChild = true;
          a = parentA;
          b = parentB;
          break;
        }
        if (_child === b) {
          didFindChild = true;
          b = parentA;
          a = parentB;
          break;
        }
        _child = _child.sibling;
      }
      if (!didFindChild) {
        // Search parent B's child set
        _child = parentB.child;
        while (_child) {
          if (_child === a) {
            didFindChild = true;
            a = parentB;
            b = parentA;
            break;
          }
          if (_child === b) {
            didFindChild = true;
            b = parentB;
            a = parentA;
            break;
          }
          _child = _child.sibling;
        }
        !didFindChild ? invariant(false, 'Child was not found in either parent set. This indicates a bug in React related to the return pointer. Please file an issue.') : void 0;
      }
    }

    !(a.alternate === b) ? invariant(false, 'Return fibers should always be each others\' alternates. This error is likely caused by a bug in React. Please file an issue.') : void 0;
  }
  // If the root is not a host container, we're in a disconnected tree. I.e.
  // unmounted.
  !(a.tag === HostRoot) ? invariant(false, 'Unable to find node on an unmounted component.') : void 0;
  if (a.stateNode.current === a) {
    // We've determined that A is the current branch.
    return fiber;
  }
  // Otherwise B has to be current branch.
  return alternate;
}

function findCurrentHostFiber(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);
  if (!currentParent) {
    return null;
  }

  // Next we'll drill down this component to find the first HostComponent/Text.
  var node = currentParent;
  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child) {
      node.child['return'] = node;
      node = node.child;
      continue;
    }
    if (node === currentParent) {
      return null;
    }
    while (!node.sibling) {
      if (!node['return'] || node['return'] === currentParent) {
        return null;
      }
      node = node['return'];
    }
    node.sibling['return'] = node['return'];
    node = node.sibling;
  }
  // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable
  return null;
}

function findCurrentHostFiberWithNoPortals(parent) {
  var currentParent = findCurrentFiberUsingSlowPath(parent);
  if (!currentParent) {
    return null;
  }

  // Next we'll drill down this component to find the first HostComponent/Text.
  var node = currentParent;
  while (true) {
    if (node.tag === HostComponent || node.tag === HostText) {
      return node;
    } else if (node.child && node.tag !== HostPortal) {
      node.child['return'] = node;
      node = node.child;
      continue;
    }
    if (node === currentParent) {
      return null;
    }
    while (!node.sibling) {
      if (!node['return'] || node['return'] === currentParent) {
        return null;
      }
      node = node['return'];
    }
    node.sibling['return'] = node['return'];
    node = node.sibling;
  }
  // Flow needs the return null here, but ESLint complains about it.
  // eslint-disable-next-line no-unreachable
  return null;
}

var CALLBACK_BOOKKEEPING_POOL_SIZE = 10;
var callbackBookkeepingPool = [];

/**
 * Find the deepest React component completely containing the root of the
 * passed-in instance (for use when entire React trees are nested within each
 * other). If React trees are not nested, returns null.
 */
function findRootContainerNode(inst) {
  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
  // traversal, but caching is difficult to do correctly without using a
  // mutation observer to listen for all DOM changes.
  while (inst['return']) {
    inst = inst['return'];
  }
  if (inst.tag !== HostRoot) {
    // This can happen if we're in a detached tree.
    return null;
  }
  return inst.stateNode.containerInfo;
}

// Used to store ancestor hierarchy in top level callback
function getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst) {
  if (callbackBookkeepingPool.length) {
    var instance = callbackBookkeepingPool.pop();
    instance.topLevelType = topLevelType;
    instance.nativeEvent = nativeEvent;
    instance.targetInst = targetInst;
    return instance;
  }
  return {
    topLevelType: topLevelType,
    nativeEvent: nativeEvent,
    targetInst: targetInst,
    ancestors: []
  };
}

function releaseTopLevelCallbackBookKeeping(instance) {
  instance.topLevelType = null;
  instance.nativeEvent = null;
  instance.targetInst = null;
  instance.ancestors.length = 0;
  if (callbackBookkeepingPool.length < CALLBACK_BOOKKEEPING_POOL_SIZE) {
    callbackBookkeepingPool.push(instance);
  }
}

function handleTopLevelImpl(bookKeeping) {
  var targetInst = bookKeeping.targetInst;

  // Loop through the hierarchy, in case there's any nested components.
  // It's important that we build the array of ancestors before calling any
  // event handlers, because event handlers can modify the DOM, leading to
  // inconsistencies with ReactMount's node cache. See #1105.
  var ancestor = targetInst;
  do {
    if (!ancestor) {
      bookKeeping.ancestors.push(ancestor);
      break;
    }
    var root = findRootContainerNode(ancestor);
    if (!root) {
      break;
    }
    bookKeeping.ancestors.push(ancestor);
    ancestor = getClosestInstanceFromNode(root);
  } while (ancestor);

  for (var i = 0; i < bookKeeping.ancestors.length; i++) {
    targetInst = bookKeeping.ancestors[i];
    _handleTopLevel(bookKeeping.topLevelType, targetInst, bookKeeping.nativeEvent, getEventTarget(bookKeeping.nativeEvent));
  }
}

// TODO: can we stop exporting these?
var _enabled = true;
var _handleTopLevel = void 0;

function setHandleTopLevel(handleTopLevel) {
  _handleTopLevel = handleTopLevel;
}

function setEnabled(enabled) {
  _enabled = !!enabled;
}

function isEnabled() {
  return _enabled;
}

/**
 * Traps top-level events by using event bubbling.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {object} element Element on which to attach listener.
 * @return {?object} An object with a remove function which will forcefully
 *                  remove the listener.
 * @internal
 */
function trapBubbledEvent(topLevelType, handlerBaseName, element) {
  if (!element) {
    return null;
  }
  return EventListener.listen(element, handlerBaseName, dispatchEvent.bind(null, topLevelType));
}

/**
 * Traps a top-level event by using event capturing.
 *
 * @param {string} topLevelType Record from `BrowserEventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {object} element Element on which to attach listener.
 * @return {?object} An object with a remove function which will forcefully
 *                  remove the listener.
 * @internal
 */
function trapCapturedEvent(topLevelType, handlerBaseName, element) {
  if (!element) {
    return null;
  }
  return EventListener.capture(element, handlerBaseName, dispatchEvent.bind(null, topLevelType));
}

function dispatchEvent(topLevelType, nativeEvent) {
  if (!_enabled) {
    return;
  }

  var nativeEventTarget = getEventTarget(nativeEvent);
  var targetInst = getClosestInstanceFromNode(nativeEventTarget);
  if (targetInst !== null && typeof targetInst.tag === 'number' && !isFiberMounted(targetInst)) {
    // If we get an event (ex: img onload) before committing that
    // component's mount, ignore it for now (that is, treat it as if it was an
    // event on a non-React tree). We might also consider queueing events and
    // dispatching them after the mount.
    targetInst = null;
  }

  var bookKeeping = getTopLevelCallbackBookKeeping(topLevelType, nativeEvent, targetInst);

  try {
    // Event queue being processed in the same cycle allows
    // `preventDefault`.
    batchedUpdates(handleTopLevelImpl, bookKeeping);
  } finally {
    releaseTopLevelCallbackBookKeeping(bookKeeping);
  }
}

var ReactDOMEventListener = Object.freeze({
	get _enabled () { return _enabled; },
	get _handleTopLevel () { return _handleTopLevel; },
	setHandleTopLevel: setHandleTopLevel,
	setEnabled: setEnabled,
	isEnabled: isEnabled,
	trapBubbledEvent: trapBubbledEvent,
	trapCapturedEvent: trapCapturedEvent,
	dispatchEvent: dispatchEvent
});

/**
 * Generate a mapping of standard vendor prefixes using the defined style property and event name.
 *
 * @param {string} styleProp
 * @param {string} eventName
 * @returns {object}
 */
function makePrefixMap(styleProp, eventName) {
  var prefixes = {};

  prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
  prefixes['Webkit' + styleProp] = 'webkit' + eventName;
  prefixes['Moz' + styleProp] = 'moz' + eventName;
  prefixes['ms' + styleProp] = 'MS' + eventName;
  prefixes['O' + styleProp] = 'o' + eventName.toLowerCase();

  return prefixes;
}

/**
 * A list of event names to a configurable list of vendor prefixes.
 */
var vendorPrefixes = {
  animationend: makePrefixMap('Animation', 'AnimationEnd'),
  animationiteration: makePrefixMap('Animation', 'AnimationIteration'),
  animationstart: makePrefixMap('Animation', 'AnimationStart'),
  transitionend: makePrefixMap('Transition', 'TransitionEnd')
};

/**
 * Event names that have already been detected and prefixed (if applicable).
 */
var prefixedEventNames = {};

/**
 * Element to check for prefixes on.
 */
var style = {};

/**
 * Bootstrap if a DOM exists.
 */
if (ExecutionEnvironment.canUseDOM) {
  style = document.createElement('div').style;

  // On some platforms, in particular some releases of Android 4.x,
  // the un-prefixed "animation" and "transition" properties are defined on the
  // style object but the events that fire will still be prefixed, so we need
  // to check if the un-prefixed events are usable, and if not remove them from the map.
  if (!('AnimationEvent' in window)) {
    delete vendorPrefixes.animationend.animation;
    delete vendorPrefixes.animationiteration.animation;
    delete vendorPrefixes.animationstart.animation;
  }

  // Same as above
  if (!('TransitionEvent' in window)) {
    delete vendorPrefixes.transitionend.transition;
  }
}

/**
 * Attempts to determine the correct vendor prefixed event name.
 *
 * @param {string} eventName
 * @returns {string}
 */
function getVendorPrefixedEventName(eventName) {
  if (prefixedEventNames[eventName]) {
    return prefixedEventNames[eventName];
  } else if (!vendorPrefixes[eventName]) {
    return eventName;
  }

  var prefixMap = vendorPrefixes[eventName];

  for (var styleProp in prefixMap) {
    if (prefixMap.hasOwnProperty(styleProp) && styleProp in style) {
      return prefixedEventNames[eventName] = prefixMap[styleProp];
    }
  }

  return '';
}

/**
 * Types of raw signals from the browser caught at the top level.
 *
 * For events like 'submit' which don't consistently bubble (which we
 * trap at a lower node than `document`), binding at `document` would
 * cause duplicate events so we don't include them here.
 */
var topLevelTypes$1 = {
  topAbort: 'abort',
  topAnimationEnd: getVendorPrefixedEventName('animationend') || 'animationend',
  topAnimationIteration: getVendorPrefixedEventName('animationiteration') || 'animationiteration',
  topAnimationStart: getVendorPrefixedEventName('animationstart') || 'animationstart',
  topBlur: 'blur',
  topCancel: 'cancel',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topChange: 'change',
  topClick: 'click',
  topClose: 'close',
  topCompositionEnd: 'compositionend',
  topCompositionStart: 'compositionstart',
  topCompositionUpdate: 'compositionupdate',
  topContextMenu: 'contextmenu',
  topCopy: 'copy',
  topCut: 'cut',
  topDoubleClick: 'dblclick',
  topDrag: 'drag',
  topDragEnd: 'dragend',
  topDragEnter: 'dragenter',
  topDragExit: 'dragexit',
  topDragLeave: 'dragleave',
  topDragOver: 'dragover',
  topDragStart: 'dragstart',
  topDrop: 'drop',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topFocus: 'focus',
  topInput: 'input',
  topKeyDown: 'keydown',
  topKeyPress: 'keypress',
  topKeyUp: 'keyup',
  topLoadedData: 'loadeddata',
  topLoad: 'load',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topMouseDown: 'mousedown',
  topMouseMove: 'mousemove',
  topMouseOut: 'mouseout',
  topMouseOver: 'mouseover',
  topMouseUp: 'mouseup',
  topPaste: 'paste',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topScroll: 'scroll',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topSelectionChange: 'selectionchange',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTextInput: 'textInput',
  topTimeUpdate: 'timeupdate',
  topToggle: 'toggle',
  topTouchCancel: 'touchcancel',
  topTouchEnd: 'touchend',
  topTouchMove: 'touchmove',
  topTouchStart: 'touchstart',
  topTransitionEnd: getVendorPrefixedEventName('transitionend') || 'transitionend',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting',
  topWheel: 'wheel'
};

var BrowserEventConstants = {
  topLevelTypes: topLevelTypes$1
};

function runEventQueueInBatch(events) {
  enqueueEvents(events);
  processEventQueue(false);
}

/**
 * Streams a fired top-level event to `EventPluginHub` where plugins have the
 * opportunity to create `ReactEvent`s to be dispatched.
 */
function handleTopLevel(topLevelType, targetInst, nativeEvent, nativeEventTarget) {
  var events = extractEvents(topLevelType, targetInst, nativeEvent, nativeEventTarget);
  runEventQueueInBatch(events);
}

var topLevelTypes = BrowserEventConstants.topLevelTypes;

/**
 * Summary of `ReactBrowserEventEmitter` event handling:
 *
 *  - Top-level delegation is used to trap most native browser events. This
 *    may only occur in the main thread and is the responsibility of
 *    ReactDOMEventListener, which is injected and can therefore support
 *    pluggable event sources. This is the only work that occurs in the main
 *    thread.
 *
 *  - We normalize and de-duplicate events to account for browser quirks. This
 *    may be done in the worker thread.
 *
 *  - Forward these native events (with the associated top-level type used to
 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
 *    to extract any synthetic events.
 *
 *  - The `EventPluginHub` will then process each event by annotating them with
 *    "dispatches", a sequence of listeners and IDs that care about that event.
 *
 *  - The `EventPluginHub` then dispatches the events.
 *
 * Overview of React and the event system:
 *
 * +------------+    .
 * |    DOM     |    .
 * +------------+    .
 *       |           .
 *       v           .
 * +------------+    .
 * | ReactEvent |    .
 * |  Listener  |    .
 * +------------+    .                         +-----------+
 *       |           .               +--------+|SimpleEvent|
 *       |           .               |         |Plugin     |
 * +-----|------+    .               v         +-----------+
 * |     |      |    .    +--------------+                    +------------+
 * |     +-----------.--->|EventPluginHub|                    |    Event   |
 * |            |    .    |              |     +-----------+  | Propagators|
 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
 * |            |    .    |              |     +-----------+  |  utilities |
 * |     +-----------.--->|              |                    +------------+
 * |     |      |    .    +--------------+
 * +-----|------+    .                ^        +-----------+
 *       |           .                |        |Enter/Leave|
 *       +           .                +-------+|Plugin     |
 * +-------------+   .                         +-----------+
 * | application |   .
 * |-------------|   .
 * |             |   .
 * |             |   .
 * +-------------+   .
 *                   .
 *    React Core     .  General Purpose Event Plugin System
 */

var alreadyListeningTo = {};
var reactTopListenersCounter = 0;

/**
 * To ensure no conflicts with other potential React instances on the page
 */
var topListenersIDKey = '_reactListenersID' + ('' + Math.random()).slice(2);

function getListeningForDocument(mountAt) {
  // In IE8, `mountAt` is a host object and doesn't have `hasOwnProperty`
  // directly.
  if (!Object.prototype.hasOwnProperty.call(mountAt, topListenersIDKey)) {
    mountAt[topListenersIDKey] = reactTopListenersCounter++;
    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
  }
  return alreadyListeningTo[mountAt[topListenersIDKey]];
}

/**
 * We listen for bubbled touch events on the document object.
 *
 * Firefox v8.01 (and possibly others) exhibited strange behavior when
 * mounting `onmousemove` events at some node that was not the document
 * element. The symptoms were that if your mouse is not moving over something
 * contained within that mount point (for example on the background) the
 * top-level listeners for `onmousemove` won't be called. However, if you
 * register the `mousemove` on the document object, then it will of course
 * catch all `mousemove`s. This along with iOS quirks, justifies restricting
 * top-level listeners to the document object only, at least for these
 * movement types of events and possibly all events.
 *
 * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
 *
 * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
 * they bubble to document.
 *
 * @param {string} registrationName Name of listener (e.g. `onClick`).
 * @param {object} contentDocumentHandle Document which owns the container
 */
function listenTo(registrationName, contentDocumentHandle) {
  var mountAt = contentDocumentHandle;
  var isListening = getListeningForDocument(mountAt);
  var dependencies = registrationNameDependencies[registrationName];

  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
      if (dependency === 'topScroll') {
        trapCapturedEvent('topScroll', 'scroll', mountAt);
      } else if (dependency === 'topFocus' || dependency === 'topBlur') {
        trapCapturedEvent('topFocus', 'focus', mountAt);
        trapCapturedEvent('topBlur', 'blur', mountAt);

        // to make sure blur and focus event listeners are only attached once
        isListening.topBlur = true;
        isListening.topFocus = true;
      } else if (dependency === 'topCancel') {
        if (isEventSupported('cancel', true)) {
          trapCapturedEvent('topCancel', 'cancel', mountAt);
        }
        isListening.topCancel = true;
      } else if (dependency === 'topClose') {
        if (isEventSupported('close', true)) {
          trapCapturedEvent('topClose', 'close', mountAt);
        }
        isListening.topClose = true;
      } else if (topLevelTypes.hasOwnProperty(dependency)) {
        trapBubbledEvent(dependency, topLevelTypes[dependency], mountAt);
      }

      isListening[dependency] = true;
    }
  }
}

function isListeningToAllDependencies(registrationName, mountAt) {
  var isListening = getListeningForDocument(mountAt);
  var dependencies = registrationNameDependencies[registrationName];
  for (var i = 0; i < dependencies.length; i++) {
    var dependency = dependencies[i];
    if (!(isListening.hasOwnProperty(dependency) && isListening[dependency])) {
      return false;
    }
  }
  return true;
}

/**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */
function getLeafNode(node) {
  while (node && node.firstChild) {
    node = node.firstChild;
  }
  return node;
}

/**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */
function getSiblingNode(node) {
  while (node) {
    if (node.nextSibling) {
      return node.nextSibling;
    }
    node = node.parentNode;
  }
}

/**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */
function getNodeForCharacterOffset(root, offset) {
  var node = getLeafNode(root);
  var nodeStart = 0;
  var nodeEnd = 0;

  while (node) {
    if (node.nodeType === TEXT_NODE) {
      nodeEnd = nodeStart + node.textContent.length;

      if (nodeStart <= offset && nodeEnd >= offset) {
        return {
          node: node,
          offset: offset - nodeStart
        };
      }

      nodeStart = nodeEnd;
    }

    node = getLeafNode(getSiblingNode(node));
  }
}

/**
 * @param {DOMElement} outerNode
 * @return {?object}
 */
function getOffsets(outerNode) {
  var selection = window.getSelection && window.getSelection();

  if (!selection || selection.rangeCount === 0) {
    return null;
  }

  var anchorNode = selection.anchorNode,
      anchorOffset = selection.anchorOffset,
      focusNode$$1 = selection.focusNode,
      focusOffset = selection.focusOffset;

  // In Firefox, anchorNode and focusNode can be "anonymous divs", e.g. the
  // up/down buttons on an <input type="number">. Anonymous divs do not seem to
  // expose properties, triggering a "Permission denied error" if any of its
  // properties are accessed. The only seemingly possible way to avoid erroring
  // is to access a property that typically works for non-anonymous divs and
  // catch any error that may otherwise arise. See
  // https://bugzilla.mozilla.org/show_bug.cgi?id=208427

  try {
    /* eslint-disable no-unused-expressions */
    anchorNode.nodeType;
    focusNode$$1.nodeType;
    /* eslint-enable no-unused-expressions */
  } catch (e) {
    return null;
  }

  return getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode$$1, focusOffset);
}

/**
 * Returns {start, end} where `start` is the character/codepoint index of
 * (anchorNode, anchorOffset) within the textContent of `outerNode`, and
 * `end` is the index of (focusNode, focusOffset).
 *
 * Returns null if you pass in garbage input but we should probably just crash.
 *
 * Exported only for testing.
 */
function getModernOffsetsFromPoints(outerNode, anchorNode, anchorOffset, focusNode$$1, focusOffset) {
  var length = 0;
  var start = -1;
  var end = -1;
  var indexWithinAnchor = 0;
  var indexWithinFocus = 0;
  var node = outerNode;
  var parentNode = null;

  outer: while (true) {
    var next = null;

    while (true) {
      if (node === anchorNode && (anchorOffset === 0 || node.nodeType === TEXT_NODE)) {
        start = length + anchorOffset;
      }
      if (node === focusNode$$1 && (focusOffset === 0 || node.nodeType === TEXT_NODE)) {
        end = length + focusOffset;
      }

      if (node.nodeType === TEXT_NODE) {
        length += node.nodeValue.length;
      }

      if ((next = node.firstChild) === null) {
        break;
      }
      // Moving from `node` to its first child `next`.
      parentNode = node;
      node = next;
    }

    while (true) {
      if (node === outerNode) {
        // If `outerNode` has children, this is always the second time visiting
        // it. If it has no children, this is still the first loop, and the only
        // valid selection is anchorNode and focusNode both equal to this node
        // and both offsets 0, in which case we will have handled above.
        break outer;
      }
      if (parentNode === anchorNode && ++indexWithinAnchor === anchorOffset) {
        start = length;
      }
      if (parentNode === focusNode$$1 && ++indexWithinFocus === focusOffset) {
        end = length;
      }
      if ((next = node.nextSibling) !== null) {
        break;
      }
      node = parentNode;
      parentNode = node.parentNode;
    }

    // Moving from `node` to its next sibling `next`.
    node = next;
  }

  if (start === -1 || end === -1) {
    // This should never happen. (Would happen if the anchor/focus nodes aren't
    // actually inside the passed-in node.)
    return null;
  }

  return {
    start: start,
    end: end
  };
}

/**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programmatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setOffsets(node, offsets) {
  if (!window.getSelection) {
    return;
  }

  var selection = window.getSelection();
  var length = node[getTextContentAccessor()].length;
  var start = Math.min(offsets.start, length);
  var end = offsets.end === undefined ? start : Math.min(offsets.end, length);

  // IE 11 uses modern selection, but doesn't support the extend method.
  // Flip backward selections, so we can set with a single range.
  if (!selection.extend && start > end) {
    var temp = end;
    end = start;
    start = temp;
  }

  var startMarker = getNodeForCharacterOffset(node, start);
  var endMarker = getNodeForCharacterOffset(node, end);

  if (startMarker && endMarker) {
    if (selection.rangeCount === 1 && selection.anchorNode === startMarker.node && selection.anchorOffset === startMarker.offset && selection.focusNode === endMarker.node && selection.focusOffset === endMarker.offset) {
      return;
    }
    var range = document.createRange();
    range.setStart(startMarker.node, startMarker.offset);
    selection.removeAllRanges();

    if (start > end) {
      selection.addRange(range);
      selection.extend(endMarker.node, endMarker.offset);
    } else {
      range.setEnd(endMarker.node, endMarker.offset);
      selection.addRange(range);
    }
  }
}

function isInDocument(node) {
  return containsNode(document.documentElement, node);
}

/**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */

function hasSelectionCapabilities(elem) {
  var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
  return nodeName && (nodeName === 'input' && elem.type === 'text' || nodeName === 'textarea' || elem.contentEditable === 'true');
}

function getSelectionInformation() {
  var focusedElem = getActiveElement();
  return {
    focusedElem: focusedElem,
    selectionRange: hasSelectionCapabilities(focusedElem) ? getSelection$1(focusedElem) : null
  };
}

/**
 * @restoreSelection: If any selection information was potentially lost,
 * restore it. This is useful when performing operations that could remove dom
 * nodes and place them back in, resulting in focus being lost.
 */
function restoreSelection(priorSelectionInformation) {
  var curFocusedElem = getActiveElement();
  var priorFocusedElem = priorSelectionInformation.focusedElem;
  var priorSelectionRange = priorSelectionInformation.selectionRange;
  if (curFocusedElem !== priorFocusedElem && isInDocument(priorFocusedElem)) {
    if (hasSelectionCapabilities(priorFocusedElem)) {
      setSelection(priorFocusedElem, priorSelectionRange);
    }

    // Focusing a node can change the scroll position, which is undesirable
    var ancestors = [];
    var ancestor = priorFocusedElem;
    while (ancestor = ancestor.parentNode) {
      if (ancestor.nodeType === ELEMENT_NODE) {
        ancestors.push({
          element: ancestor,
          left: ancestor.scrollLeft,
          top: ancestor.scrollTop
        });
      }
    }

    focusNode(priorFocusedElem);

    for (var i = 0; i < ancestors.length; i++) {
      var info = ancestors[i];
      info.element.scrollLeft = info.left;
      info.element.scrollTop = info.top;
    }
  }
}

/**
 * @getSelection: Gets the selection bounds of a focused textarea, input or
 * contentEditable node.
 * -@input: Look up selection bounds of this input
 * -@return {start: selectionStart, end: selectionEnd}
 */
function getSelection$1(input) {
  var selection = void 0;

  if ('selectionStart' in input) {
    // Modern browser with input or textarea.
    selection = {
      start: input.selectionStart,
      end: input.selectionEnd
    };
  } else {
    // Content editable or old IE textarea.
    selection = getOffsets(input);
  }

  return selection || { start: 0, end: 0 };
}

/**
 * @setSelection: Sets the selection bounds of a textarea or input and focuses
 * the input.
 * -@input     Set selection bounds of this input or textarea
 * -@offsets   Object of same form that is returned from get*
 */
function setSelection(input, offsets) {
  var start = offsets.start,
      end = offsets.end;

  if (end === undefined) {
    end = start;
  }

  if ('selectionStart' in input) {
    input.selectionStart = start;
    input.selectionEnd = Math.min(end, input.value.length);
  } else {
    setOffsets(input, offsets);
  }
}

var skipSelectionChangeEvent = ExecutionEnvironment.canUseDOM && 'documentMode' in document && document.documentMode <= 11;

var eventTypes$3 = {
  select: {
    phasedRegistrationNames: {
      bubbled: 'onSelect',
      captured: 'onSelectCapture'
    },
    dependencies: ['topBlur', 'topContextMenu', 'topFocus', 'topKeyDown', 'topKeyUp', 'topMouseDown', 'topMouseUp', 'topSelectionChange']
  }
};

var activeElement$1 = null;
var activeElementInst$1 = null;
var lastSelection = null;
var mouseDown = false;

/**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 *
 * @param {DOMElement} node
 * @return {object}
 */
function getSelection(node) {
  if ('selectionStart' in node && hasSelectionCapabilities(node)) {
    return {
      start: node.selectionStart,
      end: node.selectionEnd
    };
  } else if (window.getSelection) {
    var selection = window.getSelection();
    return {
      anchorNode: selection.anchorNode,
      anchorOffset: selection.anchorOffset,
      focusNode: selection.focusNode,
      focusOffset: selection.focusOffset
    };
  }
}

/**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @return {?SyntheticEvent}
 */
function constructSelectEvent(nativeEvent, nativeEventTarget) {
  // Ensure we have the right element, and that the user is not dragging a
  // selection (this matches native `select` event behavior). In HTML5, select
  // fires only on input and textarea thus if there's no focused element we
  // won't dispatch.
  if (mouseDown || activeElement$1 == null || activeElement$1 !== getActiveElement()) {
    return null;
  }

  // Only fire when selection has actually changed.
  var currentSelection = getSelection(activeElement$1);
  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
    lastSelection = currentSelection;

    var syntheticEvent = SyntheticEvent$1.getPooled(eventTypes$3.select, activeElementInst$1, nativeEvent, nativeEventTarget);

    syntheticEvent.type = 'select';
    syntheticEvent.target = activeElement$1;

    accumulateTwoPhaseDispatches(syntheticEvent);

    return syntheticEvent;
  }

  return null;
}

/**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */
var SelectEventPlugin = {
  eventTypes: eventTypes$3,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : nativeEventTarget.nodeType === DOCUMENT_NODE ? nativeEventTarget : nativeEventTarget.ownerDocument;
    // Track whether all listeners exists for this plugin. If none exist, we do
    // not extract events. See #3639.
    if (!doc || !isListeningToAllDependencies('onSelect', doc)) {
      return null;
    }

    var targetNode = targetInst ? getNodeFromInstance$1(targetInst) : window;

    switch (topLevelType) {
      // Track the input node that has focus.
      case 'topFocus':
        if (isTextInputElement(targetNode) || targetNode.contentEditable === 'true') {
          activeElement$1 = targetNode;
          activeElementInst$1 = targetInst;
          lastSelection = null;
        }
        break;
      case 'topBlur':
        activeElement$1 = null;
        activeElementInst$1 = null;
        lastSelection = null;
        break;
      // Don't fire the event while the user is dragging. This matches the
      // semantics of the native select event.
      case 'topMouseDown':
        mouseDown = true;
        break;
      case 'topContextMenu':
      case 'topMouseUp':
        mouseDown = false;
        return constructSelectEvent(nativeEvent, nativeEventTarget);
      // Chrome and IE fire non-standard event when selection is changed (and
      // sometimes when it hasn't). IE's event fires out of order with respect
      // to key and input events on deletion, so we discard it.
      //
      // Firefox doesn't support selectionchange, so check selection status
      // after each key entry. The selection changes after keydown and before
      // keyup, but we check on keydown as well in the case of holding down a
      // key, when multiple keydown events are fired but only one keyup is.
      // This is also our approach for IE handling, for the reason above.
      case 'topSelectionChange':
        if (skipSelectionChangeEvent) {
          break;
        }
      // falls through
      case 'topKeyDown':
      case 'topKeyUp':
        return constructSelectEvent(nativeEvent, nativeEventTarget);
    }

    return null;
  }
};

/**
 * @interface Event
 * @see http://www.w3.org/TR/css3-animations/#AnimationEvent-interface
 * @see https://developer.mozilla.org/en-US/docs/Web/API/AnimationEvent
 */
var AnimationEventInterface = {
  animationName: null,
  elapsedTime: null,
  pseudoElement: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticAnimationEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticAnimationEvent, AnimationEventInterface);

/**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */
var ClipboardEventInterface = {
  clipboardData: function (event) {
    return 'clipboardData' in event ? event.clipboardData : window.clipboardData;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticClipboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticClipboardEvent, ClipboardEventInterface);

/**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var FocusEventInterface = {
  relatedTarget: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticFocusEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticFocusEvent, FocusEventInterface);

/**
 * `charCode` represents the actual "character code" and is safe to use with
 * `String.fromCharCode`. As such, only keys that correspond to printable
 * characters produce a valid `charCode`, the only exception to this is Enter.
 * The Tab-key is considered non-printable and does not have a `charCode`,
 * presumably because it does not produce a tab-character in browsers.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {number} Normalized `charCode` property.
 */
function getEventCharCode(nativeEvent) {
  var charCode;
  var keyCode = nativeEvent.keyCode;

  if ('charCode' in nativeEvent) {
    charCode = nativeEvent.charCode;

    // FF does not set `charCode` for the Enter-key, check against `keyCode`.
    if (charCode === 0 && keyCode === 13) {
      charCode = 13;
    }
  } else {
    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
    charCode = keyCode;
  }

  // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
  // Must not discard the (non-)printable Enter-key.
  if (charCode >= 32 || charCode === 13) {
    return charCode;
  }

  return 0;
}

/**
 * Normalization of deprecated HTML5 `key` values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var normalizeKey = {
  Esc: 'Escape',
  Spacebar: ' ',
  Left: 'ArrowLeft',
  Up: 'ArrowUp',
  Right: 'ArrowRight',
  Down: 'ArrowDown',
  Del: 'Delete',
  Win: 'OS',
  Menu: 'ContextMenu',
  Apps: 'ContextMenu',
  Scroll: 'ScrollLock',
  MozPrintableKey: 'Unidentified'
};

/**
 * Translation from legacy `keyCode` to HTML5 `key`
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var translateToKey = {
  '8': 'Backspace',
  '9': 'Tab',
  '12': 'Clear',
  '13': 'Enter',
  '16': 'Shift',
  '17': 'Control',
  '18': 'Alt',
  '19': 'Pause',
  '20': 'CapsLock',
  '27': 'Escape',
  '32': ' ',
  '33': 'PageUp',
  '34': 'PageDown',
  '35': 'End',
  '36': 'Home',
  '37': 'ArrowLeft',
  '38': 'ArrowUp',
  '39': 'ArrowRight',
  '40': 'ArrowDown',
  '45': 'Insert',
  '46': 'Delete',
  '112': 'F1',
  '113': 'F2',
  '114': 'F3',
  '115': 'F4',
  '116': 'F5',
  '117': 'F6',
  '118': 'F7',
  '119': 'F8',
  '120': 'F9',
  '121': 'F10',
  '122': 'F11',
  '123': 'F12',
  '144': 'NumLock',
  '145': 'ScrollLock',
  '224': 'Meta'
};

/**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */
function getEventKey(nativeEvent) {
  if (nativeEvent.key) {
    // Normalize inconsistent values reported by browsers due to
    // implementations of a working draft specification.

    // FireFox implements `key` but returns `MozPrintableKey` for all
    // printable characters (normalized to `Unidentified`), ignore it.
    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
    if (key !== 'Unidentified') {
      return key;
    }
  }

  // Browser does not implement `key`, polyfill as much of it as we can.
  if (nativeEvent.type === 'keypress') {
    var charCode = getEventCharCode(nativeEvent);

    // The enter-key is technically both printable and non-printable and can
    // thus be captured by `keypress`, no other non-printable key should.
    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
  }
  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
    // While user keyboard layout determines the actual meaning of each
    // `keyCode` value, almost all function keys have a universal value.
    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
  }
  return '';
}

/**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var KeyboardEventInterface = {
  key: getEventKey,
  location: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  repeat: null,
  locale: null,
  getModifierState: getEventModifierState,
  // Legacy Interface
  charCode: function (event) {
    // `charCode` is the result of a KeyPress event and represents the value of
    // the actual printable character.

    // KeyPress is deprecated, but its replacement is not yet final and not
    // implemented in any major browser. Only KeyPress has charCode.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    return 0;
  },
  keyCode: function (event) {
    // `keyCode` is the result of a KeyDown/Up event and represents the value of
    // physical keyboard key.

    // The actual meaning of the value depends on the users' keyboard layout
    // which cannot be detected. Assuming that it is a US keyboard layout
    // provides a surprisingly accurate mapping for US and European users.
    // Due to this, it is left to the user to implement at this time.
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  },
  which: function (event) {
    // `which` is an alias for either `keyCode` or `charCode` depending on the
    // type of the event.
    if (event.type === 'keypress') {
      return getEventCharCode(event);
    }
    if (event.type === 'keydown' || event.type === 'keyup') {
      return event.keyCode;
    }
    return 0;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticKeyboardEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticKeyboardEvent, KeyboardEventInterface);

/**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var DragEventInterface = {
  dataTransfer: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticMouseEvent}
 */
function SyntheticDragEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticMouseEvent.augmentClass(SyntheticDragEvent, DragEventInterface);

/**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */
var TouchEventInterface = {
  touches: null,
  targetTouches: null,
  changedTouches: null,
  altKey: null,
  metaKey: null,
  ctrlKey: null,
  shiftKey: null,
  getModifierState: getEventModifierState
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticTouchEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticUIEvent.augmentClass(SyntheticTouchEvent, TouchEventInterface);

/**
 * @interface Event
 * @see http://www.w3.org/TR/2009/WD-css3-transitions-20090320/#transition-events-
 * @see https://developer.mozilla.org/en-US/docs/Web/API/TransitionEvent
 */
var TransitionEventInterface = {
  propertyName: null,
  elapsedTime: null,
  pseudoElement: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticTransitionEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticEvent$1.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticEvent$1.augmentClass(SyntheticTransitionEvent, TransitionEventInterface);

/**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var WheelEventInterface = {
  deltaX: function (event) {
    return 'deltaX' in event ? event.deltaX : // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
    'wheelDeltaX' in event ? -event.wheelDeltaX : 0;
  },
  deltaY: function (event) {
    return 'deltaY' in event ? event.deltaY : // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
    'wheelDeltaY' in event ? -event.wheelDeltaY : // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
    'wheelDelta' in event ? -event.wheelDelta : 0;
  },
  deltaZ: null,

  // Browsers without "deltaMode" is reporting in raw wheel delta where one
  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
  deltaMode: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticMouseEvent}
 */
function SyntheticWheelEvent(dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget) {
  return SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent, nativeEventTarget);
}

SyntheticMouseEvent.augmentClass(SyntheticWheelEvent, WheelEventInterface);

/**
 * Turns
 * ['abort', ...]
 * into
 * eventTypes = {
 *   'abort': {
 *     phasedRegistrationNames: {
 *       bubbled: 'onAbort',
 *       captured: 'onAbortCapture',
 *     },
 *     dependencies: ['topAbort'],
 *   },
 *   ...
 * };
 * topLevelEventsToDispatchConfig = {
 *   'topAbort': { sameConfig }
 * };
 */
var eventTypes$4 = {};
var topLevelEventsToDispatchConfig = {};
['abort', 'animationEnd', 'animationIteration', 'animationStart', 'blur', 'cancel', 'canPlay', 'canPlayThrough', 'click', 'close', 'contextMenu', 'copy', 'cut', 'doubleClick', 'drag', 'dragEnd', 'dragEnter', 'dragExit', 'dragLeave', 'dragOver', 'dragStart', 'drop', 'durationChange', 'emptied', 'encrypted', 'ended', 'error', 'focus', 'input', 'invalid', 'keyDown', 'keyPress', 'keyUp', 'load', 'loadedData', 'loadedMetadata', 'loadStart', 'mouseDown', 'mouseMove', 'mouseOut', 'mouseOver', 'mouseUp', 'paste', 'pause', 'play', 'playing', 'progress', 'rateChange', 'reset', 'scroll', 'seeked', 'seeking', 'stalled', 'submit', 'suspend', 'timeUpdate', 'toggle', 'touchCancel', 'touchEnd', 'touchMove', 'touchStart', 'transitionEnd', 'volumeChange', 'waiting', 'wheel'].forEach(function (event) {
  var capitalizedEvent = event[0].toUpperCase() + event.slice(1);
  var onEvent = 'on' + capitalizedEvent;
  var topEvent = 'top' + capitalizedEvent;

  var type = {
    phasedRegistrationNames: {
      bubbled: onEvent,
      captured: onEvent + 'Capture'
    },
    dependencies: [topEvent]
  };
  eventTypes$4[event] = type;
  topLevelEventsToDispatchConfig[topEvent] = type;
});

// Only used in DEV for exhaustiveness validation.
var knownHTMLTopLevelTypes = ['topAbort', 'topCancel', 'topCanPlay', 'topCanPlayThrough', 'topClose', 'topDurationChange', 'topEmptied', 'topEncrypted', 'topEnded', 'topError', 'topInput', 'topInvalid', 'topLoad', 'topLoadedData', 'topLoadedMetadata', 'topLoadStart', 'topPause', 'topPlay', 'topPlaying', 'topProgress', 'topRateChange', 'topReset', 'topSeeked', 'topSeeking', 'topStalled', 'topSubmit', 'topSuspend', 'topTimeUpdate', 'topToggle', 'topVolumeChange', 'topWaiting'];

var SimpleEventPlugin = {
  eventTypes: eventTypes$4,

  extractEvents: function (topLevelType, targetInst, nativeEvent, nativeEventTarget) {
    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
    if (!dispatchConfig) {
      return null;
    }
    var EventConstructor;
    switch (topLevelType) {
      case 'topKeyPress':
        // Firefox creates a keypress event for function keys too. This removes
        // the unwanted keypress events. Enter is however both printable and
        // non-printable. One would expect Tab to be as well (but it isn't).
        if (getEventCharCode(nativeEvent) === 0) {
          return null;
        }
      /* falls through */
      case 'topKeyDown':
      case 'topKeyUp':
        EventConstructor = SyntheticKeyboardEvent;
        break;
      case 'topBlur':
      case 'topFocus':
        EventConstructor = SyntheticFocusEvent;
        break;
      case 'topClick':
        // Firefox creates a click event on right mouse clicks. This removes the
        // unwanted click events.
        if (nativeEvent.button === 2) {
          return null;
        }
      /* falls through */
      case 'topDoubleClick':
      case 'topMouseDown':
      case 'topMouseMove':
      case 'topMouseUp':
      // TODO: Disabled elements should not respond to mouse events
      /* falls through */
      case 'topMouseOut':
      case 'topMouseOver':
      case 'topContextMenu':
        EventConstructor = SyntheticMouseEvent;
        break;
      case 'topDrag':
      case 'topDragEnd':
      case 'topDragEnter':
      case 'topDragExit':
      case 'topDragLeave':
      case 'topDragOver':
      case 'topDragStart':
      case 'topDrop':
        EventConstructor = SyntheticDragEvent;
        break;
      case 'topTouchCancel':
      case 'topTouchEnd':
      case 'topTouchMove':
      case 'topTouchStart':
        EventConstructor = SyntheticTouchEvent;
        break;
      case 'topAnimationEnd':
      case 'topAnimationIteration':
      case 'topAnimationStart':
        EventConstructor = SyntheticAnimationEvent;
        break;
      case 'topTransitionEnd':
        EventConstructor = SyntheticTransitionEvent;
        break;
      case 'topScroll':
        EventConstructor = SyntheticUIEvent;
        break;
      case 'topWheel':
        EventConstructor = SyntheticWheelEvent;
        break;
      case 'topCopy':
      case 'topCut':
      case 'topPaste':
        EventConstructor = SyntheticClipboardEvent;
        break;
      default:
        {
          if (knownHTMLTopLevelTypes.indexOf(topLevelType) === -1) {
            warning(false, 'SimpleEventPlugin: Unhandled event type, `%s`. This warning ' + 'is likely caused by a bug in React. Please file an issue.', topLevelType);
          }
        }
        // HTML Events
        // @see http://www.w3.org/TR/html5/index.html#events-0
        EventConstructor = SyntheticEvent$1;
        break;
    }
    var event = EventConstructor.getPooled(dispatchConfig, targetInst, nativeEvent, nativeEventTarget);
    accumulateTwoPhaseDispatches(event);
    return event;
  }
};

setHandleTopLevel(handleTopLevel);

/**
 * Inject modules for resolving DOM hierarchy and plugin ordering.
 */
injection$1.injectEventPluginOrder(DOMEventPluginOrder);
injection$2.injectComponentTree(ReactDOMComponentTree);

/**
 * Some important event plugins included by default (without having to require
 * them).
 */
injection$1.injectEventPluginsByName({
  SimpleEventPlugin: SimpleEventPlugin,
  EnterLeaveEventPlugin: EnterLeaveEventPlugin,
  ChangeEventPlugin: ChangeEventPlugin,
  SelectEventPlugin: SelectEventPlugin,
  BeforeInputEventPlugin: BeforeInputEventPlugin
});

var enableAsyncSubtreeAPI = true;
var enableAsyncSchedulingByDefaultInReactDOM = false;
// Exports ReactDOM.createRoot
var enableCreateRoot = false;
var enableUserTimingAPI = true;

// Mutating mode (React DOM, React ART, React Native):
var enableMutatingReconciler = true;
// Experimental noop mode (currently unused):
var enableNoopReconciler = false;
// Experimental persistent mode (CS):
var enablePersistentReconciler = false;

// Helps identify side effects in begin-phase lifecycle hooks and setState reducers:
var debugRenderPhaseSideEffects = false;

// Only used in www builds.

var valueStack = [];

{
  var fiberStack = [];
}

var index = -1;

function createCursor(defaultValue) {
  return {
    current: defaultValue
  };
}



function pop(cursor, fiber) {
  if (index < 0) {
    {
      warning(false, 'Unexpected pop.');
    }
    return;
  }

  {
    if (fiber !== fiberStack[index]) {
      warning(false, 'Unexpected Fiber popped.');
    }
  }

  cursor.current = valueStack[index];

  valueStack[index] = null;

  {
    fiberStack[index] = null;
  }

  index--;
}

function push(cursor, value, fiber) {
  index++;

  valueStack[index] = cursor.current;

  {
    fiberStack[index] = fiber;
  }

  cursor.current = value;
}

function reset$1() {
  while (index > -1) {
    valueStack[index] = null;

    {
      fiberStack[index] = null;
    }

    index--;
  }
}

var describeComponentFrame = function (name, source, ownerName) {
  return '\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');
};

function describeFiber(fiber) {
  switch (fiber.tag) {
    case IndeterminateComponent:
    case FunctionalComponent:
    case ClassComponent:
    case HostComponent:
      var owner = fiber._debugOwner;
      var source = fiber._debugSource;
      var name = getComponentName(fiber);
      var ownerName = null;
      if (owner) {
        ownerName = getComponentName(owner);
      }
      return describeComponentFrame(name, source, ownerName);
    default:
      return '';
  }
}

// This function can only be called with a work-in-progress fiber and
// only during begin or complete phase. Do not call it under any other
// circumstances.
function getStackAddendumByWorkInProgressFiber(workInProgress) {
  var info = '';
  var node = workInProgress;
  do {
    info += describeFiber(node);
    // Otherwise this return pointer might point to the wrong tree:
    node = node['return'];
  } while (node);
  return info;
}

function getCurrentFiberOwnerName() {
  {
    var fiber = ReactDebugCurrentFiber.current;
    if (fiber === null) {
      return null;
    }
    var owner = fiber._debugOwner;
    if (owner !== null && typeof owner !== 'undefined') {
      return getComponentName(owner);
    }
  }
  return null;
}

function getCurrentFiberStackAddendum() {
  {
    var fiber = ReactDebugCurrentFiber.current;
    if (fiber === null) {
      return null;
    }
    // Safe because if current fiber exists, we are reconciling,
    // and it is guaranteed to be the work-in-progress version.
    return getStackAddendumByWorkInProgressFiber(fiber);
  }
  return null;
}

function resetCurrentFiber() {
  ReactDebugCurrentFrame.getCurrentStack = null;
  ReactDebugCurrentFiber.current = null;
  ReactDebugCurrentFiber.phase = null;
}

function setCurrentFiber(fiber) {
  ReactDebugCurrentFrame.getCurrentStack = getCurrentFiberStackAddendum;
  ReactDebugCurrentFiber.current = fiber;
  ReactDebugCurrentFiber.phase = null;
}

function setCurrentPhase(phase) {
  ReactDebugCurrentFiber.phase = phase;
}

var ReactDebugCurrentFiber = {
  current: null,
  phase: null,
  resetCurrentFiber: resetCurrentFiber,
  setCurrentFiber: setCurrentFiber,
  setCurrentPhase: setCurrentPhase,
  getCurrentFiberOwnerName: getCurrentFiberOwnerName,
  getCurrentFiberStackAddendum: getCurrentFiberStackAddendum
};

// Prefix measurements so that it's possible to filter them.
// Longer prefixes are hard to read in DevTools.
var reactEmoji = '\u269B';
var warningEmoji = '\u26D4';
var supportsUserTiming = typeof performance !== 'undefined' && typeof performance.mark === 'function' && typeof performance.clearMarks === 'function' && typeof performance.measure === 'function' && typeof performance.clearMeasures === 'function';

// Keep track of current fiber so that we know the path to unwind on pause.
// TODO: this looks the same as nextUnitOfWork in scheduler. Can we unify them?
var currentFiber = null;
// If we're in the middle of user code, which fiber and method is it?
// Reusing `currentFiber` would be confusing for this because user code fiber
// can change during commit phase too, but we don't need to unwind it (since
// lifecycles in the commit phase don't resemble a tree).
var currentPhase = null;
var currentPhaseFiber = null;
// Did lifecycle hook schedule an update? This is often a performance problem,
// so we will keep track of it, and include it in the report.
// Track commits caused by cascading updates.
var isCommitting = false;
var hasScheduledUpdateInCurrentCommit = false;
var hasScheduledUpdateInCurrentPhase = false;
var commitCountInCurrentWorkLoop = 0;
var effectCountInCurrentCommit = 0;
var isWaitingForCallback = false;
// During commits, we only show a measurement once per method name
// to avoid stretch the commit phase with measurement overhead.
var labelsInCurrentCommit = new Set();

var formatMarkName = function (markName) {
  return reactEmoji + ' ' + markName;
};

var formatLabel = function (label, warning$$1) {
  var prefix = warning$$1 ? warningEmoji + ' ' : reactEmoji + ' ';
  var suffix = warning$$1 ? ' Warning: ' + warning$$1 : '';
  return '' + prefix + label + suffix;
};

var beginMark = function (markName) {
  performance.mark(formatMarkName(markName));
};

var clearMark = function (markName) {
  performance.clearMarks(formatMarkName(markName));
};

var endMark = function (label, markName, warning$$1) {
  var formattedMarkName = formatMarkName(markName);
  var formattedLabel = formatLabel(label, warning$$1);
  try {
    performance.measure(formattedLabel, formattedMarkName);
  } catch (err) {}
  // If previous mark was missing for some reason, this will throw.
  // This could only happen if React crashed in an unexpected place earlier.
  // Don't pile on with more errors.

  // Clear marks immediately to avoid growing buffer.
  performance.clearMarks(formattedMarkName);
  performance.clearMeasures(formattedLabel);
};

var getFiberMarkName = function (label, debugID) {
  return label + ' (#' + debugID + ')';
};

var getFiberLabel = function (componentName, isMounted, phase) {
  if (phase === null) {
    // These are composite component total time measurements.
    return componentName + ' [' + (isMounted ? 'update' : 'mount') + ']';
  } else {
    // Composite component methods.
    return componentName + '.' + phase;
  }
};

var beginFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);

  if (isCommitting && labelsInCurrentCommit.has(label)) {
    // During the commit phase, we don't show duplicate labels because
    // there is a fixed overhead for every measurement, and we don't
    // want to stretch the commit phase beyond necessary.
    return false;
  }
  labelsInCurrentCommit.add(label);

  var markName = getFiberMarkName(label, debugID);
  beginMark(markName);
  return true;
};

var clearFiberMark = function (fiber, phase) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  clearMark(markName);
};

var endFiberMark = function (fiber, phase, warning$$1) {
  var componentName = getComponentName(fiber) || 'Unknown';
  var debugID = fiber._debugID;
  var isMounted = fiber.alternate !== null;
  var label = getFiberLabel(componentName, isMounted, phase);
  var markName = getFiberMarkName(label, debugID);
  endMark(label, markName, warning$$1);
};

var shouldIgnoreFiber = function (fiber) {
  // Host components should be skipped in the timeline.
  // We could check typeof fiber.type, but does this work with RN?
  switch (fiber.tag) {
    case HostRoot:
    case HostComponent:
    case HostText:
    case HostPortal:
    case ReturnComponent:
    case Fragment:
      return true;
    default:
      return false;
  }
};

var clearPendingPhaseMeasurement = function () {
  if (currentPhase !== null && currentPhaseFiber !== null) {
    clearFiberMark(currentPhaseFiber, currentPhase);
  }
  currentPhaseFiber = null;
  currentPhase = null;
  hasScheduledUpdateInCurrentPhase = false;
};

var pauseTimers = function () {
  // Stops all currently active measurements so that they can be resumed
  // if we continue in a later deferred loop from the same unit of work.
  var fiber = currentFiber;
  while (fiber) {
    if (fiber._debugIsCurrentlyTiming) {
      endFiberMark(fiber, null, null);
    }
    fiber = fiber['return'];
  }
};

var resumeTimersRecursively = function (fiber) {
  if (fiber['return'] !== null) {
    resumeTimersRecursively(fiber['return']);
  }
  if (fiber._debugIsCurrentlyTiming) {
    beginFiberMark(fiber, null);
  }
};

var resumeTimers = function () {
  // Resumes all measurements that were active during the last deferred loop.
  if (currentFiber !== null) {
    resumeTimersRecursively(currentFiber);
  }
};

function recordEffect() {
  if (enableUserTimingAPI) {
    effectCountInCurrentCommit++;
  }
}

function recordScheduleUpdate() {
  if (enableUserTimingAPI) {
    if (isCommitting) {
      hasScheduledUpdateInCurrentCommit = true;
    }
    if (currentPhase !== null && currentPhase !== 'componentWillMount' && currentPhase !== 'componentWillReceiveProps') {
      hasScheduledUpdateInCurrentPhase = true;
    }
  }
}

function startRequestCallbackTimer() {
  if (enableUserTimingAPI) {
    if (supportsUserTiming && !isWaitingForCallback) {
      isWaitingForCallback = true;
      beginMark('(Waiting for async callback...)');
    }
  }
}

function stopRequestCallbackTimer(didExpire) {
  if (enableUserTimingAPI) {
    if (supportsUserTiming) {
      isWaitingForCallback = false;
      var warning$$1 = didExpire ? 'React was blocked by main thread' : null;
      endMark('(Waiting for async callback...)', '(Waiting for async callback...)', warning$$1);
    }
  }
}

function startWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, this is the fiber to unwind from.
    currentFiber = fiber;
    if (!beginFiberMark(fiber, null)) {
      return;
    }
    fiber._debugIsCurrentlyTiming = true;
  }
}

function cancelWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // Remember we shouldn't complete measurement for this fiber.
    // Otherwise flamechart will be deep even for small updates.
    fiber._debugIsCurrentlyTiming = false;
    clearFiberMark(fiber, null);
  }
}

function stopWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, its parent is the fiber to unwind from.
    currentFiber = fiber['return'];
    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }
    fiber._debugIsCurrentlyTiming = false;
    endFiberMark(fiber, null, null);
  }
}

function stopFailedWorkTimer(fiber) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming || shouldIgnoreFiber(fiber)) {
      return;
    }
    // If we pause, its parent is the fiber to unwind from.
    currentFiber = fiber['return'];
    if (!fiber._debugIsCurrentlyTiming) {
      return;
    }
    fiber._debugIsCurrentlyTiming = false;
    var warning$$1 = 'An error was thrown inside this error boundary';
    endFiberMark(fiber, null, warning$$1);
  }
}

function startPhaseTimer(fiber, phase) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    clearPendingPhaseMeasurement();
    if (!beginFiberMark(fiber, phase)) {
      return;
    }
    currentPhaseFiber = fiber;
    currentPhase = phase;
  }
}

function stopPhaseTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    if (currentPhase !== null && currentPhaseFiber !== null) {
      var warning$$1 = hasScheduledUpdateInCurrentPhase ? 'Scheduled a cascading update' : null;
      endFiberMark(currentPhaseFiber, currentPhase, warning$$1);
    }
    currentPhase = null;
    currentPhaseFiber = null;
  }
}

function startWorkLoopTimer(nextUnitOfWork) {
  if (enableUserTimingAPI) {
    currentFiber = nextUnitOfWork;
    if (!supportsUserTiming) {
      return;
    }
    commitCountInCurrentWorkLoop = 0;
    // This is top level call.
    // Any other measurements are performed within.
    beginMark('(React Tree Reconciliation)');
    // Resume any measurements that were in progress during the last loop.
    resumeTimers();
  }
}

function stopWorkLoopTimer(interruptedBy) {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var warning$$1 = null;
    if (interruptedBy !== null) {
      if (interruptedBy.tag === HostRoot) {
        warning$$1 = 'A top-level update interrupted the previous render';
      } else {
        var componentName = getComponentName(interruptedBy) || 'Unknown';
        warning$$1 = 'An update to ' + componentName + ' interrupted the previous render';
      }
    } else if (commitCountInCurrentWorkLoop > 1) {
      warning$$1 = 'There were cascading updates';
    }
    commitCountInCurrentWorkLoop = 0;
    // Pause any measurements until the next loop.
    pauseTimers();
    endMark('(React Tree Reconciliation)', '(React Tree Reconciliation)', warning$$1);
  }
}

function startCommitTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    isCommitting = true;
    hasScheduledUpdateInCurrentCommit = false;
    labelsInCurrentCommit.clear();
    beginMark('(Committing Changes)');
  }
}

function stopCommitTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }

    var warning$$1 = null;
    if (hasScheduledUpdateInCurrentCommit) {
      warning$$1 = 'Lifecycle hook scheduled a cascading update';
    } else if (commitCountInCurrentWorkLoop > 0) {
      warning$$1 = 'Caused by a cascading update in earlier commit';
    }
    hasScheduledUpdateInCurrentCommit = false;
    commitCountInCurrentWorkLoop++;
    isCommitting = false;
    labelsInCurrentCommit.clear();

    endMark('(Committing Changes)', '(Committing Changes)', warning$$1);
  }
}

function startCommitHostEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Committing Host Effects)');
  }
}

function stopCommitHostEffectsTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Committing Host Effects: ' + count + ' Total)', '(Committing Host Effects)', null);
  }
}

function startCommitLifeCyclesTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    effectCountInCurrentCommit = 0;
    beginMark('(Calling Lifecycle Methods)');
  }
}

function stopCommitLifeCyclesTimer() {
  if (enableUserTimingAPI) {
    if (!supportsUserTiming) {
      return;
    }
    var count = effectCountInCurrentCommit;
    effectCountInCurrentCommit = 0;
    endMark('(Calling Lifecycle Methods: ' + count + ' Total)', '(Calling Lifecycle Methods)', null);
  }
}

{
  var warnedAboutMissingGetChildContext = {};
}

// A cursor to the current merged context object on the stack.
var contextStackCursor = createCursor(emptyObject);
// A cursor to a boolean indicating whether the context has changed.
var didPerformWorkStackCursor = createCursor(false);
// Keep track of the previous context object that was on the stack.
// We use this to get access to the parent context after we have already
// pushed the next context provider, and now need to merge their contexts.
var previousContext = emptyObject;

function getUnmaskedContext(workInProgress) {
  var hasOwnContext = isContextProvider(workInProgress);
  if (hasOwnContext) {
    // If the fiber is a context provider itself, when we read its context
    // we have already pushed its own child context on the stack. A context
    // provider should not "see" its own child context. Therefore we read the
    // previous (parent) context instead for a context provider.
    return previousContext;
  }
  return contextStackCursor.current;
}

function cacheContext(workInProgress, unmaskedContext, maskedContext) {
  var instance = workInProgress.stateNode;
  instance.__reactInternalMemoizedUnmaskedChildContext = unmaskedContext;
  instance.__reactInternalMemoizedMaskedChildContext = maskedContext;
}

function getMaskedContext(workInProgress, unmaskedContext) {
  var type = workInProgress.type;
  var contextTypes = type.contextTypes;
  if (!contextTypes) {
    return emptyObject;
  }

  // Avoid recreating masked context unless unmasked context has changed.
  // Failing to do this will result in unnecessary calls to componentWillReceiveProps.
  // This may trigger infinite loops if componentWillReceiveProps calls setState.
  var instance = workInProgress.stateNode;
  if (instance && instance.__reactInternalMemoizedUnmaskedChildContext === unmaskedContext) {
    return instance.__reactInternalMemoizedMaskedChildContext;
  }

  var context = {};
  for (var key in contextTypes) {
    context[key] = unmaskedContext[key];
  }

  {
    var name = getComponentName(workInProgress) || 'Unknown';
    checkPropTypes(contextTypes, context, 'context', name, ReactDebugCurrentFiber.getCurrentFiberStackAddendum);
  }

  // Cache unmasked context so we can avoid recreating masked context unless necessary.
  // Context is created before the class component is instantiated so check for instance.
  if (instance) {
    cacheContext(workInProgress, unmaskedContext, context);
  }

  return context;
}

function hasContextChanged() {
  return didPerformWorkStackCursor.current;
}

function isContextConsumer(fiber) {
  return fiber.tag === ClassComponent && fiber.type.contextTypes != null;
}

function isContextProvider(fiber) {
  return fiber.tag === ClassComponent && fiber.type.childContextTypes != null;
}

function popContextProvider(fiber) {
  if (!isContextProvider(fiber)) {
    return;
  }

  pop(didPerformWorkStackCursor, fiber);
  pop(contextStackCursor, fiber);
}

function popTopLevelContextObject(fiber) {
  pop(didPerformWorkStackCursor, fiber);
  pop(contextStackCursor, fiber);
}

function pushTopLevelContextObject(fiber, context, didChange) {
  !(contextStackCursor.cursor == null) ? invariant(false, 'Unexpected context found on stack. This error is likely caused by a bug in React. Please file an issue.') : void 0;

  push(contextStackCursor, context, fiber);
  push(didPerformWorkStackCursor, didChange, fiber);
}

function processChildContext(fiber, parentContext) {
  var instance = fiber.stateNode;
  var childContextTypes = fiber.type.childContextTypes;

  // TODO (bvaughn) Replace this behavior with an invariant() in the future.
  // It has only been added in Fiber to match the (unintentional) behavior in Stack.
  if (typeof instance.getChildContext !== 'function') {
    {
      var componentName = getComponentName(fiber) || 'Unknown';

      if (!warnedAboutMissingGetChildContext[componentName]) {
        warnedAboutMissingGetChildContext[componentName] = true;
        warning(false, '%s.childContextTypes is specified but there is no getChildContext() method ' + 'on the instance. You can either define getChildContext() on %s or remove ' + 'childContextTypes from it.', componentName, componentName);
      }
    }
    return parentContext;
  }

  var childContext = void 0;
  {
    ReactDebugCurrentFiber.setCurrentPhase('getChildContext');
  }
  startPhaseTimer(fiber, 'getChildContext');
  childContext = instance.getChildContext();
  stopPhaseTimer();
  {
    ReactDebugCurrentFiber.setCurrentPhase(null);
  }
  for (var contextKey in childContext) {
    !(contextKey in childContextTypes) ? invariant(false, '%s.getChildContext(): key "%s" is not defined in childContextTypes.', getComponentName(fiber) || 'Unknown', contextKey) : void 0;
  }
  {
    var name = getComponentName(fiber) || 'Unknown';
    checkPropTypes(childContextTypes, childContext, 'child context', name,
    // In practice, there is one case in which we won't get a stack. It's when
    // somebody calls unstable_renderSubtreeIntoContainer() and we process
    // context from the parent component instance. The stack will be missing
    // because it's outside of the reconciliation, and so the pointer has not
    // been set. This is rare and doesn't matter. We'll also remove that API.
    ReactDebugCurrentFiber.getCurrentFiberStackAddendum);
  }

  return _assign({}, parentContext, childContext);
}

function pushContextProvider(workInProgress) {
  if (!isContextProvider(workInProgress)) {
    return false;
  }

  var instance = workInProgress.stateNode;
  // We push the context as early as possible to ensure stack integrity.
  // If the instance does not exist yet, we will push null at first,
  // and replace it on the stack later when invalidating the context.
  var memoizedMergedChildContext = instance && instance.__reactInternalMemoizedMergedChildContext || emptyObject;

  // Remember the parent context so we can merge with it later.
  // Inherit the parent's did-perform-work value to avoid inadvertently blocking updates.
  previousContext = contextStackCursor.current;
  push(contextStackCursor, memoizedMergedChildContext, workInProgress);
  push(didPerformWorkStackCursor, didPerformWorkStackCursor.current, workInProgress);

  return true;
}

function invalidateContextProvider(workInProgress, didChange) {
  var instance = workInProgress.stateNode;
  !instance ? invariant(false, 'Expected to have an instance by this point. This error is likely caused by a bug in React. Please file an issue.') : void 0;

  if (didChange) {
    // Merge parent and own context.
    // Skip this if we're not updating due to sCU.
    // This avoids unnecessarily recomputing memoized values.
    var mergedContext = processChildContext(workInProgress, previousContext);
    instance.__reactInternalMemoizedMergedChildContext = mergedContext;

    // Replace the old (or empty) context with the new one.
    // It is important to unwind the context in the reverse order.
    pop(didPerformWorkStackCursor, workInProgress);
    pop(contextStackCursor, workInProgress);
    // Now push the new context and mark that it has changed.
    push(contextStackCursor, mergedContext, workInProgress);
    push(didPerformWorkStackCursor, didChange, workInProgress);
  } else {
    pop(didPerformWorkStackCursor, workInProgress);
    push(didPerformWorkStackCursor, didChange, workInProgress);
  }
}

function resetContext() {
  previousContext = emptyObject;
  contextStackCursor.current = emptyObject;
  didPerformWorkStackCursor.current = false;
}

function findCurrentUnmaskedContext(fiber) {
  // Currently this is only used with renderSubtreeIntoContainer; not sure if it
  // makes sense elsewhere
  !(isFiberMounted(fiber) && fiber.tag === ClassComponent) ? invariant(false, 'Expected subtree parent to be a mounted class component. This error is likely caused by a bug in React. Please file an issue.') : void 0;

  var node = fiber;
  while (node.tag !== HostRoot) {
    if (isContextProvider(node)) {
      return node.stateNode.__reactInternalMemoizedMergedChildContext;
    }
    var parent = node['return'];
    !parent ? invariant(false, 'Found unexpected detached subtree parent. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    node = parent;
  }
  return node.stateNode.context;
}

var NoWork = 0; // TODO: Use an opaque type once ESLint et al support the syntax

var Sync = 1;
var Never = 2147483647; // Max int32: Math.pow(2, 31) - 1

var UNIT_SIZE = 10;
var MAGIC_NUMBER_OFFSET = 2;

// 1 unit of expiration time represents 10ms.
function msToExpirationTime(ms) {
  // Always add an offset so that we don't clash with the magic number for NoWork.
  return (ms / UNIT_SIZE | 0) + MAGIC_NUMBER_OFFSET;
}

function expirationTimeToMs(expirationTime) {
  return (expirationTime - MAGIC_NUMBER_OFFSET) * UNIT_SIZE;
}

function ceiling(num, precision) {
  return ((num / precision | 0) + 1) * precision;
}

function computeExpirationBucket(currentTime, expirationInMs, bucketSizeMs) {
  return ceiling(currentTime + expirationInMs / UNIT_SIZE, bucketSizeMs / UNIT_SIZE);
}

var NoContext = 0;
var AsyncUpdates = 1;

{
  var hasBadMapPolyfill = false;
  try {
    var nonExtensibleObject = Object.preventExtensions({});
    /* eslint-disable no-new */
    
    /* eslint-enable no-new */
  } catch (e) {
    // TODO: Consider warning about bad polyfills
    hasBadMapPolyfill = true;
  }
}

// A Fiber is work on a Component that needs to be done or was done. There can
// be more than one per component.


{
  var debugCounter = 1;
}

function FiberNode(tag, key, internalContextTag) {
  // Instance
  this.tag = tag;
  this.key = key;
  this.type = null;
  this.stateNode = null;

  // Fiber
  this['return'] = null;
  this.child = null;
  this.sibling = null;
  this.index = 0;

  this.ref = null;

  this.pendingProps = null;
  this.memoizedProps = null;
  this.updateQueue = null;
  this.memoizedState = null;

  this.internalContextTag = internalContextTag;

  // Effects
  this.effectTag = NoEffect;
  this.nextEffect = null;

  this.firstEffect = null;
  this.lastEffect = null;

  this.expirationTime = NoWork;

  this.alternate = null;

  {
    this._debugID = debugCounter++;
    this._debugSource = null;
    this._debugOwner = null;
    this._debugIsCurrentlyTiming = false;
    if (!hasBadMapPolyfill && typeof Object.preventExtensions === 'function') {
      Object.preventExtensions(this);
    }
  }
}

// This is a constructor function, rather than a POJO constructor, still
// please ensure we do the following:
// 1) Nobody should add any instance methods on this. Instance methods can be
//    more difficult to predict when they get optimized and they are almost
//    never inlined properly in static compilers.
// 2) Nobody should rely on `instanceof Fiber` for type testing. We should
//    always know when it is a fiber.
// 3) We might want to experiment with using numeric keys since they are easier
//    to optimize in a non-JIT environment.
// 4) We can easily go from a constructor to a createFiber object literal if that
//    is faster.
// 5) It should be easy to port this to a C struct and keep a C implementation
//    compatible.
var createFiber = function (tag, key, internalContextTag) {
  // $FlowFixMe: the shapes are exact here but Flow doesn't like constructors
  return new FiberNode(tag, key, internalContextTag);
};

function shouldConstruct(Component) {
  return !!(Component.prototype && Component.prototype.isReactComponent);
}

// This is used to create an alternate fiber to do work on.
function createWorkInProgress(current, pendingProps, expirationTime) {
  var workInProgress = current.alternate;
  if (workInProgress === null) {
    // We use a double buffering pooling technique because we know that we'll
    // only ever need at most two versions of a tree. We pool the "other" unused
    // node that we're free to reuse. This is lazily created to avoid allocating
    // extra objects for things that are never updated. It also allow us to
    // reclaim the extra memory if needed.
    workInProgress = createFiber(current.tag, current.key, current.internalContextTag);
    workInProgress.type = current.type;
    workInProgress.stateNode = current.stateNode;

    {
      // DEV-only fields
      workInProgress._debugID = current._debugID;
      workInProgress._debugSource = current._debugSource;
      workInProgress._debugOwner = current._debugOwner;
    }

    workInProgress.alternate = current;
    current.alternate = workInProgress;
  } else {
    // We already have an alternate.
    // Reset the effect tag.
    workInProgress.effectTag = NoEffect;

    // The effect list is no longer valid.
    workInProgress.nextEffect = null;
    workInProgress.firstEffect = null;
    workInProgress.lastEffect = null;
  }

  workInProgress.expirationTime = expirationTime;
  workInProgress.pendingProps = pendingProps;

  workInProgress.child = current.child;
  workInProgress.memoizedProps = current.memoizedProps;
  workInProgress.memoizedState = current.memoizedState;
  workInProgress.updateQueue = current.updateQueue;

  // These will be overridden during the parent's reconciliation
  workInProgress.sibling = current.sibling;
  workInProgress.index = current.index;
  workInProgress.ref = current.ref;

  return workInProgress;
}

function createHostRootFiber() {
  var fiber = createFiber(HostRoot, null, NoContext);
  return fiber;
}

function createFiberFromElement(element, internalContextTag, expirationTime) {
  var owner = null;
  {
    owner = element._owner;
  }

  var fiber = void 0;
  var type = element.type,
      key = element.key;

  if (typeof type === 'function') {
    fiber = shouldConstruct(type) ? createFiber(ClassComponent, key, internalContextTag) : createFiber(IndeterminateComponent, key, internalContextTag);
    fiber.type = type;
    fiber.pendingProps = element.props;
  } else if (typeof type === 'string') {
    fiber = createFiber(HostComponent, key, internalContextTag);
    fiber.type = type;
    fiber.pendingProps = element.props;
  } else if (typeof type === 'object' && type !== null && typeof type.tag === 'number') {
    // Currently assumed to be a continuation and therefore is a fiber already.
    // TODO: The yield system is currently broken for updates in some cases.
    // The reified yield stores a fiber, but we don't know which fiber that is;
    // the current or a workInProgress? When the continuation gets rendered here
    // we don't know if we can reuse that fiber or if we need to clone it.
    // There is probably a clever way to restructure this.
    fiber = type;
    fiber.pendingProps = element.props;
  } else {
    var info = '';
    {
      if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {
        info += ' You likely forgot to export your component from the file ' + "it's defined in, or you might have mixed up default and named imports.";
      }
      var ownerName = owner ? getComponentName(owner) : null;
      if (ownerName) {
        info += '\n\nCheck the render method of `' + ownerName + '`.';
      }
    }
    invariant(false, 'Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s', type == null ? type : typeof type, info);
  }

  {
    fiber._debugSource = element._source;
    fiber._debugOwner = element._owner;
  }

  fiber.expirationTime = expirationTime;

  return fiber;
}

function createFiberFromFragment(elements, internalContextTag, expirationTime, key) {
  var fiber = createFiber(Fragment, key, internalContextTag);
  fiber.pendingProps = elements;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromText(content, internalContextTag, expirationTime) {
  var fiber = createFiber(HostText, null, internalContextTag);
  fiber.pendingProps = content;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromHostInstanceForDeletion() {
  var fiber = createFiber(HostComponent, null, NoContext);
  fiber.type = 'DELETED';
  return fiber;
}

function createFiberFromCall(call, internalContextTag, expirationTime) {
  var fiber = createFiber(CallComponent, call.key, internalContextTag);
  fiber.type = call.handler;
  fiber.pendingProps = call;
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromReturn(returnNode, internalContextTag, expirationTime) {
  var fiber = createFiber(ReturnComponent, null, internalContextTag);
  fiber.expirationTime = expirationTime;
  return fiber;
}

function createFiberFromPortal(portal, internalContextTag, expirationTime) {
  var fiber = createFiber(HostPortal, portal.key, internalContextTag);
  fiber.pendingProps = portal.children || [];
  fiber.expirationTime = expirationTime;
  fiber.stateNode = {
    containerInfo: portal.containerInfo,
    pendingChildren: null, // Used by persistent updates
    implementation: portal.implementation
  };
  return fiber;
}

function createFiberRoot(containerInfo, hydrate) {
  // Cyclic construction. This cheats the type system right now because
  // stateNode is any.
  var uninitializedFiber = createHostRootFiber();
  var root = {
    current: uninitializedFiber,
    containerInfo: containerInfo,
    pendingChildren: null,
    remainingExpirationTime: NoWork,
    isReadyForCommit: false,
    finishedWork: null,
    context: null,
    pendingContext: null,
    hydrate: hydrate,
    nextScheduledRoot: null
  };
  uninitializedFiber.stateNode = root;
  return root;
}

var onCommitFiberRoot = null;
var onCommitFiberUnmount = null;
var hasLoggedError = false;

function catchErrors(fn) {
  return function (arg) {
    try {
      return fn(arg);
    } catch (err) {
      if (true && !hasLoggedError) {
        hasLoggedError = true;
        warning(false, 'React DevTools encountered an error: %s', err);
      }
    }
  };
}

function injectInternals(internals) {
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
    // No DevTools
    return false;
  }
  var hook = __REACT_DEVTOOLS_GLOBAL_HOOK__;
  if (hook.isDisabled) {
    // This isn't a real property on the hook, but it can be set to opt out
    // of DevTools integration and associated warnings and logs.
    // https://github.com/facebook/react/issues/3877
    return true;
  }
  if (!hook.supportsFiber) {
    {
      warning(false, 'The installed version of React DevTools is too old and will not work ' + 'with the current version of React. Please update React DevTools. ' + 'https://fb.me/react-devtools');
    }
    // DevTools exists, even though it doesn't support Fiber.
    return true;
  }
  try {
    var rendererID = hook.inject(internals);
    // We have successfully injected, so now it is safe to set up hooks.
    onCommitFiberRoot = catchErrors(function (root) {
      return hook.onCommitFiberRoot(rendererID, root);
    });
    onCommitFiberUnmount = catchErrors(function (fiber) {
      return hook.onCommitFiberUnmount(rendererID, fiber);
    });
  } catch (err) {
    // Catch all errors because it is unsafe to throw during initialization.
    {
      warning(false, 'React DevTools encountered an error: %s.', err);
    }
  }
  // DevTools exists
  return true;
}

function onCommitRoot(root) {
  if (typeof onCommitFiberRoot === 'function') {
    onCommitFiberRoot(root);
  }
}

function onCommitUnmount(fiber) {
  if (typeof onCommitFiberUnmount === 'function') {
    onCommitFiberUnmount(fiber);
  }
}

{
  var didWarnUpdateInsideUpdate = false;
}

// Callbacks are not validated until invocation


// Singly linked-list of updates. When an update is scheduled, it is added to
// the queue of the current fiber and the work-in-progress fiber. The two queues
// are separate but they share a persistent structure.
//
// During reconciliation, updates are removed from the work-in-progress fiber,
// but they remain on the current fiber. That ensures that if a work-in-progress
// is aborted, the aborted updates are recovered by cloning from current.
//
// The work-in-progress queue is always a subset of the current queue.
//
// When the tree is committed, the work-in-progress becomes the current.


function createUpdateQueue(baseState) {
  var queue = {
    baseState: baseState,
    expirationTime: NoWork,
    first: null,
    last: null,
    callbackList: null,
    hasForceUpdate: false,
    isInitialized: false
  };
  {
    queue.isProcessing = false;
  }
  return queue;
}

function insertUpdateIntoQueue(queue, update) {
  // Append the update to the end of the list.
  if (queue.last === null) {
    // Queue is empty
    queue.first = queue.last = update;
  } else {
    queue.last.next = update;
    queue.last = update;
  }
  if (queue.expirationTime === NoWork || queue.expirationTime > update.expirationTime) {
    queue.expirationTime = update.expirationTime;
  }
}

function insertUpdateIntoFiber(fiber, update) {
  // We'll have at least one and at most two distinct update queues.
  var alternateFiber = fiber.alternate;
  var queue1 = fiber.updateQueue;
  if (queue1 === null) {
    // TODO: We don't know what the base state will be until we begin work.
    // It depends on which fiber is the next current. Initialize with an empty
    // base state, then set to the memoizedState when rendering. Not super
    // happy with this approach.
    queue1 = fiber.updateQueue = createUpdateQueue(null);
  }

  var queue2 = void 0;
  if (alternateFiber !== null) {
    queue2 = alternateFiber.updateQueue;
    if (queue2 === null) {
      queue2 = alternateFiber.updateQueue = createUpdateQueue(null);
    }
  } else {
    queue2 = null;
  }
  queue2 = queue2 !== queue1 ? queue2 : null;

  // Warn if an update is scheduled from inside an updater function.
  {
    if ((queue1.isProcessing || queue2 !== null && queue2.isProcessing) && !didWarnUpdateInsideUpdate) {
      warning(false, 'An update (setState, replaceState, or forceUpdate) was scheduled ' + 'from inside an update function. Update functions should be pure, ' + 'with zero side-effects. Consider using componentDidUpdate or a ' + 'callback.');
      didWarnUpdateInsideUpdate = true;
    }
  }

  // If there's only one queue, add the update to that queue and exit.
  if (queue2 === null) {
    insertUpdateIntoQueue(queue1, update);
    return;
  }

  // If either queue is empty, we need to add to both queues.
  if (queue1.last === null || queue2.last === null) {
    insertUpdateIntoQueue(queue1, update);
    insertUpdateIntoQueue(queue2, update);
    return;
  }

  // If both lists are not empty, the last update is the same for both lists
  // because of structural sharing. So, we should only append to one of
  // the lists.
  insertUpdateIntoQueue(queue1, update);
  // But we still need to update the `last` pointer of queue2.
  queue2.last = update;
}

function getUpdateExpirationTime(fiber) {
  if (fiber.tag !== ClassComponent && fiber.tag !== HostRoot) {
    return NoWork;
  }
  var updateQueue = fiber.updateQueue;
  if (updateQueue === null) {
    return NoWork;
  }
  return updateQueue.expirationTime;
}

function getStateFromUpdate(update, instance, prevState, props) {
  var partialState = update.partialState;
  if (typeof partialState === 'function') {
    var updateFn = partialState;

    // Invoke setState callback an extra time to help detect side-effects.
    if (debugRenderPhaseSideEffects) {
      updateFn.call(instance, prevState, props);
    }

    return updateFn.call(instance, prevState, props);
  } else {
    return partialState;
  }
}

function processUpdateQueue(current, workInProgress, queue, instance, props, renderExpirationTime) {
  if (current !== null && current.updateQueue === queue) {
    // We need to create a work-in-progress queue, by cloning the current queue.
    var currentQueue = queue;
    queue = workInProgress.updateQueue = {
      baseState: currentQueue.baseState,
      expirationTime: currentQueue.expirationTime,
      first: currentQueue.first,
      last: currentQueue.last,
      isInitialized: currentQueue.isInitialized,
      // These fields are no longer valid because they were already committed.
      // Reset them.
      callbackList: null,
      hasForceUpdate: false
    };
  }

  {
    // Set this flag so we can warn if setState is called inside the update
    // function of another setState.
    queue.isProcessing = true;
  }

  // Reset the remaining expiration time. If we skip over any updates, we'll
  // increase this accordingly.
  queue.expirationTime = NoWork;

  // TODO: We don't know what the base state will be until we begin work.
  // It depends on which fiber is the next current. Initialize with an empty
  // base state, then set to the memoizedState when rendering. Not super
  // happy with this approach.
  var state = void 0;
  if (queue.isInitialized) {
    state = queue.baseState;
  } else {
    state = queue.baseState = workInProgress.memoizedState;
    queue.isInitialized = true;
  }
  var dontMutatePrevState = true;
  var update = queue.first;
  var didSkip = false;
  while (update !== null) {
    var updateExpirationTime = update.expirationTime;
    if (updateExpirationTime > renderExpirationTime) {
      // This update does not have sufficient priority. Skip it.
      var remainingExpirationTime = queue.expirationTime;
      if (remainingExpirationTime === NoWork || remainingExpirationTime > updateExpirationTime) {
        // Update the remaining expiration time.
        queue.expirationTime = updateExpirationTime;
      }
      if (!didSkip) {
        didSkip = true;
        queue.baseState = state;
      }
      // Continue to the next update.
      update = update.next;
      continue;
    }

    // This update does have sufficient priority.

    // If no previous updates were skipped, drop this update from the queue by
    // advancing the head of the list.
    if (!didSkip) {
      queue.first = update.next;
      if (queue.first === null) {
        queue.last = null;
      }
    }

    // Process the update
    var _partialState = void 0;
    if (update.isReplace) {
      state = getStateFromUpdate(update, instance, state, props);
      dontMutatePrevState = true;
    } else {
      _partialState = getStateFromUpdate(update, instance, state, props);
      if (_partialState) {
        if (dontMutatePrevState) {
          // $FlowFixMe: Idk how to type this properly.
          state = _assign({}, state, _partialState);
        } else {
          state = _assign(state, _partialState);
        }
        dontMutatePrevState = false;
      }
    }
    if (update.isForced) {
      queue.hasForceUpdate = true;
    }
    if (update.callback !== null) {
      // Append to list of callbacks.
      var _callbackList = queue.callbackList;
      if (_callbackList === null) {
        _callbackList = queue.callbackList = [];
      }
      _callbackList.push(update);
    }
    update = update.next;
  }

  if (queue.callbackList !== null) {
    workInProgress.effectTag |= Callback;
  } else if (queue.first === null && !queue.hasForceUpdate) {
    // The queue is empty. We can reset it.
    workInProgress.updateQueue = null;
  }

  if (!didSkip) {
    didSkip = true;
    queue.baseState = state;
  }

  {
    // No longer processing.
    queue.isProcessing = false;
  }

  return state;
}

function commitCallbacks(queue, context) {
  var callbackList = queue.callbackList;
  if (callbackList === null) {
    return;
  }
  // Set the list to null to make sure they don't get called more than once.
  queue.callbackList = null;
  for (var i = 0; i < callbackList.length; i++) {
    var update = callbackList[i];
    var _callback = update.callback;
    // This update might be processed again. Clear the callback so it's only
    // called once.
    update.callback = null;
    !(typeof _callback === 'function') ? invariant(false, 'Invalid argument passed as callback. Expected a function. Instead received: %s', _callback) : void 0;
    _callback.call(context);
  }
}

var fakeInternalInstance = {};
var isArray = Array.isArray;

{
  var didWarnAboutStateAssignmentForComponent = {};

  var warnOnInvalidCallback = function (callback, callerName) {
    warning(callback === null || typeof callback === 'function', '%s(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callerName, callback);
  };

  // This is so gross but it's at least non-critical and can be removed if
  // it causes problems. This is meant to give a nicer error message for
  // ReactDOM15.unstable_renderSubtreeIntoContainer(reactDOM16Component,
  // ...)) which otherwise throws a "_processChildContext is not a function"
  // exception.
  Object.defineProperty(fakeInternalInstance, '_processChildContext', {
    enumerable: false,
    value: function () {
      invariant(false, '_processChildContext is not available in React 16+. This likely means you have multiple copies of React and are attempting to nest a React 15 tree inside a React 16 tree using unstable_renderSubtreeIntoContainer, which isn\'t supported. Try to make sure you have only one copy of React (and ideally, switch to ReactDOM.createPortal).');
    }
  });
  Object.freeze(fakeInternalInstance);
}

var ReactFiberClassComponent = function (scheduleWork, computeExpirationForFiber, memoizeProps, memoizeState) {
  // Class component state updater
  var updater = {
    isMounted: isMounted,
    enqueueSetState: function (instance, partialState, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'setState');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: partialState,
        callback: callback,
        isReplace: false,
        isForced: false,
        nextCallback: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    },
    enqueueReplaceState: function (instance, state, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'replaceState');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: state,
        callback: callback,
        isReplace: true,
        isForced: false,
        nextCallback: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    },
    enqueueForceUpdate: function (instance, callback) {
      var fiber = get(instance);
      callback = callback === undefined ? null : callback;
      {
        warnOnInvalidCallback(callback, 'forceUpdate');
      }
      var expirationTime = computeExpirationForFiber(fiber);
      var update = {
        expirationTime: expirationTime,
        partialState: null,
        callback: callback,
        isReplace: false,
        isForced: true,
        nextCallback: null,
        next: null
      };
      insertUpdateIntoFiber(fiber, update);
      scheduleWork(fiber, expirationTime);
    }
  };

  function checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext) {
    if (oldProps === null || workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate) {
      // If the workInProgress already has an Update effect, return true
      return true;
    }

    var instance = workInProgress.stateNode;
    var type = workInProgress.type;
    if (typeof instance.shouldComponentUpdate === 'function') {
      startPhaseTimer(workInProgress, 'shouldComponentUpdate');
      var shouldUpdate = instance.shouldComponentUpdate(newProps, newState, newContext);
      stopPhaseTimer();

      // Simulate an async bailout/interruption by invoking lifecycle twice.
      if (debugRenderPhaseSideEffects) {
        instance.shouldComponentUpdate(newProps, newState, newContext);
      }

      {
        warning(shouldUpdate !== undefined, '%s.shouldComponentUpdate(): Returned undefined instead of a ' + 'boolean value. Make sure to return true or false.', getComponentName(workInProgress) || 'Unknown');
      }

      return shouldUpdate;
    }

    if (type.prototype && type.prototype.isPureReactComponent) {
      return !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState);
    }

    return true;
  }

  function checkClassInstance(workInProgress) {
    var instance = workInProgress.stateNode;
    var type = workInProgress.type;
    {
      var name = getComponentName(workInProgress);
      var renderPresent = instance.render;

      if (!renderPresent) {
        if (type.prototype && typeof type.prototype.render === 'function') {
          warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: did you accidentally return an object from the constructor?', name);
        } else {
          warning(false, '%s(...): No `render` method found on the returned component ' + 'instance: you may have forgotten to define `render`.', name);
        }
      }

      var noGetInitialStateOnES6 = !instance.getInitialState || instance.getInitialState.isReactClassApproved || instance.state;
      warning(noGetInitialStateOnES6, 'getInitialState was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Did you mean to define a state property instead?', name);
      var noGetDefaultPropsOnES6 = !instance.getDefaultProps || instance.getDefaultProps.isReactClassApproved;
      warning(noGetDefaultPropsOnES6, 'getDefaultProps was defined on %s, a plain JavaScript class. ' + 'This is only supported for classes created using React.createClass. ' + 'Use a static property to define defaultProps instead.', name);
      var noInstancePropTypes = !instance.propTypes;
      warning(noInstancePropTypes, 'propTypes was defined as an instance property on %s. Use a static ' + 'property to define propTypes instead.', name);
      var noInstanceContextTypes = !instance.contextTypes;
      warning(noInstanceContextTypes, 'contextTypes was defined as an instance property on %s. Use a static ' + 'property to define contextTypes instead.', name);
      var noComponentShouldUpdate = typeof instance.componentShouldUpdate !== 'function';
      warning(noComponentShouldUpdate, '%s has a method called ' + 'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' + 'The name is phrased as a question because the function is ' + 'expected to return a value.', name);
      if (type.prototype && type.prototype.isPureReactComponent && typeof instance.shouldComponentUpdate !== 'undefined') {
        warning(false, '%s has a method called shouldComponentUpdate(). ' + 'shouldComponentUpdate should not be used when extending React.PureComponent. ' + 'Please extend React.Component if shouldComponentUpdate is used.', getComponentName(workInProgress) || 'A pure component');
      }
      var noComponentDidUnmount = typeof instance.componentDidUnmount !== 'function';
      warning(noComponentDidUnmount, '%s has a method called ' + 'componentDidUnmount(). But there is no such lifecycle method. ' + 'Did you mean componentWillUnmount()?', name);
      var noComponentDidReceiveProps = typeof instance.componentDidReceiveProps !== 'function';
      warning(noComponentDidReceiveProps, '%s has a method called ' + 'componentDidReceiveProps(). But there is no such lifecycle method. ' + 'If you meant to update the state in response to changing props, ' + 'use componentWillReceiveProps(). If you meant to fetch data or ' + 'run side-effects or mutations after React has updated the UI, use componentDidUpdate().', name);
      var noComponentWillRecieveProps = typeof instance.componentWillRecieveProps !== 'function';
      warning(noComponentWillRecieveProps, '%s has a method called ' + 'componentWillRecieveProps(). Did you mean componentWillReceiveProps()?', name);
      var hasMutatedProps = instance.props !== workInProgress.pendingProps;
      warning(instance.props === undefined || !hasMutatedProps, '%s(...): When calling super() in `%s`, make sure to pass ' + "up the same props that your component's constructor was passed.", name, name);
      var noInstanceDefaultProps = !instance.defaultProps;
      warning(noInstanceDefaultProps, 'Setting defaultProps as an instance property on %s is not supported and will be ignored.' + ' Instead, define defaultProps as a static property on %s.', name, name);
    }

    var state = instance.state;
    if (state && (typeof state !== 'object' || isArray(state))) {
      warning(false, '%s.state: must be set to an object or null', getComponentName(workInProgress));
    }
    if (typeof instance.getChildContext === 'function') {
      warning(typeof workInProgress.type.childContextTypes === 'object', '%s.getChildContext(): childContextTypes must be defined in order to ' + 'use getChildContext().', getComponentName(workInProgress));
    }
  }

  function resetInputPointers(workInProgress, instance) {
    instance.props = workInProgress.memoizedProps;
    instance.state = workInProgress.memoizedState;
  }

  function adoptClassInstance(workInProgress, instance) {
    instance.updater = updater;
    workInProgress.stateNode = instance;
    // The instance needs access to the fiber so that it can schedule updates
    set(instance, workInProgress);
    {
      instance._reactInternalInstance = fakeInternalInstance;
    }
  }

  function constructClassInstance(workInProgress, props) {
    var ctor = workInProgress.type;
    var unmaskedContext = getUnmaskedContext(workInProgress);
    var needsContext = isContextConsumer(workInProgress);
    var context = needsContext ? getMaskedContext(workInProgress, unmaskedContext) : emptyObject;
    var instance = new ctor(props, context);
    adoptClassInstance(workInProgress, instance);

    // Cache unmasked context so we can avoid recreating masked context unless necessary.
    // ReactFiberContext usually updates this cache but can't for newly-created instances.
    if (needsContext) {
      cacheContext(workInProgress, unmaskedContext, context);
    }

    return instance;
  }

  function callComponentWillMount(workInProgress, instance) {
    startPhaseTimer(workInProgress, 'componentWillMount');
    var oldState = instance.state;
    instance.componentWillMount();
    stopPhaseTimer();

    // Simulate an async bailout/interruption by invoking lifecycle twice.
    if (debugRenderPhaseSideEffects) {
      instance.componentWillMount();
    }

    if (oldState !== instance.state) {
      {
        warning(false, '%s.componentWillMount(): Assigning directly to this.state is ' + "deprecated (except inside a component's " + 'constructor). Use setState instead.', getComponentName(workInProgress));
      }
      updater.enqueueReplaceState(instance, instance.state, null);
    }
  }

  function callComponentWillReceiveProps(workInProgress, instance, newProps, newContext) {
    startPhaseTimer(workInProgress, 'componentWillReceiveProps');
    var oldState = instance.state;
    instance.componentWillReceiveProps(newProps, newContext);
    stopPhaseTimer();

    // Simulate an async bailout/interruption by invoking lifecycle twice.
    if (debugRenderPhaseSideEffects) {
      instance.componentWillReceiveProps(newProps, newContext);
    }

    if (instance.state !== oldState) {
      {
        var componentName = getComponentName(workInProgress) || 'Component';
        if (!didWarnAboutStateAssignmentForComponent[componentName]) {
          warning(false, '%s.componentWillReceiveProps(): Assigning directly to ' + "this.state is deprecated (except inside a component's " + 'constructor). Use setState instead.', componentName);
          didWarnAboutStateAssignmentForComponent[componentName] = true;
        }
      }
      updater.enqueueReplaceState(instance, instance.state, null);
    }
  }

  // Invokes the mount life-cycles on a previously never rendered instance.
  function mountClassInstance(workInProgress, renderExpirationTime) {
    var current = workInProgress.alternate;

    {
      checkClassInstance(workInProgress);
    }

    var instance = workInProgress.stateNode;
    var state = instance.state || null;

    var props = workInProgress.pendingProps;
    !props ? invariant(false, 'There must be pending props for an initial mount. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    var unmaskedContext = getUnmaskedContext(workInProgress);

    instance.props = props;
    instance.state = workInProgress.memoizedState = state;
    instance.refs = emptyObject;
    instance.context = getMaskedContext(workInProgress, unmaskedContext);

    if (enableAsyncSubtreeAPI && workInProgress.type != null && workInProgress.type.prototype != null && workInProgress.type.prototype.unstable_isAsyncReactComponent === true) {
      workInProgress.internalContextTag |= AsyncUpdates;
    }

    if (typeof instance.componentWillMount === 'function') {
      callComponentWillMount(workInProgress, instance);
      // If we had additional state updates during this life-cycle, let's
      // process them now.
      var updateQueue = workInProgress.updateQueue;
      if (updateQueue !== null) {
        instance.state = processUpdateQueue(current, workInProgress, updateQueue, instance, props, renderExpirationTime);
      }
    }
    if (typeof instance.componentDidMount === 'function') {
      workInProgress.effectTag |= Update;
    }
  }

  // Called on a preexisting class instance. Returns false if a resumed render
  // could be reused.
  // function resumeMountClassInstance(
  //   workInProgress: Fiber,
  //   priorityLevel: PriorityLevel,
  // ): boolean {
  //   const instance = workInProgress.stateNode;
  //   resetInputPointers(workInProgress, instance);

  //   let newState = workInProgress.memoizedState;
  //   let newProps = workInProgress.pendingProps;
  //   if (!newProps) {
  //     // If there isn't any new props, then we'll reuse the memoized props.
  //     // This could be from already completed work.
  //     newProps = workInProgress.memoizedProps;
  //     invariant(
  //       newProps != null,
  //       'There should always be pending or memoized props. This error is ' +
  //         'likely caused by a bug in React. Please file an issue.',
  //     );
  //   }
  //   const newUnmaskedContext = getUnmaskedContext(workInProgress);
  //   const newContext = getMaskedContext(workInProgress, newUnmaskedContext);

  //   const oldContext = instance.context;
  //   const oldProps = workInProgress.memoizedProps;

  //   if (
  //     typeof instance.componentWillReceiveProps === 'function' &&
  //     (oldProps !== newProps || oldContext !== newContext)
  //   ) {
  //     callComponentWillReceiveProps(
  //       workInProgress,
  //       instance,
  //       newProps,
  //       newContext,
  //     );
  //   }

  //   // Process the update queue before calling shouldComponentUpdate
  //   const updateQueue = workInProgress.updateQueue;
  //   if (updateQueue !== null) {
  //     newState = processUpdateQueue(
  //       workInProgress,
  //       updateQueue,
  //       instance,
  //       newState,
  //       newProps,
  //       priorityLevel,
  //     );
  //   }

  //   // TODO: Should we deal with a setState that happened after the last
  //   // componentWillMount and before this componentWillMount? Probably
  //   // unsupported anyway.

  //   if (
  //     !checkShouldComponentUpdate(
  //       workInProgress,
  //       workInProgress.memoizedProps,
  //       newProps,
  //       workInProgress.memoizedState,
  //       newState,
  //       newContext,
  //     )
  //   ) {
  //     // Update the existing instance's state, props, and context pointers even
  //     // though we're bailing out.
  //     instance.props = newProps;
  //     instance.state = newState;
  //     instance.context = newContext;
  //     return false;
  //   }

  //   // Update the input pointers now so that they are correct when we call
  //   // componentWillMount
  //   instance.props = newProps;
  //   instance.state = newState;
  //   instance.context = newContext;

  //   if (typeof instance.componentWillMount === 'function') {
  //     callComponentWillMount(workInProgress, instance);
  //     // componentWillMount may have called setState. Process the update queue.
  //     const newUpdateQueue = workInProgress.updateQueue;
  //     if (newUpdateQueue !== null) {
  //       newState = processUpdateQueue(
  //         workInProgress,
  //         newUpdateQueue,
  //         instance,
  //         newState,
  //         newProps,
  //         priorityLevel,
  //       );
  //     }
  //   }

  //   if (typeof instance.componentDidMount === 'function') {
  //     workInProgress.effectTag |= Update;
  //   }

  //   instance.state = newState;

  //   return true;
  // }

  // Invokes the update life-cycles and returns false if it shouldn't rerender.
  function updateClassInstance(current, workInProgress, renderExpirationTime) {
    var instance = workInProgress.stateNode;
    resetInputPointers(workInProgress, instance);

    var oldProps = workInProgress.memoizedProps;
    var newProps = workInProgress.pendingProps;
    if (!newProps) {
      // If there aren't any new props, then we'll reuse the memoized props.
      // This could be from already completed work.
      newProps = oldProps;
      !(newProps != null) ? invariant(false, 'There should always be pending or memoized props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    }
    var oldContext = instance.context;
    var newUnmaskedContext = getUnmaskedContext(workInProgress);
    var newContext = getMaskedContext(workInProgress, newUnmaskedContext);

    // Note: During these life-cycles, instance.props/instance.state are what
    // ever the previously attempted to render - not the "current". However,
    // during componentDidUpdate we pass the "current" props.

    if (typeof instance.componentWillReceiveProps === 'function' && (oldProps !== newProps || oldContext !== newContext)) {
      callComponentWillReceiveProps(workInProgress, instance, newProps, newContext);
    }

    // Compute the next state using the memoized state and the update queue.
    var oldState = workInProgress.memoizedState;
    // TODO: Previous state can be null.
    var newState = void 0;
    if (workInProgress.updateQueue !== null) {
      newState = processUpdateQueue(current, workInProgress, workInProgress.updateQueue, instance, newProps, renderExpirationTime);
    } else {
      newState = oldState;
    }

    if (oldProps === newProps && oldState === newState && !hasContextChanged() && !(workInProgress.updateQueue !== null && workInProgress.updateQueue.hasForceUpdate)) {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Update;
        }
      }
      return false;
    }

    var shouldUpdate = checkShouldComponentUpdate(workInProgress, oldProps, newProps, oldState, newState, newContext);

    if (shouldUpdate) {
      if (typeof instance.componentWillUpdate === 'function') {
        startPhaseTimer(workInProgress, 'componentWillUpdate');
        instance.componentWillUpdate(newProps, newState, newContext);
        stopPhaseTimer();

        // Simulate an async bailout/interruption by invoking lifecycle twice.
        if (debugRenderPhaseSideEffects) {
          instance.componentWillUpdate(newProps, newState, newContext);
        }
      }
      if (typeof instance.componentDidUpdate === 'function') {
        workInProgress.effectTag |= Update;
      }
    } else {
      // If an update was already in progress, we should schedule an Update
      // effect even though we're bailing out, so that cWU/cDU are called.
      if (typeof instance.componentDidUpdate === 'function') {
        if (oldProps !== current.memoizedProps || oldState !== current.memoizedState) {
          workInProgress.effectTag |= Update;
        }
      }

      // If shouldComponentUpdate returned false, we should still update the
      // memoized props/state to indicate that this work can be reused.
      memoizeProps(workInProgress, newProps);
      memoizeState(workInProgress, newState);
    }

    // Update the existing instance's state, props, and context pointers even
    // if shouldComponentUpdate returns false.
    instance.props = newProps;
    instance.state = newState;
    instance.context = newContext;

    return shouldUpdate;
  }

  return {
    adoptClassInstance: adoptClassInstance,
    constructClassInstance: constructClassInstance,
    mountClassInstance: mountClassInstance,
    // resumeMountClassInstance,
    updateClassInstance: updateClassInstance
  };
};

// The Symbol used to tag the ReactElement-like types. If there is no native Symbol
// nor polyfill, then a plain number is used for performance.
var hasSymbol = typeof Symbol === 'function' && Symbol['for'];

var REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;
var REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;
var REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;
var REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;
var REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;

var MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
var FAUX_ITERATOR_SYMBOL = '@@iterator';

function getIteratorFn(maybeIterable) {
  if (maybeIterable === null || typeof maybeIterable === 'undefined') {
    return null;
  }
  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];
  if (typeof maybeIterator === 'function') {
    return maybeIterator;
  }
  return null;
}

var getCurrentFiberStackAddendum$1 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;


{
  var didWarnAboutMaps = false;
  /**
   * Warn if there's no key explicitly set on dynamic arrays of children or
   * object keys are not valid. This allows us to keep track of children between
   * updates.
   */
  var ownerHasKeyUseWarning = {};
  var ownerHasFunctionTypeWarning = {};

  var warnForMissingKey = function (child) {
    if (child === null || typeof child !== 'object') {
      return;
    }
    if (!child._store || child._store.validated || child.key != null) {
      return;
    }
    !(typeof child._store === 'object') ? invariant(false, 'React Component in warnForMissingKey should have a _store. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    child._store.validated = true;

    var currentComponentErrorInfo = 'Each child in an array or iterator should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.' + (getCurrentFiberStackAddendum$1() || '');
    if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {
      return;
    }
    ownerHasKeyUseWarning[currentComponentErrorInfo] = true;

    warning(false, 'Each child in an array or iterator should have a unique ' + '"key" prop. See https://fb.me/react-warning-keys for ' + 'more information.%s', getCurrentFiberStackAddendum$1());
  };
}

var isArray$1 = Array.isArray;

function coerceRef(current, element) {
  var mixedRef = element.ref;
  if (mixedRef !== null && typeof mixedRef !== 'function') {
    if (element._owner) {
      var owner = element._owner;
      var inst = void 0;
      if (owner) {
        var ownerFiber = owner;
        !(ownerFiber.tag === ClassComponent) ? invariant(false, 'Stateless function components cannot have refs.') : void 0;
        inst = ownerFiber.stateNode;
      }
      !inst ? invariant(false, 'Missing owner for string ref %s. This error is likely caused by a bug in React. Please file an issue.', mixedRef) : void 0;
      var stringRef = '' + mixedRef;
      // Check if previous string ref matches new string ref
      if (current !== null && current.ref !== null && current.ref._stringRef === stringRef) {
        return current.ref;
      }
      var ref = function (value) {
        var refs = inst.refs === emptyObject ? inst.refs = {} : inst.refs;
        if (value === null) {
          delete refs[stringRef];
        } else {
          refs[stringRef] = value;
        }
      };
      ref._stringRef = stringRef;
      return ref;
    } else {
      !(typeof mixedRef === 'string') ? invariant(false, 'Expected ref to be a function or a string.') : void 0;
      !element._owner ? invariant(false, 'Element ref was specified as a string (%s) but no owner was set. You may have multiple copies of React loaded. (details: https://fb.me/react-refs-must-have-owner).', mixedRef) : void 0;
    }
  }
  return mixedRef;
}

function throwOnInvalidObjectType(returnFiber, newChild) {
  if (returnFiber.type !== 'textarea') {
    var addendum = '';
    {
      addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + (getCurrentFiberStackAddendum$1() || '');
    }
    invariant(false, 'Objects are not valid as a React child (found: %s).%s', Object.prototype.toString.call(newChild) === '[object Object]' ? 'object with keys {' + Object.keys(newChild).join(', ') + '}' : newChild, addendum);
  }
}

function warnOnFunctionType() {
  var currentComponentErrorInfo = 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.' + (getCurrentFiberStackAddendum$1() || '');

  if (ownerHasFunctionTypeWarning[currentComponentErrorInfo]) {
    return;
  }
  ownerHasFunctionTypeWarning[currentComponentErrorInfo] = true;

  warning(false, 'Functions are not valid as a React child. This may happen if ' + 'you return a Component instead of <Component /> from render. ' + 'Or maybe you meant to call this function rather than return it.%s', getCurrentFiberStackAddendum$1() || '');
}

// This wrapper function exists because I expect to clone the code in each path
// to be able to optimize each path individually by branching early. This needs
// a compiler or we can do it manually. Helpers that don't need this branching
// live outside of this function.
function ChildReconciler(shouldTrackSideEffects) {
  function deleteChild(returnFiber, childToDelete) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return;
    }
    // Deletions are added in reversed order so we add it to the front.
    // At this point, the return fiber's effect list is empty except for
    // deletions, so we can just append the deletion to the list. The remaining
    // effects aren't added until the complete phase. Once we implement
    // resuming, this may not be true.
    var last = returnFiber.lastEffect;
    if (last !== null) {
      last.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }
    childToDelete.nextEffect = null;
    childToDelete.effectTag = Deletion;
  }

  function deleteRemainingChildren(returnFiber, currentFirstChild) {
    if (!shouldTrackSideEffects) {
      // Noop.
      return null;
    }

    // TODO: For the shouldClone case, this could be micro-optimized a bit by
    // assuming that after the first child we've already added everything.
    var childToDelete = currentFirstChild;
    while (childToDelete !== null) {
      deleteChild(returnFiber, childToDelete);
      childToDelete = childToDelete.sibling;
    }
    return null;
  }

  function mapRemainingChildren(returnFiber, currentFirstChild) {
    // Add the remaining children to a temporary map so that we can find them by
    // keys quickly. Implicit (null) keys get added to this set with their index
    var existingChildren = new Map();

    var existingChild = currentFirstChild;
    while (existingChild !== null) {
      if (existingChild.key !== null) {
        existingChildren.set(existingChild.key, existingChild);
      } else {
        existingChildren.set(existingChild.index, existingChild);
      }
      existingChild = existingChild.sibling;
    }
    return existingChildren;
  }

  function useFiber(fiber, pendingProps, expirationTime) {
    // We currently set sibling to null and index to 0 here because it is easy
    // to forget to do before returning it. E.g. for the single child case.
    var clone = createWorkInProgress(fiber, pendingProps, expirationTime);
    clone.index = 0;
    clone.sibling = null;
    return clone;
  }

  function placeChild(newFiber, lastPlacedIndex, newIndex) {
    newFiber.index = newIndex;
    if (!shouldTrackSideEffects) {
      // Noop.
      return lastPlacedIndex;
    }
    var current = newFiber.alternate;
    if (current !== null) {
      var oldIndex = current.index;
      if (oldIndex < lastPlacedIndex) {
        // This is a move.
        newFiber.effectTag = Placement;
        return lastPlacedIndex;
      } else {
        // This item can stay in place.
        return oldIndex;
      }
    } else {
      // This is an insertion.
      newFiber.effectTag = Placement;
      return lastPlacedIndex;
    }
  }

  function placeSingleChild(newFiber) {
    // This is simpler for the single child case. We only need to do a
    // placement for inserting new children.
    if (shouldTrackSideEffects && newFiber.alternate === null) {
      newFiber.effectTag = Placement;
    }
    return newFiber;
  }

  function updateTextNode(returnFiber, current, textContent, expirationTime) {
    if (current === null || current.tag !== HostText) {
      // Insert
      var created = createFiberFromText(textContent, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, textContent, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateElement(returnFiber, current, element, expirationTime) {
    if (current !== null && current.type === element.type) {
      // Move based on index
      var existing = useFiber(current, element.props, expirationTime);
      existing.ref = coerceRef(current, element);
      existing['return'] = returnFiber;
      {
        existing._debugSource = element._source;
        existing._debugOwner = element._owner;
      }
      return existing;
    } else {
      // Insert
      var created = createFiberFromElement(element, returnFiber.internalContextTag, expirationTime);
      created.ref = coerceRef(current, element);
      created['return'] = returnFiber;
      return created;
    }
  }

  function updateCall(returnFiber, current, call, expirationTime) {
    // TODO: Should this also compare handler to determine whether to reuse?
    if (current === null || current.tag !== CallComponent) {
      // Insert
      var created = createFiberFromCall(call, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Move based on index
      var existing = useFiber(current, call, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateReturn(returnFiber, current, returnNode, expirationTime) {
    if (current === null || current.tag !== ReturnComponent) {
      // Insert
      var created = createFiberFromReturn(returnNode, returnFiber.internalContextTag, expirationTime);
      created.type = returnNode.value;
      created['return'] = returnFiber;
      return created;
    } else {
      // Move based on index
      var existing = useFiber(current, null, expirationTime);
      existing.type = returnNode.value;
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updatePortal(returnFiber, current, portal, expirationTime) {
    if (current === null || current.tag !== HostPortal || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation) {
      // Insert
      var created = createFiberFromPortal(portal, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, portal.children || [], expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function updateFragment(returnFiber, current, fragment, expirationTime, key) {
    if (current === null || current.tag !== Fragment) {
      // Insert
      var created = createFiberFromFragment(fragment, returnFiber.internalContextTag, expirationTime, key);
      created['return'] = returnFiber;
      return created;
    } else {
      // Update
      var existing = useFiber(current, fragment, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
  }

  function createChild(returnFiber, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      var created = createFiberFromText('' + newChild, returnFiber.internalContextTag, expirationTime);
      created['return'] = returnFiber;
      return created;
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            if (newChild.type === REACT_FRAGMENT_TYPE) {
              var _created = createFiberFromFragment(newChild.props.children, returnFiber.internalContextTag, expirationTime, newChild.key);
              _created['return'] = returnFiber;
              return _created;
            } else {
              var _created2 = createFiberFromElement(newChild, returnFiber.internalContextTag, expirationTime);
              _created2.ref = coerceRef(null, newChild);
              _created2['return'] = returnFiber;
              return _created2;
            }
          }

        case REACT_CALL_TYPE:
          {
            var _created3 = createFiberFromCall(newChild, returnFiber.internalContextTag, expirationTime);
            _created3['return'] = returnFiber;
            return _created3;
          }

        case REACT_RETURN_TYPE:
          {
            var _created4 = createFiberFromReturn(newChild, returnFiber.internalContextTag, expirationTime);
            _created4.type = newChild.value;
            _created4['return'] = returnFiber;
            return _created4;
          }

        case REACT_PORTAL_TYPE:
          {
            var _created5 = createFiberFromPortal(newChild, returnFiber.internalContextTag, expirationTime);
            _created5['return'] = returnFiber;
            return _created5;
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _created6 = createFiberFromFragment(newChild, returnFiber.internalContextTag, expirationTime, null);
        _created6['return'] = returnFiber;
        return _created6;
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateSlot(returnFiber, oldFiber, newChild, expirationTime) {
    // Update the fiber if the keys match, otherwise return null.

    var key = oldFiber !== null ? oldFiber.key : null;

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys. If the previous node is implicitly keyed
      // we can continue to replace it without aborting even if it is not a text
      // node.
      if (key !== null) {
        return null;
      }
      return updateTextNode(returnFiber, oldFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            if (newChild.key === key) {
              if (newChild.type === REACT_FRAGMENT_TYPE) {
                return updateFragment(returnFiber, oldFiber, newChild.props.children, expirationTime, key);
              }
              return updateElement(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_CALL_TYPE:
          {
            if (newChild.key === key) {
              return updateCall(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_RETURN_TYPE:
          {
            // Returns don't have keys. If the previous node is implicitly keyed
            // we can continue to replace it without aborting even if it is not a
            // yield.
            if (key === null) {
              return updateReturn(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }

        case REACT_PORTAL_TYPE:
          {
            if (newChild.key === key) {
              return updatePortal(returnFiber, oldFiber, newChild, expirationTime);
            } else {
              return null;
            }
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        if (key !== null) {
          return null;
        }

        return updateFragment(returnFiber, oldFiber, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  function updateFromMap(existingChildren, returnFiber, newIdx, newChild, expirationTime) {
    if (typeof newChild === 'string' || typeof newChild === 'number') {
      // Text nodes don't have keys, so we neither have to check the old nor
      // new node for the key. If both are text nodes, they match.
      var matchedFiber = existingChildren.get(newIdx) || null;
      return updateTextNode(returnFiber, matchedFiber, '' + newChild, expirationTime);
    }

    if (typeof newChild === 'object' && newChild !== null) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          {
            var _matchedFiber = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            if (newChild.type === REACT_FRAGMENT_TYPE) {
              return updateFragment(returnFiber, _matchedFiber, newChild.props.children, expirationTime, newChild.key);
            }
            return updateElement(returnFiber, _matchedFiber, newChild, expirationTime);
          }

        case REACT_CALL_TYPE:
          {
            var _matchedFiber2 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            return updateCall(returnFiber, _matchedFiber2, newChild, expirationTime);
          }

        case REACT_RETURN_TYPE:
          {
            // Returns don't have keys, so we neither have to check the old nor
            // new node for the key. If both are returns, they match.
            var _matchedFiber3 = existingChildren.get(newIdx) || null;
            return updateReturn(returnFiber, _matchedFiber3, newChild, expirationTime);
          }

        case REACT_PORTAL_TYPE:
          {
            var _matchedFiber4 = existingChildren.get(newChild.key === null ? newIdx : newChild.key) || null;
            return updatePortal(returnFiber, _matchedFiber4, newChild, expirationTime);
          }
      }

      if (isArray$1(newChild) || getIteratorFn(newChild)) {
        var _matchedFiber5 = existingChildren.get(newIdx) || null;
        return updateFragment(returnFiber, _matchedFiber5, newChild, expirationTime, null);
      }

      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }

    return null;
  }

  /**
   * Warns if there is a duplicate or missing key
   */
  function warnOnInvalidKey(child, knownKeys) {
    {
      if (typeof child !== 'object' || child === null) {
        return knownKeys;
      }
      switch (child.$$typeof) {
        case REACT_ELEMENT_TYPE:
        case REACT_CALL_TYPE:
        case REACT_PORTAL_TYPE:
          warnForMissingKey(child);
          var key = child.key;
          if (typeof key !== 'string') {
            break;
          }
          if (knownKeys === null) {
            knownKeys = new Set();
            knownKeys.add(key);
            break;
          }
          if (!knownKeys.has(key)) {
            knownKeys.add(key);
            break;
          }
          warning(false, 'Encountered two children with the same key, `%s`. ' + 'Keys should be unique so that components maintain their identity ' + 'across updates. Non-unique keys may cause children to be ' + 'duplicated and/or omitted — the behavior is unsupported and ' + 'could change in a future version.%s', key, getCurrentFiberStackAddendum$1());
          break;
        default:
          break;
      }
    }
    return knownKeys;
  }

  function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, expirationTime) {
    // This algorithm can't optimize by searching from boths ends since we
    // don't have backpointers on fibers. I'm trying to see how far we can get
    // with that model. If it ends up not being worth the tradeoffs, we can
    // add it later.

    // Even with a two ended optimization, we'd want to optimize for the case
    // where there are few changes and brute force the comparison instead of
    // going for the Map. It'd like to explore hitting that path first in
    // forward-only mode and only go for the Map once we notice that we need
    // lots of look ahead. This doesn't handle reversal as well as two ended
    // search but that's unusual. Besides, for the two ended optimization to
    // work on Iterables, we'd need to copy the whole set.

    // In this first iteration, we'll just live with hitting the bad case
    // (adding everything to a Map) in for every insert/move.

    // If you change this code, also update reconcileChildrenIterator() which
    // uses the same algorithm.

    {
      // First, validate keys.
      var knownKeys = null;
      for (var i = 0; i < newChildren.length; i++) {
        var child = newChildren[i];
        knownKeys = warnOnInvalidKey(child, knownKeys);
      }
    }

    var resultingFirstChild = null;
    var previousNewFiber = null;

    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;
    for (; oldFiber !== null && newIdx < newChildren.length; newIdx++) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }
      var newFiber = updateSlot(returnFiber, oldFiber, newChildren[newIdx], expirationTime);
      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (oldFiber === null) {
          oldFiber = nextOldFiber;
        }
        break;
      }
      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }
      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }
      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (newIdx === newChildren.length) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; newIdx < newChildren.length; newIdx++) {
        var _newFiber = createChild(returnFiber, newChildren[newIdx], expirationTime);
        if (!_newFiber) {
          continue;
        }
        lastPlacedIndex = placeChild(_newFiber, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber;
        } else {
          previousNewFiber.sibling = _newFiber;
        }
        previousNewFiber = _newFiber;
      }
      return resultingFirstChild;
    }

    // Add all children to a key map for quick lookups.
    var existingChildren = mapRemainingChildren(returnFiber, oldFiber);

    // Keep scanning and use the map to restore deleted items as moves.
    for (; newIdx < newChildren.length; newIdx++) {
      var _newFiber2 = updateFromMap(existingChildren, returnFiber, newIdx, newChildren[newIdx], expirationTime);
      if (_newFiber2) {
        if (shouldTrackSideEffects) {
          if (_newFiber2.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren['delete'](_newFiber2.key === null ? newIdx : _newFiber2.key);
          }
        }
        lastPlacedIndex = placeChild(_newFiber2, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber2;
        } else {
          previousNewFiber.sibling = _newFiber2;
        }
        previousNewFiber = _newFiber2;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildrenIterable, expirationTime) {
    // This is the same implementation as reconcileChildrenArray(),
    // but using the iterator instead.

    var iteratorFn = getIteratorFn(newChildrenIterable);
    !(typeof iteratorFn === 'function') ? invariant(false, 'An object is not an iterable. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    {
      // Warn about using Maps as children
      if (typeof newChildrenIterable.entries === 'function') {
        var possibleMap = newChildrenIterable;
        if (possibleMap.entries === iteratorFn) {
          warning(didWarnAboutMaps, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', getCurrentFiberStackAddendum$1());
          didWarnAboutMaps = true;
        }
      }

      // First, validate keys.
      // We'll get a different iterator later for the main pass.
      var _newChildren = iteratorFn.call(newChildrenIterable);
      if (_newChildren) {
        var knownKeys = null;
        var _step = _newChildren.next();
        for (; !_step.done; _step = _newChildren.next()) {
          var child = _step.value;
          knownKeys = warnOnInvalidKey(child, knownKeys);
        }
      }
    }

    var newChildren = iteratorFn.call(newChildrenIterable);
    !(newChildren != null) ? invariant(false, 'An iterable object provided no iterator.') : void 0;

    var resultingFirstChild = null;
    var previousNewFiber = null;

    var oldFiber = currentFirstChild;
    var lastPlacedIndex = 0;
    var newIdx = 0;
    var nextOldFiber = null;

    var step = newChildren.next();
    for (; oldFiber !== null && !step.done; newIdx++, step = newChildren.next()) {
      if (oldFiber.index > newIdx) {
        nextOldFiber = oldFiber;
        oldFiber = null;
      } else {
        nextOldFiber = oldFiber.sibling;
      }
      var newFiber = updateSlot(returnFiber, oldFiber, step.value, expirationTime);
      if (newFiber === null) {
        // TODO: This breaks on empty slots like null children. That's
        // unfortunate because it triggers the slow path all the time. We need
        // a better way to communicate whether this was a miss or null,
        // boolean, undefined, etc.
        if (!oldFiber) {
          oldFiber = nextOldFiber;
        }
        break;
      }
      if (shouldTrackSideEffects) {
        if (oldFiber && newFiber.alternate === null) {
          // We matched the slot, but we didn't reuse the existing fiber, so we
          // need to delete the existing child.
          deleteChild(returnFiber, oldFiber);
        }
      }
      lastPlacedIndex = placeChild(newFiber, lastPlacedIndex, newIdx);
      if (previousNewFiber === null) {
        // TODO: Move out of the loop. This only happens for the first run.
        resultingFirstChild = newFiber;
      } else {
        // TODO: Defer siblings if we're not at the right index for this slot.
        // I.e. if we had null values before, then we want to defer this
        // for each null value. However, we also don't want to call updateSlot
        // with the previous one.
        previousNewFiber.sibling = newFiber;
      }
      previousNewFiber = newFiber;
      oldFiber = nextOldFiber;
    }

    if (step.done) {
      // We've reached the end of the new children. We can delete the rest.
      deleteRemainingChildren(returnFiber, oldFiber);
      return resultingFirstChild;
    }

    if (oldFiber === null) {
      // If we don't have any more existing children we can choose a fast path
      // since the rest will all be insertions.
      for (; !step.done; newIdx++, step = newChildren.next()) {
        var _newFiber3 = createChild(returnFiber, step.value, expirationTime);
        if (_newFiber3 === null) {
          continue;
        }
        lastPlacedIndex = placeChild(_newFiber3, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          // TODO: Move out of the loop. This only happens for the first run.
          resultingFirstChild = _newFiber3;
        } else {
          previousNewFiber.sibling = _newFiber3;
        }
        previousNewFiber = _newFiber3;
      }
      return resultingFirstChild;
    }

    // Add all children to a key map for quick lookups.
    var existingChildren = mapRemainingChildren(returnFiber, oldFiber);

    // Keep scanning and use the map to restore deleted items as moves.
    for (; !step.done; newIdx++, step = newChildren.next()) {
      var _newFiber4 = updateFromMap(existingChildren, returnFiber, newIdx, step.value, expirationTime);
      if (_newFiber4 !== null) {
        if (shouldTrackSideEffects) {
          if (_newFiber4.alternate !== null) {
            // The new fiber is a work in progress, but if there exists a
            // current, that means that we reused the fiber. We need to delete
            // it from the child list so that we don't add it to the deletion
            // list.
            existingChildren['delete'](_newFiber4.key === null ? newIdx : _newFiber4.key);
          }
        }
        lastPlacedIndex = placeChild(_newFiber4, lastPlacedIndex, newIdx);
        if (previousNewFiber === null) {
          resultingFirstChild = _newFiber4;
        } else {
          previousNewFiber.sibling = _newFiber4;
        }
        previousNewFiber = _newFiber4;
      }
    }

    if (shouldTrackSideEffects) {
      // Any existing children that weren't consumed above were deleted. We need
      // to add them to the deletion list.
      existingChildren.forEach(function (child) {
        return deleteChild(returnFiber, child);
      });
    }

    return resultingFirstChild;
  }

  function reconcileSingleTextNode(returnFiber, currentFirstChild, textContent, expirationTime) {
    // There's no need to check for keys on text nodes since we don't have a
    // way to define them.
    if (currentFirstChild !== null && currentFirstChild.tag === HostText) {
      // We already have an existing node so let's just update it and delete
      // the rest.
      deleteRemainingChildren(returnFiber, currentFirstChild.sibling);
      var existing = useFiber(currentFirstChild, textContent, expirationTime);
      existing['return'] = returnFiber;
      return existing;
    }
    // The existing first child is not a text node so we need to create one
    // and delete the existing ones.
    deleteRemainingChildren(returnFiber, currentFirstChild);
    var created = createFiberFromText(textContent, returnFiber.internalContextTag, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSingleElement(returnFiber, currentFirstChild, element, expirationTime) {
    var key = element.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === Fragment ? element.type === REACT_FRAGMENT_TYPE : child.type === element.type) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, element.type === REACT_FRAGMENT_TYPE ? element.props.children : element.props, expirationTime);
          existing.ref = coerceRef(child, element);
          existing['return'] = returnFiber;
          {
            existing._debugSource = element._source;
            existing._debugOwner = element._owner;
          }
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    if (element.type === REACT_FRAGMENT_TYPE) {
      var created = createFiberFromFragment(element.props.children, returnFiber.internalContextTag, expirationTime, element.key);
      created['return'] = returnFiber;
      return created;
    } else {
      var _created7 = createFiberFromElement(element, returnFiber.internalContextTag, expirationTime);
      _created7.ref = coerceRef(currentFirstChild, element);
      _created7['return'] = returnFiber;
      return _created7;
    }
  }

  function reconcileSingleCall(returnFiber, currentFirstChild, call, expirationTime) {
    var key = call.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === CallComponent) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, call, expirationTime);
          existing['return'] = returnFiber;
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    var created = createFiberFromCall(call, returnFiber.internalContextTag, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSingleReturn(returnFiber, currentFirstChild, returnNode, expirationTime) {
    // There's no need to check for keys on yields since they're stateless.
    var child = currentFirstChild;
    if (child !== null) {
      if (child.tag === ReturnComponent) {
        deleteRemainingChildren(returnFiber, child.sibling);
        var existing = useFiber(child, null, expirationTime);
        existing.type = returnNode.value;
        existing['return'] = returnFiber;
        return existing;
      } else {
        deleteRemainingChildren(returnFiber, child);
      }
    }

    var created = createFiberFromReturn(returnNode, returnFiber.internalContextTag, expirationTime);
    created.type = returnNode.value;
    created['return'] = returnFiber;
    return created;
  }

  function reconcileSinglePortal(returnFiber, currentFirstChild, portal, expirationTime) {
    var key = portal.key;
    var child = currentFirstChild;
    while (child !== null) {
      // TODO: If key === null and child.key === null, then this only applies to
      // the first item in the list.
      if (child.key === key) {
        if (child.tag === HostPortal && child.stateNode.containerInfo === portal.containerInfo && child.stateNode.implementation === portal.implementation) {
          deleteRemainingChildren(returnFiber, child.sibling);
          var existing = useFiber(child, portal.children || [], expirationTime);
          existing['return'] = returnFiber;
          return existing;
        } else {
          deleteRemainingChildren(returnFiber, child);
          break;
        }
      } else {
        deleteChild(returnFiber, child);
      }
      child = child.sibling;
    }

    var created = createFiberFromPortal(portal, returnFiber.internalContextTag, expirationTime);
    created['return'] = returnFiber;
    return created;
  }

  // This API will tag the children with the side-effect of the reconciliation
  // itself. They will be added to the side-effect list as we pass through the
  // children and the parent.
  function reconcileChildFibers(returnFiber, currentFirstChild, newChild, expirationTime) {
    // This function is not recursive.
    // If the top level item is an array, we treat it as a set of children,
    // not as a fragment. Nested arrays on the other hand will be treated as
    // fragment nodes. Recursion happens at the normal flow.

    // Handle top level unkeyed fragments as if they were arrays.
    // This leads to an ambiguity between <>{[...]}</> and <>...</>.
    // We treat the ambiguous cases above the same.
    if (typeof newChild === 'object' && newChild !== null && newChild.type === REACT_FRAGMENT_TYPE && newChild.key === null) {
      newChild = newChild.props.children;
    }

    // Handle object types
    var isObject = typeof newChild === 'object' && newChild !== null;

    if (isObject) {
      switch (newChild.$$typeof) {
        case REACT_ELEMENT_TYPE:
          return placeSingleChild(reconcileSingleElement(returnFiber, currentFirstChild, newChild, expirationTime));

        case REACT_CALL_TYPE:
          return placeSingleChild(reconcileSingleCall(returnFiber, currentFirstChild, newChild, expirationTime));
        case REACT_RETURN_TYPE:
          return placeSingleChild(reconcileSingleReturn(returnFiber, currentFirstChild, newChild, expirationTime));
        case REACT_PORTAL_TYPE:
          return placeSingleChild(reconcileSinglePortal(returnFiber, currentFirstChild, newChild, expirationTime));
      }
    }

    if (typeof newChild === 'string' || typeof newChild === 'number') {
      return placeSingleChild(reconcileSingleTextNode(returnFiber, currentFirstChild, '' + newChild, expirationTime));
    }

    if (isArray$1(newChild)) {
      return reconcileChildrenArray(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (getIteratorFn(newChild)) {
      return reconcileChildrenIterator(returnFiber, currentFirstChild, newChild, expirationTime);
    }

    if (isObject) {
      throwOnInvalidObjectType(returnFiber, newChild);
    }

    {
      if (typeof newChild === 'function') {
        warnOnFunctionType();
      }
    }
    if (typeof newChild === 'undefined') {
      // If the new child is undefined, and the return fiber is a composite
      // component, throw an error. If Fiber return types are disabled,
      // we already threw above.
      switch (returnFiber.tag) {
        case ClassComponent:
          {
            {
              var instance = returnFiber.stateNode;
              if (instance.render._isMockFunction) {
                // We allow auto-mocks to proceed as if they're returning null.
                break;
              }
            }
          }
        // Intentionally fall through to the next case, which handles both
        // functions and classes
        // eslint-disable-next-lined no-fallthrough
        case FunctionalComponent:
          {
            var Component = returnFiber.type;
            invariant(false, '%s(...): Nothing was returned from render. This usually means a return statement is missing. Or, to render nothing, return null.', Component.displayName || Component.name || 'Component');
          }
      }
    }

    // Remaining cases are all treated as empty.
    return deleteRemainingChildren(returnFiber, currentFirstChild);
  }

  return reconcileChildFibers;
}

var reconcileChildFibers = ChildReconciler(true);
var mountChildFibers = ChildReconciler(false);

function cloneChildFibers(current, workInProgress) {
  !(current === null || workInProgress.child === current.child) ? invariant(false, 'Resuming work not yet implemented.') : void 0;

  if (workInProgress.child === null) {
    return;
  }

  var currentChild = workInProgress.child;
  var newChild = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
  workInProgress.child = newChild;

  newChild['return'] = workInProgress;
  while (currentChild.sibling !== null) {
    currentChild = currentChild.sibling;
    newChild = newChild.sibling = createWorkInProgress(currentChild, currentChild.pendingProps, currentChild.expirationTime);
    newChild['return'] = workInProgress;
  }
  newChild.sibling = null;
}

{
  var warnedAboutStatelessRefs = {};
}

var ReactFiberBeginWork = function (config, hostContext, hydrationContext, scheduleWork, computeExpirationForFiber) {
  var shouldSetTextContent = config.shouldSetTextContent,
      useSyncScheduling = config.useSyncScheduling,
      shouldDeprioritizeSubtree = config.shouldDeprioritizeSubtree;
  var pushHostContext = hostContext.pushHostContext,
      pushHostContainer = hostContext.pushHostContainer;
  var enterHydrationState = hydrationContext.enterHydrationState,
      resetHydrationState = hydrationContext.resetHydrationState,
      tryToClaimNextHydratableInstance = hydrationContext.tryToClaimNextHydratableInstance;

  var _ReactFiberClassCompo = ReactFiberClassComponent(scheduleWork, computeExpirationForFiber, memoizeProps, memoizeState),
      adoptClassInstance = _ReactFiberClassCompo.adoptClassInstance,
      constructClassInstance = _ReactFiberClassCompo.constructClassInstance,
      mountClassInstance = _ReactFiberClassCompo.mountClassInstance,
      updateClassInstance = _ReactFiberClassCompo.updateClassInstance;

  // TODO: Remove this and use reconcileChildrenAtExpirationTime directly.


  function reconcileChildren(current, workInProgress, nextChildren) {
    reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, workInProgress.expirationTime);
  }

  function reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, renderExpirationTime) {
    if (current === null) {
      // If this is a fresh new component that hasn't been rendered yet, we
      // won't update its child set by applying minimal side-effects. Instead,
      // we will add them all to the child before it gets rendered. That means
      // we can optimize this reconciliation pass by not tracking side-effects.
      workInProgress.child = mountChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
    } else {
      // If the current child is the same as the work in progress, it means that
      // we haven't yet started any work on these children. Therefore, we use
      // the clone algorithm to create a copy of all the current children.

      // If we had any progressed work already, that is invalid at this point so
      // let's throw it out.
      workInProgress.child = reconcileChildFibers(workInProgress, current.child, nextChildren, renderExpirationTime);
    }
  }

  function updateFragment(current, workInProgress) {
    var nextChildren = workInProgress.pendingProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextChildren === null) {
        nextChildren = workInProgress.memoizedProps;
      }
    } else if (nextChildren === null || workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextChildren);
    return workInProgress.child;
  }

  function markRef(current, workInProgress) {
    var ref = workInProgress.ref;
    if (ref !== null && (!current || current.ref !== ref)) {
      // Schedule a Ref effect
      workInProgress.effectTag |= Ref;
    }
  }

  function updateFunctionalComponent(current, workInProgress) {
    var fn = workInProgress.type;
    var nextProps = workInProgress.pendingProps;

    var memoizedProps = workInProgress.memoizedProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextProps === null) {
        nextProps = memoizedProps;
      }
    } else {
      if (nextProps === null || memoizedProps === nextProps) {
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      }
      // TODO: consider bringing fn.shouldComponentUpdate() back.
      // It used to be here.
    }

    var unmaskedContext = getUnmaskedContext(workInProgress);
    var context = getMaskedContext(workInProgress, unmaskedContext);

    var nextChildren;

    {
      ReactCurrentOwner.current = workInProgress;
      ReactDebugCurrentFiber.setCurrentPhase('render');
      nextChildren = fn(nextProps, context);
      ReactDebugCurrentFiber.setCurrentPhase(null);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;
    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextProps);
    return workInProgress.child;
  }

  function updateClassComponent(current, workInProgress, renderExpirationTime) {
    // Push context providers early to prevent context stack mismatches.
    // During mounting we don't know the child context yet as the instance doesn't exist.
    // We will invalidate the child context in finishClassComponent() right after rendering.
    var hasContext = pushContextProvider(workInProgress);

    var shouldUpdate = void 0;
    if (current === null) {
      if (!workInProgress.stateNode) {
        // In the initial pass we might need to construct the instance.
        constructClassInstance(workInProgress, workInProgress.pendingProps);
        mountClassInstance(workInProgress, renderExpirationTime);
        shouldUpdate = true;
      } else {
        invariant(false, 'Resuming work not yet implemented.');
        // In a resume, we'll already have an instance we can reuse.
        // shouldUpdate = resumeMountClassInstance(workInProgress, renderExpirationTime);
      }
    } else {
      shouldUpdate = updateClassInstance(current, workInProgress, renderExpirationTime);
    }
    return finishClassComponent(current, workInProgress, shouldUpdate, hasContext);
  }

  function finishClassComponent(current, workInProgress, shouldUpdate, hasContext) {
    // Refs should update even if shouldComponentUpdate returns false
    markRef(current, workInProgress);

    if (!shouldUpdate) {
      // Context providers should defer to sCU for rendering
      if (hasContext) {
        invalidateContextProvider(workInProgress, false);
      }

      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var instance = workInProgress.stateNode;

    // Rerender
    ReactCurrentOwner.current = workInProgress;
    var nextChildren = void 0;
    {
      ReactDebugCurrentFiber.setCurrentPhase('render');
      nextChildren = instance.render();
      if (debugRenderPhaseSideEffects) {
        instance.render();
      }
      ReactDebugCurrentFiber.setCurrentPhase(null);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;
    reconcileChildren(current, workInProgress, nextChildren);
    // Memoize props and state using the values we just used to render.
    // TODO: Restructure so we never read values from the instance.
    memoizeState(workInProgress, instance.state);
    memoizeProps(workInProgress, instance.props);

    // The context might have changed so we need to recalculate it.
    if (hasContext) {
      invalidateContextProvider(workInProgress, true);
    }

    return workInProgress.child;
  }

  function pushHostRootContext(workInProgress) {
    var root = workInProgress.stateNode;
    if (root.pendingContext) {
      pushTopLevelContextObject(workInProgress, root.pendingContext, root.pendingContext !== root.context);
    } else if (root.context) {
      // Should always be set
      pushTopLevelContextObject(workInProgress, root.context, false);
    }
    pushHostContainer(workInProgress, root.containerInfo);
  }

  function updateHostRoot(current, workInProgress, renderExpirationTime) {
    pushHostRootContext(workInProgress);
    var updateQueue = workInProgress.updateQueue;
    if (updateQueue !== null) {
      var prevState = workInProgress.memoizedState;
      var state = processUpdateQueue(current, workInProgress, updateQueue, null, null, renderExpirationTime);
      if (prevState === state) {
        // If the state is the same as before, that's a bailout because we had
        // no work that expires at this time.
        resetHydrationState();
        return bailoutOnAlreadyFinishedWork(current, workInProgress);
      }
      var element = state.element;
      var root = workInProgress.stateNode;
      if ((current === null || current.child === null) && root.hydrate && enterHydrationState(workInProgress)) {
        // If we don't have any current children this might be the first pass.
        // We always try to hydrate. If this isn't a hydration pass there won't
        // be any children to hydrate which is effectively the same thing as
        // not hydrating.

        // This is a bit of a hack. We track the host root as a placement to
        // know that we're currently in a mounting state. That way isMounted
        // works as expected. We must reset this before committing.
        // TODO: Delete this when we delete isMounted and findDOMNode.
        workInProgress.effectTag |= Placement;

        // Ensure that children mount into this root without tracking
        // side-effects. This ensures that we don't store Placement effects on
        // nodes that will be hydrated.
        workInProgress.child = mountChildFibers(workInProgress, null, element, renderExpirationTime);
      } else {
        // Otherwise reset hydration state in case we aborted and resumed another
        // root.
        resetHydrationState();
        reconcileChildren(current, workInProgress, element);
      }
      memoizeState(workInProgress, state);
      return workInProgress.child;
    }
    resetHydrationState();
    // If there is no update queue, that's a bailout because the root has no props.
    return bailoutOnAlreadyFinishedWork(current, workInProgress);
  }

  function updateHostComponent(current, workInProgress, renderExpirationTime) {
    pushHostContext(workInProgress);

    if (current === null) {
      tryToClaimNextHydratableInstance(workInProgress);
    }

    var type = workInProgress.type;
    var memoizedProps = workInProgress.memoizedProps;
    var nextProps = workInProgress.pendingProps;
    if (nextProps === null) {
      nextProps = memoizedProps;
      !(nextProps !== null) ? invariant(false, 'We should always have pending or current props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    }
    var prevProps = current !== null ? current.memoizedProps : null;

    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
    } else if (nextProps === null || memoizedProps === nextProps) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var nextChildren = nextProps.children;
    var isDirectTextChild = shouldSetTextContent(type, nextProps);

    if (isDirectTextChild) {
      // We special case a direct text child of a host node. This is a common
      // case. We won't handle it as a reified child. We will instead handle
      // this in the host environment that also have access to this prop. That
      // avoids allocating another HostText fiber and traversing it.
      nextChildren = null;
    } else if (prevProps && shouldSetTextContent(type, prevProps)) {
      // If we're switching from a direct text child to a normal child, or to
      // empty, we need to schedule the text content to be reset.
      workInProgress.effectTag |= ContentReset;
    }

    markRef(current, workInProgress);

    // Check the host config to see if the children are offscreen/hidden.
    if (renderExpirationTime !== Never && !useSyncScheduling && shouldDeprioritizeSubtree(type, nextProps)) {
      // Down-prioritize the children.
      workInProgress.expirationTime = Never;
      // Bailout and come back to this fiber later.
      return null;
    }

    reconcileChildren(current, workInProgress, nextChildren);
    memoizeProps(workInProgress, nextProps);
    return workInProgress.child;
  }

  function updateHostText(current, workInProgress) {
    if (current === null) {
      tryToClaimNextHydratableInstance(workInProgress);
    }
    var nextProps = workInProgress.pendingProps;
    if (nextProps === null) {
      nextProps = workInProgress.memoizedProps;
    }
    memoizeProps(workInProgress, nextProps);
    // Nothing to do here. This is terminal. We'll do the completion step
    // immediately after.
    return null;
  }

  function mountIndeterminateComponent(current, workInProgress, renderExpirationTime) {
    !(current === null) ? invariant(false, 'An indeterminate component should never have mounted. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    var fn = workInProgress.type;
    var props = workInProgress.pendingProps;
    var unmaskedContext = getUnmaskedContext(workInProgress);
    var context = getMaskedContext(workInProgress, unmaskedContext);

    var value;

    {
      if (fn.prototype && typeof fn.prototype.render === 'function') {
        var componentName = getComponentName(workInProgress);
        warning(false, "The <%s /> component appears to have a render method, but doesn't extend React.Component. " + 'This is likely to cause errors. Change %s to extend React.Component instead.', componentName, componentName);
      }
      ReactCurrentOwner.current = workInProgress;
      value = fn(props, context);
    }
    // React DevTools reads this flag.
    workInProgress.effectTag |= PerformedWork;

    if (typeof value === 'object' && value !== null && typeof value.render === 'function') {
      // Proceed under the assumption that this is a class instance
      workInProgress.tag = ClassComponent;

      // Push context providers early to prevent context stack mismatches.
      // During mounting we don't know the child context yet as the instance doesn't exist.
      // We will invalidate the child context in finishClassComponent() right after rendering.
      var hasContext = pushContextProvider(workInProgress);
      adoptClassInstance(workInProgress, value);
      mountClassInstance(workInProgress, renderExpirationTime);
      return finishClassComponent(current, workInProgress, true, hasContext);
    } else {
      // Proceed under the assumption that this is a functional component
      workInProgress.tag = FunctionalComponent;
      {
        var Component = workInProgress.type;

        if (Component) {
          warning(!Component.childContextTypes, '%s(...): childContextTypes cannot be defined on a functional component.', Component.displayName || Component.name || 'Component');
        }
        if (workInProgress.ref !== null) {
          var info = '';
          var ownerName = ReactDebugCurrentFiber.getCurrentFiberOwnerName();
          if (ownerName) {
            info += '\n\nCheck the render method of `' + ownerName + '`.';
          }

          var warningKey = ownerName || workInProgress._debugID || '';
          var debugSource = workInProgress._debugSource;
          if (debugSource) {
            warningKey = debugSource.fileName + ':' + debugSource.lineNumber;
          }
          if (!warnedAboutStatelessRefs[warningKey]) {
            warnedAboutStatelessRefs[warningKey] = true;
            warning(false, 'Stateless function components cannot be given refs. ' + 'Attempts to access this ref will fail.%s%s', info, ReactDebugCurrentFiber.getCurrentFiberStackAddendum());
          }
        }
      }
      reconcileChildren(current, workInProgress, value);
      memoizeProps(workInProgress, props);
      return workInProgress.child;
    }
  }

  function updateCallComponent(current, workInProgress, renderExpirationTime) {
    var nextCall = workInProgress.pendingProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextCall === null) {
        nextCall = current && current.memoizedProps;
        !(nextCall !== null) ? invariant(false, 'We should always have pending or current props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
      }
    } else if (nextCall === null || workInProgress.memoizedProps === nextCall) {
      nextCall = workInProgress.memoizedProps;
      // TODO: When bailing out, we might need to return the stateNode instead
      // of the child. To check it for work.
      // return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    var nextChildren = nextCall.children;

    // The following is a fork of reconcileChildrenAtExpirationTime but using
    // stateNode to store the child.
    if (current === null) {
      workInProgress.stateNode = mountChildFibers(workInProgress, workInProgress.stateNode, nextChildren, renderExpirationTime);
    } else {
      workInProgress.stateNode = reconcileChildFibers(workInProgress, workInProgress.stateNode, nextChildren, renderExpirationTime);
    }

    memoizeProps(workInProgress, nextCall);
    // This doesn't take arbitrary time so we could synchronously just begin
    // eagerly do the work of workInProgress.child as an optimization.
    return workInProgress.stateNode;
  }

  function updatePortalComponent(current, workInProgress, renderExpirationTime) {
    pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
    var nextChildren = workInProgress.pendingProps;
    if (hasContextChanged()) {
      // Normally we can bail out on props equality but if context has changed
      // we don't do the bailout and we have to reuse existing props instead.
      if (nextChildren === null) {
        nextChildren = current && current.memoizedProps;
        !(nextChildren != null) ? invariant(false, 'We should always have pending or current props. This error is likely caused by a bug in React. Please file an issue.') : void 0;
      }
    } else if (nextChildren === null || workInProgress.memoizedProps === nextChildren) {
      return bailoutOnAlreadyFinishedWork(current, workInProgress);
    }

    if (current === null) {
      // Portals are special because we don't append the children during mount
      // but at commit. Therefore we need to track insertions which the normal
      // flow doesn't do during mount. This doesn't happen at the root because
      // the root always starts with a "current" with a null child.
      // TODO: Consider unifying this with how the root works.
      workInProgress.child = reconcileChildFibers(workInProgress, null, nextChildren, renderExpirationTime);
      memoizeProps(workInProgress, nextChildren);
    } else {
      reconcileChildren(current, workInProgress, nextChildren);
      memoizeProps(workInProgress, nextChildren);
    }
    return workInProgress.child;
  }

  /*
  function reuseChildrenEffects(returnFiber : Fiber, firstChild : Fiber) {
    let child = firstChild;
    do {
      // Ensure that the first and last effect of the parent corresponds
      // to the children's first and last effect.
      if (!returnFiber.firstEffect) {
        returnFiber.firstEffect = child.firstEffect;
      }
      if (child.lastEffect) {
        if (returnFiber.lastEffect) {
          returnFiber.lastEffect.nextEffect = child.firstEffect;
        }
        returnFiber.lastEffect = child.lastEffect;
      }
    } while (child = child.sibling);
  }
  */

  function bailoutOnAlreadyFinishedWork(current, workInProgress) {
    cancelWorkTimer(workInProgress);

    // TODO: We should ideally be able to bail out early if the children have no
    // more work to do. However, since we don't have a separation of this
    // Fiber's priority and its children yet - we don't know without doing lots
    // of the same work we do anyway. Once we have that separation we can just
    // bail out here if the children has no more work at this priority level.
    // if (workInProgress.priorityOfChildren <= priorityLevel) {
    //   // If there are side-effects in these children that have not yet been
    //   // committed we need to ensure that they get properly transferred up.
    //   if (current && current.child !== workInProgress.child) {
    //     reuseChildrenEffects(workInProgress, child);
    //   }
    //   return null;
    // }

    cloneChildFibers(current, workInProgress);
    return workInProgress.child;
  }

  function bailoutOnLowPriority(current, workInProgress) {
    cancelWorkTimer(workInProgress);

    // TODO: Handle HostComponent tags here as well and call pushHostContext()?
    // See PR 8590 discussion for context
    switch (workInProgress.tag) {
      case HostRoot:
        pushHostRootContext(workInProgress);
        break;
      case ClassComponent:
        pushContextProvider(workInProgress);
        break;
      case HostPortal:
        pushHostContainer(workInProgress, workInProgress.stateNode.containerInfo);
        break;
    }
    // TODO: What if this is currently in progress?
    // How can that happen? How is this not being cloned?
    return null;
  }

  // TODO: Delete memoizeProps/State and move to reconcile/bailout instead
  function memoizeProps(workInProgress, nextProps) {
    workInProgress.memoizedProps = nextProps;
  }

  function memoizeState(workInProgress, nextState) {
    workInProgress.memoizedState = nextState;
    // Don't reset the updateQueue, in case there are pending updates. Resetting
    // is handled by processUpdateQueue.
  }

  function beginWork(current, workInProgress, renderExpirationTime) {
    if (workInProgress.expirationTime === NoWork || workInProgress.expirationTime > renderExpirationTime) {
      return bailoutOnLowPriority(current, workInProgress);
    }

    switch (workInProgress.tag) {
      case IndeterminateComponent:
        return mountIndeterminateComponent(current, workInProgress, renderExpirationTime);
      case FunctionalComponent:
        return updateFunctionalComponent(current, workInProgress);
      case ClassComponent:
        return updateClassComponent(current, workInProgress, renderExpirationTime);
      case HostRoot:
        return updateHostRoot(current, workInProgress, renderExpirationTime);
      case HostComponent:
        return updateHostComponent(current, workInProgress, renderExpirationTime);
      case HostText:
        return updateHostText(current, workInProgress);
      case CallHandlerPhase:
        // This is a restart. Reset the tag to the initial phase.
        workInProgress.tag = CallComponent;
      // Intentionally fall through since this is now the same.
      case CallComponent:
        return updateCallComponent(current, workInProgress, renderExpirationTime);
      case ReturnComponent:
        // A return component is just a placeholder, we can just run through the
        // next one immediately.
        return null;
      case HostPortal:
        return updatePortalComponent(current, workInProgress, renderExpirationTime);
      case Fragment:
        return updateFragment(current, workInProgress);
      default:
        invariant(false, 'Unknown unit of work tag. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  function beginFailedWork(current, workInProgress, renderExpirationTime) {
    // Push context providers here to avoid a push/pop context mismatch.
    switch (workInProgress.tag) {
      case ClassComponent:
        pushContextProvider(workInProgress);
        break;
      case HostRoot:
        pushHostRootContext(workInProgress);
        break;
      default:
        invariant(false, 'Invalid type of work. This error is likely caused by a bug in React. Please file an issue.');
    }

    // Add an error effect so we can handle the error during the commit phase
    workInProgress.effectTag |= Err;

    // This is a weird case where we do "resume" work — work that failed on
    // our first attempt. Because we no longer have a notion of "progressed
    // deletions," reset the child to the current child to make sure we delete
    // it again. TODO: Find a better way to handle this, perhaps during a more
    // general overhaul of error handling.
    if (current === null) {
      workInProgress.child = null;
    } else if (workInProgress.child !== current.child) {
      workInProgress.child = current.child;
    }

    if (workInProgress.expirationTime === NoWork || workInProgress.expirationTime > renderExpirationTime) {
      return bailoutOnLowPriority(current, workInProgress);
    }

    // If we don't bail out, we're going be recomputing our children so we need
    // to drop our effect list.
    workInProgress.firstEffect = null;
    workInProgress.lastEffect = null;

    // Unmount the current children as if the component rendered null
    var nextChildren = null;
    reconcileChildrenAtExpirationTime(current, workInProgress, nextChildren, renderExpirationTime);

    if (workInProgress.tag === ClassComponent) {
      var instance = workInProgress.stateNode;
      workInProgress.memoizedProps = instance.props;
      workInProgress.memoizedState = instance.state;
    }

    return workInProgress.child;
  }

  return {
    beginWork: beginWork,
    beginFailedWork: beginFailedWork
  };
};

var ReactFiberCompleteWork = function (config, hostContext, hydrationContext) {
  var createInstance = config.createInstance,
      createTextInstance = config.createTextInstance,
      appendInitialChild = config.appendInitialChild,
      finalizeInitialChildren = config.finalizeInitialChildren,
      prepareUpdate = config.prepareUpdate,
      mutation = config.mutation,
      persistence = config.persistence;
  var getRootHostContainer = hostContext.getRootHostContainer,
      popHostContext = hostContext.popHostContext,
      getHostContext = hostContext.getHostContext,
      popHostContainer = hostContext.popHostContainer;
  var prepareToHydrateHostInstance = hydrationContext.prepareToHydrateHostInstance,
      prepareToHydrateHostTextInstance = hydrationContext.prepareToHydrateHostTextInstance,
      popHydrationState = hydrationContext.popHydrationState;


  function markUpdate(workInProgress) {
    // Tag the fiber with an update effect. This turns a Placement into
    // an UpdateAndPlacement.
    workInProgress.effectTag |= Update;
  }

  function markRef(workInProgress) {
    workInProgress.effectTag |= Ref;
  }

  function appendAllReturns(returns, workInProgress) {
    var node = workInProgress.stateNode;
    if (node) {
      node['return'] = workInProgress;
    }
    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText || node.tag === HostPortal) {
        invariant(false, 'A call cannot have host component children.');
      } else if (node.tag === ReturnComponent) {
        returns.push(node.type);
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === workInProgress) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function moveCallToHandlerPhase(current, workInProgress, renderExpirationTime) {
    var call = workInProgress.memoizedProps;
    !call ? invariant(false, 'Should be resolved by now. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    // First step of the call has completed. Now we need to do the second.
    // TODO: It would be nice to have a multi stage call represented by a
    // single component, or at least tail call optimize nested ones. Currently
    // that requires additional fields that we don't want to add to the fiber.
    // So this requires nested handlers.
    // Note: This doesn't mutate the alternate node. I don't think it needs to
    // since this stage is reset for every pass.
    workInProgress.tag = CallHandlerPhase;

    // Build up the returns.
    // TODO: Compare this to a generator or opaque helpers like Children.
    var returns = [];
    appendAllReturns(returns, workInProgress);
    var fn = call.handler;
    var props = call.props;
    var nextChildren = fn(props, returns);

    var currentFirstChild = current !== null ? current.child : null;
    workInProgress.child = reconcileChildFibers(workInProgress, currentFirstChild, nextChildren, renderExpirationTime);
    return workInProgress.child;
  }

  function appendAllChildren(parent, workInProgress) {
    // We only have the top Fiber that was created but we need recurse down its
    // children to find all the terminal nodes.
    var node = workInProgress.child;
    while (node !== null) {
      if (node.tag === HostComponent || node.tag === HostText) {
        appendInitialChild(parent, node.stateNode);
      } else if (node.tag === HostPortal) {
        // If we have a portal child, then we don't want to traverse
        // down its children. Instead, we'll get insertions from each child in
        // the portal directly.
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === workInProgress) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === workInProgress) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  var updateHostContainer = void 0;
  var updateHostComponent = void 0;
  var updateHostText = void 0;
  if (mutation) {
    if (enableMutatingReconciler) {
      // Mutation mode
      updateHostContainer = function (workInProgress) {
        // Noop
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance) {
        // TODO: Type this specific to this type of component.
        workInProgress.updateQueue = updatePayload;
        // If the update payload indicates that there is a change or if there
        // is a new ref we mark this as an update. All the work is done in commitWork.
        if (updatePayload) {
          markUpdate(workInProgress);
        }
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        // If the text differs, mark it as an update. All the work in done in commitWork.
        if (oldText !== newText) {
          markUpdate(workInProgress);
        }
      };
    } else {
      invariant(false, 'Mutating reconciler is disabled.');
    }
  } else if (persistence) {
    if (enablePersistentReconciler) {
      // Persistent host tree mode
      var cloneInstance = persistence.cloneInstance,
          createContainerChildSet = persistence.createContainerChildSet,
          appendChildToContainerChildSet = persistence.appendChildToContainerChildSet,
          finalizeContainerChildren = persistence.finalizeContainerChildren;

      // An unfortunate fork of appendAllChildren because we have two different parent types.

      var appendAllChildrenToContainer = function (containerChildSet, workInProgress) {
        // We only have the top Fiber that was created but we need recurse down its
        // children to find all the terminal nodes.
        var node = workInProgress.child;
        while (node !== null) {
          if (node.tag === HostComponent || node.tag === HostText) {
            appendChildToContainerChildSet(containerChildSet, node.stateNode);
          } else if (node.tag === HostPortal) {
            // If we have a portal child, then we don't want to traverse
            // down its children. Instead, we'll get insertions from each child in
            // the portal directly.
          } else if (node.child !== null) {
            node.child['return'] = node;
            node = node.child;
            continue;
          }
          if (node === workInProgress) {
            return;
          }
          while (node.sibling === null) {
            if (node['return'] === null || node['return'] === workInProgress) {
              return;
            }
            node = node['return'];
          }
          node.sibling['return'] = node['return'];
          node = node.sibling;
        }
      };
      updateHostContainer = function (workInProgress) {
        var portalOrRoot = workInProgress.stateNode;
        var childrenUnchanged = workInProgress.firstEffect === null;
        if (childrenUnchanged) {
          // No changes, just reuse the existing instance.
        } else {
          var container = portalOrRoot.containerInfo;
          var newChildSet = createContainerChildSet(container);
          if (finalizeContainerChildren(container, newChildSet)) {
            markUpdate(workInProgress);
          }
          portalOrRoot.pendingChildren = newChildSet;
          // If children might have changed, we have to add them all to the set.
          appendAllChildrenToContainer(newChildSet, workInProgress);
          // Schedule an update on the container to swap out the container.
          markUpdate(workInProgress);
        }
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance) {
        // If there are no effects associated with this node, then none of our children had any updates.
        // This guarantees that we can reuse all of them.
        var childrenUnchanged = workInProgress.firstEffect === null;
        var currentInstance = current.stateNode;
        if (childrenUnchanged && updatePayload === null) {
          // No changes, just reuse the existing instance.
          // Note that this might release a previous clone.
          workInProgress.stateNode = currentInstance;
        } else {
          var recyclableInstance = workInProgress.stateNode;
          var newInstance = cloneInstance(currentInstance, updatePayload, type, oldProps, newProps, workInProgress, childrenUnchanged, recyclableInstance);
          if (finalizeInitialChildren(newInstance, type, newProps, rootContainerInstance)) {
            markUpdate(workInProgress);
          }
          workInProgress.stateNode = newInstance;
          if (childrenUnchanged) {
            // If there are no other effects in this tree, we need to flag this node as having one.
            // Even though we're not going to use it for anything.
            // Otherwise parents won't know that there are new children to propagate upwards.
            markUpdate(workInProgress);
          } else {
            // If children might have changed, we have to add them all to the set.
            appendAllChildren(newInstance, workInProgress);
          }
        }
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        if (oldText !== newText) {
          // If the text content differs, we'll create a new text instance for it.
          var rootContainerInstance = getRootHostContainer();
          var currentHostContext = getHostContext();
          workInProgress.stateNode = createTextInstance(newText, rootContainerInstance, currentHostContext, workInProgress);
          // We'll have to mark it as having an effect, even though we won't use the effect for anything.
          // This lets the parents know that at least one of their children has changed.
          markUpdate(workInProgress);
        }
      };
    } else {
      invariant(false, 'Persistent reconciler is disabled.');
    }
  } else {
    if (enableNoopReconciler) {
      // No host operations
      updateHostContainer = function (workInProgress) {
        // Noop
      };
      updateHostComponent = function (current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance) {
        // Noop
      };
      updateHostText = function (current, workInProgress, oldText, newText) {
        // Noop
      };
    } else {
      invariant(false, 'Noop reconciler is disabled.');
    }
  }

  function completeWork(current, workInProgress, renderExpirationTime) {
    // Get the latest props.
    var newProps = workInProgress.pendingProps;
    if (newProps === null) {
      newProps = workInProgress.memoizedProps;
    } else if (workInProgress.expirationTime !== Never || renderExpirationTime === Never) {
      // Reset the pending props, unless this was a down-prioritization.
      workInProgress.pendingProps = null;
    }

    switch (workInProgress.tag) {
      case FunctionalComponent:
        return null;
      case ClassComponent:
        {
          // We are leaving this subtree, so pop context if any.
          popContextProvider(workInProgress);
          return null;
        }
      case HostRoot:
        {
          popHostContainer(workInProgress);
          popTopLevelContextObject(workInProgress);
          var fiberRoot = workInProgress.stateNode;
          if (fiberRoot.pendingContext) {
            fiberRoot.context = fiberRoot.pendingContext;
            fiberRoot.pendingContext = null;
          }

          if (current === null || current.child === null) {
            // If we hydrated, pop so that we can delete any remaining children
            // that weren't hydrated.
            popHydrationState(workInProgress);
            // This resets the hacky state to fix isMounted before committing.
            // TODO: Delete this when we delete isMounted and findDOMNode.
            workInProgress.effectTag &= ~Placement;
          }
          updateHostContainer(workInProgress);
          return null;
        }
      case HostComponent:
        {
          popHostContext(workInProgress);
          var rootContainerInstance = getRootHostContainer();
          var type = workInProgress.type;
          if (current !== null && workInProgress.stateNode != null) {
            // If we have an alternate, that means this is an update and we need to
            // schedule a side-effect to do the updates.
            var oldProps = current.memoizedProps;
            // If we get updated because one of our children updated, we don't
            // have newProps so we'll have to reuse them.
            // TODO: Split the update API as separate for the props vs. children.
            // Even better would be if children weren't special cased at all tho.
            var instance = workInProgress.stateNode;
            var currentHostContext = getHostContext();
            var updatePayload = prepareUpdate(instance, type, oldProps, newProps, rootContainerInstance, currentHostContext);

            updateHostComponent(current, workInProgress, updatePayload, type, oldProps, newProps, rootContainerInstance);

            if (current.ref !== workInProgress.ref) {
              markRef(workInProgress);
            }
          } else {
            if (!newProps) {
              !(workInProgress.stateNode !== null) ? invariant(false, 'We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.') : void 0;
              // This can happen when we abort work.
              return null;
            }

            var _currentHostContext = getHostContext();
            // TODO: Move createInstance to beginWork and keep it on a context
            // "stack" as the parent. Then append children as we go in beginWork
            // or completeWork depending on we want to add then top->down or
            // bottom->up. Top->down is faster in IE11.
            var wasHydrated = popHydrationState(workInProgress);
            if (wasHydrated) {
              // TODO: Move this and createInstance step into the beginPhase
              // to consolidate.
              if (prepareToHydrateHostInstance(workInProgress, rootContainerInstance, _currentHostContext)) {
                // If changes to the hydrated node needs to be applied at the
                // commit-phase we mark this as such.
                markUpdate(workInProgress);
              }
            } else {
              var _instance = createInstance(type, newProps, rootContainerInstance, _currentHostContext, workInProgress);

              appendAllChildren(_instance, workInProgress);

              // Certain renderers require commit-time effects for initial mount.
              // (eg DOM renderer supports auto-focus for certain elements).
              // Make sure such renderers get scheduled for later work.
              if (finalizeInitialChildren(_instance, type, newProps, rootContainerInstance)) {
                markUpdate(workInProgress);
              }
              workInProgress.stateNode = _instance;
            }

            if (workInProgress.ref !== null) {
              // If there is a ref on a host node we need to schedule a callback
              markRef(workInProgress);
            }
          }
          return null;
        }
      case HostText:
        {
          var newText = newProps;
          if (current && workInProgress.stateNode != null) {
            var oldText = current.memoizedProps;
            // If we have an alternate, that means this is an update and we need
            // to schedule a side-effect to do the updates.
            updateHostText(current, workInProgress, oldText, newText);
          } else {
            if (typeof newText !== 'string') {
              !(workInProgress.stateNode !== null) ? invariant(false, 'We must have new props for new mounts. This error is likely caused by a bug in React. Please file an issue.') : void 0;
              // This can happen when we abort work.
              return null;
            }
            var _rootContainerInstance = getRootHostContainer();
            var _currentHostContext2 = getHostContext();
            var _wasHydrated = popHydrationState(workInProgress);
            if (_wasHydrated) {
              if (prepareToHydrateHostTextInstance(workInProgress)) {
                markUpdate(workInProgress);
              }
            } else {
              workInProgress.stateNode = createTextInstance(newText, _rootContainerInstance, _currentHostContext2, workInProgress);
            }
          }
          return null;
        }
      case CallComponent:
        return moveCallToHandlerPhase(current, workInProgress, renderExpirationTime);
      case CallHandlerPhase:
        // Reset the tag to now be a first phase call.
        workInProgress.tag = CallComponent;
        return null;
      case ReturnComponent:
        // Does nothing.
        return null;
      case Fragment:
        return null;
      case HostPortal:
        popHostContainer(workInProgress);
        updateHostContainer(workInProgress);
        return null;
      // Error cases
      case IndeterminateComponent:
        invariant(false, 'An indeterminate component should have become determinate before completing. This error is likely caused by a bug in React. Please file an issue.');
      // eslint-disable-next-line no-fallthrough
      default:
        invariant(false, 'Unknown unit of work tag. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  return {
    completeWork: completeWork
  };
};

var invokeGuardedCallback$2 = ReactErrorUtils.invokeGuardedCallback;
var hasCaughtError$1 = ReactErrorUtils.hasCaughtError;
var clearCaughtError$1 = ReactErrorUtils.clearCaughtError;


var ReactFiberCommitWork = function (config, captureError) {
  var getPublicInstance = config.getPublicInstance,
      mutation = config.mutation,
      persistence = config.persistence;


  var callComponentWillUnmountWithTimer = function (current, instance) {
    startPhaseTimer(current, 'componentWillUnmount');
    instance.props = current.memoizedProps;
    instance.state = current.memoizedState;
    instance.componentWillUnmount();
    stopPhaseTimer();
  };

  // Capture errors so they don't interrupt unmounting.
  function safelyCallComponentWillUnmount(current, instance) {
    {
      invokeGuardedCallback$2(null, callComponentWillUnmountWithTimer, null, current, instance);
      if (hasCaughtError$1()) {
        var unmountError = clearCaughtError$1();
        captureError(current, unmountError);
      }
    }
  }

  function safelyDetachRef(current) {
    var ref = current.ref;
    if (ref !== null) {
      {
        invokeGuardedCallback$2(null, ref, null, null);
        if (hasCaughtError$1()) {
          var refError = clearCaughtError$1();
          captureError(current, refError);
        }
      }
    }
  }

  function commitLifeCycles(current, finishedWork) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          var instance = finishedWork.stateNode;
          if (finishedWork.effectTag & Update) {
            if (current === null) {
              startPhaseTimer(finishedWork, 'componentDidMount');
              instance.props = finishedWork.memoizedProps;
              instance.state = finishedWork.memoizedState;
              instance.componentDidMount();
              stopPhaseTimer();
            } else {
              var prevProps = current.memoizedProps;
              var prevState = current.memoizedState;
              startPhaseTimer(finishedWork, 'componentDidUpdate');
              instance.props = finishedWork.memoizedProps;
              instance.state = finishedWork.memoizedState;
              instance.componentDidUpdate(prevProps, prevState);
              stopPhaseTimer();
            }
          }
          var updateQueue = finishedWork.updateQueue;
          if (updateQueue !== null) {
            commitCallbacks(updateQueue, instance);
          }
          return;
        }
      case HostRoot:
        {
          var _updateQueue = finishedWork.updateQueue;
          if (_updateQueue !== null) {
            var _instance = finishedWork.child !== null ? finishedWork.child.stateNode : null;
            commitCallbacks(_updateQueue, _instance);
          }
          return;
        }
      case HostComponent:
        {
          var _instance2 = finishedWork.stateNode;

          // Renderers may schedule work to be done after host components are mounted
          // (eg DOM renderer may schedule auto-focus for inputs and form controls).
          // These effects should only be committed when components are first mounted,
          // aka when there is no current/alternate.
          if (current === null && finishedWork.effectTag & Update) {
            var type = finishedWork.type;
            var props = finishedWork.memoizedProps;
            commitMount(_instance2, type, props, finishedWork);
          }

          return;
        }
      case HostText:
        {
          // We have no life-cycles associated with text.
          return;
        }
      case HostPortal:
        {
          // We have no life-cycles associated with portals.
          return;
        }
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitAttachRef(finishedWork) {
    var ref = finishedWork.ref;
    if (ref !== null) {
      var instance = finishedWork.stateNode;
      switch (finishedWork.tag) {
        case HostComponent:
          ref(getPublicInstance(instance));
          break;
        default:
          ref(instance);
      }
    }
  }

  function commitDetachRef(current) {
    var currentRef = current.ref;
    if (currentRef !== null) {
      currentRef(null);
    }
  }

  // User-originating errors (lifecycles and refs) should not interrupt
  // deletion, so don't let them throw. Host-originating errors should
  // interrupt deletion, so it's okay
  function commitUnmount(current) {
    if (typeof onCommitUnmount === 'function') {
      onCommitUnmount(current);
    }

    switch (current.tag) {
      case ClassComponent:
        {
          safelyDetachRef(current);
          var instance = current.stateNode;
          if (typeof instance.componentWillUnmount === 'function') {
            safelyCallComponentWillUnmount(current, instance);
          }
          return;
        }
      case HostComponent:
        {
          safelyDetachRef(current);
          return;
        }
      case CallComponent:
        {
          commitNestedUnmounts(current.stateNode);
          return;
        }
      case HostPortal:
        {
          // TODO: this is recursive.
          // We are also not using this parent because
          // the portal will get pushed immediately.
          if (enableMutatingReconciler && mutation) {
            unmountHostComponents(current);
          } else if (enablePersistentReconciler && persistence) {
            emptyPortalContainer(current);
          }
          return;
        }
    }
  }

  function commitNestedUnmounts(root) {
    // While we're inside a removed host node we don't want to call
    // removeChild on the inner nodes because they're removed by the top
    // call anyway. We also want to call componentWillUnmount on all
    // composites before this host node is removed from the tree. Therefore
    var node = root;
    while (true) {
      commitUnmount(node);
      // Visit children because they may contain more composite or host nodes.
      // Skip portals because commitUnmount() currently visits them recursively.
      if (node.child !== null && (
      // If we use mutation we drill down into portals using commitUnmount above.
      // If we don't use mutation we drill down into portals here instead.
      !mutation || node.tag !== HostPortal)) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === root) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === root) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function detachFiber(current) {
    // Cut off the return pointers to disconnect it from the tree. Ideally, we
    // should clear the child pointer of the parent alternate to let this
    // get GC:ed but we don't know which for sure which parent is the current
    // one so we'll settle for GC:ing the subtree of this child. This child
    // itself will be GC:ed when the parent updates the next time.
    current['return'] = null;
    current.child = null;
    if (current.alternate) {
      current.alternate.child = null;
      current.alternate['return'] = null;
    }
  }

  if (!mutation) {
    var commitContainer = void 0;
    if (persistence) {
      var replaceContainerChildren = persistence.replaceContainerChildren,
          createContainerChildSet = persistence.createContainerChildSet;

      var emptyPortalContainer = function (current) {
        var portal = current.stateNode;
        var containerInfo = portal.containerInfo;

        var emptyChildSet = createContainerChildSet(containerInfo);
        replaceContainerChildren(containerInfo, emptyChildSet);
      };
      commitContainer = function (finishedWork) {
        switch (finishedWork.tag) {
          case ClassComponent:
            {
              return;
            }
          case HostComponent:
            {
              return;
            }
          case HostText:
            {
              return;
            }
          case HostRoot:
          case HostPortal:
            {
              var portalOrRoot = finishedWork.stateNode;
              var containerInfo = portalOrRoot.containerInfo,
                  _pendingChildren = portalOrRoot.pendingChildren;

              replaceContainerChildren(containerInfo, _pendingChildren);
              return;
            }
          default:
            {
              invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
            }
        }
      };
    } else {
      commitContainer = function (finishedWork) {
        // Noop
      };
    }
    if (enablePersistentReconciler || enableNoopReconciler) {
      return {
        commitResetTextContent: function (finishedWork) {},
        commitPlacement: function (finishedWork) {},
        commitDeletion: function (current) {
          // Detach refs and call componentWillUnmount() on the whole subtree.
          commitNestedUnmounts(current);
          detachFiber(current);
        },
        commitWork: function (current, finishedWork) {
          commitContainer(finishedWork);
        },

        commitLifeCycles: commitLifeCycles,
        commitAttachRef: commitAttachRef,
        commitDetachRef: commitDetachRef
      };
    } else if (persistence) {
      invariant(false, 'Persistent reconciler is disabled.');
    } else {
      invariant(false, 'Noop reconciler is disabled.');
    }
  }
  var commitMount = mutation.commitMount,
      commitUpdate = mutation.commitUpdate,
      resetTextContent = mutation.resetTextContent,
      commitTextUpdate = mutation.commitTextUpdate,
      appendChild = mutation.appendChild,
      appendChildToContainer = mutation.appendChildToContainer,
      insertBefore = mutation.insertBefore,
      insertInContainerBefore = mutation.insertInContainerBefore,
      removeChild = mutation.removeChild,
      removeChildFromContainer = mutation.removeChildFromContainer;


  function getHostParentFiber(fiber) {
    var parent = fiber['return'];
    while (parent !== null) {
      if (isHostParent(parent)) {
        return parent;
      }
      parent = parent['return'];
    }
    invariant(false, 'Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.');
  }

  function isHostParent(fiber) {
    return fiber.tag === HostComponent || fiber.tag === HostRoot || fiber.tag === HostPortal;
  }

  function getHostSibling(fiber) {
    // We're going to search forward into the tree until we find a sibling host
    // node. Unfortunately, if multiple insertions are done in a row we have to
    // search past them. This leads to exponential search for the next sibling.
    var node = fiber;
    siblings: while (true) {
      // If we didn't find anything, let's try the next sibling.
      while (node.sibling === null) {
        if (node['return'] === null || isHostParent(node['return'])) {
          // If we pop out of the root or hit the parent the fiber we are the
          // last sibling.
          return null;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
      while (node.tag !== HostComponent && node.tag !== HostText) {
        // If it is not host node and, we might have a host node inside it.
        // Try to search down until we find one.
        if (node.effectTag & Placement) {
          // If we don't have a child, try the siblings instead.
          continue siblings;
        }
        // If we don't have a child, try the siblings instead.
        // We also skip portals because they are not part of this host tree.
        if (node.child === null || node.tag === HostPortal) {
          continue siblings;
        } else {
          node.child['return'] = node;
          node = node.child;
        }
      }
      // Check if this host node is stable or about to be placed.
      if (!(node.effectTag & Placement)) {
        // Found it!
        return node.stateNode;
      }
    }
  }

  function commitPlacement(finishedWork) {
    // Recursively insert all host nodes into the parent.
    var parentFiber = getHostParentFiber(finishedWork);
    var parent = void 0;
    var isContainer = void 0;
    switch (parentFiber.tag) {
      case HostComponent:
        parent = parentFiber.stateNode;
        isContainer = false;
        break;
      case HostRoot:
        parent = parentFiber.stateNode.containerInfo;
        isContainer = true;
        break;
      case HostPortal:
        parent = parentFiber.stateNode.containerInfo;
        isContainer = true;
        break;
      default:
        invariant(false, 'Invalid host parent fiber. This error is likely caused by a bug in React. Please file an issue.');
    }
    if (parentFiber.effectTag & ContentReset) {
      // Reset the text content of the parent before doing any insertions
      resetTextContent(parent);
      // Clear ContentReset from the effect tag
      parentFiber.effectTag &= ~ContentReset;
    }

    var before = getHostSibling(finishedWork);
    // We only have the top Fiber that was inserted but we need recurse down its
    // children to find all the terminal nodes.
    var node = finishedWork;
    while (true) {
      if (node.tag === HostComponent || node.tag === HostText) {
        if (before) {
          if (isContainer) {
            insertInContainerBefore(parent, node.stateNode, before);
          } else {
            insertBefore(parent, node.stateNode, before);
          }
        } else {
          if (isContainer) {
            appendChildToContainer(parent, node.stateNode);
          } else {
            appendChild(parent, node.stateNode);
          }
        }
      } else if (node.tag === HostPortal) {
        // If the insertion itself is a portal, then we don't want to traverse
        // down its children. Instead, we'll get insertions from each child in
        // the portal directly.
      } else if (node.child !== null) {
        node.child['return'] = node;
        node = node.child;
        continue;
      }
      if (node === finishedWork) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === finishedWork) {
          return;
        }
        node = node['return'];
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function unmountHostComponents(current) {
    // We only have the top Fiber that was inserted but we need recurse down its
    var node = current;

    // Each iteration, currentParent is populated with node's host parent if not
    // currentParentIsValid.
    var currentParentIsValid = false;
    var currentParent = void 0;
    var currentParentIsContainer = void 0;

    while (true) {
      if (!currentParentIsValid) {
        var parent = node['return'];
        findParent: while (true) {
          !(parent !== null) ? invariant(false, 'Expected to find a host parent. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          switch (parent.tag) {
            case HostComponent:
              currentParent = parent.stateNode;
              currentParentIsContainer = false;
              break findParent;
            case HostRoot:
              currentParent = parent.stateNode.containerInfo;
              currentParentIsContainer = true;
              break findParent;
            case HostPortal:
              currentParent = parent.stateNode.containerInfo;
              currentParentIsContainer = true;
              break findParent;
          }
          parent = parent['return'];
        }
        currentParentIsValid = true;
      }

      if (node.tag === HostComponent || node.tag === HostText) {
        commitNestedUnmounts(node);
        // After all the children have unmounted, it is now safe to remove the
        // node from the tree.
        if (currentParentIsContainer) {
          removeChildFromContainer(currentParent, node.stateNode);
        } else {
          removeChild(currentParent, node.stateNode);
        }
        // Don't visit children because we already visited them.
      } else if (node.tag === HostPortal) {
        // When we go into a portal, it becomes the parent to remove from.
        // We will reassign it back when we pop the portal on the way up.
        currentParent = node.stateNode.containerInfo;
        // Visit children because portals might contain host components.
        if (node.child !== null) {
          node.child['return'] = node;
          node = node.child;
          continue;
        }
      } else {
        commitUnmount(node);
        // Visit children because we may find more host components below.
        if (node.child !== null) {
          node.child['return'] = node;
          node = node.child;
          continue;
        }
      }
      if (node === current) {
        return;
      }
      while (node.sibling === null) {
        if (node['return'] === null || node['return'] === current) {
          return;
        }
        node = node['return'];
        if (node.tag === HostPortal) {
          // When we go out of the portal, we need to restore the parent.
          // Since we don't keep a stack of them, we will search for it.
          currentParentIsValid = false;
        }
      }
      node.sibling['return'] = node['return'];
      node = node.sibling;
    }
  }

  function commitDeletion(current) {
    // Recursively delete all host nodes from the parent.
    // Detach refs and call componentWillUnmount() on the whole subtree.
    unmountHostComponents(current);
    detachFiber(current);
  }

  function commitWork(current, finishedWork) {
    switch (finishedWork.tag) {
      case ClassComponent:
        {
          return;
        }
      case HostComponent:
        {
          var instance = finishedWork.stateNode;
          if (instance != null) {
            // Commit the work prepared earlier.
            var newProps = finishedWork.memoizedProps;
            // For hydration we reuse the update path but we treat the oldProps
            // as the newProps. The updatePayload will contain the real change in
            // this case.
            var oldProps = current !== null ? current.memoizedProps : newProps;
            var type = finishedWork.type;
            // TODO: Type the updateQueue to be specific to host components.
            var updatePayload = finishedWork.updateQueue;
            finishedWork.updateQueue = null;
            if (updatePayload !== null) {
              commitUpdate(instance, updatePayload, type, oldProps, newProps, finishedWork);
            }
          }
          return;
        }
      case HostText:
        {
          !(finishedWork.stateNode !== null) ? invariant(false, 'This should have a text node initialized. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          var textInstance = finishedWork.stateNode;
          var newText = finishedWork.memoizedProps;
          // For hydration we reuse the update path but we treat the oldProps
          // as the newProps. The updatePayload will contain the real change in
          // this case.
          var oldText = current !== null ? current.memoizedProps : newText;
          commitTextUpdate(textInstance, oldText, newText);
          return;
        }
      case HostRoot:
        {
          return;
        }
      default:
        {
          invariant(false, 'This unit of work tag should not have side-effects. This error is likely caused by a bug in React. Please file an issue.');
        }
    }
  }

  function commitResetTextContent(current) {
    resetTextContent(current.stateNode);
  }

  if (enableMutatingReconciler) {
    return {
      commitResetTextContent: commitResetTextContent,
      commitPlacement: commitPlacement,
      commitDeletion: commitDeletion,
      commitWork: commitWork,
      commitLifeCycles: commitLifeCycles,
      commitAttachRef: commitAttachRef,
      commitDetachRef: commitDetachRef
    };
  } else {
    invariant(false, 'Mutating reconciler is disabled.');
  }
};

var NO_CONTEXT = {};

var ReactFiberHostContext = function (config) {
  var getChildHostContext = config.getChildHostContext,
      getRootHostContext = config.getRootHostContext;


  var contextStackCursor = createCursor(NO_CONTEXT);
  var contextFiberStackCursor = createCursor(NO_CONTEXT);
  var rootInstanceStackCursor = createCursor(NO_CONTEXT);

  function requiredContext(c) {
    !(c !== NO_CONTEXT) ? invariant(false, 'Expected host context to exist. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    return c;
  }

  function getRootHostContainer() {
    var rootInstance = requiredContext(rootInstanceStackCursor.current);
    return rootInstance;
  }

  function pushHostContainer(fiber, nextRootInstance) {
    // Push current root instance onto the stack;
    // This allows us to reset root when portals are popped.
    push(rootInstanceStackCursor, nextRootInstance, fiber);

    var nextRootContext = getRootHostContext(nextRootInstance);

    // Track the context and the Fiber that provided it.
    // This enables us to pop only Fibers that provide unique contexts.
    push(contextFiberStackCursor, fiber, fiber);
    push(contextStackCursor, nextRootContext, fiber);
  }

  function popHostContainer(fiber) {
    pop(contextStackCursor, fiber);
    pop(contextFiberStackCursor, fiber);
    pop(rootInstanceStackCursor, fiber);
  }

  function getHostContext() {
    var context = requiredContext(contextStackCursor.current);
    return context;
  }

  function pushHostContext(fiber) {
    var rootInstance = requiredContext(rootInstanceStackCursor.current);
    var context = requiredContext(contextStackCursor.current);
    var nextContext = getChildHostContext(context, fiber.type, rootInstance);

    // Don't push this Fiber's context unless it's unique.
    if (context === nextContext) {
      return;
    }

    // Track the context and the Fiber that provided it.
    // This enables us to pop only Fibers that provide unique contexts.
    push(contextFiberStackCursor, fiber, fiber);
    push(contextStackCursor, nextContext, fiber);
  }

  function popHostContext(fiber) {
    // Do not pop unless this Fiber provided the current context.
    // pushHostContext() only pushes Fibers that provide unique contexts.
    if (contextFiberStackCursor.current !== fiber) {
      return;
    }

    pop(contextStackCursor, fiber);
    pop(contextFiberStackCursor, fiber);
  }

  function resetHostContainer() {
    contextStackCursor.current = NO_CONTEXT;
    rootInstanceStackCursor.current = NO_CONTEXT;
  }

  return {
    getHostContext: getHostContext,
    getRootHostContainer: getRootHostContainer,
    popHostContainer: popHostContainer,
    popHostContext: popHostContext,
    pushHostContainer: pushHostContainer,
    pushHostContext: pushHostContext,
    resetHostContainer: resetHostContainer
  };
};

var ReactFiberHydrationContext = function (config) {
  var shouldSetTextContent = config.shouldSetTextContent,
      hydration = config.hydration;

  // If this doesn't have hydration mode.

  if (!hydration) {
    return {
      enterHydrationState: function () {
        return false;
      },
      resetHydrationState: function () {},
      tryToClaimNextHydratableInstance: function () {},
      prepareToHydrateHostInstance: function () {
        invariant(false, 'Expected prepareToHydrateHostInstance() to never be called. This error is likely caused by a bug in React. Please file an issue.');
      },
      prepareToHydrateHostTextInstance: function () {
        invariant(false, 'Expected prepareToHydrateHostTextInstance() to never be called. This error is likely caused by a bug in React. Please file an issue.');
      },
      popHydrationState: function (fiber) {
        return false;
      }
    };
  }

  var canHydrateInstance = hydration.canHydrateInstance,
      canHydrateTextInstance = hydration.canHydrateTextInstance,
      getNextHydratableSibling = hydration.getNextHydratableSibling,
      getFirstHydratableChild = hydration.getFirstHydratableChild,
      hydrateInstance = hydration.hydrateInstance,
      hydrateTextInstance = hydration.hydrateTextInstance,
      didNotMatchHydratedContainerTextInstance = hydration.didNotMatchHydratedContainerTextInstance,
      didNotMatchHydratedTextInstance = hydration.didNotMatchHydratedTextInstance,
      didNotHydrateContainerInstance = hydration.didNotHydrateContainerInstance,
      didNotHydrateInstance = hydration.didNotHydrateInstance,
      didNotFindHydratableContainerInstance = hydration.didNotFindHydratableContainerInstance,
      didNotFindHydratableContainerTextInstance = hydration.didNotFindHydratableContainerTextInstance,
      didNotFindHydratableInstance = hydration.didNotFindHydratableInstance,
      didNotFindHydratableTextInstance = hydration.didNotFindHydratableTextInstance;

  // The deepest Fiber on the stack involved in a hydration context.
  // This may have been an insertion or a hydration.

  var hydrationParentFiber = null;
  var nextHydratableInstance = null;
  var isHydrating = false;

  function enterHydrationState(fiber) {
    var parentInstance = fiber.stateNode.containerInfo;
    nextHydratableInstance = getFirstHydratableChild(parentInstance);
    hydrationParentFiber = fiber;
    isHydrating = true;
    return true;
  }

  function deleteHydratableInstance(returnFiber, instance) {
    {
      switch (returnFiber.tag) {
        case HostRoot:
          didNotHydrateContainerInstance(returnFiber.stateNode.containerInfo, instance);
          break;
        case HostComponent:
          didNotHydrateInstance(returnFiber.type, returnFiber.memoizedProps, returnFiber.stateNode, instance);
          break;
      }
    }

    var childToDelete = createFiberFromHostInstanceForDeletion();
    childToDelete.stateNode = instance;
    childToDelete['return'] = returnFiber;
    childToDelete.effectTag = Deletion;

    // This might seem like it belongs on progressedFirstDeletion. However,
    // these children are not part of the reconciliation list of children.
    // Even if we abort and rereconcile the children, that will try to hydrate
    // again and the nodes are still in the host tree so these will be
    // recreated.
    if (returnFiber.lastEffect !== null) {
      returnFiber.lastEffect.nextEffect = childToDelete;
      returnFiber.lastEffect = childToDelete;
    } else {
      returnFiber.firstEffect = returnFiber.lastEffect = childToDelete;
    }
  }

  function insertNonHydratedInstance(returnFiber, fiber) {
    fiber.effectTag |= Placement;
    {
      switch (returnFiber.tag) {
        case HostRoot:
          {
            var parentContainer = returnFiber.stateNode.containerInfo;
            switch (fiber.tag) {
              case HostComponent:
                var type = fiber.type;
                var props = fiber.pendingProps;
                didNotFindHydratableContainerInstance(parentContainer, type, props);
                break;
              case HostText:
                var text = fiber.pendingProps;
                didNotFindHydratableContainerTextInstance(parentContainer, text);
                break;
            }
            break;
          }
        case HostComponent:
          {
            var parentType = returnFiber.type;
            var parentProps = returnFiber.memoizedProps;
            var parentInstance = returnFiber.stateNode;
            switch (fiber.tag) {
              case HostComponent:
                var _type = fiber.type;
                var _props = fiber.pendingProps;
                didNotFindHydratableInstance(parentType, parentProps, parentInstance, _type, _props);
                break;
              case HostText:
                var _text = fiber.pendingProps;
                didNotFindHydratableTextInstance(parentType, parentProps, parentInstance, _text);
                break;
            }
            break;
          }
        default:
          return;
      }
    }
  }

  function tryHydrate(fiber, nextInstance) {
    switch (fiber.tag) {
      case HostComponent:
        {
          var type = fiber.type;
          var props = fiber.pendingProps;
          var instance = canHydrateInstance(nextInstance, type, props);
          if (instance !== null) {
            fiber.stateNode = instance;
            return true;
          }
          return false;
        }
      case HostText:
        {
          var text = fiber.pendingProps;
          var textInstance = canHydrateTextInstance(nextInstance, text);
          if (textInstance !== null) {
            fiber.stateNode = textInstance;
            return true;
          }
          return false;
        }
      default:
        return false;
    }
  }

  function tryToClaimNextHydratableInstance(fiber) {
    if (!isHydrating) {
      return;
    }
    var nextInstance = nextHydratableInstance;
    if (!nextInstance) {
      // Nothing to hydrate. Make it an insertion.
      insertNonHydratedInstance(hydrationParentFiber, fiber);
      isHydrating = false;
      hydrationParentFiber = fiber;
      return;
    }
    if (!tryHydrate(fiber, nextInstance)) {
      // If we can't hydrate this instance let's try the next one.
      // We use this as a heuristic. It's based on intuition and not data so it
      // might be flawed or unnecessary.
      nextInstance = getNextHydratableSibling(nextInstance);
      if (!nextInstance || !tryHydrate(fiber, nextInstance)) {
        // Nothing to hydrate. Make it an insertion.
        insertNonHydratedInstance(hydrationParentFiber, fiber);
        isHydrating = false;
        hydrationParentFiber = fiber;
        return;
      }
      // We matched the next one, we'll now assume that the first one was
      // superfluous and we'll delete it. Since we can't eagerly delete it
      // we'll have to schedule a deletion. To do that, this node needs a dummy
      // fiber associated with it.
      deleteHydratableInstance(hydrationParentFiber, nextHydratableInstance);
    }
    hydrationParentFiber = fiber;
    nextHydratableInstance = getFirstHydratableChild(nextInstance);
  }

  function prepareToHydrateHostInstance(fiber, rootContainerInstance, hostContext) {
    var instance = fiber.stateNode;
    var updatePayload = hydrateInstance(instance, fiber.type, fiber.memoizedProps, rootContainerInstance, hostContext, fiber);
    // TODO: Type this specific to this type of component.
    fiber.updateQueue = updatePayload;
    // If the update payload indicates that there is a change or if there
    // is a new ref we mark this as an update.
    if (updatePayload !== null) {
      return true;
    }
    return false;
  }

  function prepareToHydrateHostTextInstance(fiber) {
    var textInstance = fiber.stateNode;
    var textContent = fiber.memoizedProps;
    var shouldUpdate = hydrateTextInstance(textInstance, textContent, fiber);
    {
      if (shouldUpdate) {
        // We assume that prepareToHydrateHostTextInstance is called in a context where the
        // hydration parent is the parent host component of this host text.
        var returnFiber = hydrationParentFiber;
        if (returnFiber !== null) {
          switch (returnFiber.tag) {
            case HostRoot:
              {
                var parentContainer = returnFiber.stateNode.containerInfo;
                didNotMatchHydratedContainerTextInstance(parentContainer, textInstance, textContent);
                break;
              }
            case HostComponent:
              {
                var parentType = returnFiber.type;
                var parentProps = returnFiber.memoizedProps;
                var parentInstance = returnFiber.stateNode;
                didNotMatchHydratedTextInstance(parentType, parentProps, parentInstance, textInstance, textContent);
                break;
              }
          }
        }
      }
    }
    return shouldUpdate;
  }

  function popToNextHostParent(fiber) {
    var parent = fiber['return'];
    while (parent !== null && parent.tag !== HostComponent && parent.tag !== HostRoot) {
      parent = parent['return'];
    }
    hydrationParentFiber = parent;
  }

  function popHydrationState(fiber) {
    if (fiber !== hydrationParentFiber) {
      // We're deeper than the current hydration context, inside an inserted
      // tree.
      return false;
    }
    if (!isHydrating) {
      // If we're not currently hydrating but we're in a hydration context, then
      // we were an insertion and now need to pop up reenter hydration of our
      // siblings.
      popToNextHostParent(fiber);
      isHydrating = true;
      return false;
    }

    var type = fiber.type;

    // If we have any remaining hydratable nodes, we need to delete them now.
    // We only do this deeper than head and body since they tend to have random
    // other nodes in them. We also ignore components with pure text content in
    // side of them.
    // TODO: Better heuristic.
    if (fiber.tag !== HostComponent || type !== 'head' && type !== 'body' && !shouldSetTextContent(type, fiber.memoizedProps)) {
      var nextInstance = nextHydratableInstance;
      while (nextInstance) {
        deleteHydratableInstance(fiber, nextInstance);
        nextInstance = getNextHydratableSibling(nextInstance);
      }
    }

    popToNextHostParent(fiber);
    nextHydratableInstance = hydrationParentFiber ? getNextHydratableSibling(fiber.stateNode) : null;
    return true;
  }

  function resetHydrationState() {
    hydrationParentFiber = null;
    nextHydratableInstance = null;
    isHydrating = false;
  }

  return {
    enterHydrationState: enterHydrationState,
    resetHydrationState: resetHydrationState,
    tryToClaimNextHydratableInstance: tryToClaimNextHydratableInstance,
    prepareToHydrateHostInstance: prepareToHydrateHostInstance,
    prepareToHydrateHostTextInstance: prepareToHydrateHostTextInstance,
    popHydrationState: popHydrationState
  };
};

// This lets us hook into Fiber to debug what it's doing.
// See https://github.com/facebook/react/pull/8033.
// This is not part of the public API, not even for React DevTools.
// You may only inject a debugTool if you work on React Fiber itself.
var ReactFiberInstrumentation = {
  debugTool: null
};

var ReactFiberInstrumentation_1 = ReactFiberInstrumentation;

var defaultShowDialog = function (capturedError) {
  return true;
};

var showDialog = defaultShowDialog;

function logCapturedError(capturedError) {
  var logError = showDialog(capturedError);

  // Allow injected showDialog() to prevent default console.error logging.
  // This enables renderers like ReactNative to better manage redbox behavior.
  if (logError === false) {
    return;
  }

  var error = capturedError.error;
  var suppressLogging = error && error.suppressReactErrorLogging;
  if (suppressLogging) {
    return;
  }

  {
    var componentName = capturedError.componentName,
        componentStack = capturedError.componentStack,
        errorBoundaryName = capturedError.errorBoundaryName,
        errorBoundaryFound = capturedError.errorBoundaryFound,
        willRetry = capturedError.willRetry;


    var componentNameMessage = componentName ? 'The above error occurred in the <' + componentName + '> component:' : 'The above error occurred in one of your React components:';

    var errorBoundaryMessage = void 0;
    // errorBoundaryFound check is sufficient; errorBoundaryName check is to satisfy Flow.
    if (errorBoundaryFound && errorBoundaryName) {
      if (willRetry) {
        errorBoundaryMessage = 'React will try to recreate this component tree from scratch ' + ('using the error boundary you provided, ' + errorBoundaryName + '.');
      } else {
        errorBoundaryMessage = 'This error was initially handled by the error boundary ' + errorBoundaryName + '.\n' + 'Recreating the tree from scratch failed so React will unmount the tree.';
      }
    } else {
      errorBoundaryMessage = 'Consider adding an error boundary to your tree to customize error handling behavior.\n' + 'Visit https://fb.me/react-error-boundaries to learn more about error boundaries.';
    }
    var combinedMessage = '' + componentNameMessage + componentStack + '\n\n' + ('' + errorBoundaryMessage);

    // In development, we provide our own message with just the component stack.
    // We don't include the original error message and JS stack because the browser
    // has already printed it. Even if the application swallows the error, it is still
    // displayed by the browser thanks to the DEV-only fake event trick in ReactErrorUtils.
    console.error(combinedMessage);
  }
}

var invokeGuardedCallback$1 = ReactErrorUtils.invokeGuardedCallback;
var hasCaughtError = ReactErrorUtils.hasCaughtError;
var clearCaughtError = ReactErrorUtils.clearCaughtError;


{
  var didWarnAboutStateTransition = false;
  var didWarnSetStateChildContext = false;
  var didWarnStateUpdateForUnmountedComponent = {};

  var warnAboutUpdateOnUnmounted = function (fiber) {
    var componentName = getComponentName(fiber) || 'ReactClass';
    if (didWarnStateUpdateForUnmountedComponent[componentName]) {
      return;
    }
    warning(false, 'Can only update a mounted or mounting ' + 'component. This usually means you called setState, replaceState, ' + 'or forceUpdate on an unmounted component. This is a no-op.\n\nPlease ' + 'check the code for the %s component.', componentName);
    didWarnStateUpdateForUnmountedComponent[componentName] = true;
  };

  var warnAboutInvalidUpdates = function (instance) {
    switch (ReactDebugCurrentFiber.phase) {
      case 'getChildContext':
        if (didWarnSetStateChildContext) {
          return;
        }
        warning(false, 'setState(...): Cannot call setState() inside getChildContext()');
        didWarnSetStateChildContext = true;
        break;
      case 'render':
        if (didWarnAboutStateTransition) {
          return;
        }
        warning(false, 'Cannot update during an existing state transition (such as within ' + "`render` or another component's constructor). Render methods should " + 'be a pure function of props and state; constructor side-effects are ' + 'an anti-pattern, but can be moved to `componentWillMount`.');
        didWarnAboutStateTransition = true;
        break;
    }
  };
}

var ReactFiberScheduler = function (config) {
  var hostContext = ReactFiberHostContext(config);
  var hydrationContext = ReactFiberHydrationContext(config);
  var popHostContainer = hostContext.popHostContainer,
      popHostContext = hostContext.popHostContext,
      resetHostContainer = hostContext.resetHostContainer;

  var _ReactFiberBeginWork = ReactFiberBeginWork(config, hostContext, hydrationContext, scheduleWork, computeExpirationForFiber),
      beginWork = _ReactFiberBeginWork.beginWork,
      beginFailedWork = _ReactFiberBeginWork.beginFailedWork;

  var _ReactFiberCompleteWo = ReactFiberCompleteWork(config, hostContext, hydrationContext),
      completeWork = _ReactFiberCompleteWo.completeWork;

  var _ReactFiberCommitWork = ReactFiberCommitWork(config, captureError),
      commitResetTextContent = _ReactFiberCommitWork.commitResetTextContent,
      commitPlacement = _ReactFiberCommitWork.commitPlacement,
      commitDeletion = _ReactFiberCommitWork.commitDeletion,
      commitWork = _ReactFiberCommitWork.commitWork,
      commitLifeCycles = _ReactFiberCommitWork.commitLifeCycles,
      commitAttachRef = _ReactFiberCommitWork.commitAttachRef,
      commitDetachRef = _ReactFiberCommitWork.commitDetachRef;

  var now = config.now,
      scheduleDeferredCallback = config.scheduleDeferredCallback,
      cancelDeferredCallback = config.cancelDeferredCallback,
      useSyncScheduling = config.useSyncScheduling,
      prepareForCommit = config.prepareForCommit,
      resetAfterCommit = config.resetAfterCommit;

  // Represents the current time in ms.

  var startTime = now();
  var mostRecentCurrentTime = msToExpirationTime(0);

  // Represents the expiration time that incoming updates should use. (If this
  // is NoWork, use the default strategy: async updates in async mode, sync
  // updates in sync mode.)
  var expirationContext = NoWork;

  var isWorking = false;

  // The next work in progress fiber that we're currently working on.
  var nextUnitOfWork = null;
  var nextRoot = null;
  // The time at which we're currently rendering work.
  var nextRenderExpirationTime = NoWork;

  // The next fiber with an effect that we're currently committing.
  var nextEffect = null;

  // Keep track of which fibers have captured an error that need to be handled.
  // Work is removed from this collection after componentDidCatch is called.
  var capturedErrors = null;
  // Keep track of which fibers have failed during the current batch of work.
  // This is a different set than capturedErrors, because it is not reset until
  // the end of the batch. This is needed to propagate errors correctly if a
  // subtree fails more than once.
  var failedBoundaries = null;
  // Error boundaries that captured an error during the current commit.
  var commitPhaseBoundaries = null;
  var firstUncaughtError = null;
  var didFatal = false;

  var isCommitting = false;
  var isUnmounting = false;

  // Used for performance tracking.
  var interruptedBy = null;

  function resetContextStack() {
    // Reset the stack
    reset$1();
    // Reset the cursors
    resetContext();
    resetHostContainer();
  }

  function commitAllHostEffects() {
    while (nextEffect !== null) {
      {
        ReactDebugCurrentFiber.setCurrentFiber(nextEffect);
      }
      recordEffect();

      var effectTag = nextEffect.effectTag;
      if (effectTag & ContentReset) {
        commitResetTextContent(nextEffect);
      }

      if (effectTag & Ref) {
        var current = nextEffect.alternate;
        if (current !== null) {
          commitDetachRef(current);
        }
      }

      // The following switch statement is only concerned about placement,
      // updates, and deletions. To avoid needing to add a case for every
      // possible bitmap value, we remove the secondary effects from the
      // effect tag and switch on that value.
      var primaryEffectTag = effectTag & ~(Callback | Err | ContentReset | Ref | PerformedWork);
      switch (primaryEffectTag) {
        case Placement:
          {
            commitPlacement(nextEffect);
            // Clear the "placement" from effect tag so that we know that this is inserted, before
            // any life-cycles like componentDidMount gets called.
            // TODO: findDOMNode doesn't rely on this any more but isMounted
            // does and isMounted is deprecated anyway so we should be able
            // to kill this.
            nextEffect.effectTag &= ~Placement;
            break;
          }
        case PlacementAndUpdate:
          {
            // Placement
            commitPlacement(nextEffect);
            // Clear the "placement" from effect tag so that we know that this is inserted, before
            // any life-cycles like componentDidMount gets called.
            nextEffect.effectTag &= ~Placement;

            // Update
            var _current = nextEffect.alternate;
            commitWork(_current, nextEffect);
            break;
          }
        case Update:
          {
            var _current2 = nextEffect.alternate;
            commitWork(_current2, nextEffect);
            break;
          }
        case Deletion:
          {
            isUnmounting = true;
            commitDeletion(nextEffect);
            isUnmounting = false;
            break;
          }
      }
      nextEffect = nextEffect.nextEffect;
    }

    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
  }

  function commitAllLifeCycles() {
    while (nextEffect !== null) {
      var effectTag = nextEffect.effectTag;

      if (effectTag & (Update | Callback)) {
        recordEffect();
        var current = nextEffect.alternate;
        commitLifeCycles(current, nextEffect);
      }

      if (effectTag & Ref) {
        recordEffect();
        commitAttachRef(nextEffect);
      }

      if (effectTag & Err) {
        recordEffect();
        commitErrorHandling(nextEffect);
      }

      var next = nextEffect.nextEffect;
      // Ensure that we clean these up so that we don't accidentally keep them.
      // I'm not actually sure this matters because we can't reset firstEffect
      // and lastEffect since they're on every node, not just the effectful
      // ones. So we have to clean everything as we reuse nodes anyway.
      nextEffect.nextEffect = null;
      // Ensure that we reset the effectTag here so that we can rely on effect
      // tags to reason about the current life-cycle.
      nextEffect = next;
    }
  }

  function commitRoot(finishedWork) {
    // We keep track of this so that captureError can collect any boundaries
    // that capture an error during the commit phase. The reason these aren't
    // local to this function is because errors that occur during cWU are
    // captured elsewhere, to prevent the unmount from being interrupted.
    isWorking = true;
    isCommitting = true;
    startCommitTimer();

    var root = finishedWork.stateNode;
    !(root.current !== finishedWork) ? invariant(false, 'Cannot commit the same tree as before. This is probably a bug related to the return field. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    root.isReadyForCommit = false;

    // Reset this to null before calling lifecycles
    ReactCurrentOwner.current = null;

    var firstEffect = void 0;
    if (finishedWork.effectTag > PerformedWork) {
      // A fiber's effect list consists only of its children, not itself. So if
      // the root has an effect, we need to add it to the end of the list. The
      // resulting list is the set that would belong to the root's parent, if
      // it had one; that is, all the effects in the tree including the root.
      if (finishedWork.lastEffect !== null) {
        finishedWork.lastEffect.nextEffect = finishedWork;
        firstEffect = finishedWork.firstEffect;
      } else {
        firstEffect = finishedWork;
      }
    } else {
      // There is no effect on the root.
      firstEffect = finishedWork.firstEffect;
    }

    prepareForCommit();

    // Commit all the side-effects within a tree. We'll do this in two passes.
    // The first pass performs all the host insertions, updates, deletions and
    // ref unmounts.
    nextEffect = firstEffect;
    startCommitHostEffectsTimer();
    while (nextEffect !== null) {
      var didError = false;
      var _error = void 0;
      {
        invokeGuardedCallback$1(null, commitAllHostEffects, null);
        if (hasCaughtError()) {
          didError = true;
          _error = clearCaughtError();
        }
      }
      if (didError) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        captureError(nextEffect, _error);
        // Clean-up
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }
    stopCommitHostEffectsTimer();

    resetAfterCommit();

    // The work-in-progress tree is now the current tree. This must come after
    // the first pass of the commit phase, so that the previous tree is still
    // current during componentWillUnmount, but before the second pass, so that
    // the finished work is current during componentDidMount/Update.
    root.current = finishedWork;

    // In the second pass we'll perform all life-cycles and ref callbacks.
    // Life-cycles happen as a separate pass so that all placements, updates,
    // and deletions in the entire tree have already been invoked.
    // This pass also triggers any renderer-specific initial effects.
    nextEffect = firstEffect;
    startCommitLifeCyclesTimer();
    while (nextEffect !== null) {
      var _didError = false;
      var _error2 = void 0;
      {
        invokeGuardedCallback$1(null, commitAllLifeCycles, null);
        if (hasCaughtError()) {
          _didError = true;
          _error2 = clearCaughtError();
        }
      }
      if (_didError) {
        !(nextEffect !== null) ? invariant(false, 'Should have next effect. This error is likely caused by a bug in React. Please file an issue.') : void 0;
        captureError(nextEffect, _error2);
        if (nextEffect !== null) {
          nextEffect = nextEffect.nextEffect;
        }
      }
    }

    isCommitting = false;
    isWorking = false;
    stopCommitLifeCyclesTimer();
    stopCommitTimer();
    if (typeof onCommitRoot === 'function') {
      onCommitRoot(finishedWork.stateNode);
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onCommitWork(finishedWork);
    }

    // If we caught any errors during this commit, schedule their boundaries
    // to update.
    if (commitPhaseBoundaries) {
      commitPhaseBoundaries.forEach(scheduleErrorRecovery);
      commitPhaseBoundaries = null;
    }

    if (firstUncaughtError !== null) {
      var _error3 = firstUncaughtError;
      firstUncaughtError = null;
      onUncaughtError(_error3);
    }

    var remainingTime = root.current.expirationTime;

    if (remainingTime === NoWork) {
      capturedErrors = null;
      failedBoundaries = null;
    }

    return remainingTime;
  }

  function resetExpirationTime(workInProgress, renderTime) {
    if (renderTime !== Never && workInProgress.expirationTime === Never) {
      // The children of this component are hidden. Don't bubble their
      // expiration times.
      return;
    }

    // Check for pending updates.
    var newExpirationTime = getUpdateExpirationTime(workInProgress);

    // TODO: Calls need to visit stateNode

    // Bubble up the earliest expiration time.
    var child = workInProgress.child;
    while (child !== null) {
      if (child.expirationTime !== NoWork && (newExpirationTime === NoWork || newExpirationTime > child.expirationTime)) {
        newExpirationTime = child.expirationTime;
      }
      child = child.sibling;
    }
    workInProgress.expirationTime = newExpirationTime;
  }

  function completeUnitOfWork(workInProgress) {
    while (true) {
      // The current, flushed, state of this fiber is the alternate.
      // Ideally nothing should rely on this, but relying on it here
      // means that we don't need an additional field on the work in
      // progress.
      var current = workInProgress.alternate;
      {
        ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
      }
      var next = completeWork(current, workInProgress, nextRenderExpirationTime);
      {
        ReactDebugCurrentFiber.resetCurrentFiber();
      }

      var returnFiber = workInProgress['return'];
      var siblingFiber = workInProgress.sibling;

      resetExpirationTime(workInProgress, nextRenderExpirationTime);

      if (next !== null) {
        stopWorkTimer(workInProgress);
        if (true && ReactFiberInstrumentation_1.debugTool) {
          ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
        }
        // If completing this work spawned new work, do that next. We'll come
        // back here again.
        return next;
      }

      if (returnFiber !== null) {
        // Append all the effects of the subtree and this fiber onto the effect
        // list of the parent. The completion order of the children affects the
        // side-effect order.
        if (returnFiber.firstEffect === null) {
          returnFiber.firstEffect = workInProgress.firstEffect;
        }
        if (workInProgress.lastEffect !== null) {
          if (returnFiber.lastEffect !== null) {
            returnFiber.lastEffect.nextEffect = workInProgress.firstEffect;
          }
          returnFiber.lastEffect = workInProgress.lastEffect;
        }

        // If this fiber had side-effects, we append it AFTER the children's
        // side-effects. We can perform certain side-effects earlier if
        // needed, by doing multiple passes over the effect list. We don't want
        // to schedule our own side-effect on our own list because if end up
        // reusing children we'll schedule this effect onto itself since we're
        // at the end.
        var effectTag = workInProgress.effectTag;
        // Skip both NoWork and PerformedWork tags when creating the effect list.
        // PerformedWork effect is read by React DevTools but shouldn't be committed.
        if (effectTag > PerformedWork) {
          if (returnFiber.lastEffect !== null) {
            returnFiber.lastEffect.nextEffect = workInProgress;
          } else {
            returnFiber.firstEffect = workInProgress;
          }
          returnFiber.lastEffect = workInProgress;
        }
      }

      stopWorkTimer(workInProgress);
      if (true && ReactFiberInstrumentation_1.debugTool) {
        ReactFiberInstrumentation_1.debugTool.onCompleteWork(workInProgress);
      }

      if (siblingFiber !== null) {
        // If there is more work to do in this returnFiber, do that next.
        return siblingFiber;
      } else if (returnFiber !== null) {
        // If there's no more work in this returnFiber. Complete the returnFiber.
        workInProgress = returnFiber;
        continue;
      } else {
        // We've reached the root.
        var root = workInProgress.stateNode;
        root.isReadyForCommit = true;
        return null;
      }
    }

    // Without this explicit null return Flow complains of invalid return type
    // TODO Remove the above while(true) loop
    // eslint-disable-next-line no-unreachable
    return null;
  }

  function performUnitOfWork(workInProgress) {
    // The current, flushed, state of this fiber is the alternate.
    // Ideally nothing should rely on this, but relying on it here
    // means that we don't need an additional field on the work in
    // progress.
    var current = workInProgress.alternate;

    // See if beginning this work spawns more work.
    startWorkTimer(workInProgress);
    {
      ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
    }

    var next = beginWork(current, workInProgress, nextRenderExpirationTime);
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onBeginWork(workInProgress);
    }

    if (next === null) {
      // If this doesn't spawn new work, complete the current work.
      next = completeUnitOfWork(workInProgress);
    }

    ReactCurrentOwner.current = null;

    return next;
  }

  function performFailedUnitOfWork(workInProgress) {
    // The current, flushed, state of this fiber is the alternate.
    // Ideally nothing should rely on this, but relying on it here
    // means that we don't need an additional field on the work in
    // progress.
    var current = workInProgress.alternate;

    // See if beginning this work spawns more work.
    startWorkTimer(workInProgress);
    {
      ReactDebugCurrentFiber.setCurrentFiber(workInProgress);
    }
    var next = beginFailedWork(current, workInProgress, nextRenderExpirationTime);
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }
    if (true && ReactFiberInstrumentation_1.debugTool) {
      ReactFiberInstrumentation_1.debugTool.onBeginWork(workInProgress);
    }

    if (next === null) {
      // If this doesn't spawn new work, complete the current work.
      next = completeUnitOfWork(workInProgress);
    }

    ReactCurrentOwner.current = null;

    return next;
  }

  function workLoop(expirationTime) {
    if (capturedErrors !== null) {
      // If there are unhandled errors, switch to the slow work loop.
      // TODO: How to avoid this check in the fast path? Maybe the renderer
      // could keep track of which roots have unhandled errors and call a
      // forked version of renderRoot.
      slowWorkLoopThatChecksForFailedWork(expirationTime);
      return;
    }
    if (nextRenderExpirationTime === NoWork || nextRenderExpirationTime > expirationTime) {
      return;
    }

    if (nextRenderExpirationTime <= mostRecentCurrentTime) {
      // Flush all expired work.
      while (nextUnitOfWork !== null) {
        nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
      }
    } else {
      // Flush asynchronous work until the deadline runs out of time.
      while (nextUnitOfWork !== null && !shouldYield()) {
        nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
      }
    }
  }

  function slowWorkLoopThatChecksForFailedWork(expirationTime) {
    if (nextRenderExpirationTime === NoWork || nextRenderExpirationTime > expirationTime) {
      return;
    }

    if (nextRenderExpirationTime <= mostRecentCurrentTime) {
      // Flush all expired work.
      while (nextUnitOfWork !== null) {
        if (hasCapturedError(nextUnitOfWork)) {
          // Use a forked version of performUnitOfWork
          nextUnitOfWork = performFailedUnitOfWork(nextUnitOfWork);
        } else {
          nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
        }
      }
    } else {
      // Flush asynchronous work until the deadline runs out of time.
      while (nextUnitOfWork !== null && !shouldYield()) {
        if (hasCapturedError(nextUnitOfWork)) {
          // Use a forked version of performUnitOfWork
          nextUnitOfWork = performFailedUnitOfWork(nextUnitOfWork);
        } else {
          nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
        }
      }
    }
  }

  function renderRootCatchBlock(root, failedWork, boundary, expirationTime) {
    // We're going to restart the error boundary that captured the error.
    // Conceptually, we're unwinding the stack. We need to unwind the
    // context stack, too.
    unwindContexts(failedWork, boundary);

    // Restart the error boundary using a forked version of
    // performUnitOfWork that deletes the boundary's children. The entire
    // failed subree will be unmounted. During the commit phase, a special
    // lifecycle method is called on the error boundary, which triggers
    // a re-render.
    nextUnitOfWork = performFailedUnitOfWork(boundary);

    // Continue working.
    workLoop(expirationTime);
  }

  function renderRoot(root, expirationTime) {
    !!isWorking ? invariant(false, 'renderRoot was called recursively. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    isWorking = true;

    // We're about to mutate the work-in-progress tree. If the root was pending
    // commit, it no longer is: we'll need to complete it again.
    root.isReadyForCommit = false;

    // Check if we're starting from a fresh stack, or if we're resuming from
    // previously yielded work.
    if (root !== nextRoot || expirationTime !== nextRenderExpirationTime || nextUnitOfWork === null) {
      // Reset the stack and start working from the root.
      resetContextStack();
      nextRoot = root;
      nextRenderExpirationTime = expirationTime;
      nextUnitOfWork = createWorkInProgress(nextRoot.current, null, expirationTime);
    }

    startWorkLoopTimer(nextUnitOfWork);

    var didError = false;
    var error = null;
    {
      invokeGuardedCallback$1(null, workLoop, null, expirationTime);
      if (hasCaughtError()) {
        didError = true;
        error = clearCaughtError();
      }
    }

    // An error was thrown during the render phase.
    while (didError) {
      if (didFatal) {
        // This was a fatal error. Don't attempt to recover from it.
        firstUncaughtError = error;
        break;
      }

      var failedWork = nextUnitOfWork;
      if (failedWork === null) {
        // An error was thrown but there's no current unit of work. This can
        // happen during the commit phase if there's a bug in the renderer.
        didFatal = true;
        continue;
      }

      // "Capture" the error by finding the nearest boundary. If there is no
      // error boundary, we use the root.
      var boundary = captureError(failedWork, error);
      !(boundary !== null) ? invariant(false, 'Should have found an error boundary. This error is likely caused by a bug in React. Please file an issue.') : void 0;

      if (didFatal) {
        // The error we just captured was a fatal error. This happens
        // when the error propagates to the root more than once.
        continue;
      }

      didError = false;
      error = null;
      {
        invokeGuardedCallback$1(null, renderRootCatchBlock, null, root, failedWork, boundary, expirationTime);
        if (hasCaughtError()) {
          didError = true;
          error = clearCaughtError();
          continue;
        }
      }
      // We're finished working. Exit the error loop.
      break;
    }

    var uncaughtError = firstUncaughtError;

    // We're done performing work. Time to clean up.
    stopWorkLoopTimer(interruptedBy);
    interruptedBy = null;
    isWorking = false;
    didFatal = false;
    firstUncaughtError = null;

    if (uncaughtError !== null) {
      onUncaughtError(uncaughtError);
    }

    return root.isReadyForCommit ? root.current.alternate : null;
  }

  // Returns the boundary that captured the error, or null if the error is ignored
  function captureError(failedWork, error) {
    // It is no longer valid because we exited the user code.
    ReactCurrentOwner.current = null;
    {
      ReactDebugCurrentFiber.resetCurrentFiber();
    }

    // Search for the nearest error boundary.
    var boundary = null;

    // Passed to logCapturedError()
    var errorBoundaryFound = false;
    var willRetry = false;
    var errorBoundaryName = null;

    // Host containers are a special case. If the failed work itself is a host
    // container, then it acts as its own boundary. In all other cases, we
    // ignore the work itself and only search through the parents.
    if (failedWork.tag === HostRoot) {
      boundary = failedWork;

      if (isFailedBoundary(failedWork)) {
        // If this root already failed, there must have been an error when
        // attempting to unmount it. This is a worst-case scenario and
        // should only be possible if there's a bug in the renderer.
        didFatal = true;
      }
    } else {
      var node = failedWork['return'];
      while (node !== null && boundary === null) {
        if (node.tag === ClassComponent) {
          var instance = node.stateNode;
          if (typeof instance.componentDidCatch === 'function') {
            errorBoundaryFound = true;
            errorBoundaryName = getComponentName(node);

            // Found an error boundary!
            boundary = node;
            willRetry = true;
          }
        } else if (node.tag === HostRoot) {
          // Treat the root like a no-op error boundary
          boundary = node;
        }

        if (isFailedBoundary(node)) {
          // This boundary is already in a failed state.

          // If we're currently unmounting, that means this error was
          // thrown while unmounting a failed subtree. We should ignore
          // the error.
          if (isUnmounting) {
            return null;
          }

          // If we're in the commit phase, we should check to see if
          // this boundary already captured an error during this commit.
          // This case exists because multiple errors can be thrown during
          // a single commit without interruption.
          if (commitPhaseBoundaries !== null && (commitPhaseBoundaries.has(node) || node.alternate !== null && commitPhaseBoundaries.has(node.alternate))) {
            // If so, we should ignore this error.
            return null;
          }

          // The error should propagate to the next boundary -— we keep looking.
          boundary = null;
          willRetry = false;
        }

        node = node['return'];
      }
    }

    if (boundary !== null) {
      // Add to the collection of failed boundaries. This lets us know that
      // subsequent errors in this subtree should propagate to the next boundary.
      if (failedBoundaries === null) {
        failedBoundaries = new Set();
      }
      failedBoundaries.add(boundary);

      // This method is unsafe outside of the begin and complete phases.
      // We might be in the commit phase when an error is captured.
      // The risk is that the return path from this Fiber may not be accurate.
      // That risk is acceptable given the benefit of providing users more context.
      var _componentStack = getStackAddendumByWorkInProgressFiber(failedWork);
      var _componentName = getComponentName(failedWork);

      // Add to the collection of captured errors. This is stored as a global
      // map of errors and their component stack location keyed by the boundaries
      // that capture them. We mostly use this Map as a Set; it's a Map only to
      // avoid adding a field to Fiber to store the error.
      if (capturedErrors === null) {
        capturedErrors = new Map();
      }

      var capturedError = {
        componentName: _componentName,
        componentStack: _componentStack,
        error: error,
        errorBoundary: errorBoundaryFound ? boundary.stateNode : null,
        errorBoundaryFound: errorBoundaryFound,
        errorBoundaryName: errorBoundaryName,
        willRetry: willRetry
      };

      capturedErrors.set(boundary, capturedError);

      try {
        logCapturedError(capturedError);
      } catch (e) {
        // Prevent cycle if logCapturedError() throws.
        // A cycle may still occur if logCapturedError renders a component that throws.
        var suppressLogging = e && e.suppressReactErrorLogging;
        if (!suppressLogging) {
          console.error(e);
        }
      }

      // If we're in the commit phase, defer scheduling an update on the
      // boundary until after the commit is complete
      if (isCommitting) {
        if (commitPhaseBoundaries === null) {
          commitPhaseBoundaries = new Set();
        }
        commitPhaseBoundaries.add(boundary);
      } else {
        // Otherwise, schedule an update now.
        // TODO: Is this actually necessary during the render phase? Is it
        // possible to unwind and continue rendering at the same priority,
        // without corrupting internal state?
        scheduleErrorRecovery(boundary);
      }
      return boundary;
    } else if (firstUncaughtError === null) {
      // If no boundary is found, we'll need to throw the error
      firstUncaughtError = error;
    }
    return null;
  }

  function hasCapturedError(fiber) {
    // TODO: capturedErrors should store the boundary instance, to avoid needing
    // to check the alternate.
    return capturedErrors !== null && (capturedErrors.has(fiber) || fiber.alternate !== null && capturedErrors.has(fiber.alternate));
  }

  function isFailedBoundary(fiber) {
    // TODO: failedBoundaries should store the boundary instance, to avoid
    // needing to check the alternate.
    return failedBoundaries !== null && (failedBoundaries.has(fiber) || fiber.alternate !== null && failedBoundaries.has(fiber.alternate));
  }

  function commitErrorHandling(effectfulFiber) {
    var capturedError = void 0;
    if (capturedErrors !== null) {
      capturedError = capturedErrors.get(effectfulFiber);
      capturedErrors['delete'](effectfulFiber);
      if (capturedError == null) {
        if (effectfulFiber.alternate !== null) {
          effectfulFiber = effectfulFiber.alternate;
          capturedError = capturedErrors.get(effectfulFiber);
          capturedErrors['delete'](effectfulFiber);
        }
      }
    }

    !(capturedError != null) ? invariant(false, 'No error for given unit of work. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    switch (effectfulFiber.tag) {
      case ClassComponent:
        var instance = effectfulFiber.stateNode;

        var info = {
          componentStack: capturedError.componentStack
        };

        // Allow the boundary to handle the error, usually by scheduling
        // an update to itself
        instance.componentDidCatch(capturedError.error, info);
        return;
      case HostRoot:
        if (firstUncaughtError === null) {
          firstUncaughtError = capturedError.error;
        }
        return;
      default:
        invariant(false, 'Invalid type of work. This error is likely caused by a bug in React. Please file an issue.');
    }
  }

  function unwindContexts(from, to) {
    var node = from;
    while (node !== null) {
      switch (node.tag) {
        case ClassComponent:
          popContextProvider(node);
          break;
        case HostComponent:
          popHostContext(node);
          break;
        case HostRoot:
          popHostContainer(node);
          break;
        case HostPortal:
          popHostContainer(node);
          break;
      }
      if (node === to || node.alternate === to) {
        stopFailedWorkTimer(node);
        break;
      } else {
        stopWorkTimer(node);
      }
      node = node['return'];
    }
  }

  function computeAsyncExpiration() {
    // Given the current clock time, returns an expiration time. We use rounding
    // to batch like updates together.
    // Should complete within ~1000ms. 1200ms max.
    var currentTime = recalculateCurrentTime();
    var expirationMs = 1000;
    var bucketSizeMs = 200;
    return computeExpirationBucket(currentTime, expirationMs, bucketSizeMs);
  }

  function computeExpirationForFiber(fiber) {
    var expirationTime = void 0;
    if (expirationContext !== NoWork) {
      // An explicit expiration context was set;
      expirationTime = expirationContext;
    } else if (isWorking) {
      if (isCommitting) {
        // Updates that occur during the commit phase should have sync priority
        // by default.
        expirationTime = Sync;
      } else {
        // Updates during the render phase should expire at the same time as
        // the work that is being rendered.
        expirationTime = nextRenderExpirationTime;
      }
    } else {
      // No explicit expiration context was set, and we're not currently
      // performing work. Calculate a new expiration time.
      if (useSyncScheduling && !(fiber.internalContextTag & AsyncUpdates)) {
        // This is a sync update
        expirationTime = Sync;
      } else {
        // This is an async update
        expirationTime = computeAsyncExpiration();
      }
    }
    return expirationTime;
  }

  function scheduleWork(fiber, expirationTime) {
    return scheduleWorkImpl(fiber, expirationTime, false);
  }

  function checkRootNeedsClearing(root, fiber, expirationTime) {
    if (!isWorking && root === nextRoot && expirationTime < nextRenderExpirationTime) {
      // Restart the root from the top.
      if (nextUnitOfWork !== null) {
        // This is an interruption. (Used for performance tracking.)
        interruptedBy = fiber;
      }
      nextRoot = null;
      nextUnitOfWork = null;
      nextRenderExpirationTime = NoWork;
    }
  }

  function scheduleWorkImpl(fiber, expirationTime, isErrorRecovery) {
    recordScheduleUpdate();

    {
      if (!isErrorRecovery && fiber.tag === ClassComponent) {
        var instance = fiber.stateNode;
        warnAboutInvalidUpdates(instance);
      }
    }

    var node = fiber;
    while (node !== null) {
      // Walk the parent path to the root and update each node's
      // expiration time.
      if (node.expirationTime === NoWork || node.expirationTime > expirationTime) {
        node.expirationTime = expirationTime;
      }
      if (node.alternate !== null) {
        if (node.alternate.expirationTime === NoWork || node.alternate.expirationTime > expirationTime) {
          node.alternate.expirationTime = expirationTime;
        }
      }
      if (node['return'] === null) {
        if (node.tag === HostRoot) {
          var root = node.stateNode;

          checkRootNeedsClearing(root, fiber, expirationTime);
          requestWork(root, expirationTime);
          checkRootNeedsClearing(root, fiber, expirationTime);
        } else {
          {
            if (!isErrorRecovery && fiber.tag === ClassComponent) {
              warnAboutUpdateOnUnmounted(fiber);
            }
          }
          return;
        }
      }
      node = node['return'];
    }
  }

  function scheduleErrorRecovery(fiber) {
    scheduleWorkImpl(fiber, Sync, true);
  }

  function recalculateCurrentTime() {
    // Subtract initial time so it fits inside 32bits
    var ms = now() - startTime;
    mostRecentCurrentTime = msToExpirationTime(ms);
    return mostRecentCurrentTime;
  }

  function deferredUpdates(fn) {
    var previousExpirationContext = expirationContext;
    expirationContext = computeAsyncExpiration();
    try {
      return fn();
    } finally {
      expirationContext = previousExpirationContext;
    }
  }

  function syncUpdates(fn) {
    var previousExpirationContext = expirationContext;
    expirationContext = Sync;
    try {
      return fn();
    } finally {
      expirationContext = previousExpirationContext;
    }
  }

  // TODO: Everything below this is written as if it has been lifted to the
  // renderers. I'll do this in a follow-up.

  // Linked-list of roots
  var firstScheduledRoot = null;
  var lastScheduledRoot = null;

  var callbackExpirationTime = NoWork;
  var callbackID = -1;
  var isRendering = false;
  var nextFlushedRoot = null;
  var nextFlushedExpirationTime = NoWork;
  var deadlineDidExpire = false;
  var hasUnhandledError = false;
  var unhandledError = null;
  var deadline = null;

  var isBatchingUpdates = false;
  var isUnbatchingUpdates = false;

  // Use these to prevent an infinite loop of nested updates
  var NESTED_UPDATE_LIMIT = 1000;
  var nestedUpdateCount = 0;

  var timeHeuristicForUnitOfWork = 1;

  function scheduleCallbackWithExpiration(expirationTime) {
    if (callbackExpirationTime !== NoWork) {
      // A callback is already scheduled. Check its expiration time (timeout).
      if (expirationTime > callbackExpirationTime) {
        // Existing callback has sufficient timeout. Exit.
        return;
      } else {
        // Existing callback has insufficient timeout. Cancel and schedule a
        // new one.
        cancelDeferredCallback(callbackID);
      }
      // The request callback timer is already running. Don't start a new one.
    } else {
      startRequestCallbackTimer();
    }

    // Compute a timeout for the given expiration time.
    var currentMs = now() - startTime;
    var expirationMs = expirationTimeToMs(expirationTime);
    var timeout = expirationMs - currentMs;

    callbackExpirationTime = expirationTime;
    callbackID = scheduleDeferredCallback(performAsyncWork, { timeout: timeout });
  }

  // requestWork is called by the scheduler whenever a root receives an update.
  // It's up to the renderer to call renderRoot at some point in the future.
  function requestWork(root, expirationTime) {
    if (nestedUpdateCount > NESTED_UPDATE_LIMIT) {
      invariant(false, 'Maximum update depth exceeded. This can happen when a component repeatedly calls setState inside componentWillUpdate or componentDidUpdate. React limits the number of nested updates to prevent infinite loops.');
    }

    // Add the root to the schedule.
    // Check if this root is already part of the schedule.
    if (root.nextScheduledRoot === null) {
      // This root is not already scheduled. Add it.
      root.remainingExpirationTime = expirationTime;
      if (lastScheduledRoot === null) {
        firstScheduledRoot = lastScheduledRoot = root;
        root.nextScheduledRoot = root;
      } else {
        lastScheduledRoot.nextScheduledRoot = root;
        lastScheduledRoot = root;
        lastScheduledRoot.nextScheduledRoot = firstScheduledRoot;
      }
    } else {
      // This root is already scheduled, but its priority may have increased.
      var remainingExpirationTime = root.remainingExpirationTime;
      if (remainingExpirationTime === NoWork || expirationTime < remainingExpirationTime) {
        // Update the priority.
        root.remainingExpirationTime = expirationTime;
      }
    }

    if (isRendering) {
      // Prevent reentrancy. Remaining work will be scheduled at the end of
      // the currently rendering batch.
      return;
    }

    if (isBatchingUpdates) {
      // Flush work at the end of the batch.
      if (isUnbatchingUpdates) {
        // ...unless we're inside unbatchedUpdates, in which case we should
        // flush it now.
        nextFlushedRoot = root;
        nextFlushedExpirationTime = Sync;
        performWorkOnRoot(nextFlushedRoot, nextFlushedExpirationTime);
      }
      return;
    }

    // TODO: Get rid of Sync and use current time?
    if (expirationTime === Sync) {
      performWork(Sync, null);
    } else {
      scheduleCallbackWithExpiration(expirationTime);
    }
  }

  function findHighestPriorityRoot() {
    var highestPriorityWork = NoWork;
    var highestPriorityRoot = null;

    if (lastScheduledRoot !== null) {
      var previousScheduledRoot = lastScheduledRoot;
      var root = firstScheduledRoot;
      while (root !== null) {
        var remainingExpirationTime = root.remainingExpirationTime;
        if (remainingExpirationTime === NoWork) {
          // This root no longer has work. Remove it from the scheduler.

          // TODO: This check is redudant, but Flow is confused by the branch
          // below where we set lastScheduledRoot to null, even though we break
          // from the loop right after.
          !(previousScheduledRoot !== null && lastScheduledRoot !== null) ? invariant(false, 'Should have a previous and last root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
          if (root === root.nextScheduledRoot) {
            // This is the only root in the list.
            root.nextScheduledRoot = null;
            firstScheduledRoot = lastScheduledRoot = null;
            break;
          } else if (root === firstScheduledRoot) {
            // This is the first root in the list.
            var next = root.nextScheduledRoot;
            firstScheduledRoot = next;
            lastScheduledRoot.nextScheduledRoot = next;
            root.nextScheduledRoot = null;
          } else if (root === lastScheduledRoot) {
            // This is the last root in the list.
            lastScheduledRoot = previousScheduledRoot;
            lastScheduledRoot.nextScheduledRoot = firstScheduledRoot;
            root.nextScheduledRoot = null;
            break;
          } else {
            previousScheduledRoot.nextScheduledRoot = root.nextScheduledRoot;
            root.nextScheduledRoot = null;
          }
          root = previousScheduledRoot.nextScheduledRoot;
        } else {
          if (highestPriorityWork === NoWork || remainingExpirationTime < highestPriorityWork) {
            // Update the priority, if it's higher
            highestPriorityWork = remainingExpirationTime;
            highestPriorityRoot = root;
          }
          if (root === lastScheduledRoot) {
            break;
          }
          previousScheduledRoot = root;
          root = root.nextScheduledRoot;
        }
      }
    }

    // If the next root is the same as the previous root, this is a nested
    // update. To prevent an infinite loop, increment the nested update count.
    var previousFlushedRoot = nextFlushedRoot;
    if (previousFlushedRoot !== null && previousFlushedRoot === highestPriorityRoot) {
      nestedUpdateCount++;
    } else {
      // Reset whenever we switch roots.
      nestedUpdateCount = 0;
    }
    nextFlushedRoot = highestPriorityRoot;
    nextFlushedExpirationTime = highestPriorityWork;
  }

  function performAsyncWork(dl) {
    performWork(NoWork, dl);
  }

  function performWork(minExpirationTime, dl) {
    deadline = dl;

    // Keep working on roots until there's no more work, or until the we reach
    // the deadline.
    findHighestPriorityRoot();

    if (enableUserTimingAPI && deadline !== null) {
      var didExpire = nextFlushedExpirationTime < recalculateCurrentTime();
      stopRequestCallbackTimer(didExpire);
    }

    while (nextFlushedRoot !== null && nextFlushedExpirationTime !== NoWork && (minExpirationTime === NoWork || nextFlushedExpirationTime <= minExpirationTime) && !deadlineDidExpire) {
      performWorkOnRoot(nextFlushedRoot, nextFlushedExpirationTime);
      // Find the next highest priority work.
      findHighestPriorityRoot();
    }

    // We're done flushing work. Either we ran out of time in this callback,
    // or there's no more work left with sufficient priority.

    // If we're inside a callback, set this to false since we just completed it.
    if (deadline !== null) {
      callbackExpirationTime = NoWork;
      callbackID = -1;
    }
    // If there's work left over, schedule a new callback.
    if (nextFlushedExpirationTime !== NoWork) {
      scheduleCallbackWithExpiration(nextFlushedExpirationTime);
    }

    // Clean-up.
    deadline = null;
    deadlineDidExpire = false;
    nestedUpdateCount = 0;

    if (hasUnhandledError) {
      var _error4 = unhandledError;
      unhandledError = null;
      hasUnhandledError = false;
      throw _error4;
    }
  }

  function performWorkOnRoot(root, expirationTime) {
    !!isRendering ? invariant(false, 'performWorkOnRoot was called recursively. This error is likely caused by a bug in React. Please file an issue.') : void 0;

    isRendering = true;

    // Check if this is async work or sync/expired work.
    // TODO: Pass current time as argument to renderRoot, commitRoot
    if (expirationTime <= recalculateCurrentTime()) {
      // Flush sync work.
      var finishedWork = root.finishedWork;
      if (finishedWork !== null) {
        // This root is already complete. We can commit it.
        root.finishedWork = null;
        root.remainingExpirationTime = commitRoot(finishedWork);
      } else {
        root.finishedWork = null;
        finishedWork = renderRoot(root, expirationTime);
        if (finishedWork !== null) {
          // We've completed the root. Commit it.
          root.remainingExpirationTime = commitRoot(finishedWork);
        }
      }
    } else {
      // Flush async work.
      var _finishedWork = root.finishedWork;
      if (_finishedWork !== null) {
        // This root is already complete. We can commit it.
        root.finishedWork = null;
        root.remainingExpirationTime = commitRoot(_finishedWork);
      } else {
        root.finishedWork = null;
        _finishedWork = renderRoot(root, expirationTime);
        if (_finishedWork !== null) {
          // We've completed the root. Check the deadline one more time
          // before committing.
          if (!shouldYield()) {
            // Still time left. Commit the root.
            root.remainingExpirationTime = commitRoot(_finishedWork);
          } else {
            // There's no time left. Mark this root as complete. We'll come
            // back and commit it later.
            root.finishedWork = _finishedWork;
          }
        }
      }
    }

    isRendering = false;
  }

  // When working on async work, the reconciler asks the renderer if it should
  // yield execution. For DOM, we implement this with requestIdleCallback.
  function shouldYield() {
    if (deadline === null) {
      return false;
    }
    if (deadline.timeRemaining() > timeHeuristicForUnitOfWork) {
      // Disregard deadline.didTimeout. Only expired work should be flushed
      // during a timeout. This path is only hit for non-expired work.
      return false;
    }
    deadlineDidExpire = true;
    return true;
  }

  // TODO: Not happy about this hook. Conceptually, renderRoot should return a
  // tuple of (isReadyForCommit, didError, error)
  function onUncaughtError(error) {
    !(nextFlushedRoot !== null) ? invariant(false, 'Should be working on a root. This error is likely caused by a bug in React. Please file an issue.') : void 0;
    // Unschedule this root so we don't work on it again until there's
    // another update.
    nextFlushedRoot.remainingExpirationTime = NoWork;
    if (!hasUnhandledError) {
      hasUnhandledError = true;
      unhandledError = error;
    }
  }

  // TODO: Batching should be implemented at the renderer level, not inside
  // the reconciler.
  function batchedUpdates(fn, a) {
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      return fn(a);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      if (!isBatchingUpdates && !isRendering) {
        performWork(Sync, null);
      }
    }
  }

  // TODO: Batching should be implemented at the renderer level, not inside
  // the reconciler.
  function unbatchedUpdates(fn) {
    if (isBatchingUpdates && !isUnbatchingUpdates) {
      isUnbatchingUpdates = true;
      try {
        return fn();
      } finally {
        isUnbatchingUpdates = false;
      }
    }
    return fn();
  }

  // TODO: Batching should be implemented at the renderer level, not within
  // the reconciler.
  function flushSync(fn) {
    var previousIsBatchingUpdates = isBatchingUpdates;
    isBatchingUpdates = true;
    try {
      return syncUpdates(fn);
    } finally {
      isBatchingUpdates = previousIsBatchingUpdates;
      !!isRendering ? invariant(false, 'flushSync was called from inside a lifecycle method. It cannot be called when React is already rendering.') : void 0;
      performWork(Sync, null);
    }
  }

  return {
    computeAsyncExpiration: computeAsyncExpiration,
    computeExpirationForFiber: computeExpirationForFiber,
    scheduleWork: scheduleWork,
    batchedUpdates: batchedUpdates,
    unbatchedUpdates: unbatchedUpdates,
    flushSync: flushSync,
    deferredUpdates: deferredUpdates
  };
};

{
  var didWarnAboutNestedUpdates = false;
}

// 0 is PROD, 1 is DEV.
// Might add PROFILE later.


function getContextForSubtree(parentComponent) {
  if (!parentComponent) {
    return emptyObject;
  }

  var fiber = get(parentComponent);
  var parentContext = findCurrentUnmaskedContext(fiber);
  return isContextProvider(fiber) ? processChildContext(fiber, parentContext) : parentContext;
}

var ReactFiberReconciler$1 = function (config) {
  var getPublicInstance = config.getPublicInstance;

  var _ReactFiberScheduler = ReactFiberScheduler(config),
      computeAsyncExpiration = _ReactFiberScheduler.computeAsyncExpiration,
      computeExpirationForFiber = _ReactFiberScheduler.computeExpirationForFiber,
      scheduleWork = _ReactFiberScheduler.scheduleWork,
      batchedUpdates = _ReactFiberScheduler.batchedUpdates,
      unbatchedUpdates = _ReactFiberScheduler.unbatchedUpdates,
      flushSync = _ReactFiberScheduler.flushSync,
      deferredUpdates = _ReactFiberScheduler.deferredUpdates;

  function scheduleTopLevelUpdate(current, element, callback) {
    {
      if (ReactDebugCurrentFiber.phase === 'render' && ReactDebugCurrentFiber.current !== null && !didWarnAboutNestedUpdates) {
        didWarnAboutNestedUpdates = true;
        warning(false, 'Render methods should be a pure function of props and state; ' + 'triggering nested component updates from render is not allowed. ' + 'If necessary, trigger nested updates in componentDidUpdate.\n\n' + 'Check the render method of %s.', getComponentName(ReactDebugCurrentFiber.current) || 'Unknown');
      }
    }

    callback = callback === undefined ? null : callback;
    {
      warning(callback === null || typeof callback === 'function', 'render(...): Expected the last optional `callback` argument to be a ' + 'function. Instead received: %s.', callback);
    }

    var expirationTime = void 0;
    // Check if the top-level element is an async wrapper component. If so,
    // treat updates to the root as async. This is a bit weird but lets us
    // avoid a separate `renderAsync` API.
    if (enableAsyncSubtreeAPI && element != null && element.type != null && element.type.prototype != null && element.type.prototype.unstable_isAsyncReactComponent === true) {
      expirationTime = computeAsyncExpiration();
    } else {
      expirationTime = computeExpirationForFiber(current);
    }

    var update = {
      expirationTime: expirationTime,
      partialState: { element: element },
      callback: callback,
      isReplace: false,
      isForced: false,
      nextCallback: null,
      next: null
    };
    insertUpdateIntoFiber(current, update);
    scheduleWork(current, expirationTime);
  }

  function findHostInstance(fiber) {
    var hostFiber = findCurrentHostFiber(fiber);
    if (hostFiber === null) {
      return null;
    }
    return hostFiber.stateNode;
  }

  return {
    createContainer: function (containerInfo, hydrate) {
      return createFiberRoot(containerInfo, hydrate);
    },
    updateContainer: function (element, container, parentComponent, callback) {
      // TODO: If this is a nested container, this won't be the root.
      var current = container.current;

      {
        if (ReactFiberInstrumentation_1.debugTool) {
          if (current.alternate === null) {
            ReactFiberInstrumentation_1.debugTool.onMountContainer(container);
          } else if (element === null) {
            ReactFiberInstrumentation_1.debugTool.onUnmountContainer(container);
          } else {
            ReactFiberInstrumentation_1.debugTool.onUpdateContainer(container);
          }
        }
      }

      var context = getContextForSubtree(parentComponent);
      if (container.context === null) {
        container.context = context;
      } else {
        container.pendingContext = context;
      }

      scheduleTopLevelUpdate(current, element, callback);
    },


    batchedUpdates: batchedUpdates,

    unbatchedUpdates: unbatchedUpdates,

    deferredUpdates: deferredUpdates,

    flushSync: flushSync,

    getPublicRootInstance: function (container) {
      var containerFiber = container.current;
      if (!containerFiber.child) {
        return null;
      }
      switch (containerFiber.child.tag) {
        case HostComponent:
          return getPublicInstance(containerFiber.child.stateNode);
        default:
          return containerFiber.child.stateNode;
      }
    },


    findHostInstance: findHostInstance,

    findHostInstanceWithNoPortals: function (fiber) {
      var hostFiber = findCurrentHostFiberWithNoPortals(fiber);
      if (hostFiber === null) {
        return null;
      }
      return hostFiber.stateNode;
    },
    injectIntoDevTools: function (devToolsConfig) {
      var findFiberByHostInstance = devToolsConfig.findFiberByHostInstance;

      return injectInternals(_assign({}, devToolsConfig, {
        findHostInstanceByFiber: function (fiber) {
          return findHostInstance(fiber);
        },
        findFiberByHostInstance: function (instance) {
          if (!findFiberByHostInstance) {
            // Might not be implemented by the renderer.
            return null;
          }
          return findFiberByHostInstance(instance);
        }
      }));
    }
  };
};

var ReactFiberReconciler$2 = Object.freeze({
	default: ReactFiberReconciler$1
});

var ReactFiberReconciler$3 = ( ReactFiberReconciler$2 && ReactFiberReconciler$1 ) || ReactFiberReconciler$2;

// TODO: bundle Flow types with the package.



// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var reactReconciler = ReactFiberReconciler$3['default'] ? ReactFiberReconciler$3['default'] : ReactFiberReconciler$3;

function createPortal$1(children, containerInfo,
// TODO: figure out the API for cross-renderer implementation.
implementation) {
  var key = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : null;

  return {
    // This tag allow us to uniquely identify this as a React Portal
    $$typeof: REACT_PORTAL_TYPE,
    key: key == null ? null : '' + key,
    children: children,
    containerInfo: containerInfo,
    implementation: implementation
  };
}

// TODO: this is special because it gets imported during build.

var ReactVersion = '16.2.0';

// a requestAnimationFrame, storing the time for the start of the frame, then
// scheduling a postMessage which gets scheduled after paint. Within the
// postMessage handler do as much work as possible until time + frame rate.
// By separating the idle call into a separate event tick we ensure that
// layout, paint and other browser work is counted against the available time.
// The frame rate is dynamically adjusted.

{
  if (ExecutionEnvironment.canUseDOM && typeof requestAnimationFrame !== 'function') {
    warning(false, 'React depends on requestAnimationFrame. Make sure that you load a ' + 'polyfill in older browsers. http://fb.me/react-polyfills');
  }
}

var hasNativePerformanceNow = typeof performance === 'object' && typeof performance.now === 'function';

var now = void 0;
if (hasNativePerformanceNow) {
  now = function () {
    return performance.now();
  };
} else {
  now = function () {
    return Date.now();
  };
}

// TODO: There's no way to cancel, because Fiber doesn't atm.
var rIC = void 0;
var cIC = void 0;

if (!ExecutionEnvironment.canUseDOM) {
  rIC = function (frameCallback) {
    return setTimeout(function () {
      frameCallback({
        timeRemaining: function () {
          return Infinity;
        }
      });
    });
  };
  cIC = function (timeoutID) {
    clearTimeout(timeoutID);
  };
} else if (typeof requestIdleCallback !== 'function' || typeof cancelIdleCallback !== 'function') {
  // Polyfill requestIdleCallback and cancelIdleCallback

  var scheduledRICCallback = null;
  var isIdleScheduled = false;
  var timeoutTime = -1;

  var isAnimationFrameScheduled = false;

  var frameDeadline = 0;
  // We start out assuming that we run at 30fps but then the heuristic tracking
  // will adjust this value to a faster fps if we get more frequent animation
  // frames.
  var previousFrameTime = 33;
  var activeFrameTime = 33;

  var frameDeadlineObject;
  if (hasNativePerformanceNow) {
    frameDeadlineObject = {
      didTimeout: false,
      timeRemaining: function () {
        // We assume that if we have a performance timer that the rAF callback
        // gets a performance timer value. Not sure if this is always true.
        var remaining = frameDeadline - performance.now();
        return remaining > 0 ? remaining : 0;
      }
    };
  } else {
    frameDeadlineObject = {
      didTimeout: false,
      timeRemaining: function () {
        // Fallback to Date.now()
        var remaining = frameDeadline - Date.now();
        return remaining > 0 ? remaining : 0;
      }
    };
  }

  // We use the postMessage trick to defer idle work until after the repaint.
  var messageKey = '__reactIdleCallback$' + Math.random().toString(36).slice(2);
  var idleTick = function (event) {
    if (event.source !== window || event.data !== messageKey) {
      return;
    }

    isIdleScheduled = false;

    var currentTime = now();
    if (frameDeadline - currentTime <= 0) {
      // There's no time left in this idle period. Check if the callback has
      // a timeout and whether it's been exceeded.
      if (timeoutTime !== -1 && timeoutTime <= currentTime) {
        // Exceeded the timeout. Invoke the callback even though there's no
        // time left.
        frameDeadlineObject.didTimeout = true;
      } else {
        // No timeout.
        if (!isAnimationFrameScheduled) {
          // Schedule another animation callback so we retry later.
          isAnimationFrameScheduled = true;
          requestAnimationFrame(animationTick);
        }
        // Exit without invoking the callback.
        return;
      }
    } else {
      // There's still time left in this idle period.
      frameDeadlineObject.didTimeout = false;
    }

    timeoutTime = -1;
    var callback = scheduledRICCallback;
    scheduledRICCallback = null;
    if (callback !== null) {
      callback(frameDeadlineObject);
    }
  };
  // Assumes that we have addEventListener in this environment. Might need
  // something better for old IE.
  window.addEventListener('message', idleTick, false);

  var animationTick = function (rafTime) {
    isAnimationFrameScheduled = false;
    var nextFrameTime = rafTime - frameDeadline + activeFrameTime;
    if (nextFrameTime < activeFrameTime && previousFrameTime < activeFrameTime) {
      if (nextFrameTime < 8) {
        // Defensive coding. We don't support higher frame rates than 120hz.
        // If we get lower than that, it is probably a bug.
        nextFrameTime = 8;
      }
      // If one frame goes long, then the next one can be short to catch up.
      // If two frames are short in a row, then that's an indication that we
      // actually have a higher frame rate than what we're currently optimizing.
      // We adjust our heuristic dynamically accordingly. For example, if we're
      // running on 120hz display or 90hz VR display.
      // Take the max of the two in case one of them was an anomaly due to
      // missed frame deadlines.
      activeFrameTime = nextFrameTime < previousFrameTime ? previousFrameTime : nextFrameTime;
    } else {
      previousFrameTime = nextFrameTime;
    }
    frameDeadline = rafTime + activeFrameTime;
    if (!isIdleScheduled) {
      isIdleScheduled = true;
      window.postMessage(messageKey, '*');
    }
  };

  rIC = function (callback, options) {
    // This assumes that we only schedule one callback at a time because that's
    // how Fiber uses it.
    scheduledRICCallback = callback;
    if (options != null && typeof options.timeout === 'number') {
      timeoutTime = now() + options.timeout;
    }
    if (!isAnimationFrameScheduled) {
      // If rAF didn't already schedule one, we need to schedule a frame.
      // TODO: If this rAF doesn't materialize because the browser throttles, we
      // might want to still have setTimeout trigger rIC as a backup to ensure
      // that we keep performing work.
      isAnimationFrameScheduled = true;
      requestAnimationFrame(animationTick);
    }
    return 0;
  };

  cIC = function () {
    scheduledRICCallback = null;
    isIdleScheduled = false;
    timeoutTime = -1;
  };
} else {
  rIC = window.requestIdleCallback;
  cIC = window.cancelIdleCallback;
}

/**
 * Forked from fbjs/warning:
 * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js
 *
 * Only change is we use console.warn instead of console.error,
 * and do nothing when 'console' is not supported.
 * This really simplifies the code.
 * ---
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var lowPriorityWarning = function () {};

{
  var printWarning = function (format) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    var argIndex = 0;
    var message = 'Warning: ' + format.replace(/%s/g, function () {
      return args[argIndex++];
    });
    if (typeof console !== 'undefined') {
      console.warn(message);
    }
    try {
      // --- Welcome to debugging React ---
      // This error was thrown as a convenience so that you can use this stack
      // to find the callsite that caused this warning to fire.
      throw new Error(message);
    } catch (x) {}
  };

  lowPriorityWarning = function (condition, format) {
    if (format === undefined) {
      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');
    }
    if (!condition) {
      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {
        args[_key2 - 2] = arguments[_key2];
      }

      printWarning.apply(undefined, [format].concat(args));
    }
  };
}

var lowPriorityWarning$1 = lowPriorityWarning;

// isAttributeNameSafe() is currently duplicated in DOMMarkupOperations.
// TODO: Find a better place for this.
var VALID_ATTRIBUTE_NAME_REGEX = new RegExp('^[' + ATTRIBUTE_NAME_START_CHAR + '][' + ATTRIBUTE_NAME_CHAR + ']*$');
var illegalAttributeNameCache = {};
var validatedAttributeNameCache = {};
function isAttributeNameSafe(attributeName) {
  if (validatedAttributeNameCache.hasOwnProperty(attributeName)) {
    return true;
  }
  if (illegalAttributeNameCache.hasOwnProperty(attributeName)) {
    return false;
  }
  if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName)) {
    validatedAttributeNameCache[attributeName] = true;
    return true;
  }
  illegalAttributeNameCache[attributeName] = true;
  {
    warning(false, 'Invalid attribute name: `%s`', attributeName);
  }
  return false;
}

// shouldIgnoreValue() is currently duplicated in DOMMarkupOperations.
// TODO: Find a better place for this.
function shouldIgnoreValue(propertyInfo, value) {
  return value == null || propertyInfo.hasBooleanValue && !value || propertyInfo.hasNumericValue && isNaN(value) || propertyInfo.hasPositiveNumericValue && value < 1 || propertyInfo.hasOverloadedBooleanValue && value === false;
}

/**
 * Operations for dealing with DOM properties.
 */





/**
 * Get the value for a property on a node. Only used in DEV for SSR validation.
 * The "expected" argument is used as a hint of what the expected value is.
 * Some properties have multiple equivalent values.
 */
function getValueForProperty(node, name, expected) {
  {
    var propertyInfo = getPropertyInfo(name);
    if (propertyInfo) {
      var mutationMethod = propertyInfo.mutationMethod;
      if (mutationMethod || propertyInfo.mustUseProperty) {
        return node[propertyInfo.propertyName];
      } else {
        var attributeName = propertyInfo.attributeName;

        var stringValue = null;

        if (propertyInfo.hasOverloadedBooleanValue) {
          if (node.hasAttribute(attributeName)) {
            var value = node.getAttribute(attributeName);
            if (value === '') {
              return true;
            }
            if (shouldIgnoreValue(propertyInfo, expected)) {
              return value;
            }
            if (value === '' + expected) {
              return expected;
            }
            return value;
          }
        } else if (node.hasAttribute(attributeName)) {
          if (shouldIgnoreValue(propertyInfo, expected)) {
            // We had an attribute but shouldn't have had one, so read it
            // for the error message.
            return node.getAttribute(attributeName);
          }
          if (propertyInfo.hasBooleanValue) {
            // If this was a boolean, it doesn't matter what the value is
            // the fact that we have it is the same as the expected.
            return expected;
          }
          // Even if this property uses a namespace we use getAttribute
          // because we assume its namespaced name is the same as our config.
          // To use getAttributeNS we need the local name which we don't have
          // in our config atm.
          stringValue = node.getAttribute(attributeName);
        }

        if (shouldIgnoreValue(propertyInfo, expected)) {
          return stringValue === null ? expected : stringValue;
        } else if (stringValue === '' + expected) {
          return expected;
        } else {
          return stringValue;
        }
      }
    }
  }
}

/**
 * Get the value for a attribute on a node. Only used in DEV for SSR validation.
 * The third argument is used as a hint of what the expected value is. Some
 * attributes have multiple equivalent values.
 */
function getValueForAttribute(node, name, expected) {
  {
    if (!isAttributeNameSafe(name)) {
      return;
    }
    if (!node.hasAttribute(name)) {
      return expected === undefined ? undefined : null;
    }
    var value = node.getAttribute(name);
    if (value === '' + expected) {
      return expected;
    }
    return value;
  }
}

/**
 * Sets the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 * @param {*} value
 */
function setValueForProperty(node, name, value) {
  var propertyInfo = getPropertyInfo(name);

  if (propertyInfo && shouldSetAttribute(name, value)) {
    var mutationMethod = propertyInfo.mutationMethod;
    if (mutationMethod) {
      mutationMethod(node, value);
    } else if (shouldIgnoreValue(propertyInfo, value)) {
      deleteValueForProperty(node, name);
      return;
    } else if (propertyInfo.mustUseProperty) {
      // Contrary to `setAttribute`, object properties are properly
      // `toString`ed by IE8/9.
      node[propertyInfo.propertyName] = value;
    } else {
      var attributeName = propertyInfo.attributeName;
      var namespace = propertyInfo.attributeNamespace;
      // `setAttribute` with objects becomes only `[object]` in IE8/9,
      // ('' + value) makes it output the correct toString()-value.
      if (namespace) {
        node.setAttributeNS(namespace, attributeName, '' + value);
      } else if (propertyInfo.hasBooleanValue || propertyInfo.hasOverloadedBooleanValue && value === true) {
        node.setAttribute(attributeName, '');
      } else {
        node.setAttribute(attributeName, '' + value);
      }
    }
  } else {
    setValueForAttribute(node, name, shouldSetAttribute(name, value) ? value : null);
    return;
  }

  {
    
  }
}

function setValueForAttribute(node, name, value) {
  if (!isAttributeNameSafe(name)) {
    return;
  }
  if (value == null) {
    node.removeAttribute(name);
  } else {
    node.setAttribute(name, '' + value);
  }

  {
    
  }
}

/**
 * Deletes an attributes from a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 */
function deleteValueForAttribute(node, name) {
  node.removeAttribute(name);
}

/**
 * Deletes the value for a property on a node.
 *
 * @param {DOMElement} node
 * @param {string} name
 */
function deleteValueForProperty(node, name) {
  var propertyInfo = getPropertyInfo(name);
  if (propertyInfo) {
    var mutationMethod = propertyInfo.mutationMethod;
    if (mutationMethod) {
      mutationMethod(node, undefined);
    } else if (propertyInfo.mustUseProperty) {
      var propName = propertyInfo.propertyName;
      if (propertyInfo.hasBooleanValue) {
        node[propName] = false;
      } else {
        node[propName] = '';
      }
    } else {
      node.removeAttribute(propertyInfo.attributeName);
    }
  } else {
    node.removeAttribute(name);
  }
}

var ReactControlledValuePropTypes = {
  checkPropTypes: null
};

{
  var hasReadOnlyValue = {
    button: true,
    checkbox: true,
    image: true,
    hidden: true,
    radio: true,
    reset: true,
    submit: true
  };

  var propTypes = {
    value: function (props, propName, componentName) {
      if (!props[propName] || hasReadOnlyValue[props.type] || props.onChange || props.readOnly || props.disabled) {
        return null;
      }
      return new Error('You provided a `value` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultValue`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    },
    checked: function (props, propName, componentName) {
      if (!props[propName] || props.onChange || props.readOnly || props.disabled) {
        return null;
      }
      return new Error('You provided a `checked` prop to a form field without an ' + '`onChange` handler. This will render a read-only field. If ' + 'the field should be mutable use `defaultChecked`. Otherwise, ' + 'set either `onChange` or `readOnly`.');
    }
  };

  /**
   * Provide a linked `value` attribute for controlled forms. You should not use
   * this outside of the ReactDOM controlled form components.
   */
  ReactControlledValuePropTypes.checkPropTypes = function (tagName, props, getStack) {
    checkPropTypes(propTypes, props, 'prop', tagName, getStack);
  };
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$2 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$3 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnValueDefaultValue = false;
var didWarnCheckedDefaultChecked = false;
var didWarnControlledToUncontrolled = false;
var didWarnUncontrolledToControlled = false;

function isControlled(props) {
  var usesChecked = props.type === 'checkbox' || props.type === 'radio';
  return usesChecked ? props.checked != null : props.value != null;
}

/**
 * Implements an <input> host component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * See http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */

function getHostProps(element, props) {
  var node = element;
  var value = props.value;
  var checked = props.checked;

  var hostProps = _assign({
    // Make sure we set .type before any other properties (setting .value
    // before .type means .value is lost in IE11 and below)
    type: undefined,
    // Make sure we set .step before .value (setting .value before .step
    // means .value is rounded on mount, based upon step precision)
    step: undefined,
    // Make sure we set .min & .max before .value (to ensure proper order
    // in corner cases such as min or max deriving from value, e.g. Issue #7170)
    min: undefined,
    max: undefined
  }, props, {
    defaultChecked: undefined,
    defaultValue: undefined,
    value: value != null ? value : node._wrapperState.initialValue,
    checked: checked != null ? checked : node._wrapperState.initialChecked
  });

  return hostProps;
}

function initWrapperState(element, props) {
  {
    ReactControlledValuePropTypes.checkPropTypes('input', props, getCurrentFiberStackAddendum$3);

    if (props.checked !== undefined && props.defaultChecked !== undefined && !didWarnCheckedDefaultChecked) {
      warning(false, '%s contains an input of type %s with both checked and defaultChecked props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the checked prop, or the defaultChecked prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerName$2() || 'A component', props.type);
      didWarnCheckedDefaultChecked = true;
    }
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue) {
      warning(false, '%s contains an input of type %s with both value and defaultValue props. ' + 'Input elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled input ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components', getCurrentFiberOwnerName$2() || 'A component', props.type);
      didWarnValueDefaultValue = true;
    }
  }

  var defaultValue = props.defaultValue;
  var node = element;
  node._wrapperState = {
    initialChecked: props.checked != null ? props.checked : props.defaultChecked,
    initialValue: props.value != null ? props.value : defaultValue,
    controlled: isControlled(props)
  };
}

function updateChecked(element, props) {
  var node = element;
  var checked = props.checked;
  if (checked != null) {
    setValueForProperty(node, 'checked', checked);
  }
}

function updateWrapper(element, props) {
  var node = element;
  {
    var controlled = isControlled(props);

    if (!node._wrapperState.controlled && controlled && !didWarnUncontrolledToControlled) {
      warning(false, 'A component is changing an uncontrolled input of type %s to be controlled. ' + 'Input elements should not switch from uncontrolled to controlled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components%s', props.type, getCurrentFiberStackAddendum$3());
      didWarnUncontrolledToControlled = true;
    }
    if (node._wrapperState.controlled && !controlled && !didWarnControlledToUncontrolled) {
      warning(false, 'A component is changing a controlled input of type %s to be uncontrolled. ' + 'Input elements should not switch from controlled to uncontrolled (or vice versa). ' + 'Decide between using a controlled or uncontrolled input ' + 'element for the lifetime of the component. More info: https://fb.me/react-controlled-components%s', props.type, getCurrentFiberStackAddendum$3());
      didWarnControlledToUncontrolled = true;
    }
  }

  updateChecked(element, props);

  var value = props.value;
  if (value != null) {
    if (value === 0 && node.value === '') {
      node.value = '0';
      // Note: IE9 reports a number inputs as 'text', so check props instead.
    } else if (props.type === 'number') {
      // Simulate `input.valueAsNumber`. IE9 does not support it
      var valueAsNumber = parseFloat(node.value) || 0;

      if (
      // eslint-disable-next-line
      value != valueAsNumber ||
      // eslint-disable-next-line
      value == valueAsNumber && node.value != value) {
        // Cast `value` to a string to ensure the value is set correctly. While
        // browsers typically do this as necessary, jsdom doesn't.
        node.value = '' + value;
      }
    } else if (node.value !== '' + value) {
      // Cast `value` to a string to ensure the value is set correctly. While
      // browsers typically do this as necessary, jsdom doesn't.
      node.value = '' + value;
    }
  } else {
    if (props.value == null && props.defaultValue != null) {
      // In Chrome, assigning defaultValue to certain input types triggers input validation.
      // For number inputs, the display value loses trailing decimal points. For email inputs,
      // Chrome raises "The specified value <x> is not a valid email address".
      //
      // Here we check to see if the defaultValue has actually changed, avoiding these problems
      // when the user is inputting text
      //
      // https://github.com/facebook/react/issues/7253
      if (node.defaultValue !== '' + props.defaultValue) {
        node.defaultValue = '' + props.defaultValue;
      }
    }
    if (props.checked == null && props.defaultChecked != null) {
      node.defaultChecked = !!props.defaultChecked;
    }
  }
}

function postMountWrapper(element, props) {
  var node = element;

  // Detach value from defaultValue. We won't do anything if we're working on
  // submit or reset inputs as those values & defaultValues are linked. They
  // are not resetable nodes so this operation doesn't matter and actually
  // removes browser-default values (eg "Submit Query") when no value is
  // provided.

  switch (props.type) {
    case 'submit':
    case 'reset':
      break;
    case 'color':
    case 'date':
    case 'datetime':
    case 'datetime-local':
    case 'month':
    case 'time':
    case 'week':
      // This fixes the no-show issue on iOS Safari and Android Chrome:
      // https://github.com/facebook/react/issues/7233
      node.value = '';
      node.value = node.defaultValue;
      break;
    default:
      node.value = node.value;
      break;
  }

  // Normally, we'd just do `node.checked = node.checked` upon initial mount, less this bug
  // this is needed to work around a chrome bug where setting defaultChecked
  // will sometimes influence the value of checked (even after detachment).
  // Reference: https://bugs.chromium.org/p/chromium/issues/detail?id=608416
  // We need to temporarily unset name to avoid disrupting radio button groups.
  var name = node.name;
  if (name !== '') {
    node.name = '';
  }
  node.defaultChecked = !node.defaultChecked;
  node.defaultChecked = !node.defaultChecked;
  if (name !== '') {
    node.name = name;
  }
}

function restoreControlledState$1(element, props) {
  var node = element;
  updateWrapper(node, props);
  updateNamedCousins(node, props);
}

function updateNamedCousins(rootNode, props) {
  var name = props.name;
  if (props.type === 'radio' && name != null) {
    var queryRoot = rootNode;

    while (queryRoot.parentNode) {
      queryRoot = queryRoot.parentNode;
    }

    // If `rootNode.form` was non-null, then we could try `form.elements`,
    // but that sometimes behaves strangely in IE8. We could also try using
    // `form.getElementsByName`, but that will only return direct children
    // and won't include inputs that use the HTML5 `form=` attribute. Since
    // the input might not even be in a form. It might not even be in the
    // document. Let's just use the local `querySelectorAll` to ensure we don't
    // miss anything.
    var group = queryRoot.querySelectorAll('input[name=' + JSON.stringify('' + name) + '][type="radio"]');

    for (var i = 0; i < group.length; i++) {
      var otherNode = group[i];
      if (otherNode === rootNode || otherNode.form !== rootNode.form) {
        continue;
      }
      // This will throw if radio buttons rendered by different copies of React
      // and the same name are rendered into the same form (same as #1939).
      // That's probably okay; we don't support it just as we don't support
      // mixing React radio buttons with non-React ones.
      var otherProps = getFiberCurrentPropsFromNode$1(otherNode);
      !otherProps ? invariant(false, 'ReactDOMInput: Mixing React and non-React radio inputs with the same `name` is not supported.') : void 0;

      // We need update the tracked value on the named cousin since the value
      // was changed but the input saw no event or value set
      updateValueIfChanged(otherNode);

      // If this is a controlled radio button group, forcing the input that
      // was previously checked to update will cause it to be come re-checked
      // as appropriate.
      updateWrapper(otherNode, otherProps);
    }
  }
}

function flattenChildren(children) {
  var content = '';

  // Flatten children and warn if they aren't strings or numbers;
  // invalid types are ignored.
  // We can silently skip them because invalid DOM nesting warning
  // catches these cases in Fiber.
  React.Children.forEach(children, function (child) {
    if (child == null) {
      return;
    }
    if (typeof child === 'string' || typeof child === 'number') {
      content += child;
    }
  });

  return content;
}

/**
 * Implements an <option> host component that warns when `selected` is set.
 */

function validateProps(element, props) {
  // TODO (yungsters): Remove support for `selected` in <option>.
  {
    warning(props.selected == null, 'Use the `defaultValue` or `value` props on <select> instead of ' + 'setting `selected` on <option>.');
  }
}

function postMountWrapper$1(element, props) {
  // value="" should make a value attribute (#6219)
  if (props.value != null) {
    element.setAttribute('value', props.value);
  }
}

function getHostProps$1(element, props) {
  var hostProps = _assign({ children: undefined }, props);
  var content = flattenChildren(props.children);

  if (content) {
    hostProps.children = content;
  }

  return hostProps;
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$3 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$4 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;


{
  var didWarnValueDefaultValue$1 = false;
}

function getDeclarationErrorAddendum() {
  var ownerName = getCurrentFiberOwnerName$3();
  if (ownerName) {
    return '\n\nCheck the render method of `' + ownerName + '`.';
  }
  return '';
}

var valuePropNames = ['value', 'defaultValue'];

/**
 * Validation function for `value` and `defaultValue`.
 */
function checkSelectPropTypes(props) {
  ReactControlledValuePropTypes.checkPropTypes('select', props, getCurrentFiberStackAddendum$4);

  for (var i = 0; i < valuePropNames.length; i++) {
    var propName = valuePropNames[i];
    if (props[propName] == null) {
      continue;
    }
    var isArray = Array.isArray(props[propName]);
    if (props.multiple && !isArray) {
      warning(false, 'The `%s` prop supplied to <select> must be an array if ' + '`multiple` is true.%s', propName, getDeclarationErrorAddendum());
    } else if (!props.multiple && isArray) {
      warning(false, 'The `%s` prop supplied to <select> must be a scalar ' + 'value if `multiple` is false.%s', propName, getDeclarationErrorAddendum());
    }
  }
}

function updateOptions(node, multiple, propValue, setDefaultSelected) {
  var options = node.options;

  if (multiple) {
    var selectedValues = propValue;
    var selectedValue = {};
    for (var i = 0; i < selectedValues.length; i++) {
      // Prefix to avoid chaos with special keys.
      selectedValue['$' + selectedValues[i]] = true;
    }
    for (var _i = 0; _i < options.length; _i++) {
      var selected = selectedValue.hasOwnProperty('$' + options[_i].value);
      if (options[_i].selected !== selected) {
        options[_i].selected = selected;
      }
      if (selected && setDefaultSelected) {
        options[_i].defaultSelected = true;
      }
    }
  } else {
    // Do not set `select.value` as exact behavior isn't consistent across all
    // browsers for all cases.
    var _selectedValue = '' + propValue;
    var defaultSelected = null;
    for (var _i2 = 0; _i2 < options.length; _i2++) {
      if (options[_i2].value === _selectedValue) {
        options[_i2].selected = true;
        if (setDefaultSelected) {
          options[_i2].defaultSelected = true;
        }
        return;
      }
      if (defaultSelected === null && !options[_i2].disabled) {
        defaultSelected = options[_i2];
      }
    }
    if (defaultSelected !== null) {
      defaultSelected.selected = true;
    }
  }
}

/**
 * Implements a <select> host component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * stringable. If `multiple` is true, the prop must be an array of stringables.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */

function getHostProps$2(element, props) {
  return _assign({}, props, {
    value: undefined
  });
}

function initWrapperState$1(element, props) {
  var node = element;
  {
    checkSelectPropTypes(props);
  }

  var value = props.value;
  node._wrapperState = {
    initialValue: value != null ? value : props.defaultValue,
    wasMultiple: !!props.multiple
  };

  {
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValueDefaultValue$1) {
      warning(false, 'Select elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled select ' + 'element and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
      didWarnValueDefaultValue$1 = true;
    }
  }
}

function postMountWrapper$2(element, props) {
  var node = element;
  node.multiple = !!props.multiple;
  var value = props.value;
  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (props.defaultValue != null) {
    updateOptions(node, !!props.multiple, props.defaultValue, true);
  }
}

function postUpdateWrapper(element, props) {
  var node = element;
  // After the initial mount, we control selected-ness manually so don't pass
  // this value down
  node._wrapperState.initialValue = undefined;

  var wasMultiple = node._wrapperState.wasMultiple;
  node._wrapperState.wasMultiple = !!props.multiple;

  var value = props.value;
  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  } else if (wasMultiple !== !!props.multiple) {
    // For simplicity, reapply `defaultValue` if `multiple` is toggled.
    if (props.defaultValue != null) {
      updateOptions(node, !!props.multiple, props.defaultValue, true);
    } else {
      // Revert the select back to its default unselected state.
      updateOptions(node, !!props.multiple, props.multiple ? [] : '', false);
    }
  }
}

function restoreControlledState$2(element, props) {
  var node = element;
  var value = props.value;

  if (value != null) {
    updateOptions(node, !!props.multiple, value, false);
  }
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberStackAddendum$5 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnValDefaultVal = false;

/**
 * Implements a <textarea> host component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */

function getHostProps$3(element, props) {
  var node = element;
  !(props.dangerouslySetInnerHTML == null) ? invariant(false, '`dangerouslySetInnerHTML` does not make sense on <textarea>.') : void 0;

  // Always set children to the same thing. In IE9, the selection range will
  // get reset if `textContent` is mutated.  We could add a check in setTextContent
  // to only set the value if/when the value differs from the node value (which would
  // completely solve this IE9 bug), but Sebastian+Sophie seemed to like this
  // solution. The value can be a boolean or object so that's why it's forced
  // to be a string.
  var hostProps = _assign({}, props, {
    value: undefined,
    defaultValue: undefined,
    children: '' + node._wrapperState.initialValue
  });

  return hostProps;
}

function initWrapperState$2(element, props) {
  var node = element;
  {
    ReactControlledValuePropTypes.checkPropTypes('textarea', props, getCurrentFiberStackAddendum$5);
    if (props.value !== undefined && props.defaultValue !== undefined && !didWarnValDefaultVal) {
      warning(false, 'Textarea elements must be either controlled or uncontrolled ' + '(specify either the value prop, or the defaultValue prop, but not ' + 'both). Decide between using a controlled or uncontrolled textarea ' + 'and remove one of these props. More info: ' + 'https://fb.me/react-controlled-components');
      didWarnValDefaultVal = true;
    }
  }

  var initialValue = props.value;

  // Only bother fetching default value if we're going to use it
  if (initialValue == null) {
    var defaultValue = props.defaultValue;
    // TODO (yungsters): Remove support for children content in <textarea>.
    var children = props.children;
    if (children != null) {
      {
        warning(false, 'Use the `defaultValue` or `value` props instead of setting ' + 'children on <textarea>.');
      }
      !(defaultValue == null) ? invariant(false, 'If you supply `defaultValue` on a <textarea>, do not pass children.') : void 0;
      if (Array.isArray(children)) {
        !(children.length <= 1) ? invariant(false, '<textarea> can only have at most one child.') : void 0;
        children = children[0];
      }

      defaultValue = '' + children;
    }
    if (defaultValue == null) {
      defaultValue = '';
    }
    initialValue = defaultValue;
  }

  node._wrapperState = {
    initialValue: '' + initialValue
  };
}

function updateWrapper$1(element, props) {
  var node = element;
  var value = props.value;
  if (value != null) {
    // Cast `value` to a string to ensure the value is set correctly. While
    // browsers typically do this as necessary, jsdom doesn't.
    var newValue = '' + value;

    // To avoid side effects (such as losing text selection), only set value if changed
    if (newValue !== node.value) {
      node.value = newValue;
    }
    if (props.defaultValue == null) {
      node.defaultValue = newValue;
    }
  }
  if (props.defaultValue != null) {
    node.defaultValue = props.defaultValue;
  }
}

function postMountWrapper$3(element, props) {
  var node = element;
  // This is in postMount because we need access to the DOM node, which is not
  // available until after the component has mounted.
  var textContent = node.textContent;

  // Only set node.value if textContent is equal to the expected
  // initial value. In IE10/IE11 there is a bug where the placeholder attribute
  // will populate textContent as well.
  // https://developer.microsoft.com/microsoft-edge/platform/issues/101525/
  if (textContent === node._wrapperState.initialValue) {
    node.value = textContent;
  }
}

function restoreControlledState$3(element, props) {
  // DOM component is still mounted; update
  updateWrapper$1(element, props);
}

var HTML_NAMESPACE$1 = 'http://www.w3.org/1999/xhtml';
var MATH_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
var SVG_NAMESPACE = 'http://www.w3.org/2000/svg';

var Namespaces = {
  html: HTML_NAMESPACE$1,
  mathml: MATH_NAMESPACE,
  svg: SVG_NAMESPACE
};

// Assumes there is no parent namespace.
function getIntrinsicNamespace(type) {
  switch (type) {
    case 'svg':
      return SVG_NAMESPACE;
    case 'math':
      return MATH_NAMESPACE;
    default:
      return HTML_NAMESPACE$1;
  }
}

function getChildNamespace(parentNamespace, type) {
  if (parentNamespace == null || parentNamespace === HTML_NAMESPACE$1) {
    // No (or default) parent namespace: potential entry point.
    return getIntrinsicNamespace(type);
  }
  if (parentNamespace === SVG_NAMESPACE && type === 'foreignObject') {
    // We're leaving SVG.
    return HTML_NAMESPACE$1;
  }
  // By default, pass namespace below.
  return parentNamespace;
}

/* globals MSApp */

/**
 * Create a function which has 'unsafe' privileges (required by windows8 apps)
 */
var createMicrosoftUnsafeLocalFunction = function (func) {
  if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
    return function (arg0, arg1, arg2, arg3) {
      MSApp.execUnsafeLocalFunction(function () {
        return func(arg0, arg1, arg2, arg3);
      });
    };
  } else {
    return func;
  }
};

// SVG temp container for IE lacking innerHTML
var reusableSVGContainer = void 0;

/**
 * Set the innerHTML property of a node
 *
 * @param {DOMElement} node
 * @param {string} html
 * @internal
 */
var setInnerHTML = createMicrosoftUnsafeLocalFunction(function (node, html) {
  // IE does not have innerHTML for SVG nodes, so instead we inject the
  // new markup in a temp node and then move the child nodes across into
  // the target node

  if (node.namespaceURI === Namespaces.svg && !('innerHTML' in node)) {
    reusableSVGContainer = reusableSVGContainer || document.createElement('div');
    reusableSVGContainer.innerHTML = '<svg>' + html + '</svg>';
    var svgNode = reusableSVGContainer.firstChild;
    while (node.firstChild) {
      node.removeChild(node.firstChild);
    }
    while (svgNode.firstChild) {
      node.appendChild(svgNode.firstChild);
    }
  } else {
    node.innerHTML = html;
  }
});

/**
 * Set the textContent property of a node, ensuring that whitespace is preserved
 * even in IE8. innerText is a poor substitute for textContent and, among many
 * issues, inserts <br> instead of the literal newline chars. innerHTML behaves
 * as it should.
 *
 * @param {DOMElement} node
 * @param {string} text
 * @internal
 */
var setTextContent = function (node, text) {
  if (text) {
    var firstChild = node.firstChild;

    if (firstChild && firstChild === node.lastChild && firstChild.nodeType === TEXT_NODE) {
      firstChild.nodeValue = text;
      return;
    }
  }
  node.textContent = text;
};

/**
 * CSS properties which accept numbers but are not in units of "px".
 */
var isUnitlessNumber = {
  animationIterationCount: true,
  borderImageOutset: true,
  borderImageSlice: true,
  borderImageWidth: true,
  boxFlex: true,
  boxFlexGroup: true,
  boxOrdinalGroup: true,
  columnCount: true,
  columns: true,
  flex: true,
  flexGrow: true,
  flexPositive: true,
  flexShrink: true,
  flexNegative: true,
  flexOrder: true,
  gridRow: true,
  gridRowEnd: true,
  gridRowSpan: true,
  gridRowStart: true,
  gridColumn: true,
  gridColumnEnd: true,
  gridColumnSpan: true,
  gridColumnStart: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  tabSize: true,
  widows: true,
  zIndex: true,
  zoom: true,

  // SVG-related properties
  fillOpacity: true,
  floodOpacity: true,
  stopOpacity: true,
  strokeDasharray: true,
  strokeDashoffset: true,
  strokeMiterlimit: true,
  strokeOpacity: true,
  strokeWidth: true
};

/**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */
function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}

/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */
var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.
Object.keys(isUnitlessNumber).forEach(function (prop) {
  prefixes.forEach(function (prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});

/**
 * Convert a value into the proper css writable value. The style name `name`
 * should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} name CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */
function dangerousStyleValue(name, value, isCustomProperty) {
  // Note that we've removed escapeTextForBrowser() calls here since the
  // whole string will be escaped when the attribute is injected into
  // the markup. If you provide unsafe user data here they can inject
  // arbitrary CSS which may be problematic (I couldn't repro this):
  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
  // This is not an XSS hole but instead a potential CSS injection issue
  // which has lead to a greater discussion about how we're going to
  // trust URLs moving forward. See #2115901

  var isEmpty = value == null || typeof value === 'boolean' || value === '';
  if (isEmpty) {
    return '';
  }

  if (!isCustomProperty && typeof value === 'number' && value !== 0 && !(isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name])) {
    return value + 'px'; // Presumes implicit 'px' suffix for unitless numbers
  }

  return ('' + value).trim();
}

var warnValidStyle = emptyFunction;

{
  // 'msTransform' is correct, but the other prefixes should be capitalized
  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;

  // style values shouldn't contain a semicolon
  var badStyleValueWithSemicolonPattern = /;\s*$/;

  var warnedStyleNames = {};
  var warnedStyleValues = {};
  var warnedForNaNValue = false;
  var warnedForInfinityValue = false;

  var warnHyphenatedStyleName = function (name, getStack) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    warning(false, 'Unsupported style property %s. Did you mean %s?%s', name, camelizeStyleName(name), getStack());
  };

  var warnBadVendoredStyleName = function (name, getStack) {
    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
      return;
    }

    warnedStyleNames[name] = true;
    warning(false, 'Unsupported vendor-prefixed style property %s. Did you mean %s?%s', name, name.charAt(0).toUpperCase() + name.slice(1), getStack());
  };

  var warnStyleValueWithSemicolon = function (name, value, getStack) {
    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
      return;
    }

    warnedStyleValues[value] = true;
    warning(false, "Style property values shouldn't contain a semicolon. " + 'Try "%s: %s" instead.%s', name, value.replace(badStyleValueWithSemicolonPattern, ''), getStack());
  };

  var warnStyleValueIsNaN = function (name, value, getStack) {
    if (warnedForNaNValue) {
      return;
    }

    warnedForNaNValue = true;
    warning(false, '`NaN` is an invalid value for the `%s` css style property.%s', name, getStack());
  };

  var warnStyleValueIsInfinity = function (name, value, getStack) {
    if (warnedForInfinityValue) {
      return;
    }

    warnedForInfinityValue = true;
    warning(false, '`Infinity` is an invalid value for the `%s` css style property.%s', name, getStack());
  };

  warnValidStyle = function (name, value, getStack) {
    if (name.indexOf('-') > -1) {
      warnHyphenatedStyleName(name, getStack);
    } else if (badVendoredStyleNamePattern.test(name)) {
      warnBadVendoredStyleName(name, getStack);
    } else if (badStyleValueWithSemicolonPattern.test(value)) {
      warnStyleValueWithSemicolon(name, value, getStack);
    }

    if (typeof value === 'number') {
      if (isNaN(value)) {
        warnStyleValueIsNaN(name, value, getStack);
      } else if (!isFinite(value)) {
        warnStyleValueIsInfinity(name, value, getStack);
      }
    }
  };
}

var warnValidStyle$1 = warnValidStyle;

/**
 * Operations for dealing with CSS properties.
 */

/**
 * This creates a string that is expected to be equivalent to the style
 * attribute generated by server-side rendering. It by-passes warnings and
 * security checks so it's not safe to use this value for anything other than
 * comparison. It is only used in DEV for SSR validation.
 */
function createDangerousStringForStyles(styles) {
  {
    var serialized = '';
    var delimiter = '';
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      var styleValue = styles[styleName];
      if (styleValue != null) {
        var isCustomProperty = styleName.indexOf('--') === 0;
        serialized += delimiter + hyphenateStyleName(styleName) + ':';
        serialized += dangerousStyleValue(styleName, styleValue, isCustomProperty);

        delimiter = ';';
      }
    }
    return serialized || null;
  }
}

/**
 * Sets the value for multiple styles on a node.  If a value is specified as
 * '' (empty string), the corresponding style property will be unset.
 *
 * @param {DOMElement} node
 * @param {object} styles
 */
function setValueForStyles(node, styles, getStack) {
  var style = node.style;
  for (var styleName in styles) {
    if (!styles.hasOwnProperty(styleName)) {
      continue;
    }
    var isCustomProperty = styleName.indexOf('--') === 0;
    {
      if (!isCustomProperty) {
        warnValidStyle$1(styleName, styles[styleName], getStack);
      }
    }
    var styleValue = dangerousStyleValue(styleName, styles[styleName], isCustomProperty);
    if (styleName === 'float') {
      styleName = 'cssFloat';
    }
    if (isCustomProperty) {
      style.setProperty(styleName, styleValue);
    } else {
      style[styleName] = styleValue;
    }
  }
}

// For HTML, certain tags should omit their close tag. We keep a whitelist for
// those special-case tags.

var omittedCloseTags = {
  area: true,
  base: true,
  br: true,
  col: true,
  embed: true,
  hr: true,
  img: true,
  input: true,
  keygen: true,
  link: true,
  meta: true,
  param: true,
  source: true,
  track: true,
  wbr: true
};

// For HTML, certain tags cannot have children. This has the same purpose as
// `omittedCloseTags` except that `menuitem` should still have its closing tag.

var voidElementTags = _assign({
  menuitem: true
}, omittedCloseTags);

var HTML$1 = '__html';

function assertValidProps(tag, props, getStack) {
  if (!props) {
    return;
  }
  // Note the use of `==` which checks for null or undefined.
  if (voidElementTags[tag]) {
    !(props.children == null && props.dangerouslySetInnerHTML == null) ? invariant(false, '%s is a void element tag and must neither have `children` nor use `dangerouslySetInnerHTML`.%s', tag, getStack()) : void 0;
  }
  if (props.dangerouslySetInnerHTML != null) {
    !(props.children == null) ? invariant(false, 'Can only set one of `children` or `props.dangerouslySetInnerHTML`.') : void 0;
    !(typeof props.dangerouslySetInnerHTML === 'object' && HTML$1 in props.dangerouslySetInnerHTML) ? invariant(false, '`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. Please visit https://fb.me/react-invariant-dangerously-set-inner-html for more information.') : void 0;
  }
  {
    warning(props.suppressContentEditableWarning || !props.contentEditable || props.children == null, 'A component is `contentEditable` and contains `children` managed by ' + 'React. It is now your responsibility to guarantee that none of ' + 'those nodes are unexpectedly modified or duplicated. This is ' + 'probably not intentional.%s', getStack());
  }
  !(props.style == null || typeof props.style === 'object') ? invariant(false, 'The `style` prop expects a mapping from style properties to values, not a string. For example, style={{marginRight: spacing + \'em\'}} when using JSX.%s', getStack()) : void 0;
}

function isCustomComponent(tagName, props) {
  if (tagName.indexOf('-') === -1) {
    return typeof props.is === 'string';
  }
  switch (tagName) {
    // These are reserved SVG and MathML elements.
    // We don't mind this whitelist too much because we expect it to never grow.
    // The alternative is to track the namespace in a few places which is convoluted.
    // https://w3c.github.io/webcomponents/spec/custom/#custom-elements-core-concepts
    case 'annotation-xml':
    case 'color-profile':
    case 'font-face':
    case 'font-face-src':
    case 'font-face-uri':
    case 'font-face-format':
    case 'font-face-name':
    case 'missing-glyph':
      return false;
    default:
      return true;
  }
}

var ariaProperties = {
  'aria-current': 0, // state
  'aria-details': 0,
  'aria-disabled': 0, // state
  'aria-hidden': 0, // state
  'aria-invalid': 0, // state
  'aria-keyshortcuts': 0,
  'aria-label': 0,
  'aria-roledescription': 0,
  // Widget Attributes
  'aria-autocomplete': 0,
  'aria-checked': 0,
  'aria-expanded': 0,
  'aria-haspopup': 0,
  'aria-level': 0,
  'aria-modal': 0,
  'aria-multiline': 0,
  'aria-multiselectable': 0,
  'aria-orientation': 0,
  'aria-placeholder': 0,
  'aria-pressed': 0,
  'aria-readonly': 0,
  'aria-required': 0,
  'aria-selected': 0,
  'aria-sort': 0,
  'aria-valuemax': 0,
  'aria-valuemin': 0,
  'aria-valuenow': 0,
  'aria-valuetext': 0,
  // Live Region Attributes
  'aria-atomic': 0,
  'aria-busy': 0,
  'aria-live': 0,
  'aria-relevant': 0,
  // Drag-and-Drop Attributes
  'aria-dropeffect': 0,
  'aria-grabbed': 0,
  // Relationship Attributes
  'aria-activedescendant': 0,
  'aria-colcount': 0,
  'aria-colindex': 0,
  'aria-colspan': 0,
  'aria-controls': 0,
  'aria-describedby': 0,
  'aria-errormessage': 0,
  'aria-flowto': 0,
  'aria-labelledby': 0,
  'aria-owns': 0,
  'aria-posinset': 0,
  'aria-rowcount': 0,
  'aria-rowindex': 0,
  'aria-rowspan': 0,
  'aria-setsize': 0
};

var warnedProperties = {};
var rARIA = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
var rARIACamel = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

var hasOwnProperty = Object.prototype.hasOwnProperty;

function getStackAddendum() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

function validateProperty(tagName, name) {
  if (hasOwnProperty.call(warnedProperties, name) && warnedProperties[name]) {
    return true;
  }

  if (rARIACamel.test(name)) {
    var ariaName = 'aria-' + name.slice(4).toLowerCase();
    var correctName = ariaProperties.hasOwnProperty(ariaName) ? ariaName : null;

    // If this is an aria-* attribute, but is not listed in the known DOM
    // DOM properties, then it is an invalid aria-* attribute.
    if (correctName == null) {
      warning(false, 'Invalid ARIA attribute `%s`. ARIA attributes follow the pattern aria-* and must be lowercase.%s', name, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
    // aria-* attributes should be lowercase; suggest the lowercase version.
    if (name !== correctName) {
      warning(false, 'Invalid ARIA attribute `%s`. Did you mean `%s`?%s', name, correctName, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
  }

  if (rARIA.test(name)) {
    var lowerCasedName = name.toLowerCase();
    var standardName = ariaProperties.hasOwnProperty(lowerCasedName) ? lowerCasedName : null;

    // If this is an aria-* attribute, but is not listed in the known DOM
    // DOM properties, then it is an invalid aria-* attribute.
    if (standardName == null) {
      warnedProperties[name] = true;
      return false;
    }
    // aria-* attributes should be lowercase; suggest the lowercase version.
    if (name !== standardName) {
      warning(false, 'Unknown ARIA attribute `%s`. Did you mean `%s`?%s', name, standardName, getStackAddendum());
      warnedProperties[name] = true;
      return true;
    }
  }

  return true;
}

function warnInvalidARIAProps(type, props) {
  var invalidProps = [];

  for (var key in props) {
    var isValid = validateProperty(type, key);
    if (!isValid) {
      invalidProps.push(key);
    }
  }

  var unknownPropString = invalidProps.map(function (prop) {
    return '`' + prop + '`';
  }).join(', ');

  if (invalidProps.length === 1) {
    warning(false, 'Invalid aria prop %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop%s', unknownPropString, type, getStackAddendum());
  } else if (invalidProps.length > 1) {
    warning(false, 'Invalid aria props %s on <%s> tag. ' + 'For details, see https://fb.me/invalid-aria-prop%s', unknownPropString, type, getStackAddendum());
  }
}

function validateProperties(type, props) {
  if (isCustomComponent(type, props)) {
    return;
  }
  warnInvalidARIAProps(type, props);
}

var didWarnValueNull = false;

function getStackAddendum$1() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

function validateProperties$1(type, props) {
  if (type !== 'input' && type !== 'textarea' && type !== 'select') {
    return;
  }

  if (props != null && props.value === null && !didWarnValueNull) {
    didWarnValueNull = true;
    if (type === 'select' && props.multiple) {
      warning(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty array when `multiple` is set to `true` ' + 'to clear the component or `undefined` for uncontrolled components.%s', type, getStackAddendum$1());
    } else {
      warning(false, '`value` prop on `%s` should not be null. ' + 'Consider using an empty string to clear the component or `undefined` ' + 'for uncontrolled components.%s', type, getStackAddendum$1());
    }
  }
}

// When adding attributes to the HTML or SVG whitelist, be sure to
// also add them to this module to ensure casing and incorrect name
// warnings.
var possibleStandardNames = {
  // HTML
  accept: 'accept',
  acceptcharset: 'acceptCharset',
  'accept-charset': 'acceptCharset',
  accesskey: 'accessKey',
  action: 'action',
  allowfullscreen: 'allowFullScreen',
  alt: 'alt',
  as: 'as',
  async: 'async',
  autocapitalize: 'autoCapitalize',
  autocomplete: 'autoComplete',
  autocorrect: 'autoCorrect',
  autofocus: 'autoFocus',
  autoplay: 'autoPlay',
  autosave: 'autoSave',
  capture: 'capture',
  cellpadding: 'cellPadding',
  cellspacing: 'cellSpacing',
  challenge: 'challenge',
  charset: 'charSet',
  checked: 'checked',
  children: 'children',
  cite: 'cite',
  'class': 'className',
  classid: 'classID',
  classname: 'className',
  cols: 'cols',
  colspan: 'colSpan',
  content: 'content',
  contenteditable: 'contentEditable',
  contextmenu: 'contextMenu',
  controls: 'controls',
  controlslist: 'controlsList',
  coords: 'coords',
  crossorigin: 'crossOrigin',
  dangerouslysetinnerhtml: 'dangerouslySetInnerHTML',
  data: 'data',
  datetime: 'dateTime',
  'default': 'default',
  defaultchecked: 'defaultChecked',
  defaultvalue: 'defaultValue',
  defer: 'defer',
  dir: 'dir',
  disabled: 'disabled',
  download: 'download',
  draggable: 'draggable',
  enctype: 'encType',
  'for': 'htmlFor',
  form: 'form',
  formmethod: 'formMethod',
  formaction: 'formAction',
  formenctype: 'formEncType',
  formnovalidate: 'formNoValidate',
  formtarget: 'formTarget',
  frameborder: 'frameBorder',
  headers: 'headers',
  height: 'height',
  hidden: 'hidden',
  high: 'high',
  href: 'href',
  hreflang: 'hrefLang',
  htmlfor: 'htmlFor',
  httpequiv: 'httpEquiv',
  'http-equiv': 'httpEquiv',
  icon: 'icon',
  id: 'id',
  innerhtml: 'innerHTML',
  inputmode: 'inputMode',
  integrity: 'integrity',
  is: 'is',
  itemid: 'itemID',
  itemprop: 'itemProp',
  itemref: 'itemRef',
  itemscope: 'itemScope',
  itemtype: 'itemType',
  keyparams: 'keyParams',
  keytype: 'keyType',
  kind: 'kind',
  label: 'label',
  lang: 'lang',
  list: 'list',
  loop: 'loop',
  low: 'low',
  manifest: 'manifest',
  marginwidth: 'marginWidth',
  marginheight: 'marginHeight',
  max: 'max',
  maxlength: 'maxLength',
  media: 'media',
  mediagroup: 'mediaGroup',
  method: 'method',
  min: 'min',
  minlength: 'minLength',
  multiple: 'multiple',
  muted: 'muted',
  name: 'name',
  nonce: 'nonce',
  novalidate: 'noValidate',
  open: 'open',
  optimum: 'optimum',
  pattern: 'pattern',
  placeholder: 'placeholder',
  playsinline: 'playsInline',
  poster: 'poster',
  preload: 'preload',
  profile: 'profile',
  radiogroup: 'radioGroup',
  readonly: 'readOnly',
  referrerpolicy: 'referrerPolicy',
  rel: 'rel',
  required: 'required',
  reversed: 'reversed',
  role: 'role',
  rows: 'rows',
  rowspan: 'rowSpan',
  sandbox: 'sandbox',
  scope: 'scope',
  scoped: 'scoped',
  scrolling: 'scrolling',
  seamless: 'seamless',
  selected: 'selected',
  shape: 'shape',
  size: 'size',
  sizes: 'sizes',
  span: 'span',
  spellcheck: 'spellCheck',
  src: 'src',
  srcdoc: 'srcDoc',
  srclang: 'srcLang',
  srcset: 'srcSet',
  start: 'start',
  step: 'step',
  style: 'style',
  summary: 'summary',
  tabindex: 'tabIndex',
  target: 'target',
  title: 'title',
  type: 'type',
  usemap: 'useMap',
  value: 'value',
  width: 'width',
  wmode: 'wmode',
  wrap: 'wrap',

  // SVG
  about: 'about',
  accentheight: 'accentHeight',
  'accent-height': 'accentHeight',
  accumulate: 'accumulate',
  additive: 'additive',
  alignmentbaseline: 'alignmentBaseline',
  'alignment-baseline': 'alignmentBaseline',
  allowreorder: 'allowReorder',
  alphabetic: 'alphabetic',
  amplitude: 'amplitude',
  arabicform: 'arabicForm',
  'arabic-form': 'arabicForm',
  ascent: 'ascent',
  attributename: 'attributeName',
  attributetype: 'attributeType',
  autoreverse: 'autoReverse',
  azimuth: 'azimuth',
  basefrequency: 'baseFrequency',
  baselineshift: 'baselineShift',
  'baseline-shift': 'baselineShift',
  baseprofile: 'baseProfile',
  bbox: 'bbox',
  begin: 'begin',
  bias: 'bias',
  by: 'by',
  calcmode: 'calcMode',
  capheight: 'capHeight',
  'cap-height': 'capHeight',
  clip: 'clip',
  clippath: 'clipPath',
  'clip-path': 'clipPath',
  clippathunits: 'clipPathUnits',
  cliprule: 'clipRule',
  'clip-rule': 'clipRule',
  color: 'color',
  colorinterpolation: 'colorInterpolation',
  'color-interpolation': 'colorInterpolation',
  colorinterpolationfilters: 'colorInterpolationFilters',
  'color-interpolation-filters': 'colorInterpolationFilters',
  colorprofile: 'colorProfile',
  'color-profile': 'colorProfile',
  colorrendering: 'colorRendering',
  'color-rendering': 'colorRendering',
  contentscripttype: 'contentScriptType',
  contentstyletype: 'contentStyleType',
  cursor: 'cursor',
  cx: 'cx',
  cy: 'cy',
  d: 'd',
  datatype: 'datatype',
  decelerate: 'decelerate',
  descent: 'descent',
  diffuseconstant: 'diffuseConstant',
  direction: 'direction',
  display: 'display',
  divisor: 'divisor',
  dominantbaseline: 'dominantBaseline',
  'dominant-baseline': 'dominantBaseline',
  dur: 'dur',
  dx: 'dx',
  dy: 'dy',
  edgemode: 'edgeMode',
  elevation: 'elevation',
  enablebackground: 'enableBackground',
  'enable-background': 'enableBackground',
  end: 'end',
  exponent: 'exponent',
  externalresourcesrequired: 'externalResourcesRequired',
  fill: 'fill',
  fillopacity: 'fillOpacity',
  'fill-opacity': 'fillOpacity',
  fillrule: 'fillRule',
  'fill-rule': 'fillRule',
  filter: 'filter',
  filterres: 'filterRes',
  filterunits: 'filterUnits',
  floodopacity: 'floodOpacity',
  'flood-opacity': 'floodOpacity',
  floodcolor: 'floodColor',
  'flood-color': 'floodColor',
  focusable: 'focusable',
  fontfamily: 'fontFamily',
  'font-family': 'fontFamily',
  fontsize: 'fontSize',
  'font-size': 'fontSize',
  fontsizeadjust: 'fontSizeAdjust',
  'font-size-adjust': 'fontSizeAdjust',
  fontstretch: 'fontStretch',
  'font-stretch': 'fontStretch',
  fontstyle: 'fontStyle',
  'font-style': 'fontStyle',
  fontvariant: 'fontVariant',
  'font-variant': 'fontVariant',
  fontweight: 'fontWeight',
  'font-weight': 'fontWeight',
  format: 'format',
  from: 'from',
  fx: 'fx',
  fy: 'fy',
  g1: 'g1',
  g2: 'g2',
  glyphname: 'glyphName',
  'glyph-name': 'glyphName',
  glyphorientationhorizontal: 'glyphOrientationHorizontal',
  'glyph-orientation-horizontal': 'glyphOrientationHorizontal',
  glyphorientationvertical: 'glyphOrientationVertical',
  'glyph-orientation-vertical': 'glyphOrientationVertical',
  glyphref: 'glyphRef',
  gradienttransform: 'gradientTransform',
  gradientunits: 'gradientUnits',
  hanging: 'hanging',
  horizadvx: 'horizAdvX',
  'horiz-adv-x': 'horizAdvX',
  horizoriginx: 'horizOriginX',
  'horiz-origin-x': 'horizOriginX',
  ideographic: 'ideographic',
  imagerendering: 'imageRendering',
  'image-rendering': 'imageRendering',
  in2: 'in2',
  'in': 'in',
  inlist: 'inlist',
  intercept: 'intercept',
  k1: 'k1',
  k2: 'k2',
  k3: 'k3',
  k4: 'k4',
  k: 'k',
  kernelmatrix: 'kernelMatrix',
  kernelunitlength: 'kernelUnitLength',
  kerning: 'kerning',
  keypoints: 'keyPoints',
  keysplines: 'keySplines',
  keytimes: 'keyTimes',
  lengthadjust: 'lengthAdjust',
  letterspacing: 'letterSpacing',
  'letter-spacing': 'letterSpacing',
  lightingcolor: 'lightingColor',
  'lighting-color': 'lightingColor',
  limitingconeangle: 'limitingConeAngle',
  local: 'local',
  markerend: 'markerEnd',
  'marker-end': 'markerEnd',
  markerheight: 'markerHeight',
  markermid: 'markerMid',
  'marker-mid': 'markerMid',
  markerstart: 'markerStart',
  'marker-start': 'markerStart',
  markerunits: 'markerUnits',
  markerwidth: 'markerWidth',
  mask: 'mask',
  maskcontentunits: 'maskContentUnits',
  maskunits: 'maskUnits',
  mathematical: 'mathematical',
  mode: 'mode',
  numoctaves: 'numOctaves',
  offset: 'offset',
  opacity: 'opacity',
  operator: 'operator',
  order: 'order',
  orient: 'orient',
  orientation: 'orientation',
  origin: 'origin',
  overflow: 'overflow',
  overlineposition: 'overlinePosition',
  'overline-position': 'overlinePosition',
  overlinethickness: 'overlineThickness',
  'overline-thickness': 'overlineThickness',
  paintorder: 'paintOrder',
  'paint-order': 'paintOrder',
  panose1: 'panose1',
  'panose-1': 'panose1',
  pathlength: 'pathLength',
  patterncontentunits: 'patternContentUnits',
  patterntransform: 'patternTransform',
  patternunits: 'patternUnits',
  pointerevents: 'pointerEvents',
  'pointer-events': 'pointerEvents',
  points: 'points',
  pointsatx: 'pointsAtX',
  pointsaty: 'pointsAtY',
  pointsatz: 'pointsAtZ',
  prefix: 'prefix',
  preservealpha: 'preserveAlpha',
  preserveaspectratio: 'preserveAspectRatio',
  primitiveunits: 'primitiveUnits',
  property: 'property',
  r: 'r',
  radius: 'radius',
  refx: 'refX',
  refy: 'refY',
  renderingintent: 'renderingIntent',
  'rendering-intent': 'renderingIntent',
  repeatcount: 'repeatCount',
  repeatdur: 'repeatDur',
  requiredextensions: 'requiredExtensions',
  requiredfeatures: 'requiredFeatures',
  resource: 'resource',
  restart: 'restart',
  result: 'result',
  results: 'results',
  rotate: 'rotate',
  rx: 'rx',
  ry: 'ry',
  scale: 'scale',
  security: 'security',
  seed: 'seed',
  shaperendering: 'shapeRendering',
  'shape-rendering': 'shapeRendering',
  slope: 'slope',
  spacing: 'spacing',
  specularconstant: 'specularConstant',
  specularexponent: 'specularExponent',
  speed: 'speed',
  spreadmethod: 'spreadMethod',
  startoffset: 'startOffset',
  stddeviation: 'stdDeviation',
  stemh: 'stemh',
  stemv: 'stemv',
  stitchtiles: 'stitchTiles',
  stopcolor: 'stopColor',
  'stop-color': 'stopColor',
  stopopacity: 'stopOpacity',
  'stop-opacity': 'stopOpacity',
  strikethroughposition: 'strikethroughPosition',
  'strikethrough-position': 'strikethroughPosition',
  strikethroughthickness: 'strikethroughThickness',
  'strikethrough-thickness': 'strikethroughThickness',
  string: 'string',
  stroke: 'stroke',
  strokedasharray: 'strokeDasharray',
  'stroke-dasharray': 'strokeDasharray',
  strokedashoffset: 'strokeDashoffset',
  'stroke-dashoffset': 'strokeDashoffset',
  strokelinecap: 'strokeLinecap',
  'stroke-linecap': 'strokeLinecap',
  strokelinejoin: 'strokeLinejoin',
  'stroke-linejoin': 'strokeLinejoin',
  strokemiterlimit: 'strokeMiterlimit',
  'stroke-miterlimit': 'strokeMiterlimit',
  strokewidth: 'strokeWidth',
  'stroke-width': 'strokeWidth',
  strokeopacity: 'strokeOpacity',
  'stroke-opacity': 'strokeOpacity',
  suppresscontenteditablewarning: 'suppressContentEditableWarning',
  suppresshydrationwarning: 'suppressHydrationWarning',
  surfacescale: 'surfaceScale',
  systemlanguage: 'systemLanguage',
  tablevalues: 'tableValues',
  targetx: 'targetX',
  targety: 'targetY',
  textanchor: 'textAnchor',
  'text-anchor': 'textAnchor',
  textdecoration: 'textDecoration',
  'text-decoration': 'textDecoration',
  textlength: 'textLength',
  textrendering: 'textRendering',
  'text-rendering': 'textRendering',
  to: 'to',
  transform: 'transform',
  'typeof': 'typeof',
  u1: 'u1',
  u2: 'u2',
  underlineposition: 'underlinePosition',
  'underline-position': 'underlinePosition',
  underlinethickness: 'underlineThickness',
  'underline-thickness': 'underlineThickness',
  unicode: 'unicode',
  unicodebidi: 'unicodeBidi',
  'unicode-bidi': 'unicodeBidi',
  unicoderange: 'unicodeRange',
  'unicode-range': 'unicodeRange',
  unitsperem: 'unitsPerEm',
  'units-per-em': 'unitsPerEm',
  unselectable: 'unselectable',
  valphabetic: 'vAlphabetic',
  'v-alphabetic': 'vAlphabetic',
  values: 'values',
  vectoreffect: 'vectorEffect',
  'vector-effect': 'vectorEffect',
  version: 'version',
  vertadvy: 'vertAdvY',
  'vert-adv-y': 'vertAdvY',
  vertoriginx: 'vertOriginX',
  'vert-origin-x': 'vertOriginX',
  vertoriginy: 'vertOriginY',
  'vert-origin-y': 'vertOriginY',
  vhanging: 'vHanging',
  'v-hanging': 'vHanging',
  videographic: 'vIdeographic',
  'v-ideographic': 'vIdeographic',
  viewbox: 'viewBox',
  viewtarget: 'viewTarget',
  visibility: 'visibility',
  vmathematical: 'vMathematical',
  'v-mathematical': 'vMathematical',
  vocab: 'vocab',
  widths: 'widths',
  wordspacing: 'wordSpacing',
  'word-spacing': 'wordSpacing',
  writingmode: 'writingMode',
  'writing-mode': 'writingMode',
  x1: 'x1',
  x2: 'x2',
  x: 'x',
  xchannelselector: 'xChannelSelector',
  xheight: 'xHeight',
  'x-height': 'xHeight',
  xlinkactuate: 'xlinkActuate',
  'xlink:actuate': 'xlinkActuate',
  xlinkarcrole: 'xlinkArcrole',
  'xlink:arcrole': 'xlinkArcrole',
  xlinkhref: 'xlinkHref',
  'xlink:href': 'xlinkHref',
  xlinkrole: 'xlinkRole',
  'xlink:role': 'xlinkRole',
  xlinkshow: 'xlinkShow',
  'xlink:show': 'xlinkShow',
  xlinktitle: 'xlinkTitle',
  'xlink:title': 'xlinkTitle',
  xlinktype: 'xlinkType',
  'xlink:type': 'xlinkType',
  xmlbase: 'xmlBase',
  'xml:base': 'xmlBase',
  xmllang: 'xmlLang',
  'xml:lang': 'xmlLang',
  xmlns: 'xmlns',
  'xml:space': 'xmlSpace',
  xmlnsxlink: 'xmlnsXlink',
  'xmlns:xlink': 'xmlnsXlink',
  xmlspace: 'xmlSpace',
  y1: 'y1',
  y2: 'y2',
  y: 'y',
  ychannelselector: 'yChannelSelector',
  z: 'z',
  zoomandpan: 'zoomAndPan'
};

function getStackAddendum$2() {
  var stack = ReactDebugCurrentFrame.getStackAddendum();
  return stack != null ? stack : '';
}

{
  var warnedProperties$1 = {};
  var hasOwnProperty$1 = Object.prototype.hasOwnProperty;
  var EVENT_NAME_REGEX = /^on./;
  var INVALID_EVENT_NAME_REGEX = /^on[^A-Z]/;
  var rARIA$1 = new RegExp('^(aria)-[' + ATTRIBUTE_NAME_CHAR + ']*$');
  var rARIACamel$1 = new RegExp('^(aria)[A-Z][' + ATTRIBUTE_NAME_CHAR + ']*$');

  var validateProperty$1 = function (tagName, name, value, canUseEventSystem) {
    if (hasOwnProperty$1.call(warnedProperties$1, name) && warnedProperties$1[name]) {
      return true;
    }

    var lowerCasedName = name.toLowerCase();
    if (lowerCasedName === 'onfocusin' || lowerCasedName === 'onfocusout') {
      warning(false, 'React uses onFocus and onBlur instead of onFocusIn and onFocusOut. ' + 'All React events are normalized to bubble, so onFocusIn and onFocusOut ' + 'are not needed/supported by React.');
      warnedProperties$1[name] = true;
      return true;
    }

    // We can't rely on the event system being injected on the server.
    if (canUseEventSystem) {
      if (registrationNameModules.hasOwnProperty(name)) {
        return true;
      }
      var registrationName = possibleRegistrationNames.hasOwnProperty(lowerCasedName) ? possibleRegistrationNames[lowerCasedName] : null;
      if (registrationName != null) {
        warning(false, 'Invalid event handler property `%s`. Did you mean `%s`?%s', name, registrationName, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
      if (EVENT_NAME_REGEX.test(name)) {
        warning(false, 'Unknown event handler property `%s`. It will be ignored.%s', name, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
    } else if (EVENT_NAME_REGEX.test(name)) {
      // If no event plugins have been injected, we are in a server environment.
      // So we can't tell if the event name is correct for sure, but we can filter
      // out known bad ones like `onclick`. We can't suggest a specific replacement though.
      if (INVALID_EVENT_NAME_REGEX.test(name)) {
        warning(false, 'Invalid event handler property `%s`. ' + 'React events use the camelCase naming convention, for example `onClick`.%s', name, getStackAddendum$2());
      }
      warnedProperties$1[name] = true;
      return true;
    }

    // Let the ARIA attribute hook validate ARIA attributes
    if (rARIA$1.test(name) || rARIACamel$1.test(name)) {
      return true;
    }

    if (lowerCasedName === 'innerhtml') {
      warning(false, 'Directly setting property `innerHTML` is not permitted. ' + 'For more information, lookup documentation on `dangerouslySetInnerHTML`.');
      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'aria') {
      warning(false, 'The `aria` attribute is reserved for future use in React. ' + 'Pass individual `aria-` attributes instead.');
      warnedProperties$1[name] = true;
      return true;
    }

    if (lowerCasedName === 'is' && value !== null && value !== undefined && typeof value !== 'string') {
      warning(false, 'Received a `%s` for a string attribute `is`. If this is expected, cast ' + 'the value to a string.%s', typeof value, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'number' && isNaN(value)) {
      warning(false, 'Received NaN for the `%s` attribute. If this is expected, cast ' + 'the value to a string.%s', name, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    var isReserved = isReservedProp(name);

    // Known attributes should match the casing specified in the property config.
    if (possibleStandardNames.hasOwnProperty(lowerCasedName)) {
      var standardName = possibleStandardNames[lowerCasedName];
      if (standardName !== name) {
        warning(false, 'Invalid DOM property `%s`. Did you mean `%s`?%s', name, standardName, getStackAddendum$2());
        warnedProperties$1[name] = true;
        return true;
      }
    } else if (!isReserved && name !== lowerCasedName) {
      // Unknown attributes should have lowercase casing since that's how they
      // will be cased anyway with server rendering.
      warning(false, 'React does not recognize the `%s` prop on a DOM element. If you ' + 'intentionally want it to appear in the DOM as a custom ' + 'attribute, spell it as lowercase `%s` instead. ' + 'If you accidentally passed it from a parent component, remove ' + 'it from the DOM element.%s', name, lowerCasedName, getStackAddendum$2());
      warnedProperties$1[name] = true;
      return true;
    }

    if (typeof value === 'boolean' && !shouldAttributeAcceptBooleanValue(name)) {
      if (value) {
        warning(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.%s', value, name, name, value, name, getStackAddendum$2());
      } else {
        warning(false, 'Received `%s` for a non-boolean attribute `%s`.\n\n' + 'If you want to write it to the DOM, pass a string instead: ' + '%s="%s" or %s={value.toString()}.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.%s', value, name, name, value, name, name, name, getStackAddendum$2());
      }
      warnedProperties$1[name] = true;
      return true;
    }

    // Now that we've validated casing, do not validate
    // data types for reserved props
    if (isReserved) {
      return true;
    }

    // Warn when a known attribute is a bad type
    if (!shouldSetAttribute(name, value)) {
      warnedProperties$1[name] = true;
      return false;
    }

    return true;
  };
}

var warnUnknownProperties = function (type, props, canUseEventSystem) {
  var unknownProps = [];
  for (var key in props) {
    var isValid = validateProperty$1(type, key, props[key], canUseEventSystem);
    if (!isValid) {
      unknownProps.push(key);
    }
  }

  var unknownPropString = unknownProps.map(function (prop) {
    return '`' + prop + '`';
  }).join(', ');
  if (unknownProps.length === 1) {
    warning(false, 'Invalid value for prop %s on <%s> tag. Either remove it from the element, ' + 'or pass a string or number value to keep it in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior%s', unknownPropString, type, getStackAddendum$2());
  } else if (unknownProps.length > 1) {
    warning(false, 'Invalid values for props %s on <%s> tag. Either remove them from the element, ' + 'or pass a string or number value to keep them in the DOM. ' + 'For details, see https://fb.me/react-attribute-behavior%s', unknownPropString, type, getStackAddendum$2());
  }
};

function validateProperties$2(type, props, canUseEventSystem) {
  if (isCustomComponent(type, props)) {
    return;
  }
  warnUnknownProperties(type, props, canUseEventSystem);
}

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberOwnerName$1 = ReactDebugCurrentFiber.getCurrentFiberOwnerName;
var getCurrentFiberStackAddendum$2 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var didWarnInvalidHydration = false;
var didWarnShadyDOM = false;

var DANGEROUSLY_SET_INNER_HTML = 'dangerouslySetInnerHTML';
var SUPPRESS_CONTENT_EDITABLE_WARNING = 'suppressContentEditableWarning';
var SUPPRESS_HYDRATION_WARNING$1 = 'suppressHydrationWarning';
var AUTOFOCUS = 'autoFocus';
var CHILDREN = 'children';
var STYLE = 'style';
var HTML = '__html';

var HTML_NAMESPACE = Namespaces.html;


var getStack = emptyFunction.thatReturns('');

{
  getStack = getCurrentFiberStackAddendum$2;

  var warnedUnknownTags = {
    // Chrome is the only major browser not shipping <time>. But as of July
    // 2017 it intends to ship it due to widespread usage. We intentionally
    // *don't* warn for <time> even if it's unrecognized by Chrome because
    // it soon will be, and many apps have been using it anyway.
    time: true,
    // There are working polyfills for <dialog>. Let people use it.
    dialog: true
  };

  var validatePropertiesInDevelopment = function (type, props) {
    validateProperties(type, props);
    validateProperties$1(type, props);
    validateProperties$2(type, props, /* canUseEventSystem */true);
  };

  // HTML parsing normalizes CR and CRLF to LF.
  // It also can turn \u0000 into \uFFFD inside attributes.
  // https://www.w3.org/TR/html5/single-page.html#preprocessing-the-input-stream
  // If we have a mismatch, it might be caused by that.
  // We will still patch up in this case but not fire the warning.
  var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
  var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;

  var normalizeMarkupForTextOrAttribute = function (markup) {
    var markupString = typeof markup === 'string' ? markup : '' + markup;
    return markupString.replace(NORMALIZE_NEWLINES_REGEX, '\n').replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, '');
  };

  var warnForTextDifference = function (serverText, clientText) {
    if (didWarnInvalidHydration) {
      return;
    }
    var normalizedClientText = normalizeMarkupForTextOrAttribute(clientText);
    var normalizedServerText = normalizeMarkupForTextOrAttribute(serverText);
    if (normalizedServerText === normalizedClientText) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Text content did not match. Server: "%s" Client: "%s"', normalizedServerText, normalizedClientText);
  };

  var warnForPropDifference = function (propName, serverValue, clientValue) {
    if (didWarnInvalidHydration) {
      return;
    }
    var normalizedClientValue = normalizeMarkupForTextOrAttribute(clientValue);
    var normalizedServerValue = normalizeMarkupForTextOrAttribute(serverValue);
    if (normalizedServerValue === normalizedClientValue) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Prop `%s` did not match. Server: %s Client: %s', propName, JSON.stringify(normalizedServerValue), JSON.stringify(normalizedClientValue));
  };

  var warnForExtraAttributes = function (attributeNames) {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    var names = [];
    attributeNames.forEach(function (name) {
      names.push(name);
    });
    warning(false, 'Extra attributes from the server: %s', names);
  };

  var warnForInvalidEventListener = function (registrationName, listener) {
    if (listener === false) {
      warning(false, 'Expected `%s` listener to be a function, instead got `false`.\n\n' + 'If you used to conditionally omit it with %s={condition && value}, ' + 'pass %s={condition ? value : undefined} instead.%s', registrationName, registrationName, registrationName, getCurrentFiberStackAddendum$2());
    } else {
      warning(false, 'Expected `%s` listener to be a function, instead got a value of `%s` type.%s', registrationName, typeof listener, getCurrentFiberStackAddendum$2());
    }
  };

  // Parse the HTML and read it back to normalize the HTML string so that it
  // can be used for comparison.
  var normalizeHTML = function (parent, html) {
    // We could have created a separate document here to avoid
    // re-initializing custom elements if they exist. But this breaks
    // how <noscript> is being handled. So we use the same document.
    // See the discussion in https://github.com/facebook/react/pull/11157.
    var testElement = parent.namespaceURI === HTML_NAMESPACE ? parent.ownerDocument.createElement(parent.tagName) : parent.ownerDocument.createElementNS(parent.namespaceURI, parent.tagName);
    testElement.innerHTML = html;
    return testElement.innerHTML;
  };
}

function ensureListeningTo(rootContainerElement, registrationName) {
  var isDocumentOrFragment = rootContainerElement.nodeType === DOCUMENT_NODE || rootContainerElement.nodeType === DOCUMENT_FRAGMENT_NODE;
  var doc = isDocumentOrFragment ? rootContainerElement : rootContainerElement.ownerDocument;
  listenTo(registrationName, doc);
}

function getOwnerDocumentFromRootContainer(rootContainerElement) {
  return rootContainerElement.nodeType === DOCUMENT_NODE ? rootContainerElement : rootContainerElement.ownerDocument;
}

// There are so many media events, it makes sense to just
// maintain a list rather than create a `trapBubbledEvent` for each
var mediaEvents = {
  topAbort: 'abort',
  topCanPlay: 'canplay',
  topCanPlayThrough: 'canplaythrough',
  topDurationChange: 'durationchange',
  topEmptied: 'emptied',
  topEncrypted: 'encrypted',
  topEnded: 'ended',
  topError: 'error',
  topLoadedData: 'loadeddata',
  topLoadedMetadata: 'loadedmetadata',
  topLoadStart: 'loadstart',
  topPause: 'pause',
  topPlay: 'play',
  topPlaying: 'playing',
  topProgress: 'progress',
  topRateChange: 'ratechange',
  topSeeked: 'seeked',
  topSeeking: 'seeking',
  topStalled: 'stalled',
  topSuspend: 'suspend',
  topTimeUpdate: 'timeupdate',
  topVolumeChange: 'volumechange',
  topWaiting: 'waiting'
};

function trapClickOnNonInteractiveElement(node) {
  // Mobile Safari does not fire properly bubble click events on
  // non-interactive elements, which means delegated click listeners do not
  // fire. The workaround for this bug involves attaching an empty click
  // listener on the target node.
  // http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
  // Just set it using the onclick property so that we don't have to manage any
  // bookkeeping for it. Not sure if we need to clear it when the listener is
  // removed.
  // TODO: Only do this for the relevant Safaris maybe?
  node.onclick = emptyFunction;
}

function setInitialDOMProperties(tag, domElement, rootContainerElement, nextProps, isCustomComponentTag) {
  for (var propKey in nextProps) {
    if (!nextProps.hasOwnProperty(propKey)) {
      continue;
    }
    var nextProp = nextProps[propKey];
    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }
      // Relies on `updateStylesByID` not mutating `styleUpdates`.
      setValueForStyles(domElement, nextProp, getStack);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML] : undefined;
      if (nextHtml != null) {
        setInnerHTML(domElement, nextHtml);
      }
    } else if (propKey === CHILDREN) {
      if (typeof nextProp === 'string') {
        // Avoid setting initial textContent when the text is empty. In IE11 setting
        // textContent on a <textarea> will cause the placeholder to not
        // show within the <textarea> until it has been focused and blurred again.
        // https://github.com/facebook/react/issues/6731#issuecomment-254874553
        var canSetTextContent = tag !== 'textarea' || nextProp !== '';
        if (canSetTextContent) {
          setTextContent(domElement, nextProp);
        }
      } else if (typeof nextProp === 'number') {
        setTextContent(domElement, '' + nextProp);
      }
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (propKey === AUTOFOCUS) {
      // We polyfill it separately on the client during commit.
      // We blacklist it here rather than in the property list because we emit it in SSR.
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
    } else if (isCustomComponentTag) {
      setValueForAttribute(domElement, propKey, nextProp);
    } else if (nextProp != null) {
      // If we're updating to null or undefined, we should remove the property
      // from the DOM node instead of inadvertently setting to a string. This
      // brings us in line with the same behavior we have on initial render.
      setValueForProperty(domElement, propKey, nextProp);
    }
  }
}

function updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag) {
  // TODO: Handle wasCustomComponentTag
  for (var i = 0; i < updatePayload.length; i += 2) {
    var propKey = updatePayload[i];
    var propValue = updatePayload[i + 1];
    if (propKey === STYLE) {
      setValueForStyles(domElement, propValue, getStack);
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      setInnerHTML(domElement, propValue);
    } else if (propKey === CHILDREN) {
      setTextContent(domElement, propValue);
    } else if (isCustomComponentTag) {
      if (propValue != null) {
        setValueForAttribute(domElement, propKey, propValue);
      } else {
        deleteValueForAttribute(domElement, propKey);
      }
    } else if (propValue != null) {
      setValueForProperty(domElement, propKey, propValue);
    } else {
      // If we're updating to null or undefined, we should remove the property
      // from the DOM node instead of inadvertently setting to a string. This
      // brings us in line with the same behavior we have on initial render.
      deleteValueForProperty(domElement, propKey);
    }
  }
}

function createElement$1(type, props, rootContainerElement, parentNamespace) {
  // We create tags in the namespace of their parent container, except HTML
  var ownerDocument = getOwnerDocumentFromRootContainer(rootContainerElement);
  var domElement;
  var namespaceURI = parentNamespace;
  if (namespaceURI === HTML_NAMESPACE) {
    namespaceURI = getIntrinsicNamespace(type);
  }
  if (namespaceURI === HTML_NAMESPACE) {
    {
      var isCustomComponentTag = isCustomComponent(type, props);
      // Should this check be gated by parent namespace? Not sure we want to
      // allow <SVG> or <mATH>.
      warning(isCustomComponentTag || type === type.toLowerCase(), '<%s /> is using uppercase HTML. Always use lowercase HTML tags ' + 'in React.', type);
    }

    if (type === 'script') {
      // Create the script via .innerHTML so its "parser-inserted" flag is
      // set to true and it does not execute
      var div = ownerDocument.createElement('div');
      div.innerHTML = '<script><' + '/script>'; // eslint-disable-line
      // This is guaranteed to yield a script element.
      var firstChild = div.firstChild;
      domElement = div.removeChild(firstChild);
    } else if (typeof props.is === 'string') {
      // $FlowIssue `createElement` should be updated for Web Components
      domElement = ownerDocument.createElement(type, { is: props.is });
    } else {
      // Separate else branch instead of using `props.is || undefined` above because of a Firefox bug.
      // See discussion in https://github.com/facebook/react/pull/6896
      // and discussion in https://bugzilla.mozilla.org/show_bug.cgi?id=1276240
      domElement = ownerDocument.createElement(type);
    }
  } else {
    domElement = ownerDocument.createElementNS(namespaceURI, type);
  }

  {
    if (namespaceURI === HTML_NAMESPACE) {
      if (!isCustomComponentTag && Object.prototype.toString.call(domElement) === '[object HTMLUnknownElement]' && !Object.prototype.hasOwnProperty.call(warnedUnknownTags, type)) {
        warnedUnknownTags[type] = true;
        warning(false, 'The tag <%s> is unrecognized in this browser. ' + 'If you meant to render a React component, start its name with ' + 'an uppercase letter.', type);
      }
    }
  }

  return domElement;
}

function createTextNode$1(text, rootContainerElement) {
  return getOwnerDocumentFromRootContainer(rootContainerElement).createTextNode(text);
}

function setInitialProperties$1(domElement, tag, rawProps, rootContainerElement) {
  var isCustomComponentTag = isCustomComponent(tag, rawProps);
  {
    validatePropertiesInDevelopment(tag, rawProps);
    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      warning(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerName$1() || 'A component');
      didWarnShadyDOM = true;
    }
  }

  // TODO: Make sure that we check isMounted before firing any of these events.
  var props;
  switch (tag) {
    case 'iframe':
    case 'object':
      trapBubbledEvent('topLoad', 'load', domElement);
      props = rawProps;
      break;
    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var event in mediaEvents) {
        if (mediaEvents.hasOwnProperty(event)) {
          trapBubbledEvent(event, mediaEvents[event], domElement);
        }
      }
      props = rawProps;
      break;
    case 'source':
      trapBubbledEvent('topError', 'error', domElement);
      props = rawProps;
      break;
    case 'img':
    case 'image':
      trapBubbledEvent('topError', 'error', domElement);
      trapBubbledEvent('topLoad', 'load', domElement);
      props = rawProps;
      break;
    case 'form':
      trapBubbledEvent('topReset', 'reset', domElement);
      trapBubbledEvent('topSubmit', 'submit', domElement);
      props = rawProps;
      break;
    case 'details':
      trapBubbledEvent('topToggle', 'toggle', domElement);
      props = rawProps;
      break;
    case 'input':
      initWrapperState(domElement, rawProps);
      props = getHostProps(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'option':
      validateProps(domElement, rawProps);
      props = getHostProps$1(domElement, rawProps);
      break;
    case 'select':
      initWrapperState$1(domElement, rawProps);
      props = getHostProps$2(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      props = getHostProps$3(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    default:
      props = rawProps;
  }

  assertValidProps(tag, props, getStack);

  setInitialDOMProperties(tag, domElement, rootContainerElement, props, isCustomComponentTag);

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps);
      break;
    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement, rawProps);
      break;
    case 'option':
      postMountWrapper$1(domElement, rawProps);
      break;
    case 'select':
      postMountWrapper$2(domElement, rawProps);
      break;
    default:
      if (typeof props.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }
}

// Calculate the diff between the two objects.
function diffProperties$1(domElement, tag, lastRawProps, nextRawProps, rootContainerElement) {
  {
    validatePropertiesInDevelopment(tag, nextRawProps);
  }

  var updatePayload = null;

  var lastProps;
  var nextProps;
  switch (tag) {
    case 'input':
      lastProps = getHostProps(domElement, lastRawProps);
      nextProps = getHostProps(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'option':
      lastProps = getHostProps$1(domElement, lastRawProps);
      nextProps = getHostProps$1(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'select':
      lastProps = getHostProps$2(domElement, lastRawProps);
      nextProps = getHostProps$2(domElement, nextRawProps);
      updatePayload = [];
      break;
    case 'textarea':
      lastProps = getHostProps$3(domElement, lastRawProps);
      nextProps = getHostProps$3(domElement, nextRawProps);
      updatePayload = [];
      break;
    default:
      lastProps = lastRawProps;
      nextProps = nextRawProps;
      if (typeof lastProps.onClick !== 'function' && typeof nextProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }

  assertValidProps(tag, nextProps, getStack);

  var propKey;
  var styleName;
  var styleUpdates = null;
  for (propKey in lastProps) {
    if (nextProps.hasOwnProperty(propKey) || !lastProps.hasOwnProperty(propKey) || lastProps[propKey] == null) {
      continue;
    }
    if (propKey === STYLE) {
      var lastStyle = lastProps[propKey];
      for (styleName in lastStyle) {
        if (lastStyle.hasOwnProperty(styleName)) {
          if (!styleUpdates) {
            styleUpdates = {};
          }
          styleUpdates[styleName] = '';
        }
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML || propKey === CHILDREN) {
      // Noop. This is handled by the clear text mechanism.
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (propKey === AUTOFOCUS) {
      // Noop. It doesn't work on updates anyway.
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      // This is a special case. If any listener updates we need to ensure
      // that the "current" fiber pointer gets updated so we need a commit
      // to update this element.
      if (!updatePayload) {
        updatePayload = [];
      }
    } else {
      // For all other deleted properties we add it to the queue. We use
      // the whitelist in the commit phase instead.
      (updatePayload = updatePayload || []).push(propKey, null);
    }
  }
  for (propKey in nextProps) {
    var nextProp = nextProps[propKey];
    var lastProp = lastProps != null ? lastProps[propKey] : undefined;
    if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp || nextProp == null && lastProp == null) {
      continue;
    }
    if (propKey === STYLE) {
      {
        if (nextProp) {
          // Freeze the next style object so that we can assume it won't be
          // mutated. We have already warned for this in the past.
          Object.freeze(nextProp);
        }
      }
      if (lastProp) {
        // Unset styles on `lastProp` but not on `nextProp`.
        for (styleName in lastProp) {
          if (lastProp.hasOwnProperty(styleName) && (!nextProp || !nextProp.hasOwnProperty(styleName))) {
            if (!styleUpdates) {
              styleUpdates = {};
            }
            styleUpdates[styleName] = '';
          }
        }
        // Update styles that changed since `lastProp`.
        for (styleName in nextProp) {
          if (nextProp.hasOwnProperty(styleName) && lastProp[styleName] !== nextProp[styleName]) {
            if (!styleUpdates) {
              styleUpdates = {};
            }
            styleUpdates[styleName] = nextProp[styleName];
          }
        }
      } else {
        // Relies on `updateStylesByID` not mutating `styleUpdates`.
        if (!styleUpdates) {
          if (!updatePayload) {
            updatePayload = [];
          }
          updatePayload.push(propKey, styleUpdates);
        }
        styleUpdates = nextProp;
      }
    } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
      var nextHtml = nextProp ? nextProp[HTML] : undefined;
      var lastHtml = lastProp ? lastProp[HTML] : undefined;
      if (nextHtml != null) {
        if (lastHtml !== nextHtml) {
          (updatePayload = updatePayload || []).push(propKey, '' + nextHtml);
        }
      } else {
        // TODO: It might be too late to clear this if we have children
        // inserted already.
      }
    } else if (propKey === CHILDREN) {
      if (lastProp !== nextProp && (typeof nextProp === 'string' || typeof nextProp === 'number')) {
        (updatePayload = updatePayload || []).push(propKey, '' + nextProp);
      }
    } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1) {
      // Noop
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        // We eagerly listen to this even though we haven't committed yet.
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
      if (!updatePayload && lastProp !== nextProp) {
        // This is a special case. If any listener updates we need to ensure
        // that the "current" props pointer gets updated so we need a commit
        // to update this element.
        updatePayload = [];
      }
    } else {
      // For any other property we always add it to the queue and then we
      // filter it out using the whitelist during the commit.
      (updatePayload = updatePayload || []).push(propKey, nextProp);
    }
  }
  if (styleUpdates) {
    (updatePayload = updatePayload || []).push(STYLE, styleUpdates);
  }
  return updatePayload;
}

// Apply the diff.
function updateProperties$1(domElement, updatePayload, tag, lastRawProps, nextRawProps) {
  // Update checked *before* name.
  // In the middle of an update, it is possible to have multiple checked.
  // When a checked radio tries to change name, browser makes another radio's checked false.
  if (tag === 'input' && nextRawProps.type === 'radio' && nextRawProps.name != null) {
    updateChecked(domElement, nextRawProps);
  }

  var wasCustomComponentTag = isCustomComponent(tag, lastRawProps);
  var isCustomComponentTag = isCustomComponent(tag, nextRawProps);
  // Apply the diff.
  updateDOMProperties(domElement, updatePayload, wasCustomComponentTag, isCustomComponentTag);

  // TODO: Ensure that an update gets scheduled if any of the special props
  // changed.
  switch (tag) {
    case 'input':
      // Update the wrapper around inputs *after* updating props. This has to
      // happen after `updateDOMProperties`. Otherwise HTML5 input validations
      // raise warnings and prevent the new value from being assigned.
      updateWrapper(domElement, nextRawProps);
      break;
    case 'textarea':
      updateWrapper$1(domElement, nextRawProps);
      break;
    case 'select':
      // <select> value update needs to occur after <option> children
      // reconciliation
      postUpdateWrapper(domElement, nextRawProps);
      break;
  }
}

function diffHydratedProperties$1(domElement, tag, rawProps, parentNamespace, rootContainerElement) {
  {
    var suppressHydrationWarning = rawProps[SUPPRESS_HYDRATION_WARNING$1] === true;
    var isCustomComponentTag = isCustomComponent(tag, rawProps);
    validatePropertiesInDevelopment(tag, rawProps);
    if (isCustomComponentTag && !didWarnShadyDOM && domElement.shadyRoot) {
      warning(false, '%s is using shady DOM. Using shady DOM with React can ' + 'cause things to break subtly.', getCurrentFiberOwnerName$1() || 'A component');
      didWarnShadyDOM = true;
    }
  }

  // TODO: Make sure that we check isMounted before firing any of these events.
  switch (tag) {
    case 'iframe':
    case 'object':
      trapBubbledEvent('topLoad', 'load', domElement);
      break;
    case 'video':
    case 'audio':
      // Create listener for each media event
      for (var event in mediaEvents) {
        if (mediaEvents.hasOwnProperty(event)) {
          trapBubbledEvent(event, mediaEvents[event], domElement);
        }
      }
      break;
    case 'source':
      trapBubbledEvent('topError', 'error', domElement);
      break;
    case 'img':
    case 'image':
      trapBubbledEvent('topError', 'error', domElement);
      trapBubbledEvent('topLoad', 'load', domElement);
      break;
    case 'form':
      trapBubbledEvent('topReset', 'reset', domElement);
      trapBubbledEvent('topSubmit', 'submit', domElement);
      break;
    case 'details':
      trapBubbledEvent('topToggle', 'toggle', domElement);
      break;
    case 'input':
      initWrapperState(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'option':
      validateProps(domElement, rawProps);
      break;
    case 'select':
      initWrapperState$1(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
    case 'textarea':
      initWrapperState$2(domElement, rawProps);
      trapBubbledEvent('topInvalid', 'invalid', domElement);
      // For controlled components we always need to ensure we're listening
      // to onChange. Even if there is no listener.
      ensureListeningTo(rootContainerElement, 'onChange');
      break;
  }

  assertValidProps(tag, rawProps, getStack);

  {
    var extraAttributeNames = new Set();
    var attributes = domElement.attributes;
    for (var i = 0; i < attributes.length; i++) {
      var name = attributes[i].name.toLowerCase();
      switch (name) {
        // Built-in SSR attribute is whitelisted
        case 'data-reactroot':
          break;
        // Controlled attributes are not validated
        // TODO: Only ignore them on controlled tags.
        case 'value':
          break;
        case 'checked':
          break;
        case 'selected':
          break;
        default:
          // Intentionally use the original name.
          // See discussion in https://github.com/facebook/react/pull/10676.
          extraAttributeNames.add(attributes[i].name);
      }
    }
  }

  var updatePayload = null;
  for (var propKey in rawProps) {
    if (!rawProps.hasOwnProperty(propKey)) {
      continue;
    }
    var nextProp = rawProps[propKey];
    if (propKey === CHILDREN) {
      // For text content children we compare against textContent. This
      // might match additional HTML that is hidden when we read it using
      // textContent. E.g. "foo" will match "f<span>oo</span>" but that still
      // satisfies our requirement. Our requirement is not to produce perfect
      // HTML and attributes. Ideally we should preserve structure but it's
      // ok not to if the visible content is still enough to indicate what
      // even listeners these nodes might be wired up to.
      // TODO: Warn if there is more than a single textNode as a child.
      // TODO: Should we use domElement.firstChild.nodeValue to compare?
      if (typeof nextProp === 'string') {
        if (domElement.textContent !== nextProp) {
          if (true && !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }
          updatePayload = [CHILDREN, nextProp];
        }
      } else if (typeof nextProp === 'number') {
        if (domElement.textContent !== '' + nextProp) {
          if (true && !suppressHydrationWarning) {
            warnForTextDifference(domElement.textContent, nextProp);
          }
          updatePayload = [CHILDREN, '' + nextProp];
        }
      }
    } else if (registrationNameModules.hasOwnProperty(propKey)) {
      if (nextProp != null) {
        if (true && typeof nextProp !== 'function') {
          warnForInvalidEventListener(propKey, nextProp);
        }
        ensureListeningTo(rootContainerElement, propKey);
      }
    } else {
      // Validate that the properties correspond to their expected values.
      var serverValue;
      var propertyInfo;
      if (suppressHydrationWarning) {
        // Don't bother comparing. We're ignoring all these warnings.
      } else if (propKey === SUPPRESS_CONTENT_EDITABLE_WARNING || propKey === SUPPRESS_HYDRATION_WARNING$1 ||
      // Controlled attributes are not validated
      // TODO: Only ignore them on controlled tags.
      propKey === 'value' || propKey === 'checked' || propKey === 'selected') {
        // Noop
      } else if (propKey === DANGEROUSLY_SET_INNER_HTML) {
        var rawHtml = nextProp ? nextProp[HTML] || '' : '';
        var serverHTML = domElement.innerHTML;
        var expectedHTML = normalizeHTML(domElement, rawHtml);
        if (expectedHTML !== serverHTML) {
          warnForPropDifference(propKey, serverHTML, expectedHTML);
        }
      } else if (propKey === STYLE) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames['delete'](propKey);
        var expectedStyle = createDangerousStringForStyles(nextProp);
        serverValue = domElement.getAttribute('style');
        if (expectedStyle !== serverValue) {
          warnForPropDifference(propKey, serverValue, expectedStyle);
        }
      } else if (isCustomComponentTag) {
        // $FlowFixMe - Should be inferred as not undefined.
        extraAttributeNames['delete'](propKey.toLowerCase());
        serverValue = getValueForAttribute(domElement, propKey, nextProp);

        if (nextProp !== serverValue) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      } else if (shouldSetAttribute(propKey, nextProp)) {
        if (propertyInfo = getPropertyInfo(propKey)) {
          // $FlowFixMe - Should be inferred as not undefined.
          extraAttributeNames['delete'](propertyInfo.attributeName);
          serverValue = getValueForProperty(domElement, propKey, nextProp);
        } else {
          var ownNamespace = parentNamespace;
          if (ownNamespace === HTML_NAMESPACE) {
            ownNamespace = getIntrinsicNamespace(tag);
          }
          if (ownNamespace === HTML_NAMESPACE) {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames['delete'](propKey.toLowerCase());
          } else {
            // $FlowFixMe - Should be inferred as not undefined.
            extraAttributeNames['delete'](propKey);
          }
          serverValue = getValueForAttribute(domElement, propKey, nextProp);
        }

        if (nextProp !== serverValue) {
          warnForPropDifference(propKey, serverValue, nextProp);
        }
      }
    }
  }

  {
    // $FlowFixMe - Should be inferred as not undefined.
    if (extraAttributeNames.size > 0 && !suppressHydrationWarning) {
      // $FlowFixMe - Should be inferred as not undefined.
      warnForExtraAttributes(extraAttributeNames);
    }
  }

  switch (tag) {
    case 'input':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper(domElement, rawProps);
      break;
    case 'textarea':
      // TODO: Make sure we check if this is still unmounted or do any clean
      // up necessary since we never stop tracking anymore.
      track(domElement);
      postMountWrapper$3(domElement, rawProps);
      break;
    case 'select':
    case 'option':
      // For input and textarea we current always set the value property at
      // post mount to force it to diverge from attributes. However, for
      // option and select we don't quite do the same thing and select
      // is not resilient to the DOM state changing so we don't do that here.
      // TODO: Consider not doing this for input and textarea.
      break;
    default:
      if (typeof rawProps.onClick === 'function') {
        // TODO: This cast may not be sound for SVG, MathML or custom elements.
        trapClickOnNonInteractiveElement(domElement);
      }
      break;
  }

  return updatePayload;
}

function diffHydratedText$1(textNode, text) {
  var isDifferent = textNode.nodeValue !== text;
  return isDifferent;
}

function warnForUnmatchedText$1(textNode, text) {
  {
    warnForTextDifference(textNode.nodeValue, text);
  }
}

function warnForDeletedHydratableElement$1(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Did not expect server HTML to contain a <%s> in <%s>.', child.nodeName.toLowerCase(), parentNode.nodeName.toLowerCase());
  }
}

function warnForDeletedHydratableText$1(parentNode, child) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Did not expect server HTML to contain the text node "%s" in <%s>.', child.nodeValue, parentNode.nodeName.toLowerCase());
  }
}

function warnForInsertedHydratedElement$1(parentNode, tag, props) {
  {
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Expected server HTML to contain a matching <%s> in <%s>.', tag, parentNode.nodeName.toLowerCase());
  }
}

function warnForInsertedHydratedText$1(parentNode, text) {
  {
    if (text === '') {
      // We expect to insert empty text nodes since they're not represented in
      // the HTML.
      // TODO: Remove this special case if we can just avoid inserting empty
      // text nodes.
      return;
    }
    if (didWarnInvalidHydration) {
      return;
    }
    didWarnInvalidHydration = true;
    warning(false, 'Expected server HTML to contain a matching text node for "%s" in <%s>.', text, parentNode.nodeName.toLowerCase());
  }
}

function restoreControlledState(domElement, tag, props) {
  switch (tag) {
    case 'input':
      restoreControlledState$1(domElement, props);
      return;
    case 'textarea':
      restoreControlledState$3(domElement, props);
      return;
    case 'select':
      restoreControlledState$2(domElement, props);
      return;
  }
}

var ReactDOMFiberComponent = Object.freeze({
	createElement: createElement$1,
	createTextNode: createTextNode$1,
	setInitialProperties: setInitialProperties$1,
	diffProperties: diffProperties$1,
	updateProperties: updateProperties$1,
	diffHydratedProperties: diffHydratedProperties$1,
	diffHydratedText: diffHydratedText$1,
	warnForUnmatchedText: warnForUnmatchedText$1,
	warnForDeletedHydratableElement: warnForDeletedHydratableElement$1,
	warnForDeletedHydratableText: warnForDeletedHydratableText$1,
	warnForInsertedHydratedElement: warnForInsertedHydratedElement$1,
	warnForInsertedHydratedText: warnForInsertedHydratedText$1,
	restoreControlledState: restoreControlledState
});

// TODO: direct imports like some-package/src/* are bad. Fix me.
var getCurrentFiberStackAddendum$6 = ReactDebugCurrentFiber.getCurrentFiberStackAddendum;

var validateDOMNesting = emptyFunction;

{
  // This validation code was written based on the HTML5 parsing spec:
  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  //
  // Note: this does not catch all invalid nesting, nor does it try to (as it's
  // not clear what practical benefit doing so provides); instead, we warn only
  // for cases where the parser will give a parse tree differing from what React
  // intended. For example, <b><div></div></b> is invalid but we don't warn
  // because it still parses correctly; we do warn for other cases like nested
  // <p> tags where the beginning of the second element implicitly closes the
  // first, causing a confusing mess.

  // https://html.spec.whatwg.org/multipage/syntax.html#special
  var specialTags = ['address', 'applet', 'area', 'article', 'aside', 'base', 'basefont', 'bgsound', 'blockquote', 'body', 'br', 'button', 'caption', 'center', 'col', 'colgroup', 'dd', 'details', 'dir', 'div', 'dl', 'dt', 'embed', 'fieldset', 'figcaption', 'figure', 'footer', 'form', 'frame', 'frameset', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'iframe', 'img', 'input', 'isindex', 'li', 'link', 'listing', 'main', 'marquee', 'menu', 'menuitem', 'meta', 'nav', 'noembed', 'noframes', 'noscript', 'object', 'ol', 'p', 'param', 'plaintext', 'pre', 'script', 'section', 'select', 'source', 'style', 'summary', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'title', 'tr', 'track', 'ul', 'wbr', 'xmp'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-scope
  var inScopeTags = ['applet', 'caption', 'html', 'table', 'td', 'th', 'marquee', 'object', 'template',

  // https://html.spec.whatwg.org/multipage/syntax.html#html-integration-point
  // TODO: Distinguish by namespace here -- for <title>, including it here
  // errs on the side of fewer warnings
  'foreignObject', 'desc', 'title'];

  // https://html.spec.whatwg.org/multipage/syntax.html#has-an-element-in-button-scope
  var buttonScopeTags = inScopeTags.concat(['button']);

  // https://html.spec.whatwg.org/multipage/syntax.html#generate-implied-end-tags
  var impliedEndTags = ['dd', 'dt', 'li', 'option', 'optgroup', 'p', 'rp', 'rt'];

  var emptyAncestorInfo = {
    current: null,

    formTag: null,
    aTagInScope: null,
    buttonTagInScope: null,
    nobrTagInScope: null,
    pTagInButtonScope: null,

    listItemTagAutoclosing: null,
    dlItemTagAutoclosing: null
  };

  var updatedAncestorInfo$1 = function (oldInfo, tag, instance) {
    var ancestorInfo = _assign({}, oldInfo || emptyAncestorInfo);
    var info = { tag: tag, instance: instance };

    if (inScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.aTagInScope = null;
      ancestorInfo.buttonTagInScope = null;
      ancestorInfo.nobrTagInScope = null;
    }
    if (buttonScopeTags.indexOf(tag) !== -1) {
      ancestorInfo.pTagInButtonScope = null;
    }

    // See rules for 'li', 'dd', 'dt' start tags in
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    if (specialTags.indexOf(tag) !== -1 && tag !== 'address' && tag !== 'div' && tag !== 'p') {
      ancestorInfo.listItemTagAutoclosing = null;
      ancestorInfo.dlItemTagAutoclosing = null;
    }

    ancestorInfo.current = info;

    if (tag === 'form') {
      ancestorInfo.formTag = info;
    }
    if (tag === 'a') {
      ancestorInfo.aTagInScope = info;
    }
    if (tag === 'button') {
      ancestorInfo.buttonTagInScope = info;
    }
    if (tag === 'nobr') {
      ancestorInfo.nobrTagInScope = info;
    }
    if (tag === 'p') {
      ancestorInfo.pTagInButtonScope = info;
    }
    if (tag === 'li') {
      ancestorInfo.listItemTagAutoclosing = info;
    }
    if (tag === 'dd' || tag === 'dt') {
      ancestorInfo.dlItemTagAutoclosing = info;
    }

    return ancestorInfo;
  };

  /**
   * Returns whether
   */
  var isTagValidWithParent = function (tag, parentTag) {
    // First, let's check if we're in an unusual parsing mode...
    switch (parentTag) {
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inselect
      case 'select':
        return tag === 'option' || tag === 'optgroup' || tag === '#text';
      case 'optgroup':
        return tag === 'option' || tag === '#text';
      // Strictly speaking, seeing an <option> doesn't mean we're in a <select>
      // but
      case 'option':
        return tag === '#text';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intd
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incaption
      // No special behavior since these rules fall back to "in body" mode for
      // all except special table nodes which cause bad parsing behavior anyway.

      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intr
      case 'tr':
        return tag === 'th' || tag === 'td' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intbody
      case 'tbody':
      case 'thead':
      case 'tfoot':
        return tag === 'tr' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-incolgroup
      case 'colgroup':
        return tag === 'col' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-intable
      case 'table':
        return tag === 'caption' || tag === 'colgroup' || tag === 'tbody' || tag === 'tfoot' || tag === 'thead' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inhead
      case 'head':
        return tag === 'base' || tag === 'basefont' || tag === 'bgsound' || tag === 'link' || tag === 'meta' || tag === 'title' || tag === 'noscript' || tag === 'noframes' || tag === 'style' || tag === 'script' || tag === 'template';
      // https://html.spec.whatwg.org/multipage/semantics.html#the-html-element
      case 'html':
        return tag === 'head' || tag === 'body';
      case '#document':
        return tag === 'html';
    }

    // Probably in the "in body" parsing mode, so we outlaw only tag combos
    // where the parsing rules cause implicit opens or closes to be added.
    // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody
    switch (tag) {
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return parentTag !== 'h1' && parentTag !== 'h2' && parentTag !== 'h3' && parentTag !== 'h4' && parentTag !== 'h5' && parentTag !== 'h6';

      case 'rp':
      case 'rt':
        return impliedEndTags.indexOf(parentTag) === -1;

      case 'body':
      case 'caption':
      case 'col':
      case 'colgroup':
      case 'frame':
      case 'head':
      case 'html':
      case 'tbody':
      case 'td':
      case 'tfoot':
      case 'th':
      case 'thead':
      case 'tr':
        // These tags are only valid with a few parents that have special child
        // parsing rules -- if we're down here, then none of those matched and
        // so we allow it only if we don't know what the parent is, as all other
        // cases are invalid.
        return parentTag == null;
    }

    return true;
  };

  /**
   * Returns whether
   */
  var findInvalidAncestorForTag = function (tag, ancestorInfo) {
    switch (tag) {
      case 'address':
      case 'article':
      case 'aside':
      case 'blockquote':
      case 'center':
      case 'details':
      case 'dialog':
      case 'dir':
      case 'div':
      case 'dl':
      case 'fieldset':
      case 'figcaption':
      case 'figure':
      case 'footer':
      case 'header':
      case 'hgroup':
      case 'main':
      case 'menu':
      case 'nav':
      case 'ol':
      case 'p':
      case 'section':
      case 'summary':
      case 'ul':
      case 'pre':
      case 'listing':
      case 'table':
      case 'hr':
      case 'xmp':
      case 'h1':
      case 'h2':
      case 'h3':
      case 'h4':
      case 'h5':
      case 'h6':
        return ancestorInfo.pTagInButtonScope;

      case 'form':
        return ancestorInfo.formTag || ancestorInfo.pTagInButtonScope;

      case 'li':
        return ancestorInfo.listItemTagAutoclosing;

      case 'dd':
      case 'dt':
        return ancestorInfo.dlItemTagAutoclosing;

      case 'button':
        return ancestorInfo.buttonTagInScope;

      case 'a':
        // Spec says something about storing a list of markers, but it sounds
        // equivalent to this check.
        return ancestorInfo.aTagInScope;

      case 'nobr':
        return ancestorInfo.nobrTagInScope;
    }

    return null;
  };

  var didWarn = {};

  validateDOMNesting = function (childTag, childText, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.current;
    var parentTag = parentInfo && parentInfo.tag;

    if (childText != null) {
      warning(childTag == null, 'validateDOMNesting: when childText is passed, childTag should be null');
      childTag = '#text';
    }

    var invalidParent = isTagValidWithParent(childTag, parentTag) ? null : parentInfo;
    var invalidAncestor = invalidParent ? null : findInvalidAncestorForTag(childTag, ancestorInfo);
    var invalidParentOrAncestor = invalidParent || invalidAncestor;
    if (!invalidParentOrAncestor) {
      return;
    }

    var ancestorTag = invalidParentOrAncestor.tag;
    var addendum = getCurrentFiberStackAddendum$6();

    var warnKey = !!invalidParent + '|' + childTag + '|' + ancestorTag + '|' + addendum;
    if (didWarn[warnKey]) {
      return;
    }
    didWarn[warnKey] = true;

    var tagDisplayName = childTag;
    var whitespaceInfo = '';
    if (childTag === '#text') {
      if (/\S/.test(childText)) {
        tagDisplayName = 'Text nodes';
      } else {
        tagDisplayName = 'Whitespace text nodes';
        whitespaceInfo = " Make sure you don't have any extra whitespace between tags on " + 'each line of your source code.';
      }
    } else {
      tagDisplayName = '<' + childTag + '>';
    }

    if (invalidParent) {
      var info = '';
      if (ancestorTag === 'table' && childTag === 'tr') {
        info += ' Add a <tbody> to your code to match the DOM tree generated by ' + 'the browser.';
      }
      warning(false, 'validateDOMNesting(...): %s cannot appear as a child of <%s>.%s%s%s', tagDisplayName, ancestorTag, whitespaceInfo, info, addendum);
    } else {
      warning(false, 'validateDOMNesting(...): %s cannot appear as a descendant of ' + '<%s>.%s', tagDisplayName, ancestorTag, addendum);
    }
  };

  // TODO: turn this into a named export
  validateDOMNesting.updatedAncestorInfo = updatedAncestorInfo$1;

  // For testing
  validateDOMNesting.isTagValidInContext = function (tag, ancestorInfo) {
    ancestorInfo = ancestorInfo || emptyAncestorInfo;
    var parentInfo = ancestorInfo.current;
    var parentTag = parentInfo && parentInfo.tag;
    return isTagValidWithParent(tag, parentTag) && !findInvalidAncestorForTag(tag, ancestorInfo);
  };
}

var validateDOMNesting$1 = validateDOMNesting;

// TODO: direct imports like some-package/src/* are bad. Fix me.
var createElement = createElement$1;
var createTextNode = createTextNode$1;
var setInitialProperties = setInitialProperties$1;
var diffProperties = diffProperties$1;
var updateProperties = updateProperties$1;
var diffHydratedProperties = diffHydratedProperties$1;
var diffHydratedText = diffHydratedText$1;
var warnForUnmatchedText = warnForUnmatchedText$1;
var warnForDeletedHydratableElement = warnForDeletedHydratableElement$1;
var warnForDeletedHydratableText = warnForDeletedHydratableText$1;
var warnForInsertedHydratedElement = warnForInsertedHydratedElement$1;
var warnForInsertedHydratedText = warnForInsertedHydratedText$1;
var updatedAncestorInfo = validateDOMNesting$1.updatedAncestorInfo;
var precacheFiberNode = precacheFiberNode$1;
var updateFiberProps = updateFiberProps$1;


{
  var SUPPRESS_HYDRATION_WARNING = 'suppressHydrationWarning';
  if (typeof Map !== 'function' || Map.prototype == null || typeof Map.prototype.forEach !== 'function' || typeof Set !== 'function' || Set.prototype == null || typeof Set.prototype.clear !== 'function' || typeof Set.prototype.forEach !== 'function') {
    warning(false, 'React depends on Map and Set built-in types. Make sure that you load a ' + 'polyfill in older browsers. http://fb.me/react-polyfills');
  }
}

injection$3.injectFiberControlledHostComponent(ReactDOMFiberComponent);

var eventsEnabled = null;
var selectionInformation = null;

/**
 * True if the supplied DOM node is a valid node element.
 *
 * @param {?DOMElement} node The candidate DOM node.
 * @return {boolean} True if the DOM is a valid DOM node.
 * @internal
 */
function isValidContainer(node) {
  return !!(node && (node.nodeType === ELEMENT_NODE || node.nodeType === DOCUMENT_NODE || node.nodeType === DOCUMENT_FRAGMENT_NODE || node.nodeType === COMMENT_NODE && node.nodeValue === ' react-mount-point-unstable '));
}

function getReactRootElementInContainer(container) {
  if (!container) {
    return null;
  }

  if (container.nodeType === DOCUMENT_NODE) {
    return container.documentElement;
  } else {
    return container.firstChild;
  }
}

function shouldHydrateDueToLegacyHeuristic(container) {
  var rootElement = getReactRootElementInContainer(container);
  return !!(rootElement && rootElement.nodeType === ELEMENT_NODE && rootElement.hasAttribute(ROOT_ATTRIBUTE_NAME));
}

function shouldAutoFocusHostComponent(type, props) {
  switch (type) {
    case 'button':
    case 'input':
    case 'select':
    case 'textarea':
      return !!props.autoFocus;
  }
  return false;
}

var DOMRenderer = reactReconciler({
  getRootHostContext: function (rootContainerInstance) {
    var type = void 0;
    var namespace = void 0;
    var nodeType = rootContainerInstance.nodeType;
    switch (nodeType) {
      case DOCUMENT_NODE:
      case DOCUMENT_FRAGMENT_NODE:
        {
          type = nodeType === DOCUMENT_NODE ? '#document' : '#fragment';
          var root = rootContainerInstance.documentElement;
          namespace = root ? root.namespaceURI : getChildNamespace(null, '');
          break;
        }
      default:
        {
          var container = nodeType === COMMENT_NODE ? rootContainerInstance.parentNode : rootContainerInstance;
          var ownNamespace = container.namespaceURI || null;
          type = container.tagName;
          namespace = getChildNamespace(ownNamespace, type);
          break;
        }
    }
    {
      var validatedTag = type.toLowerCase();
      var _ancestorInfo = updatedAncestorInfo(null, validatedTag, null);
      return { namespace: namespace, ancestorInfo: _ancestorInfo };
    }
    return namespace;
  },
  getChildHostContext: function (parentHostContext, type) {
    {
      var parentHostContextDev = parentHostContext;
      var _namespace = getChildNamespace(parentHostContextDev.namespace, type);
      var _ancestorInfo2 = updatedAncestorInfo(parentHostContextDev.ancestorInfo, type, null);
      return { namespace: _namespace, ancestorInfo: _ancestorInfo2 };
    }
    var parentNamespace = parentHostContext;
    return getChildNamespace(parentNamespace, type);
  },
  getPublicInstance: function (instance) {
    return instance;
  },
  prepareForCommit: function () {
    eventsEnabled = isEnabled();
    selectionInformation = getSelectionInformation();
    setEnabled(false);
  },
  resetAfterCommit: function () {
    restoreSelection(selectionInformation);
    selectionInformation = null;
    setEnabled(eventsEnabled);
    eventsEnabled = null;
  },
  createInstance: function (type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
    var parentNamespace = void 0;
    {
      // TODO: take namespace into account when validating.
      var hostContextDev = hostContext;
      validateDOMNesting$1(type, null, hostContextDev.ancestorInfo);
      if (typeof props.children === 'string' || typeof props.children === 'number') {
        var string = '' + props.children;
        var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type, null);
        validateDOMNesting$1(null, string, ownAncestorInfo);
      }
      parentNamespace = hostContextDev.namespace;
    }
    var domElement = createElement(type, props, rootContainerInstance, parentNamespace);
    precacheFiberNode(internalInstanceHandle, domElement);
    updateFiberProps(domElement, props);
    return domElement;
  },
  appendInitialChild: function (parentInstance, child) {
    parentInstance.appendChild(child);
  },
  finalizeInitialChildren: function (domElement, type, props, rootContainerInstance) {
    setInitialProperties(domElement, type, props, rootContainerInstance);
    return shouldAutoFocusHostComponent(type, props);
  },
  prepareUpdate: function (domElement, type, oldProps, newProps, rootContainerInstance, hostContext) {
    {
      var hostContextDev = hostContext;
      if (typeof newProps.children !== typeof oldProps.children && (typeof newProps.children === 'string' || typeof newProps.children === 'number')) {
        var string = '' + newProps.children;
        var ownAncestorInfo = updatedAncestorInfo(hostContextDev.ancestorInfo, type, null);
        validateDOMNesting$1(null, string, ownAncestorInfo);
      }
    }
    return diffProperties(domElement, type, oldProps, newProps, rootContainerInstance);
  },
  shouldSetTextContent: function (type, props) {
    return type === 'textarea' || typeof props.children === 'string' || typeof props.children === 'number' || typeof props.dangerouslySetInnerHTML === 'object' && props.dangerouslySetInnerHTML !== null && typeof props.dangerouslySetInnerHTML.__html === 'string';
  },
  shouldDeprioritizeSubtree: function (type, props) {
    return !!props.hidden;
  },
  createTextInstance: function (text, rootContainerInstance, hostContext, internalInstanceHandle) {
    {
      var hostContextDev = hostContext;
      validateDOMNesting$1(null, text, hostContextDev.ancestorInfo);
    }
    var textNode = createTextNode(text, rootContainerInstance);
    precacheFiberNode(internalInstanceHandle, textNode);
    return textNode;
  },


  now: now,

  mutation: {
    commitMount: function (domElement, type, newProps, internalInstanceHandle) {
      domElement.focus();
    },
    commitUpdate: function (domElement, updatePayload, type, oldProps, newProps, internalInstanceHandle) {
      // Update the props handle so that we know which props are the ones with
      // with current event handlers.
      updateFiberProps(domElement, newProps);
      // Apply the diff to the DOM node.
      updateProperties(domElement, updatePayload, type, oldProps, newProps);
    },
    resetTextContent: function (domElement) {
      domElement.textContent = '';
    },
    commitTextUpdate: function (textInstance, oldText, newText) {
      textInstance.nodeValue = newText;
    },
    appendChild: function (parentInstance, child) {
      parentInstance.appendChild(child);
    },
    appendChildToContainer: function (container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, container);
      } else {
        container.appendChild(child);
      }
    },
    insertBefore: function (parentInstance, child, beforeChild) {
      parentInstance.insertBefore(child, beforeChild);
    },
    insertInContainerBefore: function (container, child, beforeChild) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.insertBefore(child, beforeChild);
      } else {
        container.insertBefore(child, beforeChild);
      }
    },
    removeChild: function (parentInstance, child) {
      parentInstance.removeChild(child);
    },
    removeChildFromContainer: function (container, child) {
      if (container.nodeType === COMMENT_NODE) {
        container.parentNode.removeChild(child);
      } else {
        container.removeChild(child);
      }
    }
  },

  hydration: {
    canHydrateInstance: function (instance, type, props) {
      if (instance.nodeType !== ELEMENT_NODE || type.toLowerCase() !== instance.nodeName.toLowerCase()) {
        return null;
      }
      // This has now been refined to an element node.
      return instance;
    },
    canHydrateTextInstance: function (instance, text) {
      if (text === '' || instance.nodeType !== TEXT_NODE) {
        // Empty strings are not parsed by HTML so there won't be a correct match here.
        return null;
      }
      // This has now been refined to a text node.
      return instance;
    },
    getNextHydratableSibling: function (instance) {
      var node = instance.nextSibling;
      // Skip non-hydratable nodes.
      while (node && node.nodeType !== ELEMENT_NODE && node.nodeType !== TEXT_NODE) {
        node = node.nextSibling;
      }
      return node;
    },
    getFirstHydratableChild: function (parentInstance) {
      var next = parentInstance.firstChild;
      // Skip non-hydratable nodes.
      while (next && next.nodeType !== ELEMENT_NODE && next.nodeType !== TEXT_NODE) {
        next = next.nextSibling;
      }
      return next;
    },
    hydrateInstance: function (instance, type, props, rootContainerInstance, hostContext, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, instance);
      // TODO: Possibly defer this until the commit phase where all the events
      // get attached.
      updateFiberProps(instance, props);
      var parentNamespace = void 0;
      {
        var hostContextDev = hostContext;
        parentNamespace = hostContextDev.namespace;
      }
      return diffHydratedProperties(instance, type, props, parentNamespace, rootContainerInstance);
    },
    hydrateTextInstance: function (textInstance, text, internalInstanceHandle) {
      precacheFiberNode(internalInstanceHandle, textInstance);
      return diffHydratedText(textInstance, text);
    },
    didNotMatchHydratedContainerTextInstance: function (parentContainer, textInstance, text) {
      {
        warnForUnmatchedText(textInstance, text);
      }
    },
    didNotMatchHydratedTextInstance: function (parentType, parentProps, parentInstance, textInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForUnmatchedText(textInstance, text);
      }
    },
    didNotHydrateContainerInstance: function (parentContainer, instance) {
      {
        if (instance.nodeType === 1) {
          warnForDeletedHydratableElement(parentContainer, instance);
        } else {
          warnForDeletedHydratableText(parentContainer, instance);
        }
      }
    },
    didNotHydrateInstance: function (parentType, parentProps, parentInstance, instance) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        if (instance.nodeType === 1) {
          warnForDeletedHydratableElement(parentInstance, instance);
        } else {
          warnForDeletedHydratableText(parentInstance, instance);
        }
      }
    },
    didNotFindHydratableContainerInstance: function (parentContainer, type, props) {
      {
        warnForInsertedHydratedElement(parentContainer, type, props);
      }
    },
    didNotFindHydratableContainerTextInstance: function (parentContainer, text) {
      {
        warnForInsertedHydratedText(parentContainer, text);
      }
    },
    didNotFindHydratableInstance: function (parentType, parentProps, parentInstance, type, props) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedElement(parentInstance, type, props);
      }
    },
    didNotFindHydratableTextInstance: function (parentType, parentProps, parentInstance, text) {
      if (true && parentProps[SUPPRESS_HYDRATION_WARNING] !== true) {
        warnForInsertedHydratedText(parentInstance, text);
      }
    }
  },

  scheduleDeferredCallback: rIC,
  cancelDeferredCallback: cIC,

  useSyncScheduling: !enableAsyncSchedulingByDefaultInReactDOM
});

injection$4.injectFiberBatchedUpdates(DOMRenderer.batchedUpdates);

var warnedAboutHydrateAPI = false;

function renderSubtreeIntoContainer(parentComponent, children, container, forceHydrate, callback) {
  !isValidContainer(container) ? invariant(false, 'Target container is not a DOM element.') : void 0;

  {
    if (container._reactRootContainer && container.nodeType !== COMMENT_NODE) {
      var hostInstance = DOMRenderer.findHostInstanceWithNoPortals(container._reactRootContainer.current);
      if (hostInstance) {
        warning(hostInstance.parentNode === container, 'render(...): It looks like the React-rendered content of this ' + 'container was removed without using React. This is not ' + 'supported and will cause errors. Instead, call ' + 'ReactDOM.unmountComponentAtNode to empty a container.');
      }
    }

    var isRootRenderedBySomeReact = !!container._reactRootContainer;
    var rootEl = getReactRootElementInContainer(container);
    var hasNonRootReactChild = !!(rootEl && getInstanceFromNode$1(rootEl));

    warning(!hasNonRootReactChild || isRootRenderedBySomeReact, 'render(...): Replacing React-rendered children with a new root ' + 'component. If you intended to update the children of this node, ' + 'you should instead have the existing children update their state ' + 'and render the new components instead of calling ReactDOM.render.');

    warning(container.nodeType !== ELEMENT_NODE || !container.tagName || container.tagName.toUpperCase() !== 'BODY', 'render(): Rendering components directly into document.body is ' + 'discouraged, since its children are often manipulated by third-party ' + 'scripts and browser extensions. This may lead to subtle ' + 'reconciliation issues. Try rendering into a container element created ' + 'for your app.');
  }

  var root = container._reactRootContainer;
  if (!root) {
    var shouldHydrate = forceHydrate || shouldHydrateDueToLegacyHeuristic(container);
    // First clear any existing content.
    if (!shouldHydrate) {
      var warned = false;
      var rootSibling = void 0;
      while (rootSibling = container.lastChild) {
        {
          if (!warned && rootSibling.nodeType === ELEMENT_NODE && rootSibling.hasAttribute(ROOT_ATTRIBUTE_NAME)) {
            warned = true;
            warning(false, 'render(): Target node has markup rendered by React, but there ' + 'are unrelated nodes as well. This is most commonly caused by ' + 'white-space inserted around server-rendered markup.');
          }
        }
        container.removeChild(rootSibling);
      }
    }
    {
      if (shouldHydrate && !forceHydrate && !warnedAboutHydrateAPI) {
        warnedAboutHydrateAPI = true;
        lowPriorityWarning$1(false, 'render(): Calling ReactDOM.render() to hydrate server-rendered markup ' + 'will stop working in React v17. Replace the ReactDOM.render() call ' + 'with ReactDOM.hydrate() if you want React to attach to the server HTML.');
      }
    }
    var newRoot = DOMRenderer.createContainer(container, shouldHydrate);
    root = container._reactRootContainer = newRoot;
    // Initial mount should not be batched.
    DOMRenderer.unbatchedUpdates(function () {
      DOMRenderer.updateContainer(children, newRoot, parentComponent, callback);
    });
  } else {
    DOMRenderer.updateContainer(children, root, parentComponent, callback);
  }
  return DOMRenderer.getPublicRootInstance(root);
}

function createPortal(children, container) {
  var key = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : null;

  !isValidContainer(container) ? invariant(false, 'Target container is not a DOM element.') : void 0;
  // TODO: pass ReactDOM portal implementation as third argument
  return createPortal$1(children, container, null, key);
}

function ReactRoot(container, hydrate) {
  var root = DOMRenderer.createContainer(container, hydrate);
  this._reactRootContainer = root;
}
ReactRoot.prototype.render = function (children, callback) {
  var root = this._reactRootContainer;
  DOMRenderer.updateContainer(children, root, null, callback);
};
ReactRoot.prototype.unmount = function (callback) {
  var root = this._reactRootContainer;
  DOMRenderer.updateContainer(null, root, null, callback);
};

var ReactDOM = {
  createPortal: createPortal,

  findDOMNode: function (componentOrElement) {
    {
      var owner = ReactCurrentOwner.current;
      if (owner !== null) {
        var warnedAboutRefsInRender = owner.stateNode._warnedAboutRefsInRender;
        warning(warnedAboutRefsInRender, '%s is accessing findDOMNode inside its render(). ' + 'render() should be a pure function of props and state. It should ' + 'never access something that requires stale data from the previous ' + 'render, such as refs. Move this logic to componentDidMount and ' + 'componentDidUpdate instead.', getComponentName(owner) || 'A component');
        owner.stateNode._warnedAboutRefsInRender = true;
      }
    }
    if (componentOrElement == null) {
      return null;
    }
    if (componentOrElement.nodeType === ELEMENT_NODE) {
      return componentOrElement;
    }

    var inst = get(componentOrElement);
    if (inst) {
      return DOMRenderer.findHostInstance(inst);
    }

    if (typeof componentOrElement.render === 'function') {
      invariant(false, 'Unable to find node on an unmounted component.');
    } else {
      invariant(false, 'Element appears to be neither ReactComponent nor DOMNode. Keys: %s', Object.keys(componentOrElement));
    }
  },
  hydrate: function (element, container, callback) {
    // TODO: throw or warn if we couldn't hydrate?
    return renderSubtreeIntoContainer(null, element, container, true, callback);
  },
  render: function (element, container, callback) {
    return renderSubtreeIntoContainer(null, element, container, false, callback);
  },
  unstable_renderSubtreeIntoContainer: function (parentComponent, element, containerNode, callback) {
    !(parentComponent != null && has(parentComponent)) ? invariant(false, 'parentComponent must be a valid React Component') : void 0;
    return renderSubtreeIntoContainer(parentComponent, element, containerNode, false, callback);
  },
  unmountComponentAtNode: function (container) {
    !isValidContainer(container) ? invariant(false, 'unmountComponentAtNode(...): Target container is not a DOM element.') : void 0;

    if (container._reactRootContainer) {
      {
        var rootEl = getReactRootElementInContainer(container);
        var renderedByDifferentReact = rootEl && !getInstanceFromNode$1(rootEl);
        warning(!renderedByDifferentReact, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by another copy of React.');
      }

      // Unmount should not be batched.
      DOMRenderer.unbatchedUpdates(function () {
        renderSubtreeIntoContainer(null, null, container, false, function () {
          container._reactRootContainer = null;
        });
      });
      // If you call unmountComponentAtNode twice in quick succession, you'll
      // get `true` twice. That's probably fine?
      return true;
    } else {
      {
        var _rootEl = getReactRootElementInContainer(container);
        var hasNonRootReactChild = !!(_rootEl && getInstanceFromNode$1(_rootEl));

        // Check if the container itself is a React root node.
        var isContainerReactRoot = container.nodeType === 1 && isValidContainer(container.parentNode) && !!container.parentNode._reactRootContainer;

        warning(!hasNonRootReactChild, "unmountComponentAtNode(): The node you're attempting to unmount " + 'was rendered by React and is not a top-level container. %s', isContainerReactRoot ? 'You may have accidentally passed in a React root node instead ' + 'of its container.' : 'Instead, have the parent component update its state and ' + 'rerender in order to remove this component.');
      }

      return false;
    }
  },


  // Temporary alias since we already shipped React 16 RC with it.
  // TODO: remove in React 17.
  unstable_createPortal: createPortal,

  unstable_batchedUpdates: batchedUpdates,

  unstable_deferredUpdates: DOMRenderer.deferredUpdates,

  flushSync: DOMRenderer.flushSync,

  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {
    // For TapEventPlugin which is popular in open source
    EventPluginHub: EventPluginHub,
    // Used by test-utils
    EventPluginRegistry: EventPluginRegistry,
    EventPropagators: EventPropagators,
    ReactControlledComponent: ReactControlledComponent,
    ReactDOMComponentTree: ReactDOMComponentTree,
    ReactDOMEventListener: ReactDOMEventListener
  }
};

if (enableCreateRoot) {
  ReactDOM.createRoot = function createRoot(container, options) {
    var hydrate = options != null && options.hydrate === true;
    return new ReactRoot(container, hydrate);
  };
}

var foundDevTools = DOMRenderer.injectIntoDevTools({
  findFiberByHostInstance: getClosestInstanceFromNode,
  bundleType: 1,
  version: ReactVersion,
  rendererPackageName: 'react-dom'
});

{
  if (!foundDevTools && ExecutionEnvironment.canUseDOM && window.top === window.self) {
    // If we're in Chrome or Firefox, provide a download link if not installed.
    if (navigator.userAgent.indexOf('Chrome') > -1 && navigator.userAgent.indexOf('Edge') === -1 || navigator.userAgent.indexOf('Firefox') > -1) {
      var protocol = window.location.protocol;
      // Don't warn in exotic cases like chrome-extension://.
      if (/^(https?|file):$/.test(protocol)) {
        console.info('%cDownload the React DevTools ' + 'for a better development experience: ' + 'https://fb.me/react-devtools' + (protocol === 'file:' ? '\nYou might need to use a local HTTP server (instead of file://): ' + 'https://fb.me/react-devtools-faq' : ''), 'font-weight:bold');
      }
    }
  }
}



var ReactDOM$2 = Object.freeze({
	default: ReactDOM
});

var ReactDOM$3 = ( ReactDOM$2 && ReactDOM ) || ReactDOM$2;

// TODO: decide on the top-level export form.
// This is hacky but makes it work with both Rollup and Jest.
var reactDom = ReactDOM$3['default'] ? ReactDOM$3['default'] : ReactDOM$3;

module.exports = reactDom;
  })();
}

/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(0)))

/***/ }),
/* 25 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */



var hyphenate = __webpack_require__(26);

var msPattern = /^ms-/;

/**
 * Hyphenates a camelcased CSS property name, for example:
 *
 *   > hyphenateStyleName('backgroundColor')
 *   < "background-color"
 *   > hyphenateStyleName('MozTransition')
 *   < "-moz-transition"
 *   > hyphenateStyleName('msTransition')
 *   < "-ms-transition"
 *
 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
 * is converted to `-ms-`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenateStyleName(string) {
  return hyphenate(string).replace(msPattern, '-ms-');
}

module.exports = hyphenateStyleName;

/***/ }),
/* 26 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var _uppercasePattern = /([A-Z])/g;

/**
 * Hyphenates a camelcased string, for example:
 *
 *   > hyphenate('backgroundColor')
 *   < "background-color"
 *
 * For CSS style names, use `hyphenateStyleName` instead which works properly
 * with all vendor prefixes, including `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function hyphenate(string) {
  return string.replace(_uppercasePattern, '-$1').toLowerCase();
}

module.exports = hyphenate;

/***/ }),
/* 27 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */



var camelize = __webpack_require__(28);

var msPattern = /^-ms-/;

/**
 * Camelcases a hyphenated CSS property name, for example:
 *
 *   > camelizeStyleName('background-color')
 *   < "backgroundColor"
 *   > camelizeStyleName('-moz-transition')
 *   < "MozTransition"
 *   > camelizeStyleName('-ms-transition')
 *   < "msTransition"
 *
 * As Andi Smith suggests
 * (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
 * is converted to lowercase `ms`.
 *
 * @param {string} string
 * @return {string}
 */
function camelizeStyleName(string) {
  return camelize(string.replace(msPattern, 'ms-'));
}

module.exports = camelizeStyleName;

/***/ }),
/* 28 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


/**
 * Copyright (c) 2013-present, Facebook, Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @typechecks
 */

var _hyphenPattern = /-(.)/g;

/**
 * Camelcases a hyphenated string, for example:
 *
 *   > camelize('background-color')
 *   < "backgroundColor"
 *
 * @param {string} string
 * @return {string}
 */
function camelize(string) {
  return string.replace(_hyphenPattern, function (_, character) {
    return character.toUpperCase();
  });
}

module.exports = camelize;

/***/ })
/******/ ]);
//# sourceMappingURL=scala-js-starter-opt-bundle.js.map